# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_PATH_EQUIVALENCE_VALIDATOR_ZERO_SCIENCE_REPAIR_AND_CERTIFICATION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / zero-science validator-repair executor

Owner: final scientific authority

## 1. Authority basis

Immediate live parent at publication:

`4e5975031596d13e60e98e04b4051b10c286bf29`

The parent task authorized the isolated MATLAB call-725 same-active-input HJB100/HJB500 replay. A local execution attempt stopped **before any MATLAB scientific household/HJB/KFE call** because its external validator treated the path returned by `which('HANK_2ASSETS_HJB')` as if it had to equal the logical Junction path textually.

The observed environment instead resolves the protected source through the physical target path under `D:\MatlabProgram`, while the exact accepted storage relation is:

`C:\MatlabProgram` -> `D:\MatlabProgram`

with link type `Junction`, one target, and the protected source hashes matching on both logical and physical paths.

This is a validator-layer path-equivalence defect. It is not a model, MATLAB-source, HJB, KFE, parameter, convergence, or scientific-result failure.

No blocked execution report was published to GitHub and live `main` remained the parent task authority. This successor task establishes a new zero-science authority for repairing and certifying only the external path gate. It does **not** reopen MATLAB scientific-call budget.

## 2. Controlling accepted path authority

Read and obey:

- `AGENTS.md`;
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
- all CURRENT project rules;
- parent MATLAB replay task;
- `validators/multi_province/matlab/mp4b_beijing_household_wrapper.m`.

The repository wrapper is controlling reference evidence for path semantics. It already implements the accepted finite-root rule:

- expected logical root = `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- expected physical root = `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- verify exact `C:\MatlabProgram -> D:\MatlabProgram` Junction;
- after `addpath(logical_root,'-begin')`, accept the parent directory of `which('HANK_2ASSETS_HJB')` iff it is **exactly one of the finite pair** `{logical_root, physical_root}`;
- additionally require the resolved file SHA-256 to equal the protected HJB SHA;
- reject sibling and unrelated roots.

Current reference wrapper Git blob:

`99b25751e8dd30ee12ea6da8dca87a668bb29696`

Do not modify that repository wrapper in this task.

## 3. Exact protected-source identities

Logical protected root:

`C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Physical protected root:

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Required SHA-256:

- `HANK_2ASSETS_HJB.m`: `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m`: `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m`: `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m`: `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

These identities are read-only evidence. Do not edit protected MATLAB source.

## 4. Task type and hard boundary

Task type:

`ZERO_SCIENCE_EXTERNAL_VALIDATOR_PATH_EQUIVALENCE_REPAIR__NO_HJB_NO_KFE_NO_MODEL_CALL`

Forbidden:

- calling `HANK_2ASSETS_HJB`;
- any MATLAB HJB solve;
- any MATLAB KFE solve;
- any protected household scientific call;
- any Python HJB/KFE/model call;
- any GE/stationary/annual execution;
- R/PLM;
- shock/IRF/Results;
- changing economics, grid, calibration, `maxit`, `crit`, `Delta`, KFE semantics, or parent scientific task definitions;
- editing any repository production/model/test/validator source;
- editing protected MATLAB source;
- Java canonical-path substitution as authority;
- automatic scientific retry.

Allowed MATLAB activity is **zero-call path-resolution smoke only**: `addpath`, `which`, file hashing, environment/version reporting, and assertions that do not invoke the protected household function.

All repaired external-validator source must remain in a fresh external evidence root and must not be committed to the repository.

## 5. Root cause to repair

The rejected predicate is semantically equivalent to:

`normalize(fileparts(which('HANK_2ASSETS_HJB'))) == normalize(logical_root)`

or any rule that rejects the physical root solely because `which` returns `D:\...` rather than `C:\...`.

Replace it with the accepted finite-root predicate:

1. independently prove that `C:\MatlabProgram` is a Junction;
2. prove target count = 1;
3. prove sole target = `D:\MatlabProgram` using Windows full-path comparison;
4. require the normalized resolved parent to equal **either** the exact logical model root or exact physical model root;
5. require the resolved HJB file SHA-256 to equal `049136...C3EAE`;
6. reject every other path, including prefix/sibling/lookalike roots.

The exact intended rule is set membership, not prefix containment:

`resolved_parent in {expected_logical, expected_physical}`.

Do not use startswith/prefix logic.

## 6. Mandatory zero-science certification

Create a fresh no-overwrite external root, preferred:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-path-validator-zero-science-repair-20260904-001`

Before any real MATLAB `which` smoke, certify the repaired path predicate using deterministic string/path cases:

- exact logical root -> PASS;
- exact physical root -> PASS;
- mixed slash/case spelling that normalizes to the exact logical root -> PASS;
- mixed slash/case spelling that normalizes to the exact physical root -> PASS;
- `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK-sibling` -> FAIL;
- `D:\MatlabProgram\other-model` -> FAIL;
- child directory below protected root -> FAIL as resolved parent authority;
- parent directory `D:\MatlabProgram` -> FAIL;
- prefix lookalike -> FAIL.

Then perform exactly one **zero-science MATLAB path smoke**:

- save/restore MATLAB path;
- `addpath(logical_root,'-begin')`;
- evaluate `which('HANK_2ASSETS_HJB')` only;
- do not call the returned function;
- record returned path and parent;
- apply repaired finite-root predicate;
- compute the returned file SHA-256;
- require protected SHA match;
- verify logical and physical source path SHA-256 both match the protected SHA;
- independently verify the Junction using the accepted PowerShell finite-root method;
- record MATLAB version/release.

Scientific-call ledger must remain exactly zero for HJB/KFE/household/GE/model calls.

## 7. Required equivalence assertion

The certification report must explicitly establish:

`LOGICAL_AND_PHYSICAL_PROTECTED_ROOTS_ARE_AUTHORITY_EQUIVALENT_ONLY_AFTER_EXACT_JUNCTION_AND_SOURCE_HASH_PROOF`

This does **not** mean arbitrary symlink/Junction paths are accepted. The accepted equivalence is only the frozen finite pair:

- `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`.

If MATLAB `which` resolves to either exact root and the protected SHA matches, path identity PASS.

If it resolves outside the finite pair, STOP.

## 8. Integrity regression against parent scientific contract

Statically compare the repaired external validator with the failed external validator and prove the only semantic change is the path-membership predicate / related evidence serialization.

Require unchanged:

- protected-source hashes;
- logical/physical root strings;
- call-725 scientific state contract;
- HJB100/HJB500 definitions;
- scientific call budgets;
- `maxit` values;
- KFE semantics;
- no-retry boundary;
- MATLAB source selection;
- report/evidence requirements of the parent scientific task.

No scientific parameter change is authorized.

## 9. Evidence to persist

At minimum:

- `authority_identity.json`;
- `failed_validator_identity_or_copy.json` plus source copy if available;
- `repaired_validator_source`;
- `validator_exact_diff`;
- `finite_root_authority_contract.json`;
- `synthetic_path_membership_cases.csv`;
- `junction_evidence.json`;
- `matlab_which_zero_call_smoke.json`;
- `protected_source_hashes.json`;
- `scientific_contract_integrity_regression.json`;
- `zero_science_ledger.json`;
- stdout/stderr;
- `audit_manifest.json`.

Hash/read back every artifact.

## 10. PASS/FAIL

PASS requires all of:

- exact Junction proof PASS;
- logical and physical protected source hashes PASS;
- synthetic finite-root predicate cases PASS;
- MATLAB `which` zero-call smoke resolves to one member of the exact finite pair;
- resolved source SHA PASS;
- sibling/unrelated/prefix probes FAIL as required;
- scientific call counts all zero;
- external validator diff limited to path-equivalence handling/evidence plumbing;
- no repository or protected-source mutation.

PASS terminal:

`MP4C_2018_CALL725_MATLAB_PATH_EQUIVALENCE_VALIDATOR_ZERO_SCIENCE_REPAIRED_AND_CERTIFIED__LOGICAL_PHYSICAL_FINITE_ROOT_PAIR_ACCEPTED__READY_FOR_FRESH_MATLAB_TERMINATION_REPLAY_TASK`

FAIL terminal:

`MP4C_2018_CALL725_MATLAB_PATH_EQUIVALENCE_VALIDATOR_CERTIFICATION_FAILED__MATLAB_TERMINATION_REPLAY_REMAINS_BLOCKED__NO_SCIENCE_CALL`

## 11. Required report and publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_PATH_EQUIVALENCE_VALIDATOR_ZERO_SCIENCE_REPAIR_AND_CERTIFICATION_REPORT.md`

If PASS, only a report-only commit + non-force push is authorized.

Suggested commit:

`Certify MP4C MATLAB logical-physical path validator repair`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Do **not** automatically run HJB100/HJB500 after PASS. Return to ChatGPT L3 for a fresh scientific execution task with a new call budget.
