# CH5_TWO_ASSET_HANK_MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / read-only MATLAB-side provenance auditor

Owner: final scientific authority

## 1. Purpose

Search for and adjudicate any already-preserved MATLAB-side evidence that can sharpen the accepted Zhejiang one-extra-low-return-controller-action signature without rerunning any model.

Accepted predecessor terminal:

`MP4B_FINAL_STATE_FIRST_DIVERGENCE_AND_CONTROLLER_BRANCH_LOCALIZATION_PASS`

Accepted predecessor classification:

`MP4B_CONTROLLER_BRANCH_SIGNATURE_LOCALIZED__CHRONOLOGY_UNAVAILABLE`

Accepted marker:

`MP4B_FINAL_GOVINV_CONTROLLER_BRANCH_COUNT_DIFFERENCE_LOCALIZED`

Accepted facts to preserve:

- corrected-calendar-2009 MATLAB and Python both terminate at outer turn `184`;
- Python terminal contains full 184-turn controller history;
- preserved MATLAB final state has no already-identified turn-level provincial state trace;
- common Zhejiang initial `GovInv=486346722590.8805`;
- MATLAB final Zhejiang `GovInv=161948418.38387933`;
- Python final Zhejiang `GovInv=179942687.09319925`;
- MATLAB/Python final GovInv ratio is exactly IEEE `0.9`;
- bounded integer controller lattice identifies MATLAB `(n_low,n_high)=(76,0)` and Python `(75,0)`;
- Python history directly records 75 `LOW_RA_DECREASE_0P9` actions and zero high-return actions, with bit-for-bit reconstruction of its final GovInv;
- `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT` remains established;
- there is still no supported MATLAB/Python fault attribution and no supported chronological first-divergence turn.

This task must determine whether the preserved MATLAB run artifacts, profiler structure, run wrappers/logs, or final-state lag/carry fields contain additional branch-trigger provenance that was not yet exploited.

This is a **zero-science, read-only provenance audit**. No stationary rerun, household/HJB/KFE replay, MP2/MP3 execution, comparator replay, MATLAB invocation, another year, shocks, transition, IRF, R5, or Results is authorized.

## 2. Required predecessor and live continuity

Required execution-start predecessor:

`b0e357b7df6d56340258a712299003b13671881c`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `b0e357b7df6d56340258a712299003b13671881c`;
3. require clean worktree;
4. read `AGENTS.md`, `project_rules/PROJECT_RULE_INDEX_CURRENT.md`, `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`, and `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
5. verify every immutable repository and local-artifact identity below;
6. verify no active historical R5 / `chapter5_model` runtime dependency.

Any identity failure => BLOCKED before provenance work.

## 3. Immutable repository identities

Require exact SHA-256 and do not modify:

- `src/ch5_two_asset_hank/economics.py`: `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`: `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`: `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `one_turn.py`: `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `steady_state.py`: `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `stationary_runtime.py`: `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map: `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter: `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver: `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- canonical corrected-2009 input: `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- comparator contract: `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map: `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- remediated comparator helper: `0FD6889E10E502F32C10B3373702445333B452B25A11A63A3899184A67853EF3`
- remediated comparator helper Git blob: `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Do not modify any comparator/helper/test/scientific code in this task.

## 4. Immutable durable artifacts

### 4.1 Preserved MATLAB corrected-2009 root

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require:

- `matlab_calendar2009_stationary_output.mat`: `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`: `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`: `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`.

### 4.2 Durable Python corrected-2009 root

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require:

- `python_run_manifest.json`: `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- `python_terminal_summary.json`: `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`.

### 4.3 Aligned comparator replay

`D:\ProjectTemp\ch5-mp4b-final-state-comparator-replay-after-representation-remediation-20260831-001.json`

Require:

- bytes `106389`;
- SHA-256 `77916C2376B96D7C94CBF15A2E5DED1BCF366C4430FACCE84746794B468986AB`;
- schema `MP4B_PRESERVED_MATLAB_PYTHON_FINAL_STATE_COMPARISON_V2`.

No durable artifact may be overwritten.

## 5. Protected MATLAB source authority

Read-only inspect the protected MATLAB source through the already accepted logical/physical root pair:

- logical root: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- physical root: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

At minimum inspect:

- `HANK_mp_1eq.m`, protected SHA-256 `ED39E661AF951E01D1F5F9D123CE0FAD980F5D3DB33FD338DE60DA87731E0AEF`;
- `HANK_mp_1turn.m`, protected SHA-256 `D3D03F37286ED66202673EA63D49BABCE8D5309BAC9C13793C8E60585C21FECF`;
- `HANK_firm.m`, using the already audited protected source identity if recorded locally; if the exact source hash is not already available in controlling evidence, hash read-only and report it, but do not elevate a newly discovered copy over the protected-root provenance.

Verify the exact C/D root relationship under the existing local-file-safety rule. Do not use broad-D trust or mutate the MATLAB tree.

## 6. Source-line branch observability audit

Freeze the exact `HANK_mp_1eq.m` source lines for:

- outer convergence predicate;
- adaptation-allow predicate (`max_nk_gap < 0.1` and steady-state mode);
- Zhejiang-relevant low-return branch `ra < ramin + 0.02`;
- `GovInv *= 0.9` body line;
- high-return branch and `GovInv *= 1.1` body line;
- `Zt` reset branch/body;
- `tKNratio` damping update;
- terminal-convergence path where `_adapt`/MATLAB adjustment is not executed.

For each source line/branch state whether MATLAB profiler `ExecutedLines`, `FunctionHistory`, console output, or saved state could in principle reveal:

- condition evaluation count;
- branch-body execution count;
- province identity;
- outer-turn identity;
- chronological order;
- variable values.

Do not assume profiler semantics; verify them from the persisted profiler structure itself.

## 7. MATLAB profiler branch-count provenance

This is the highest-priority diagnostic.

Read `matlab_profile_summary.mat` directly without MATLAB execution. Inventory its top-level structure and the `FunctionTable` entry corresponding to `HANK_mp_1eq.m`.

If `ExecutedLines` or an equivalent line-level execution-count object exists:

1. map each recorded source line to the exact current protected `HANK_mp_1eq.m` line content;
2. extract execution counts for the low-return condition/body, high-return condition/body, adaptation gate/body, `Zt` reset body, damping line, and convergence/termination lines;
3. distinguish condition-line execution counts from branch-body execution counts;
4. document whether line counts are aggregate across all provinces/turns;
5. do not infer province/turn identity from an aggregate count.

Independently derive from the durable Python 184-turn history, with no model calls:

- total low-return `0.9` actions over all 31 provinces;
- total high-return `1.1` actions;
- total `Zt` adjustments;
- the same counts for Zhejiang alone;
- if faithfully comparable to a specific MATLAB source line, the total number of adaptation-eligible province-turns and terminal skipped-adaptation province-turns.

If the MATLAB profiler branch-body line count can be interpreted as the same event count and proves exactly one additional low-return action relative to Python while high-return counts are otherwise consistent, establish:

`MP4B_MATLAB_PROFILE_LOW_GOVINV_BRANCH_EXECUTION_COUNT_EXCEEDS_PYTHON_BY_ONE`

This marker is aggregate provenance only. It does not identify the province or turn by itself. Zhejiang attribution remains from the already accepted final-state lattice marker.

If profiler line data cannot support event-count interpretation, state why and do not create the marker.

## 8. Profiler chronology audit

Inspect any persisted `FunctionHistory`, call-history, or timeline object in the MATLAB profiler.

Determine exactly whether it preserves:

- invocation order of `HANK_mp_1eq` / `HANK_mp_1turn` / `HANK_firm`;
- source line sequence;
- outer-turn number;
- loop/province index;
- controller branch-body events;
- variable values.

If it contains call chronology only but no line/variable context, classify that limitation explicitly.

Do not reconstruct province or turn identity from execution timing alone.

## 9. Preserved MATLAB root inventory and hidden-evidence search

Inventory the preserved MATLAB run root recursively, read-only, recording for every file:

- relative path;
- size;
- timestamp;
- extension/type;
- SHA-256 for text/JSON/MAT/wrapper/log artifacts that are inspected.

Search specifically for already-existing:

- wrapper scripts;
- command transcripts;
- stdout/stderr captures;
- `diary` logs;
- warning logs;
- manifest files;
- per-turn snapshots;
- temporary MAT/JSON/CSV/TXT artifacts;
- profiler exports;
- branch/action counters;
- serialized `results_temp`, `tKNratio`, `k`, `iteration`, `GovInv`, `ra`, or Zhejiang-specific records.

Do not create or trigger any missing artifact.

If useful, perform a bounded filename-only search under `D:\ProjectTemp` for artifacts clearly tied to this exact corrected-2009 MATLAB run by the run-root stem, accepted artifact hashes, or explicit manifest provenance. Before reading any candidate outside the preserved root, require a direct provenance tie to this exact run. Do not browse unrelated ProjectTemp experiments as substitute evidence.

## 10. Final-state lag/carry provenance audit

Inspect all fields in preserved MATLAB `st/results` and `st/grids`, not only comparator fields.

For Zhejiang and, where useful, all provinces, identify fields that explicitly encode old/lagged or controller-carry state, including candidates such as:

- `Yt_1`;
- `Zt_1`;
- `Kt_prev` / `Kt0` or equivalent;
- `Lt_prev`;
- `pit_1`;
- any persisted `tKNratio`, previous return/wage, old `GovInv`, iteration counter, controller status, or adjustment flag.

For each field classify:

- terminal-current state;
- immediately prior-turn state;
- initialization-only state;
- bookkeeping/display-only;
- ambiguous/unresolved.

Use protected-source assignment order to justify the classification.

Determine whether the final artifact preserves enough one-step provenance to prove or exclude an extra MATLAB Zhejiang `LOW_RA_DECREASE_0P9` action in the final few turns. Do not infer a turn unless the persisted data uniquely support it.

## 11. Python near-threshold candidate signature

Using only persisted Python controller history, list all Zhejiang nonterminal turns where:

- adaptation was allowed;
- Python action was `NONE`;
- and the pre-adaptation `ra` was on the non-action side of the low-return threshold `ramin+0.02`.

For those turns report the exact IEEE/floating values of:

- `ra`;
- `ramin+0.02`;
- signed distance `ra-(ramin+0.02)`;
- `max_nk_gap`;
- `GovInv`;
- `Zt`;
- wage-bound state.

Rank these turns by the exact absolute distance to the low-return threshold, but do not choose a MATLAB divergence turn from Python proximity alone.

This ranked list is intended only to define possible future same-input adjudication candidates if the MATLAB provenance remains insufficient.

## 12. Classification

Use exactly one primary terminal classification after the audit:

### A. Preserved MATLAB trigger evidence localized

`MP4B_MATLAB_SIDE_BRANCH_TRIGGER_PROVENANCE_LOCALIZED`

Use only if an existing MATLAB artifact directly preserves a source-backed trigger event, branch count, or one-step state that materially sharpens the accepted one-extra-low-action signature.

### B. Profiler aggregate count sharpened, turn unavailable

`MP4B_MATLAB_PROFILE_BRANCH_COUNT_PROVENANCE_SHARPENED__TURN_UNAVAILABLE`

Use if profiler line counts establish source-defined branch execution counts, including a supported one-extra-low-action aggregate signature, but cannot identify province/turn chronology.

### C. Provenance exhausted without turn localization

`MP4B_MATLAB_SIDE_BRANCH_TRIGGER_PROVENANCE_EXHAUSTED_WITHOUT_TURN_LOCALIZATION`

Use if all authorized preserved artifacts and source-observability channels are exhausted and no additional MATLAB branch-trigger evidence exists beyond final-state lattice/profiler terminal counts.

### D. Blocked

`MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT_BLOCKED`

Use for identity failure, unreadable/corrupt required artifacts, or inability to safely establish provenance scope.

Do not classify MATLAB or Python as scientifically wrong in this task.

## 13. Required terminal verdict

PASS terminal:

`MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT_PASS`

PASS requires:

- all identities/provenance gates pass;
- source-line observability is frozen;
- profiler structure/count evidence is fully audited;
- preserved run-root evidence inventory is complete within scope;
- final lag/carry fields are classified;
- Python near-threshold candidate list is produced;
- one Section 12 classification is assigned;
- all scientific/model call counts remain zero.

BLOCKED terminal:

`MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT_BLOCKED`

## 14. Zero-science ledger

Must remain exactly zero:

- Python stationary;
- Python household adapter;
- HJB;
- KFE;
- household aggregation;
- MP2/MP3 scientific execution;
- qualified comparator / `compare_terminal`;
- MATLAB stationary;
- MATLAB household/HJB/KFE/presolver;
- any MATLAB function invocation whatsoever;
- Shanxi replay;
- Zhejiang scientific replay;
- other years / annual batch;
- shocks / AR1 / transition / dynamics / IRF;
- historical R5 runtime;
- Results.

Read-only parsing/hashing of existing JSON/HDF5/MAT/text/profiler artifacts is authorized.

## 15. Allowed repository changes

Allowed only:

- one bounded report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT_REPORT.md`;
- optional one compact no-science evidence JSON under `validators/evidence/` only if needed for durable text-sized provenance and if it contains no large/raw data; prefer an external no-overwrite `D:\ProjectTemp` JSON instead;
- bounded CURRENT roadmap status update only if necessary to prevent stale routing.

Do not modify scientific code, tests, comparator/helper, driver, adapter, contracts, field map, protected MATLAB, canonical data, rules, or prior reports.

## 16. Required report

Include at minimum:

1. terminal verdict and Section 12 classification;
2. live continuity and all identities;
3. complete zero-science ledger;
4. protected `HANK_mp_1eq.m` branch source-line map;
5. profiler top-level schema and relevant `FunctionTable`/`ExecutedLines` evidence;
6. exact MATLAB line execution counts and interpretation limits;
7. independently derived Python aggregate branch/action counts;
8. whether `MP4B_MATLAB_PROFILE_LOW_GOVINV_BRANCH_EXECUTION_COUNT_EXCEEDS_PYTHON_BY_ONE` is established;
9. profiler chronology/variable-observability limits;
10. preserved MATLAB run-root file inventory summary and any discovered provenance artifacts;
11. any bounded external ProjectTemp candidates inspected and their exact provenance tie;
12. complete Zhejiang final lag/carry field classification;
13. whether any final one-step state sharpens the branch location;
14. ranked Python Zhejiang near-low-threshold `NONE` turns with exact floating values;
15. material supported findings;
16. unsupported hypotheses, including any exact divergence-turn or fault attribution not proven;
17. `git diff --check`;
18. changed paths;
19. forbidden-operation audit;
20. commit SHA and non-force push;
21. GitHub read-back of every changed path;
22. final `HEAD == origin/main`, ahead/behind `0/0`, clean worktree;
23. exactly one recommended next gate.

## 17. Next-stage routing

Recommend exactly one next gate based on evidence:

- if profiler/preserved evidence identifies a bounded branch event but not enough for same-input attribution: one separately authorized **same-input, single-turn/single-province branch adjudication** using an already persisted state, without full stationary rerun;
- if no MATLAB trigger evidence survives: one Owner-reviewed decision on whether a bounded targeted MATLAB same-input replay is scientifically justified;
- if direct preserved evidence proves a source implementation inconsistency without any new execution: one L3 scientific adjudication gate before code repair;
- never proceed directly to IRF/Results from this task.

Executor must not auto-authorize its own next scientific replay.
