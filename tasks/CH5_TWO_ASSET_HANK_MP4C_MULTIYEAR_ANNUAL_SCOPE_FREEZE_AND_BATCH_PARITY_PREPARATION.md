# CH5_TWO_ASSET_HANK_MP4C_MULTIYEAR_ANNUAL_SCOPE_FREEZE_AND_BATCH_PARITY_PREPARATION

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor: Codex bounded Builder / multiyear scope auditor and batch-preparation builder

Owner: final scientific authority

## Authority basis

Required predecessor acceptance:

`MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_FORMALLY_ACCEPTED`

Required predecessor commit:

`84c26d2e61fb397e0e54621869a95c5b136963eb`

Accepted anchor level:

`CORRECTED2009_MULTI_PROVINCE_STATIONARY_PARITY_ACCEPTED__MULTIYEAR_AND_DYNAMICS_NOT_YET_ACCEPTED`

## Purpose

Prepare the next phase without consuming expensive scientific runs.

The task must determine the exact intended annual steady-state coverage from the protected MATLAB program, primary annual data, existing annual-index authority, and dissertation/project route. Do not assume a year count from chat wording.

Then freeze a compute-efficient multi-year parity plan that uses corrected-2009 as the accepted anchor and avoids repeating full forensic diagnostics for every year.

## Hard execution budget

All MATLAB/Python model, household, HJB, KFE, MP2/MP3, stationary, comparator, shock, AR(1), transition/IRF, R5 and Results executions: `0`.

Allowed: source/data metadata reads, hashing, workbook/calendar identity inspection without model execution, static code analysis, deterministic manifest/schema preparation, tests that do not invoke scientific solvers.

## Required continuity

Fresh-fetch live `main` and require this task as the direct child of `84c26d2e61fb397e0e54621869a95c5b136963eb`.

Read all current project rules and at minimum:

- formal corrected-2009 acceptance report;
- MP4A2 annual binding report and annual-index code;
- protected MATLAB top-level annual-loop sources (`main`, `main2`, `multi_prov_HANK_12sts` or exact live equivalents);
- primary workbook calendar coverage and accepted source hashes;
- current Python annual input/binding preparation code;
- current stationary driver and comparator contracts;
- current project roadmap/status documents relevant to annual coverage.

## Objectives

### A. Freeze exact annual scope

Determine and document:

- first and last economic calendar years actually intended for the Chapter 5 multi-province stationary sequence;
- exact number of annual steady states;
- workbook row mapping for each year;
- analysis index and `data_MAT`/regression-vintage mapping for each year;
- whether every year has complete primary-source inputs under the accepted calendar-decoupling contract;
- whether any year is excluded by source availability or dissertation scope.

Do not silently equate analysis index, workbook row, cache index, regression vintage or output filename year.

### B. Freeze batch execution architecture

Design a fail-isolated annual runner so every year has:

- explicit calendar identity;
- immutable canonical-input hash;
- province-order hash;
- source hashes;
- scientific code identity hashes;
- fresh no-overwrite run root;
- convergence/category summary;
- national Ct/At/Bt/Yt;
- final 31x20 field artifact/summary;
- execution ledger;
- terminal status.

Corrected-2009 must be reused as the accepted anchor and not rerun during preparation.

### C. Freeze acceptance strategy

Do not require full MATLAB/Python forensic chronology for every year.

Prepare a tiered strategy:

1. **Anchor parity**: corrected-2009 already formally accepted.
2. **Batch annual execution**: run each remaining year independently in Python under source-faithful semantics.
3. **MATLAB comparison coverage**: identify which historical MATLAB annual outputs already exist and are source-admissible; use comparator-only evidence where possible without rerunning MATLAB.
4. **Fail isolation**: only years with convergence/category/material comparison anomalies enter a dedicated diagnostic gate.
5. **No blanket reruns**: one failing year must not invalidate or rerun already accepted years.

### D. Prepare exact next execution task template

Produce a ready-to-publish execution template that can launch the annual batch with finite per-year budgets and stop/continue rules.

The template must state whether years run sequentially or in bounded chunks and must minimize compute cost. Prefer resumable per-year outputs so an interruption does not force completed years to rerun.

### E. Separate dynamics route

Document, but do not execute, the later sequence:

annual steady-state coverage -> annual acceptance -> AR(1)/shock source audit -> representative-year dynamic validation -> required paper IRFs.

Do not combine stationary batch and shocks in one scientific execution task.

## Required artifacts

Use a fresh external no-overwrite root under `D:\ProjectTemp` and persist at minimum:

- `multiyear_calendar_scope.json`;
- `annual_index_mapping.json`;
- `annual_source_availability.json`;
- `batch_manifest_schema.json`;
- `batch_acceptance_matrix.md`;
- `next_execution_task_template.md`;
- `preparation_manifest.json`.

## Required repository report

`docs/CH5_TWO_ASSET_HANK_MP4C_MULTIYEAR_ANNUAL_SCOPE_FREEZE_AND_BATCH_PARITY_PREPARATION_REPORT.md`

PASS terminal:

`MP4C_MULTIYEAR_ANNUAL_SCOPE_FREEZE_AND_BATCH_PARITY_PREPARATION_PASS`

Repository mutation: report only unless one narrowly necessary non-scientific batch-preparation validator/test is explicitly justified in the report. No scientific production code mutation.

Exactly one recommended next gate: bounded multi-year Python annual stationary batch execution, with the exact year set and compute budget frozen by this task.
