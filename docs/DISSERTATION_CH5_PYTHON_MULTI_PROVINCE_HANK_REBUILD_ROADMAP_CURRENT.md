# Chapter 5 Python 多省份两资产 HANK 路线
更新：2026-09-16。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
Results eligibility=`FALSE`。

## Accepted route

The frozen source-faithful reference and separately governed corrected successor remain distinct. Owner adopted D1 upper numerical state constraints, D2 consumed-total-drift conservative assembly, and D3 regularized-cost-consistent KKT for the corrected diagnostic target. Static implementation is accepted.

The fresh ten-cell historical derivative panel remains accepted as bounded fail-closed evidence: seven cells selected admissible policies and passed strict D2; Cells 4/8/10 returned `NO_ADMISSIBLE_POLICY`. Zero-science attribution established structural incompatibility of those historical derivative states with the corrected target and no selector omission. Repeating that same historical-derivative panel is closed.

## KFE-D2C-A — completed design / Owner seed decision

The corrected HJB one-step design/input-binding report is accepted. It froze the common call-725 grid/scalars/derivative law/policy-map/D2/HJB-step contract and returned only the seed choice to Owner.

Owner selected Option A:
`hjb100_initialization.mat:v0`, field SHA-256 `564B95B818713477691389903C3CFF72B5A7F991B924D52FBEB23D5A3675D665`.

The selection is based on provenance/path dependence: Option A is an accepted source-native numerical initialization rather than an endogenous output of the historical source-faithful boundary/operator trajectory. It is not a claim about fixed-point proximity or convergence probability.

## KFE-D2C-B — active: single corrected policy map + one direct HJB step

Active task:
`tasks/CH5_MP4C_2018_KFE_D123_CORRECTED_HJB_OPTION_A_SINGLE_POLICY_MAP_DIRECT_STEP_20260916.md`

Run at most one full 800-cell corrected policy map from Option A on the exact call-725 `(20,20,2)` F-order grid. Ceiling: <=800 selector evaluations and <=264 scalar roots. Any first failed cell stops immediately. Only after all 800 cells pass may D2 be assembled once and at most one sparse direct HJB solve be executed. Retries=0; no second map or nonlinear continuation.

The purpose is to generate the first derivative/value state from the corrected D1-D3 target rather than reuse historical MATLAB-faithful derivatives.

## KFE-D2D — later corrected KFE validation

Even a successful one-step HJB experiment does not establish nonlinear HJB convergence or stationary-density validity. Corrected KFE mass/nonnegativity/uniqueness requires a later exact task.

## KFE-D3 — later production closure

Corrected multi-province production replacement, steady state, GE/annual/dynamics/IRF and Results remain downstream gates with separate evidence and authority.

Capital/labor K1/C1/K1B/K2 routes remain deferred, not abandoned, behind the current household/KFE integration blocker.
