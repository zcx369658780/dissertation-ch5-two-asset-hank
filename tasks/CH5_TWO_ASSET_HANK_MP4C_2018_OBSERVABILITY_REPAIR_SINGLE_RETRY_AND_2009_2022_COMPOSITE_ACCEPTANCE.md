# CH5_TWO_ASSET_HANK_MP4C_2018_OBSERVABILITY_REPAIR_SINGLE_RETRY_AND_2009_2022_COMPOSITE_ACCEPTANCE

Date: 2026-09-03

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / observability engineer / controlled single-year executor

Owner: final scientific authority

## 1. Authority basis and accepted predecessor findings

Immediate predecessor execution:

`f765a733a1893c1418b8949f6128b56d8840ff43`

Accepted findings from that diagnostic gate:

1. all 13 successful Owner-A corrected Python years (`2009–2017`, `2019–2022`) passed artifact-integrity audit;
2. their corrected input identities, semantic indices, 31×20 terminal tables, `Lt_mat`, checkpoints and hashes are internally consistent;
3. protected multi-year MATLAB outputs are legacy `data_year=ii` diagnostics and are not corrected-calendar same-input parity references;
4. corrected-2009 same-input MATLAB–Python parity remains the valid cross-language numerical anchor under its own frozen input contract;
5. the 2018 first attempt exited `1`, and the scheduler mapped non-`0`/non-`2` exit to `SHARED_FAIL`;
6. the scheduler did not preserve stdout/stderr/traceback, and the worker only serializes known convergence failures;
7. strongest supported classification:

`2018_SHARED_FAIL_RUNNER_EXCEPTION_CAPTURE_DEFECT`

This is an engineering observability defect. The underlying triggering exception remains unknown. It is **not** evidence of a scientific solver failure.

## 2. Purpose of this gate

This gate has exactly three goals:

1. implement the smallest **non-scientific observability repair** needed to preserve a future subprocess failure;
2. perform exactly **one authorized 2018 scientific retry** using the same corrected Owner-A 2018 scientific input and unchanged scientific model code;
3. if that retry succeeds and all identities pass, combine the immutable 13 prior successful years with the new 2018 result to establish complete corrected Python annual stationary coverage for `2009–2022` without rerunning the other 13 years.

This task does not authorize MATLAB reruns, R/PLM work, 2023, shock/IRF, R5, Results, or any scientific algorithm modification.

## 3. Required live continuity

At start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as direct child of `f765a733a1893c1418b8949f6128b56d8840ff43`;
3. require `HEAD == origin/main`, ahead/behind `0/0`;
4. require clean tracked worktree;
5. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md` and every CURRENT rule it names;
   - predecessor 2009–2022 execution report;
   - 13-pass/MATLAB/2018 diagnostic report;
   - corrected-2009 same-input parity acceptance;
   - current Owner-A input adapter;
   - current annual production worker;
   - current scheduler/launcher;
   - focused tests.

## 4. Immutable prior scientific evidence

Preserve read-only:

`D:\ProjectTemp\ch5-mp4c-owner-a-corrected-2009-2022-8worker-20260902-001`

The following 13 year directories are accepted as immutable successful evidence and MUST NOT be rerun or overwritten:

`2009–2017`, `2019–2022`.

The failed first-attempt `year_2018` directory is immutable historical failure evidence. Do not modify, repair, delete, rename, or reuse it as the retry output root.

The predecessor diagnostic evidence root is also read-only:

`D:\ProjectTemp\ch5-mp4c-13pass-matlab-comparator-2018-diagnostic-20260903-001`

## 5. Frozen 2018 scientific input contract

Before any retry, identify the exact 2018 corrected runtime input used in the failed batch and freeze:

- input file path;
- SHA-256;
- representation;
- `steady_state_calendar_year = 2018`;
- `rolling_window_entry_index = 10`;
- `regression_vintage_index = 19`;
- `calendar_level_row_index = 19`;
- rolling window `2009–2018`;
- capital authority `R语言计算资本存量`;
- GDP scaling ×1000;
- CAP scaling ×1000;
- POP scaling ×100;
- exact 31-province order;
- exact runtime cache identity;
- exact source workbook identities;
- `no_2023_scientific_input = true`.

The retry MUST consume byte-identical scientific input unless a purely serialization wrapper path requires a byte-for-byte copied file in the fresh retry root. If copied, source and copy hashes must match exactly.

Any scientific input difference from the failed attempt is a STOP condition.

## 6. Scientific-code immutability

No changes are permitted to:

- Owner-A input values;
- household equations;
- HJB/KFE;
- transfer FOC;
- adjustment costs;
- grids;
- calibration;
- migration;
- one-turn order;
- firm/wage/monetary/fiscal blocks;
- controller semantics;
- convergence thresholds;
- max outer turns;
- max household calls;
- model initialization;
- checkpoint scientific content.

Before the retry, record scientific-code identities and prove that any changed file is observability/orchestration only.

## 7. Phase A — minimal observability repair — ZERO SCIENCE

Exact scientific calls in Phase A:

- Python stationary: `0`;
- household/HJB/KFE: `0`;
- MATLAB: `0`;
- R/PLM: `0`;
- comparator: `0`;
- shock/IRF/R5/Results: `0`.

### 7.1 Required observability behavior

Implement the smallest repair so a subprocess that exits nonzero preserves at minimum:

- stdout;
- stderr;
- Python traceback if emitted;
- exact command;
- exact environment relevant to threads;
- process exit code;
- launch/start/end timestamps;
- stage/classification receipt;
- exact runtime-input SHA;
- scientific-code identities.

Preferred implementation boundary:

- scheduler/retry harness only;
- do not broadly catch exceptions inside scientific model logic unless strictly necessary for logging and unless re-raising preserves original exit semantics.

A subprocess traceback naturally emitted to stderr is sufficient if stderr is durably captured.

### 7.2 Exit classification

Do not silently call every non-`2` exit a shared scientific failure.

For the bounded retry, distinguish at minimum:

- exit `0`: `PASS`;
- exit `2` with normal scientific `FAILURE.json`: `SCIENTIFIC_NONCONVERGENCE_FAIL`;
- any other nonzero: `PROCESS_EXCEPTION_FAIL`, with preserved stderr/traceback;
- missing process launch / OS failure: `INFRASTRUCTURE_FAIL`.

Do not change the production worker's scientific return semantics.

### 7.3 Zero-science tests

Add focused tests proving:

- stdout capture;
- stderr capture;
- traceback capture using a dummy failing subprocess, not the HANK model;
- correct exit classification;
- failure receipt serialization;
- successful subprocess path remains unchanged;
- thread environment remains pinned to `1`;
- no scientific function is invoked by observability tests;
- 2018 retry harness accepts exactly one year, `2018`, and rejects all others;
- no 2023 scope.

Run `py_compile` on changed/new Python files.

Phase-A PASS marker:

`MP4C_2018_OBSERVABILITY_REPAIR_IMPLEMENTED_AND_ZERO_SCIENCE_TESTED__SINGLE_RETRY_AUTHORIZED`

## 8. Fresh retry root and no-overwrite rule

Use a fresh no-overwrite root, preferred:

`D:\ProjectTemp\ch5-mp4c-owner-a-2018-observable-single-retry-20260903-001`

If that exact path already exists, choose the next unused suffix. Never overwrite an existing root.

The retry root must contain its own:

- copied/linked canonical 2018 input with exact SHA identity;
- run manifest;
- observability receipt;
- stdout log;
- stderr log;
- process receipt;
- success/failure scientific outputs if produced.

## 9. Phase B — exactly one 2018 scientific retry

If and only if Phase A PASS and all input/scientific-code identity gates pass, execute exactly **one** retry of year `2018`.

Use:

- year: `2018` only;
- subprocess count: `1`;
- year-level workers: `1` for this bounded single-year retry;
- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`;
- max outer turns `250`;
- max household calls `7750`;
- automatic reruns `0`;
- no default wall-clock kill.

This single-year `Workers=1` execution is an orchestration choice for isolation/diagnosis only and does not change scientific inputs or algorithms.

Do NOT execute any other year.

Do NOT automatically retry 2018 a second time under any outcome.

## 10. Retry outcomes

### R1 — 2018 succeeds

Require:

- process exit `0`;
- `SOURCE_CONVERGED`;
- exact 2018 corrected input SHA;
- exact scientific-code identities;
- 31/31 household/output contract;
- 31×20 finite terminal fields;
- valid `Lt_mat` 31×31;
- valid restart/checkpoint artifacts;
- exact output hashes;
- no automatic rerun;
- no scientific parameter mutation.

Classify:

`MP4C_2018_OWNER_A_CORRECTED_SINGLE_RETRY_PASS__FIRST_FAILURE_WAS_NONREPRODUCED_PROCESS_EXCEPTION_WITH_REPAIRED_OBSERVABILITY`

Do not assert the exact original trigger unless evidence supports it. A successful retry only establishes that the first lost exception did not recur under the bounded isolated retry.

### R2 — 2018 exits `2` with scientific failure artifact

Classify:

`MP4C_2018_OWNER_A_CORRECTED_SINGLE_RETRY_SCIENTIFIC_NONCONVERGENCE_FAIL__NO_SECOND_RETRY`

Persist the scientific failure artifact and STOP.

### R3 — 2018 exits other nonzero with traceback/logs

Classify according to the preserved exception:

`MP4C_2018_OWNER_A_CORRECTED_SINGLE_RETRY_PROCESS_EXCEPTION_FAIL__ROOT_CAUSE_CAPTURED__NO_SECOND_RETRY`

Then provide the exact root-cause class and minimal next action. STOP.

### R4 — infrastructure failure before/around launch

Classify:

`MP4C_2018_OWNER_A_CORRECTED_SINGLE_RETRY_INFRASTRUCTURE_FAIL__NO_SECOND_RETRY`

STOP.

## 11. Phase C — composite 2009–2022 acceptance if and only if R1

Do not rerun the 13 previously successful years.

Read-only combine:

- immutable original PASS evidence for `2009–2017` and `2019–2022`;
- new successful 2018 retry evidence.

Build a composite 14-year acceptance manifest covering exactly:

`2009–2022` inclusive.

For every year verify:

- `SOURCE_CONVERGED`;
- exact Owner-A corrected input identity;
- correct semantic indices;
- 31×20 finite terminal table;
- valid `Lt_mat`;
- valid checkpoint/restart artifacts;
- exact hashes;
- no 2023 output;
- no scientific code mutation across the evidence set except observability/orchestration files outside the scientific model identity set.

The composite manifest MUST record provenance per year so it is transparent that 13 years come from the original eight-worker batch and 2018 comes from the bounded single-year retry.

Required composite artifacts in a fresh read-only aggregation/evidence root:

- `composite_2009_2022_year_provenance.csv`;
- `composite_2009_2022_integrity_audit.json`;
- `composite_2009_2022_acceptance_manifest.json`;
- `2018_retry_vs_2017_2019_diagnostic.csv`;
- `observability_repair_test_receipt.json`;
- `retry_2018_execution_receipt.json`;
- `audit_manifest.json`.

Do not fabricate a single historical batch manifest claiming all 14 years were produced in one run.

## 12. Cross-year 2018 sanity diagnostic after R1

Comparator-only/read-only checks may compare the successful 2018 output with 2017 and 2019 for:

- outer turns;
- household calls;
- runtime;
- national aggregates;
- `ra`, `rah`, `rb`;
- wage/tax/controller quantities;
- field extrema;
- province outliers.

This is a diagnostic sanity check, not a continuity restriction. Do not reject a valid 2018 solution merely because economic data produce a genuine year-to-year change.

## 13. MATLAB boundary

No MATLAB run is authorized.

The existing multi-year MATLAB outputs remain:

`LEGACY_CALENDAR_BINDING_DIAGNOSTIC_ONLY`.

Do not claim strict 2010–2022 MATLAB–Python parity.

The corrected-2009 same-input MATLAB–Python parity remains the accepted cross-language numerical anchor.

## 14. Acceptance boundary

If and only if R1 and Phase-C composite audit pass, terminal:

`MP4C_OWNER_A_2009_2022_CORRECTED_ANNUAL_STATIONARY_COMPOSITE_COVERAGE_PASS`

Recommended acceptance level:

`OWNER_A_2009_2022_CORRECTED_ANNUAL_STATIONARY_PYTHON_AUTHORITY_ACCEPTED__2009_CROSS_LANGUAGE_PARITY_RETAINED__2018_RECOVERED_BY_SINGLE_OBSERVABLE_RETRY__PLM_ESTIMATOR_REPRODUCIBILITY_AND_2023_DATA_EXTENSION_DEFERRED__SHOCK_NUMERICS_NOT_YET_ACCEPTED`

This does not authorize numerical shock/IRF work in the same task.

If R2/R3/R4 occurs, no 14-year acceptance is allowed.

## 15. External evidence root

Use fresh no-overwrite root, preferred:

`D:\ProjectTemp\ch5-mp4c-2018-observability-single-retry-and-composite-acceptance-20260903-001`

Persist at minimum:

- `original_2018_failed_input_identity.json`;
- `scientific_code_identity_before_retry.json`;
- `observability_repair_contract.json`;
- `observability_repair_test_receipt.json`;
- `retry_2018_command_and_environment.json`;
- `retry_2018_stdout.log`;
- `retry_2018_stderr.log`;
- `retry_2018_execution_receipt.json`;
- `retry_2018_failure_root_cause.json` if non-PASS;
- composite artifacts from Section 11 if PASS;
- `zero_or_bounded_science_ledger.json`;
- `audit_manifest.json`.

## 16. Git boundary and report

Do not commit generated MAT/NPZ/XLSX/CSV scientific outputs or local data.

One bounded final commit/push may include only:

- observability/retry harness code;
- focused tests;
- the repository report.

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_OBSERVABILITY_REPAIR_SINGLE_RETRY_AND_2009_2022_COMPOSITE_ACCEPTANCE_REPORT.md`

## 17. Final STOP rules

- exactly one scientific retry of 2018 maximum;
- no automatic second retry;
- no rerun of the other 13 years;
- no scientific parameter changes;
- no MATLAB/R/PLM/shock/IRF/R5/Results;
- no 2023;
- no claim of strict multi-year MATLAB parity without same-input evidence.
