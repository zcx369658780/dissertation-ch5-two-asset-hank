# CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_AFTER_HELPER_REMEDIATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded MATLAB scalar-source diagnostician

Owner: final scientific authority

## 1. Purpose

Execute the now fully remediated validation-only MATLAB `HANK3_FOC` ten-case raw-`pb` edge-semantics freeze.

This task follows the accepted infrastructure-only terminal:

`MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION_PASS`

and acceptance marker:

`MP4B_RAW_VB_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_REMEDIATION_PASS`

The exact-junction path guard has already passed its independent smoke gate, and the edge helper has now been remediated to own a fresh run root, durably persist a first protected-source failure, bind every successful row to the resolved protected source identity, and record a complete explicit call ledger.

This task authorizes exactly one replacement validation-only MATLAB scalar batch over the already frozen ten cases.

It does **not** authorize any Python production-oracle repair, local-policy parity, HJB, KFE, household solution, multi-province stationary execution, MP2/MP3 empirical execution, annual batch, shock, transition, dynamics, IRF, legacy R5, or Results work.

## 2. Controlling authority and required reads

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_ONLY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION_REPORT.md`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_path_equivalence_smoke.m`

Preserve accepted evidence unchanged:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`
- `MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_STATIC_REVIEW_PASS`
- `MP4B_RAW_VB_HANK3_FOC_EXACT_JUNCTION_GUARD_SMOKE_PASS`
- `MP4B_RAW_VB_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_REMEDIATION_PASS`

Primary numerical authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent:

`de25cf6a03da2c3949f4e10245204e871e937fd0`

At execution start Codex MUST:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the expected parent;
3. require a clean worktree;
4. verify the remediated helper and all preserved smoke/source identities below;
5. verify production/export identities remain unchanged;
6. verify no historical R5 / `chapter5_model` runtime dependency.

Any continuity or identity failure => stop before consuming the scalar batch.

## 4. Frozen identities

### 4.1 Remediated helper identity

The scalar helper MUST be byte-identical to the accepted remediation output:

`validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`

Required SHA-256:

`F98FC7D1AADA01A39693951F4CD266A7174727975FBBFE55E54343867D7E11E0`

Expected public signature:

`mp4b_raw_vb_hank3_foc_edge_diagnostic(run_root, logical_root, physical_root)`

Do not modify this helper in this task.

### 4.2 Exact-junction evidence

Required smoke helper SHA-256:

`8F3D7E87CDFA63510505042F938286DC58BAA4F734253C74520AF91742BB601E`

Required preserved smoke manifest SHA-256:

`A82DC905E7D057EBE0645E3C8F3331F438CF939716762B17AC5C9B270B758D8B`

Required smoke marker:

`MP4B_RAW_VB_HANK3_FOC_EXACT_JUNCTION_GUARD_SMOKE_PASS`

Do not rerun the exact-junction smoke.

The protected roots remain exactly:

- logical: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- physical: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

`C:\MatlabProgram` must still independently resolve as a Windows Junction with exactly one target `D:\MatlabProgram`.

### 4.3 Protected MATLAB source identities

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

### 4.4 Production/export identities before execution

Rehash and record the current accepted production/export identities. At minimum verify that the predecessor accepted bytes have not been scientifically mutated since the remediation task. No production/export path may be changed by this task.

## 5. Exact ten-case scientific freeze

The helper's ten cases are already frozen. Their order, ids, `pa`, `pb`, `a`, `chi0`, `chi1`, classification logic and source call expression MUST NOT be changed, reordered, replaced, tuned, expanded or normalized after seeing output.

Ordered case ids:

1. `localized_BB`
2. `localized_BF`
3. `localized_FB`
4. `localized_FF`
5. `positive_pb`
6. `negative_pb`
7. `zero_pb_positive_pa`
8. `zero_pb_negative_pa`
9. `zero_pa_zero_pb`
10. `zero_a_negative_pb`

The localized Beijing iteration-5 witness remains:

- raw `VaB = 0.0183013418028827`
- raw `VaF = 0.029712870660726632`
- raw `VbB = 0.0036470322698923963`
- raw `VbF = -0.014003744365506235`
- `a = 9.473684210526315`
- `chi0 = 0.1`
- `chi1 = 2`

The exact protected call expression MUST remain:

`value = HANK3_FOC(dummy,chi,pa(k),pb(k),a(k),0);`

Forbidden:

- epsilon insertion;
- clipping;
- positivity guards;
- alternate transfer formula;
- Python emulation;
- catch-and-substitute numerical result;
- case removal/addition/reordering;
- post-output tolerance or semantic adjustment.

## 6. Mandatory preflight before scalar execution

Before creating the scientific run root, verify:

- remediated helper SHA exactly matches `F98FC7D1AADA01A39693951F4CD266A7174727975FBBFE55E54343867D7E11E0`;
- preserved smoke helper and manifest hashes match exactly;
- exact C/D Junction evidence still holds;
- logical/physical/resolved `HANK3_FOC.m` all match the protected SHA;
- no `canonical_root`, `getCanonicalPath`, `startsWith`, substring matcher, broad-D-root trust, arbitrary sibling trust or filename-only trust exists;
- helper still owns direct-child `D:\ProjectTemp` run-root exclusive creation;
- `failure.json` first-error persistence contract remains present;
- every success row contains `resolved_helper_path` and `resolved_helper_sha256`;
- success/failure evidence both use the complete explicit call-ledger schema;
- exact ten-case scientific content and protected call expression remain unchanged;
- MATLAB R2022b `checkcode(...,'-id')` on the helper returns zero findings;
- focused evidence-contract and exact-junction tests pass;
- `git diff --check` passes before execution.

Required preflight marker:

`MP4B_RAW_VB_HANK3_FOC_REPLACEMENT_SCALAR_PREFLIGHT_PASS`

If any preflight check fails, do not create the run root and do not consume the scalar batch.

## 7. Authorized scientific execution budget

After and only after preflight PASS:

- replacement MATLAB scalar batch: exactly `1` maximum;
- reruns: `0`;
- protected `HANK3_FOC` attempted calls: up to `10`;
- protected `HANK3_FOC` completed calls on full success: exactly `10`.

Do not rerun after any output or error.

All other computational/scientific/model calls MUST remain exactly zero:

- exact-junction smoke rerun: `0`;
- MATLAB HJB: `0`;
- MATLAB KFE: `0`;
- MATLAB household / `HANK_2ASSETS_HJB`: `0`;
- MATLAB multi-province: `0`;
- MATLAB stationary / GE: `0`;
- Python local-policy: `0`;
- Python HJB/KFE/household/stationary: all `0`;
- old 50-state HJB parity: `0`;
- Beijing household parity: `0`;
- MP2/MP3 empirical: `0/0`;
- annual batch: `0`;
- shocks/transition/dynamics/IRF/R5/Results: all `0`.

## 8. Fresh run-root contract

Use one caller-selected fresh direct child of `D:\ProjectTemp`.

The helper itself MUST atomically/exclusively create that exact run root before any protected `HANK3_FOC` call.

Do not pre-create the directory externally.

Do not reuse, delete, truncate or repurpose an existing path.

All helper artifacts must remain inside that owned root.

Record the exact run root in the report.

## 9. Success semantics

If all ten protected calls complete, the helper must persist:

`success_manifest.json`

inside the owned run root.

The manifest must durably contain all ten typed rows and the complete explicit ledger.

For every successful case record at minimum:

- case id;
- `pa`, `pb`, `a`, `chi0`, `chi1`;
- `resolved_helper_path`;
- `resolved_helper_sha256`;
- MATLAB `pa./pb` classification: `finite`, `+Inf`, `-Inf`, or `NaN`;
- finite ratio encoding/value where applicable;
- protected `HANK3_FOC` output classification: `finite`, `+Inf`, `-Inf`, or `NaN`;
- finite output encoding/value where applicable.

`Inf` or `NaN` output is source evidence and is NOT automatically a task failure.

PASS requires:

- scalar preflight marker established;
- one scalar batch only;
- all ten attempted calls complete;
- `HANK3_FOC_attempted_calls == 10`;
- `HANK3_FOC_completed_calls == 10`;
- success manifest persists durably;
- per-row source identity matches the verified protected source;
- every prohibited-route ledger entry remains exactly zero;
- production/export mutation remains zero.

On PASS establish:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`

Terminal PASS:

`MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_PASS`

## 10. Protected-source error semantics

If protected MATLAB `HANK3_FOC` errors on any frozen case, the helper must:

1. persist `failure.json` inside the owned run root before termination;
2. record the exact first failing case id and one-based case index;
3. record that case's `pa`, `pb`, `a`, `chi0`, `chi1`;
4. preserve the original MATLAB error identifier and message exactly;
5. record resolved protected helper path/SHA and exact roots;
6. record `HANK3_FOC_attempted_calls` and `HANK3_FOC_completed_calls`;
7. record the complete explicit zero-call ledger;
8. rethrow the original protected error;
9. stop immediately; do not continue to later cases;
10. do not synthesize, clip, replace or reinterpret a numerical result.

If this occurs, preserve the failure artifact and terminate fail-closed. Do not repair/retry in this task.

Terminal classification should be:

`MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_BLOCKED`

unless the exact source evidence uniquely supports a more specific source-semantics terminal already defined by controlling authority. Do not invent one after output.

## 11. Repository mutation boundary

Preferred repository mutation is report-only.

Allowed:

- one bounded execution ledger if strictly needed and non-scientific;
- bounded CURRENT roadmap status update if necessary;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_AFTER_HELPER_REMEDIATION_REPORT.md`

Do NOT modify:

- the remediated scalar helper;
- the PASSed exact-junction smoke helper;
- `src/ch5_two_asset_hank/economics.py`;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`;
- faithful HJB/operator/KFE modules;
- corrected/reference modules;
- `exports/matlab_faithful_two_asset_ha.py`;
- protected MATLAB source;
- canonical input/data/cache;
- MP2/MP3;
- historical R5.

Production/export mutation must remain exactly `0`.

If the remediated helper still has an implementation defect before execution, stop BLOCKED and publish evidence. Do not repair it in this task.

## 12. Required report

Report at minimum:

- terminal verdict;
- live GitHub continuity;
- helper/source/smoke identities;
- scalar preflight marker and checks;
- exact fresh run root;
- success manifest SHA or failure artifact SHA;
- complete ten-case table on full success, or exact first-error evidence on protected-source failure;
- exact attempted/completed protected call counts;
- per-row protected-source identity audit on success;
- complete zero-call model ledger;
- production/export mutation `0`;
- no helper mutation after preflight;
- MATLAB checkcode / focused tests / `git diff --check`;
- forbidden-operation check;
- Git closeout;
- exactly one recommended next gate.

## 13. Git closeout

Explicit-path staging only.

One execution commit.

One non-force push.

GitHub read-back every changed path.

Require:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree.

## 14. Successor boundary

On PASS recommend exactly one successor:

**resume the faithful-only raw-`Vb` source-order repair using a dedicated faithful transfer helper, then revalidate affected local-policy, HJB and standalone Beijing-household parity while empirical 2009 stationary remains closed.**

That successor must reuse the frozen ten-case MATLAB artifact from this task and MUST NOT rerun this scalar batch.

On BLOCKED, recommend only the minimum evidence/infrastructure gate required by the exact blocker.