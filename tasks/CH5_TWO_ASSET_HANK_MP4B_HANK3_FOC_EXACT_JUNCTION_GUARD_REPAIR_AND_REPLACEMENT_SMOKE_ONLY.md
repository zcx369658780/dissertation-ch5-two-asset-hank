# CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_ONLY

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded validation-helper repairer / infrastructure-only smoke executor

Owner: final scientific authority

## 1. Purpose

Resolve only the remaining validation-infrastructure blocker from:

`MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_REPLACEMENT_BLOCKED`

The predecessor task correctly established the exact protected logical/physical root pair and independently verified the Windows junction relation:

- logical storage root: `C:\MatlabProgram`
- physical storage root: `D:\MatlabProgram`
- exact logical model root: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- exact physical model root: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- `C:\MatlabProgram` is a Junction with the sole target `D:\MatlabProgram`
- logical and physical `HANK3_FOC.m` both hash to `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`

The predecessor guard-only smoke then failed because the validation helpers additionally required Java `java.io.File(...).getCanonicalPath()` to dereference the Windows junction. In the actual MATLAB R2022b / Windows environment that Java canonicalization did not prove the already independently established junction relation.

This task authorizes only:

1. removal of that non-authoritative Java canonical-root equivalence assertion from the two active raw-`Vb` `HANK3_FOC` validation helpers;
2. preservation/strengthening of the already source-backed finite exact-pair guard using explicit roots, PowerShell junction evidence, exact file hashes, and exact resolved-parent membership;
3. exactly one new guard-only MATLAB smoke.

This task **does not authorize the replacement scalar `HANK3_FOC` batch**, any production-oracle repair, any local-policy parity, HJB, KFE, household solve, multi-province execution, or Results.

A smoke PASS here only makes a later, separately published scalar-edge task eligible.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B tasks/reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_PATH_EQUIVALENCE_REPAIR_AND_REPLACEMENT_FREEZE_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`
- `validators/multi_province/matlab/mp4b_path_equivalence_smoke.m`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_path_equivalence_smoke.m`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`

Preserve accepted scientific evidence unchanged:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- raw-`Vb` root-cause classification `PYTHON_IMPLEMENTATION_ERROR__NONSOURCE_PRE_FLOOR_LIQUID_DERIVATIVE_POSITIVITY_GUARD`
- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`

The raw-`pb` edge semantics marker is still **not** established and must remain so in this task:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN` = NOT AUTHORIZED / NOT REACHED.

## 3. Live continuity

Expected execution-start parent:

`09631a581611cc2b209b1bf8dd676299f82f327b`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of `09631a581611cc2b209b1bf8dd676299f82f327b`;
3. require clean worktree and `HEAD == origin/main`;
4. verify the predecessor BLOCKED report and both active raw-`Vb` validation helpers exist;
5. verify protected MATLAB source hashes;
6. verify production/export hashes remain unchanged;
7. verify no historical R5 / `chapter5_model` runtime dependency.

Any continuity failure => stop before mutation.

## 4. Protected identities

Required protected MATLAB hashes:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Required current production identities at task start:

- `src/ch5_two_asset_hank/economics.py` `66E3C56F177DB6DAFE7FE0A5FD6DA480D71A7ACC10B5209BC0E3F7360226DC55`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- accepted standalone `exports/matlab_faithful_two_asset_ha.py` `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

No production identity may change in this task.

## 5. Exact blocker adjudication

Freeze the predecessor failure as validation infrastructure only:

`MP4B_HANK3_FOC_JAVA_CANONICALPATH_IS_NOT_JUNCTION_AUTHORITY`

The following evidence is authoritative for this bounded path guard:

1. exact input root equality to the two frozen model-root strings after case/slash/trailing-separator normalization;
2. independent PowerShell `Get-Item -Force C:\MatlabProgram` proof that:
   - `LinkType == Junction`;
   - there is exactly one target;
   - that target normalizes exactly to `D:\MatlabProgram`;
3. both exact logical and physical model roots exist;
4. both logical and physical `HANK3_FOC.m` files exist and hash exactly to the protected SHA;
5. after adding only the exact logical model root, `which('HANK3_FOC')` resolves to a file whose parent is exactly one member of the finite two-root set and whose file hash equals the protected SHA;
6. synthetic sibling/unrelated root probes are rejected.

Java `getCanonicalPath()` is **not** an additional required authority because it did not dereference the verified junction in this environment.

The task must not replace it with another broad path heuristic.

## 6. Authorized helper repair

Authorized files:

- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_path_equivalence_smoke.m`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`

Repair both helpers now so that the later scalar task does not inherit the same known guard defect.

Required change:

- remove `canonical_root(...)` / `java.io.File(...).getCanonicalPath()` as a required logical-to-physical equivalence assertion;
- retain exact frozen root-string checks;
- retain the exact PowerShell junction proof;
- retain existence and SHA checks for logical source, physical source, and resolved source;
- retain finite two-root membership for the resolved parent;
- retain unrelated/sibling rejection;
- retain R2022b-compatible atomic no-overwrite persistence;
- retain zero model-call design of the smoke.

Do **not**:

- use `startsWith`;
- use substring/contains matching;
- trust all of `D:\MatlabProgram`;
- permit arbitrary siblings;
- compare only file names;
- drop the junction check;
- drop source hashes;
- alter the ten frozen scalar case ids/values/formulas/call expression;
- call `HANK3_FOC` in this task.

Required static marker:

`MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_STATIC_REVIEW_PASS`

Static review must explicitly state that no Java canonical-root assertion remains in either active raw-`Vb` helper.

## 7. Focused negative guard probes before MATLAB smoke

Before consuming the one MATLAB smoke budget, use static/unit-level validation to prove the guard rejects at least:

- logical root replaced by the physical root in the logical-root argument;
- physical root replaced by the logical root in the physical-root argument;
- a sibling such as `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK-sibling`;
- another directory under `D:\MatlabProgram`;
- a helper with wrong SHA;
- multiple or wrong junction targets in the junction-evidence parser/contract.

These checks must not call protected MATLAB model/scientific functions.

## 8. One replacement guard-only smoke

After static review PASS, run exactly one fresh MATLAB guard-only smoke using:

`mp4b_raw_vb_hank3_foc_path_equivalence_smoke`

with the exact logical and physical roots above and a fresh no-overwrite `D:\ProjectTemp\...` root.

Smoke budget:

- authorized: `1`
- reruns: `0`

The smoke must call `HANK3_FOC` exactly `0` times.

PASS requires persisted evidence of:

- exact logical root;
- exact physical root;
- exact verified junction type/sole target;
- logical-source SHA;
- physical-source SHA;
- `which` resolved path;
- resolved-source SHA;
- exact finite-root membership PASS;
- sibling/unrelated rejection PASS;
- `HANK3_FOC_calls = 0`;
- all HJB/KFE/household/multi-province calls = 0.

Required marker:

`MP4B_RAW_VB_HANK3_FOC_EXACT_JUNCTION_GUARD_SMOKE_PASS`

If smoke fails for any reason:

- stop immediately;
- do not repair and rerun;
- do not invoke scalar diagnostic;
- publish BLOCKED evidence only.

## 9. Scalar diagnostic remains closed

The predecessor scalar batch was conditional on smoke PASS and was never consumed, but the old task authority is closed.

This task does **not** inherit or reactivate that budget.

Required ledger in this task:

- replacement `HANK3_FOC` scalar diagnostic batches: `0`
- protected `HANK3_FOC` calls: `0`

Even after smoke PASS, stop and close out. A new live task is required to authorize the scalar edge batch.

## 10. Complete call budget

Must remain exactly:

- guard-only MATLAB smoke: at most `1`
- protected `HANK3_FOC`: `0`
- MATLAB HJB: `0`
- MATLAB KFE: `0`
- MATLAB household: `0`
- MATLAB multi-province/stationary: `0`
- Python local-policy scientific batch: `0`
- Python HJB: `0`
- Python KFE: `0`
- Python household: `0`
- Python stationary: `0`
- old 50-state HJB parity: `0`
- Beijing household parity: `0`
- MP2/MP3 empirical: `0`
- annual batch/shocks/transition/dynamics/IRF/R5/Results: `0`

Production/export mutation: `0`.

## 11. Allowed repository changes

Allowed only:

- the two validation-only MATLAB helpers listed in Section 6;
- focused static/unit tests for the exact-junction guard, if needed;
- bounded CURRENT roadmap update;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_ONLY_REPORT.md`

Do not modify:

- production household/HJB/KFE code;
- `exports/matlab_faithful_two_asset_ha.py`;
- corrected/reference code;
- MP2/MP3;
- `stationary_runtime.py`;
- canonical input/data/cache;
- preserved MATLAB 2009 baseline;
- protected MATLAB source;
- project rules;
- historical R5.

## 12. Required report

Include:

1. terminal verdict;
2. live authority/parent continuity;
3. exact protected identities;
4. predecessor blocker adjudication;
5. before/after helper hashes;
6. exact removal of Java canonical-root assertion;
7. retained finite-root/junction/SHA protections;
8. negative guard probes;
9. MATLAB `checkcode` results;
10. smoke command/root;
11. smoke manifest path/SHA;
12. exact resolved `HANK3_FOC` path and hash;
13. smoke call ledger showing `HANK3_FOC=0`;
14. complete scientific/model zero-call ledger;
15. forbidden-operation check;
16. Git closeout;
17. exactly one recommended next gate.

## 13. Terminal verdicts

PASS:

`MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_PASS`

Requires both:

- `MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_STATIC_REVIEW_PASS`
- `MP4B_RAW_VB_HANK3_FOC_EXACT_JUNCTION_GUARD_SMOKE_PASS`

BLOCKED:

`MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_BLOCKED`

A PASS is infrastructure-only. It does not freeze raw-`pb` `HANK3_FOC` semantics and does not authorize production repair.

## 14. Exactly one successor boundary

On PASS recommend exactly:

**one separately authorized replacement validation-only `HANK3_FOC` ten-case scalar edge diagnostic using the now smoke-validated exact-junction guard, with all HJB/KFE/household/stationary/model calls and production mutations kept at zero.**

On BLOCKED recommend only the smallest remaining path/persistence infrastructure fix.

## 15. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.
