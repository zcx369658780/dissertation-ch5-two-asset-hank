# CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_ECONOMICS_IDENTITY_ADJUDICATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / MATLAB-source parity executor

Owner: final scientific authority

## 1. Purpose

Resume the previously authorized faithful-only raw-`Vb` source-order repair and household-authority revalidation after resolving the predecessor identity blocker.

Predecessor terminal:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_BLOCKED`

The predecessor stopped before every scientific gate because its task prose required a stale `economics.py` SHA-256 (`66E3...6DC55`) that does not identify the current live Git blob. No scientific budget was consumed, all provisional edits were rolled back, and production/export mutation closed at zero.

This successor corrects only that authority identity and otherwise preserves the prior scientific route, source semantics, one-shot budgets, rollback discipline, and 2009-stationary closure.

## 2. Corrected predecessor identity adjudication

For this task, the authoritative predecessor bytes for:

`src/ch5_two_asset_hank/economics.py`

are the bytes stored in live Git at execution start.

Expected current identities from parent commit `06363ecd338f56d0f03fc58c0b3e2dda5ab81d68`:

- Git contents/blob object id returned by GitHub: `d6611d7cca702dcaaf1371c44e6179c7f88f5318`
- SHA-256 of the exact Git-blob file bytes: `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`

The standard CRLF materialization SHA-256:

`E8A24DA690D990CBF24AACE15ACFD58A059086AF85A24EA27F8E09E54868585A`

is diagnostic only and is **not** a repository scientific-authority identity.

The historical prose value:

`66E3C56F177DB6DAFE7FE0A5FD6DA480D71A7ACC10B5209BC0E3F7360226DC55`

is superseded for current-predecessor identity checking. Do not require, recreate, or normalize the current file to that stale value.

Required marker before production mutation:

`MP4B_ECONOMICS_PREDECESSOR_GIT_BLOB_IDENTITY_ADJUDICATED_PASS`

This adjudication does not authorize any scientific formula change by itself.

## 3. Controlling evidence

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all accepted faithful household/HJB/KFE/standalone reports
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- predecessor BLOCKED report

Preserve:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- classification `PYTHON_IMPLEMENTATION_ERROR__NONSOURCE_PRE_FLOOR_LIQUID_DERIVATIVE_POSITIVITY_GUARD`
- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`
- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`

Frozen ten-case MATLAB manifest:

`D:\ProjectTemp\mp4b_hank3_foc_10case_bcb5db94-c4a2-4d89-9485-6cbb20798126\success_manifest.json`

SHA-256:

`06A31A509EF696094563CD41C9928A3E83AFB076675B09155CC82B2200E7E74E`

Do not rerun that scalar batch.

Protected MATLAB source hashes remain:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Other predecessor production identities expected unchanged before mutation:

- `src/ch5_two_asset_hank/matlab_faithful_policy.py` SHA-256 `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py` SHA-256 `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

## 4. Live continuity and no-science preflight

Expected execution-start parent:

`06363ecd338f56d0f03fc58c0b3e2dda5ab81d68`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live as a direct child of the predecessor BLOCKED commit;
3. require clean worktree;
4. verify the corrected `economics.py` Git object id and exact blob-byte SHA-256 above;
5. verify policy/export/protected-source identities;
6. verify frozen ten-case artifact/hash and no rerun;
7. verify canonical calendar-2009 evidence and preserved MATLAB stationary baseline remain unchanged;
8. verify no active historical R5 / `chapter5_model` runtime dependency.

If the current Git blob differs from the corrected identities above, stop before mutation; do not fall back to the stale `66E3...` prose value.

All scientific calls remain zero until this entire preflight passes.

## 5. Dedicated faithful-only transfer-helper route

The existing shared `economics.transfer_candidate` is used by faithful and corrected/reference code. Do **not** broaden its domain or change its existing semantics.

Introduce a dedicated MATLAB-faithful raw-`Vb` transfer helper in the minimum explicit location, preferably `economics.py`, and retarget only:

- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
- the faithful implementation inside `exports/matlab_faithful_two_asset_ha.py`

Do not change:

- `transfer_candidate_corrected_max_scale`;
- corrected/reference `policies.py`;
- corrected/reference `hjb.py`;
- corrected/reference `generator.py`;
- corrected/reference `boundaries.py`.

Required marker:

`MP4B_FAITHFUL_RAW_VB_TRANSFER_HELPER_ROUTE_ISOLATION_PASS`

The dedicated helper must reproduce the frozen protected MATLAB `HANK3_FOC` semantics for all ten persisted cases, including:

- positive `pb`;
- negative `pb`;
- `pb==0, pa>0` -> ratio/output `+Inf`;
- `pb==0, pa<0` -> ratio/output `-Inf`;
- `pa==pb==0` -> ratio `NaN`, output finite `0`;
- `a==0, pb<0` -> finite signed `-0`.

Do not use Python scalar division that raises `ZeroDivisionError`.
Do not add epsilon, clipping, positivity guards, finite replacement, or an alternate equation.
Reproduce MATLAB scalar `min/max` NaN behavior rather than NumPy NaN-propagating behavior when those differ.
Preserve signed-zero semantics where observable in the frozen artifact.

Compare Python only against the persisted ten-case MATLAB manifest; MATLAB scalar calls here remain zero.

Required marker:

`MATLAB_FAITHFUL_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`

## 6. Faithful local-policy source-order repair

Repair only the MATLAB-faithful route:

- remove the non-source pre-floor raw-`Vb` positivity rejection;
- retain `max(raw Vb,1e-6)` strictly for consumption/labor FOCs;
- retain boundary-treated **raw** `Va/Vb` in the four transfer FOCs;
- preserve exact `Ic/Idh` priority/order;
- preserve lower/upper asset boundary rules;
- preserve bare-`a` transfer scaling;
- preserve adjustment-cost denominator floor;
- preserve illiquid return taper;
- preserve drift tolerance and source sparse-rate formulas.

Required static marker:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`

## 7. Local-policy parity gate

After static review, freeze before execution a bounded local-policy fixture containing:

1. the prior accepted positive-derivative/source branches needed for regression coverage; and
2. the exact Beijing iteration-5 offending cell:

- zero-based `(i,j,k)=(5,18,1)`;
- `VbF=-0.014003744365506235`;
- `VbB=0.0036470322698923963`;
- `VaF=0.029712870660726632`;
- `VaB=0.0183013418028827`;
- `(b,a,z)=(-0.1578947368421053,9.473684210526315,1.3)`.

Use the source-extracted MATLAB local-policy evaluator/semantics already accepted for this layer. Freeze manifest/evaluator/comparator before scientific output. No case tuning after output.

Scientific budget:

- MATLAB local-policy batch: at most `1`;
- Python repaired local-policy batch: at most `1`;
- comparator: at most `1`;
- reruns: `0`.

Require exact categorical/branch agreement and the previously frozen direct machine-bound rule for same-input continuous formulas.

Required marker:

`MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`

On failure, preserve evidence, roll back unaccepted production/export bytes, stop, and do not enter later gates.

## 8. Old 50-state HJB regression gate

Only after local-policy PASS, run the repaired modular Python faithful HJB once on the exact old accepted 50-state fixture.

Do **not** rerun MATLAB.

Preserved MATLAB HJB artifact:

`7351351B5D0F7012F03CB6A8CB79A6E31D8FC65FF5D7C26B4A241047F1B5DE94`

Predecessor accepted Python HJB artifact for diagnostic comparison:

`A33EEA2F7A1698624EC4F773FFE42C2BF88B255F9C231960E8C182ECA2C870AB`

Use the already accepted propagation-aware HJB contract unchanged:

- convergence boolean exact;
- iteration count exact;
- `V` absolute `1e-7` gate unchanged;
- categorical labels exact;
- derivative-floor activation exact;
- same-input primitive/coefficient replay at `128*eps64*max(...)`;
- exact-zero-only sparse support canonicalization;
- no new broad tolerance.

Budget:

- repaired modular Python 50-state HJB: at most `1`;
- comparator: at most `1`;
- MATLAB 50-state HJB: `0`;
- reruns: `0`.

Required marker:

`MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`

On failure, preserve evidence, roll back unaccepted production/export bytes, and stop.

## 9. Beijing first-turn standalone household parity gate

Only after the HJB regression PASS, freeze the exact Beijing first-turn household input contract from accepted corrected calendar-2009 evidence.

If any required MATLAB household input cannot be uniquely bound from accepted source/canonical evidence, stop `OWNER_PROVENANCE_REQUIRED`; do not guess.

Authorized scientific budget:

- protected MATLAB Beijing `HANK_2ASSETS_HJB` household call: at most `1`;
- repaired standalone Python `solve_household_steady_state` call: at most `1`;
- comparator: at most `1`;
- second province: `0`;
- reruns: `0`.

The standalone household call may internally execute its faithful HJB and faithful stationary KFE as defined by its public API; no separate KFE experiment or multi-province stationary call is authorized.

Compare common source-backed production-interface objects, including where available:

- convergence;
- `Ct/C_ss`;
- `Lt/L_ss`;
- `At/A_ss`;
- `Bt/B_ss`;
- `At+Bt` identity;
- `AtTax` only if uniquely source-backed on both sides.

Do not invent internal objects that protected MATLAB does not expose.
Freeze aggregate floating bounds before execution.

Required marker:

`MATLAB_FAITHFUL_STANDALONE_BEIJING_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`

On failure, preserve exact evidence, roll back unaccepted production/export bytes, and stop.

## 10. Standalone revalidation

Only after Beijing household parity PASS:

- run `py_compile`;
- focused standalone tests;
- dependency audit;
- clean-room import;
- public API audit;
- source-map audit;
- confirm corrected/reference route remains unchanged.

Freeze the repaired standalone file's new SHA-256 and supersede the old standalone SHA only after all required gates pass.

Required marker:

`MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

## 11. Global execution prohibitions

Throughout this task:

- ten-case MATLAB scalar rerun: `0`;
- exact-junction smoke rerun: `0`;
- MATLAB 50-state HJB rerun: `0`;
- calendar-2009 MATLAB multi-province stationary: `0`;
- calendar-2009 Python multi-province stationary: `0`;
- MP2/MP3 empirical: `0`;
- second province household: `0`;
- 2010-2023 annual batch: `0`;
- shocks/transition/dynamics/IRF/R5/Results: `0`.

No repair-and-rerun is authorized after the first post-repair scientific parity output.

## 12. Rollback discipline

Before mutation, record exact bytes/hashes for every production/export path that may change.

If any post-repair scientific gate fails or blocks after mutation:

1. preserve all evidence;
2. restore every unaccepted production/export candidate to task-authority bytes;
3. verify hashes and clean diff;
4. publish the failure report only;
5. do not continue to later gates.

A failed candidate must not remain on live `main`.

## 13. Allowed repository mutations

Conditional production mutation on the successful route only:

- `src/ch5_two_asset_hank/economics.py`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
- `exports/matlab_faithful_two_asset_ha.py`

Focused tests/validators/comparators necessary for the bounded gates are allowed.

Also allowed:

- bounded CURRENT roadmap status update;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_ECONOMICS_IDENTITY_ADJUDICATION_REPORT.md`

Do not modify protected MATLAB, corrected/reference scientific code, MP2/MP3 semantics, canonical data/cache, project rules, or historical R5.

## 14. Terminal verdicts

Complete success:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_IDENTITY_ADJUDICATION_PASS`

Blocked before or during a required gate:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_IDENTITY_ADJUDICATION_BLOCKED`

Scientific material mismatch after repair:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_IDENTITY_ADJUDICATION_MATERIAL_MISMATCH`

Codex must not self-authorize 2009 stationary parity after this task.

## 15. Required report and closeout

Report at minimum:

- terminal verdict;
- live continuity;
- corrected `economics.py` identity adjudication with Git object id and exact blob-byte SHA-256;
- pre-mutation production/export hashes;
- dedicated faithful-helper implementation and route-isolation proof;
- persisted ten-case Python/MATLAB parity table;
- local-policy parity evidence;
- 50-state HJB regression evidence;
- Beijing household parity evidence if reached;
- new standalone SHA if accepted;
- complete call ledger;
- rollback evidence if applicable;
- tests/checks and forbidden-operation audit;
- exact changed paths;
- Git closeout;
- exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

## 16. Successor boundary

Only on complete PASS may the next gate consider **one Python-only corrected calendar-2009 multi-province stationary run against the immutable preserved MATLAB baseline**. MATLAB stationary rerun remains closed.
