# CH5_TWO_ASSET_HANK_MP4C_CHUNK02_2012_2013_PYTHON_ANNUAL_STATIONARY_EXECUTION

Date: 2026-09-02

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor: Codex bounded Builder / annual stationary executor

Owner: final scientific authority

## 1. Authority basis

Accepted predecessor execution:

`MP4C_CHUNK01_2010_2011_PYTHON_ANNUAL_STATIONARY_COVERAGE_PASS`

Execution commit:

`1d97419cf04225263e52f3a030456c09b77aaff9`

Accepted annual route remains:

- calendar scope `2009–2023` inclusive;
- corrected-2009 is formally accepted MATLAB-Python stationary parity anchor and MUST NOT be rerun;
- Python annual coverage execution set is `2010–2023`;
- Chunk 01 (`2010, 2011`) is accepted Python annual coverage and MUST NOT be rerun;
- scheduling unit is two calendar years;
- scientific execution concurrency is exactly `1`;
- one failed/anomalous year does not invalidate completed accepted years.

This task authorizes only **Chunk 02: calendar years 2012 and 2013**, sequentially, plus one bounded zero-science cleanup of the MP4C annual driver's task-specific year-capability constant so later chunks do not require scientific-code edits solely to change a hard-coded pair.

This task does not authorize MATLAB, comparator, 2009/2010/2011 reruns, 2014–2023 execution, shocks, AR(1), transition/dynamics/IRF, R5, or Results.

## 2. Required live continuity

Required execution-start predecessor:

`1d97419cf04225263e52f3a030456c09b77aaff9`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of the required predecessor;
3. require clean tracked worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - all CURRENT project rules named by the rule index;
   - formal corrected-2009 L3 parity acceptance report;
   - MP4C multiyear scope-freeze report;
   - MP4C local-data staging report;
   - Chunk 01 task/report;
   - current `src/ch5_two_asset_hank/multi_province/annual.py`;
   - current `validators/multi_province/mp4c_python_annual_empirical.py`;
   - current focused MP4C annual-driver tests;
   - frozen repaired `one_turn.py`, `firm.py`, stationary runtime, household adapter and accepted 2009 driver used by the annual route.

Any authority, identity, local-data, or source-readability failure => stop before scientific execution.

## 3. Frozen scientific identities

Before mutation or science require at minimum:

- current MP4C annual driver Git blob: `d2e74cb8b491489053dcde525cd4d83cb4bd0b3a`;
- `src/ch5_two_asset_hank/multi_province/one_turn.py` Git blob: `e5d6835cdc9e6d182e1c84e11f4d51938be592e1`;
- `src/ch5_two_asset_hank/multi_province/firm.py` Git blob: `1f7d37247e2d712fc0477a9f562dce81d1b367ce`;
- accepted 2009 scientific driver Git blob: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- accepted household oracle SHA-256: `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`;
- accepted corrected-2009 canonical SHA-256: `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`;
- formal marker: `MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_FORMALLY_ACCEPTED`.

Require Chunk 01 durable terminal identities remain readable, without replay:

- 2010 terminal SHA-256 `28670266659062D98A8F52B7C3656E374260562307ACB295E9E7F0B2C892DBD0`;
- 2011 terminal SHA-256 `7939F2B562EB3EBFE45B68E513B98AFEB9F9474E17D5BE1FFFFB21210E8AE313`.

Do not modify or replay Chunk 01 artifacts.

## 4. Verified local primary sources

Use only the ignored local source snapshot already accepted by MP4C:

`D:\ProjectTemp\ch5-pre-p5-controlled-household-exec-repo-20260829\data_local\matlab_primary_source_snapshot`

Require exact files and SHA-256:

- `2000年后各省数据_填充NA.xlsx`
  - `C826B01B6C124EAAADC063DFC2D5510E50E72ED85BB34848F28AB318E4B88929`;
- `R语言估计结果_plm估计.xlsx`
  - `A6F444FCCCB30CB93AA5DE084F1DD163C54E5F53C4287C2CD3E13A045EB64A68`;
- `中国各省省会地理距离矩阵.xlsx`
  - `26E44D174A8EFFBDCA526D95DA38F0E5883E0C78FDFD036D2DFF1D1FBA5A3566`.

Do not use the unfilled workbook or `数据估计结果_1000_100_0.mat` as primary scientific inputs.

Copied data remain ignored/untracked and must never be staged or uploaded.

## 5. One-time zero-science driver capability cleanup

The current MP4C driver contains:

`AUTHORIZED_SCIENTIFIC_YEARS = (2010, 2011)`

and rejects any other calendar year in `run_python_once`.

That constant was an execution-task guard for Chunk 01, not a scientific model object. Re-editing the scientific driver for every future two-year chunk would create unnecessary code drift.

This task therefore authorizes exactly this bounded capability generalization before science:

1. replace the task-specific pair with a technical supported-scope constant covering exactly `2010–2023` inclusive, preferably:

   `SUPPORTED_CALENDAR_YEARS = tuple(range(2010, 2024))`

2. update the `run_python_once` membership guard to reject years outside that supported calendar scope;
3. make clear in code/tests/comments that **runtime capability is not GitHub scientific execution authority**; the live task still controls which years may actually be launched;
4. update only the existing focused MP4C annual-driver test(s) necessary to prove:
   - supported technical scope is exactly 2010–2023;
   - 2009 cannot be launched through the MP4C annual execution path;
   - an out-of-scope year such as 2024 is rejected;
   - decoupled binding for 2012 and 2013 is exact;
   - scientific ceilings remain unchanged;
   - no scientific model call occurs in these tests.

Do not alter canonical formulas, source hashes, scalar calibration, grid, HJB/KFE numerics, household adapter, one-turn semantics, stationary algorithm, controller rules, convergence definition, or output schema.

No other production-source mutation is authorized.

After this cleanup, future Chunk 03–07 tasks should be executable without further driver source edits unless a genuine defect is discovered.

## 6. Frozen calendar bindings for Chunk 02

The six annual identities must remain decoupled and exact.

For 2012:

- calendar year: `2012`;
- physical Excel row: `14`;
- workbook numeric row/index: `13`;
- analysis index: `4`;
- `data_MAT` index: `4`;
- regression vintage: `13`;
- output filename year: `2012`.

For 2013:

- calendar year: `2013`;
- physical Excel row: `15`;
- workbook numeric row/index: `14`;
- analysis index: `5`;
- `data_MAT` index: `5`;
- regression vintage: `14`;
- output filename year: `2013`.

The fixed source numerical `Zt` initialization anchor remains calendar 2020 exactly as accepted. Do not reinterpret it as the current execution year.

## 7. Hard scientific execution budget

Scientific concurrency: exactly `1`.

Per-year ceilings remain unchanged:

- outer turns: maximum `250`;
- household calls: maximum `7,750`;
- wall clock: maximum `14,400` seconds;
- stationary reruns: `0`.

Exact task call budget:

- Python 2012 annual stationary top-level: maximum `1`;
- Python 2013 annual stationary top-level: maximum `1`;
- 2012 reruns: `0`;
- 2013 reruns: `0`;
- Python 2009 stationary: `0`;
- Python 2010/2011 stationary: `0`;
- Python 2014–2023 stationary: `0`;
- MATLAB process/checkcode/model/stationary/HJB/KFE/household/firm/controller: `0`;
- comparator: `0`;
- separate standalone household/HJB/KFE/MP2/MP3 replay outside each authorized annual run: `0`;
- shocks/AR(1)/transition/dynamics/IRF: `0`;
- R5/Results: `0`.

Once one year's stationary execution is launched, there is no repair-and-rerun lane for that year.

## 8. Mandatory zero-science preflight

Before the 2012 launch:

1. complete and test the Section 5 driver capability cleanup;
2. verify all Section 3 frozen identities unaffected except the specifically authorized MP4C driver/test files;
3. verify local source hashes from Section 4;
4. `py_compile` the MP4C driver and focused changed tests;
5. run the focused zero-science MP4C annual-driver test suite plus the existing non-empirical MP1/MP2 regression checks used by Chunk 01;
6. require all tests PASS and scientific/model calls `0`;
7. reconstruct corrected-2009 canonical bytes from the verified local sources, without stationary execution, and require exact SHA-256:

   `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

8. build fresh 2012 and 2013 canonical inputs using the accepted annual API and persist them under a fresh preparation root;
9. verify for each canonical input:
   - exact calendar/index binding;
   - exact 31-province order;
   - expected regression vintage/sheet;
   - exact source hashes;
   - all arrays finite;
   - source formulas preserved;
   - fixed-calendar-2020 Zt anchor preserved;
10. verify proposed 2012 and 2013 run roots do not exist;
11. verify sufficient D-drive free space;
12. persist common preflight and exact launcher commands before science.

Any common driver/source/identity failure => stop before launching either year.

## 9. Exactly two sequential annual executions

Preferred fresh roots:

Preparation:

`D:\ProjectTemp\ch5-mp4c-chunk02-2012-2013-preparation-20260902-001`

2012:

`D:\ProjectTemp\ch5-mp4c-python-annual-2012-20260902-001`

2013:

`D:\ProjectTemp\ch5-mp4c-python-annual-2013-20260902-001`

Use the next deterministic suffix if any preferred root already exists. Never overwrite.

Execution order is strict:

1. launch 2012 exactly once;
2. after it terminates, persist its complete evidence and reverify shared scientific/source identities;
3. only then consider 2013;
4. launch 2013 exactly once if no shared/common blocker was discovered.

Never run 2012 and 2013 concurrently.

## 10. Fail-isolation rule

A year is normal coverage PASS only if all are true:

- process exits normally;
- status `SOURCE_CONVERGED`;
- outer turns `<=250`;
- household calls `<=7750`;
- wall clock does not exceed task ceiling;
- final household convergence `31/31`;
- final province set is complete, unique and in the accepted order;
- all frozen `31x20` terminal fields are finite;
- source/input/scientific identities remain valid.

If a validly launched year reaches a scientific/non-convergence ceiling or otherwise produces a year-specific model anomaly:

- preserve all evidence;
- classify that calendar year as `ANOMALY`;
- do not rerun it;
- do not modify model/scientific code;
- the other independent year may still execute only if the evidence establishes the anomaly is year-specific rather than a shared driver/source/infrastructure defect.

If failure indicates a shared driver defect, source-binding defect, corrupted local data, scientific identity change, or common infrastructure failure, stop before launching the later year.

## 11. Per-year evidence requirements

For each authorized year persist at minimum:

- canonical annual JSON and SHA-256;
- exact calendar/index manifest;
- source-file hashes;
- scientific-code identities;
- exact launcher command;
- run manifest;
- terminal summary;
- outer-turn count;
- household-call count;
- final household convergence count;
- exact province order;
- complete ordered `31x20` frozen final fields:
  `Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`;
- wage upper/lower counts;
- ra upper/lower counts;
- national `Ct/At/Bt/Yt`;
- wall-clock duration;
- execution ledger;
- anomaly reason if applicable.

Create a complete artifact inventory across preparation and both per-year roots, with size and SHA-256 for every durable artifact.

## 12. Interpretation boundary

Successful 2012/2013 runs establish **Python annual stationary coverage only**.

Do not claim:

- MATLAB-Python parity for 2012 or 2013;
- multi-year parity acceptance;
- paper Results validity;
- dynamic/IRF validity;
- robustness to controller-threshold changes.

Only corrected-2009 currently has formal cross-language parity authority.

Per-year PASS markers:

`MP4C_YEAR_2012_PYTHON_ANNUAL_STATIONARY_COVERAGE_PASS`

`MP4C_YEAR_2013_PYTHON_ANNUAL_STATIONARY_COVERAGE_PASS`

If both pass, task terminal:

`MP4C_CHUNK02_2012_2013_PYTHON_ANNUAL_STATIONARY_COVERAGE_PASS`

If one or both are isolated anomalies but the task itself correctly executes the finite route, use an explicit bounded completion terminal and list anomaly years; do not falsely issue the both-years PASS marker.

If common preflight/shared infrastructure blocks science, use:

`MP4C_CHUNK02_2012_2013_PYTHON_ANNUAL_STATIONARY_EXECUTION_BLOCKED_NO_RERUN`

## 13. Repository mutation boundary

Allowed repository mutations only:

- `validators/multi_province/mp4c_python_annual_empirical.py` for the exact Section 5 capability cleanup;
- the existing focused MP4C annual-driver test file, or at most one narrowly scoped replacement/additional focused test file if necessary;
- required execution report.

Do not modify:

- `src/ch5_two_asset_hank/multi_province/annual.py`;
- household/HJB/KFE modules;
- `one_turn.py`;
- `firm.py`;
- migration/capital/wage/monetary/fiscal/controller modules;
- accepted 2009 driver;
- comparator;
- source data;
- `.gitignore`;
- project rules;
- prior reports/tasks;
- thresholds/tolerances/calibration/grids.

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4C_CHUNK02_2012_2013_PYTHON_ANNUAL_STATIONARY_EXECUTION_REPORT.md`

Before commit:

- verify only allowed changed paths;
- verify local binary data remain ignored and unstaged;
- explicit-path staging only;
- `git diff --check --cached` PASS.

Closeout:

- exactly one execution commit;
- exactly one non-force push;
- fresh GitHub read-back of every changed path;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean tracked worktree;
- exact scientific call ledger;
- exact no-rerun evidence.

## 14. Exactly one recommended next gate

If both years PASS with no shared anomaly, recommend exactly:

`MP4C_CHUNK03_2014_2015_PYTHON_ANNUAL_STATIONARY_EXECUTION`

Do not start Chunk 03, any other year, threshold sensitivity, AR(1), shocks, transition/dynamics/IRF, R5, or Results inside this task.
