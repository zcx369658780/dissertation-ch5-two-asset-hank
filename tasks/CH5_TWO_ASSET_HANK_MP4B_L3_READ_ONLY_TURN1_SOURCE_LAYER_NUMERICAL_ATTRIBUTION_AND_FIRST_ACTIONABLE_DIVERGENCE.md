# CH5_TWO_ASSET_HANK_MP4B_L3_READ_ONLY_TURN1_SOURCE_LAYER_NUMERICAL_ATTRIBUTION_AND_FIRST_ACTIONABLE_DIVERGENCE

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / read-only source-layer numerical adjudicator

Owner: final scientific authority

## 1. Authority basis and purpose

Accepted predecessor execution:

`MP4B_RUNTIME_OVERLAY_CORRECTED2009_ONE_SHOT_AND_TRAJECTORY_PARITY_DIAGNOSTIC_PASS`

Execution commit:

`6c3df05d627fee95d43bdf08577e5a87e41b2cff`

Accepted strongest classification:

`MP4B_RUNTIME_OVERLAY_INITIAL_BINDING_EQUALIZED__LATER_DIVERGENCE_PERSISTS`

Accepted facts that must remain frozen:

- MATLAB/Python turn-1 entry `Zt` is bitwise exact for 31/31 provinces under `MATLAB_CACHE_RUNTIME_PARITY_OVERLAY`;
- the overlay and canonical Python runs differ only over turns 1--14 and have exactly identical final 31x20 frozen fields and national `Ct/At/Bt/Yt`;
- the earliest remaining mapped MATLAB/Python difference is turn-1 Beijing household `Ct` at approximately `3.06e-12` absolute difference;
- the accepted Beijing household same-input comparator previously established the household block inside the frozen parity envelope;
- turn-1 Beijing firm `Yt` already differs by approximately `3.919e-06`;
- turn-8 Shanghai/Qinghai reset-category divergence persists;
- turn-154 Zhejiang MATLAB-only low-return action persists;
- final wage upper-boundary count remains MATLAB/Python `7/5`;
- no same-input protected-source violation has yet been established.

The Owner has standing delegation to L3 for bounded mechanical diagnostics that introduce no new scientific choice. This task is a zero-model-execution, read-only diagnostic over already durable MATLAB and Python evidence.

The objective is to determine whether the remaining turn-1 Beijing household/firm differences are:

1. accepted numerical non-identity that is merely propagated by source-faithful arithmetic;
2. a source-layer arithmetic/order mismatch under the same scalar inputs;
3. a wrapper/state/reduction-order mismatch between the accepted standalone household and multi-province stationary path;
4. or insufficiently attributable from existing persisted evidence.

The task must identify the **earliest actionable divergence** supported by same-input evidence. It must not force a fault attribution when inputs differ.

## 2. Required live continuity

Required execution-start predecessor:

`6c3df05d627fee95d43bdf08577e5a87e41b2cff`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `6c3df05d627fee95d43bdf08577e5a87e41b2cff`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - predecessor runtime-overlay one-shot task/report;
   - predecessor source-binding census task/report;
   - post-trace same-input adjudication task/report;
   - accepted Beijing same-input household parity report at commit `486af39d40ca4292e04845a0ba5b30fbe4918f61`;
   - current source-semantics map and final-state field map;
   - protected MATLAB `HANK_mp_1turn.m`, `HANK_firm.m`, wage/migration/capital source files named by the source map;
   - current Python `one_turn.py`, `firm.py`, wage/migration/capital modules and validation adapters corresponding to those source layers.

Any authority, identity, or readability failure => stop with the BLOCKED terminal in Section 11.

## 3. Hard execution budget

All model/scientific execution counts are exactly zero:

- MATLAB processes/checkcode/stationary/HJB/KFE/household/firm/controller: `0`;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific execution: `0`;
- comparator replay / `compare_terminal`: `0`;
- standalone Beijing household replay: `0`;
- any province replay: `0`;
- other year / annual batch: `0`;
- shocks / AR(1) / transition / dynamics / IRF: `0`;
- historical R5 / Results: `0`.

Allowed work is limited to:

- read-only hashing/source inspection;
- direct parsing of already durable JSON/MAT/HDF5 evidence;
- standard-library-only binary64/hex/ULP analysis;
- scalar or finite-vector arithmetic reproductions of explicitly identified protected-source formulas using already persisted inputs;
- NumPy may be used only if necessary to reproduce an already persisted finite reduction whose exact source order is known; do not import repository scientific modules.

No scientific module call or solver import may be used as a substitute for source arithmetic inspection.

## 4. Immutable evidence roots

### 4.1 Admissible instrumented MATLAB chronology

Root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-l3-delegated-rerun-20260831-001`

Require at minimum:

- trace MAT SHA-256 `6C4AEC5992A59C1C8D7859DEF082E5711864789CAE009FD1E5CC2B78B3F6A520`;
- instrumented final-state SHA-256 `D2F4B225AC362A6805AAD1CD065AFA87DA32551833E5FC867BA3A37C9FD0B8F0`;
- baseline-admissibility SHA-256 `68C9F396C57D385E72E6A46D94A53F4196F7E9BFBC84A6EAF66AB1FF64A0866B`;
- chronology V5 SHA-256 `A64172CC32EC7295297CED6DFE27B52CE51B5EAD64AD82453C60EBE0152965C3`.

### 4.2 Runtime-overlay Python one-shot

Root:

`D:\ProjectTemp\ch5-mp4b-python-runtime-overlay-one-shot-20260901-001`

Require at minimum:

- terminal summary SHA-256 `5C9675702D27FF03A3ADA8959166C757F152B8C9E91126533E557373CB7C74E0`;
- overlay-vs-MATLAB chronology SHA-256 `09DC11C9BBB6E4CDF81C687A2F94FA1E460F2C608544825ADCAED88EC9A00F8F`;
- diagnostic manifest SHA-256 `DBA08ACDBC5E0E769FCA18C4E806B5B648703B839ED90469493835BEE146B6AB`.

### 4.3 Accepted Beijing standalone parity evidence

Use only immutable artifacts and contracts named by commit:

`486af39d40ca4292e04845a0ba5b30fbe4918f61`

Require at minimum:

- Beijing same-input contract SHA-256 `FE833FAEB48521CD0C7594627AF6FB5012F9497A455E9B2C5E7490E0C40E6F22`;
- accepted MATLAB household full result SHA-256 `024B097CEAC5872B5E83421B186C8424ECA92CD06A6D5D0FAF7B220BBFAB9DFE`;
- accepted standalone Python full result SHA-256 `9E9967F023C89F22550AEAC3C8CD53215B70E13C3C70E16C0CCC5C3D518A0F83`;
- qualified comparison SHA-256 `28966B73605BD82BA858C3B2A3CBC144C867377C71476B1003846C88AE6382BF`;
- frozen household comparator `abs(M-P) <= 1e-7 * max(1,abs(M),abs(P))`.

Do not rerun any of these objects.

## 5. Objective A — turn-1 Beijing household numerical attribution

Compare the three durable household outputs:

1. protected/instrumented MATLAB turn-1 Beijing;
2. accepted standalone Python same-input Beijing;
3. runtime-overlay multi-province Python turn-1 Beijing.

For `Ct`, `Lt`, `At`, `Bt`, and `At+Bt`, report:

- decimal values;
- binary64 hex;
- pairwise absolute/relative/ULP distances;
- frozen comparator bound;
- whether standalone and multi-province Python differences are consistent with accepted representation-equivalent initial arrays/reduction order rather than a new semantic mismatch.

Read the full available household inputs and initialization evidence. Determine whether the multi-province Python call and accepted standalone Python call use:

- the same scalar parameters exactly;
- the same grids exactly;
- the same incoming value/labor arrays bitwise, representation-equivalently, or differently;
- the same HJB/KFE numerics;
- the same aggregation/reduction formulas and axis/order semantics.

Do not claim bitwise same-input when the evidence supports only representation-equivalent inputs.

If the differences are entirely within the accepted envelope and no semantic input/path mismatch is found, establish:

`MP4B_TURN1_BEIJING_HOUSEHOLD_DIFFERENCE_ACCEPTED_NUMERICAL_NONIDENTITY_NO_ACTIONABLE_DEFECT`

If a concrete wrapper/init/reduction mismatch is found, use:

`MP4B_TURN1_BEIJING_HOUSEHOLD_WRAPPER_OR_REDUCTION_PATH_DIVERGENCE_LOCALIZED`

If a same-input source violation is proved, use the existing same-input fault classifications and identify the exact implementation.

## 6. Objective B — turn-1 Beijing source-layer propagation to firm Yt

Build an exact source-order turn-1 Beijing chain from already persisted values:

`household outputs -> migration/Lt_supply -> At*N productive-capital allocation/Kt_supply/rah -> firm input -> firm preclip -> firm output -> wage/policy/controller`

At every layer record MATLAB and Python values and classify each field as:

- bitwise identical;
- non-bitwise but inside an already frozen layer-specific numerical envelope, if one exists;
- different-input propagation;
- same-input arithmetic mismatch;
- unavailable in persisted evidence.

For the earliest firm `Yt` difference, determine whether the source-defined firm inputs (`Kt`, `Lt`, `Zt`, `alpha`, `GovInv`, and any exact source inputs used by the firm formula) are bitwise identical.

### If firm inputs differ

Reproduce each side's protected-source firm arithmetic using its own persisted inputs. If both sides agree with source arithmetic, classify:

`MP4B_TURN1_FIRM_YT_DIVERGENCE_UPSTREAM_INPUT_DRIVEN`

Then identify the earliest upstream input field responsible.

### If firm inputs are bitwise identical

Reproduce protected-source operation order and compare MATLAB/Python outputs. Only then may establish a firm implementation/arithmetic defect.

Do not treat the absolute size `~3.9e-06` as material or causal without scale and source-input context.

## 7. Objective C — cross-province turn-1 census

Do not restrict the analysis to Beijing if the durable turn-1 data allow a cheap read-only census.

For all 31 provinces, determine the earliest source layer at which MATLAB/runtime-overlay Python become non-bitwise-equal after the now-exact turn-1 entry `Zt` binding.

At minimum report counts by earliest differing layer:

- household output;
- migration/Lt_supply;
- capital/Kt_supply/rah;
- firm input;
- firm output;
- wage/policy/controller;
- no observed difference.

Report maximum normalized difference and province for key turn-1 fields, but do not invent new tolerances.

The purpose is to determine whether Beijing is representative or exceptional.

## 8. Objective D — first actionable divergence and relevance to turn 8

Using the source-layer chain, identify the earliest divergence that meets all of:

1. source-backed inputs are sufficiently observed to adjudicate;
2. the numerical difference is not already accepted as benign household representation-level non-identity unless a propagation mechanism is explicitly demonstrated;
3. it either violates same-input source arithmetic or demonstrably changes a downstream state at a meaningful source layer.

Then inspect whether the diagnosed turn-1 difference can be connected by persisted evidence to the already accepted turn-8 Shanghai/Qinghai reset divergence.

Possible conclusions include:

- `MP4B_EARLY_DIVERGENCE_ACCEPTED_NUMERICAL_NONIDENTITY__NO_CAUSAL_LINK_TO_TURN8`
- `MP4B_EARLY_SOURCE_LAYER_PROPAGATION_PATH_LOCALIZED__FAULT_NOT_ATTRIBUTED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`

Chronological precedence alone is not causation.

## 9. No tolerance or controller changes

This task must not:

- change the frozen household comparator;
- create a new parity tolerance after seeing results;
- change `|Yt/Yt0 - 1| > 0.01`;
- change `ra < ramin + 0.02`;
- change controller multipliers/damping;
- modify any canonical/overlay input;
- repair code;
- rerun stationary or household science.

Controller-threshold robustness remains a later, separate track after parity closeout.

## 10. External evidence package

Create one fresh no-overwrite root, recommended:

`D:\ProjectTemp\ch5-mp4b-turn1-source-layer-numerical-attribution-20260901-001`

If it exists, use the next deterministic suffix.

Persist at minimum:

- `turn1_beijing_three_way_household_binary64.json`;
- `turn1_beijing_household_input_path_matrix.json`;
- `turn1_beijing_source_layer_chain.json`;
- `turn1_firm_source_arithmetic_audit.json`;
- `turn1_allprovince_earliest_difference_census.json`;
- `turn1_to_turn8_relevance.json`;
- `source_expression_map.md`;
- `diagnostic_manifest.json`.

Any helper scripts remain external and must not import repository scientific modules.

## 11. Terminal classifications

PASS terminal:

`MP4B_L3_READ_ONLY_TURN1_SOURCE_LAYER_NUMERICAL_ATTRIBUTION_AND_FIRST_ACTIONABLE_DIVERGENCE_PASS`

BLOCKED terminal:

`MP4B_L3_READ_ONLY_TURN1_SOURCE_LAYER_NUMERICAL_ATTRIBUTION_AND_FIRST_ACTIONABLE_DIVERGENCE_BLOCKED`

Fault remaining unattributed is not a blocker.

## 12. Repository report and mutation boundary

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4B_L3_READ_ONLY_TURN1_SOURCE_LAYER_NUMERICAL_ATTRIBUTION_AND_FIRST_ACTIONABLE_DIVERGENCE_REPORT.md`

Allowed repository mutation:

- required report only;
- at most one strictly necessary CURRENT status/roadmap line.

Do not change source, validators, tests, canonical/overlay bindings, MATLAB files, comparator, contracts, prior reports, tasks, or rules.

## 13. Closeout

At completion report:

- terminal verdict;
- strongest supported classification;
- live continuity and immutable identities;
- zero-model ledger;
- three-way household binary64 comparison;
- household input/path adjudication;
- turn-1 Beijing source-layer propagation chain;
- firm same-input/different-input adjudication;
- all-province turn-1 earliest-difference census;
- turn-8 causal relevance or explicit lack thereof;
- exact statement of whether any actionable implementation defect is established;
- external artifact paths/sizes/SHA-256;
- `git diff --check` on actual staged report;
- changed paths;
- forbidden-operation audit;
- exactly one execution commit;
- one non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not auto-start repair, stationary rerun, threshold sensitivity, multi-year batch, AR(1), shocks, transition/IRF, R5, or Results from inside this execution.
