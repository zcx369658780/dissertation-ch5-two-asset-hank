# MP4C 2018 Call-725 MATLAB Path Collector: Zero-Science Recertification

## Terminal verdict

`MP4C_2018_CALL725_MATLAB_PATH_COLLECTOR_ZERO_SCIENCE_RECERTIFIED__SYNTHETIC_MEMBERSHIP_AND_WHICH_SMOKE_PASS__READY_FOR_FRESH_MATLAB_TERMINATION_REPLAY_TASK`

Classification:

`COLLECTOR_PARAMETER_BINDING_ONLY__FINITE_ROOT_AUTHORITY_UNCHANGED`

This is a zero-science recertification only. It does not authorize, perform, or imply a MATLAB HJB100/HJB500 replay, MATLAB KFE, Python model rerun, GE/annual/stationary execution, R/PLM, shock/IRF, Results work, production repair, or a 2018 rerun.

## Live authority and scope

- Repository: `zcx369658780/dissertation-ch5-two-asset-hank`
- Executed live task: `c4d0cdff12ffa6adbc85ce04221b280e87114a34`
- Required direct parent: `36a3b17ac9bd0368f5f45715dacce2a23b38c537`
- Controlling wrapper blob: `99b25751e8dd30ee12ea6da8dca87a668bb29696`
- New no-overwrite evidence root: `D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-path-collector-zero-science-recertification-20260904-001`
- Evidence audit-manifest SHA-256: `A94A24DD4C32B9DE816B7E3A9B8E507CDC755BEB2759E472FD139BDEDBAD5F75`

The repository wrapper was not changed. No production, model, test, validator, or protected MATLAB source was changed.

## Predecessor failure and bounded repair

The predecessor evidence root contained only its failed synthetic CSV and an unexecuted MATLAB smoke helper. It did not contain a complete PowerShell collector source or durable outer command. The new evidence therefore preserves that availability limitation and separately records the recovered observed binding fragment:

```powershell
$allowed=@(N $logical,N $physical)
```

The previous collector transported two intended allowed-root inputs through one nested PowerShell command invocation, rather than independently normalizing two literal collector inputs. Its durable CSV showed all four exact-root positives incorrectly as `False`.

The repaired collector is a standalone Python process with all nine case strings declared literally in memory. It uses only the frozen predicate:

1. Replace `/` with `\`.
2. Compare case-insensitively.
3. Remove trailing `\` only.
4. Accept iff the normalized candidate exactly equals the normalized logical root or normalized physical root.

It uses no prefix, substring, wildcard, filesystem-canonicalization, or generic-Junction acceptance rule.

## Phase A — synthetic collector certification

`MP4C_2018_PATH_MEMBERSHIP_COLLECTOR_PARAMETER_BINDING_CERTIFIED__ALL_SYNTHETIC_CASES_PASS__MATLAB_ZERO_CALL_SMOKE_AUTHORIZED`

All nine literal in-process inputs were text-identical between intended and received values.

| Category | Cases | Result |
| --- | ---: | --- |
| Exact logical / physical roots | 2 | 2 true; PASS |
| Mixed slash/case/trailing-separator equivalents | 2 | 2 true; PASS |
| Sibling, unrelated, child, parent, and prefix-lookalike roots | 5 | 5 false; PASS |

The self-test records that no outer positional path arguments or nested-PowerShell path transport were used.

## Phase B — finite Junction and protected source identity

The independent PowerShell proof found:

- `C:\MatlabProgram` link type: `Junction`
- target count: `1`
- sole target: `D:\MatlabProgram`

All four protected sources matched their frozen SHA-256 values from both logical and physical paths (8/8 checks):

| Source | SHA-256 |
| --- | --- |
| `HANK_2ASSETS_HJB.m` | `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE` |
| `HANK3_FOC.m` | `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D` |
| `HANK3_cost.m` | `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C` |
| `lab_solve2.m` | `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20` |

## Phase C — MATLAB `which` infrastructure smoke

`MP4C_2018_MATLAB_WHICH_ZERO_CALL_SMOKE_PASS__RESOLVED_SOURCE_IN_EXACT_LOGICAL_PHYSICAL_FINITE_ROOT_PAIR`

MATLAB `9.13.0.2049777 (R2022b)` was launched once. After temporarily prepending the frozen logical root, it ran only `which('HANK_2ASSETS_HJB')`, resolved:

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK\HANK_2ASSETS_HJB.m`

Its parent normalizes to the exact physical member of the certified finite pair; the resolved HJB SHA-256 is `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`. The MATLAB path was restored by the smoke helper cleanup handler.

The physical resolution is accepted only because the exact Junction and the frozen source hashes were independently proven. It does not establish acceptance of arbitrary links, canonical paths, children, siblings, prefixes, or other storage paths.

## Zero-science ledger

| Item | Count |
| --- | ---: |
| MATLAB `which` infrastructure calls | 1 |
| MATLAB `HANK_2ASSETS_HJB` calls | 0 |
| MATLAB HJB solves | 0 |
| MATLAB KFE solves | 0 |
| MATLAB household scientific calls | 0 |
| Python HJB/KFE/model calls | 0 |
| GE, annual, stationary, R/PLM, shock, IRF, Results calls | 0 each |
| Automatic retries | 0 |

## Evidence and next gate

The evidence root contains authority and predecessor identities, recovered-binding availability record and fragment, repaired collector, limited exact available-fragment diff, self-test, per-case CSV, Junction and source-hash proofs, MATLAB stdout/stderr and smoke receipt, integrity regression, zero-science ledger, classification, and a SHA-256 audit manifest. Readback of all 24 listed pre-manifest artifacts passed with no mismatches.

The predecessor's complete raw collector source/outer command was not durable, so the source-diff evidence is explicitly limited to the recovered observed binding fragment; no reconstructed fragment is represented as a raw predecessor file.

This closes only the collector parameter-binding gate. Any MATLAB HJB100/HJB500 or KFE replay requires a fresh live GitHub task and explicit Owner/L3 scientific-call authorization.
