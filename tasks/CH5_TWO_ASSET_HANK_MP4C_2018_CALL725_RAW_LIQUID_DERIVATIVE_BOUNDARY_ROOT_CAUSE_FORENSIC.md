# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_RAW_LIQUID_DERIVATIVE_BOUNDARY_ROOT_CAUSE_FORENSIC

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded forensic auditor

Owner: final scientific authority

## 1. Purpose

The accepted predecessor evidence has durably established the first-iteration classification:

`CALL725_FIRST_ITERATION_PRE_SOLVE_SOURCE_PARITY_BREAK_CONFIRMED__EARLIEST_STAGE_IDENTIFIED__NO_PRODUCTION_CHANGE`

The earliest material differences are raw liquid derivatives:

- raw `vb_f`: 40 material entries; maximum absolute difference `0.01228045195367922`;
- raw `vb_b`: 40 material entries; maximum absolute difference `0.012844718557132808`.

This task authorizes only a bounded root-cause forensic of those already-persisted derivative differences.

It does **not** authorize a production repair, protected MATLAB mutation, derivative implementation change, HJB rerun, solver rerun, tolerance change, KFE, GE, annual execution, R/PLM, shock/IRF, or Results work.

The objective is to determine whether the 40/40 differences arise from:

1. interior liquid finite-difference arithmetic;
2. lower/upper liquid-boundary overwrite formulas;
3. boundary-side/index assignment;
4. operand/scalar binding at the liquid boundaries;
5. stage-label/persistence semantics;
6. another precisely evidenced source-level cause.

## 2. Immediate live authority

Immediate parent expected at publication:

`a0537d671b9d811979c95a9fa3b2424db352771f`

Accepted predecessor report:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_REPORT.md`

Accepted predecessor report blob at the parent:

`98ccd150b09faf9db8bdae17e7031ef2341c59f3`

At task start:

1. verify repository identity and remote;
2. `git fetch origin`;
3. require this exact task on live `origin/main` as the direct child of the parent above;
4. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
5. read `AGENTS.md` and all CURRENT project rules;
6. read this task in full;
7. read the accepted predecessor report;
8. read the predecessor execution and zero-science closure tasks needed to understand artifact provenance.

If live authority or evidence identity differs materially, STOP before forensic work.

## 3. Frozen scientific/source identities

The following identities remain frozen unless live repository evidence proves otherwise:

Repository Python source:

`exports/matlab_faithful_two_asset_ha.py`

Git blob:

`9e7dc9556a2b76811e78f89999abecc045886106`

Protected MATLAB source:

`HANK_2ASSETS_HJB.m`

SHA-256:

`049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`

Authoritative HJB100 initialization MAT SHA-256:

`1718984CB588AE586F74AB8476C57AF849BB2C80CC95500329D29BC14207BB81`

Persisted strict-pair stage artifacts:

- MATLAB `matlab_core_stagewise.mat`: `B863BBE5A3CF6327954C71520F609B9949FE9DEF64BA5225AA4CE9661392B69E`;
- Python `python_strict_stagewise.npz`: `CEAE235AC0DC62C1FCD0D9728F840DEEA1E12038ABFCE73CA0DA6BD449CE915F`.

Final zero-science closure root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-zero-science-evidence-closure-20260904-002`

Final closure manifest SHA-256:

`BFA87A4B2DCB2D4E8A439994836D9D627368B6244564106FC3EE89D5FF28EC29`

Detached readback receipt SHA-256:

`5F2184C992FEA1C676B2064FC84A6FF5765B87B7E7F82003BE2982115F081A2E`

Treat all predecessor evidence roots and protected MATLAB paths as read-only.

## 4. Scientific call budget

This task authorizes **zero** MATLAB or Python model calls.

Required call ledger:

- new MATLAB HJB calls: `0`;
- new Python HJB calls: `0`;
- direct solver calls on call-725 model objects: `0`;
- native-init probe: `0`;
- retries: `0`;
- KFE: `0`;
- GE/stationary/annual: `0`;
- R/PLM: `0`;
- shock/IRF: `0`;
- Results: `0`.

Allowed diagnostic computation is limited to static reads, hashing, array slicing/index mapping, exact comparisons, and deterministic arithmetic replay of already-persisted scalar/array operands. Do not invoke the HJB evaluator or regenerate model states.

## 5. Phase A — exact mismatch-coordinate topology

Using only the already-persisted MATLAB/Python first-iteration artifacts and comparator evidence:

1. enumerate all 40 mismatching coordinates for raw `vb_f`;
2. enumerate all 40 mismatching coordinates for raw `vb_b`;
3. map each coordinate to zero-based and MATLAB one-based `(b-index, a-index, z-index)` plus the actual `b`, `a`, `z` values;
4. separately count interior, lower-`b` boundary, and upper-`b` boundary mismatches;
5. verify whether every interior liquid difference quotient is exact under the frozen comparison rule;
6. verify whether the 40-entry count equals one complete liquid-boundary plane (`n_a * n_z`) for each derivative side;
7. identify whether `vb_f` differences occur only at the upper-`b` overwrite plane and `vb_b` differences only at the lower-`b` overwrite plane, or reject that hypothesis with exact coordinates.

Persist a machine-readable coordinate table.

Do not infer boundary localization from the count `40`; prove it from the persisted arrays.

## 6. Phase B — stage semantics audit

Audit the exact MATLAB and Python external wrapper persistence logic used for the accepted first-iteration artifacts.

Determine whether the fields named raw `vb_f` / `vb_b` were persisted:

- immediately after interior finite differences and before boundary overwrite;
- after the liquid-boundary overwrite;
- or with mixed/other semantics.

Record exact wrapper lines and source paths.

If the persisted fields are actually post-boundary derivatives, classify the naming/forensic stage precisely and do not call the mismatch an interior raw finite-difference failure.

No wrapper modification is authorized.

## 7. Phase C — protected MATLAB versus Python source-expression audit

Read the protected MATLAB source only. Record logical and resolved physical path/junction provenance as required by CURRENT local-file rules.

Recover the exact source expressions and execution order for:

1. interior forward liquid derivative;
2. interior backward liquid derivative;
3. lower-`b` boundary derivative overwrite;
4. upper-`b` boundary derivative overwrite;
5. any liquid borrowing-rate branch used at a boundary;
6. wage, productivity, baseline labor, tax, transfer-income and liquid-asset terms entering the boundary marginal-utility expression;
7. CRRA exponent/application order.

Recover the corresponding Python expressions from the frozen repository blob.

For each expression, produce a normalized source mapping table:

`MATLAB expression -> Python expression -> semantic match / mismatch -> evidence`.

Do not repair either source.

## 8. Phase D — operand-binding audit at every affected boundary point

Using only persisted/certified inputs and artifacts, compare the exact operands feeding the disputed boundary derivatives in MATLAB and Python:

- `b`;
- `a` and `z` index association where relevant;
- baseline labor `l0`;
- wage `w`;
- `tau`;
- transfer income `Tt`;
- `rb` / borrowing-rate adjustment;
- `gamma`;
- any migration/wedge scalar actually entering the expression;
- any other source operand found in Phase C.

For each operand classify:

- exact same binary64 value;
- different value with provenance;
- unavailable in durable evidence.

Do not silently reconstruct a missing scientific operand from text or a rounded decimal when an exact persisted value is required.

## 9. Phase E — deterministic boundary-formula replay

Only if all required exact operands are durably available, perform bounded arithmetic replay from those already-persisted operands.

For each affected boundary point compute, without calling any HJB/policy evaluator:

1. the MATLAB source-expression result under MATLAB source arithmetic/order as reproducibly as possible;
2. the Python source-expression result under the frozen Python expression;
3. cross-evaluation when useful: MATLAB expression on Python operands and Python expression on MATLAB operands.

The replay must distinguish:

- source-formula difference;
- operand-binding difference;
- arithmetic-order-only difference;
- indexing/side-assignment difference;
- persistence/stage-label difference.

Persist exact maximum differences and representative coordinates.

If exact MATLAB arithmetic order cannot be faithfully replayed outside MATLAB without a new MATLAB call, do not invent parity; perform static symbolic/operand attribution only and state the limitation.

## 10. Required root-cause classification

Choose the strongest supported classification from the following, or define a narrower evidence-based subtype without broadening scope:

1. `CALL725_RAW_VB_BREAK_LOCALIZED_TO_LIQUID_BOUNDARY_OVERWRITE_FORMULA_GAP__NO_PRODUCTION_CHANGE`
2. `CALL725_RAW_VB_BREAK_LOCALIZED_TO_LIQUID_BOUNDARY_OPERAND_BINDING_GAP__NO_PRODUCTION_CHANGE`
3. `CALL725_RAW_VB_BREAK_LOCALIZED_TO_LIQUID_BOUNDARY_INDEX_OR_SIDE_ASSIGNMENT_GAP__NO_PRODUCTION_CHANGE`
4. `CALL725_RAW_VB_BREAK_CONFIRMED_IN_INTERIOR_DIFFERENCE_QUOTIENT__NO_PRODUCTION_CHANGE`
5. `CALL725_RAW_VB_STAGE_NAMING_OR_PERSISTENCE_SEMANTICS_GAP_CONFIRMED__NO_PRODUCTION_CHANGE`
6. `CALL725_RAW_VB_ROOT_CAUSE_UNRESOLVED_WITH_EXISTING_DURABLE_EVIDENCE__NO_PRODUCTION_CHANGE`

If multiple mechanisms are present, identify the earliest causal mechanism and separately list downstream/representation effects.

A classification is diagnostic only and does not authorize repair.

## 11. Repair-design boundary

If and only if a unique source-level root cause is established, the report may include a **non-executable repair design** containing:

- exact file/function/line region that would need future change;
- whether the repair belongs in Python production source, an external parity wrapper, comparator staging, or another layer;
- expected scientific object affected;
- minimal future validation needed.

Do not edit source, tests, wrappers, parameters, tolerances, or protected MATLAB in this task.

If a future repair would require choosing between MATLAB legacy fidelity and redesigned economic authority, mark that as an explicit Owner decision gate rather than selecting silently.

## 12. Evidence root

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-raw-liquid-derivative-root-cause-20260904-001`

Persist at minimum:

- live authority receipt;
- source/hash receipt;
- predecessor artifact/hash receipt;
- exact mismatch-coordinate table;
- boundary topology summary;
- wrapper stage-semantics audit;
- MATLAB/Python source-expression mapping;
- exact operand-binding table;
- deterministic replay receipt if permitted by available exact operands;
- scientific call ledger proving zero new calls;
- root-cause classification;
- future repair-design note if uniquely justified;
- `audit_manifest.json` and finite readback receipt.

Hash/read back every substantive evidence artifact. Do not require recursive self-hashing.

## 13. Repository publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_RAW_LIQUID_DERIVATIVE_BOUNDARY_ROOT_CAUSE_FORENSIC_REPORT.md`

If the forensic closes consistently, authorize one report-only commit and non-force push.

Suggested commit message:

`Diagnose MP4C call-725 raw liquid derivative break`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Completed terminal:

`MP4C_2018_CALL725_RAW_LIQUID_DERIVATIVE_ROOT_CAUSE_FORENSIC_COMPLETE__NO_RERUN_NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_RAW_LIQUID_DERIVATIVE_ROOT_CAUSE_FORENSIC_BLOCKED__NO_RERUN_NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

STOP after publication and return to ChatGPT L3 / Owner. Do not create a successor task and do not implement any repair.
