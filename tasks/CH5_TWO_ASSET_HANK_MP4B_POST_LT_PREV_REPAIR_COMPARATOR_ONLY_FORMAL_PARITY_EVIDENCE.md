# CH5_TWO_ASSET_HANK_MP4B_POST_LT_PREV_REPAIR_COMPARATOR_ONLY_FORMAL_PARITY_EVIDENCE

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor: Codex bounded Builder / comparator-only evidence executor

Owner: final scientific authority

## 1. Authority basis

Accepted predecessor execution:

`MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_BLOCKED_NO_RERUN`

Execution commit:

`45653fd9d0b96b31b090f1aff2bf3965c20a5e79`

Accepted scientific classification from that blocked execution:

`MP4B_LT_PREV_REPAIR_TRAJECTORY_PARITY_MATERIAL_IMPROVEMENT`

The predecessor stationary execution itself completed successfully:

- Python corrected-calendar-2009 stationary: exactly `1` execution;
- exit `0`;
- `SOURCE_CONVERGED`;
- `184` outer turns;
- `5704` household calls;
- final household convergence `31/31`;
- repaired turn-1 Beijing firm `mt` equals MATLAB exactly;
- former turn-8 Shanghai/Qinghai reset divergence disappeared;
- former turn-154 Zhejiang MATLAB-only low action disappeared;
- final category counts match MATLAB exactly: household `31/31`, wage upper/lower `7/17`, ra upper/lower `0/0`;
- read-only extraction reported final max normalized 31x20 difference approximately `3.835889478194021e-11` and national normalized differences approximately `1e-14` to `1e-12`.

The predecessor was BLOCKED only because its single authorized comparator attempt invoked a nonexistent module path and the no-rerun comparator budget was correctly respected.

This task authorizes a **new comparator-only budget** against the already completed immutable stationary terminal. It does not authorize any stationary/model rerun or scientific-code mutation.

## 2. Required live continuity

Required execution-start predecessor:

`45653fd9d0b96b31b090f1aff2bf3965c20a5e79`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of the required predecessor;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - predecessor blocked post-repair reassessment task/report;
   - accepted comparator representation-remediation task/report;
   - accepted final-state field map and comparator contract;
   - current comparator helper.

Any authority/identity/readability failure => stop before comparator invocation.

## 3. Hard zero-model budget

All scientific/model executions are exactly zero:

- MATLAB processes/checkcode/stationary/HJB/KFE/household/firm/controller: `0`;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific execution: `0`;
- other years/batch: `0`;
- shocks/AR(1)/transition/dynamics/IRF: `0`;
- R5/Results: `0`.

Comparator-only budget:

- accepted final-state comparator invocation: maximum `1`;
- comparator reruns: `0`.

No model or stationary rerun is permitted under any failure mode.

## 4. Frozen comparator identity and invocation contract

Comparator path:

`validators/multi_province/mp4b_compare_preserved_matlab_python_final_state.py`

Required Git blob:

`cbe7ce4e4855c139cc7bb3b20b56d124c4add266`

Do not modify the comparator, field map, province-name projection, category logic, or numerical formulas.

Before the comparator budget is consumed, verify from repository root that this exact import resolves to the exact file above:

`validators.multi_province.mp4b_compare_preserved_matlab_python_final_state`

The callable is:

`compare_terminal(matlab_path, python_terminal_path, output_path, matlab_terminal_status_path)`

Do not use or guess any alternative module name. In particular, do not invoke the nonexistent historical path `validators.multi_province.compare_mp4b_final_state`.

A standard-library/import-only preflight that resolves the module and inspects the callable is allowed and does **not** consume the comparator invocation budget, provided it does not call `compare_terminal`.

## 5. Immutable durable inputs

### 5.1 Preserved MATLAB corrected-2009 baseline

Root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require exactly one file under this root matching each frozen SHA-256 identity:

- stationary output: `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`;
- terminal status JSON: `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`.

Resolve the exact filenames by SHA if necessary. If either identity is absent or non-unique, BLOCKED before comparator invocation.

### 5.2 Post-repair Python corrected-2009 terminal

Run root:

`D:\ProjectTemp\ch5-mp4b-post-lt-prev-repair-python-overlay-one-shot-20260901-001`

Require:

- `python_terminal_summary.json` SHA-256 `A3C1297107504B1023CBB395E558B64DEA5B9DD8515148C4C67BDD6A315D7987`;
- complete readable 31-province final state;
- status `SOURCE_CONVERGED`;
- outer turns `184`;
- household calls `5704`.

### 5.3 Predecessor read-only evidence

Diagnostic root:

`D:\ProjectTemp\ch5-mp4b-post-lt-prev-repair-parity-reassessment-20260901-001`

Require the predecessor report/evidence sufficient to confirm:

- turn-8 reset sets now match MATLAB;
- turn-154 Zhejiang low action now matches MATLAB;
- final categorical counts are exact;
- no stationary rerun occurred.

Do not overwrite any predecessor artifact.

## 6. Comparator preflight

Before consuming the single comparator invocation:

1. verify the exact comparator Git blob;
2. verify the exact import path resolves to the comparator file;
3. verify `compare_terminal` exists and has the expected callable interface;
4. verify MATLAB stationary and terminal-status SHA identities;
5. verify Python terminal SHA and completeness;
6. verify proposed output path does not exist;
7. verify zero model/scientific calls;
8. persist a fresh preflight receipt outside Git.

No `pytest` or code mutation is required. `py_compile` of the immutable comparator is allowed.

## 7. Exactly one comparator-only execution

Create a fresh no-overwrite output root, preferred:

`D:\ProjectTemp\ch5-mp4b-post-lt-prev-repair-comparator-only-20260901-001`

or next deterministic suffix if it already exists.

Required comparator output path:

`qualified_final_state_comparison.json`

Invoke `compare_terminal(...)` exactly once using:

- resolved preserved MATLAB stationary output;
- post-repair Python `python_terminal_summary.json`;
- fresh comparator output path;
- resolved preserved MATLAB terminal-status JSON.

Comparator reruns: `0`.

If the invocation raises for any reason, preserve the failure evidence and stop. Do not patch, alter import paths, edit files, or retry in this task.

## 8. Required comparator adjudication

If comparator execution succeeds, report the complete frozen evidence without inventing a new tolerance.

Require and report:

### A. Province identity

- 31 MATLAB rows / 31 Python rows;
- raw MATLAB names retained;
- accepted one-terminal-`省`/`市` projection only;
- projected MATLAB sequence equals Python sequence exactly;
- no collision/permutation.

### B. Terminal categorical equality

Require explicit MATLAB/Python values and exact-equality booleans for:

- outer-turn count;
- final household converged count;
- `ra_upper_count`;
- `ra_lower_count`;
- `wage_upper_count`;
- `wage_lower_count`.

The predecessor scientific evidence predicts:

- outer turns `184/184`;
- household `31/31`;
- ra upper/lower `0/0`;
- wage upper/lower `7/7` and `17/17`.

These are predictions to verify, not hard-coded substitutes for comparator output.

### C. Continuous final-state evidence

For all frozen fields:

`Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`

report at least:

- maximum absolute difference and province;
- maximum relative difference and province;
- maximum normalized difference and province;
- median normalized difference;
- exact-equality count.

Also report national comparator values for:

- `Ct`;
- `At`;
- `Bt`;
- `Yt`.

Do not create a new post-hoc numerical threshold. The accepted comparator contract is evidence-producing for continuous final values and exact for categorical/source identities.

### D. Cross-check against predecessor read-only extraction

Determine whether comparator output is consistent with the already durable post-repair read-only extraction:

- max normalized 31x20 difference approximately `3.835889478194021e-11`;
- national normalized differences approximately `2.49e-14` for Ct, `5.41e-12` for At, `2.28e-13` for Bt, and `7.31e-15` for Yt.

Do not force exact equality to those summary decimals if the comparator reports the same underlying values with different summary formatting.

## 9. Classification boundary

Successful comparator execution with exact province/category identities and continuous discrepancies consistent with the already accepted source-faithful numerical non-identity evidence supports:

`MP4B_POST_LT_PREV_REPAIR_CORRECTED2009_FINAL_COMPARATOR_PASS__L3_STATIONARY_PARITY_ACCEPTANCE_PENDING`

This task itself does **not** issue the final scientific stationary-parity acceptance marker; that remains an independent L3 acceptance decision after reviewing the comparator artifact.

If categorical identities differ or comparator reveals a new material/source inconsistency, use:

`MP4B_POST_LT_PREV_REPAIR_COMPARATOR_COMPLETED__PARITY_NOT_ESTABLISHED`

If comparator/preflight fails:

`MP4B_POST_LT_PREV_REPAIR_COMPARATOR_ONLY_BLOCKED_NO_RERUN`

## 10. Required external artifacts

Persist in the fresh comparator-only root:

- `comparator_preflight.json`;
- `qualified_final_state_comparison.json` if completed;
- `comparator_execution_ledger.json`;
- `comparator_summary.json` if completed;
- `diagnostic_manifest.json`.

Record SHA-256 and byte size for every artifact.

## 11. Required repository report and closeout

PASS terminal for a completed comparator-only evidence task:

`MP4B_POST_LT_PREV_REPAIR_COMPARATOR_ONLY_FORMAL_PARITY_EVIDENCE_PASS`

BLOCKED terminal:

`MP4B_POST_LT_PREV_REPAIR_COMPARATOR_ONLY_BLOCKED_NO_RERUN`

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4B_POST_LT_PREV_REPAIR_COMPARATOR_ONLY_FORMAL_PARITY_EVIDENCE_REPORT.md`

Repository mutation: required report only.

Do not modify source, tests, validators, tasks, rules, canonical/binding artifacts, MATLAB, thresholds, tolerances, or prior reports.

Before commit:

- explicit-path stage the report only;
- `git diff --check --cached` PASS.

Closeout:

- exactly one execution commit;
- exactly one non-force push;
- fresh GitHub read-back of the report;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate: independent L3 formal corrected-2009 stationary-parity acceptance review.

Do not start another stationary run, another year, multi-year batch, threshold sensitivity, AR(1), shocks, transition/IRF, R5, or Results from inside this task.
