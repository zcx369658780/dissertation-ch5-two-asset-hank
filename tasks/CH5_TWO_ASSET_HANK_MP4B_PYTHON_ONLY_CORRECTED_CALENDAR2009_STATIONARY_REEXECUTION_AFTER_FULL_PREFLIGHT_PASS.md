# CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_PASS

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / one-shot Python stationary executor / parity evidence producer

Owner: final scientific authority

## 1. Purpose

Execute exactly one corrected-calendar-2009 Python multi-province stationary run after the accepted source-postloop household semantics and the complete zero-science preflight have both passed.

Use the already accepted validation-only `solve_matlab_source_postloop_household` route so that a false inner HJB convergence flag still proceeds through accepted KFE and aggregation, returns the original flag, and is evaluated by the accepted MP3 outer controller.

Compare a scientifically comparable Python terminal state against the same immutable preserved MATLAB 2009 baseline. MATLAB must not be rerun.

This task does not authorize another year, annual batch, shocks, AR(1), transition, dynamics, IRF, historical R5, calibration redesign, production-model refactoring, or Results.

## 2. Accepted predecessor authority

Required execution-start predecessor:

`a51d566cd6b19c03ff43ba7bad60c21743360578`

Accepted predecessor terminal:

`MP4B_STALE_ENTRY_REGRESSION_CONTRACT_REMEDIATION_AND_FULL_ZERO_SCIENCE_PREFLIGHT_PASS`

Required markers:

- `MP4B_STALE_ENTRY_REGRESSION_CONTRACT_ALIGNED_WITH_ACCEPTED_SOURCE_POSTLOOP_ADAPTER`
- `MP4B_SOURCE_POSTLOOP_ADAPTER_2009_STATIONARY_REEXECUTION_PREFLIGHT_PASS`
- `MP4B_MATLAB_HJB_NONCONVERGENCE_POSTLOOP_KFE_AGGREGATE_SEMANTICS_FROZEN`
- `MP4B_PYTHON_MULTI_PROVINCE_SOURCE_POSTLOOP_HOUSEHOLD_ADAPTER_STATIC_PASS`
- `MP4B_PRESERVED_MATLAB_PYTHON_FINAL_STATE_FIELD_MAP_FROZEN`
- `MP4B_REEXECUTION_2009_STATIONARY_COMPARATOR_CONTRACT_REUSED_UNCHANGED`

Accepted source-semantics classification remains:

`PYTHON_IMPLEMENTATION_ERROR__MULTI_PROVINCE_DRIVER_ABORTS_BEFORE_MATLAB_SOURCE_POSTLOOP_KFE_AND_AGGREGATES`

That defect is considered repaired in validation-only routing. Do not reinterpret it as evidence that raw HJB arithmetic itself differs.

## 3. Live continuity

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `a51d566cd6b19c03ff43ba7bad60c21743360578`;
3. require a clean worktree;
4. verify repository identity and all controlling rules;
5. verify every immutable identity in Section 4;
6. verify the preserved MATLAB baseline artifacts by path and hash;
7. verify the corrected entry-test Git blob is `89ff42fb99f24ed89ff162e69f2d6c3e01a052eb`;
8. verify no active `chapter5_model` / historical R5 runtime dependency.

Any failure => stop before science.

## 4. Immutable identities

Require exact SHA-256:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py`:
  `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py`:
  `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py`:
  `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map:
  `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter:
  `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver:
  `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- canonical corrected-2009 input:
  `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- stationary comparator contract:
  `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map:
  `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- read-only final-state comparator helper:
  `A6F7D2BBF7EE0936A6A0A45880B41D7AC77DB5AA7C3CA4B0F207F2D2A2DC08CF`

Do not modify any of these objects.

## 5. Frozen 2009 identity

Use exactly:

- `calendar_year = 2009`
- `analysis_index = 1`
- `workbook_data_row_index = 10`
- physical Excel row `11`
- `data_MAT_index = 1`
- `output_filename_year = 2009`
- `regression_vintage_key = 10`
- fixed-2020 `IND_Zt` only as the accepted source numerical initialization anchor.

Canonical input path:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

The legacy wrong-year route is forbidden.

## 6. Preserved MATLAB baseline — read only

Immutable root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require exact read-only identities:

- `matlab_calendar2009_stationary_output.mat`:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`

Frozen facts:

- terminal `COMPLETED`;
- outer turns `184`;
- household calls `5,704`;
- final household convergence flags `31/31`.

MATLAB stationary, household, HJB, KFE and presolver reruns are all forbidden.

## 7. Mandatory zero-science preflight immediately before science

Re-run the exact accepted regression selection:

`python -m pytest tests/test_mp4b_matlab_source_postloop_household_adapter.py tests/test_mp4b_preserved_matlab_python_final_state_field_map.py tests/test_mp2_source_faithful_one_turn.py tests/test_mp3_manual_update_map.py tests/test_mp4b_stationary_runtime.py tests/test_matlab_faithful_two_asset_ha_standalone_export.py tests/test_matlab_faithful_kfe.py tests/test_mp4b_python_empirical_entry.py -q`

Require exactly:

`64 passed`

No selected test may be removed, skipped, xfailed or replaced by a narrower suite.

Also require:

- fresh repository/identity verification;
- direct-script bootstrap PASS;
- presolver semantic mismatch exactly `0`;
- source initialization `24,800/24,800`;
- first-Beijing input semantic mismatch exactly `0`;
- source-postloop adapter/static routing PASS;
- final-state field map and comparator helper identities unchanged;
- `py_compile` PASS for the validation entry and comparator helper;
- `git diff --check` PASS;
- intended scientific run root absent before execution.

Required marker:

`MP4B_SOURCE_POSTLOOP_ADAPTER_2009_STATIONARY_SCIENTIFIC_EXECUTION_PREFLIGHT_PASS`

If not established, consume no science.

## 8. Comparison contract remains frozen

Reuse unchanged:

`validators/multi_province/mp4b_python_only_2009_stationary_comparator_contract.json`

SHA-256:

`E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`

Reuse unchanged field map and comparator helper from Section 4.

Direct/categorical requirements remain exact for calendar identity, province order, asset labels, convergence flags, controller terminal category, and exposed return/wage boundary categories.

The first Beijing household output remains governed by the already frozen household gate:

`abs(M-P) <= 1e-7 * max(1, abs(M), abs(P))`

for `Ct`, `Lt`, `At`, `Bt`, `At+Bt`; `AtTax` remains excluded at that household comparison layer.

For final propagated province/national continuous objects, report absolute, relative and normalized differences. Do not create a new post-hoc final tolerance.

A different Python outer-turn count from preserved MATLAB `184` cannot be accepted silently. Because the preserved MATLAB baseline lacks a full matching turn trace, a different turn count is MATERIAL/UNRESOLVED in this task unless existing preserved evidence already suffices to prove the contract's threshold-crossing exception without any MATLAB rerun.

## 9. Scientific execution budget

Only after Section 7 passes:

### Python stationary

- corrected-calendar-2009 top-level invocation: maximum `1`;
- stationary reruns: `0`.

Use one fresh no-overwrite direct-child root under `D:\ProjectTemp`, recommended:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

The one top-level stationary invocation may contain the source-bounded sequence of provincial HJB/KFE/aggregation calls and up to 500 outer iterations.

A household with `hjb.converged=false` must still complete KFE and aggregation through the accepted adapter and return the unchanged false flag to the outer controller. It must not trigger an adapter-level retry or early exception solely because of that flag.

### MATLAB

All scientific/model calls: `0`.

### Qualified comparator

- maximum `1`;
- reruns `0`.

Run it only after a durable Python terminal artifact with a scientifically comparable `final_state` exists.

If Python reaches source max-iteration exhaustion and persists a complete final state, the comparator may run once to classify the terminal mismatch. If Python fails before a comparable final state exists, comparator calls remain `0`.

After Python science begins, do not repair and rerun anything in this task.

## 10. Required persistence

Persist locally, no overwrite, at minimum:

- run/environment/source identity manifest;
- canonical input identity;
- accepted adapter/driver/comparator identities;
- per-turn household inputs/outputs already emitted by the validation entry;
- per-household diagnostics including `hjb_converged`, `hjb_iterations`, `hjb_statistic`;
- per-turn household convergence count;
- outer history/controller diagnostics;
- final state if produced;
- terminal summary/status;
- errors/warnings;
- qualified comparator output if run;
- complete call ledger;
- manifest of local artifact SHA-256 and sizes.

Do not commit large local scientific artifacts.

## 11. Comparison hierarchy

If a comparable Python terminal state exists, report in this order:

1. canonical/presolver identity;
2. first-turn Beijing input conformance;
3. first-turn Beijing output against accepted repaired household authority;
4. remaining first-turn household convergence structure;
5. household false-flag occurrences by turn/province, with HJB iterations/statistics;
6. migration and destination labor where directly available;
7. At-only capital / `Kt_supply` / `rah`;
8. firm / wage / monetary / fiscal mapped objects;
9. controller/adaptation/boundary diagnostics;
10. outer-turn count;
11. final 31-province mapped state;
12. national `Ct`, `At`, `Bt`, `Yt` sums.

Do not count downstream propagated differences as independent root causes after an upstream difference is localized.

## 12. Terminal classifications

### PASS

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_PASS`

PASS requires at minimum:

- Python reaches the source-equivalent converged terminal;
- province order exact;
- final household convergence flags exact and `31/31`;
- exposed return-bound categories agree exactly with preserved MATLAB;
- wage-bound categories agree exactly with preserved MATLAB;
- outer-turn count is compatible with the frozen comparator contract;
- no unexplained source/formula/data/controller divergence;
- final continuous differences are reported and scientifically attributable to already accepted numerical propagation, not to a new implementation mismatch.

On PASS establish:

`MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_ACCEPTED`

Do not independently claim the broader roadmap marker `MP4_MULTI_PROVINCE_STATIONARY_ROUTE_ACCEPTED`; that requires subsequent independent L3 review.

### MATERIAL / UNRESOLVED

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_MATERIAL_MISMATCH`

Use if a scientific Python route exists but terminal/category/source-contract agreement fails or a first divergence remains unresolved.

### BLOCKED

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_BLOCKED`

Use for pre-science identity/environment/preflight failure, or an infrastructure failure before comparable science exists.

## 13. Forbidden operations

Exactly zero:

- MATLAB stationary/household/HJB/KFE/presolver rerun;
- Python second stationary attempt;
- standalone Shanxi diagnostic replay;
- production/export mutation;
- source-postloop adapter mutation;
- validation driver mutation;
- MP2/MP3/stationary-runtime arithmetic mutation;
- comparator-contract or field-map widening;
- canonical data/calibration mutation;
- another year / annual batch;
- shocks / AR1;
- transition / dynamics / IRF;
- historical R5 runtime;
- Results claims;
- tolerance widening after scientific output inspection.

## 14. Allowed repository changes

Allowed only after terminal classification:

- one bounded CURRENT roadmap status update if needed;
- one text report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_REPORT.md`.

Do not modify any scientific, validation-runner, adapter, comparator, field-map, test, rule, protected MATLAB, canonical-data, or calibration file in this task.

If execution reveals a defect requiring any such change, preserve evidence and stop; repair belongs to a separate task.

## 15. Required report

Include at minimum:

1. exact terminal verdict;
2. live continuity and authority;
3. immutable identities;
4. exact 64-test preflight command/result and marker;
5. preserved MATLAB artifact paths/hashes and zero-rerun proof;
6. Python run root and local artifact manifest/hashes;
7. exact Python top-level call count and rerun count;
8. outer-turn count and terminal category;
9. total and per-turn household/HJB/KFE/aggregate counts;
10. false household-HJB convergence flags by turn/province with iterations/statistics;
11. first-Beijing input/output comparison;
12. qualified comparator call count/output hash if run;
13. final 31-province mapped comparison if available;
14. national `Ct/At/Bt/Yt` comparison if available;
15. controller, return-bound and wage-bound categories;
16. first supported divergence and evidence-backed classification if any;
17. complete zero-MATLAB and forbidden-operation ledger;
18. changed paths;
19. `git diff --check`;
20. commit SHA, push/read-back, `HEAD == origin/main`, ahead/behind, clean worktree;
21. exactly one recommended next gate.

## 16. Next-stage boundary

On PASS, recommend only independent L3 review of whether the corrected-2009 evidence is sufficient to establish the broader `MP4_MULTI_PROVINCE_STATIONARY_ROUTE_ACCEPTED` roadmap marker before any MP5/shock work.

On MATERIAL/BLOCKED, recommend only the smallest evidence-backed diagnostic or remediation gate. Do not proceed to another year, shocks, transition or Results.

## 17. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require final:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- forbidden-operation audit PASS.
