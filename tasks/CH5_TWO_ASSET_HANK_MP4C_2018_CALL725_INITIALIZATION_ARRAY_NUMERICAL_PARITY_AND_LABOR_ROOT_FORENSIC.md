# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_INITIALIZATION_ARRAY_NUMERICAL_PARITY_AND_LABOR_ROOT_FORENSIC

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / isolated initialization-parity diagnostic executor

Owner: final scientific authority

## 1. Immediate authority and purpose

Immediate live parent at publication:

`b9b768e9d6f964fa30f537e3fd43281a7878a4af`

The parent forensic established:

`INITIALIZATION_FORMULAS_SOURCE_IDENTICAL__CROSS_LANGUAGE_DIVERGENCE_LIES_DOWNSTREAM__FURTHER_HJB_FORENSIC_REQUIRED`

and also preserved the evidence limitation:

`CROSS_LANGUAGE_INITIAL_ARRAY_DIRECT_COMPARISON_UNAVAILABLE__NO_REGENERATION`.

That limitation arose because the previous task explicitly prohibited regeneration: the Python call-725 evidence preserved V0/labor digests but not raw initialization arrays, while the MATLAB replay preserved raw initialization MAT files.

This successor task explicitly authorizes the smallest bounded numerical diagnostic needed to close that gap: regenerate the Python call-725 initialization arrays **once, without entering HJB/KFE**, bind that regeneration to the already persisted Python V0/labor digests, and compare the resulting arrays directly to the already persisted MATLAB call-725 initialization arrays.

The scientific question is:

> Are the Python and MATLAB call-725 initial labor and V0 arrays numerically the same under the existing same-input contract, or do different numerical labor-root implementations already create a material upstream difference before the first HJB iteration?

This task does not authorize any HJB, KFE, GE, annual, production, shock, IRF, or Results execution.

## 2. Live continuity

At start:

1. `git fetch origin`;
2. require this exact task on live `origin/main` as direct child of `b9b768e9d6f964fa30f537e3fd43281a7878a4af`;
3. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
4. read `AGENTS.md`, all CURRENT project rules, this task, the parent initialization source-parity report, the Python call-725 replay report, the MATLAB call-725 replay report, and the accepted HJB propagation-aware parity report.

## 3. Frozen source identities

Require current Git blobs/source identities:

- `validators/multi_province/mp4b_python_empirical.py`: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- `exports/matlab_faithful_two_asset_ha.py`: `9e7dc9556a2b76811e78f89999abecc045886106`;
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`: `0033baee136c0328e80ffb8b794a88d4405c976c`.

Protected MATLAB source identities remain:

- `HANK_2ASSETS_HJB.m`: `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `lab_solve2.m`: `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

No source may be modified.

## 4. Frozen call-725 state

Use only the already accepted captured state:

- province: `安徽`;
- zero-based province index: `11`;
- outer iteration: `24`;
- global household call: `725`;
- `rah=0.09`;
- `rb=0.02`;
- `rb_gap=0.07`;
- `tau=0.05`;
- `w=16.82014806560587`;
- `Tt=0.1`.

Grid/parameters remain exactly:

- shape `(20,20,2)` in `(b,a,z)` / MATLAB `(I,J,Nz)` order;
- `b` from `-2` to `5`, 20 points;
- `a` from `0` to `10`, 20 points;
- `z=[0.8,1.3]`;
- `ga=2`, `rho=0.05`, `frisch_l=0.2`, `alphal=1`;
- all initialization formulas as frozen in the parent source-parity report.

## 5. Evidence roots to reuse

Python original call-725 root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-hjb-replay-and-bounded-continuation-20260904-001`

Require audit manifest:

`B15FE27D8531D5A1CE65E5D881327F820D82501FABD10B789F9F8B0544C7A0CF`.

MATLAB call-725 root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-termination-replay-after-path-recertification-20260904-001`

Require audit manifest:

`87500FF3121ECBBEE1E18A0A574371E06AC2B03B6B24B13465FCFBBF1E02457B`.

Parent source-forensic root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-hjb-initialization-source-parity-forensic-20260904-001`

Require audit manifest:

`B3B0BE432A2CCED37812F21243CC6B032F7CEA1528EA5BAC10F23271E159A42B`.

## 6. Hard boundary

Task type:

`INITIALIZATION_ONLY_NUMERICAL_PARITY_DIAGNOSTIC__NO_HJB_NO_KFE_NO_MODEL_LOOP`.

Forbidden:

- calling `solve_matlab_faithful_hjb`;
- calling protected `HANK_2ASSETS_HJB`;
- any HJB iteration;
- any KFE solve;
- any household steady-state composition that automatically runs HJB/KFE;
- GE/stationary/annual rerun;
- R/PLM;
- production/source/test/validator mutation;
- shock/IRF/Results;
- parameter tuning;
- changing the labor root equation or root algorithm for convenience;
- retries after observing the authorized regeneration result.

Authorized new numerical work is exactly:

1. one Python initialization-only regeneration using the current `_source_initial_arrays(...)` path on the frozen call-725 state;
2. deterministic array/digest/comparator arithmetic on that regeneration and already persisted MATLAB initialization artifacts.

No MATLAB numerical regeneration is required or authorized if the persisted MATLAB initialization arrays are intact and identity-bound.

## 7. Phase A — bind the original Python initialization digests

Before regeneration, locate in the original Python call-725 evidence the exact persisted V0 and baseline-labor digest values, plus the exact digest contract:

- dtype;
- shape;
- memory/order convention;
- byte serialization/canonicalization;
- hash algorithm.

Persist those facts into the new evidence root.

If the original digest algorithm/values cannot be established unambiguously, STOP before regeneration with:

`CALL725_PYTHON_INITIALIZATION_DIGEST_CONTRACT_UNAVAILABLE__NO_REGENERATION_NO_HJB`.

Do not invent a new digest and call it the original one.

## 8. Phase B — exactly one Python initialization-only regeneration

Only after Phase A PASS.

Use the exact current source constructor:

`validators/multi_province/mp4b_python_empirical.py::_source_initial_arrays`

with the frozen call-725 state, frozen grid, and frozen parameters.

Do not call `solve_matlab_source_postloop_household` and do not enter any HJB/KFE routine.

Persist the regenerated:

- `python_regenerated_initial_value` raw binary/NumPy artifact;
- `python_regenerated_baseline_labor` raw binary/NumPy artifact;
- shape/dtype/order metadata;
- exact regenerated digests using the **original persisted digest contract**.

This regeneration budget is exactly `1` constructor invocation and `0` retries.

### Hard identity gate

The regenerated V0 and labor digests must equal the original Python call-725 persisted digests exactly.

If either digest differs:

STOP.

Classification must include:

`PYTHON_CALL725_INITIALIZATION_REGENERATION_DOES_NOT_REPRODUCE_ORIGINAL_DIGEST__EVIDENCE_CHAIN_BLOCKED__NO_HJB`.

Do not proceed to cross-language array comparison.

## 9. Phase C — bind persisted MATLAB initialization arrays

Only after Python digest identity PASS.

Load the already persisted MATLAB initialization artifact(s), expected under the MATLAB call-725 evidence root, including `hjb100_initialization.mat` and, if present, `hjb500_initialization.mat`.

Verify:

- artifact identity via the MATLAB evidence manifest;
- HJB100 and HJB500 initialization arrays are identical to each other if both contain the same original initialization, as required by the prior scientific task;
- shape `(20,20,2)`;
- finite binary64 values;
- correct `(b,a,z)`/MATLAB `(I,J,Nz)` orientation.

If HJB100 and HJB500 persisted initial arrays differ materially, classify:

`MATLAB_REPLAY_DID_NOT_RESTART_FROM_IDENTICAL_ORIGINAL_INITIALIZATION__TERMINATION_COMPARISON_INVALID__NO_HJB_RERUN`.

Do not repair or rerun MATLAB in this task.

## 10. Phase D — direct initialization-array parity

Compare regenerated-and-original-digest-bound Python arrays to persisted MATLAB arrays.

For both `baseline_labor` and `initial_value`, report:

- shape/order identity;
- elementwise finite status;
- max absolute difference;
- max relative difference with safe scale `max(1,|x|,|y|)`;
- max normalized difference under the already accepted same-input rule `128*eps64*max(1,abs(x),abs(y))`;
- number of cells exceeding that rule;
- first and worst differing `(b_index,a_index,z_index)` and corresponding `(b,a,z)`;
- sign and magnitude of differences;
- whether arrays are bitwise equal, machine-rule equal, or materially different.

Do not introduce a broader tolerance after seeing results.

If labor differs, also compute **diagnostic-only, no-solver** source residuals for both persisted MATLAB labor and regenerated Python labor at each cell using the already frozen common labor-root equation. Report residual maxima and whether both arrays satisfy the same root equation accurately despite selecting slightly different floating solutions.

If V0 differs, decompose whether the difference is fully accounted for by the labor difference through the already frozen `c0` and `v02` formulas. This is deterministic arithmetic only; do not invoke HJB.

## 11. Interpretation boundary

Possible outcomes:

### A. Arrays pass the existing machine rule

Then freeze:

`CALL725_INITIALIZATION_ARRAYS_NUMERICALLY_PARITY_ACCEPTED__DIVERGENCE_BEGINS_INSIDE_HJB_ITERATION_OR_LINEAR_SOLVE`.

This would justify a subsequent first-iteration HJB forensic using one common initialization, but does not itself authorize it.

### B. Arrays materially differ, while regenerated Python digests match the original evidence

Then freeze:

`CALL725_INITIALIZATION_NUMERICAL_PARITY_BREAK_CONFIRMED__SOURCE_FORMULAS_MATCH_BUT_NUMERICAL_LABOR_ROOT_OR_INITIALIZATION_PROPAGATION_DIFFERS__HJB_TERMINATION_COMPARISON_NOT_YET_VALID`.

Identify the minimal proximal numerical source, e.g. MATLAB `lab_solve2` numerical root versus Python `brentq`, only if evidence supports it. Do not repair.

### C. Python regenerated digests fail

Use the blocked classification in Phase B; no downstream claims.

### D. MATLAB initialization artifact is inconsistent/unusable

Use:

`CALL725_MATLAB_PERSISTED_INITIALIZATION_IDENTITY_OR_RESTART_CONTRACT_BLOCKED__NO_HJB_RERUN`.

## 12. Scientific-call ledger

Final ledger must record:

- Python `_source_initial_arrays` constructor invocations: maximum `1`;
- Python labor-root scalar solves inside that constructor: exactly whatever one normal 20x20x2 constructor entails, expected `800`, with no retries beyond the constructor's frozen deterministic algorithm;
- MATLAB HJB: `0`;
- Python HJB: `0`;
- MATLAB KFE: `0`;
- Python KFE: `0`;
- protected household calls: `0`;
- GE/stationary/annual: `0`;
- R/PLM: `0`;
- shock/IRF/Results: `0`.

## 13. Evidence root

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-initialization-array-numerical-parity-20260904-001`

Persist at minimum:

- `authority_and_source_identity.json`;
- `python_original_initialization_digest_contract.json`;
- exact Python regeneration helper/source identity;
- regenerated V0/labor arrays and metadata;
- regenerated digest receipt;
- MATLAB initialization artifact identity;
- MATLAB HJB100-vs-HJB500 initialization identity comparison;
- `cross_language_initialization_array_comparison.json`;
- per-cell comparison CSV for labor and V0;
- labor-root residual comparison CSV if applicable;
- V0 difference decomposition if applicable;
- `classification.json`;
- `scientific_call_ledger.json`;
- stdout/stderr;
- `audit_manifest.json`.

Hash and read back every artifact.

## 14. Report and publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_INITIALIZATION_ARRAY_NUMERICAL_PARITY_AND_LABOR_ROOT_FORENSIC_REPORT.md`

Only a report-only commit + non-force push is authorized.

Suggested commit:

`Compare MP4C call-725 initialization arrays`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

PASS/complete terminal:

`MP4C_2018_CALL725_INITIALIZATION_ARRAY_NUMERICAL_PARITY_FORENSIC_COMPLETE__PRE_HJB_NUMERICAL_PARITY_CLASSIFIED__NO_HJB_NO_KFE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_INITIALIZATION_ARRAY_NUMERICAL_PARITY_FORENSIC_BLOCKED__NO_HJB_NO_KFE_NO_PRODUCTION_CHANGE`

Do not automatically launch a first-iteration HJB diagnostic, modify `maxit`, rerun 2018 GE, or enter IRF. Return to ChatGPT L3 / Owner.