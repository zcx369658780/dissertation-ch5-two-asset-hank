# CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / MATLAB-faithful household parity executor

Owner: final scientific authority

## 1. Purpose

Resume the faithful household repair now that protected MATLAB `HANK3_FOC` raw-`pb` edge semantics have been frozen by:

`MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_PASS`

Established source marker:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`

The already localized Python defect is:

`PYTHON_IMPLEMENTATION_ERROR__NONSOURCE_PRE_FLOOR_LIQUID_DERIVATIVE_POSITIVITY_GUARD`

Exact empirical witness:

- first Beijing household;
- HJB iteration `5`;
- zero-based `(i,j,k)=(5,18,1)`;
- MATLAB `(6,19,2)`;
- `(b,a,z)=(-0.1578947368421053,9.473684210526315,1.3)`;
- raw `VbF=-0.014003744365506235`;
- raw `VbB=0.0036470322698923963`;
- raw `VaF=0.029712870660726632`;
- raw `VaB=0.0183013418028827`.

Protected MATLAB floors raw `Vb` only for consumption/labor FOCs, then uses boundary-treated **raw** `Va/Vb` in `HANK3_FOC`. The current faithful Python route rejects non-positive raw `Vb` before those steps.

This task may repair only that faithful source-order defect, introduce the dedicated faithful transfer helper required by the accepted call-site audit, synchronize the standalone export, and then revalidate the affected household authorities.

**Calendar-2009 multi-province stationary execution remains closed in this task.**

## 2. Controlling authority and required reads

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- accepted faithful primitive/local-policy/HJB/KFE/end-to-end/standalone reports
- all prior MP4B raw-`Vb` diagnosis/replay reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_AFTER_HELPER_REMEDIATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`

Preserve established evidence:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`
- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`
- `MP4B_INITIAL_LABOR_PYTHON_MATLAB_FROZEN_CELL_ROOT_PARITY_PASS`
- `MP4B_PYTHON_FULL_FIRST_TURN_SOURCE_INITIALIZATION_PREFLIGHT_PASS`
- primary authority `MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`.

Protected MATLAB SHA-256 identities:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Frozen ten-case source artifact:

- run root `D:\ProjectTemp\mp4b_hank3_foc_10case_bcb5db94-c4a2-4d89-9485-6cbb20798126`
- manifest SHA-256 `06A31A509EF696094563CD41C9928A3E83AFB076675B09155CC82B2200E7E74E`

Do **not** rerun that scalar batch.

## 3. Live continuity

Expected execution-start parent:

`2d35bc0d4d05616774c0c9d0a40e5b917858d35a`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the scalar PASS commit;
3. require clean worktree;
4. verify protected MATLAB identities;
5. verify frozen scalar manifest SHA and all ten rows;
6. verify current predecessor production identities before mutation:
   - `economics.py` `66E3C56F177DB6DAFE7FE0A5FD6DA480D71A7ACC10B5209BC0E3F7360226DC55`
   - `matlab_faithful_policy.py` `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
   - standalone export `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`;
7. verify corrected/reference modules and MP2/MP3 have not drifted;
8. verify canonical 2009 input and preserved completed MATLAB run identities remain unchanged;
9. verify no historical R5 / `chapter5_model` runtime dependency.

Any required identity failure => stop before production mutation.

## 4. Frozen protected `HANK3_FOC` edge semantics

Treat the following as source facts, not hypotheses:

| Case | MATLAB ratio | MATLAB output |
|---|---|---|
| localized BB | finite `5.0181463854781496` | finite `18.55964077331755` |
| localized BF | finite `8.1471367571977353` | finite `33.381174113041901` |
| localized FB | finite `-1.3068891665833482` | finite `-10.45368552592112` |
| localized FF | finite `-2.1217804242353084` | finite `-14.313696746377776` |
| positive `pb` | finite `1.5` | finite `0.20000000000000001` |
| negative `pb` | finite `-0.5` | finite `-0.69999999999999996` |
| `pb=0, pa>0` | `+Inf` | `+Inf` |
| `pb=0, pa<0` | `-Inf` | `-Inf` |
| `pa=pb=0` | `NaN` | finite `0` |
| `a=0, pb<0` | finite `-1` | finite signed `-0` |

The `pa=pb=0 -> output 0` result is source evidence and means the faithful Python helper must reproduce MATLAB's `min/max` NaN handling rather than blindly propagating NumPy `minimum/maximum` NaN behavior.

Signed zero is also evidence and should be preserved where the source arithmetic produces it.

## 5. Call-site impact boundary

The predecessor audit established that existing `economics.transfer_candidate` is shared by faithful and corrected/reference code.

Therefore:

- do **not** broaden or weaken `transfer_candidate` in place;
- do **not** modify `transfer_candidate_corrected_max_scale`;
- do **not** modify corrected/reference `policies.py`, `hjb.py`, `generator.py`, or `boundaries.py`;
- introduce a dedicated faithful-only helper with a clear name such as `transfer_candidate_matlab_faithful_raw_vb`;
- retarget only the MATLAB-faithful local policy and the standalone faithful copy to that helper.

Required marker after static call-site review:

`MP4B_FAITHFUL_RAW_VB_TRANSFER_HELPER_ROUTE_ISOLATION_PASS`

## 6. Exact production repair semantics

### 6.1 Dedicated faithful transfer helper

The helper must reproduce protected MATLAB `HANK3_FOC` arithmetic for finite, negative, zero, infinite, NaN-ratio, and signed-zero regimes.

Required semantics:

1. inputs `pa`, `pb`, `a` must be finite state/derivative scalars on entry **except `pb` may be zero or negative**; do not require `pb>0`;
2. compute raw IEEE division equivalent to MATLAB `pa./pb`, allowing `+Inf`, `-Inf`, or `NaN` without Python `ZeroDivisionError`;
3. apply the exact source threshold:
   `min(pa/pb - 1 + chi0,0) + max(pa/pb - 1 - chi0,0)`;
4. reproduce MATLAB scalar `min/max` behavior when the ratio is `NaN`, including the frozen `0/0 -> output 0` case;
5. multiply by **bare `a`** and divide by `chi1` with source-consistent signed-zero behavior;
6. do not add epsilon, floor, clipping, positivity guard, finite substitution, or alternate formula.

A NumPy implementation may use IEEE-aware primitives such as `np.divide` under a scoped error-state and MATLAB-equivalent NaN-ignoring scalar min/max primitives if and only if the frozen ten-case artifact is reproduced exactly/classification-wise.

### 6.2 Local policy ordering

In the MATLAB-faithful selector:

- remove the non-source global rejection `min(v_b_forward,v_b_backward)<=0`;
- consumption/labor continue to use `max(raw Vb,1e-6)` exactly as protected source;
- all four transfer FOCs use boundary-treated **raw** `Va/Vb` through the dedicated faithful helper;
- keep liquid `Ic_B/Ic_F/Ic_0` order unchanged;
- keep transfer `Idh_F/Idh_B/Idh_0` order and liquid-boundary overrides unchanged;
- keep taper, adjustment cost, bare-`a`, upper/lower `a`, upper/lower `b`, drift tolerance, sparse-rate formulas, and final post-convergence drift semantics unchanged.

Do not add a new monotonicity/KKT veto.

### 6.3 Standalone synchronization

Apply the same faithful-only helper and source-order repair to:

`exports/matlab_faithful_two_asset_ha.py`

The standalone must remain self-contained with only standard library, NumPy, and SciPy dependencies. Do not add repository-local imports or GE logic.

## 7. Zero-science static and ten-case Python revalidation

Before any local-policy/HJB/household scientific run:

1. audit all call sites and prove corrected/reference route is unchanged;
2. run focused tests for the dedicated faithful helper;
3. compare the repaired Python faithful helper against the **persisted** ten-case MATLAB manifest; no MATLAB scalar rerun is allowed.

Ten-case acceptance:

- finite ratios/outputs: direct source-formula bound `128*eps64*max(1,abs(x),abs(y))`;
- `+Inf/-Inf/NaN` classifications: exact;
- `0/0` output finite exactly `0`;
- signed zero sign bit for `zero_a_negative_pb`: exact negative zero;
- no missing case or reordered case.

Required marker:

`MATLAB_FAITHFUL_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`

Also require:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`

## 8. Local-policy MATLAB/Python parity revalidation

Only after Section 7 passes, freeze a fresh local-policy parity manifest **before execution**.

Coverage must include:

- the previously accepted positive-derivative local-policy branch coverage;
- the exact Beijing iteration-5 offending cell;
- at least one predeclared negative-backward/raw-positive-forward case if source-valid;
- relevant boundaries needed to prove the new helper does not disturb lower/upper `a` or `b` behavior.

Do not add/tune cases after output.

Use a source-extracted MATLAB local evaluator bound to exact protected source formulas and helpers, as in the previously accepted local-policy route. Label the evidence:

`MATLAB_SOURCE_EXTRACTED_LOCAL_POLICY_PARITY`

Scientific budget:

- MATLAB local-policy batch: max `1`;
- Python repaired local-policy batch: max `1`;
- comparator: max `1`;
- reruns: `0`.

Compare all common source-backed outputs/branches. Continuous same-input values use the unchanged machine-scaled bound; categorical branch/direction mismatches are terminal.

Required marker:

`MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`

If any local batch/comparator fails, preserve evidence, restore production/export candidates to task-authority bytes, and stop. Do not proceed to HJB.

## 9. Positive-regime full-HJB regression

Only after local-policy parity passes, rerun the **repaired modular Python faithful HJB exactly once** on the old accepted 50-state fixture.

Do **not** rerun MATLAB on that fixture. Reuse preserved accepted MATLAB HJB artifact:

`7351351B5D0F7012F03CB6A8CB79A6E31D8FC65FF5D7C26B4A241047F1B5DE94`

and accepted predecessor Python artifact:

`A33EEA2F7A1698624EC4F773FFE42C2BF88B255F9C231960E8C182ECA2C870AB`.

Budget:

- repaired modular Python 50-state HJB: max `1`;
- MATLAB 50-state HJB: `0`;
- comparator: max `1`;
- reruns: `0`.

Apply the already accepted propagation-aware HJB contract unchanged:

- convergence boolean/iteration count/grid/order/initialization exact;
- `V` absolute bound unchanged at `1e-7`;
- categorical labels and derivative-floor activation exact;
- same-input primitive/policy/source-coefficient replay uses `128*eps64*max(...)`;
- mathematical sparse support compares after exact-zero-only canonicalization;
- no broad new raw downstream tolerance.

Also compare repaired Python against the predecessor accepted Python artifact to prove the positive-regime fixture has no unexplained regression. Any difference must be explicitly classified and source-explained; prefer exact/same-runtime equality where representation permits.

Required marker:

`MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`

If this gate fails, restore production/export candidates and stop. Do not proceed to Beijing household.

## 10. Exact Beijing first-turn standalone-household parity

Only after Sections 8-9 pass, freeze the exact first-Beijing first-turn household input contract from the already validated corrected calendar-2009 state.

Required preserved input identities include:

- canonical calendar-2009 premodel input SHA `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`;
- first-turn input artifact SHA `79B7A2805ECBAACDFCC70FA194E154263FA46EE313415A7060F90C65662DCE28`;
- source-initialized `20 x 20 x 2` arrays under the already accepted initial-labor/root contract;
- exact Beijing price/input objects used by the failed empirical run.

No guessing is allowed. If any MATLAB function argument or Python household input cannot be uniquely bound from accepted evidence and protected source, stop with Owner provenance required.

### 10.1 Scientific call budget

Authorize exactly:

- protected MATLAB Beijing `HANK_2ASSETS_HJB` household call: max `1`;
- repaired standalone Python `solve_household_steady_state` call: max `1`;
- comparator: max `1`;
- reruns: `0`.

No second province.

The standalone caller may use the already accepted source-initialization constructor to supply initial value and baseline labor; do not redesign initialization inside the standalone file.

### 10.2 Comparison contract

Compare every common source-backed production-interface object exposed or uniquely reconstructible without changing protected MATLAB, including at minimum where available:

- household convergence flag;
- `Ct` / Python `C_ss`;
- productivity-weighted `Lt` / Python `L_ss`;
- `At` / Python `A_ss`;
- `Bt` / Python `B_ss`;
- `At+Bt` identity;
- `AtTax` only if the protected source formula and both sides' required arrays are uniquely available;
- any other directly exposed diagnostics only when source provenance is unambiguous.

Do not invent a density comparison if protected MATLAB does not expose/persist the required density object in this bounded call.

For source-identical aggregate sums, freeze finite-sum binary64 bounds before execution using the already accepted aggregate methodology. Do not choose tolerances after output.

HJB/KFE internal diagnostics may be reported when actually exposed, but do not claim unobservable internal parity.

Required marker on complete pass:

`MATLAB_FAITHFUL_STANDALONE_BEIJING_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`

This is a single-household production-interface parity gate, not multi-province stationary acceptance.

## 11. Standalone dependency/export revalidation

After scientific PASS, rerun only engineering checks needed to revalidate the changed standalone bytes:

- `py_compile`;
- NumPy/SciPy/standard-library dependency allowlist;
- focused standalone tests;
- clean-room isolated import;
- public API unchanged;
- no GE/dynamics API introduced.

Freeze the new standalone SHA and establish:

`MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

The prior standalone SHA `276D...31AB8` is then historical predecessor evidence, not current transfer authority.

## 12. Scientific call ledger and explicit prohibitions

Maximum scientific calls in this task after all preflights:

- persisted MATLAB ten-case scalar batch: **0 reruns**;
- MATLAB local-policy batch: `1`;
- Python local-policy batch: `1`;
- local comparator: `1`;
- repaired modular Python old 50-state HJB: `1`;
- MATLAB old 50-state HJB: `0`;
- HJB comparator: `1`;
- MATLAB Beijing household: `1`;
- standalone Python Beijing household: `1`;
- Beijing comparator: `1`.

All of the following remain `0`:

- MATLAB/Python calendar-2009 multi-province stationary top-level;
- any second province household;
- MP2 empirical one-turn;
- MP3 empirical controller;
- 2010-2023 annual batch;
- shocks;
- transition;
- dynamics;
- IRF;
- R5;
- Results.

No repair-and-rerun is authorized after the first post-repair scientific parity output. If a gate fails, preserve evidence, rollback unaccepted production/export candidate bytes to the task-authority state, publish the failure report, and stop.

## 13. Authorized repository mutations

Allowed production mutations only:

- `src/ch5_two_asset_hank/economics.py` — add the dedicated faithful-only raw-`Vb` transfer helper without changing shared corrected semantics;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` — remove the non-source guard and retarget four faithful FOCs;
- `exports/matlab_faithful_two_asset_ha.py` — synchronize the accepted faithful repair.

Allowed tests/validators/reports:

- narrowly scoped helper/local-policy/HJB/standalone tests;
- bounded source-extracted local-policy evaluator/runner/comparators under validators;
- bounded Beijing household MATLAB wrapper/comparator under validators;
- CURRENT roadmap status update;
- one final report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_REPORT.md`.

Do not modify protected MATLAB, corrected/reference production modules, faithful operator/KFE scientific formulas, MP2/MP3 production, canonical data/cache, or historical R5.

## 14. Acceptance and terminal semantics

Complete PASS requires all of:

- `MP4B_FAITHFUL_RAW_VB_TRANSFER_HELPER_ROUTE_ISOLATION_PASS`
- `MATLAB_FAITHFUL_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`
- `MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_STANDALONE_BEIJING_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`.

Terminal PASS:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_PASS`

Scientific mismatch:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_MATERIAL_MISMATCH`

Infrastructure/provenance blocker:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_BLOCKED`

Owner provenance required:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_OWNER_PROVENANCE_REQUIRED`

A PASS still does **not** accept MP4 multi-province stationary parity.

## 15. Required report

Report at minimum:

- terminal verdict and live continuity;
- protected/scalar artifact identities;
- faithful-helper route-isolation audit;
- exact dedicated helper semantics, source and new hashes;
- complete persisted-ten-case Python/MATLAB comparison including Inf/NaN/signed-zero classes;
- local-policy fixture manifest/hashes/call ledger and per-field max differences;
- old 50-state repaired-Python HJB artifact/hash, preserved MATLAB comparison, predecessor-Python regression;
- exact Beijing input manifest/hashes;
- MATLAB/Python Beijing household outputs and frozen comparison bounds;
- standalone dependency/clean-room revalidation and new SHA;
- complete scientific call ledger;
- rollback status if any;
- forbidden-operation check;
- tests/checkcode/compile/diff checks;
- Git closeout;
- exactly one recommended next gate.

## 16. Closeout and successor boundary

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

On complete PASS recommend exactly one successor:

**fresh bootstrap/presolver/full-init preflight and one Python-only corrected calendar-2009 stationary invocation against the immutable preserved MATLAB baseline; MATLAB stationary remains zero.**

Do not authorize that stationary execution inside this task.