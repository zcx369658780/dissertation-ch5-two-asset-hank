# Boundary/generator repair specification — Reviewer acceptance

Date: 2026-09-07. Repository: zcx369658780/dissertation-ch5-two-asset-hank.
Candidate: `15f51be9733b5043e738d3e522b97554e95902b9`.
Task: `tasks/CH5_MP4C_CALL725_BOUNDARY_GENERATOR_REPAIR_SPEC.md`.

## Decision and scope
Accept the completed diagnostic/specification work:
`BOUNDARY_GENERATOR_REPAIR_SPEC_COMPLETE__SCIENTIFIC_DECISIONS_EXPLICIT__NO_MODEL_RUN`.
Authorize candidate inclusion in main under Owner standing authorization. This accepts evidence and a written repair proposal, not adoption of that proposal, an implemented target, policy optimality, generator validity, nonlinear convergence, annual coverage or Results.
Current route status: `BOUNDARY_SPEC_ACCEPTED__OWNER_TARGET_ADOPTION_PENDING`.
No successor execution task is issued in this review: D1–D3 below concern the scientific definition of the proposed target. The completed specification task must not be rerun while waiting for that decision.

## Review actually performed
Fresh main and its pre-publication recheck returned `6de422f3125045ca23f0464de9013a22ac77e3fc`. The candidate is its direct child, ahead1/behind0; the dedicated branch ref was independently read at the candidate SHA. All28 changed paths are additions within the four allowed groups: six validator files, one test file, one report and twenty evidence files. Production/helper/task/CURRENT files were not changed by the candidate.
Reviewer read the full report, source map, intake and drift/witness analysis, packaging/sealing code, all10 test definitions, both readable test-attempt logs, result and ledger summaries, manifest readback receipt, and terminal-corner CSV rows. The relevant frozen export cost/FOC and consumed/shadow/rate/assembly expressions were read directly at blob `9e7dc9556a2b76811e78f89999abecc045886106`. The current task and unchanged governing agreement were reused from the already-read live checkpoint; current status/index/handoff/roadmap were read before updating them.
Evidence label: L3 commit/report review plus L4 inspection of published test logs. The first log reports10 tests,9 ok and1 AST-copy normalization failure; the second reports10 named ok lines and an OK footer. The final test compares complete function bodies after normalizing only the binding locator. The original failure remains published; it was not a failed scientific solve.
Reviewer did not independently execute these tests, access Windows MAT/NPZ, recalculate the14 real snapshots or188 reconstructed cells, decode/re-hash the raw test payloads, or revalidate all external manifest entries. Those numerical/hash checks remain Builder evidence inspected through published code and summaries. Reviewer added0 scientific calls and0 MATLAB launches.
Published manifest readback: SHA256 `3D243B94BF048FD749FDCD1A3B46662EA862A09AE9B67E277139D3FD58F98B90`,62 entries, passed. This is a read receipt, not an independent inspection of all62 files. Raw external bytes and LF-normalized repository text are explicitly distinguished.

## Accepted findings
All14 selected snapshots have zero material budget-versus-captured drift mismatches on both axes under the unchanged128-eps rule. All14 contain material upper-face outward budget occurrences. The2,256 anomalous rows are snapshot-row occurrences selected by several diagnostics, not2,256 distinct economic states or2,256 independently proven boundary bugs. The112 corner records and112 face/productivity summaries are finite selected-snapshot coverage. Some operator-coordinate differences are floating representation effects, particularly at interior P32 row704.

The strongest repair witness is structural. For a closed finite-state generator with q_ij>=0 off diagonal and Q1=0, (Qx)_i=sum_{j!=i}q_ij(x_j-x_i). At a coordinate maximum this cannot be positive. MATLAB M143_FINAL row380 (lower b, upper a) has consumed d=0 and budget_a=0.81; its stored negative backward-a rate reproduces an outward coordinate action. At row419 (upper b, lower a), budget_b=0.003191811930404853 is outward, while A*b is negative because an outgoing rate was omitted without removing its diagonal contribution. Apparent inward operator action there is not policy feasibility. Python terminal witnesses agree in the published table.
Thus clipping negative rates or repairing diagonals alone cannot simultaneously preserve these stored outward budget drifts and create a closed, nonnegative, conservative generator. One must change the boundary policy or adopt a different, explicitly specified truncation/exterior treatment. This conclusion does not choose that treatment on Owner's behalf.
The frozen source also confirms a separate consumed/shadow mismatch: upper-a rate construction can use a candidate discarded by the consumed-transfer selector. Net inward drift alone does not prove that every split rate is nonnegative. The report distinguishes channel disagreement, omitted edges, row-sum contamination and floating representation rather than assigning all discrepancies to one cause.

## Written KKT and target proposal review
The proposed inward-normal convention t=+1 at lower faces and -1 at upper state-constraint faces, q=p+t*lambda, nonnegative multipliers and complementarity is internally consistent with the stated Hamiltonian. Consumption requires q_b=c^(-gamma)>0; the labor lower-bound multiplier and the zero-transfer subgradient interval are retained. Both asset constraints must be imposed jointly at corners; a geometrically present face may have a slack drift constraint and zero multiplier. Sequential boundary overrides do not implement this joint problem.
This is a written conditional specification, not an executed optimizer or a certificate of global finite-grid optimality. A future selector must make derivative-branch selection explicit and verify primal feasibility, complementarity, active/slack alternatives, kink conditions, direction consistency and admissible Hamiltonian comparison. It must report invalid derivative domains without a new floor. Reference-derived interior controls, if retained initially, remain labelled as such and are not automatically target-optimal.
The source cost already uses s(a)=max(a,a_bar), whereas its transfer candidate uses bare a. D3 is about consistently adopting that existing cost derivative in the separate target, not pretending the frozen reference already has a consistent FOC. The consistency requirement applies wherever a<a_bar, including a=0; it is not an isolated numerical edit at one endpoint. This denominator choice does not fix P32, where a>=a_bar and the tiny liquid derivative is a different issue.
No production repair, globally sufficient selector algorithm, new correctness tolerance or proposed120-root ceiling is approved by this acceptance.

## Minimum Owner decision — one target-adoption bundle
Recommend option B from the report, with the following explicit scope. Status of every row remains PROPOSED_REQUIRES_OWNER_ADOPTION.

| Decision | Reviewer recommendation | Tradeoff / boundary |
| --- | --- | --- |
| D1 artificial upper truncations | For the independent diagnostic target, use explicit numerical state constraints on the finite box; retain existing economic lower bounds. | This restricts controls at artificial upper faces, not an asserted economic saving cap. It is not automatic reflection/absorption. Later truncation sensitivity remains required. Exterior continuation is an alternative needing its own values/flux/mass contract. |
| D2 generator discretization | Assemble from the same consumed total budget drifts, using actual neighbor distances and nonnegative retained rates, with a conservative mathematical diagonal. Reject outward boundary inputs. | Total-drift upwinding changes finite-grid numerical diffusion; it is not a faithful refactor of separate-flow upwinding. Corrected-target tests and frozen-reference parity remain separate. |
| D3 cost/FOC consistency | Keep the existing regularized adjustment cost and its coefficients; derive the target KKT transfer condition from that same cost throughout its domain. | Do not add a liquid-derivative floor, transfer cap or a different adjustment technology. This changes legacy low-a FOC behavior and requires explicit target adoption, without reopening historical fixture results. |

Reference preservation, economic lower-bound identity, exact scalar inputs, post-boundary comparison stages,128-eps unchanged-object comparisons and the mathematical sign/conservation identities are already settled; no repeated Owner approval is requested for them.
A single Owner confirmation of D1–D3 for the independent diagnostic target is sufficient to define the recommended adoption scope. It does not authorize unlimited computation, replace production, accept the resulting model, or release annual/dynamic work. After adoption, Reviewer should publish one coherent task combining bounded implementation, focused checks and selected-cell validation where dependencies permit, rather than separate approval gates for each engineering phase. Exact root/evaluator budgets, derivative bindings and numerical checks must be set in that future task before execution.
Until then there is no active Builder successor. Do not create another generic boundary-audit or documentation-only task merely to wait for this scientific decision.

## Remaining blockers and evidence anchors
P32's extreme interior transfer/cost, loss of sigma in the stored diagonal, and the independent MATLAB143/Python500 convergence split remain open. Boundary repair alone is not promised to resolve them. No KFE/GE/corrected2018/annual/dynamics/IRF/Results acceptance follows.
Builder ledger reports0 new HJB/policy/direct/root/optimization/condition calls and0 MATLAB launches;14 saved snapshots,188 local legacy-expression witnesses and1,128 scalar reconstruction comparisons. Historical budgets are unchanged.
Primary evidence: candidate `docs/CH5_MP4C_CALL725_BOUNDARY_GENERATOR_REPAIR_SPEC_REPORT.md` and `reports/call725_boundary_generator_repair_spec_20260907/`; source expressions at the frozen export blob; predecessor policy/operator and frozen-linear acceptance records. Older project-source attachments do not override this live route.
