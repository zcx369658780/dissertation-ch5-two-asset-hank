# CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / MATLAB-source parity executor / accepted-oracle repairer

Owner: final scientific authority

## 1. Purpose

Repair the now exactly localized MATLAB-faithful household defect in the accepted Python oracle and modular faithful local-policy chain:

`PYTHON_IMPLEMENTATION_ERROR__NONSOURCE_PRE_FLOOR_LIQUID_DERIVATIVE_POSITIVITY_GUARD`

The defect was localized in the first Beijing household at HJB iteration 5, zero-based cell `(i,j,k)=(5,18,1)`, where raw `VbF=-0.014003744365506235` and raw `VbB=0.0036470322698923963`. Protected MATLAB floors `VbF` only for consumption/labor FOCs and then continues with the raw derivative in `HANK3_FOC`; the current accepted Python route rejects before the floor and before transfer ratios.

This task may implement only that source-order correction and then must revalidate the affected household authorities before any further empirical multi-province stationary execution.

Empirical calendar-2009 / 31-province stationary calls in this task are **zero**.

## 2. Controlling authority and evidence

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decision/adjudication documents
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_POLICY_LOCAL_INTEGRATION_AND_PARITY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_AND_FINAL_ACCEPTANCE_RERUN_REPORT.md`
- accepted KFE/end-to-end household reports
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT_REPORT.md`
- all prior MP4B reports, especially:
  - `docs/CH5_TWO_ASSET_HANK_MP4B_HJB_LIQUID_DERIVATIVE_OBSERVABILITY_AND_MATLAB_SOURCE_SEMANTICS_DIAGNOSIS_REPORT.md`
  - `docs/CH5_TWO_ASSET_HANK_MP4B_FIRST_BEIJING_HJB_GUARD_OBSERVABILITY_REPLAY_AND_EXACT_OFFENDING_CELL_LOCALIZATION_REPORT.md`

Preserve as accepted evidence:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- `MP4B_INITIAL_LABOR_PYTHON_MATLAB_FROZEN_CELL_ROOT_PARITY_PASS`
- `MP4B_PYTHON_FULL_FIRST_TURN_SOURCE_INITIALIZATION_PREFLIGHT_PASS`
- preserved completed MATLAB 2009 baseline `COMPLETED / 184 / 5704 / 31-of-31`
- primary reconstruction authority `MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Primary protected source hashes remain:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

## 3. Live continuity

Expected execution-start parent:

`3456d5ff66216cddfd9520253ed9769ba0d6bd28`

At start Codex MUST:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the localization commit;
3. require a clean worktree;
4. verify all protected MATLAB hashes above;
5. verify current accepted modular faithful source hashes and old standalone export SHA;
6. verify canonical 2009 input and preserved MATLAB baseline hashes remain unchanged;
7. verify corrected/reference modules have not drifted;
8. verify no historical R5 / `chapter5_model` runtime dependency.

Any failed identity => stop before mutation.

## 4. Freeze the exact source-order defect

Before editing, re-establish the protected MATLAB operation order:

1. construct raw interior `VbF/VbB`;
2. substitute liquid boundary marginal utilities;
3. construct raw `VaF/VaB` and illiquid boundaries;
4. use `max(raw Vb,1e-6)` only for consumption/labor FOCs;
5. form liquid resources/drifts and liquid upwind selection;
6. call `HANK3_FOC` with raw boundary-treated `Va/Vb` ratios;
7. form transfer candidates/upwind selection;
8. assemble rates/operator.

Reconfirm the localized witness exactly:

- HJB iteration `5`;
- zero-based `(5,18,1)`, MATLAB `(6,19,2)`;
- `(b,a,z)=(-0.1578947368421053,9.473684210526315,1.3)`;
- raw `VbF=-0.014003744365506235`;
- raw `VbB=0.0036470322698923963`;
- raw `VaF=0.029712870660726632`;
- raw `VaB=0.0183013418028827`;
- MATLAB processed forward Vb for consumption/labor `1e-6`;
- all four raw transfer ratios/candidates finite as recorded in the localization report.

Required marker before mutation:

`MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`

## 5. Call-site and corrected-track impact audit

Audit every live call site of:

- `src/ch5_two_asset_hank/economics.py::transfer_candidate`;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py::select_matlab_faithful_local_policy`;
- standalone copies in `exports/matlab_faithful_two_asset_ha.py`;
- `transfer_candidate_corrected_max_scale`;
- corrected/reference `policies.py`, `hjb.py`, `generator.py`, boundaries and related tests.

The task MUST preserve the corrected/reference route. In particular:

- `transfer_candidate_corrected_max_scale` MUST NOT change;
- corrected/reference policy/HJB/generator logic MUST NOT change;
- no global semantic change may be imposed on a shared helper without a call-site proof.

Architecture rule:

- if the existing `transfer_candidate` is proven exclusive to the MATLAB-faithful bare-`a` route, it may be corrected in place;
- if it is shared with a corrected/reference path whose positive-derivative contract must remain, introduce a clearly named MATLAB-faithful raw-`Vb` helper and retarget only the faithful route;
- do not silently broaden corrected/reference domains.

Required marker:

`MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`

## 6. Protected HANK3_FOC raw-pb edge-semantics freeze — before repair

The localized negative-`pb` witness is finite and unambiguous, but the Python helper repair must also avoid inventing behavior at `pb==0`.

Before mutation, freeze a finite validation-only scalar case set for protected `HANK3_FOC.m`, maximum **12 cases**, including at minimum:

- the four exact localized `BB/BF/FB/FF` `(pa,pb,a)` tuples;
- positive `pb` baseline case;
- negative `pb` case;
- `pb=0` with positive `pa` and `a>0`;
- `pb=0` with negative `pa` and `a>0`;
- `pa=0,pb=0,a>0`;
- at least one `a=0` edge case.

One validation-only MATLAB scalar batch is authorized. It may call only protected `HANK3_FOC.m` and source-identity checks. It MUST NOT call HJB, KFE, `HANK_2ASSETS_HJB`, MP2/MP3 or multi-province code.

Persist for each case:

- `pa,pb,a,chi0,chi1`;
- raw `pa/pb` class/value;
- output class/value (finite, +Inf, -Inf, NaN);
- no-overwrite manifest/hash.

The Python repair must mirror MATLAB/IEEE source behavior, including finite/Inf/NaN class where relevant. Do not introduce epsilon, derivative flooring, Python `ZeroDivisionError`, or a new positivity rejection in the raw transfer FOC.

If protected zero-denominator behavior cannot be represented unambiguously and safely, stop before production mutation with:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_OWNER_PROVENANCE_REQUIRED`

Required marker on success:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`

## 7. Authorized production repair

Only after Sections 4-6 PASS, repair the MATLAB-faithful route.

Required semantic correction:

### 7.1 Local policy

Remove the Python-only condition equivalent to:

`min(v_b_forward,v_b_backward) <= 0 -> ValueError`

from the MATLAB-faithful local policy.

Retain unchanged:

- all finite/state/grid/boundary validation not contradicted by MATLAB source;
- `MATLAB_DERIVATIVE_FLOOR = 1e-6`;
- `vb_b=max(raw_vb_backward,1e-6)` and `vb_f=max(raw_vb_forward,1e-6)` for consumption/labor only;
- liquid resource/drift and `Ic` ordering;
- raw boundary-treated `Va/Vb` transfer inputs;
- transfer/upwind ordering;
- lower/upper `a` and `b` overrides;
- cost, return taper, operator rates and all tolerances.

### 7.2 Raw transfer FOC

The faithful bare-`a` transfer helper must no longer reject solely because raw `v_b<=0`.

It MUST implement protected `HANK3_FOC` raw ratio semantics:

`q = pa/pb - 1`

followed by the frozen threshold expression and raw `a/chi1` scale.

For `pb==0`, use an explicit IEEE/MATLAB-equivalent scalar division mechanism and preserve source result class. Do not floor `pb`, clip, replace NaN, or force finiteness.

Input validation may still reject non-source objects such as structurally invalid nonnumeric inputs, but MUST NOT convert a source-defined finite/Inf/NaN arithmetic result into a positivity exception.

### 7.3 Standalone export

Apply the exact same faithful repair to:

`exports/matlab_faithful_two_asset_ha.py`

The old accepted standalone SHA

`276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

will intentionally be superseded only if all revalidation gates below PASS.

Preserve NumPy/SciPy-only dependency, no GE/dynamics API, clean-room import, label canonicalization contract, KFE and aggregation logic.

## 8. Static repair review before scientific parity calls

Before any post-repair scientific execution:

- compile changed Python files;
- prove corrected/reference files are byte-identical to authority state;
- prove `transfer_candidate_corrected_max_scale` unchanged;
- prove no raw pre-floor positivity guard remains on the faithful local-policy path;
- prove consumption/labor still use only the `1e-6` floor;
- prove raw `Va/Vb` remains the transfer FOC input;
- compare modular faithful and standalone faithful repaired source nodes structurally;
- rerun focused raw-positive regression tests;
- add negative/raw-zero helper tests reflecting the frozen HANK3_FOC edge manifest;
- verify no legacy R5 import.

Required marker:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`

No repair is allowed after the first scientific parity output in this task.

## 9. Revalidate local-policy parity, including the exact offending regime

Freeze a new source-extracted local-policy case manifest before execution.

It MUST include:

- the original accepted 11-case local-policy fixture or a byte-identical reconstruction of those cases;
- the exact Beijing iteration-5 offending cell;
- at least one case with negative raw forward Vb and positive backward Vb;
- at least one case with negative raw backward Vb and positive forward Vb;
- at least one both-negative raw Vb case if protected source arithmetic remains finite through the required local block;
- positive-raw regression witness;
- boundary cases only if needed to ensure no affected boundary behavior drift.

Zero-`pb` cases that produce nonfinite source transfer values belong to the scalar HANK3_FOC edge gate and MUST NOT be forced into a full local-policy pass if the source local block itself would subsequently become nonfinite.

Use a validation-only source-extracted MATLAB local evaluator bound to the protected formulas. Scientific budget:

- MATLAB local-policy batch: maximum `1`;
- modular Python local-policy batch: maximum `1`;
- comparator: maximum `1`.

PASS requires:

- categorical liquid/transfer labels exact where source results are finite;
- continuous same-input objects within `128*eps64*max(1,abs(x),abs(y))`;
- exact boundary actions;
- exact derivative-floor activation;
- exact raw-transfer-ratio source semantics;
- the exact localized Beijing cell proceeds rather than raising and matches MATLAB source outputs;
- no unexplained residual.

Required marker:

`MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`

Any failure => no further HJB/household scientific call in this task.

## 10. Revalidate affected full-HJB authority

The previous 50-state propagation-aware MATLAB HJB artifact is immutable accepted evidence:

`7351351B5D0F7012F03CB6A8CB79A6E31D8FC65FF5D7C26B4A241047F1B5DE94`

and the prior comparator contract remains controlling:

- direct same-input arithmetic: `128*eps64*max(1,abs(x),abs(y))`;
- converged `V` absolute bound `1e-7`;
- convergence boolean and iteration count exact;
- categorical branch labels exact;
- derivative-floor activation exact;
- mathematical sparse support exact after dropping **exact numerical zero only**;
- solver-propagated downstream differences are diagnostic only when same-input replay has zero unexplained residual.

Do NOT rerun MATLAB on the old 50-state regression fixture if the preserved MATLAB artifact is available and hash-valid.

Run the repaired modular faithful Python HJB on the exact old 50-state fixture at most once and compare against the preserved MATLAB artifact using the frozen propagation-aware contract.

This regression verifies that the source-order repair creates no unrelated drift in the previously accepted positive-derivative fixture.

Required marker:

`MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`

If the preserved MATLAB artifact or exact fixture cannot be recovered/hash-verified, stop BLOCKED rather than silently substituting another fixture or rerunning MATLAB under this section.

## 11. Exact Beijing single-household end-to-end revalidation

After Sections 9-10 PASS, perform one bounded household-level comparison on the exact corrected calendar-2009 first-turn Beijing household inputs that produced the localized negative raw-Vb regime.

This is **not** a multi-province stationary run and MUST NOT enter migration, MP2, MP3 or GE.

Before execution, source-bind the exact protected MATLAB `HANK_2ASSETS_HJB` single-household arguments from accepted prepared-state / turn-1 evidence. No guessing is allowed. If every required argument cannot be uniquely bound, stop:

`MP4B_RAW_VB_BEIJING_HOUSEHOLD_OWNER_PROVENANCE_REQUIRED`

Scientific budget:

- protected MATLAB `HANK_2ASSETS_HJB` for Beijing only: maximum `1` top-level household call;
- repaired standalone Python `solve_household_steady_state` for Beijing only: maximum `1`;
- household comparator: maximum `1`;
- rerun after either output: `0`.

The MATLAB call may internally execute its source household HJB/KFE/aggregation chain. It MUST NOT call multi-province GE or any second province.

Use the exact same:

- `20 x 20 x 2` grid;
- source initial value/labor construction;
- `rah/rb/rb_gap/w/tau/Tt`;
- source switching matrix;
- `Delta=1000`, `crit=1e-7`, `maxit=100`;
- faithful adjustment cost/taper/bare-a FOC;
- corrected calendar-2009 first-turn Beijing state.

Compare every mutually exposed household object. At minimum require/report:

- HJB convergence and iteration count;
- HJB value / convergence statistic if exposed by both sides;
- selected policy/drift/operator objects if exposed by both sides;
- stationary KFE normalization/density objects if exposed by both sides;
- `Ct`, household `Lt`, `At`, `Bt`, `AtTax` where available;
- final household convergence flag;
- any source-exposed diagnostics.

Use previously accepted HJB propagation-aware, KFE contaminated-row, and household aggregate comparison contracts. Do not invent a new broad tolerance.

For source objects unavailable from the native MATLAB return, state `NOT_EXPOSED_BY_NATIVE_SOURCE_OUTPUT`; do not manufacture them by rerunning another MATLAB HJB in this task.

Required marker on complete supported parity:

`MATLAB_FAITHFUL_STANDALONE_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`

## 12. Standalone packaging / transfer revalidation

The repaired standalone file is a new candidate identity and must be revalidated before publication authority is restored.

Required engineering checks:

- Python compile;
- dependency allowlist remains standard library + NumPy + SciPy only;
- no repository package import;
- clean-room isolated copy/import;
- public API unchanged except explicitly documented faithful helper naming if the Section 5 audit required a split helper;
- no GE/dynamics API;
- no corrected/reference route dependency;
- no R5 dependency;
- focused standalone tests updated for negative raw-Vb / source-order behavior;
- modular-vs-standalone source/provenance map shows only the authorized faithful repair plus necessary provenance/hash marker changes.

Use the Beijing end-to-end household result from Section 11 as the scientific standalone validation; do not run a second standalone household solve merely for packaging.

If a same-runtime modular-vs-standalone comparator is needed, it may compare persisted outputs/source nodes without another scientific solve.

Preserve the existing policy-label canonicalization map only:

- `B -> -1`
- `F -> +1`
- `0 -> 0`.

Required marker:

`MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

On PASS, record and freeze the new standalone SHA-256. The old `276D...31AB8` identity becomes historical predecessor evidence, not the current oracle identity.

## 13. No empirical stationary execution

This task authorizes **zero** empirical multi-province stationary execution:

- corrected MATLAB calendar-2009 multi-province stationary: `0`;
- Python calendar-2009 multi-province stationary: `0`;
- 2010-2023 batch: `0`;
- MP2 empirical one-turn: `0`;
- MP3 empirical controller: `0`;
- shocks/AR1: `0`;
- transition/genuine dynamics/IRF: `0`;
- legacy R5: `0`;
- Results/manuscript claims: `0`.

The only scientific calls are the explicitly bounded household/local/HJB revalidation calls in Sections 6, 9, 10 and 11.

## 14. Failure / rollback rule

Once any post-repair scientific parity output exists, no repair-and-rerun is authorized.

If any local-policy, HJB, Beijing household, or standalone revalidation gate fails:

1. preserve candidate diff, hashes and local artifacts under a no-overwrite evidence root;
2. classify the exact first failure;
3. restore affected accepted production/export paths to their task-authority bytes before repository closeout;
4. do not leave an unaccepted oracle candidate as live production authority;
5. publish the report/allowed validators only;
6. recommend exactly one bounded successor.

Do not loosen tolerance or alter fixture after seeing output.

## 15. Allowed repository changes

Allowed production mutations, only within the frozen repair scope:

- `src/ch5_two_asset_hank/economics.py` and/or a new faithful raw-transfer helper if required by the Section 5 call-site audit;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`;
- `exports/matlab_faithful_two_asset_ha.py`;
- affected focused tests;
- validation-only MATLAB/Python scalar/local/household runners/comparators under `validators/`;
- bounded CURRENT roadmap status update;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`.

Do NOT modify:

- protected MATLAB;
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py` unless the task is BLOCKED and a separate future task explicitly authorizes a newly proven HJB-driver defect;
- `matlab_faithful_operator.py`;
- `matlab_faithful_kfe.py`;
- corrected/reference `policies.py`, `hjb.py`, `generator.py`, boundary logic;
- `transfer_candidate_corrected_max_scale`;
- MP1/MP2/MP3 arithmetic/controller semantics;
- multi-province `stationary_runtime.py`;
- canonical input/data/cache;
- preserved MATLAB 2009 baseline;
- controlling rule files;
- historical R5.

## 16. Required report

Write:

`docs/CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`

Include at minimum:

1. terminal verdict and acceptance level;
2. live continuity;
3. protected source identities;
4. exact localized defect/witness;
5. call-site impact audit and corrected-track non-impact proof;
6. HANK3_FOC raw-pb edge manifest/hash/table;
7. exact modular/standalone repair diff and new hashes;
8. static repair-review marker;
9. complete local-policy case manifest and MATLAB/Python comparison;
10. old 50-state HJB preserved artifact verification and repaired-Python regression result;
11. exact Beijing single-household input/provenance manifest;
12. MATLAB/Python Beijing household call ledger and output hashes;
13. HJB/KFE/aggregate parity summary with unavailable native fields clearly marked;
14. standalone clean-room/dependency/public-API evidence;
15. new standalone oracle SHA on PASS;
16. material mismatch / unresolved residual / source-environment lists;
17. explicit empirical-stationary-zero ledger;
18. tests/checks;
19. forbidden-operation check;
20. rollback status if not PASS;
21. Git closeout;
22. exactly one recommended next gate.

## 17. Terminal verdicts and freezes

Full PASS only if every required repair/revalidation gate passes:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_PASS`

On PASS freeze:

- `MATLAB_FAITHFUL_RAW_VB_TRANSFER_FOC_SOURCE_ORDER_REPAIR_ACCEPTED`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_STANDALONE_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

Material parity failure:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_MATERIAL_MISMATCH`

Infrastructure/evidence blocker:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_BLOCKED`

Owner/source-provenance blocker:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_OWNER_PROVENANCE_REQUIRED`

A PASS here restores affected household/oracle authority only. It does not itself accept MP4 multi-province stationary parity.

## 18. Next-stage boundary

On full PASS, recommend exactly one next gate:

**fresh Python-only corrected calendar-2009 multi-province stationary one-shot against the immutable preserved MATLAB baseline, with MATLAB multi-province rerun remaining zero.**

Do not automatically execute it in this task.

On MATERIAL/BLOCKED, recommend only the smallest repair/evidence successor for the first failed gate.

## 19. Git closeout

Use explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree.

If candidate production source was rolled back after failure, verify those restored blobs exactly against task-authority identities before push.
