# MP4C call-725 first-iteration comparison closure

Date: 2026-09-07
Task: CH5_MP4C_CALL725_FIRST_ITERATION_CLOSURE
Repository: zcx369658780/dissertation-ch5-two-asset-hank
Issuer: ChatGPT scientific reviewer/task issuer
Owner: final scientific authority
Builder: Codex
Publication baseline: eebc4728865bae99691e18a9f127937958d4f41f (record fresh main at start).

## 1. Complete work unit
Close the complete exact-input first HJB iteration comparison for 2018 Anhui call 725. In this one task: recover relevant existing artifacts, align equivalent comparison stages, repair permitted wrapper/comparator plumbing, run focused checks, use the conditional acquisition budget only when necessary, diagnose all available stages, and publish evidence and a report.
Do not split serialization, path, shape, static checks, manifest or report repair into new tasks.

The accepted 40/40 raw-vb finding is a pre/post-boundary persistence mismatch. It does not justify production derivative repair. The comparison contract is already decided: boundary-adjusted vb_f/vb_b consumed by policy are primary; pre-boundary arrays are separately named diagnostics. No Owner naming choice remains.

## 2. Workspace and authority
Prefer the already synced worktree:
D:\ProjectTemp\ch5-astra-local-doc-sync-20260907-001

Its accepted docs-only branch checkpoint is d2f3e6e7cc21fffe8807f577ec2262bb77afdc07. Preserve it.
The original D:\ResearchCode\dissertation-ch5-two-asset-hank has 70 untracked files and is not the work target.
D:\Zotero-Analytical-Workflow may be the chat's starting project; it is not the target repository. Explicitly use the Chapter 5 working directory for every repository command. Existing platform/global instructions still apply; do not edit them or evade workspace access controls.

Verify remote, fetch, and create a dedicated branch from fresh origin/main, preferably in the existing clean synced worktree. Suggested branch: codex/ch5-call725-first-iteration-closure-20260907. A separate worktree is authorized only if needed for clean isolation; do not create another merely by habit.
Do not discard user changes or reset/clean/stash/force-push. If ordinary non-force synchronization has conflicts, assess them; unrelated changes are not an automatic blocker.
This task must be active on live main. Completed/superseded historical tasks supply evidence, not reusable budgets.

Read:
- AGENTS.md and project_rules/PROJECT_RULE_INDEX_CURRENT.md
- project_rules/PROJECT_RULE_DISSERTATION_CH5_ROUTE_CURRENT.md
- relevant workflow, local-file and MATLAB/Python rules
- docs/CH5_TWO_ASSET_HANK_PROJECT_STATUS_CURRENT.md
- docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_RAW_LIQUID_DERIVATIVE_BOUNDARY_ROOT_CAUSE_FORENSIC_REPORT.md
- docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_REPORT.md
- predecessor task CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION.md only for the frozen evaluator, input and artifact bindings.

No full historical-chain re-audit is required.

## 3. Frozen inputs and evidence
Preserved stage artifacts:
- matlab_core_stagewise.mat SHA-256 B863BBE5A3CF6327954C71520F609B9949FE9DEF64BA5225AA4CE9661392B69E
- python_strict_stagewise.npz SHA-256 CEAE235AC0DC62C1FCD0D9728F840DEEA1E12038ABFCE73CA0DA6BD449CE915F

Known evidence roots, read-only:
- D:\ProjectTemp\ch5-mp4c-2018-call725-postcall-residual-vectorization-repair-20260904-001
- D:\ProjectTemp\ch5-mp4c-2018-call725-zero-science-evidence-closure-20260904-002
- D:\ProjectTemp\ch5-mp4c-2018-call725-raw-liquid-derivative-root-cause-20260904-001

Use their manifests to find the exact wrappers and input files, not an all-drive search.
Authoritative initialization MAT SHA-256:
1718984CB588AE586F74AB8476C57AF849BB2C80CC95500329D29BC14207BB81
Frozen scalar-binding SHA-256:
A40D088C63FC1F7EDECEA561D649B42959C646DF528ED13298014493DB4808F6

Production Python exports/matlab_faithful_two_asset_ha.py Git blob:
9e7dc9556a2b76811e78f89999abecc045886106
Protected HANK_2ASSETS_HJB.m SHA-256:
049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE

For fresh acquisition only, direct-load exact MAT b, ah, z, v0 and l0; do not recreate from linspace, rounded text or native initialization. Use the certified scalar binding, not these descriptive decimals as a replacement:
province Anhui/index 11, outer iteration 24, call 725; rah=.09, rb=.02, rb_gap=.07, tau=.05, w=16.82014806560587, Tt=.1; rho=.05, gamma=2, phi=5 (frisch_l=.2), chi0=.1, chi1=2, a_bar=1e-6, Delta=1000, crit=1e-7, drift tolerance=1e-12; switch matrix [[-1/3,1/3],[1/3,-1/3]]. Grid shape (20,20,2), MATLAB/F-order mapping.
Other consumed constants and scientific expressions must match the certified predecessor wrapper. Source/parameter conflicts are not fixable through invented defaults.

Hash relevant source/input/core-output identities once at use. Reuse unchanged prior source mapping and path equivalence evidence. A missing unrelated receipt or cache inventory must not block usable scientific evidence.

## 4. Align stages and compare existing artifacts first
Persist an explicit MATLAB-field -> stage -> Python-field map. Determine pre/post-boundary semantics from actual wrapper capture locations.
Keep original artifacts intact. New comparator views can relabel fields without modifying values.
Primary vb uses post-boundary objects. Raw/pre-boundary vb is a diagnostic; compare defined interior quotient domains, label uncomputed boundary slots, and record the already-explained historical 40/40 mismatch.
Do not silently drop any real scientific stage or manufacture a missing operand.

Compare every available stage in causal order:
1. common old/V0 and input identity;
2. va derivatives at equivalent stages and boundary-adjusted vb derivatives;
3. consumed categorical policy labels and equivalent branch masks when present;
4. consumption, labor, transfer, adjustment cost, effective illiquid return;
5. mu_a, mu_b, utility;
6. bb, bf, ab, af;
7. BB, AAH, Bswitch, A;
8. M=(1/Delta+rho)I-A and RHS;
9. tensor-form V1 and first-iteration statistic.

Record required-field completeness separately from equality. If an internal mask does not exist on one implementation, document source-equivalent branch meaning rather than fabricating an array; consumed categorical labels remain required.
For each compared object record shape/order, finite status, material mismatch count, maximum absolute and scaled differences, and representative coordinates when needed.
Complete downstream comparisons on existing outputs even when an earlier stage fails; identify the earliest genuine mismatch without letting downstream agreement erase it.
All required scientific stages must be available and pass for full first-iteration parity; explained optional raw diagnostic differences do not by themselves fail that verdict.

## 5. Fixed tests and diagnostic interpretation
- Categorical labels, masks with the same meaning, shapes and order: exact.
- Continuous values: abs(x-y) <= 128*eps64*max(1,abs(x),abs(y)).
- Sparse support: ignore exact stored 0/-0 only, no epsilon pruning. Compare mathematical coordinates and values using the same frozen rule.
- NaN/Inf cannot pass by default.
- MATLAB vec uses (:); Python uses F-order. Representation normalization cannot alter scientific indexing.
- If M and RHS pass but V1 fails, report direct-solve/conditioning divergence rather than an equation defect.
- Compute residual ||M*vec_F(V1)-rhs||_inf and normwise backward error from persisted arrays. Matrix multiplication and deterministic postprocessing are allowed without fresh solve; optional expensive condition-estimation solves are not authorized.
- Do not enlarge tolerances, regularize, switch solvers or tune parameters to get PASS.

Where a genuine difference remains, use source reads and persisted-operand arithmetic to locate its layer. Produce a concrete bounded repair recommendation, but do not modify production science under this task.

## 6. Conditional acquisition and retries — already authorized
Default new model calls are zero: use existing durable outputs if sufficient.
Only if a REQUIRED first-iteration scientific object is absent/corrupt or provenance cannot bind that output to the frozen evaluator, and it cannot be recovered through deterministic postprocessing, acquire that language's missing output with a fresh source-faithful one-iteration wrapper.
Input/evaluator identity must be available and valid first. Identity conflicts or an unexplained contradictory intact result do not authorize a retry to seek a different outcome.

New independent budget under this task:
| Language | Initial necessary acquisition | Additional retry | Maximum invocations |
| --- | ---: | ---: | ---: |
| MATLAB source-extracted first iteration | 1 | 1 | 2 |
| Python exact-MAT first iteration | 1 | 1 | 2 |

The additional invocation is allowed only after a diagnosed external instrumentation, IO, process or launch failure that prevented a valid required output, with the same frozen scientific computation. Count every invocation that enters the scientific evaluator, successful or failed. Do not spend the retry merely because numerical parity fails.
Each invocation executes exactly one HJB iteration and its one direct value-update solve; no iteration 2, full HJB convergence loop or post-loop KFE. No standalone diagnostic re-solves.
Per-invocation timeout: 15 minutes; overall task time budget: 2 hours. A timed-out scientific call is consumed; retain partial evidence and use a retry only when the external-failure condition is demonstrated.
Ordinary static/import/IO/shape checks and pure persisted-array comparisons can be repeated within the task time budget. They are not model calls. No global regression suite is required.
Native-init probe, KFE, household steady state, GE/annual, R/PLM, shock/IRF and Results calls: zero.
Historical MATLAB/Python pair budgets and the earlier unpersisted MATLAB invocation remain consumed; this is not a ledger reset.

Before a needed fresh call, inspect only the changed wrapper diff, confirm scientific expressions and inputs are preserved, and run relevant shape/serialization smoke. One compact receipt suffices; no separate zero-science certification task or exact ceremonial marker.
Save the core stage container immediately after the update, before optional diagnostics. If core persistence succeeds, fix later diagnostic problems using it instead of rerunning.
If only one side is missing, regenerate only that side; reused and new sides must share exact inputs/evaluator semantics.

## 7. Allowed paths and repair scope
Repository additions/edits:
- validators/multi_province/call725_first_iteration_closure/ (new wrapper/comparator utilities; no production import dependency)
- tests/test_call725_first_iteration_closure.py (focused comparator/IO tests only if useful)
- docs/CH5_MP4C_CALL725_FIRST_ITERATION_CLOSURE_REPORT.md
- reports/call725_first_iteration_closure_20260907/ (small JSON/CSV/Markdown summaries only)

New external work/evidence root:
D:\ProjectTemp\ch5-call725-first-iteration-closure-20260907-001
Choose a new suffix if occupied. Preserve previous roots.
Within this task workspace, scripts and drafts may be revised; preserve final sources and the exact versions used in any scientific invocation.
Allowed repairs: binding to the certified files, path equivalence, imports, field/stage names, F-order diagnostic reshape, serialization/persistence order, comparator parsing and report/manifest completion.
Protected MATLAB, existing production exports/src/validators outside the allowlist, economic equations, KKT/boundary law, operator arithmetic, solver semantics, calibration, grids and scientific tolerances remain unchanged.
Do not repair a genuine policy/operator/formula discrepancy under the label of instrumentation.

## 8. Evidence, result and publication
Deliver one report plus executable minimal comparator/wrapper changes when needed, stage comparison summary, and one finite manifest binding substantive sources/inputs/outputs. No raw large MAT/NPZ, private data, cache dump or recursive manifest hashing in Git.
Document whether comparisons used old or newly acquired outputs and report budget used/remaining per language.
Use one outcome:
- FIRST_ITERATION_PARITY_PASS
- FIRST_ITERATION_MATERIAL_MISMATCH (earliest genuine stage and repair recommendation)
- FIRST_ITERATION_EVIDENCE_INCOMPLETE (specific missing prerequisite/field)
A useful mismatch or incomplete report is a completed diagnostic deliverable, not a false scientific PASS.
No production patch, multi-iteration acceptance, annual acceptance or Results claim follows from this report.

Run only relevant checks; stage explicit allowed paths; commit and non-force push the dedicated task branch. Verify changed paths and remote commit once. Do not merge main or edit current state as if reviewer acceptance already occurred.
Return outcome, key stage results, exact local/evidence root, new model-call counts, files, checks and commit/branch.
Reviewer has standing Owner authorization to accept the report and publish the next bounded task without another routine confirmation when no substantive decision remains. Builder stops after this task's publication.
