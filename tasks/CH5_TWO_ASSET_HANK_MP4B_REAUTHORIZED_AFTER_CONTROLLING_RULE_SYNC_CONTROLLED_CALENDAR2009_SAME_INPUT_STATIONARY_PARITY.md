# CH5_TWO_ASSET_HANK_MP4B_REAUTHORIZED_AFTER_CONTROLLING_RULE_SYNC_CONTROLLED_CALENDAR2009_SAME_INPUT_STATIONARY_PARITY

Date: 2026-08-30
Repository: `zcx369658780/dissertation-ch5-two-asset-hank`
Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer
Executor: Codex bounded Builder / parity-run executor / first-divergence diagnostician
Owner: final scientific authority

## 1. Purpose

Reauthorize the previously blocked MP4B controlled calendar-2009 MATLAB-Python stationary parity task **without changing its scientific scope, numerical contract, call budget, comparison hierarchy, or repair boundary**, after publication of the two controlling rule files that were missing from live `main`.

The prior execution stopped correctly before any scientific/model call with:

`MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

Blocker classification:

`REQUIRED_CONTROLLING_RULE_FILES_MISSING_FROM_LIVE_MAIN`

Prior blocker report commit:

`3ce40ad9e5e32886232a6e5e2819e8db2f68736e`

Scientific/model calls consumed by that blocked attempt: **zero**. Therefore the original MP4B scientific budget remains entirely unused and is reauthorized here exactly once.

## 2. Incorporated scientific execution contract

The complete prior task is incorporated by reference and MUST be read in full before execution:

`tasks/CH5_TWO_ASSET_HANK_MP4B_CONTROLLED_CALENDAR2009_SAME_INPUT_MATLAB_PYTHON_STATIONARY_PARITY_AND_BOUNDED_DIVERGENCE_DIAGNOSIS.md`

All of its scientific requirements remain controlling, including but not limited to:

- corrected/decoupled calendar-2009 identity;
- MP4A2 canonical SHA and source identities;
- presolver same-input identity gate;
- MATLAB-first ordering;
- maximum one corrected MATLAB stationary top-level run;
- maximum one corrected Python stationary top-level run;
- no scientific rerun;
- complete iteration/final trace persistence;
- layer-by-layer parity hierarchy;
- first-divergence localization;
- bounded root-cause classification;
- no automatic formula repair, tolerance loosening, or repair-and-rerun;
- no wrong-year MATLAB route;
- no 2010-2023 batch;
- no shocks, transition, genuine dynamics, IRF Results, legacy R5, or manuscript Results.

Where this reauthorization task and the prior task differ, this file controls only **live continuity, controlling-rule availability, restart authority, and report/closeout identity**. The prior task continues to control the scientific execution itself.

## 3. Controlling rule repair now present on live main

The following required rules have now been published and MUST be read in full:

### Local file safety rule

Path:

`project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`

Publication commit:

`996a09f7f8e9861ca81c50415c35a017293d4bd9`

Expected SHA-256 of the governing text:

`CB36206D6FF12357F5AE4CEC7D8935BD0B1F8B5DB3051128FC83ABE8A9755BF6`

### MATLAB/model diagnostic gates rule

Path:

`project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`

Publication commit:

`d7560e48cb7aa4ddb0465f26e88fdfa0489d29af`

Expected SHA-256 of the governing text:

`82E5CD80361A356D783AFD3F0A28244F8A6893BC750461E4D38AE3E766D0AA1C`

These rules are controlling authority, not optional context.

## 4. Live continuity and restart gate

Expected execution-start parent immediately before this reauthorization task publication:

`d7560e48cb7aa4ddb0465f26e88fdfa0489d29af`

At start, Codex MUST:

1. fresh-fetch `origin/main`;
2. confirm this exact reauthorization task is live on `main` after the two rule-publication commits;
3. verify the prior blocked report commit `3ce40ad9e...` is an ancestor;
4. verify the accepted MP4A2 commit `85772bc6920db58cd6ec38bf8e1d7a5d593e12fc` is an ancestor;
5. read both newly published rule files and verify their content identities;
6. read the complete prior MP4B task in full;
7. require a clean worktree before any execution;
8. re-verify all MP4A2 canonical/workbook/regression/distance/cache identities and accepted MP1-MP3 source identities exactly as required by the prior task;
9. verify the accepted standalone HA oracle and MP4A2 MATLAB wrapper hashes;
10. verify no historical `chapter5_model` runtime import exists.

If any requirement fails, stop `MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_BLOCKED` before scientific execution.

## 5. Required presolver gate

The prior blocked attempt did not establish presolver equality. Restart from the beginning.

Before either scientific run, the task MUST establish:

`MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`

using the full manifest equality contract in the prior MP4B task.

No prior blocker evidence may be treated as a partial scientific pass.

## 6. Scientific execution budget — reauthorized, not expanded

Because the previous attempt consumed zero scientific/model calls, this task reauthorizes exactly the original unused budget:

- corrected/decoupled calendar-2009 MATLAB stationary route: **maximum 1 top-level scientific invocation**;
- corrected/decoupled calendar-2009 Python stationary route: **maximum 1 top-level scientific invocation**;
- scientific reruns after either invocation: **0**.

Internal household HJB/KFE calls within the single authorized top-level stationary invocation remain governed by the prior MP4B task and source controller semantics.

Ordering and failure rules remain unchanged:

- presolver identity failure => run neither language;
- MATLAB first;
- infrastructure failure before a scientific MATLAB attempt => do not consume Python run, stop BLOCKED;
- scientific MATLAB nonconvergence/model-domain failure => preserve it and proceed to the single Python run for comparison;
- after Python run => no automatic scientific rerun.

## 7. Local safety and no-overwrite requirements

Obey the newly restored rule files strictly.

In particular:

- protected MATLAB source remains read-only;
- treat `C:\MatlabProgram` and resolved `D:\MatlabProgram` as one high-risk boundary;
- record logical and resolved physical paths when safely checkable;
- check free space before any `.mat`-writing scientific run;
- create new timestamped no-overwrite MATLAB and Python run roots;
- do not overwrite the prior blocked report or any previous run artifact;
- do not commit raw/private workbooks, MAT scientific outputs, figures, large logs, caches, or secrets.

## 8. Bounded divergence diagnosis

If the controlled comparison reaches a mismatch, retain the prior Owner-authorized diagnosis boundary.

Localize the first divergent object/stage and classify only as one of:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

Diagnosis may read source, manifests, persisted intermediates, and code needed to explain the first divergence. It does not authorize scientific repair or rerun.

## 9. Required report and repository changes

Write a new report; do not overwrite the blocker report:

`docs/CH5_TWO_ASSET_HANK_MP4B_REAUTHORIZED_AFTER_CONTROLLING_RULE_SYNC_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_REPORT.md`

The report MUST contain all fields required by the original MP4B task plus:

- prior blocker commit and classification;
- rule-publication commits and content identities;
- restart continuity proof;
- presolver same-input terminal;
- scientific call ledger showing prior blocked attempt `0/0` and current authorized consumption;
- MATLAB/Python run roots if created;
- convergence and iteration counts;
- layer-by-layer parity table;
- qualitative sign/ranking diagnostics only if scientific outputs exist;
- first divergence and root-cause classification if any;
- forbidden-operation check under the restored rules;
- Git closeout.

Authorized repository writes are the same as the original MP4B task, except the report path above replaces the prior report path for this reauthorized attempt. Any runtime bridge, validation helper, trace instrumentation, tests, or text-first manifests must remain within the original MP4B allowed scope.

Do not modify the two newly published controlling rule files during execution.

## 10. Terminals

Use the original MP4B scientific terminal semantics.

PASS:

`MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_PASS`

Material scientific mismatch / unresolved first divergence:

`MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_MATERIAL_MISMATCH`

Infrastructure/authority/source-environment blocker:

`MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

A PASS is limited to the corrected calendar-2009 stationary parity object. It does not authorize shocks, 2010-2023 batch execution, dynamics, or Results.

## 11. Closeout

Use explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.

On PASS, recommend exactly one next gate: source-law/named-response preparation for the corrected 2009 baseline, without automatically executing shocks.

On MATERIAL or BLOCKED, recommend exactly one bounded successor addressing the localized cause. Do not widen scope.
