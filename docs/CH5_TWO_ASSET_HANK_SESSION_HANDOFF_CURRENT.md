# Chapter 5 当前交接 — interior zero-liquid Z adopted / Option-A reexecution active

更新：2026-09-16。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
状态：`INTERIOR_ZERO_LIQUID_Z_OWNER_ADOPTED__OPTION_A_REEXECUTION_ACTIVE__PRODUCTION_UNCHANGED`。
Results eligibility=`FALSE`。

## Governance

Owner is final scientific authority; ChatGPT is L3 Reviewer/scientific-route authority; Codex is bounded Builder. GitHub live `main` is repository-state authority. `deep-learning-hank` is a separate project.

## Accepted state

Option A remains the Owner-selected seed. The lower-a zero-kink multiplier repair is accepted. Fresh Option-A execution passed Cells 0–4 and failed at Cell 5 because the backward liquid derivative implied forward drift while the forward derivative implied backward drift.

The zero-science forensic candidate `fdfabfb8ba12fa52add525b68050de67c2d6369d` is accepted. It proves a unique finite positive zero-liquid shadow at Cell 5, strictly between the raw one-sided liquid shadows, and documents historical/source-faithful `I0` plus accepted-Python-HJB `Z` precedent.

## Owner decision now adopted

Owner explicitly chose to inherit the interior zero-liquid `Z` switching law into the corrected D1-D3 selector.

The Z candidate is allowed only at interior liquid states with strict backward/forward direction crossing and a unique finite positive zero-drift shadow within the one-sided-shadow interval. Controls/a-side objects are recomputed from that root under the same frozen D1/D3 laws; Z then enters the normal admissibility and Hamiltonian comparison. No averaging, interpolation, floor, clipping, fitted tolerance or D2 relaxation is authorized.

The fresh one-map root ceiling is 3144 total: prior boundary-liquid ceiling 264 plus at most 2880 interior-Z roots (`720 interior-b cells * <=4 eligible a-side/transfer combinations`).

## Active task

`tasks/CH5_MP4C_2018_KFE_D123_INTERIOR_Z_SWITCHING_REPAIR_AND_OPTION_A_REEXECUTION_20260916.md`

Builder must implement the generic Z branch, complete focused preflight, freeze scientific code, then restart Option A from Cell 0. First failed cell stops. Budget: <=800 selectors, <=3144 roots total, interior-Z <=2880, D2<=1 only after 800-cell PASS, direct HJB solve<=1 only after D2 PASS, retries=0.

No second policy map, nonlinear continuation, KFE, MATLAB, outer/firm/wage-return, GE/annual/shock/IRF/Results calls are authorized. Production replacement remains unauthorized.
