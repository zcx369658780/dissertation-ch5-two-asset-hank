# Task — V2 cell185 simultaneous two-axis zero-drift switching zero-science scientific adjudication

Date: 2026-09-19

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Task ID:
`CH5_MP4C_2018_KFE_D123_V2_CELL185_SIMULTANEOUS_TWO_AXIS_ZERO_DRIFT_SWITCHING_ZERO_SCIENCE_ADJUDICATION_20260919`

## Governance

This task is a Reviewer-authorized **zero-science scientific-design adjudication** of the first new failure exposed after the Owner-adopted interior-`a` switching law closed cell100.

Owner remains final scientific authority. ChatGPT is L3 Reviewer/scientific-route authority. Codex is bounded Builder.

Never enter, read, search, use or modify:
`zcx369658780/deep-learning-hank`.

Fresh-fetch live `origin/main`. Read in order:

1. `AGENTS.md`
2. `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
3. `docs/CH5_TWO_ASSET_HANK_PROJECT_STATUS_CURRENT.md`
4. `docs/CH5_TWO_ASSET_HANK_SESSION_HANDOFF_CURRENT.md`
5. `docs/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_IMPLEMENTATION_AND_V2_CHECKPOINT2_REEXECUTION_ACCEPTANCE_20260919.md`
6. its execution report and exact cell185 receipt
7. `docs/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_OWNER_ADOPTION_20260919.md`
8. `docs/CH5_MP4C_2018_KFE_D123_INTERIOR_ZERO_LIQUID_Z_OWNER_ADOPTION_ACCEPTANCE_20260916.md`
9. accepted D1/D2/D3, lower-`a` zero-kink, selector/upwind and nonlinear-convergence authority as needed.

This task does **not** adopt a new law. It determines whether a simultaneous interior-liquid and interior-`a` zero-drift switching branch is mathematically/scientifically supported strongly enough to return to Owner for explicit adoption.

## Frozen starting facts

Accepted V2 SHA-256:

`A85AB791D7CFC3B0BDA52D886418B9552D79824E09EACE6B6E4A4BEC8F950DF1`.

Failing cell:

- flat F index `185`
- zero-based `(i_b,i_a,i_z)=(5,9,0)`
- physical `(b,a,z)=(-0.1578947368421053,4.7368421052631575,0.8)`
- both `b` and `a` are interior nodes
- no asset-boundary active set.

Persisted derivatives:

- `p_b^B=0.013362109688537174`
- `p_b^F=0.009574726769001294`
- `p_a^B=0.00857557540065246`
- `p_a^F=0.008489317330830281`
- `r_a=0.08998919455428576`.

Existing liquid-`Z` negative-transfer candidates:

### backward-a liquid-Z
- `q_a=p_a^B=0.00857557540065246`
- `q_b=0.011845651796693632`
- canonical `g_b=0`
- `g_a=+0.009287240997760404`
- rejected as `A_DERIVATIVE_DIRECTION_INCONSISTENT`.

### forward-a liquid-Z
- `q_a=p_a^F=0.008489317330830281`
- `q_b=0.011826220238545775`
- canonical `g_b=0`
- `g_a=-0.005170298666228812`
- rejected as `A_DERIVATIVE_DIRECTION_INCONSISTENT`.

Cell185 outcome is `NO_ADMISSIBLE_POLICY`. The current Owner authority intentionally prohibits a simultaneously newly-created interior-`a` switch plus liquid-`Z` switch.

## Central scientific question

When both asset dimensions are interior and the already-adopted liquid-`Z` candidates associated with the two one-sided `a` derivatives create a strict backward-positive / forward-negative `g_a` crossing, is the correct monotone/upwind Hamiltonian closure a **joint zero-drift candidate** satisfying simultaneously

`g_b=0`

and

`g_a=0`,

with a pair of endogenous shadows `(q_b,q_a)` lying inside the closed rectangle generated by the one-sided derivative intervals and linked by the unchanged D3 transfer KKT?

Do not assume that composing the two already-adopted one-axis switching laws sequentially is valid. The task must determine whether a genuinely joint law is required and well-defined.

## Required derivation

Use only accepted equations, static source/docs, persisted receipts and direct scalar algebra.

### A. Reconstruct the two-axis switching geometry

1. Write the frozen local Hamiltonian and drifts `g_b,g_a`.
2. State the accepted one-axis liquid-`Z` and interior-`a` switching triggers and explain why neither currently authorizes a simultaneous shadow pair.
3. Show precisely what cell185 evidence establishes:
   - liquid one-sided B/F crossing for each relevant `a` endpoint where applicable;
   - after liquid-`Z`, backward-`a` gives `g_a>0` while forward-`a` gives `g_a<0`.
4. Explain whether this is the two-dimensional analogue of a strict upwind crossing or whether the coupling makes that analogy insufficient.

### B. Derive the joint zero-drift candidate

If mathematically justified, impose

`g_a=0`,

hence

`d_{ZZ}=-r_a a`.

Determine the transfer regime from the sign of `d_{ZZ}`.

Under unchanged D3 derive the exact relation

`q_a = k(q_b) q_b`

or its scalar constant-ratio form for this nonzero transfer.

Require simultaneously:

- `q_a` lies in the closed sorted interval between `p_a^F` and `p_a^B`;
- `q_b` lies in the closed sorted interval between `p_b^F` and `p_b^B`;
- `q_b>0`;
- all unchanged D3/domain/finite conditions hold.

Map the `a`-shadow interval through D3 into an implied `q_b` interval and intersect it with the liquid derivative interval. Determine statically whether the intersection is empty or nonempty.

### C. Joint liquid equality

For fixed `d_{ZZ}`, write the frozen liquid equality

`g_b(q_b,d_{ZZ})=0`.

On the joint admissible `q_b` interval:

1. establish continuity;
2. derive the derivative/monotonicity sign where possible;
3. evaluate the two interval endpoints by direct scalar substitution;
4. classify root existence/uniqueness as:
   - statically unique;
   - statically absent;
   - or unresolved without a scientific root call.

Do not execute a root routine.

If a unique `q_b^*` is supported, define only algebraically `q_a^*=k q_b^*`; do not numerically solve for the root beyond direct allowed scalar arithmetic unless the task's zero-science rules permit a closed-form expression.

### D. Hamiltonian/upwind legitimacy

Determine whether a joint `g_b=g_a=0` candidate is compatible with the monotone finite-difference Hamiltonian:

- both transport terms vanish;
- shadows lie in the appropriate one-sided derivative intervals;
- controls are reconstructed from shadows rather than interpolated;
- D3 KKT is exact;
- no boundary multiplier is involved at cell185.

Address whether a joint shadow pair should be interpreted as:
- a legitimate two-dimensional Godunov/viscosity switching closure;
- merely sequential reuse of one-axis rules;
- or a new coupled subgradient selection requiring additional mathematical authority.

### E. D2 consequence

If such a candidate were later adopted and selected, state exactly how unchanged D2 would consume `g_b=0` and `g_a=0`: no asset-transition rate on either asset axis, with no artificial diffusion or leakage repair.

Do not assemble Q.

### F. Generality and trigger design

If scientifically supported, propose a prospective generic contract for Owner review only. It must specify:

- interior-interior node requirement;
- same active-set/transfer-regime requirement;
- evidence needed before triggering a joint switch;
- both derivative intervals;
- D3 relation;
- joint interval intersection;
- exact zero-drift equations;
- root uniqueness/fail-closed rule;
- no averaging/interpolation;
- Hamiltonian comparison;
- receipt fields;
- interaction with existing ordinary, one-axis liquid-`Z`, one-axis interior-`a`, and lower-`a` zero-kink laws;
- precedence/deduplication rules so that a joint candidate is not double-counted as two sequential one-axis candidates.

Do not implement it.

### G. Alternative explanations

Assess without runtime:

- grid-spacing/discretization dependence;
- V2 trajectory/initialization dependence;
- whether the issue is specific to cell185 or generic for coupled two-asset upwinding;
- whether a refined grid could remove the crossing without invalidating the prospective law;
- whether the absence of asset boundaries here weakens a finite-domain-boundary explanation.

Do not turn these into an unapproved calibration/grid task.

## Required primary classification

Return exactly one:

- `ADJUDICATED__SIMULTANEOUS_TWO_AXIS_ZERO_DRIFT_SWITCHING_SCIENTIFICALLY_SUPPORTED__OWNER_ADOPTION_REQUIRED`
- `ADJUDICATED__SIMULTANEOUS_TWO_AXIS_SWITCHING_NOT_SUPPORTED__CELL185_FROZEN_INCOMPATIBILITY_STANDS`
- `ADJUDICATED__JOINT_SWITCHING_MATHEMATICALLY_PLAUSIBLE_BUT_SELECTION_LAW_UNDERDETERMINED__OWNER_THEORY_DECISION_REQUIRED`
- `BLOCKED__ACCEPTED_AUTHORITY_INSUFFICIENT_TO_ADJUDICATE_TWO_AXIS_SWITCHING`
- `BLOCKED__PERSISTED_EVIDENCE_INSUFFICIENT_FOR_STATIC_TWO_AXIS_ADJUDICATION`

Do not invent another primary marker.

## Zero-science ledger

All must remain exactly zero:

- selector evaluations;
- ordinary scalar roots;
- liquid-`Z` roots;
- interior-`a` switching roots;
- proposed simultaneous-switch roots;
- policy maps;
- D2/Q;
- Bellman/checkpoint diagnostics;
- HJB solves/updates;
- graph/SCC;
- KFE/SVD/eigen/nullspace/`Q.T@p`;
- MATLAB;
- outer/firm/wage-return/GE/annual/shock/IRF/Results;
- scientific retries.

Allowed:

- static Git/source/doc reads;
- persisted JSON parsing;
- hashing;
- symbolic derivation;
- direct scalar arithmetic/substitution of persisted values.

No selector/model/test/helper module may be executed as a scientific evaluator.

## Deliverable

Write exactly one report:

`docs/CH5_MP4C_2018_KFE_D123_V2_CELL185_SIMULTANEOUS_TWO_AXIS_ZERO_DRIFT_SWITCHING_ZERO_SCIENCE_ADJUDICATION_REPORT.md`

The report must contain the authority inventory, full derivation, cell185 interval/root proof, general prospective contract if supported, alternative-explanation assessment, zero-call ledger and exactly one primary classification.

Commit and ordinary non-force push one task branch.

Do not merge main.
Do not publish a successor task.
Do not modify CURRENT files.
Do not adopt the joint law on behalf of Owner.

Results eligibility remains `FALSE`.
