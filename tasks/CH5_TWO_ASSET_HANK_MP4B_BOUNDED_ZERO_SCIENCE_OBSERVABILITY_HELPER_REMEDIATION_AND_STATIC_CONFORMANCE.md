# CH5_TWO_ASSET_HANK_MP4B_BOUNDED_ZERO_SCIENCE_OBSERVABILITY_HELPER_REMEDIATION_AND_STATIC_CONFORMANCE

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / zero-science observability helper remediator

Owner: final scientific authority

## 1. Explicit Owner authorization

The Owner explicitly authorizes this bounded task:

> 同意授权一次 bounded zero-science observability-helper remediation：仅修复已审计确认的 observer container 初始化、event accounting 与必要的 trace persistence；禁止修改任何科学方程、参数、网格、算法、controller、容差、调用顺序与 MATLAB/Python scientific code；MATLAB processes=0，不授权 stationary rerun。

This authorization permits implementation of the already audited observability-only remediation in a fresh external diagnostic package. It does not authorize any MATLAB process or scientific/model execution.

The Owner also grants the L3 reviewer a standing delegation for later analogous simple rerun/debugging decisions in this project to accelerate progress. This delegation is bounded to mechanical diagnostic/infrastructure decisions that do not introduce a new scientific choice and do not modify equations, calibration, grids, solver/model algorithms, controller semantics, comparison criteria, shock design, transition/IRF design, Results claims, or scientific route. The L3 reviewer may publish a precise GitHub task for such bounded mechanical debug/rerun work without another Owner round-trip, provided the task itself states an explicit finite run/retry budget and preserves all existing scientific authority. Material scientific changes remain Owner decisions.

## 2. Required live continuity

Required execution-start predecessor:

`b35e571030b58ea07a5603a87f109efaaab6f006`

Accepted predecessor terminal:

`MP4B_ZERO_SCIENCE_OBSERVABILITY_HELPER_INITIALIZATION_AUDIT_READY_FOR_OWNER_REAUTHORIZATION_REVIEW`

Accepted predecessor classifications/markers:

- `MP4B_OBS_HOUSEHOLD_INPUT_CONTAINER_INITIALIZATION_TYPE_MISMATCH_CONFIRMED`
- `MP4B_OBS_MULTIPLE_STATIC_SCHEMA_DEFECTS_LOCALIZED`
- `MP4B_OBS_ALLOCATION_DERIVED_HOUSEHOLD_CALL_COUNT_SEMANTIC_DEFECT_CONFIRMED`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `b35e571030b58ea07a5603a87f109efaaab6f006`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`;
   - predecessor audit task and report;
   - predecessor Owner-reauthorized one-shot task/report;
   - zero-science copy-binding remediation task/report.

Any authority or immutable-identity failure => stop before mutation and report BLOCKED.

## 3. Hard zero-science budget

For this task all of the following are exactly zero:

- MATLAB processes;
- MATLAB `checkcode`;
- MATLAB stationary/HJB/KFE/household/firm/controller calls;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific calls;
- comparator replay / `compare_terminal`;
- Zhejiang/Shanxi scientific replay;
- other year/batch runs;
- shocks/AR1/transition/dynamics/IRF;
- R5/Results.

Allowed execution is limited to filesystem/text/hash operations and non-MATLAB static conformance harnesses that do not import or invoke scientific/model modules.

This task does not authorize a stationary rerun.

## 4. Immutable evidence to verify

Read-only predecessor one-shot root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-owner-reauthorized-20260831-001`

Require exact predecessor identities:

- `source_copy_manifest.json` SHA-256 `757D83D13B32BC92411F687069797A0F3DA4ADFC06FFEBD872B0674DBEDE9961`
- `instrumentation_manifest.json` SHA-256 `511F59C6F6482DC5BBA7EBBFAD5B234C7D31B840AB2DE2CDDC836F4B74A52B2B`
- `instrumentation_patch.diff` SHA-256 `0B4B4D78EF45E33052E0888CC0E4AD8703167DC71DD8FA0D79BD01B5A053BB9F`
- `pre_science_gate.json` SHA-256 `CFA0EE14410F44C9E56D8977D4F1363590CB8AC70E392F62E8CD47404EEF753D`
- `exact_launcher_command.txt` SHA-256 `9F6E9A73CE4DECE261B390567DBD3EEC1BA287502D29D0C9FFB260DD31AB90D0`
- `instrumented_invocation_binding_manifest.json` SHA-256 `50614001EF125A035EB78EB9C796BF3BFEEC6E0017A4206FCBA01402FF63A3F2`
- `instrumented_outer_trace.mat` SHA-256 `E3EC398D6B2A5284A07941D74C4DA6FB5C460BC61D373A860C7D024CD86C7EAF`
- `instrumented_terminal_status.json` SHA-256 `B36EA498862231AF914B9922B1A9581E405EFAE05893BEF8CE4D459B8F1BA5D9`
- `instrumented_trace_summary.json` SHA-256 `F15DBA7B9FF42C7F2169CD7850543FB1DEDDDFE85640BCCFE7D49A2AD7CEE90C`
- `matlab_launcher_stdout_stderr.txt` SHA-256 `40C4EFDC4091569931CA49827AC60AB131BCD424AF69AB54A9772C7B8F7C55E6`
- `run_manifest.json` SHA-256 `AE078A56AF7FA22F87049A037E9151C9D8BEC76153659946AA937C26F82D215E`.

Read-only audit root:

`D:\ProjectTemp\ch5-mp4b-observability-helper-initialization-audit-20260831-001`

Require:

- `observer_event_schema.json` `9BFC25A3C1F61CBD6825E3098A70C90A1466B5B07E691761B93F6AA953F81463`
- `observer_initializer_assignment_matrix.json` `DFE9146A4BE23CD5DD11970D62ED4C1313B88C290717C8E6E060033CCAC0061D`
- `trace_accounting_semantics.json` `4746EFDEA69C2CDFB44527F7BF10D114C6F281361191AE45723632A04F546340`
- `partial_trace_evidence_boundary.json` `37780E69217CF34D16B3AA8D536AAD3723CA4BDE6A42C2DD5A843A3A7CA00844`
- `minimal_remediation_specification.md` `E9CBD7DEA49AFA98D1DAA6AD9027257494499E0D7B2C58DB52C69DA0420AEDB4`
- `audit_manifest.json` `06C0723A0386D6539807ACC530B98A5C87CFF9743688A01E5447C7DE0B63F35E`.

Frozen path-normalized wrapper remains read-only:

- candidate wrapper SHA-256 `DA998FB04C35EE852F53A504D5F4EB17EC089A8EC616A082F38E2B5CB2CD5A93`
- scientific invocation body SHA-256 `80BAEDE65829F6A1215638F556544C92CAAB203A149FA6D1D88196BE22045F45`.

The protected MATLAB tree, canonical input, preserved MATLAB baseline, all Python scientific modules, comparator/helper/contract, field map, calibration, project rules and prior reports are read-only.

## 5. Fresh remediation package

Create exactly one fresh no-overwrite external root under `D:\ProjectTemp`, recommended:

`D:\ProjectTemp\ch5-mp4b-observability-helper-remediation-20260831-001`

If it already exists, use the next deterministic fresh suffix. Never delete, overwrite or reuse a prior diagnostic/scientific root.

The remediation package may contain only observability/instrumentation candidate files, static harnesses, manifests, diffs and documentation. It must not contain modified protected original source from `C:\MatlabProgram`/`D:\MatlabProgram` except a copied diagnostic source snapshot whose provenance/hash is explicit and which remains outside Git.

## 6. Required observer remediation

Start from the exact `MP4B_OBS.m` used by the failed Owner-reauthorized one-shot. Preserve every currently compatible direct whole-field observer assignment and existing source-layer call site unless a change is strictly necessary for the accepted observability/accounting defect.

Repair exactly the audited indexed-struct container family:

- `household_inputs`
- `household_outputs`
- `firm_inputs`
- `firm_preclip`
- `firm_outputs`
- adaptation-path `controller_actions`.

For each field:

- initialize it as a typed empty struct array compatible with the scalar struct producer currently assigned via `field(i)`;
- preserve parenthesis indexing and province order where the audit found indexing correct;
- preserve exact producer field names and source-layer semantics;
- do not substitute cells merely to avoid MATLAB typing unless static evidence proves a cell contract is required;
- do not prefill zero/false values that could be mistaken for observed events;
- absence must remain distinguishable from a recorded event whose values happen to be zero/false.

Do not change direct whole-field fields already classified compatible: `entry`, `household_convergence`, `migration`, `capital`, `composite_wage`, `policy`, `controller`, `damping`, or terminal marker semantics except as explicitly necessary for event accounting/persistence below.

## 7. Event accounting remediation

Remove `numel(trace.turns)*31` or any equivalent allocation-derived quantity from scientific execution accounting.

Implement explicit event-derived counters with unambiguous semantics. At minimum the candidate trace/summary contract must distinguish:

- `scheduled_household_slots`
- `household_call_entries`
- `household_call_completions`
- `firm_call_entries`
- `firm_call_completions`
- `controller_evaluations`
- `low_action_events`
- `high_action_events`
- `zt_reset_events`.

Requirements:

- scheduled slots may be derived from turn/province allocation only if clearly labelled non-execution capacity;
- entries increment only at the actual source-adjacent observer event immediately before the corresponding scientific call/block;
- completions increment only after the scientific call/block returns/completes and its output observer event is reached;
- controller/action/Zt counters derive from actual recorded controller events, never array capacity;
- exception-path summaries must preserve partial counts exactly and must not infer unexecuted events;
- scientific ledgers must use completion counts where they claim completed executions.

`controller_no_adaptation` synthesized `NONE` state snapshots must remain distinguishable from source branch-body action events. They must not inflate low/high action counts.

## 8. Necessary trace persistence only

Review the success and exception persistence paths and make only changes necessary so that a partial/failing run would serialize:

- the trace actually observed up to the failure;
- explicit counters above;
- terminal/error status;
- no preallocated/default slots represented as executed science.

Do not increase persistence frequency inside the scientific loop. No per-event disk IO is authorized. Keep the existing in-memory-observer + terminal/exception serialization design unless the static audit proves a minimal observability-only adjustment is necessary.

Do not change the path-normalized candidate wrapper's scientific invocation body. If an unexecuted future wrapper candidate must be updated solely to serialize the new summary schema, preserve the frozen scientific invocation body SHA-256 `80BAEDE65829F6A1215638F556544C92CAAB203A149FA6D1D88196BE22045F45` exactly and document a wrapper-only persistence diff. Do not execute it.

## 9. Static conformance harness

Because MATLAB processes are forbidden, validate the repaired candidate with a non-MATLAB static conformance harness. It must cover the complete accepted observer schema rather than only the previously failing line.

At minimum verify:

1. all six indexed struct destinations are initialized as struct arrays rather than empty double;
2. each destination's initialized field schema matches the corresponding producer struct schema;
3. indexed assignment mode remains the audited-compatible mode;
4. no compatible whole-field destination is accidentally converted to an incompatible indexed schema;
5. every source observer call site still appears in the same scientific source-order location relative to the protected scientific statements;
6. no additional HJB/KFE/household/firm/equilibrium/model call is introduced;
7. no observer assignment writes back into a scientific variable;
8. no equation, calibration, grid, comparison operator, controller threshold/multiplier, damping coefficient, tolerance, convergence rule or scientific update order changes;
9. summary counters are event-derived according to Section 7;
10. a synthetic partial-event sequence equivalent to the prior failure yields `scheduled_household_slots=31` but `household_call_completions=0`, not 31;
11. missing event remains distinct from recorded zero/false event;
12. exception and success persistence paths expose the same counter schema;
13. no MATLAB executable invocation exists in the harness.

A Python/PowerShell standard-library-only text/schema harness is allowed. Do not import project scientific modules.

Persist the full test matrix, expected/actual results and SHA-256.

## 10. Candidate package invariance evidence

Persist at minimum:

- original failed observer SHA-256;
- repaired observer candidate SHA-256;
- complete observer diff;
- any copied call-site instrumentation files before/after SHA-256;
- complete instrumentation diff;
- static conformance test artifact and SHA-256;
- event/counter schema JSON and SHA-256;
- persistence-schema artifact and SHA-256;
- remediation manifest containing every source/evidence identity.

The diff must demonstrate that changes are confined to observer container initialization, event accounting, and necessary trace persistence.

No repaired candidate may be written into the protected MATLAB original tree or committed as production/scientific code. Keep all candidate MATLAB instrumentation under the fresh external `D:\ProjectTemp` remediation package.

## 11. Readiness classifications

If all audited defects are repaired and static conformance passes, terminal:

`MP4B_BOUNDED_ZERO_SCIENCE_OBSERVABILITY_HELPER_REMEDIATION_AND_STATIC_CONFORMANCE_PASS`

and establish:

`MP4B_OBS_INDEXED_STRUCT_CONTAINER_FAMILY_REMEDIATED_STATIC`

`MP4B_OBS_EVENT_DERIVED_EXECUTION_ACCOUNTING_REMEDIATED_STATIC`

`MP4B_OBS_TRACE_PERSISTENCE_STATIC_CONFORMANCE_PASS`

This is zero-science engineering readiness only. It does not prove MATLAB runtime behavior or baseline admissibility.

If a remaining observer/schema/persistence blocker is found, terminal:

`MP4B_BOUNDED_ZERO_SCIENCE_OBSERVABILITY_HELPER_REMEDIATION_AND_STATIC_CONFORMANCE_BLOCKED`

and do not widen the task.

## 12. Delegated next-routing rule

The Owner's standing delegation in Section 1 permits the L3 reviewer, after independently accepting a PASS from this task, to decide and publish the next bounded mechanical debug/rerun task without another Owner message if all of the following hold:

- no new scientific/economic/model choice is introduced;
- protected scientific sources and all frozen model identities remain unchanged;
- the purpose is only to validate or resume the already authorized corrected-2009 instrumented diagnostic route;
- the run/retry budget is finite and explicit;
- no Results, shocks, other years, IRFs, calibration changes or scientific-route changes are included.

If any material scientific decision is implicated, stop for Owner review.

## 13. Repository change boundary

Required repository report:

`docs/CH5_TWO_ASSET_HANK_MP4B_BOUNDED_ZERO_SCIENCE_OBSERVABILITY_HELPER_REMEDIATION_AND_STATIC_CONFORMANCE_REPORT.md`

Allowed repository changes:

- exactly the required report;
- at most one strictly necessary CURRENT roadmap/status line if genuinely needed.

Do not commit copied/repaired MATLAB instrumentation, large artifacts, MAT files, logs, scientific source, Python scientific code, validator/comparator changes, project-rule changes or prior-report edits.

## 14. Closeout

At completion report:

- terminal verdict and markers;
- live continuity and direct-parent evidence;
- Owner authorization and standing-delegation boundary;
- immutable identity verification;
- zero-science ledger;
- fresh remediation root;
- exact six-container remediation table;
- event accounting before/after contract;
- trace persistence changes;
- full static conformance matrix and results;
- original/candidate observer hashes and complete diff hash;
- all external artifact paths/sizes/SHA-256;
- scientific-invariance audit;
- protected-source no-mutation audit;
- `git diff --check` on the actual staged/committed report content, correcting whitespace before commit if necessary;
- changed paths;
- forbidden-operation audit;
- exactly one execution commit;
- non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not run MATLAB and do not auto-start a stationary rerun inside this task.