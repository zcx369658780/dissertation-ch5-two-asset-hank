# Chapter 5 两资产重构路线与科学边界
更新：2026-09-08；轻量治理保持有效。

## 唯一活动路线
唯一活动模型仓库是zcx369658780/dissertation-ch5-two-asset-hank。旧one-asset R5只读历史，不保留其科学runtime为当前依赖；deep-learning-hank是另一个项目。
当前是MATLAB-faithful两资产、多省份重构。忠实复现不等于所有legacy经济公式正确。忠实实现、经济合理性、数值有效性和论文结果分别判断，不能悄然把reference替换为redesign。

## 比较阶段与标准
call-725液体导数的主比较是边界覆盖后、实际policy使用的vb_f/vb_b。处理前数组可作独立诊断，阶段不存在时注明，不能补造或删掉实际差异。此前Owner命名等待已由Reviewer阶段选择解决，不重新申请。
类别/mask、shape/order精确一致；同输入同阶段连续值按abs(x-y)<=128*eps64*max(1,abs(x),abs(y))；稀疏支持仅忽略精确存储零，不作epsilon pruning。不得观察结果后调阈值。旧首轮任务已完成，不能从历史建议重新执行。

## 当前Owner决定与预算来源
2026-09-08 Owner要求保留原MATLAB算法、a_bar及其他现有设置，先检查各省rah/wjt是否越界/触界及原自适应机制。当前exact task从规则索引和状态读取，不从本长期规则推断可执行预算。
D1-D3独立边界/生成算子/FOC修复target仅为已审提案，未采纳、未实施、暂缓；不再以等待其采纳阻止Owner已经授权的价格诊断。既有边界/算子异常仍是有效历史证据。
价格诊断必须区分raw ra0/wt0、clipped ra/wjt、household rah/w及生成时点；厂商clip不自动成为综合家户变量的硬约束。读取实际运行/缓存边界，不能用入口注释或经验回忆替代。只有同一来源口径、年份、阶段和精度的对象才能直接比较。
保留原收敛和自适应条件；新增near-boundary标签只是描述，不修改source条件。只读审计不自动授权新的rate sensitivity、Zt/GovInv调整、2018重跑或算法修复。

## 后续科学与Results
首轮通过不代表多轮、KFE、GE、年度或动态通过；高价格相关性不等于不收敛因果，也不解除生成算子有效性要求。新阶段按明确范围、前提和预算执行，一个完整问题可以在同task内完成工程检查与有界实验。
冲击law、动态时序、初末条件、校准和政策解释等实质选择由Owner/指定科学authority决定。MATLAB命名IRF的顺序比较静态不当作已验证动态HANK。
文献/方法草稿可在已有证据范围准备；正式结果、福利和政策claim须绑定已接受输出及对应审阅，诊断表不能误标论文结果。
