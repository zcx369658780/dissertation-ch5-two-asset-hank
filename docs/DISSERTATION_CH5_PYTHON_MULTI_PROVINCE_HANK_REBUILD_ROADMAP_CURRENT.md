# Chapter 5 Python 多省份两资产 HANK 路线
更新：2026-09-16。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
Results eligibility=`FALSE`。

## Accepted route

The frozen source-faithful reference and separately governed corrected successor remain distinct. Owner-adopted corrected semantics include D1 upper numerical state constraints, D2 consumed-total-drift conservative assembly, D3 regularized-cost-consistent KKT, active lower-a zero-kink multiplier handling, and interior zero-liquid Z switching.

## KFE-D2C-D — completed and accepted

Candidate `9a4eb0e5ec3627743596daa9b991436d2124efa9` completed a fresh 800-cell Option-A V0 policy map, one D2 Q0 assembly PASS and one direct HJB V1 step PASS. This is bounded one-step corrected-HJB evidence only; nonlinear HJB convergence remains unproven.

## KFE-D2D — Q0 operator route result

Q0 source-free KFE uniqueness was tested under a pin-free exact-positive graph contract. After closing the secondary floating-point reaggregation issue, candidate `132b2657c61e0707a5fff96b936b5a62a78dbb2e` found two closed communicating classes and correctly stopped before GESVD/stationary-mass construction.

Structural attribution candidate `2723c475943f7e6a767a940a9a52bad7b9c25e6f` is accepted. The two recurrent classes are exact-zero-asset-drift productivity pairs at lower-a nodes `(i_b,i_a)=(5,0)` and `(6,0)`. Their four states use interior-liquid Z plus active lower-a zero-kink handling, with exact `g_b=g_a=0`; only z switching remains. Q0 therefore has a structurally nonunique invariant stationary space conditional on the V0 policy operator.

This is not economic multiple equilibria or HJB fixed-point multiplicity.

## KFE-D2E-A — active: V1 policy remap and Q1 recurrent-topology diagnostic

Active task:
`tasks/CH5_MP4C_2018_KFE_D123_V1_POLICY_REMAP_AND_Q1_RECURRENT_TOPOLOGY_DIAGNOSTIC_20260916.md`

Use the already accepted V1 from the one direct HJB step and perform exactly one fresh 800-cell corrected selector map. If all cells pass, assemble Q1 exactly once and audit only its exact-positive graph, SCCs and condensation/reachability topology.

The purpose is to distinguish a V0-initialization-specific two-sink topology from persistence after one HJB policy update. Hard ceilings: one V1 map, <=800 selectors, <=3144 scalar roots including <=2880 interior-Z, Q1 D2<=1, graph/SCC/condensation<=1 each, retries=0.

No second HJB solve, nonlinear continuation, V2, KFE stationary-mass solve, SVD/eigen/nullspace, Q1.T@p, row replacement/pin/source RHS, MATLAB or downstream work is authorized.

## KFE-D2E-B — conditional successor

Only after independent acceptance of the Q1 topology may Reviewer decide whether the next scientifically justified route is bounded nonlinear HJB continuation, a fixed-point design gate, or a different diagnosis. Do not infer convergence from one remap.

## KFE-D3 — later production closure

Corrected multi-province production replacement, steady state, GE/annual/dynamics/IRF and Results remain downstream gates with separate evidence and authority.

Capital/labor K1/C1/K1B/K2 routes remain deferred, not abandoned, behind the current household/KFE integration route.