# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_FIRST_ITERATION_HJB_SCALAR_MANIFEST_BINDING_REPAIR_AND_FRESH_STAGEWISE_EXECUTION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / external wrapper repair certifier and isolated first-iteration HJB forensic executor

Owner: final scientific authority

## 1. Immediate authority and purpose

Immediate live parent at publication:

`8f3baa1a8dda8e02d5cf13ddf41e460c397a3959`

The parent report closed with:

`MP4C_2018_CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_FORENSIC_BLOCKED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

and classified the blocker as an external wrapper scalar-manifest schema mismatch. The exact-MAT common-input certification itself passed. The MATLAB wrapper was invoked once but failed at scalar JSON parsing before derivative construction, policy evaluation, sparse operator assembly, linear solve, or a completed HJB iteration. Python strict-common execution was not started.

Therefore the predecessor consumed zero completed first-iteration scientific evaluations and established no HJB/KFE/model failure.

This successor task explicitly authorizes:

1. a zero-science audit and repair of the external MATLAB wrapper's scalar-manifest binding;
2. zero-call certification that the repaired scalar binding is exact and source-preserving;
3. only after that certification, a fresh bounded first-iteration MATLAB/Python stagewise scientific budget for the unchanged question from the parent task.

No production repair, `maxit` change, full HJB rerun, KFE, GE, annual rerun, shock/IRF, or Results work is authorized.

## 2. Live continuity

At start:

1. `git fetch origin`;
2. require this exact task on live `origin/main` as direct child of `8f3baa1a8dda8e02d5cf13ddf41e460c397a3959`;
3. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
4. read `AGENTS.md`, all CURRENT project rules, this task, the parent exact-MAT execution report, the parent exact-MAT task, the initialization-array parity report, the MATLAB/Python call-725 replay reports, and the accepted HJB propagation-aware parity report.

## 3. Frozen source/evidence identities

Require repository Git blobs:

- `exports/matlab_faithful_two_asset_ha.py`: `9e7dc9556a2b76811e78f89999abecc045886106`;
- `validators/multi_province/mp4b_python_empirical.py`: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`: `0033baee136c0328e80ffb8b794a88d4405c976c`.

Protected MATLAB SHA-256:

- `HANK_2ASSETS_HJB.m`: `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m`: `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m`: `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m`: `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

Initialization-parity evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-initialization-array-numerical-parity-20260904-001`

Require final supplemental audit manifest SHA-256:

`817845439CDC77E2C3873AA3D9675E16704E0AB48263F02CFBD653626245D07C`.

MATLAB replay evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-termination-replay-after-path-recertification-20260904-001`

Require audit manifest SHA-256:

`87500FF3121ECBBEE1E18A0A574371E06AC2B03B6B24B13465FCFBBF1E02457B`.

Parent blocked execution evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-first-iteration-exact-mat-common-input-20260904-001`

Require audit manifest SHA-256:

`1A2A72FA19C7E745F9C5C303FDA10A508A5C342887143BAE385F7F4FD6472E29`.

Authoritative HJB100 initialization MAT SHA-256:

`1718984CB588AE586F74AB8476C57AF849BB2C80CC95500329D29BC14207BB81`.

## 4. Frozen exact-MAT common input

The strict pair must continue to use the authoritative HJB100 MAT arrays directly:

- `b`;
- `ah`;
- `z`;
- `v0`;
- `l0`.

The parent already certified these exact float64 arrays and persisted MATLAB/Python ingestion receipts. Reuse that certification; do not regenerate grid or initialization arrays.

Forbidden in the strict pair:

- `linspace` reconstruction of `b` or `ah`;
- JSON reconstruction of grid values;
- decimal-text reconstruction;
- `_source_initial_arrays(...)`;
- any alternative common-input source.

## 5. Frozen call-725 scalar/economic contract

The scientific scalar values remain exactly:

- province `安徽`;
- zero-based province index `11`;
- outer iteration `24`;
- global household call `725`;
- `rah=0.09`;
- `rb=0.02`;
- `rb_gap=0.07`;
- `tau=0.05`;
- `w=16.82014806560587`;
- `Tt=0.1`;
- switch matrix `[[-1/3,1/3],[1/3,-1/3]]`;
- `rho=0.05`;
- `gamma=2`;
- `phi=5` / source `frisch_l=0.2`;
- `chi0=0.1`;
- `chi1=2`;
- `a_bar=1e-6`;
- `Delta=1000`;
- `crit=1e-7`;
- drift tolerance `1e-12`.

No scalar or parameter may be changed.

## 6. Phase 0A — zero-science historical scalar-contract schema audit

Before changing the wrapper, inspect the exact historical active-input/scalar contract that the failed wrapper received.

Persist:

- contract absolute path;
- contract SHA-256;
- JSON top-level keys;
- nested key paths needed by the wrapper;
- exact values at those paths;
- exact failed assumptions from the predecessor wrapper (`m.parameters`, `m.grid`, `m.numerics`, etc.);
- source/provenance mapping for every scalar used by the first-iteration evaluator.

Do not guess the schema from previous code or chat.

The audit must show whether every required scalar is durably available from existing evidence. If any execution-critical scalar is ambiguous or absent, STOP before wrapper repair/science with:

`MP4C_2018_CALL725_SCALAR_MANIFEST_BINDING_BLOCKED__REQUIRED_SCALAR_NOT_DURABLY_AVAILABLE__NO_HJB_NO_KFE`.

## 7. Phase 0B — construct one canonical external scalar binding manifest

If Phase 0A passes, create a fresh no-overwrite external `call725_first_iteration_scalar_binding.json` with a simple task-owned schema.

The manifest must contain only the scalar/matrix quantities actually needed by the one-iteration evaluator and must bind each value to its audited source path/provenance.

At minimum include:

- `rah`, `rb`, `rb_gap`, `tau`, `w`, `Tt`;
- `rho`, `gamma`, `phi` or exact source-equivalent parameterization;
- `chi0`, `chi1`, `a_bar`;
- `Delta`, `crit`, drift tolerance;
- exact 2x2 switch matrix;
- call/province identifiers for provenance only.

Do not include or reconstruct `b/ah/z/v0/l0`; those remain MAT-authoritative.

The manifest must be independently validated against the frozen values in Section 5 and the audited historical source contract. Any mismatch: STOP before science.

## 8. Phase 0C — repair and certify the external MATLAB wrapper

Create a fresh external wrapper; do not overwrite the failed wrapper.

Permitted semantic changes relative to the failed wrapper are strictly limited to:

- reading the new canonical scalar-binding manifest schema;
- direct loading of MAT-authoritative `b/ah/z/v0/l0`;
- persistence/serialization plumbing;
- explicit stop after iteration 1.

Forbidden changes:

- derivative formulas;
- boundary formulas;
- local policy/FOC logic;
- drift/rate formulas;
- sparse assembly;
- `M`/RHS construction;
- direct sparse solver;
- arithmetic ordering intentionally;
- any economic/numerical parameter.

Persist an exact diff between the failed and repaired wrapper, with every changed line classified as `SCALAR_BINDING_OR_PERSISTENCE_ONLY` or else fail closed.

Before any scientific call, perform zero-science certification:

1. syntax/checkcode/static parse;
2. parse the new scalar manifest without entering derivative/policy/HJB code;
3. load and hash exact MAT `b/ah/z/v0/l0`;
4. prove all parsed scalars exactly equal the frozen Section-5 contract;
5. prove scientific source expressions are unchanged from the accepted source-extracted evaluator;
6. persist zero-science ledger.

Required marker:

`MP4C_2018_CALL725_SCALAR_MANIFEST_BINDING_REPAIRED_AND_EXACT_MAT_WRAPPER_CERTIFIED__FRESH_FIRST_ITERATION_BUDGET_OPEN`.

If this marker is not achieved, STOP with no first-iteration scientific call.

## 9. Fresh scientific budget after certification only

The predecessor completed zero HJB iterations. After Phase 0 certification, open a fresh budget:

- MATLAB exact-MAT/common-scalar first iteration: `1`;
- Python exact-MAT/common-scalar first iteration: `1`, only after MATLAB persistence;
- Python native-initialization amplification probe: `1`, conditional exactly as in the parent task;
- retries: `0`.

Forbidden counts remain zero:

- full HJB100/HJB500 rerun;
- iteration 2+;
- KFE;
- household steady state;
- GE/stationary/annual 2018;
- R/PLM;
- shock/IRF/Results.

## 10. Stagewise first-iteration forensic contract

Carry forward the parent task's exact stage order and comparison rules.

Persist and compare in causal order:

1. `old=V0` identity;
2. raw `vb_f/vb_b/va_f/va_b`;
3. boundary-adjusted derivatives;
4. categorical liquid/transfer labels and source-identical branch masks if exposed;
5. consumption/labor/transfer/adjustment cost/effective illiquid return;
6. `mu_a`, `mu_b`, utility;
7. `bb/bf/ab/af`;
8. sparse `BB`;
9. sparse `AAH`;
10. `Bswitch`;
11. full `A`;
12. `M=(1/Delta+rho)I-A`;
13. RHS;
14. direct-solve `V1`;
15. first-iteration statistic.

Frozen comparison rules:

- categorical/masks: exact equality;
- shape/order: exact equality;
- continuous same-input values: `128*eps64*max(1,abs(x),abs(y))`;
- sparse mathematical support: ignore only exact stored `0.0/-0.0`;
- sparse values: same machine rule;
- no epsilon pruning;
- no post-observation tolerance broadening.

Classification must be controlled by the **earliest material stage**.

## 11. Direct-solve classification

If `M` and RHS pass direct parity but `V1` materially differs, do not call it an equation or formula mismatch.

Record for both outputs:

- `||M*V1-rhs||_inf`;
- backward-error certificate/scale;
- cross-language `V1` difference;
- read-only conditioning diagnostics if available without changing solver semantics.

Such a case is `DIRECT_SOLVE_PROPAGATION_OR_CONDITIONING`, not a model-equation defect.

No regularization, pseudoinverse, alternate solver, intentional reordering, or retry is authorized.

## 12. Conditional Python native-init amplification probe

Only if the strict exact-MAT common-input pair reaches `M/RHS/V1` without an earlier material source-formula mismatch may one additional Python first iteration run.

Reuse the already persisted digest-bound native Python initialization arrays from the initialization-parity evidence root. Do not regenerate them.

Compare native-init versus exact-MAT-common-init Python stage objects and quantify first-step amplification only. Do not infer full 196-vs-275 convergence causality from one step.

## 13. Required classification

Choose the strongest supported:

1. `CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_PARITY_PASS__NATIVE_MACHINE_PERTURBATION_AMPLIFICATION_DIAGNOSTIC_RECORDED__MULTI_ITERATION_PROPAGATION_FORENSIC_REQUIRED`

2. `CALL725_FIRST_ITERATION_PRE_SOLVE_SOURCE_PARITY_BREAK_CONFIRMED__EARLIEST_STAGE_IDENTIFIED__NO_PRODUCTION_CHANGE`

3. `CALL725_FIRST_ITERATION_LINEAR_SYSTEM_PARITY_PASS__DIRECT_SOLVE_PROPAGATION_DIVERGENCE_CONFIRMED__NO_SOLVER_CHANGE_AUTHORIZED`

4. `CALL725_FIRST_ITERATION_FORENSIC_BLOCKED__SCALAR_BINDING_OR_WRAPPER_CERTIFICATION_FAILED__NO_PRODUCTION_CHANGE`.

No classification authorizes `maxit=500`, production solver changes, 2018 GE, shock, IRF, or Results.

## 14. Evidence root

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-first-iteration-scalar-binding-repair-20260904-001`

Persist at minimum:

- authority/source/evidence identities;
- historical scalar-contract schema audit;
- canonical scalar-binding manifest + provenance;
- failed wrapper identity/source;
- repaired wrapper source + exact diff;
- zero-science wrapper certification receipt;
- exact MAT common-input hashes/receipts;
- MATLAB first-iteration stage artifacts if run;
- Python strict-common stage artifacts if run;
- stagewise comparator JSON/CSV;
- sparse support/value comparison;
- linear-system/backward-error evidence;
- conditional native-probe evidence if run;
- scientific call ledger;
- stdout/stderr/warnings;
- classification;
- `audit_manifest.json`.

Hash and read back every current evidence artifact.

## 15. Repository publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_FIRST_ITERATION_HJB_SCALAR_MANIFEST_BINDING_REPAIR_AND_FRESH_STAGEWISE_EXECUTION_REPORT.md`

If completed consistently, only report-only commit + non-force push is authorized.

Suggested commit:

`Repair MP4C call-725 scalar binding and diagnose first HJB iteration`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Completed terminal:

`MP4C_2018_CALL725_SCALAR_BINDING_REPAIR_AND_FIRST_ITERATION_FORENSIC_COMPLETE__EARLIEST_DIVERGENCE_CLASSIFIED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_SCALAR_BINDING_REPAIR_AND_FIRST_ITERATION_FORENSIC_BLOCKED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Stop after publication and return to ChatGPT L3 / Owner. Do not continue to production changes, 2018 GE, or IRF.