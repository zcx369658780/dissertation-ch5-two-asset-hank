# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_FIRST_ITERATION_HJB_EXACT_MAT_COMMON_INPUT_WRAPPER_AND_FRESH_STAGEWISE_EXECUTION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / external capture-wrapper certifier and first-iteration forensic executor

Owner: final scientific authority

## 1. Immediate authority and classification

Immediate live parent at publication:

`0a4e102e80af3681c766e54c21029661922e9304`

The parent task authorized strict common-input first-iteration MATLAB/Python HJB stagewise forensic. Its pre-call freeze consumed **zero** first-iteration scientific calls and found only an input-plumbing blocker:

- the existing MATLAB evaluator reconstructed `b/ah` from JSON;
- those reconstructed grid values differed from the authoritative persisted HJB100 MAT arrays by about `8.88e-16`;
- therefore the exact-common-grid gate correctly remained closed before science.

Classification:

`PRECALL_EXACT_COMMON_GRID_PLUMBING_BLOCKER__JSON_RECONSTRUCTION_NOT_ACCEPTABLE_FOR_STRICT_PAIR__ZERO_SCIENCE_CALLS_CONSUMED`.

This is not a model, HJB, KFE, parameter, convergence, or parity failure. The scientific question and all numerical semantics of the parent task remain unchanged.

This successor explicitly authorizes a fresh external **capture-only MAT loader/wrapper**, certification of exact MAT-backed common inputs, and—only after that zero-call gate passes—the same bounded first-iteration scientific execution budget.

## 2. Live continuity

At start:

1. `git fetch origin`;
2. require this task live on `origin/main` as direct child of `0a4e102e80af3681c766e54c21029661922e9304`;
3. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
4. read `AGENTS.md`, all CURRENT project rules, this task, the parent first-iteration task, the initialization-array forensic report, MATLAB/Python call-725 replay reports, and accepted HJB propagation-aware parity report.

Do not infer authority from chat alone.

## 3. Frozen source and evidence identities

Require repository Git blobs:

- `exports/matlab_faithful_two_asset_ha.py`: `9e7dc9556a2b76811e78f89999abecc045886106`;
- `validators/multi_province/mp4b_python_empirical.py`: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`: `0033baee136c0328e80ffb8b794a88d4405c976c`.

Protected MATLAB SHA-256:

- `HANK_2ASSETS_HJB.m`: `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m`: `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m`: `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m`: `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

Initialization-parity root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-initialization-array-numerical-parity-20260904-001`

Require final supplemental manifest SHA-256:

`817845439CDC77E2C3873AA3D9675E16704E0AB48263F02CFBD653626245D07C`.

MATLAB replay root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-termination-replay-after-path-recertification-20260904-001`

Require manifest SHA-256:

`87500FF3121ECBBEE1E18A0A574371E06AC2B03B6B24B13465FCFBBF1E02457B`.

## 4. Frozen scientific state

Use only call 725:

- province `安徽`;
- zero-based province index `11`;
- outer iteration `24`;
- global household call `725`;
- `rah=0.09`;
- `rb=0.02`;
- `rb_gap=0.07`;
- `tau=0.05`;
- `w=16.82014806560587`;
- `Tt=0.1`;
- shape `(20,20,2)` / MATLAB `(I,J,Nz)`;
- switch matrix `[[-1/3,1/3],[1/3,-1/3]]`;
- `rho=.05`, `gamma=2`, `phi=5`, `chi0=.1`, `chi1=2`, `a_bar=1e-6`;
- `Delta=1000`, `crit=1e-7`, drift tolerance `1e-12`.

No economic or numerical parameter may change.

## 5. Exact MAT common-input authority

The strict pair must use the already persisted MATLAB HJB100 initialization MAT artifact from the MATLAB replay root as the sole common numerical authority for:

- `b`;
- `ah` / illiquid grid;
- `z`;
- `v0`;
- `l0`.

Also validate any persisted shape/orientation metadata.

Do **not** reconstruct `b`, `ah`, or `z` from JSON, `linspace`, decimal text, or source formulas for the strict scientific pair.

Do **not** regenerate `v0/l0` for the strict pair.

The earlier JSON reconstruction may be retained only as a diagnostic comparison showing why it was rejected; it must never feed the scientific calls.

## 6. Phase 0A — zero-science capture-only MAT wrapper

Before any HJB iteration, create a fresh external wrapper/helper in the new evidence root. It may only:

1. load the authoritative HJB100 MAT artifact directly;
2. extract `b`, `ah`, `z`, `v0`, `l0` without arithmetic reconstruction;
3. validate dtype/class, dimensions, finiteness, orientation, and exact field presence;
4. produce deterministic canonical binary copies/hashes for each extracted array;
5. expose those exact arrays to the later MATLAB one-iteration evaluator;
6. persist a manifest proving the scientific evaluator will consume those exact objects.

No HJB/policy/operator/linear solve is allowed in Phase 0A.

Do not modify the protected MAT artifact.

## 7. Phase 0B — Python common-input capture

Using a non-model external loader, read the **same authoritative MAT fields** into Python.

Required:

- preserve exact binary64 numeric values;
- perform only explicit dimension/orientation conversion needed to obtain `(b,a,z)` semantics;
- no `linspace` or JSON reconstruction;
- no `_source_initial_arrays`;
- no HJB.

Persist canonical hashes before and after orientation conversion. Prove that the Python scientific evaluator will ingest the same numeric `b/ah/z/v0/l0` values as MATLAB.

For every scalar element of all five objects, strict common-input plumbing is an **exact numeric equality gate**, not the 128-eps scientific-output tolerance.

If exact input identity cannot be proven, STOP before science with:

`MP4C_2018_CALL725_EXACT_MAT_COMMON_INPUT_CERTIFICATION_BLOCKED__NO_FIRST_ITERATION_CALLS`.

## 8. Phase 0C — wrapper source freeze

Only after 0A/0B pass:

- freeze/hash the external MATLAB capture-only + one-iteration wrapper;
- freeze/hash the external Python one-iteration extractor;
- preserve exact source maps to protected MATLAB HJB and accepted Python faithful HJB;
- diff against the previously accepted source-extracted evaluator architecture where applicable.

Allowed adaptation only:

- direct MAT input loading;
- 800-state shape plumbing;
- persistence/serialization;
- exposure of intermediate objects;
- explicit stop after iteration 1.

Forbidden changes:

- derivative/boundary formulas;
- local policy logic;
- FOC or cost formulas;
- drift/rate formulas;
- sparse assembly;
- `M`/RHS formulas;
- sparse linear solver;
- arithmetic ordering intentionally;
- tolerances/parameters.

Required marker before science:

`MP4C_2018_CALL725_EXACT_MAT_COMMON_INPUT_AND_CAPTURE_WRAPPERS_CERTIFIED__FRESH_FIRST_ITERATION_BUDGET_OPEN`.

## 9. Fresh first-iteration scientific budget

The prior attempt consumed zero scientific calls, so this successor authorizes a fresh budget:

- MATLAB source-extracted first iteration: exactly `1` maximum;
- Python faithful strict-common first iteration: exactly `1` maximum;
- Python native-initialization amplification probe: exactly `1` maximum and only if parent-task conditions are met;
- retries: `0`.

Forbidden:

- iteration 2+;
- full HJB100/HJB500;
- KFE;
- household steady state;
- GE/stationary/annual 2018;
- R/PLM;
- shock/IRF/Results.

## 10. Phase A — MATLAB iteration 1

Using exact MAT-backed common `b/ah/z/v0/l0`, run exactly one MATLAB source-extracted iteration 1.

Persist the same parent-task stage objects:

- old/V0;
- raw `vb_f/vb_b/va_f/va_b`;
- boundary-adjusted derivatives;
- branch/selector masks and liquid/transfer labels;
- consumption, labor, transfer, adjustment cost, effective illiquid return;
- `mu_a`, `mu_b`, utility;
- `bb/bf/ab/af`;
- `BB`, `AAH`, `Bswitch`, full `A`;
- `M=(1/Delta+rho)I-A`;
- RHS;
- direct-solve `V1`;
- statistic `max(abs(V1-V0))`;
- warnings and linear-system residual/backward-error diagnostics.

If post-call instrumentation fails, persist and STOP. No rerun.

## 11. Phase B — Python strict-common iteration 1

Only after MATLAB output is durable.

Using the exact same MAT-backed common `b/ah/z/v0/l0`, run exactly one Python faithful iteration 1 and persist the same stage objects.

No `_source_initial_arrays` in this phase.

If post-call instrumentation fails, persist and STOP. No rerun.

## 12. Stagewise comparison contract

In causal order compare:

1. raw derivatives;
2. boundary replacements;
3. categorical labels/masks;
4. continuous local policies;
5. `mu_a/mu_b/utility`;
6. iteration rates;
7. `BB`;
8. `AAH`;
9. `Bswitch`;
10. full `A`;
11. `M`;
12. RHS;
13. `V1` and statistic.

Frozen rules:

- categories/masks exact;
- shape/order exact;
- continuous same-input values `128*eps64*max(1,abs(M),abs(P))`;
- sparse mathematical support ignores only exact stored `0.0/-0.0`;
- sparse values use same machine rule;
- no epsilon pruning and no post-observation tolerance broadening.

For each stage report PASS/MATERIAL, mismatch count, max abs diff, max normalized diff, first mismatch, worst mismatch, and `(b,a,z)` or sparse coordinate.

Scientific classification is controlled by the **earliest material stage**.

## 13. Linear-solve diagnostic rule

If `M` and RHS pass but `V1` is materially different:

- compute each output residual `||M V1-rhs||_inf`;
- use the already accepted backward-error scale/certificate;
- compare the two `V1` vectors;
- read-only conditioning diagnostics are allowed.

Do not regularize, pseudoinvert, change solver, reorder intentionally, or retry.

Such evidence supports a direct-solve propagation/conditioning classification, not an equation mismatch.

## 14. Conditional Python native-init probe

Only if the strict common-input pair reaches the linear solve without an earlier material source-formula mismatch.

Reuse only the already persisted digest-bound regenerated Python arrays from the initialization-parity root. Do not call `_source_initial_arrays` again.

Run one Python iteration 1 from native Python initialization and compare its stages/V1 to Python exact-MAT-common-input output.

This is amplification diagnostics only; do not infer full 196-vs-275 convergence causality from one step.

## 15. Required classifications

Choose the strongest supported:

- `CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_PARITY_PASS__NATIVE_MACHINE_PERTURBATION_AMPLIFICATION_DIAGNOSTIC_RECORDED__MULTI_ITERATION_FORENSIC_REQUIRED`;
- `CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_PRE_SOLVE_PARITY_BREAK_CONFIRMED__EARLIEST_STAGE_IDENTIFIED__NO_PRODUCTION_CHANGE`;
- `CALL725_EXACT_MAT_COMMON_INPUT_LINEAR_SYSTEM_PARITY_PASS__DIRECT_SOLVE_PROPAGATION_DIVERGENCE_CONFIRMED__NO_SOLVER_CHANGE_AUTHORIZED`;
- `CALL725_EXACT_MAT_COMMON_INPUT_STAGEWISE_FORENSIC_BLOCKED__NO_PRODUCTION_CHANGE`.

No classification from this task authorizes production `maxit=500`, solver changes, KFE changes, 2018 GE, or IRF.

## 16. Evidence root

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-first-iteration-exact-mat-common-input-20260904-001`

Persist at minimum:

- authority/source identities;
- authoritative MAT identity/hash;
- extracted common arrays and canonical hashes;
- rejected JSON-reconstruction comparison diagnostic;
- MATLAB/Python input-ingestion receipts;
- wrapper source/diff/source-map hashes;
- pre-call certification marker;
- MATLAB stage artifacts;
- Python strict-common stage artifacts;
- stagewise comparator CSV/JSON;
- sparse comparison;
- linear solve/backward-error diagnostics;
- conditional native-probe artifacts;
- scientific-call ledger;
- stdout/stderr/warnings;
- classification;
- `audit_manifest.json`.

Hash and read back all evidence.

## 17. Report and publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_FIRST_ITERATION_HJB_EXACT_MAT_COMMON_INPUT_WRAPPER_AND_FRESH_STAGEWISE_EXECUTION_REPORT.md`

Only report-only commit + non-force push is authorized after consistent completion.

Suggested commit:

`Diagnose MP4C call-725 first HJB iteration with exact MAT inputs`

After push, fresh-fetch and require:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Completed terminal:

`MP4C_2018_CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_FORENSIC_COMPLETE__EARLIEST_DIVERGENCE_CLASSIFIED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_FORENSIC_BLOCKED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`.

Stop after publication and return to ChatGPT L3 / Owner.