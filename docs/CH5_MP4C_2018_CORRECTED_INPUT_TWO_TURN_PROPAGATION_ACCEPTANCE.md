# Chapter 5 corrected-2018 two-turn propagation — Reviewer acceptance

Date: 2026-09-09.
Repository: `zcx369658780/dissertation-ch5-two-asset-hank`.
Accepted candidate: `406d4132836e08d30b42b41e09f8237e6aac66a9`.

## Verdict

Reviewer marker: `CORRECTED_2018_TWO_TURN_ACCEPTED__TURN2_PROPAGATION_PASS__TURN3_IS_FIRST_DIRECT_CORRECTED_RATE_TRANSMISSION_GATE`.

Accept candidate verdict `CORRECTED_2018_TWO_TURN_PASS__TURN2_PROPAGATION_OBSERVED`.

## Accepted findings

1. Fresh turn 1 reproduces the previously accepted corrected-2018 single-turn evidence exactly (`mismatch_count=0`).
2. Exactly two ordered turns were executed in the frozen 31-province source order; no third turn, steady-state loop, GE, annual model, MATLAB model, IRF or Results execution occurred.
3. For Anhui, turn-2 household `rah=0.0829892058879816` is generated by the native pre-firm capital-allocation rule from the turn-1 entering `ra` vector, whose Anhui value is still `0.09`. It was not manually assigned and is not a direct copy of turn-1 firm used `ra=0.02`.
4. Turn-1 firm used `ra=0.02` becomes turn-2 entering firm state `ra=0.02`. Under the source timing, that corrected firm rate first becomes eligible to affect the next household composite `rah` through the following capital-allocation step, i.e. the turn-3 household input. This makes turn 3 the next high-information propagation gate.
5. Anhui firm raw return remains on the lower side in both observed turns: `-0.02496997113112164 -> -0.024968505109415375`; firm used rate stays at the lower clip `0.02`.
6. Anhui raw wage falls `2.5721358283733027 -> 2.1373365306922518` but remains above the upper wage bound, so used wage stays `1.3`.
7. Household numerical execution remains intact after one state propagation step: Anhui HJB iterations `64 -> 31`, both HJB calls converge, both KFE calls return, and all 62 province household/HJB/KFE/firm calls return.
8. National firm rate clip counts are `31/0/0` lower/interior/upper in both turns. Wage clip counts improve from `5/4/22` to `5/8/18`.
9. National max `nk_gap` falls from `204296.40029802968` to `0.45946738761290562`, but remains above the existing `<0.1` adaptation gate; therefore no Zt or GovInv adaptation occurs in either turn.
10. The drop in gaps and stable numerical returns are propagation evidence only; they do not establish monotone convergence, stationary convergence, KFE global admissibility, or Results validity.

## Interpretation boundary

The two-turn evidence materially weakens the old mixed-year interpretation that the 2018 system is intrinsically driven toward the upper firm-rate clip from the outset. Corrected 2018 first-turn and second-turn raw firm returns remain below the lower bound. However, old late-prefix/call725 behavior is not yet invalidated: only two corrected turns have been observed, and turn 2 household `rah` still contains legacy entering-rate information through the native lag/composite rule.

Do not claim that `rah=.09` or the old call725 failure is solved. Turn 3 is the first turn in which the corrected turn-1 firm `ra=.02` can enter the household composite return through the source timing contract.

## Calls and authority

Accepted scientific ledger: one process, two turns, 62 province updates, 62 household/HJB/KFE/firm calls, 2,599 HJB direct solves, 62 KFE direct solves, 49,600 labor roots and 49,600 Brent calls, retries 0. MATLAB/GE/annual/IRF/Results all 0.

Results eligibility = FALSE. Steady state remains unauthorized.
