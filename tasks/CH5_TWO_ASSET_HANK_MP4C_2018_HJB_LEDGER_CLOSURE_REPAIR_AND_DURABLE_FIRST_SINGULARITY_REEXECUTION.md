# CH5_TWO_ASSET_HANK_MP4C_2018_HJB_LEDGER_CLOSURE_REPAIR_AND_DURABLE_FIRST_SINGULARITY_REEXECUTION

Date: 2026-09-03

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / diagnostic-ledger repairer / single-run scientific diagnostician

Owner: final scientific authority

## 1. Authority basis

Immediate predecessor execution:

`54120459d0b7917a6d4601be300ed1330dd6506b`

with terminal:

`MP4C_2018_DURABLE_FIRST_SINGULARITY_CAPTURE_UNHANDLED_INSTRUMENTATION_FAILURE__NO_SECOND_CHILD__NO_POSTMORTEM`

Accepted predecessor facts:

- the frozen Owner-A 2018 input identity and binding were verified exactly;
- one durable scientific child was launched with one worker and thread pins equal to one;
- the first production MATLAB-faithful HJB returned successfully;
- the diagnostic then failed before KFE entry because nested function `hs` assigned `hjb_writer` without a valid closure binding, producing `UnboundLocalError`;
- zero KFE calls occurred;
- no singular operator, contaminated matrix, SCC, rank/nullity, or KFE-root-cause evidence was produced;
- no HJB/KFE/model/input/grid/controller/calibration mutation occurred;
- no second child was launched.

This is a diagnostic-only instrumentation defect. Under the standing bounded-debugging delegation, L3 authorizes repair of the HJB-return ledger mechanism and allocates exactly one new 2018 diagnostic scientific-child budget.

## 2. Core objective

1. Repair only the diagnostic HJB-return ledger state/closure handling.
2. Prove in zero-science tests that a dummy HJB return is durably appended and flushed before dummy KFE entry.
3. If and only if those tests pass, launch exactly one durable 2018 child with the unchanged scientific model/input.
4. At the first genuine singular KFE event, persist raw evidence first and stop fail-closed.
5. Only after the child terminates and raw evidence is durable, run read-only postmortem SCC/rank/nullity analysis.
6. Do not repair HJB/KFE science in this task.

## 3. Required live continuity

At start:

1. `git fetch origin`;
2. require this exact task live on `origin/main` as direct child of `54120459d0b7917a6d4601be300ed1330dd6506b`;
3. require `HEAD == origin/main`, ahead/behind `0/0`;
4. require clean tracked worktree;
5. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md` and all CURRENT rules;
   - the three most recent 2018 diagnostic tasks/reports;
   - current `validators/multi_province/mp4c_2018_first_singularity_diagnostic.py`;
   - `exports/matlab_faithful_two_asset_ha.py`;
   - `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`;
   - current Owner-A input adapter and stationary runtime.

## 4. Frozen scientific contract

The scientific model is immutable in this task.

Use exact Owner-A 2018 input SHA-256:

`F84D25FD49A76229CA49958764D1167CAA56FB68CF99A4ED7B20C508812E6ED0`

Required binding:

- steady-state year 2018;
- rolling entry 10;
- PLM vintage 19;
- calendar row 19;
- rolling window 2009–2018;
- GDP source `GDP` ×1000;
- CAP source `R语言计算资本存量` ×1000;
- POP source `就业人数` ×100;
- exact 31-province order;
- no 2023 scientific input.

Any input mismatch: STOP.

## 5. Diagnostic-only ledger repair

The current failure is specifically the nested `hs` writer state.

Repair only diagnostic bookkeeping. Acceptable approaches include:

- declare the correct `nonlocal` binding in the exact nested scope; or
- preferably, replace ad-hoc CSV-writer closure state with a tiny diagnostic-only ledger object/helper whose writer lifecycle is explicit and testable.

The repair must not alter the arguments or return values of the production HJB/KFE callables or the post-loop adapter.

Required HJB-return ledger semantics:

- on every HJB return, construct exactly one row containing current household context plus:
  - `hjb_converged`;
  - `hjb_iterations`;
  - `hjb_convergence_statistic`;
  - `kfe_path` = `HJB_CONVERGED` or `MATLAB_FAITHFUL_POSTLOOP_AFTER_HJB_NONCONVERGENCE`;
- initialize CSV header exactly once;
- append exactly once per HJB return;
- `flush()` and `os.fsync()` before returning control to KFE;
- no ledger exception may be silently swallowed.

Do not change production science to simplify logging.

## 6. Phase A — mandatory zero-science regression gate

Before any 2018 input read or model launch, add/extend focused tests that prove the exact failing path is fixed.

The test must exercise:

`dummy household context -> injected dummy HJB return -> HJB ledger append -> flush/fsync -> injected dummy KFE -> aggregate`

Required assertions:

- production HJB/KFE callable identities still resolve to `exports.matlab_faithful_two_asset_ha`;
- adapter injected-callable sequence is exact;
- HJB ledger file exists;
- header exists exactly once;
- exactly one dummy HJB row exists;
- row context and HJB fields are exact;
- KFE is entered only after ledger row is durably written;
- a second dummy HJB return appends a second row without rewriting the header;
- no `UnboundLocalError`, closure-state mutation error, or file-handle lifecycle error;
- existing dummy singularity raw-capture test still passes;
- `py_compile` passes;
- scientific call counters remain exactly zero.

Required marker:

`MP4C_2018_HJB_LEDGER_CLOSURE_REPAIR_ZERO_SCIENCE_PASS__ONE_DURABLE_2018_CHILD_AUTHORIZED`

If Phase A fails: STOP. No 2018 execution.

## 7. Durable scientific execution budget

Only after Phase-A PASS, allocate exactly one new scientific child.

Exactly:

- year = 2018;
- scientific child count = 1;
- worker count = 1;
- subprocess count = 1;
- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`;
- automatic reruns = 0.

Use a durable process/session that is not killed by the short outer tool-call lifetime.

Preferred fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-durable-first-singularity-capture-ledger-fix-20260903-001`

If that root is consumed before a scientific PID exists, preserve it and use a new numbered root. Do not reuse a dirty root.

Persist before/at launch:

- exact command;
- Python executable;
- PID;
- environment;
- input path/SHA;
- code identities;
- launch timestamp.

Never launch a second scientific child while the authorized child exists or after it terminates.

## 8. Live pre-call and HJB ledgers

Before every household call append + flush + fsync:

- outer iteration;
- province index/name;
- global household-call number;
- `rah`, `rb`, `tau`, `w`, `Tt`, `rb_gap`;
- `Yt`, `Lt`, `Kt`, `Zt`, `GovInv`.

After every HJB return append + flush + fsync:

- same context;
- HJB converged flag;
- HJB iterations;
- HJB convergence statistic;
- KFE-entry classification.

## 9. First genuine singularity — capture first, analyze later

At the first `MatrixRankWarning` or non-finite contaminated-row solve, persist immediately before any heavy analysis:

- localization context;
- exact HJB status;
- post-HJB operator A;
- A transpose;
- contaminated matrix;
- RHS;
- raw solve vector if available;
- warning records;
- traceback;
- terminal sentinel.

Flush/fsync all raw artifacts, then terminate the scientific child fail-closed.

Do not compute dense SVD/SCC/rank before raw evidence is durable.

## 10. Read-only postmortem after child termination

Only if raw first-singularity artifacts are complete, run postmortem with zero new model calls.

Compute and persist:

- matrix shape/nnz/finite flags;
- max absolute generator row sum;
- diagonal/off-diagonal extrema;
- positive/negative off-diagonal counts;
- zero-outflow and isolated states;
- SCC count;
- closed SCC count, sizes, members;
- rank/nullity of A' and contaminated matrix;
- smallest singular/eigenvalue diagnostics;
- method and tolerance.

Dense 800×800 SVD is allowed post-child if practical.

## 11. Root-cause classification

Choose the strongest evidence-supported classification:

- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_HJB_NONCONVERGED_POSTLOOP_OPERATOR`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_MULTIPLE_CLOSED_COMMUNICATING_CLASSES`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_OPERATOR_RANK_DEFICIENCY_BEYOND_SINGLE_STATIONARY_NULL_DIRECTION`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_GENERATOR_CONSTRUCTION_DEFECT`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_OTHER_CAPTURED_NUMERICAL_PATHOLOGY`
- `2018_KFE_SINGULARITY_ROOT_CAUSE_REMAINS_UNRESOLVED_AFTER_FIRST_EVENT_CAPTURE`

Do not call a MATLAB-faithful convention a defect without source/parity evidence.

## 12. No scientific repair

Even if the cause is obvious, do NOT change:

- HJB equations/parameters;
- KFE equations;
- grids/tolerances/iteration limits;
- policy/upwind logic;
- transfer FOC;
- adjustment cost;
- raw-Vb logic;
- illiquid-return taper;
- generator assembly;
- contaminated-row index/RHS/normalization;
- household aggregation;
- migration/firm/wage/monetary/fiscal blocks;
- outer controllers;
- annual input.

Forbidden: pseudoinverse, regularization, alternate solver, fallback density, changed contaminated row, second scientific run.

## 13. If singularity does not recur

If 2018 completes without singularity, preserve all outputs and STOP. Do not rerun and do not automatically accept full 14-year coverage.

Terminal:

`2018_FIRST_SINGULARITY_NOT_REPRODUCED_AFTER_HJB_LEDGER_REPAIR__NO_SECOND_RUN`

## 14. If infrastructure fails again

Do not launch a second child. Persist traceback/ledgers/receipts and STOP.

## 15. Required report

Write:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_HJB_LEDGER_CLOSURE_REPAIR_AND_DURABLE_FIRST_SINGULARITY_REEXECUTION_REPORT.md`

Expected successful diagnostic terminal:

`MP4C_2018_DURABLE_FIRST_SINGULARITY_CAPTURE_COMPLETE_AFTER_HJB_LEDGER_REPAIR__POSTMORTEM_CLASSIFIED_OR_BOUNDED__NO_SCIENTIFIC_REPAIR_NO_RETRY`
