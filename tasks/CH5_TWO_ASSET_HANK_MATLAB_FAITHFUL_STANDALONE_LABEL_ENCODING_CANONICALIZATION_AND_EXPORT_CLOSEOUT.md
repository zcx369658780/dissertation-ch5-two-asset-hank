# CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / transfer closeout executor

Owner: final scientific authority

## 1. Purpose

Close the standalone two-asset HA export after the predecessor established that the **only** failed comparison objects were two policy-label arrays whose categories are scientifically identical but encoded differently:

- standalone/source-level representation: strings `B/F/0`;
- accepted persisted artifact representation: integers `-1/+1/0`.

The predecessor already proved that HJB value, all continuous policies, all sparse operators, KFE contaminated system/solve/density, and all stationary aggregates are exact matches. This successor must therefore:

1. prove the label difference is representation-only and comes from artifact serialization/encoding rather than policy logic;
2. freeze the exact bijection `B -> -1`, `F -> +1`, `0 -> 0`;
3. create a representation-aware **comparator-only** canonicalization for the two label arrays;
4. run exactly one replacement comparator against the already persisted standalone validation artifact and accepted Python artifact;
5. if PASS, publish the previously preserved standalone export and focused test **byte-identically**, without rerunning the standalone scientific model.

This is not authority to change the standalone implementation, the modular household implementation, the accepted artifact, any numerical tolerance, GE logic, or dynamics.

## 2. Controlling authority

Read and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_AND_FINAL_ACCEPTANCE_RERUN_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_KFE_COMPARATOR_NUMPY_BOOL_SERIALIZATION_CORRECTION_AND_CLOSEOUT_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_END_TO_END_AGGREGATE_COMPARATOR_NUMPY_BOOL_SERIALIZATION_CORRECTION_AND_CLOSEOUT_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_AND_TRANSFER_VALIDATION_REPORT.md`

Accepted household authorities remain frozen:

- `MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_ACCEPTED`
- `MATLAB_PYTHON_TWO_ASSET_HA_FAITHFUL_HJB_OPERATOR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_STATIONARY_KFE_CONTAMINATED_ROW_AND_SAME_OPERATOR_DENSITY_PARITY_ACCEPTED`
- `MATLAB_PYTHON_TWO_ASSET_HA_FAITHFUL_KFE_DENSITY_PARITY_ACCEPTED`
- `MATLAB_PYTHON_TWO_ASSET_HA_FAITHFUL_END_TO_END_STATIONARY_DISTRIBUTION_PARITY_ACCEPTED`
- `MATLAB_PYTHON_TWO_ASSET_HA_FAITHFUL_HOUSEHOLD_AGGREGATE_PARITY_ACCEPTED`

Primary numerical authority:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

GE provenance remains independently unresolved:

`MATLAB_FAITHFUL_GE_STEADY_STATE_CLOSURE_OWNER_PROVENANCE_REQUIRED`

Do not touch GE in this task.

## 3. Live continuity

Task-authoring parent observed before publication:

`1e813f2abc26b106dd5d612ba4d0f090a74019d6`

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task exists on live `main` as a direct child of the predecessor MATERIAL report commit;
3. verify clean worktree;
4. verify predecessor report identity;
5. verify no `exports/matlab_faithful_two_asset_ha.py` or standalone test exists on live `main`;
6. verify accepted modular faithful household source identities remain unchanged.

## 4. Frozen predecessor evidence — reuse only

Preserved validation root:

`D:\ProjectTemp\ch5-standalone-export-validation-20260830-001`

Preserved candidate export:

- expected SHA-256: `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`
- size: `27,961` bytes
- line count: `633`

Preserved focused test:

- expected SHA-256: `3FB6BAA4FB757C4CA3732035CE8E6A72368315E27C96A98CA41573327FC91CF8`

Preserved standalone scientific validation artifact:

`3DD8F459D7D37885888EC2164BF384510595A5C3960BE8B8B50274126EF51AA6`

Predecessor comparator artifact:

`368B9928F8339733A34EAE41C4E51F0058140F007C050B033E5960B0C68538FE`

Known predecessor engineering evidence, reusable only if candidate/test hashes match exactly:

- `py_compile`: PASS
- dependency allowlist: standard library + NumPy + SciPy only
- focused tests: `5 passed`
- clean-room isolated import: PASS
- no `ch5_two_asset_hank` module loaded
- no GE/dynamics API

Known predecessor scientific evidence:

- standalone HJB converged in exactly `12` iterations
- convergence statistic `9.07700581365134e-10`
- all numerical HJB arrays exact
- all sparse operators exact
- KFE contaminated system, raw solve, normalization, density exact
- all five aggregates exact
- only `liquid_label` and `transfer_label` failed representation identity

Accepted aggregate regression values:

- `C_ss = 1.1296890749136979`
- `L_ss = 0.7341069339182127`
- `A_ss = 0.44059476682729026`
- `B_ss = 0.4601208223181049`
- `A_ss+B_ss = 0.9007155891453952`

Do not regenerate any of these scientific objects.

## 5. Mandatory label-encoding provenance audit

Before comparator modification, perform a read-only audit proving all of the following:

1. the accepted modular faithful source-level policy/HJB path represents labels as strings `B/F/0`;
2. the standalone candidate preserved the same source-level string categories and did not alter branch logic;
3. the accepted persisted artifact stores the same labels as integer codes `-1/+1/0` because of its artifact serialization/runner encoding convention, not because the accepted modular scientific implementation uses different branches;
4. at every state, the exact observed pairing is only:
   - `('B', -1)`
   - `('F', +1)`
   - `('0', 0)`;
5. there is no unmapped category, missing category, state mismatch, branch mismatch, ordering mismatch, shape mismatch, or dtype-dependent scientific calculation;
6. the label arrays are diagnostic/categorical outputs and are not consumed downstream by the already persisted standalone HJB/KFE/aggregate numerical objects after the scientific run.

If any point fails, stop with MATERIAL/BLOCKED as appropriate. Do not modify source.

If all points pass, freeze:

`MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_IS_REPRESENTATION_ONLY`

and the canonical mapping:

`MATLAB_FAITHFUL_POLICY_LABEL_CANONICAL_MAP_B_NEG1_F_POS1_ZERO_0`

## 6. Allowed comparator-only normalization

Create a new comparator artifact. Do not overwrite the predecessor comparator.

The only scientific-comparison change allowed is to canonicalize the two label arrays **on comparison copies only** using the exact frozen bijection:

- `B -> -1`
- `F -> +1`
- `0 -> 0`

Apply this only to:

- `liquid_label`
- `transfer_label`

Do not canonicalize any other field.

Do not modify:

- standalone export bytes;
- accepted modular source;
- accepted artifact bytes;
- grid/order;
- HJB values;
- policies;
- operators;
- KFE objects;
- density;
- aggregates;
- sparse exact-zero rules;
- numeric tolerances;
- PASS/MATERIAL aggregation except that the two labels are compared after the pre-authorized representation normalization.

Every comparator change line must be classified:

`STANDALONE_LABEL_ENCODING_COMPARATOR_CANONICALIZATION_ONLY`

## 7. No-science canonicalization preflight

Before the replacement comparator, freeze/hash:

- candidate export;
- focused test;
- standalone validation artifact;
- accepted Python artifact used by predecessor;
- predecessor comparator;
- corrected comparator;
- exact comparator diff;
- label-provenance audit;
- canonical mapping manifest;
- successor call ledger.

Run exactly one no-science preflight proving:

1. string arrays containing only `B/F/0` map losslessly to `-1/+1/0`;
2. integer arrays containing only `-1/+1/0` remain identical under canonicalization;
3. unknown string category fails closed;
4. unknown integer category fails closed;
5. shape/order remain unchanged;
6. a synthetic state-category mismatch still FAILS after canonicalization;
7. a synthetic numerical mismatch still FAILS;
8. no non-label field changes.

If preflight fails, stop BLOCKED without consuming comparator budget.

## 8. Execution budget

Exactly zero model/scientific reruns:

- MATLAB scientific calls: `0`
- modular Python scientific rerun: `0`
- standalone scientific rerun: `0`
- HJB/KFE/aggregate reruns: `0`
- focused regression rerun: `0` unless required solely to verify file identity/import after publication; such a run must not execute the scientific solver
- replacement comparator: at most `1`

The predecessor standalone scientific run is the only scientific run for this export candidate.

## 9. Replacement comparator acceptance

Run exactly one replacement comparator using the already persisted predecessor standalone validation artifact and the accepted Python artifact.

PASS requires:

1. candidate/test hashes match the preserved predecessor identities exactly;
2. all predecessor numerical exact-PASS objects remain exact PASS;
3. sparse representation handling remains unchanged;
4. both label arrays match exactly after the frozen `B/-1`, `F/+1`, `0/0` canonicalization;
5. no raw state/category mismatch exists;
6. material mismatch list is empty;
7. unresolved scientific residual list is empty;
8. source/environment failure list is empty;
9. dependency/clean-room evidence remains applicable because candidate bytes are unchanged.

If PASS, classify the historical two-label difference only:

`LABEL_ENCODING_REPRESENTATION_ONLY_DIFFERENCE`

Do not claim raw dtype equality.

## 10. PASS publication and freezes

If the replacement comparator PASSes, restore from the preserved root and publish **byte-identically**:

`exports/matlab_faithful_two_asset_ha.py`

with SHA-256 exactly:

`276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

and:

`tests/test_matlab_faithful_two_asset_ha_standalone_export.py`

with SHA-256 exactly:

`3FB6BAA4FB757C4CA3732035CE8E6A72368315E27C96A98CA41573327FC91CF8`

Do not edit either file while restoring/publishing.

Freeze:

- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_DEPENDENCY_AUDIT_PASS`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_CLEAN_ROOM_IMPORT_PASS`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_ACCEPTED`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_LABEL_ENCODING_CANONICALIZATION_ACCEPTED`

Terminal:

`MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT_PASS`

## 11. Non-PASS terminals

If a genuine scientific/category mismatch remains:

`MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT_MATERIAL_MISMATCH`

If artifacts/source/environment prevent controlled closeout:

`MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT_BLOCKED`

On non-PASS:

- do not publish export/test;
- preserve artifacts externally;
- publish report only;
- clean worktree.

## 12. Required successor report

Write:

`docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_STANDALONE_LABEL_ENCODING_CANONICALIZATION_AND_EXPORT_CLOSEOUT_REPORT.md`

Include at minimum:

1. terminal classification;
2. live start/final authority;
3. predecessor report identity;
4. exact candidate/test hashes;
5. label source/artifact provenance audit;
6. exact canonical mapping;
7. corrected comparator hash and exact diff;
8. no-science preflight result;
9. zero-scientific-rerun ledger;
10. replacement comparator hash/result;
11. complete object parity summary;
12. explicit statement that raw label dtypes differ but scientific categories match;
13. all five aggregate values;
14. dependency/clean-room evidence continuity;
15. material/unresolved/source-environment lists;
16. published path hashes if PASS;
17. git closeout evidence;
18. acceptance level and transfer status.

## 13. Repository closeout

On PASS stage only:

- `exports/matlab_faithful_two_asset_ha.py`
- `tests/test_matlab_faithful_two_asset_ha_standalone_export.py`
- successor report

Commit once, non-force push once, GitHub read-back all three paths, require `HEAD == origin/main`, require clean worktree.

On non-PASS stage only successor report.

## 14. Explicit prohibitions

Do not:

- modify accepted modular household source;
- modify the preserved standalone candidate;
- rerun standalone HJB/KFE/aggregates;
- rerun MATLAB;
- alter numerical tolerances;
- canonicalize arbitrary labels/strings;
- hide real categorical mismatches;
- resolve GE provenance;
- run GE;
- run D1-D3;
- run transition/IRF/dynamics;
- add neural-network code.

If PASS, the standalone file is transfer-ready as the accepted MATLAB-faithful two-asset HA numerical baseline/oracle. No further household scientific gate is required before transfer.
