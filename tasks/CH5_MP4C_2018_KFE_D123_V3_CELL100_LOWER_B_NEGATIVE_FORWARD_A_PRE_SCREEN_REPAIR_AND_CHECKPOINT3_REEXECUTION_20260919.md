# Task — V3 cell100 lower-b negative forward-a pre-screen repair and checkpoint-3 reexecution

Date: 2026-09-19

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Task ID:
`CH5_MP4C_2018_KFE_D123_V3_CELL100_LOWER_B_NEGATIVE_FORWARD_A_PRE_SCREEN_REPAIR_AND_CHECKPOINT3_REEXECUTION_20260919`

## Governance

Owner remains final scientific authority. ChatGPT is L3 Reviewer/scientific-route authority. Codex is bounded Builder.

Never enter, read, search, use or modify:
`zcx369658780/deep-learning-hank`.

Fresh-fetch live `origin/main`. Read in order:

1. `AGENTS.md`
2. `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
3. `docs/CH5_TWO_ASSET_HANK_PROJECT_STATUS_CURRENT.md`
4. `docs/CH5_TWO_ASSET_HANK_SESSION_HANDOFF_CURRENT.md`
5. `docs/CH5_MP4C_2018_KFE_D123_CHECKPOINT2_TO_CHECKPOINT6_BOUNDED_NONLINEAR_CONTINUATION_ACCEPTANCE_20260919.md`
6. its execution report and exact V3 cell100 receipt
7. accepted lower-b branch-coverage repair/attribution authority
8. Owner-adopted D1/D2/D3, liquid-`Z`, interior-`a`, joint-switching, lower-`a` zero-kink and nonlinear convergence authorities.

This task repairs an implementation false negative under existing authority. It does not adopt a new scientific law.

## Frozen V3 input

Accepted V3 SHA-256:

`4FDB36C17ACDC60B56661AEE1C0437B65EC4A871FD4E5E08A9A996FB10EF85EF`.

Accepted V2->V3 direct-update artifact SHA-256:

`17F330AB0A226EE826880039055B9EA16FB93DD7B02F36F820D078178C6E4CCF`.

Accepted direct-solve evidence:

- residual infinity norm `1.4391265956703592e-14`;
- backward error `2.439151619375125e-16`;
- `Delta=1000`;
- solver `scipy.sparse.linalg.spsolve`.

Do not rerun or alter the V2->V3 solve.

## Frozen V3 cell100 evidence

State:

- flat F index `100`
- zero-based `(i_b,i_a,i_z)=(0,5,0)`
- `(b,a,z)=(-2.0,2.6315789473684212,0.8)`.

Derivatives:

- `p_b^B=p_b^F=0.005156057482672655`
- `p_a^B=0.012088089476579748`
- `p_a^F=0.01134140574122853`.

Current selector census has only seven candidates and omits active lower-b / negative-transfer / forward-`a`.

The accepted Reviewer attribution proves that this omission is caused by the coarse 513-point unbounded log viability pre-screen.

For forward-`a` negative transfer, the exact legal interval is:

`q_b in (0.012601561934698366,0.015751950034835593)`.

The current log screen jumps from approximately

`0.005156057482672656`

directly to

`0.020837512395988838`

and therefore misses the complete legal interval.

Static liquid-drift signs prove one unique active-face root exists inside the legal interval.

Do not hard-code the eventual root or selection outcome.

## Required minimal implementation repair

Repair **only** the active lower-b / negative-transfer branch viability filtering so that a legal interior-`a` derivative direction cannot be dropped by sparse sampling of an unbounded shadow domain.

Preferred minimal semantics:

- active lower-b negative transfer must represent every distinct one-sided interior-`a` derivative branch authorized by the frozen upwind law;
- let the existing exact scalar-root procedure plus transfer-sign, direction, KKT, domain, finite-value and Hamiltonian gates determine whether each represented branch is admissible.

An exact analytic viability-interval test is also acceptable if it is mathematically equivalent and generic. A coarse sampling screen must not be used as proof of branch nonexistence.

Preserve exactly:

- upper-b active logic;
- lower-b positive and zero-kink logic;
- liquid-`Z`;
- one-axis interior-`a`;
- joint switching;
- lower-`a` zero-kink;
- D1/D2/D3;
- root routine/tolerance;
- grid/calibration;
- HJB update and convergence laws.

No new tolerance, clipping, derivative floor, optimizer, widening, retry, or special cell ID.

## Focused engineering gate

Before scientific execution, cover at minimum:

- exact V3 cell100 now represents active lower-b negative/backward-`a` and negative/forward-`a`;
- V3 forward-`a` negative branch is not removed by the coarse-log gap;
- its root, if invoked in the focused fixture, lies inside the authority-backed legal interval and satisfies negative transfer plus forward `a` direction;
- V2 cell100 previously accepted one-axis interior-`a` selection remains unchanged;
- lower-b candidate completeness from the earlier repair remains intact;
- upper-b active-domain logic unchanged;
- positive active no-root behavior unchanged;
- liquid-`Z`, interior-`a`, joint switching and lower-`a` zero-kink non-regression;
- D1/D3/KKT and D2 zero-drift tests pass;
- no tolerance weakening/assertion deletion.

Engineering fixture calls are separate from the scientific budget.

## Scientific execution

Only after preflight passes:

1. load the exact accepted V3 value field once;
2. perform exactly one fresh V3 policy-map attempt;
3. stop at first fail-closed selector cell;
4. if all 800 cells pass, assemble exactly one Q3 under unchanged D2;
5. if Q3 passes, evaluate exactly one complete checkpoint-3 diagnostic:
   - `B3`;
   - `D3=||V3-V2||_inf`;
   - policy/operator/switching changes versus checkpoint 2;
   - Q3 identity and `Q3-Q2` norm;
   - primary convergence first;
   - exact-cycle rule;
   - approximate cycle only if a complete authorized window exists.

At checkpoint 3 no complete approximate period-2 or period-3 window exists; record this rather than inventing a cycle test.

Do **not** execute V3->V4 in this task.

## Scientific budget

- V2->V3 reruns: `0`
- fresh V3 policy maps: `1`
- selector evaluations: `<=800`
- scalar/liquid-Z/interior-a/joint roots: natural calls only; record exact counts
- Q3/D2 assemblies: `<=1`
- complete checkpoint-3 evaluations: `<=1`
- direct HJB solves/updates: `0`
- graph/SCC/topology/KFE/SVD/eigen/nullspace/`Q.T@p`: `0`
- MATLAB/source-faithful/production/outer/firm/GE/annual/shock/IRF/Results: `0`
- scientific retries/solver substitutions/damping/relaxation/adaptive Delta/continuation/clipping/artificial diffusion: `0`.

If the map fails at a different scientific object, stop without repair.

## Evidence

Use a fresh no-overwrite evidence root.

Persist:

- accepted V3 binding;
- pre/post scientific-code hashes;
- focused tests;
- exact scientific ledger;
- durable cell receipts through failure or all 800;
- explicit V3 cell100 branch census;
- P3/u3/Q3 and checkpoint metrics if reached;
- sealed manifest/readback;
- changed-file list and final Git status.

## Deliverable

Write:

`docs/CH5_MP4C_2018_KFE_D123_V3_CELL100_LOWER_B_NEGATIVE_FORWARD_A_PRE_SCREEN_REPAIR_AND_CHECKPOINT3_REEXECUTION_REPORT.md`

Commit and ordinary non-force push one task branch.

Do not merge main.
Do not publish a successor task.
Do not modify CURRENT files.

Results eligibility remains `FALSE`.
