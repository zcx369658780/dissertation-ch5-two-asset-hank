# CH5_TWO_ASSET_HANK_MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_CORRECTED2009_ONE_SHOT_AND_CHRONOLOGICAL_ADJUDICATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / instrumented MATLAB observability executor / chronological parity adjudicator

Owner: final scientific authority

## 1. Explicit Owner reauthorization

The Owner has explicitly authorized exactly this bounded scientific execution:

> 同意重新授权一次 instrumented MATLAB corrected-2009 stationary one-shot，使用已冻结的 path-normalized candidate wrapper，仅增加 observability；禁止修改方程、参数、网格、算法、controller、容差与更新顺序；MATLAB stationary=1，rerun=0。

This task operationalizes that reauthorization and nothing broader.

Hard scientific budget:

- instrumented MATLAB corrected-calendar-2009 stationary top-level execution: maximum `1`;
- reruns after the first MATLAB launcher begins: `0`;
- other MATLAB model/scientific executions: `0`;
- Python stationary / HJB / KFE / household / MP2 / MP3 scientific executions: `0`;
- comparator replay / `compare_terminal`: `0`;
- Zhejiang or Shanxi standalone scientific replay: `0`;
- other year / annual batch: `0`;
- shocks / AR(1) / transition / dynamics / IRF: `0`;
- historical R5 / Results: `0`.

A launcher failure before the stationary call does **not** create a repair-and-rerun lane. Once the single MATLAB launcher process begins, this task has no second MATLAB process.

## 2. Required live continuity

Required execution-start predecessor:

`b2d6a1ed6c230f2c2be1ca3acf08109753defe57`

Accepted predecessor terminal:

`MP4B_ZERO_SCIENCE_COPY_BINDING_PATH_NORMALIZATION_REMEDIATION_READY_FOR_OWNER_REAUTHORIZATION`

Accepted predecessor classification:

`PATH_REPRESENTATION_ONLY`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `b2d6a1ed6c230f2c2be1ca3acf08109753defe57`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`;
   - predecessor zero-science remediation task/report;
   - predecessor blocked instrumented-run task/report;
   - accepted final-state comparator/localization/provenance reports named below.

Any authority/identity failure before the single launcher begins => stop with zero science.

## 3. Accepted facts that remain frozen before new admissible trace evidence

Do not reinterpret or erase the following accepted facts:

- `MP4B_FINAL_GOVINV_CONTROLLER_BRANCH_COUNT_DIFFERENCE_LOCALIZED`;
- `MP4B_MATLAB_PROFILE_LOW_GOVINV_BRANCH_EXECUTION_COUNT_EXCEEDS_PYTHON_BY_ONE`;
- `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`;
- corrected-calendar-2009 preserved MATLAB and durable Python both terminate at outer turn `184`;
- both represent `5,704` household calls and final `31/31` household convergence;
- common Zhejiang initial `GovInv = 486346722590.8805`;
- preserved MATLAB final Zhejiang `GovInv = 161948418.38387933`;
- durable Python final Zhejiang `GovInv = 179942687.09319925`;
- MATLAB/Python final Zhejiang GovInv ratio is exactly IEEE `0.9`;
- MATLAB bounded controller lattice `(n_low,n_high)=(76,0)`;
- Python bounded controller lattice `(75,0)`;
- Python durable history directly records `75` Zhejiang `LOW_RA_DECREASE_0P9` actions and `0` high-return actions;
- preserved MATLAB profiler aggregate low-return branch-body count `2,337` versus Python aggregate `2,336`;
- high-return branch-body actions `0` on both sides;
- preserved MATLAB profiler records `2,366` Zt-reset events versus Python `2,365`;
- preserved MATLAB prior baseline has wage upper/lower `7/17`, Python has `5/17`;
- no MATLAB/Python fault attribution is accepted yet.

## 4. Protected repository/scientific identities

Require exact SHA-256 unless explicitly identified as a Git blob. Do not modify these objects:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py`:
  `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py`:
  `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py`:
  `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map:
  `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter:
  `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver:
  `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- comparator contract:
  `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map:
  `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- remediated comparator helper:
  `0FD6889E10E502F32C10B3373702445333B452B25A11A63A3899184A67853EF3`
- remediated comparator helper Git blob:
  `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Protected MATLAB source identities:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`
- `HANK_mp_1eq.m` `ED39E661AF951E01D1F5F9D123CE0FAD980F5D3DB33FD338DE60DA87731E0AEF`
- `HANK_mp_1turn.m` `D3D03F37286ED66202673EA63D49BABCE8D5309BAC9C13793C8E60585C21FECF`
- `HANK_firm.m` `EE02C15414ADF9F99AADE04F1F22E64FA7094C8AB77753B6130BC4BFA6CE7BD5`.

Original protected roots:

- logical: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- physical: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Verify the documented exact C Junction -> D relationship. Never use Java-canonical-path trust, broad path prefix, substring containment, or broad-D trust.

The original protected MATLAB tree is strictly read-only and must match all protected hashes both before and after the run.

## 5. Corrected-calendar-2009 immutable binding

Canonical input:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Frozen binding:

- calendar year `2009`;
- analysis/data-MAT index `1`;
- workbook row index `10` / physical Excel row `11`;
- output filename year `2009`;
- regression vintage key `10`;
- industry `4`;
- accepted fixed-2020 `IND_Zt` initialization anchor only in its already source-authorized role.

Preserved corrected-2009 MATLAB baseline root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require exact immutable identities:

- `matlab_calendar2009_stationary_output.mat`:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`
- preserved presolver manifest:
  `DF9380BB057ECF24E519BBF03D5A643036CC92B4FFAC18DB411B75783E5AABB9`.

## 6. Frozen path-remediation evidence and candidate wrapper authority

Read-only remediation package:

`D:\ProjectTemp\ch5-mp4b-copy-binding-remediation-20260831-001`

Require:

- static normalization matrix result SHA-256:
  `4EA6C18F71D16678BFD5E7094EBE143C22C31D380182E7518602228AEFE28345`
- failed-to-candidate wrapper diff SHA-256:
  `BE627CC2C5DD953C588F04BF139BB83E35F3ED3DE96FBC50ECF28BA9C6F8874E`
- frozen path-normalized candidate wrapper SHA-256:
  `DA998FB04C35EE852F53A504D5F4EB17EC089A8EC616A082F38E2B5CB2CD5A93`
- scientific invocation body SHA-256 identical old/new:
  `80BAEDE65829F6A1215638F556544C92CAAB203A149FA6D1D88196BE22045F45`
- body length: `5,424` characters.

The candidate wrapper is immutable for this task. Do not edit it.

The accepted normalization contract is representation-only:

- normalize `/` to `filesep`;
- safely remove trailing separators above drive-root length;
- reject non-absolute paths;
- require `source_copy` to equal the exact normalized `fullfile(run_root,'source_copy')`;
- for each required helper require exact normalized equality of `fileparts(which(helper))` to that exact copied root;
- case-insensitive equality only;
- reject sibling, parent, unauthorized child, same-prefix-other-directory, relative path, different drive, protected original root, and helper-parent mismatch;
- no Java canonical path;
- no broad `startsWith`/substring membership;
- no MATLAB search-order change.

The prior static matrix is accepted as `13/13 PASS`; this task does not need a second static redesign.

## 7. Fresh run root and copied-source reconstruction

Create exactly one fresh no-overwrite external run root under `D:\ProjectTemp`.

Preferred deterministic name:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-owner-reauthorized-20260831-001`

If it already exists, choose the next deterministic fresh suffix before any MATLAB process. Do not delete, overwrite, rename, or reuse an existing scientific run root.

Before creating/copying:

- record D: free space;
- require at least `max(10 GiB, 5 * protected_source_tree_size)`.

Create `<run_root>\source_copy` from the protected MATLAB source tree, preserving relative paths.

Before instrumentation:

- hash all copied `.m` files;
- require all 32 protected/copied `.m` identities to match the original protected tree exactly;
- persist a fresh source-copy manifest.

## 8. Reapply the already-audited observability patch only

The predecessor blocked run root is read-only evidence:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-20260831-001`

Require the predecessor instrumentation evidence identities:

- `source_copy_manifest.json` `1F40F2E207BEE74DCE49B5887CB581B39CA9CCA7C6EF7C8E18E13A685673FB59`
- `instrumentation_manifest.json` `2E61A340D3908BE473FB72AAFC4D0076420974B202136E71FDE07FDA1A97684A`
- `instrumentation_manifest_revision_001.json` `903EEFE050D65F9736820E3C0FFAC4E6496A0BFDF94726B14012158A0B38B5D9`
- `instrumentation_patch.diff` `8320F93726ADE831DE4B530551E66A153B7795E3AA7C36315C758D1F2025FDFA`.

Reapply exactly the already-audited observability instrumentation semantics to the new copied tree. Allowed copied-tree mutations are limited to:

- copied `HANK_mp_1eq.m`;
- copied `HANK_mp_1turn.m`;
- copied `HANK_firm.m`;
- namespaced observer helper `MP4B_OBS.m` / equivalent exact predecessor observer semantics.

Do not invent new observability fields unless needed solely to serialize a value the predecessor patch already intended to observe. If the exact predecessor patch cannot be cleanly re-established on the fresh identical source copy, stop before MATLAB rather than redesigning instrumentation.

Never modify copied:

- `HANK_2ASSETS_HJB.m`;
- `HANK3_FOC.m`;
- `HANK3_cost.m`;
- `lab_solve2.m`;
- any economics/data/calibration source.

Instrumentation may only copy already-computed values into diagnostic state and serialize them. It must not:

- assign diagnostic values back into scientific state;
- change equations, parameters, grids, branch conditions, comparison operators, tolerances, multipliers, damping, update order, solver initialization, function-call order, province order, or convergence logic;
- add extra HJB/KFE/household/firm/equilibrium calls;
- introduce retry logic;
- perform per-event disk writes that could perturb the route.

Do not launch MATLAB `checkcode` in a separate MATLAB process. The prior identical observability patch was already syntax-checked; the current scientific budget permits only the one launcher process below.

Persist before launch:

- new source-copy manifest;
- instrumentation manifest;
- complete copied-source instrumentation diff;
- hashes of every modified/added copied diagnostic file;
- a pre-science gate JSON.

## 9. Single frozen launcher execution

Use the frozen path-normalized candidate wrapper from the remediation package without modification.

Construct the single launcher command by substituting only the fresh selected `<run_root>` arguments into the already frozen future launcher pattern. The canonical input SHA argument remains exactly:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

The wrapper must resolve the exact new `<run_root>\source_copy` using the accepted representation-normalization contract.

Immediately before launch persist the exact command text and candidate-wrapper SHA.

Then start exactly **one** MATLAB launcher process.

MATLAB launcher-process budget: `1`.

MATLAB stationary top-level model-call budget within that launcher: maximum `1`.

Rerun budget: `0`.

If the launcher fails at binding, initialization, instrumentation, serialization, or model execution:

- preserve stdout/stderr/terminal evidence;
- do not repair and retry;
- stop.

## 10. Required chronological observability schema

The admissible trace must be sufficient to align MATLAB with the already durable Python 184-turn history in source order.

For every turn and province, persist at minimum the already authorized scalar/vector evidence:

### 10.1 Outer-turn entry / household interface

- turn number;
- province index and raw source province name;
- `rb`, `rah`, `w`, `rb_gap`, `tau`, `Tt`;
- carried `ra`, `wjt`, `GovInv`, `Zt`, `Yt`, `Yt0`, `Kt`, `Lt`, `KNratio`, `tKNratio` where source-available;
- relevant bounds;
- exact household input tuple;
- household outputs `Ct`, household labor aggregate, `At`, `Bt`, `AtTax`, HJB convergence flag/iterations/statistic where already available without extra calls.

### 10.2 Migration / capital / firm

- `Lt_supply`;
- `At`-only capital-allocation inputs and `Kt_supply`;
- newly computed `rah` and its source inputs;
- firm inputs `Kt_supply`, `GovInv`, `Lt_supply`, `Zt`, `alpha` and source-carried values;
- pre-clip `wt0`, `ra0` and exact source clip predicates;
- firm outputs `Kt`, `Lt`, `Yt`, `mt`, `KNratio`, `wjt`, `rk`, `ra`, `Govinc`;
- new composite `w`.

### 10.3 Policy / controller

- Taylor inputs that are actually source-available at that point;
- `it`, `rb`;
- final household-convergence count;
- ra/wage upper/lower counts;
- max KN-ratio gap and adaptation eligibility;
- `Yt/Yt0 - 1`;
- Zt-reset predicate and before/after values;
- `ra < ramin + 0.02` predicate;
- `ra > ramax - 0.02` predicate;
- `GovInv` before/after;
- action exactly one of `NONE`, `LOW_RA_DECREASE_0P9`, `HIGH_RA_INCREASE_1P1`;
- `tKNratio` before/after;
- outer convergence predicates and terminal decision.

Prefer one terminal serialization after the solve rather than per-event disk IO.

## 11. Mandatory baseline-admissibility gate before any causal interpretation

The new instrumented run is not scientific evidence until it reproduces the immutable preserved MATLAB baseline.

At minimum require exact categorical/controller reproduction:

- outer turns `184`;
- household calls `5704`;
- final household convergence `31/31`;
- adaptation-eligible province-turns `3100`;
- low-return `0.9` actions `2337`;
- high-return `1.1` actions `0`;
- Zt resets `2366`;
- preserved MATLAB wage upper/lower `7/17`;
- preserved MATLAB ra upper/lower `0/0`.

Also compare all 31-province frozen final fields from the accepted final-state field map against the preserved MATLAB baseline under the already accepted province-name representation projection:

`Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`

No post-hoc tolerance widening is allowed.

For categorical/discrete fields require exact equality.

For continuous final fields first report bitwise/exact equality; if any are not bitwise exact, report raw absolute/relative/normalized differences and apply only already frozen comparison semantics where applicable. Any material scientific perturbation attributable to instrumentation makes the trace inadmissible.

If the instrumented execution does not reproduce the preserved MATLAB baseline sufficiently to establish non-perturbation:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_BASELINE_PERTURBED__TRACE_NOT_ADMISSIBLE`

Stop. Do not use that trace for MATLAB/Python fault attribution and do not rerun.

If admissible, establish:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_BASELINE_ADMISSIBLE`

before chronological comparison.

## 12. Chronological MATLAB/Python alignment after admissibility only

Use the already durable Python corrected-2009 terminal/history artifacts read-only. Do not rerun Python.

Align by exact turn, exact province, and exact source layer in this order:

`outer entry -> household input -> household output -> migration -> At-only capital/rah -> firm input -> firm pre-clip -> firm output -> composite wage -> Taylor/policy -> fiscal -> controller/adaptation -> next-turn carry`.

Do not replace final firm `Lt` with household labor.

Do not use `At+Bt` as productive capital where source uses `At`.

For each aligned layer distinguish explicitly:

1. earliest bitwise/nonzero numerical difference;
2. earliest continuous difference exceeding an already frozen comparison gate, if such a gate exists for that field/layer;
3. earliest categorical/branch difference;
4. earliest controller-action difference;
5. earliest difference that is demonstrated to propagate into the accepted final-state mismatch.

A tiny floating-point difference is not automatically a scientific root cause.

## 13. Required localization of the extra MATLAB actions

If the trace is admissible, locate exactly:

- the one MATLAB low-return `0.9` action absent from Python aggregate history;
- the one MATLAB Zt-reset event absent from Python aggregate history.

For each event report:

- turn;
- province;
- raw province names/comparison key;
- same-turn `ra`, `ramin`, `ramax`, threshold `ramin+0.02`, signed threshold distance;
- `GovInv` before/after;
- `Zt` before/after;
- `Yt/Yt0-1`;
- `KNratio`, `tKNratio`, max_nk_gap/adaptation eligibility;
- `wjt` and wage-boundary status;
- all immediately upstream source-layer values sufficient to determine whether the controller merely consumed already-divergent inputs.

Specifically test whether the prior Python turn-154 Zhejiang near-threshold state is or is not the actual chronological divergence. Do not privilege turn 154 unless the admissible MATLAB trace proves it.

## 14. Same-input fault-adjudication rule

Fault attribution requires a source-backed same-input violation.

For any candidate first divergence, determine whether MATLAB and Python enter that exact source layer with scientifically equivalent inputs under the frozen layer-specific comparison contract.

Only if same-input is established may you attribute an implementation defect:

- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`

Examples of valid same-input evidence include exact/sufficiently frozen-equal input tuples followed by different branch/output behavior where one side violates protected MATLAB source semantics or accepted scientific authority.

If controller inputs already differ, the controller action itself is not the root cause. Trace upstream.

If an upstream trajectory difference is localized but no same-input violation is established, use:

`MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`

If chronology is localized but causal fault remains unresolved, use:

`MP4B_CHRONOLOGICAL_FIRST_DIVERGENCE_LOCALIZED`

Do not classify MATLAB as wrong because it historically executes one extra `0.9` action. Do not classify Python as correct because it was AI-authored. Do not classify Python as wrong merely because MATLAB is the legacy implementation.

## 15. Required output artifacts

Keep all copied MATLAB source, MAT/HDF5/JSON/CSV trace artifacts, logs, wrappers and large diagnostic outputs outside Git under the fresh `D:\ProjectTemp` run root.

Persist and hash at minimum:

- run manifest;
- exact launcher command;
- source-copy manifest;
- instrumentation manifest;
- instrumentation diff;
- terminal status;
- chronological trace artifact;
- compact trace summary;
- baseline-admissibility result;
- chronological MATLAB/Python alignment/localization result;
- stdout/stderr/log evidence;
- final copied-source hashes.

Do not overwrite predecessor artifacts.

## 16. Terminal classifications

If any pre-launch authority/identity/copy/instrumentation/candidate-wrapper gate fails:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_CORRECTED2009_ONE_SHOT_BLOCKED_PRELAUNCH`

If the single launcher begins but fails before an admissible completed stationary trace:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_CORRECTED2009_ONE_SHOT_BLOCKED_NO_RERUN`

If completed but instrumentation perturbs the preserved MATLAB baseline:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_BASELINE_PERTURBED__TRACE_NOT_ADMISSIBLE`

If admissible and chronology is localized without same-input fault attribution:

`MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_CORRECTED2009_ONE_SHOT_AND_CHRONOLOGICAL_ADJUDICATION_PASS`

with one of:

- `MP4B_CHRONOLOGICAL_FIRST_DIVERGENCE_LOCALIZED`
- `MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`

If admissible same-input evidence proves a defect, the PASS terminal may additionally establish exactly one of:

- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`.

Do not establish full corrected-2009 MATLAB/Python stationary parity unless all frozen parity requirements independently pass; this task is designed primarily for chronological adjudication of the already accepted mismatch.

## 17. Required repository report

Write:

`docs/CH5_TWO_ASSET_HANK_MP4B_OWNER_REAUTHORIZED_INSTRUMENTED_MATLAB_CORRECTED2009_ONE_SHOT_AND_CHRONOLOGICAL_ADJUDICATION_REPORT.md`

The report must include:

- exact Owner reauthorization text;
- live continuity and task authority;
- all immutable identities;
- free-space gate and fresh run root;
- protected-root/Junction evidence;
- source-copy pre-instrument identity;
- predecessor observability patch identities and newly applied instrumentation diff/hashes;
- path-normalized candidate-wrapper identity and exact launcher command;
- explicit one-shot scientific ledger;
- launcher/stationary terminal;
- all output artifact paths/sizes/SHA-256;
- original protected-source post-run hash verification;
- baseline-admissibility gate and exact discrepancies, if any;
- complete aggregate event counts;
- exact extra low-return event turn/province if admissible;
- exact extra Zt-reset event turn/province if admissible;
- wage-boundary chronological findings;
- first bitwise difference;
- first gate-material continuous difference;
- first categorical difference;
- first controller-action difference;
- source-layer propagation chain;
- same-input adjudication;
- supported fault classification, if any;
- unsupported hypotheses explicitly separated;
- zero counts for all forbidden scientific routes;
- `git diff --check`;
- exact changed paths;
- forbidden-operation audit;
- execution commit SHA;
- non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

## 18. Repository mutation and closeout

Allowed repository changes:

- the required report above;
- at most one strictly necessary CURRENT roadmap/status line if scientifically required.

Do not commit copied MATLAB source, candidate wrapper, traces, MAT files, logs, data, external manifests, or large diagnostics.

Closeout requirements:

1. `git diff --check` PASS;
2. explicit-path staging only;
3. exactly one execution commit;
4. non-force push;
5. fresh GitHub read-back of every changed repository path;
6. final `HEAD == origin/main`;
7. ahead/behind `0/0`;
8. clean worktree.

Do not auto-publish or execute any later repair, replay, shock, IRF, annual-batch, R5, or Results task.
