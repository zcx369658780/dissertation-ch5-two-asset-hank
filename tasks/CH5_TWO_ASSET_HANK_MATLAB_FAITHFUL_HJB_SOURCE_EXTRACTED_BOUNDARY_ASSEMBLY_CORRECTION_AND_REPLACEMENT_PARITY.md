# CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_SOURCE_EXTRACTED_BOUNDARY_ASSEMBLY_CORRECTION_AND_REPLACEMENT_PARITY

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / executor

Owner: final scientific authority

## 1. Purpose

Correct the now-proven source-extraction defect in the external MATLAB HJB-only evaluator at liquid-grid boundaries, explicitly replace the previously consumed but disqualified MATLAB scientific object with one new source-faithful HJB object, and—only after that replacement object is qualified—perform one bounded Python faithful HJB execution and one comparator execution on the same frozen fixture.

This task exists because the predecessor audit proved that the old external evaluator did **not** reproduce designated `HANK_2ASSETS_HJB.m` iteration-`BB` boundary assembly exactly. The prior MATLAB HJB object therefore remains valuable diagnostic evidence but is no longer an admissible parity oracle.

This task stops before KFE, stationary distribution, steady-state aggregates, asset-tail, transition, IRF, dynamics, calibration extension, or Results.

## 2. Controlling authority

Read and obey:

- `docs/CH5_TWO_ASSET_HANK_OWNER_MATLAB_FAITHFUL_NUMERICAL_APPROXIMATION_AUTHORITY_RESET_AND_GAP_AUDIT_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HOUSEHOLD_PRIMITIVES_BARE_A_FOC_IMPLEMENTATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_ILLIQUID_RETURN_TAPER_AND_DRIFT_GENERATOR_PLUMBING_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_POLICY_LOCAL_INTEGRATION_AND_PARITY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_FULL_HJB_DRIVER_AND_CONVERGED_OPERATOR_PARITY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_NEGATIVE_ITERATION_BB_COMPONENT_ADJUDICATION_AND_HJB_RESUMPTION_REPORT.md`

Accepted predecessor terminal:

`MATLAB_FAITHFUL_NEGATIVE_ITERATION_BB_ADJUDICATION_AND_HJB_RESUMPTION_BLOCKED`

Primary authority:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

The corrected/reference route remains reference-only and must not be repurposed.

## 3. Live authority and continuity

Task-authoring parent observed before publication:

`2f1797779e4c84e6737cc5ff14ae0e0018fc78e6`

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task and all controlling reports exist on live `main`;
3. record live start SHA;
4. verify designated MATLAB hashes;
5. verify accepted faithful primitive/local-policy files have not drifted;
6. verify the predecessor report is the direct accepted parent of this task authority;
7. start from a clean worktree.

Do not begin from uncommitted scientific source changes.

## 4. Explicit replacement authority for the prior MATLAB scientific object

The predecessor frozen MATLAB HJB object:

`3457F51AC0F910EA40FC35A832518B9068456E22DEA4E4783F487976432DDC0A`

is hereby classified:

`DISQUALIFIED_SOURCE_EXTRACTION_OBJECT_RETAINED_FOR_DIAGNOSTIC_PROVENANCE`

Reason: its external evaluator closed the iteration `BB` diagonal only over couplings actually inserted into the sparse matrix, whereas designated `HANK_2ASSETS_HJB.m:155-157` computes diagonal `Y` from the full source expressions before `spdiags` boundary truncation. The old object therefore cannot serve as exact MATLAB-source parity authority.

This task explicitly authorizes **one replacement MATLAB HJB scientific batch** generated by a corrected, pre-frozen source-extracted evaluator.

This is not a tuning rerun of an accepted object. It is a replacement of a disqualified extraction object after a proven evaluator defect.

Historical old-object hashes/call counts must remain recorded; do not overwrite or delete them.

## 5. Designated MATLAB source identity

Designated root:

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Required hashes:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Stop BLOCKED if any identity differs.

## 6. Mandatory exact boundary-assembly source audit — before any scientific HJB execution

Re-read the exact designated iteration `BB` construction and document the full mapping from source arrays to sparse matrix entries.

At minimum audit:

- line 155 `X`;
- line 156 `Y`;
- line 157 `Z`;
- the exact reshaping/stacking used before `spdiags`;
- the exact diagonal offsets supplied to `spdiags`;
- first-liquid-node behavior;
- last-liquid-node behavior;
- how MATLAB `spdiags` truncates elements that lie outside the matrix for the `-1/+1` diagonals;
- whether diagonal `Y` retains terms whose corresponding outward off-diagonal is truncated;
- the analogous `AAH` boundary assembly and whether it has the same or a different truncation behavior.

The audit must explicitly establish the designated source truth for upper `b` rows where:

- `Ic_F=1`, `sc_F>0`;
- `Idh_B=1` is forced by the upper-`b` override;
- `Idh_F=0`;
- `sdh_B>0` can generate a negative backward coefficient;
- `Z=Ic_F*sc_F/db > 0` contributes to source diagonal `Y` even if the outward `+1` coupling cannot be represented.

Do not impose row closure or off-diagonal nonnegativity on the iteration matrix unless the designated source algebra itself implies them.

## 7. Freeze the source-faithful iteration-operator authority

If the audit confirms the predecessor diagnosis, freeze:

`MATLAB_FAITHFUL_HJB_ITERATION_OPERATOR_FOLLOWS_EXACT_SPDlAGS_BOUNDARY_TRUNCATION`

and:

`MATLAB_FAITHFUL_HJB_ITERATION_BB_MAY_HAVE_SIGNED_OFFDIAGONALS_AND_NONZERO_BOUNDARY_ROW_SUMS`

Interpretation:

- iteration `BB/AAH/A` are source-faithful HJB discretization/update matrices;
- iteration off-diagonal nonnegativity is not required;
- iteration exact row-sum closure is not required at boundaries when the designated `Y`/`spdiags` algebra does not close;
- no coefficient may be clipped, abs-transformed, renormalized, re-closed, or netted to manufacture generator properties;
- exact source `X/Y/Z`, sparse-diagonal placement/truncation, ordering, and implicit HJB update are the faithful contract;
- this authority applies ONLY to the iteration HJB operator;
- it does NOT alter the post-convergence pre-KFE operator contract, which must still be reconstructed from final net drifts exactly as designated MATLAB does.

If the exact source audit does not support this interpretation, stop BLOCKED and report the smallest contradiction.

## 8. Corrected source-extracted MATLAB HJB-only evaluator

Create a fresh external evaluator by correcting only the proven extraction defect.

Relative to the predecessor evaluator `E81AB34611E3C31DAF2400ED6A34B58F91C4FA0E0FBCCEE843828F5A6588DCBA`, the scientific correction must be limited to exact MATLAB sparse-diagonal assembly semantics for iteration `BB` and, if required by the source audit, the analogous exact assembly semantics for `AAH`.

Every changed evaluator line must be classified:

`SOURCE_EXTRACTED_SPARSE_BOUNDARY_ASSEMBLY_CORRECTION_ONLY`

Do not change:

- parameters;
- grid;
- ordering;
- initialization;
- `lab_solve2` use;
- `Rb`/borrowing-rate logic;
- bare-`a` FOC;
- cost;
- taper;
- `Ic`/`Idh` indicators;
- `Bswitch`;
- HJB matrix/RHS;
- `Delta=1000`;
- `crit=1e-7`;
- `maxit=100`;
- post-convergence net-drift reconstruction;
- output fields;
- comparator rules/tolerances.

Do not modify designated MATLAB source.

## 9. Engineering-only preflight for the corrected MATLAB evaluator

Before the replacement MATLAB HJB scientific run, work in a fresh no-overwrite artifact root and freeze/hash/read back:

- corrected MATLAB HJB-only evaluator;
- exact predecessor-evaluator-to-corrected-evaluator diff;
- source-boundary assembly audit artifact;
- parameter/grid manifest;
- ordering adapter;
- initialization artifact;
- comparator;
- tolerance artifact;
- execution ledger.

Run no converged HJB in the engineering preflight.

Required engineering checks:

1. synthetic upper-`b` case reproduces exact source `X/Y/Z` values;
2. outward `Z` coupling is truncated exactly as MATLAB `spdiags` truncates it;
3. source diagonal `Y` is retained unchanged rather than reclosed over inserted couplings;
4. the resulting synthetic row sum matches the exact source/truncation algebra, even if nonzero;
5. a lower-`b` synthetic case reproduces exact source sparse placement;
6. an interior-`b` synthetic case reproduces exact source sparse placement;
7. at least one source-backed negative backward coefficient is preserved without clipping;
8. `AAH` boundary placement matches exact designated source semantics;
9. post-convergence operator extraction remains unchanged;
10. no HJB iteration/convergence occurs.

If these preflights fail, stop before scientific execution. Do not consume the replacement MATLAB budget.

## 10. One replacement MATLAB HJB scientific batch

Only after the corrected evaluator and all artifacts are frozen and engineering preflights pass, execute exactly one replacement MATLAB HJB scientific batch.

Use EXACTLY the predecessor scientific fixture:

- same parameter/grid manifest `784ADA4834A3FD8CFBCE7C3B5BC652DE63C2A986802603799CE3670860EF6C7A`;
- same 50 states;
- same `5 b × 5 a × 2 z` grid;
- same ordering adapter;
- same initialization artifact `C6662095D14CB83D820FACFB4779CA188BE23958BE162B943BDD2F3959522A9F`;
- same `Delta=1000`, `crit=1e-7`, `maxit=100`;
- same economic parameters.

Do not change the fixture because the corrected evaluator changes convergence behavior.

Replacement MATLAB call budget: at most `1`.

Required persistence before Python execution:

- converged/nonconverged status;
- iteration count;
- convergence statistic;
- full `V`;
- controls/policy outputs already authorized;
- iteration `BB`, `AAH`, `Bswitch`, full `A`;
- post-convergence `BB`, `AAH`, full `A`;
- exact sparse patterns/values;
- boundary-row diagnostics showing source-faithful nonzero iteration row sums where applicable.

If the corrected MATLAB HJB does not converge under the unchanged source fixture, stop BLOCKED. Do not tune or rerun.

## 11. Conditional Python faithful HJB rebuild and engineering preflight

Only after a valid replacement MATLAB HJB object exists, rebuild the distinct faithful Python HJB candidate so it implements the exact same source sparse-boundary semantics.

Preferred faithful modules:

- `src/ch5_two_asset_hank/matlab_faithful_operator.py`
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py`
- minimal faithful-only extension of `src/ch5_two_asset_hank/matlab_faithful_policy.py` if exact source component fields are needed.

Do not modify/repurpose:

- `src/ch5_two_asset_hank/policies.py`
- `src/ch5_two_asset_hank/hjb.py`
- `src/ch5_two_asset_hank/generator.py`
- corrected/reference KFE/steady-state modules.

Python faithful iteration assembly must:

- preserve signed `X/Z` coefficients exactly;
- preserve source diagonal `Y` exactly;
- truncate outward sparse-diagonal entries exactly as MATLAB does;
- not re-close boundary diagonals;
- not validate iteration matrices as Markov generators;
- preserve exact source ordering adapter;
- preserve post-convergence net-drift operator logic separately.

Every scientific change relative to the predecessor candidate architecture must be classified:

`MATLAB_SPARSE_BOUNDARY_ASSEMBLY_FAITHFULNESS_ONLY`

Before the Python scientific call, freeze/hash/read back the rebuilt candidate and run targeted engineering tests only, including exact reproduction of source upper/interior/lower `b` sparse rows and the replacement MATLAB object's boundary operator entries. No converged Python HJB in engineering tests.

## 12. Replacement Python and comparator budgets

After all successor Python artifacts are frozen:

- replacement Python faithful HJB: at most `1`;
- comparator: at most `1`, only after valid Python persistence.

Use the exact replacement MATLAB HJB object and unchanged scientific fixture/tolerances.

If Python fails, do not repair/rerun in this task.

## 13. Comparator contract

Compare all previously authorized full-HJB parity objects.

### Identity/convergence

- state/grid count;
- ordering;
- initialization identity;
- convergence boolean;
- iteration count;
- convergence statistic.

### Full policy/value arrays

- `V`;
- consumption;
- labor;
- transfer;
- adjustment cost;
- effective illiquid return;
- `mu_a`;
- `mu_b`;
- utility;
- liquid labels;
- transfer labels.

### Iteration HJB matrices

- `BB`, `AAH`, `Bswitch`, full `A`;
- sparsity patterns;
- every stored value;
- diagonal values;
- representative upper/interior/lower boundary rows;
- row sums, compared to MATLAB source values rather than forced to zero;
- minimum/maximum off-diagonal coefficients.

Signed off-diagonals and nonzero boundary row sums are acceptable only if they match the corrected source-extracted MATLAB object under the frozen source-spdiags authority.

### Post-convergence pre-KFE matrices

- post-convergence `BB`, `AAH`, full `A`;
- sparsity patterns and values;
- row sums;
- off-diagonal signs.

Do not weaken post-convergence/KFE-facing operator sign/closure expectations because the iteration matrix has different semantics.

### Frozen tolerances

Reuse unchanged predecessor tolerances:

- direct scalar/rate/operator arithmetic: `128*eps64*max(1,abs(x),abs(y))`;
- converged solver-derived `V`: absolute `1e-7`.

No tolerance tuning or loosening.

Categorical/order/sparsity mismatches are terminal.

## 14. Exact scientific call ledger

Historical calls remain recorded separately.

Calls authorized in this task:

- corrected replacement MATLAB HJB: at most `1`;
- replacement Python faithful HJB: at most `1`, only after valid MATLAB replacement object;
- comparator: at most `1`, only after valid Python persistence.

Do not rerun the disqualified old MATLAB evaluator/object.

## 15. Explicit prohibitions

Do not:

- modify designated MATLAB source;
- change parameters/grid/initialization/ordering/Delta/crit/maxit;
- tune fixture or tolerances;
- clip/abs/net/reclose iteration coefficients to enforce generator properties;
- use corrected KKT/candidate machinery in faithful production;
- modify corrected/reference `policies.py`, `hjb.py`, or `generator.py`;
- run KFE;
- run stationary distribution;
- run steady-state aggregates;
- run D1-D3;
- run asset-tail;
- run transition/IRF/dynamics;
- run calibration extension or Results.

## 16. Terminal classifications

Return exactly one:

### PASS

`MATLAB_FAITHFUL_HJB_SOURCE_EXTRACTED_BOUNDARY_ASSEMBLY_CORRECTION_AND_REPLACEMENT_PARITY_PASS`

Use only if:

- corrected evaluator is source-faithful and preflighted;
- replacement MATLAB HJB converges under unchanged fixture;
- Python faithful HJB persists a valid object;
- comparator passes every frozen full-HJB/operator object;
- no forbidden mutation/tuning occurs.

### MATERIAL MISMATCH

`MATLAB_FAITHFUL_HJB_SOURCE_EXTRACTED_BOUNDARY_ASSEMBLY_CORRECTION_AND_REPLACEMENT_PARITY_MATERIAL_MISMATCH`

Use if both corrected MATLAB and Python objects are valid but a frozen numerical/categorical/sparsity comparison fails.

### BLOCKED

`MATLAB_FAITHFUL_HJB_SOURCE_EXTRACTED_BOUNDARY_ASSEMBLY_CORRECTION_AND_REPLACEMENT_PARITY_BLOCKED`

Use if source audit contradicts the frozen interpretation, corrected MATLAB evaluator/preflight fails, replacement MATLAB HJB fails to converge, Python fails before valid persistence, or a source/environment blocker prevents comparison.

## 17. Required report

Write only:

`docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_SOURCE_EXTRACTED_BOUNDARY_ASSEMBLY_CORRECTION_AND_REPLACEMENT_PARITY_REPORT.md`

Include at minimum:

1. terminal classification;
2. live start/final `origin/main`;
3. designated source hashes;
4. old disqualified MATLAB object identity and explicit replacement rationale;
5. exact `spdiags` boundary source audit;
6. exact predecessor-evaluator-to-corrected-evaluator diff;
7. corrected evaluator/preflight/artifact hashes;
8. engineering preflight results;
9. replacement MATLAB scientific call/output hash, convergence, iterations/statistic;
10. iteration `BB/AAH/A` boundary rows, row sums, signs, patterns;
11. post-convergence operator diagnostics;
12. Python faithful candidate diff/hashes and engineering preflight;
13. Python scientific call/output hash and convergence;
14. comparator call/output hash and complete parity summary;
15. maximum differences/worst cases for all full arrays;
16. sparse-pattern/value mismatch counts;
17. historical and current call ledger;
18. complete mismatch list;
19. complete source/environment failure list;
20. changed-path list;
21. git status;
22. acceptance level;
23. exact recommended next gate.

## 18. Next gate boundary

If and only if PASS, recommend only:

**MATLAB-faithful stationary KFE contaminated-row implementation and same-operator density parity.**

Do not authorize full steady-state aggregates or dynamics from this task alone.
