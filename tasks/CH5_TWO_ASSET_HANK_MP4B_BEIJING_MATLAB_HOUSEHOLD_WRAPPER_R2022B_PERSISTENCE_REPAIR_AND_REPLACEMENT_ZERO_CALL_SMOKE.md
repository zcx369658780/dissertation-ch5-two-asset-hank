# CH5_TWO_ASSET_HANK_MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_REPAIR_AND_REPLACEMENT_ZERO_CALL_SMOKE

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded validation-infrastructure remediator

Owner: final scientific authority

## 1. Purpose

Resolve only the pre-scientific MATLAB wrapper-smoke blocker from:

`MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_BLOCKED`

The predecessor successfully established the standalone raw-`Vb` helper and local-policy semantics and froze a Beijing first-turn same-input candidate contract. It then attempted exactly one MATLAB zero-household-call wrapper smoke. The MATLAB process exited without producing the required smoke manifest. Because the live task prohibited repair/retry after that attempt, no household call was executed and all production/export candidates were rolled back.

This successor is **infrastructure-only**. It may diagnose and repair the validation-only MATLAB Beijing wrapper's R2022b-compatible no-overwrite/persistence behavior and execute exactly one replacement zero-household-call wrapper smoke.

It does **not** authorize any protected household/HJB/KFE scientific call, any standalone Python household call, any comparator, any production/export mutation, or any calendar-2009 multi-province stationary execution.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / HANK3_FOC / local-policy / HJB / standalone tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`

Preserve accepted evidence unchanged:

- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_PASS`
- `MP4B_FAITHFUL_RAW_VB_STANDALONE_CANDIDATE_PATCH_FROZEN`
- `MP4B_MODULAR_STANDALONE_RAW_VB_SOURCE_MAP_EQUIVALENCE_PASS`
- `MATLAB_FAITHFUL_STANDALONE_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`
- `MATLAB_FAITHFUL_STANDALONE_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_PASS`

The predecessor did **not** establish:

- `MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_SMOKE_PASS`
- any Beijing household parity marker
- any repaired standalone export acceptance marker.

## 3. Live continuity

Expected execution-start parent:

`ae1f0b0fa88fa13be43a70816aa5c678d386e120`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the predecessor BLOCKED commit;
3. require clean worktree;
4. verify the predecessor report and evidence paths below;
5. verify current production/export bytes remain rolled back;
6. verify protected MATLAB hashes;
7. verify no historical R5 / `chapter5_model` runtime dependency.

Required rolled-back current identities:

- `src/ch5_two_asset_hank/economics.py` exact Git-blob-byte SHA-256:
  `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` SHA-256:
  `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py` SHA-256:
  `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

Required protected MATLAB identities:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Any identity failure => stop before mutation.

## 4. Preserve predecessor scientific/evidence objects read-only

Do not modify or rerun these objects.

### 4.1 Frozen Beijing same-input candidate contract

Path:

`D:\ProjectTemp\ch5-mp4b-beijing-household-20260831-001\beijing_same_input_contract.json`

Required SHA-256:

`FE833FAEB48521CD0C7594627AF6FB5012F9497A455E9B2C5E7490E0C40E6F22`

This remains a **candidate contract**, not yet household-parity acceptance. Do not alter its scientific values in this infrastructure task.

### 4.2 Standalone candidate evidence

Preserve read-only:

- standalone patch:
  `validators/multi_province/mp4b_faithful_raw_vb_standalone_candidate.patch`
- patch SHA-256:
  `FC4DAC660130DEB73E1A88C6638F1C4B282D511AA06875123437693FBE4C5A71`
- predecessor repaired standalone candidate SHA-256:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- standalone semantic replay artifact:
  `D:\ProjectTemp\ch5-mp4b-standalone-prehousehold-20260831-001\standalone_replay.json`
- replay SHA-256:
  `B1EA2D07DB0940CCBAFF76EAED2C844403728A1396748F3B20D7E32FA1D7D0B4`

Do not apply the modular or standalone production patches in this task unless a purely static applicability check is needed; no candidate scientific execution is authorized.

## 5. Predecessor blocker to diagnose

The predecessor launched one MATLAB wrapper smoke in a mode whose static control flow placed the protected `HANK_2ASSETS_HJB` call after the smoke return. Therefore protected household calls remained zero.

Observed terminal evidence:

- smoke attempts: `1`
- MATLAB process exited
- expected `matlab_wrapper_smoke.json` was absent
- no durable `checkcode` result existed
- no exact MATLAB error identifier/message was persisted
- no smoke retry occurred.

Do not infer a cause from missing output alone. Diagnose the validation wrapper/harness statically and, if useful, through zero-protected-call persistence-only checks before the single replacement smoke.

## 6. Required wrapper design

Create one validation-only MATLAB wrapper under:

`validators/multi_province/matlab/`

Preferred path:

`validators/multi_province/matlab/mp4b_beijing_household_wrapper.m`

The wrapper must expose an explicit smoke mode and a future scientific run mode, but this task may execute only smoke mode.

### 6.1 Smoke mode must be provably zero-household-call

Smoke mode must return/terminate before any possible call to:

- `HANK_2ASSETS_HJB`
- `HANK_mp_1turn`
- `HANK_mp_1eq`
- `mpHANK_equilibrium_2000`
- `multi_prov_HANK_12sts`
- any KFE/HJB scientific helper reached through those routes.

Static focused tests must prove the protected household call is dominated by a non-smoke branch and cannot execute in smoke mode.

### 6.2 R2022b-compatible strict no-overwrite persistence

Do not use unsupported `fopen(...,'x')`.

The wrapper must use the already validated R2022b-compatible pattern:

1. require a fresh direct-child run root under `D:\ProjectTemp` or a fresh manifest path inside an explicitly owned fresh root;
2. reject pre-existing file/directory targets;
3. atomically reserve a new artifact using `java.io.File(...).createNewFile()` or atomically create the exact owned run directory with `java.io.File(...).mkdir()` and then reserve the manifest;
4. only open the newly reserved empty file with `'w'`;
5. verify write count / write completion;
6. close the file durably before success returns;
7. never truncate or reuse an existing artifact.

Use no recursive deletion/recreation and no overwrite fallback.

### 6.3 Smoke manifest contents

The replacement smoke manifest must contain at minimum:

- marker `MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_SMOKE_PASS`
- wrapper path and SHA-256
- protected logical/physical source roots
- resolved `HANK_2ASSETS_HJB` path and SHA-256
- exact C:\MatlabProgram -> D:\MatlabProgram junction evidence or reuse of already validated exact-pair evidence
- same-input candidate contract path/SHA
- mode=`smoke`
- `HANK_2ASSETS_HJB_calls=0`
- `HJB_calls=0`
- `KFE_calls=0`
- `multi_province_calls=0`
- `stationary_calls=0`
- `scientific_household_calls=0`
- explicit prohibited-route zero ledger for MP2/MP3, annual batch, shocks, transition, dynamics, IRF, R5 and Results
- MATLAB version/release if cheaply available without scientific execution.

The manifest must be independently readable after MATLAB exits.

## 7. Fail-closed source binding

Before the replacement smoke, the wrapper must verify:

- exact logical model root:
  `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- exact physical model root:
  `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- logical and physical protected source existence
- protected `HANK_2ASSETS_HJB.m` SHA exact
- `which('HANK_2ASSETS_HJB')` resolves to a parent in the exact finite two-root set
- resolved source SHA exact
- sibling/unrelated roots rejected.

Do not rely on Java `getCanonicalPath()` to dereference the junction. Do not use `startsWith`, substring matching, broad `D:\MatlabProgram` trust, arbitrary sibling trust or filename-only matching.

## 8. Infrastructure-only verification before consuming smoke budget

Before the one replacement MATLAB smoke:

1. run focused Python/static tests for:
   - smoke-mode zero-call control flow;
   - no-overwrite artifact reservation contract;
   - exact root/source binding;
   - negative path/source probes;
   - manifest schema/zero-ledger presence;
2. run Python `py_compile` for any Python focused tests/helpers;
3. run MATLAB R2022b `checkcode(...,'-id')` on the wrapper and persist/record the result outside the smoke scientific ledger;
4. run `git diff --check`.

A zero-protected-call persistence-only local test is allowed before the replacement smoke **only if** it does not invoke the actual wrapper smoke entry and does not consume the one replacement smoke budget. Prefer static/unit verification over another MATLAB process.

Required pre-smoke markers:

`MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_STATIC_REVIEW_PASS`

`MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_CONTROL_FLOW_STATIC_REVIEW_PASS`

If either fails, do not consume the replacement smoke.

## 9. Replacement smoke budget

Only after all pre-smoke gates pass:

- MATLAB Beijing wrapper replacement smoke: exactly `1`
- smoke reruns: `0`
- protected `HANK_2ASSETS_HJB` calls: `0`
- protected household/HJB/KFE scientific calls: `0`

Use one fresh no-overwrite root under `D:\ProjectTemp`.

On PASS require:

- MATLAB process exit success;
- smoke manifest exists;
- smoke manifest hash persisted;
- manifest marker exact;
- all scientific call counts exact zero;
- source binding exact;
- independent post-process read-back succeeds.

Establish:

`MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_SMOKE_PASS`

Terminal PASS:

`MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_REPAIR_AND_REPLACEMENT_ZERO_CALL_SMOKE_PASS`

If the single replacement smoke fails, preserve exact command/stdout/stderr/exit and any partial artifact state, then stop. Do not repair or rerun in the same task.

## 10. Scientific/model call budget

All must remain exactly zero in this task:

- MATLAB `HANK_2ASSETS_HJB`
- MATLAB HJB/KFE household scientific calls
- standalone Python household/HJB/KFE
- modular Python HJB/KFE
- MATLAB/Python local-policy scientific reruns
- ten-case MATLAB scalar rerun
- exact-junction smoke rerun
- second-province household
- MATLAB/Python calendar-2009 multi-province stationary
- MP2/MP3 empirical
- annual batch
- shocks
- transition
- dynamics
- IRF
- R5
- Results.

Production/export mutation must remain exactly `0`.

## 11. Allowed repository changes

Allowed only:

- validation-only MATLAB wrapper under `validators/multi_province/matlab/`
- one narrowly scoped focused test/helper for wrapper persistence/control-flow/source-binding
- bounded CURRENT roadmap status update if needed
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_REPAIR_AND_REPLACEMENT_ZERO_CALL_SMOKE_REPORT.md`

Do not modify:

- production modular files
- standalone export
- protected MATLAB
- canonical input/data/cache
- accepted HJB/operator/KFE modules
- MP2/MP3
- historical R5
- preserved ProjectTemp scientific artifacts.

## 12. Required report and closeout

Report:

- terminal verdict
- live continuity
- wrapper/helper hashes
- exact diagnosis of predecessor persistence failure if established, otherwise `UNRESOLVED_BUT_REPLACEMENT_SMOKE_PASS` is acceptable if the new mechanism is independently validated
- no-overwrite/persistence design
- source-binding evidence
- focused tests and `checkcode`
- replacement smoke root/manifest path/SHA
- exact zero scientific-call ledger
- production/export mutation `0`
- forbidden-operation audit
- `git diff --check`
- Git closeout
- exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

## 13. Successor boundary

On PASS recommend exactly one successor:

**resume the Beijing first-turn household parity route from the frozen same-input candidate contract and accepted standalone raw-`Vb` patches, reusing this wrapper smoke PASS; authorize one MATLAB Beijing household call, one repaired standalone Python household call and one comparator only after the remaining source-map/comparator/runner preflight gates are re-established. Calendar-2009 multi-province stationary remains closed.**
