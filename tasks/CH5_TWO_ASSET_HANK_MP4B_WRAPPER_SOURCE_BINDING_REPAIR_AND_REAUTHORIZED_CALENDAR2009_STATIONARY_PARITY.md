# CH5_TWO_ASSET_HANK_MP4B_WRAPPER_SOURCE_BINDING_REPAIR_AND_REAUTHORIZED_CALENDAR2009_STATIONARY_PARITY

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / validation-wrapper repairer / stationary-parity executor / first-divergence diagnostician

Owner: final scientific authority

## 1. Purpose

Repair the validation-only MATLAB calendar-2009 entry after the accepted reauthorized MP4B attempt consumed its single MATLAB top-level invocation before any province household HJB/KFE call because the wrapper omitted a source-required environment binding.

The prior terminal was:

`MP4B_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

with root cause:

`SOURCE_ENVIRONMENT_FAILURE__VALIDATION_WRAPPER_OMITS_GLOBAL_N_PROV_BINDING`

Prior execution/report commit:

`f0b9b5aa3b7479c1813bf33249391b3f42d859d7`

This task has two tightly bounded phases:

1. **static source-binding completeness audit + validation-wrapper repair**, with no scientific model call;
2. **freshly authorized corrected calendar-2009 MATLAB/Python stationary parity**, one top-level run per language maximum, only after all static and presolver gates pass.

Do not widen the scientific model or annual route. Do not modify protected MATLAB.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- `docs/CH5_TWO_ASSET_HANK_OWNER_MP4_MULTI_YEAR_BASELINE_CACHE_AND_FIRST_YEAR_PARITY_DECISION.md`
- `docs/CH5_TWO_ASSET_HANK_OWNER_MP4_ANNUAL_YEAR_AXIS_DECOUPLING_AND_2009_BINDING_ADJUDICATION.md`
- `docs/CH5_TWO_ASSET_HANK_MP4A2_2009_DECOUPLED_ANNUAL_BINDING_CANONICAL_INPUT_AND_MATLAB_PARITY_WRAPPER_PREPARATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_REAUTHORIZED_AFTER_CONTROLLING_RULE_SYNC_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_REPORT.md`
- prior MP4B task and reauthorization task in `tasks/`.

Preserve all accepted MP1-MP4A2 scientific contracts and Owner decisions, including:

- `OWNER_SINGLE_ACTIVE_CODEBASE_TWO_ASSET_HANK_ONLY`
- `OWNER_MP4_FINAL_CONTRACT_MULTI_YEAR_2009_2023`
- `OWNER_MP4_INITIAL_CONTROLLED_ANCHOR_YEAR_2009`
- `OWNER_ECONOMIC_CALENDAR_YEAR_IS_WORKBOOK_CALENDAR_YEAR`
- `OWNER_2009_ANCHOR_USES_EXPLICIT_WORKBOOK_2009_ROW`
- `MATLAB_LEGACY_ANNUAL_YEAR_INDEX_COUPLING_DEFECT_CONFIRMED`
- `OWNER_DERIVED_MAT_CALIBRATION_CACHE_NOT_PRIMARY_SCIENTIFIC_AUTHORITY`
- `MATLAB_FIXED_2020_IND_ZT_RETAINED_AS_SOURCE_NUMERICAL_INITIALIZATION_ANCHOR`
- `OWNER_PARITY_MISMATCH_BOUNDED_ROOT_CAUSE_DIAGNOSIS_AUTHORIZED`
- `MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS` from the prior attempt as evidence only; it must be re-established after repair before the new scientific runs.

Primary reconstruction authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent / prior blocked report commit:

`f0b9b5aa3b7479c1813bf33249391b3f42d859d7`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the prior blocked report commit;
3. require clean worktree;
4. verify the two controlling rule blobs are still present and byte-identical to the accepted rule publications;
5. verify accepted household oracle, MP2, MP3, MP4A2 `annual.py`, canonical artifact identity, primary-source hashes, and current MP4B validation helper identities;
6. verify no legacy `chapter5_model` runtime dependency exists.

If any continuity/authority identity fails, stop `MP4B_WRAPPER_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_BLOCKED` before scientific execution.

## 4. Protected MATLAB source-binding completeness audit — mandatory before repair

Protected source root remains read-only:

`C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

where `C:\MatlabProgram` is the logical compatibility path and `D:\MatlabProgram` is its physical target when the junction resolves as documented.

Read and hash at minimum:

- `multi_prov_HANK_12sts.m`
- `mpHANK_equilibrium_2000.m`
- `HANK_mp_1eq.m`
- `HANK_mp_1turn.m`
- `HANK_2ASSETS_HJB.m`
- every directly called helper needed before the first provincial household invocation.

Before editing the validation wrapper, perform a **source-binding completeness audit** of every state/environment side effect established by the bypassed upper-level source path before `mpHANK_equilibrium_2000` is entered.

Audit explicitly:

- all `global` declarations and assignments;
- persistent/environment variables;
- path-dependent initialization;
- `N_prov` and any other dimension/state scalar;
- parameter/grid/numerics/CHI/inits construction;
- source-defined constants not passed as explicit function arguments;
- source working-directory assumptions that affect data/helper resolution;
- any source-side initialization needed before the first `for i=1:N_prov` household loop.

Required static conclusion:

- either `SOURCE_BINDING_AUDIT_COMPLETE__N_PROV_IS_ONLY_MISSING_REQUIRED_BINDING`,
- or an explicit finite list of additional source-required bindings with source lines and values,
- or `SOURCE_BINDING_AUDIT_BLOCKED` if completeness cannot be established without guessing.

Do not spend another scientific run merely to discover the next missing wrapper environment variable.

## 5. Authorized validation-wrapper repair

The known repair target is source binding:

```matlab
global N_prov;
N_prov = 31;
```

because the protected upper-level route establishes that binding before lower-level execution.

You may modify only validation/helper code under:

`validators/multi_province/matlab/`

as needed to reproduce **all statically proven source-required pre-entry bindings** from Section 4.

Preferred target is the existing validation-only wrapper/helper chain, not protected MATLAB.

Repair rules:

- source-defined binding only;
- no economic formula change;
- no HJB/KFE formula change;
- no convergence/tolerance/clipping/update-order change;
- no calendar/index change;
- no defaulting from old R5 or literature;
- no modification of protected MATLAB;
- no hidden switch to the legacy wrong-year annual route;
- no scientific invocation during repair.

If practical, make the wrapper fail closed before model entry unless `N_prov==31` and all other audited source bindings equal their expected source values.

## 6. Independent static review gate

Before any model run, independently review the repaired validation route against protected source.

Required checks:

1. MATLAB `checkcode` / syntax check on all changed validation `.m` files;
2. source-to-wrapper binding table with exact source lines;
3. static proof that the repair changes only environment/source binding and observability;
4. no changed economic expression relative to the accepted validation route;
5. no change to the frozen calendar-2009 identity;
6. no overwrite risk;
7. protected source remains hash-identical;
8. fresh timestamped run roots will be used;
9. free-space preflight satisfies the controlling local-safety rule.

Required marker before scientific execution:

`MP4B_VALIDATION_WRAPPER_SOURCE_BINDING_REPAIR_STATIC_REVIEW_PASS`

If this marker is not established, run neither language.

## 7. Re-establish presolver same-input identity

Restart the presolver gate after wrapper repair.

Require again:

`MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`

using the full prior MP4B manifest-equality contract.

Frozen 2009 identity remains:

- `calendar_year=2009`
- `analysis_index=1`
- `workbook_data_row_index=10`
- physical Excel row `11`
- `data_MAT_index=1`
- `output_filename_year=2009`
- regression key `10`
- fixed-2020 `IND_Zt` as source numerical initialization anchor only.

Canonical input SHA must remain:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Abort before model execution on any semantic manifest mismatch.

## 8. Fresh scientific execution budget

The previous task's MATLAB invocation is consumed and MUST NOT be reused or called a retry.

This task separately authorizes a **new bounded parity gate**:

### MATLAB

- corrected/decoupled calendar-2009 stationary top-level invocation: **maximum 1**.
- no scientific rerun.

### Python

- corrected/decoupled calendar-2009 stationary top-level invocation: **maximum 1**.
- no scientific rerun.

Internal household HJB/KFE calls are part of the single stationary invocation and remain bounded by source controller semantics.

Ordering:

1. all static/source-binding/presolver gates first;
2. MATLAB first;
3. if MATLAB fails before any province household HJB/KFE call because of infrastructure/environment/wrapper failure, stop BLOCKED and do not run Python;
4. if MATLAB reaches scientific household/model execution but scientifically nonconverges or hits a model-domain failure, preserve the outcome and proceed with the single Python run for comparison;
5. if MATLAB completes, run Python once;
6. after Python invocation, no automatic repair or rerun.

Forbidden calls remain zero:

- legacy wrong-year MATLAB route;
- 2010-2023 batch;
- shock/AR1 response;
- transition/genuine dynamics/IRF Results;
- legacy one-asset R5;
- Results/manuscript claims.

## 9. MATLAB trace and run-root contract

Use a new timestamped no-overwrite root, distinct from all prior MP4B roots.

Persist at minimum:

- source/storage/environment manifest;
- repaired wrapper/helper hashes;
- presolver manifest;
- source-binding manifest including `N_prov`;
- MATLAB terminal status;
- provincial household-call count;
- complete available iteration trace and final stationary object if reached;
- warnings/errors;
- artifact hashes.

Trace instrumentation remains observability-only and must not modify computation.

## 10. Python stationary integration contract

Use the already created `stationary_runtime.py` only after verifying it remains unchanged from the prior blocked commit unless this task finds a purely integration-interface defect before scientific execution.

Before empirical Python execution:

- rerun exact regression against all seven accepted MP3 tiny scenarios;
- require exact controller equivalence;
- verify 31 household solves per turn observe the same copied old-state snapshot;
- verify accepted household adapter/oracle, MP2, and MP3 arithmetic remain unchanged.

Do not modify accepted scientific arithmetic in this task.

## 11. Layer-by-layer parity and first-divergence diagnosis

If both scientific routes execute, compare in the already frozen hierarchy:

1. pre-solver input identity;
2. first-turn household inputs;
3. first-turn household outputs;
4. migration `Lt_mat/Lt_supply`;
5. At-only capital contribution / `Kt_supply` / `rah`;
6. firm block;
7. composite wage / Taylor / fiscal diagnostics;
8. controller gaps/counts/adaptation/damping;
9. subsequent turns in order;
10. final 31-province stationary state.

Persist enough intermediate objects to localize the first divergence.

Allowed root-cause classifications remain:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

If execution stops before two scientific routes exist, use a source/environment/infrastructure blocker classification instead of forcing one of the scientific mismatch classes.

No automatic repair-and-rerun.

## 12. Numerical and qualitative comparison

Use previously accepted same-input parity contracts for household/source-local objects.

Before inspecting new results, freeze any comparison bounds for newly compared outer/final floating objects; no post-hoc loosening.

Qualitative diagnostics are secondary and include, where meaningful for stationary province vectors:

- sign;
- relative direction versus targets/baselines;
- cross-province ranking/order agreement;
- clipping/boundary status agreement.

Qualitative similarity never overrides an unexplained source/formula mismatch.

## 13. Allowed repository changes

Authorized writes are limited to:

- bounded repair of `validators/multi_province/matlab/mp4b_calendar2009_stationary_wrapper.m` and/or existing MP4B validation helpers;
- focused wrapper/source-binding tests/static validators;
- bounded MP4B parity comparator/trace helper changes if required for observability only;
- `stationary_runtime.py` only if a non-scientific integration-interface issue is found before run and the change preserves exact MP3 regression; otherwise leave it unchanged;
- CURRENT roadmap status update;
- one new report:

`docs/CH5_TWO_ASSET_HANK_MP4B_WRAPPER_SOURCE_BINDING_REPAIR_AND_REAUTHORIZED_CALENDAR2009_STATIONARY_PARITY_REPORT.md`

Do not overwrite prior MP4B reports or run roots.

Do not modify:

- protected MATLAB;
- accepted household/HJB/KFE/oracle arithmetic;
- MP2 arithmetic;
- MP3 controller semantics;
- canonical 2009 input;
- primary workbooks/regression/distance/cache;
- historical R5;
- controlling rule files.

Do not commit raw/private data, large MAT outputs, figures, logs, caches, or secrets.

## 14. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. prior blocker identity and consumed prior call ledger;
4. source-binding completeness audit;
5. exact wrapper repair diff and source-line justification;
6. independent static review marker;
7. controlling-rule/storage/no-overwrite preflight;
8. re-established presolver equality;
9. fresh scientific call ledger;
10. MATLAB/Python run roots;
11. province household-call counts;
12. convergence statuses and outer iteration counts;
13. layer-by-layer parity table;
14. exact/within-bound/material classification;
15. first divergent stage/object if any;
16. bounded root-cause classification;
17. qualitative diagnostics if scientific outputs exist;
18. material mismatch / unresolved residual / environment failure lists;
19. tests/checks;
20. forbidden-operation check;
21. Git closeout;
22. exactly one recommended next gate.

## 15. Terminals

PASS:

`MP4B_WRAPPER_SOURCE_BINDING_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_PASS`

Use PASS only if corrected calendar-2009 stationary parity itself is accepted under the frozen comparison contract.

Scientific mismatch:

`MP4B_WRAPPER_SOURCE_BINDING_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_MATERIAL_MISMATCH`

Infrastructure/source/environment/authority blocker:

`MP4B_WRAPPER_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

If static audit reveals that the bypassed MATLAB path requires an ambiguous scientific binding not uniquely determined by protected source, stop:

`MP4B_WRAPPER_SOURCE_BINDING_OWNER_PROVENANCE_REQUIRED`

## 16. Next-stage boundary

Even on PASS, this task does not authorize shocks or 2010-2023.

On PASS recommend exactly one next gate: **MP5A source-law / named-response role freeze and corrected-2009 response preparation**, initially audit/preparation first.

On MATERIAL or BLOCKED recommend exactly one bounded successor addressing only the localized first cause.

## 17. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.
