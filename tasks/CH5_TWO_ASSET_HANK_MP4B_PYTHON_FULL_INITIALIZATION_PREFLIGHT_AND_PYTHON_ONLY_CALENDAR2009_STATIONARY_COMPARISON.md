# CH5_TWO_ASSET_HANK_MP4B_PYTHON_FULL_INITIALIZATION_PREFLIGHT_AND_PYTHON_ONLY_CALENDAR2009_STATIONARY_COMPARISON

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / Python preflight executor / Python-only stationary executor / preserved-MATLAB comparator

Owner: final scientific authority

## 1. Purpose

Use the now-accepted MATLAB/Python eight-cell initial-labor scalar-root parity to perform one final bounded Python-side preflight, then execute exactly one fresh Python-only corrected calendar-2009 stationary invocation and compare it against the already completed immutable MATLAB calendar-2009 run.

The immediately prior accepted scalar-validation terminal is:

`MP4B_INITIAL_LABOR_REPLACEMENT_8CELL_SCALAR_ROOT_PARITY_PASS`

with accepted marker:

`MP4B_INITIAL_LABOR_PYTHON_MATLAB_FROZEN_CELL_ROOT_PARITY_PASS`.

Accepted scalar-parity implementation/report commit:

`199261016e2a1b7382c28b112ec258eca07b029a`

This task MUST NOT rerun MATLAB stationary, HJB, KFE, one-turn, equilibrium, or the multi-province MATLAB model.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decision/adjudication documents
- accepted MP4A2 report
- all prior MP4B tasks/reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_SCALAR_DIAGNOSTIC_HELPER_REVIEW_AND_REPLACEMENT_8CELL_ROOT_PARITY_REPORT.md`
- `validators/multi_province/mp4b_comparison_contract.json`
- current `validators/multi_province/mp4b_python_empirical.py`

Preserve these accepted identities/contracts:

- `OWNER_SINGLE_ACTIVE_CODEBASE_TWO_ASSET_HANK_ONLY`
- corrected calendar-2009 identity
- canonical input SHA-256 `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- accepted standalone HA oracle SHA-256 `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`
- accepted MP2 arithmetic
- accepted MP3 controller semantics
- accepted direct-script bootstrap contract
- `MP4B_INITIAL_LABOR_MATLAB_SOURCE_SEMANTICS_FROZEN`
- `MP4B_PYTHON_INITIAL_LABOR_ZERO_ENDPOINT_IS_NONSOURCE_DOMAIN_ERROR_CONFIRMED`
- `MP4B_INITIAL_LABOR_PYTHON_MATLAB_FROZEN_CELL_ROOT_PARITY_PASS`
- `OWNER_PARITY_MISMATCH_BOUNDED_ROOT_CAUSE_DIAGNOSIS_AUTHORIZED`

Primary reconstruction authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent / accepted scalar-parity commit:

`199261016e2a1b7382c28b112ec258eca07b029a`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the accepted scalar-parity commit;
3. require clean worktree;
4. verify controlling rule blobs and protected-source hashes unchanged;
5. verify accepted oracle, MP2, MP3, MP4A2 annual binding, stationary runtime, bootstrap entry, comparison contract, canonical input, and accepted initial-labor Python repair identities;
6. verify the accepted scalar-parity report and manifests/hashes if locally available;
7. verify no historical `chapter5_model` / R5 runtime dependency.

Any continuity/identity failure => stop before Python scientific execution.

## 4. Immutable preserved MATLAB comparison baseline

The MATLAB calendar-2009 stationary run is already completed and MUST NOT be rerun.

Preserved root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Before Python execution, rehash read-only and require:

- stationary output SHA-256: `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- profile SHA-256: `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- terminal JSON SHA-256: `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`
- terminal status: `COMPLETED`
- outer turns: `184`
- household HJB/KFE calls: `5704`
- final household convergence flags: `31/31` true.

Read-only extraction from the preserved `.mat` is authorized only to compare already-computed final objects. Do not invoke protected MATLAB functions while reading it.

Known descriptive anchors from the preserved run are comparison references only, not Results claims:

- national `sum Ct = 283.3909431582526`
- national `sum At = 47.95553248807161`
- national `sum Bt = 65.2831672243048`
- national `sum Yt = 350556701.89460325`
- final wage-bound counts upper/lower = `7/17`.

## 5. Fresh zero-science bootstrap smoke

Before any Python scientific call, rerun the accepted direct-file bootstrap smoke on a fresh no-overwrite path using the exact entry command form.

The smoke MUST:

- derive only current repository root and current repository `src` from `__file__`;
- verify actual module origins;
- verify oracle SHA;
- reject R5 / `chapter5_model`;
- perform zero household/HJB/KFE/MP2/MP3/stationary calls;
- create exactly one small no-overwrite bootstrap manifest.

Required marker:

`MP4B_PYTHON_DIRECT_SCRIPT_BOOTSTRAP_SMOKE_PASS`

If this fails, stop BLOCKED. Do not consume Python science.

## 6. Fresh calendar-2009 presolver equality

Re-establish the full presolver identity after the accepted initial-labor repair.

Require:

`MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`

with semantic mismatch count exactly `0` across the already frozen annual/prepared contract.

Frozen identity remains:

- calendar year `2009`
- analysis index `1`
- workbook numeric row `10`
- physical Excel row `11`
- `data_MAT_index=1`
- output year `2009`
- regression key `10`
- fixed-2020 `IND_Zt` only as source numerical initialization anchor.

Any semantic mismatch => stop before Python science.

## 7. Mandatory full first-turn Python source-initialization preflight

The prior scientific failure occurred before the first household solver call. Eight-cell scalar parity is now accepted, but this task MUST verify the repaired initial-array construction over the **entire first-turn calendar-2009 household initialization support** before consuming the one-shot Python stationary budget.

This preflight is validation-only. It may execute the repaired scalar initial-labor root construction and the source-identical `c0`/`v02` initialization formulas, but it MUST NOT call:

- `solve_household_steady_state`
- HJB
- KFE
- MP2 one-turn
- MP3/online stationary controller
- any MATLAB model function.

Use the exact first-turn 31-province snapshot and the exact accepted household grid/state ordering that the scientific Python run will use.

For every province and every `(b,a,z)` cell, require and summarize:

- source `x0` finite and strictly inside the real domain;
- repaired bracket endpoints strictly inside the open real domain;
- root finite and strictly inside the open real domain;
- source residual magnitude `<= 1e-10`;
- `c0` finite and strictly positive wherever source initialization requires real utility;
- `v02` finite and real;
- no clipping, epsilon substitution, NaN replacement, formula substitution, or alternate solver path;
- deterministic loop/state ordering identical to the scientific entry.

Persist only a compact validation manifest/summary, not all raw cell values unless needed for a failure witness.

If any cell fails, persist the first deterministic failing province/index/value tuple and stop before Python science.

Required marker:

`MP4B_PYTHON_FULL_FIRST_TURN_SOURCE_INITIALIZATION_PREFLIGHT_PASS`

The manifest MUST report exact total checked province-grid cells and zero household/HJB/KFE/model calls.

## 8. Pre-science regression/static gate

Before the Python one-shot, require all of:

- Python compile PASS;
- focused bootstrap/initial-labor tests PASS;
- all seven frozen MP3 controller scenarios exact PASS;
- online runtime regression PASS;
- accepted household/oracle, MP2, MP3 and stationary-runtime hashes unchanged;
- no active R5 / `chapter5_model` import;
- no-overwrite destination root absent;
- sufficient output-drive free space;
- `git diff --check` PASS for any pre-science validation-only changes;
- comparison contract unchanged.

Required marker:

`MP4B_PYTHON_ONLY_CALENDAR2009_SCIENTIFIC_EXECUTION_PREFLIGHT_PASS`

No Python scientific call without this marker.

## 9. Scientific execution budget

Only after Sections 3-8 PASS:

### Python

- corrected calendar-2009 stationary top-level invocation: maximum **1**;
- scientific rerun: **0**.

Internal household solves are part of that single invocation and bounded by the accepted source controller semantics.

### MATLAB

- stationary / HJB / KFE / one-turn / equilibrium / multi-province calls: **0**.
- scalar diagnostic calls: **0** in this task; scalar parity is already accepted.

If Python fails after scientific entry, preserve the exact outcome and stop. Do not repair and rerun.

Forbidden remain zero:

- wrong-year MATLAB route;
- 2010-2023 annual batch;
- shocks/AR1;
- transition/genuine dynamics/IRF;
- historical R5;
- Results/manuscript claims.

## 10. Required Python evidence

Use a fresh timestamped no-overwrite Python run root.

Persist, as available:

- run manifest and exact code/source identities;
- outer-turn index;
- per-turn household input summary/hash;
- per-turn household outputs `Ct`, household `Lt`, `At`, `Bt`, `AtTax`, convergence flag/statistic;
- household-call count;
- MP2 outputs and controller history where already exposed by current runtime;
- terminal convergence/failure status;
- iteration count;
- final 31-province state;
- national aggregates and categorical boundary/clipping diagnostics;
- artifact hashes.

Do not modify accepted scientific arithmetic merely to increase observability. If some MATLAB upstream traces are unavailable, state that limitation instead of rerunning MATLAB.

## 11. MATLAB-Python comparison contract

If Python produces a final state or a scientifically meaningful partial state, compare against the preserved MATLAB evidence under the already frozen:

`validators/multi_province/mp4b_comparison_contract.json`

Do not change that contract post hoc.

Comparison hierarchy:

1. presolver identity;
2. initial-labor/root layer — now supported by accepted scalar parity and full first-turn preflight;
3. household outputs where comparable evidence exists;
4. migration;
5. At-only capital / `Kt_supply` / `rah`;
6. firm;
7. wage/monetary/fiscal;
8. controller;
9. later turns;
10. final 31-province stationary state.

For preserved MATLAB final state and completed Python final state, compare at minimum:

- convergence status and outer iteration count;
- 31 province order;
- per-province `Ct`, `At`, `Bt`, `Yt`, `w`, `wjt`, `ra`, `rah`, `rb`, `Kt`, `Lt`, `GovInv`, `Zt` where both are available;
- national `sum Ct`, `sum At`, `sum Bt`, `sum Yt`;
- household convergence flags/count;
- `ra` and wage boundary/clipping categories/counts;
- province ranking/order agreement for key final objects;
- absolute, relative and normalized differences required by the frozen comparison contract.

Do not claim a downstream independent mismatch if it is clearly propagated from an already localized upstream divergence.

## 12. Root-cause classification

If a first scientific divergence can be localized, classify only as one of:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

If execution stops before a valid scientific object exists, use an infrastructure/validation blocker classification instead.

No automatic scientific repair or rerun.

## 13. Allowed repository changes

Authorized writes only:

- bounded validation/preflight additions to `validators/multi_province/mp4b_python_empirical.py` if needed to expose a zero-science full-initialization preflight mode without changing scientific formulas;
- focused validation/preflight tests;
- one compact preflight/comparison validator or manifest schema if needed;
- CURRENT roadmap status update;
- one new report:

`docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_FULL_INITIALIZATION_PREFLIGHT_AND_PYTHON_ONLY_CALENDAR2009_STATIONARY_COMPARISON_REPORT.md`

Default: do not modify `stationary_runtime.py`, accepted household/oracle code, MP2, or MP3.

Do not modify:

- protected MATLAB;
- `exports/matlab_faithful_two_asset_ha.py`;
- accepted modular HJB/KFE/household arithmetic;
- MP2 arithmetic;
- MP3 controller semantics;
- canonical 2009 input;
- primary workbooks/regression/distance/cache;
- historical R5;
- controlling rule files;
- preserved MATLAB run artifacts.

Do not commit raw/private data, large scientific outputs, figures, logs, caches, or secrets.

## 14. Terminals

Pre-science blocker:

`MP4B_PYTHON_FULL_INITIALIZATION_PREFLIGHT_BLOCKED`

Python scientific failure after one-shot entry:

`MP4B_PYTHON_ONLY_CALENDAR2009_STATIONARY_SCIENTIFIC_FAILURE_AFTER_FULL_INIT_PREFLIGHT`

Complete comparison package:

`MP4B_PYTHON_ONLY_CALENDAR2009_STATIONARY_COMPARISON_COMPLETE__L3_ACCEPTANCE_PENDING`

The executor MUST NOT self-upgrade a complete comparison to final stationary parity PASS. ChatGPT L3 independent review is required.

## 15. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. accepted scalar-parity evidence/marker;
4. bootstrap smoke command/root/manifest/hash;
5. fresh presolver mismatch count and manifest identities;
6. complete first-turn initialization preflight design and exact checked-cell count;
7. first failing initialization cell if any;
8. pre-science regression/static marker;
9. preserved MATLAB root and reverified hashes/status/184/5704/31-of-31 evidence;
10. call ledger separating validation scalar-root work from scientific household/HJB/KFE work;
11. Python scientific run root and artifact hashes;
12. Python household-call count and outer iterations;
13. Python convergence/failure status;
14. final MATLAB-Python province/national comparison where available;
15. boundary/clipping/ranking comparison;
16. unavailable MATLAB upstream evidence;
17. first supported divergence and root-cause classification;
18. material mismatch / unresolved residual / infrastructure failure lists;
19. tests/checks;
20. forbidden-operation check;
21. Git closeout;
22. exactly one recommended next gate.

## 16. Next-stage boundary

If a complete comparison package is produced, stop for L3 review. Do not enter MP5A automatically.

If Python fails, recommend exactly one bounded successor addressing only the first localized cause.

MATLAB rerun, shocks, and 2010-2023 annual execution remain unauthorized regardless of this task outcome.

## 17. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.
