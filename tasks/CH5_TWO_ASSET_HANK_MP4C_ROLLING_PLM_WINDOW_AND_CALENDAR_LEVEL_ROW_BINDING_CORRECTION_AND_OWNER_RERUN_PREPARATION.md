# CH5_TWO_ASSET_HANK_MP4C_ROLLING_PLM_WINDOW_AND_CALENDAR_LEVEL_ROW_BINDING_CORRECTION_AND_OWNER_RERUN_PREPARATION

Date: 2026-09-02

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / annual-data binding verifier / batch-preparation executor

Owner: final scientific authority and manual batch executor

## 1. Authority basis and route priority

Immediate predecessor L3 acceptance:

`MP4D_SHOCK_AND_IRF_SOURCE_SEMANTICS_FORMALLY_ACCEPTED`

Acceptance commit:

`4a4acf2dc80ed7b79be4ef135c991395ba2aadb2`

Accepted MP4D route classification:

`SEQUENTIAL_STATIONARY_COMPARATIVE_STATICS_RESPONSE_PATH_CONFIRMED`

Do **not** proceed yet to numerical shock implementation.

The Owner has clarified a material annual-data construction that must be corrected before any MP4D numerical response uses annual checkpoints.

The previously published marker:

`MP4C_2009_2023_PYTHON_ANNUAL_STATIONARY_COVERAGE_FORMALLY_ACCEPTED`

is suspended as final annual scientific authority pending this correction and a corrected Owner rerun.

The previous 15-year batch remains valid only as evidence that the solver/runner converged under its then-bound input representation. It is not final calendar-year paper evidence.

The separately accepted corrected-2009 MATLAB–Python stationary parity remains valid under its own frozen same-input contract and must be used as a regression anchor.

## 2. Owner scientific clarification: 10-year rolling PLM windows

The Owner designates the following scientific construction:

- processed annual level data span calendar years `2000–2023`, exactly 24 years;
- annual steady states are reported for `2009–2023`, exactly 15 years;
- each steady-state year `Y` uses a **10-year rolling panel window** `[Y-9, Y]` to estimate province productivity/technology objects using R `plm`;
- the first window is `2000–2009` and produces the 2009 steady-state technology estimate;
- the second window is `2001–2010` and produces the 2010 estimate;
- ...;
- the fifteenth window is `2014–2023` and produces the 2023 estimate;
- existing R estimation outputs are stored in `R语言估计结果_plm估计.xlsx` and correspond to vintages `10–24`;
- the actual MATLAB runtime uses the processed cache `数据估计结果_1000_100_0.mat` generated by `load_GDPdata.m`.

This task must verify and freeze the distinction between:

1. **rolling-window/cache-entry index**, expected `1–15` for steady-state years `2009–2023`;
2. **calendar-year level-data row inside 24-row arrays**, expected `10–24` for calendar years `2009–2023`.

These two indices MUST NOT be silently treated as the same object.

## 3. Suspected current Python defect to verify

Current production cache adapter at the predecessor route contains logic equivalent to:

`entry_index = binding.analysis_index`

`row_index = binding.data_mat_index`

and the current binding gives both indices a `year-2008` style `1–15` value.

The Owner clarification implies the scientifically correct cache interpretation is likely:

- outer rolling-window entry: `window_entry_index = year - 2008` (`1–15`);
- inner calendar level row: `calendar_level_row_index = year - 1999` (`10–24`).

This task MUST verify this from protected source, the runtime cache structure, the R regression workbook, and direct data identities before code mutation.

Do not assume the suspected correction merely from chat text. The Owner designation sets the scientific interpretation; Codex must prove the exact source/cache indexing implementation.

## 4. Required live continuity

At Phase A start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as direct child of `4a4acf2dc80ed7b79be4ef135c991395ba2aadb2`;
3. require `HEAD == origin/main`, ahead/behind `0/0`;
4. require clean tracked worktree;
5. read completely:
   - `AGENTS.md`;
   - all CURRENT project rules named by the rule index;
   - formal corrected-2009 parity acceptance report;
   - prior MP4C scope/data/runtime-cache reports;
   - full 15-year batch execution report;
   - MP4D source-semantics execution and L3 acceptance reports;
   - current `annual.py`;
   - current `mp4c_matlab_runtime_cache.py`;
   - current annual production worker and batch runner;
   - current focused tests.

## 5. Mandatory protected source/data reads

Keep protected MATLAB and data artifacts read-only.

Read at minimum:

- `load_GDPdata.m`;
- `multi_prov_HANK_12sts.m`;
- `mpHANK_equilibrium_2000.m`;
- `main.m` and `main2.m` where annual indexing is assigned;
- any R-integration or regression-sheet loading code called by `load_GDPdata.m`;
- `R语言估计结果_plm估计.xlsx`;
- `数据估计结果_1000_100_0.mat`, expected SHA-256:
  `923CC9E592C14B320C624509A0B498DBCC7D2533F77F0E4B4793521B10849E9A`;
- the processed/filled 2000–2023 workbook used in provenance;
- the Owner-provided/available historical MATLAB steady-state summary workbooks under the protected original and diagnostic-patch trees.

No R estimation rerun is authorized. Existing R output is provenance/authority evidence.

No MATLAB model run is authorized in Phase A.

## 6. Exact rolling-window / calendar-row contract to prove

Build a 15-row contract for calendar years 2009–2023 containing at minimum:

- steady_state_year;
- rolling_window_start;
- rolling_window_end;
- rolling_window_length;
- outer cache/mydata2 entry index (MATLAB 1-based);
- R regression vintage key;
- exact R workbook sheet name used for industry 4;
- inner 24-row level-data calendar row index (MATLAB 1-based);
- calendar level year represented by that row;
- output filename year;
- province-order identity.

Expected pattern to verify:

| Steady-state year | Rolling window | Cache entry | R vintage | Level row |
|---:|---|---:|---:|---:|
| 2009 | 2000–2009 | 1 | 10 | 10 |
| 2010 | 2001–2010 | 2 | 11 | 11 |
| ... | ... | ... | ... | ... |
| 2023 | 2014–2023 | 15 | 24 | 24 |

The task must prove this mapping from source/cache/workbook evidence.

## 7. Direct value-level proof before mutation

Before changing Python, perform zero-science spot and full-scope checks.

At minimum prove for 2009 and 2023, then for all 15 years:

- cache outer entry corresponds to the correct rolling PLM vintage;
- `IND_alpha` and `IND_Zt` (or exact protected technology fields) come from the rolling-window entry;
- `GDP`, `CAP`, `POP`, `log_pgdp`, `log_pcap` level vectors are selected from the calendar-year row `10–24`, not row `1–15`;
- the selected level vectors correspond to the steady-state calendar year;
- all required values are finite/admissible;
- 2022/2023 level capital remains positive/admissible under the protected runtime cache when the correct row is used.

Compare the corrected 2009 runtime input against the formally accepted corrected-2009 canonical/runtime evidence. Any material contradiction must stop before expensive rerun.

Compare corrected 2010/2011 inputs against the prior primary-source annual canonical inputs and record all differences.

## 8. Required classification before code mutation

Choose one:

`MP4C_ROLLING_WINDOW_CALENDAR_ROW_INDEX_SEPARATION_CONFIRMED__PYTHON_RUNTIME_CACHE_BINDING_DEFECT`

or

`MP4C_ROLLING_WINDOW_CALENDAR_ROW_OWNER_DESIGNATION_CONTRADICTED_BY_PROTECTED_SOURCE__STOP`

or

`MP4C_ROLLING_WINDOW_CALENDAR_ROW_MAPPING_UNRESOLVED__OWNER_DECISION_REQUIRED`

Only the first classification authorizes implementation correction.

## 9. Conditional implementation correction

If and only if the first classification is proven, make the smallest production-route correction.

Preferred semantic separation:

- `window_entry_index = binding.analysis_index` or another explicitly proven `1–15` rolling-window index;
- `calendar_level_row_index = binding.workbook_data_row_index` or another explicitly proven `10–24` calendar-row index.

Do not continue using one ambiguous `data_mat_index` variable for both roles.

The implementation should make the semantic distinction explicit in code, manifests and tests.

Preferred scope:

- correct `validators/multi_province/mp4c_matlab_runtime_cache.py`;
- update only production/batch serializers/manifests if needed to expose both indices;
- update focused tests;
- do not modify HJB/KFE/household/one-turn/firm/controller scientific algorithms;
- do not modify calibration, grids, tolerances, convergence rules or shock semantics.

Avoid mutating historical accepted `annual.py` unless source inspection proves a contract-level correction is necessary. Prefer fixing the runtime-cache adapter where the conflation occurs.

## 10. Mandatory 15-year zero-science preflight after correction

Before any Owner rerun, materialize all 15 corrected runtime inputs with model calls `0`.

For every year require:

- rolling window exactly `[year-9, year]`;
- rolling-window/cache entry exactly `1–15`;
- regression vintage exactly `10–24`;
- calendar level row exactly `10–24`;
- correct calendar level year;
- exact 31-province order;
- expected cache and source hashes;
- finite/admissible GDP/CAP/POP;
- finite log fields;
- admissible technology fields;
- no worker launch;
- no stationary/HJB/KFE/household call.

Persist exact corrected runtime-input SHA-256 for each year.

For 2009 require an explicit regression comparison against the accepted corrected-2009 input contract before READY.

## 11. Prior 15-year acceptance status

The prior batch root and prior execution commit must remain immutable historical evidence.

Do not delete or overwrite it.

Classify it explicitly as:

`LEGACY_CONFLATED_WINDOW_AND_LEVEL_ROW_BATCH__ENGINEERING_CONVERGENCE_ONLY__NOT_FINAL_CALENDAR_YEAR_AUTHORITY`

The prior formal MP4C annual-coverage acceptance remains suspended and must not be cited as final Results authority.

The corrected-2009 formal parity authority remains intact unless direct same-input evidence contradicts it.

## 12. Existing MATLAB output provenance and future comparator

Read-only locate the Owner's existing MATLAB annual steady-state records under both:

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

and

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK_DIAGNOSTIC_PATCH_2026_06_23`

Do not rerun MATLAB.

Build a provenance map identifying which existing artifacts can compare corrected Python annual outputs, including where present:

- annual `Multi_Province_12sts_<year>.mat` objects;
- steady-state summary workbook(s), including historical `12年稳态值.xlsx` / `12年稳态Ltmat.xlsx` or actual current equivalents;
- diagnostic-patch full-state outputs;
- calendar coverage represented by each artifact;
- variables/fields available;
- whether their rolling-window/calendar-row semantics are source-proven.

Do not claim full 31×20 parity if a MATLAB artifact contains only a smaller field set.

Prepare, but do not execute before Owner rerun, a comparator-only plan for every provenance-qualified field/year.

## 13. Owner manual rerun preparation

Codex must not execute the corrected 15-year scientific batch in Phase A.

After all Phase-A proof and tests pass, prepare a new receipt-bound Owner launcher.

Use a **fresh output root**; never reuse the previous batch root.

Recommended Owner execution:

`Workers = 8`

with every worker still pinned to:

- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`.

Year-level parallelism only. Do not parallelize households inside one year.

`Workers=8` is an orchestration choice, not a scientific change.

Before launch instructions are issued, verify the existing launcher/runner can safely support 8 workers and record available physical RAM. Do not invent a model-result difference based on worker count.

If Owner later encounters memory/infrastructure pressure, preserve evidence and use a separately documented lower worker count such as 6 without changing scientific code or inputs. Do not change model tolerances or grids for performance.

Per-year frozen scientific ceilings remain:

- outer turns max `250`;
- household calls max `7750`;
- automatic reruns `0`;
- no default wall-clock kill.

## 14. Phase-A external evidence package

Use a fresh no-overwrite root, preferred:

`D:\ProjectTemp\ch5-mp4c-rolling-window-calendar-row-correction-20260902-001`

Persist at minimum:

- `rolling_plm_window_calendar_row_contract.csv`;
- `protected_source_index_trace.json`;
- `runtime_cache_window_entry_map.json`;
- `runtime_cache_calendar_level_row_map.json`;
- `r_plm_vintage_sheet_map.json`;
- `corrected_vs_prior_runtime_input_diff_2009_2023.json`;
- `corrected_2009_parity_anchor_input_check.json`;
- `corrected_2010_2011_primary_canonical_check.json`;
- `all_15_year_corrected_input_preflight.json`;
- `existing_matlab_annual_output_provenance_map.json`;
- `future_matlab_python_comparator_plan.md`;
- `zero_science_test_receipt.json`;
- `batch_runner_build_receipt_corrected.json`;
- `owner_corrected_rerun_instructions.txt`;
- `correction_manifest.json`.

## 15. Phase-A tests

All tests must be zero-science.

At minimum cover:

- 15 rolling windows exactly `2000–2009` through `2014–2023`;
- cache/window entry `1–15`;
- level row `10–24`;
- R vintage `10–24`;
- exact province order;
- wrong conflated row index fails;
- wrong cache entry fails;
- wrong R vintage fails;
- wrong calendar row fails;
- 2009 corrected input anchor check;
- all 15 corrected inputs finite/admissible;
- 2022/2023 corrected runtime CAP positive/admissible;
- no scientific calls;
- terminal-only logging retained;
- checkpoint schema retained;
- scheduler 2009–2023 exact;
- worker subprocess BLAS=1;
- launcher accepts Workers=8;
- no protected source/data mutation.

Run `py_compile` on changed/new Python files.

## 16. Phase-A READY marker

If and only if the binding defect is confirmed, corrected, and all zero-science checks pass, stop with:

`MP4C_ROLLING_PLM_WINDOW_AND_CALENDAR_LEVEL_ROW_BINDING_CORRECTED__FULL_2009_2023_OWNER_RERUN_READY__SCIENTIFIC_CALLS_0`

Print exactly:

1. fresh corrected PowerShell command using `-Workers 8`;
2. corrected build receipt path and SHA-256;
3. fresh output-root policy;
4. exact statement that the prior batch must not be reused as final calendar-year evidence;
5. exact statement that MATLAB is not to be rerun.

Do not run the scientific batch yourself.

## 17. Phase B after Owner reports corrected rerun complete

Only after explicit Owner notification and output-root identification:

1. verify code/launcher hashes against the corrected Phase-A receipt;
2. audit all 15 corrected years read-only;
3. require 15/15 expected annual outputs or isolate anomalies without automatic rerun;
4. verify corrected runtime-input hashes and both indices for every year;
5. verify complete final 31×20 Python outputs/checkpoints;
6. compare corrected Python outputs against every provenance-qualified existing MATLAB annual artifact identified in Phase A;
7. use field/year-specific comparison only where MATLAB evidence exists;
8. do not rerun MATLAB;
9. do not rerun Python in Phase B;
10. generate a final correction/parity report;
11. commit/push only authorized code/tests/contracts/report, never generated batch binaries or local data.

Possible final classifications must distinguish:

- corrected Python annual coverage PASS;
- MATLAB–Python parity on provenance-qualified fields/years;
- fields/years with no admissible MATLAB reference;
- any genuine numerical mismatch requiring a separate forensic gate.

## 18. Interpretation boundary

Until corrected Owner rerun and Phase-B acceptance are complete:

- do not use the prior 15-year batch as final paper annual evidence;
- do not start numerical shock/IRF implementation;
- do not write Results claims based on the prior annual calendar mapping.

After corrected annual acceptance, return to the already accepted MP4D route and publish the representative-year shock-response implementation/validation gate.

## 19. Repository mutation boundary

Phase A may modify only the minimum files necessary for the runtime-cache indexing correction, focused tests, launcher/runner metadata if required, and the eventual report.

Do not modify:

- protected MATLAB source;
- raw/processed data files;
- runtime cache bytes;
- household/HJB/KFE scientific implementation;
- one-turn economics;
- firm/migration/capital/wage/monetary/fiscal/controller semantics;
- calibration/grid/tolerances/convergence thresholds;
- MP4D shock semantics.

Do not commit generated MAT/NPZ/XLSX/CSV batch outputs.
