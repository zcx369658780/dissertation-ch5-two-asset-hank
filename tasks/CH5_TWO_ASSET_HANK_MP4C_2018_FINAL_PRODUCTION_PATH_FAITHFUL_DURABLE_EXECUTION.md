# CH5_TWO_ASSET_HANK_MP4C_2018_FINAL_PRODUCTION_PATH_FAITHFUL_DURABLE_EXECUTION

Date: 2026-09-03

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / execution-only 2018 scientific diagnostician

Owner: final scientific authority

## 1. Authority basis

Immediate predecessor execution:

`a72de261f53c36f9c21d8193b8e4dd56bf3bfa3a`

with terminal:

`MP4C_2018_TERMINAL_CAPTURE_ZERO_SCIENCE_REGRESSION_REPAIRED_AND_CERTIFIED__N_TO_N_BATCH_SEMANTICS_RESTORED__READY_FOR_FINAL_DURABLE_2018_EXECUTION_TASK`

Accepted predecessor facts:

- production-path parity is certified, including per-batch in-place `phi[:]` recomputation from entering `Yt/Lt` with destination-row/origin-column orientation;
- production-literal `AtTax` is certified using `a_ss`, `rah`, faithful illiquid return, KFE density and cell weight;
- N-input → N-output `PreFrozenHouseholdOutputBatch` semantics are restored and certified for 2- and 3-province fixtures;
- normal-completion terminal persistence is certified with a valid 31-province synthetic object;
- durable HJB ledger, raw capture-before-postmortem and separate read-only postmortem remain certified;
- focused zero-science validation passed `7 passed, 1 warning`; the warning is the intentional dummy `MatrixRankWarning`;
- frozen 2018 input reads, scientific PIDs, stationary, household, production HJB/KFE, MATLAB, R/PLM, shock and IRF calls were all zero.

This task allocates exactly one final production-path-faithful 2018 scientific child.

## 2. Frozen code identities

At predecessor commit `a72de261f53c36f9c21d8193b8e4dd56bf3bfa3a`:

- diagnostic wrapper Git blob: `a021b0749f572846a264ad206a2250dae3285e5b`;
- focused tests Git blob: `158cab1bdaa1f08678890243ce7b1f61a6363354`;
- production worker Git blob: `7473e04418744d745000afb21d84588273cc5bca`;
- faithful HA export Git blob: `9e7dc9556a2b76811e78f89999abecc045886106`;
- post-loop household adapter Git blob: `0033baee136c0328e80ffb8b794a88d4405c976c`;
- one-turn Git blob: `e5d6835cdc9e6d182e1c84e11f4d51938be592e1`;
- stationary runtime Git blob: `8717cfa759948bd1ad3c8cd788f8f4736f250598`;
- steady-state controller Git blob: `5c5b56e6a2eb82fab4e80eb7b8e5bbbf97f08c22`;
- Owner-A adapter Git blob: `f89927c7a6234cf1c5106318b2b4249183b90cce`.

Before launch, verify these paths are unchanged relative to the predecessor content. Any unexpected code drift: STOP before reading the scientific input or creating a scientific PID.

## 3. Execution-only freeze

No source or test edits are authorized in this task.

Do not rerun Phase A or change the certified recorder. No further pre-execution implementation work is allowed.

If the certified diagnostic cannot run as-is, STOP. Do not consume the scientific-child budget.

## 4. Frozen 2018 scientific input

Use the exact preserved Owner-A 2018 input SHA-256:

`F84D25FD49A76229CA49958764D1167CAA56FB68CF99A4ED7B20C508812E6ED0`

Required binding:

- steady-state calendar year = 2018;
- rolling entry = 10;
- regression vintage = 19;
- calendar row = 19;
- rolling window = 2009–2018;
- GDP = `GDP` ×1000;
- CAP = `R语言计算资本存量` ×1000;
- POP = `就业人数` ×100;
- exact 31-province order;
- no 2023 scientific input.

Any byte or semantic mismatch: STOP before scientific PID.

## 5. Scientific immutability

Do not change:

- HJB equations, parameters, tolerances or max iterations;
- KFE equations or solver;
- grid/state support;
- transfer FOC;
- adjustment cost;
- raw-Vb logic;
- illiquid-return taper;
- generator assembly;
- contaminated-row index;
- RHS `0.007`;
- density normalization;
- household aggregation;
- certified `phi` formula/timing/orientation;
- certified `AtTax` formula;
- migration, capital allocation, firm, wage, monetary or fiscal logic;
- steady-state controller, adaptation or convergence criteria;
- annual input/calendar semantics.

Forbidden:

- pseudoinverse;
- regularization;
- alternate solver;
- matrix perturbation;
- fallback density;
- changed contaminated row;
- parameter/grid/controller edits;
- second scientific child.

## 6. Exactly one durable scientific child

Launch exactly one child with:

- year = 2018;
- scientific-child count = 1;
- worker count = 1;
- subprocess count = 1;
- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`;
- automatic reruns = 0.

Use the already proven durable local process/session. Do not use a launch method that terminates the child when a short outer tool call returns.

Persist before/at launch:

- PID;
- exact command;
- Python executable/version;
- cwd;
- thread environment;
- input path and SHA;
- all frozen code identities;
- launch timestamp;
- stdout and stderr paths.

Preferred fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-final-production-path-faithful-durable-execution-20260903-001`

If a pre-PID infrastructure action occupies `-001`, preserve it and use the next suffix. Once a scientific PID exists, no second child is authorized.

## 7. Required live evidence

Use the certified recorder unchanged.

Before every household call append + flush + fsync:

- outer iteration;
- province index/name;
- global household-call number;
- `rah`, `rb`, `tau`, `w`, `Tt`, `rb_gap`;
- `Yt`, `Lt`, `Kt`, `Zt`, `GovInv`.

After every HJB return append + flush + fsync:

- same context;
- HJB convergence flag;
- HJB iterations;
- HJB convergence statistic;
- KFE path classification.

## 8. If genuine KFE singularity is reproduced

At the first genuine `MatrixRankWarning` or non-finite contaminated-row solve:

1. persist raw evidence first:
   - exact localization/context;
   - household inputs and entering macro state;
   - HJB status;
   - operator A;
   - A transpose;
   - contaminated matrix;
   - RHS;
   - raw solve vector if available;
   - warning records;
   - traceback/capture stack;
   - terminal sentinel;
2. flush/fsync;
3. stop the scientific child fail-closed;
4. only after child termination run the existing read-only postmortem.

Postmortem must report at minimum:

- shape, nnz, finite flags and max absolute row-sum residual;
- diagonal/off-diagonal extrema and sign counts;
- zero-outflow and isolated states;
- SCC count;
- closed SCC count, sizes and exact members;
- rank/nullity of A transpose and contaminated matrix;
- numerical tolerance/method;
- smallest singular/eigenvalue diagnostics;
- HJB convergence state at the failing call.

No repair in this task.

Terminal:

`MP4C_2018_FINAL_PRODUCTION_PATH_FAITHFUL_FIRST_SINGULARITY_CAPTURED__POSTMORTEM_CLASSIFIED_OR_BOUNDED__NO_REPAIR_NO_RETRY`

## 9. If the run completes normally

The certified diagnostic must persist `normal_completion_summary.json` after normal return.

Require:

- child exit 0;
- `normal_completion_summary.json` exists and its SHA matches the execution receipt;
- `converged == true`;
- `termination_reason == TERMINATION_CONVERGED`;
- `household_call_count == 31 * iteration_count`;
- final household-converged count = 31;
- final ra upper count = 0;
- final ra lower count = 0;
- max final KN-ratio gap < `1e-9`;
- max final Y gap < `1e-9`;
- exact 31-province order;
- final `final_31x20` contains 31 rows and all scientific scalars are finite;
- no singular raw-evidence artifacts exist;
- no postmortem is run;
- no second scientific run occurs.

Do not automatically declare 2009–2022 accepted or run shock/IRF. Preserve the terminal object for L3 acceptance review.

Terminal:

`MP4C_2018_FINAL_PRODUCTION_PATH_FAITHFUL_SOURCE_CONVERGED__NO_SINGULARITY__OWNER_ACCEPTANCE_REVIEW_REQUIRED__NO_SECOND_RUN`

## 10. Other failures

If infrastructure/instrumentation fails after scientific PID creation:

`MP4C_2018_FINAL_PRODUCTION_PATH_FAITHFUL_INFRASTRUCTURE_OR_INSTRUMENTATION_FAILED__NO_SECOND_CHILD`

If the stationary controller reaches max iterations or another scientific exception occurs without the target KFE singularity, preserve the exact exception, receipts, ledgers and traceback, then STOP. Do not retry and do not reinterpret it as convergence or singularity.

## 11. Required report and publication boundary

Write:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_FINAL_PRODUCTION_PATH_FAITHFUL_DURABLE_EXECUTION_REPORT.md`

The report must include:

- authority/direct-parent verification;
- frozen code identities;
- frozen input identity;
- PID/thread/one-child proof;
- household/HJB ledger counts;
- exact terminal classification;
- if singular: raw + postmortem evidence;
- if converged: full normal-completion summary and final 31×20 evidence;
- explicit no-retry/no-repair/no-shock boundary.

Do not modify code/tests in this task. Only the execution report may be added to the repository after execution. If execution ends cleanly under one of the recognized terminals, one normal report-only commit and push is authorized. No task-chain continuation is authorized inside this task.
