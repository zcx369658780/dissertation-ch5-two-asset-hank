# CH5_TWO_ASSET_HANK_MP4B_LOGICAL_PHYSICAL_PATH_EQUIVALENCE_GUARD_REPAIR_AND_FRESH_CALENDAR2009_STATIONARY_PARITY

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / validation-path repairer / stationary-parity executor / first-divergence diagnostician

Owner: final scientific authority

## 1. Purpose

Repair only the validation-helper path-equivalence guard that blocked the prior calendar-2009 MATLAB entry before any province household call, then independently prove the logical/physical MatlabProgram path relation in a **non-scientific MATLAB smoke**, re-establish the full presolver same-input identity, and only then execute one freshly authorized corrected calendar-2009 stationary run per language maximum.

Prior terminal:

`MP4B_WRAPPER_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

Prior first divergence:

`VALIDATION_HELPER_PATH_EQUIVALENCE_CHECK_BEFORE_MODEL_ENTRY`

Prior root cause:

`SOURCE_ENVIRONMENT_FAILURE__LOGICAL_JUNCTION_PHYSICAL_TARGET_EQUIVALENCE_GUARD_ERROR`

Prior report/implementation commit:

`d3ac5caa597ff0906a7aeb3e40ebacfc520d2cfb`

The prior task consumed its one fresh MATLAB scientific top-level invocation and zero province HJB/KFE calls. That invocation is permanently consumed and MUST NOT be retried or reclassified. This task provides a separate new bounded budget only after all repair, smoke, and presolver gates pass.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- `docs/CH5_TWO_ASSET_HANK_OWNER_MP4_MULTI_YEAR_BASELINE_CACHE_AND_FIRST_YEAR_PARITY_DECISION.md`
- `docs/CH5_TWO_ASSET_HANK_OWNER_MP4_ANNUAL_YEAR_AXIS_DECOUPLING_AND_2009_BINDING_ADJUDICATION.md`
- `docs/CH5_TWO_ASSET_HANK_MP4A2_2009_DECOUPLED_ANNUAL_BINDING_CANONICAL_INPUT_AND_MATLAB_PARITY_WRAPPER_PREPARATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_REAUTHORIZED_AFTER_CONTROLLING_RULE_SYNC_CONTROLLED_CALENDAR2009_STATIONARY_PARITY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_WRAPPER_SOURCE_BINDING_REPAIR_AND_REAUTHORIZED_CALENDAR2009_STATIONARY_PARITY_REPORT.md`
- all prior MP4B task authorities.

Preserve accepted decisions and evidence:

- `OWNER_SINGLE_ACTIVE_CODEBASE_TWO_ASSET_HANK_ONLY`
- `OWNER_ECONOMIC_CALENDAR_YEAR_IS_WORKBOOK_CALENDAR_YEAR`
- `OWNER_2009_ANCHOR_USES_EXPLICIT_WORKBOOK_2009_ROW`
- `MATLAB_LEGACY_ANNUAL_YEAR_INDEX_COUPLING_DEFECT_CONFIRMED`
- `OWNER_DERIVED_MAT_CALIBRATION_CACHE_NOT_PRIMARY_SCIENTIFIC_AUTHORITY`
- `MATLAB_FIXED_2020_IND_ZT_RETAINED_AS_SOURCE_NUMERICAL_INITIALIZATION_ANCHOR`
- `OWNER_PARITY_MISMATCH_BOUNDED_ROOT_CAUSE_DIAGNOSIS_AUTHORIZED`
- `SOURCE_BINDING_AUDIT_COMPLETE__N_PROV_IS_ONLY_MISSING_REQUIRED_BINDING`
- source-faithful `global N_prov; N_prov=31;` repair
- prior `MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS` as evidence only; re-establish it after this repair.

Primary reconstruction authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent:

`d3ac5caa597ff0906a7aeb3e40ebacfc520d2cfb`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the prior blocked report commit;
3. require clean worktree;
4. verify both controlling rule blobs are present and unchanged;
5. verify protected MATLAB source hashes remain accepted;
6. verify accepted household oracle, MP2, MP3, MP4A2 annual binding/canonical identity, current stationary runtime, and current MATLAB validation helper identities;
7. verify no historical `chapter5_model` runtime dependency.

If continuity or authority identity fails, stop before any scientific execution.

## 4. Frozen path-boundary fact

The controlling local-file safety rule explicitly defines:

- logical compatibility entry: `C:\MatlabProgram`
- physical storage root: `D:\MatlabProgram`

and states that when the C path is the documented junction/compatibility path to D, the two paths are **one high-risk MatlabProgram storage boundary**, not two independent copies.

The prior wrapper incorrectly used a literal condition equivalent to:

```matlab
startsWith(which(helper), protected_root)
```

where `protected_root` was the logical C path but MATLAB `which` returned the resolved physical D path.

The repair MUST preserve fail-closed source identity checking while accepting only the independently verified logical-root/physical-target equivalence. It MUST NOT simply remove the path guard.

## 5. Required path-equivalence repair design

Before coding, inspect the local junction safely and record:

- logical MatlabProgram root supplied to the command;
- resolved/documented physical target;
- logical protected model root;
- corresponding physical protected model root;
- the exact mechanism used to verify that the logical root resolves to the physical target;
- the resolved `which(...)` path for every required protected helper.

Required helpers remain at minimum:

- `load_GDPdata`
- `load_distdata`
- `mpHANK_equilibrium_2000`
- `HANK_mp_1eq`
- `HANK_mp_1turn`
- `HANK_2ASSETS_HJB`

Repair requirements:

1. keep `global N_prov; N_prov=31;` unchanged;
2. keep protected MATLAB read-only;
3. keep `addpath(protected_root)` or an equivalent source-faithful path binding;
4. require every protected helper to resolve under one of the **explicitly verified equivalent protected roots** only;
5. do not accept arbitrary D paths merely because they start with `D:\MatlabProgram`;
6. normalize separators/case only as needed for Windows path comparison; do not broaden to unrelated directories;
7. record both logical and physical source roots in the source-binding manifest;
8. fail closed if the verified junction/target relationship cannot be established or if any helper resolves outside the finite allowed equivalent root set.

Preferred implementation pattern: pass or construct an explicit finite pair/set of verified source roots (`logical_protected_root`, `physical_protected_root`) and test `which` results against that finite set. Do not rely on a single literal prefix tied to only the logical spelling.

Do not use an opaque hard-coded path substitution that would silently succeed on a different machine. The guard must remain auditable.

## 6. Independent non-scientific path smoke — mandatory

Add or reuse a validation-only MATLAB helper that performs **only** source-path/environment validation and does not call:

- `mpHANK_equilibrium_2000`
- `HANK_mp_1eq`
- `HANK_mp_1turn`
- `HANK_2ASSETS_HJB`
- any HJB/KFE/model solver.

It may:

- verify the junction/physical-target relation;
- `addpath` the protected root;
- bind/check `N_prov=31`;
- run `which` for the required helpers;
- verify every helper against the finite equivalent-root set;
- write a small timestamped no-overwrite source-path smoke manifest.

This is infrastructure validation, not a scientific model call.

Required marker:

`MP4B_LOGICAL_PHYSICAL_PATH_EQUIVALENCE_SMOKE_PASS`

The smoke MUST use the same logical and physical roots that the subsequent scientific wrapper will use. If it fails, stop BLOCKED and consume zero new scientific runs.

## 7. Static review gate after repair

Before the non-scientific smoke and before any scientific run:

- MATLAB `checkcode`/syntax check all changed validation `.m` files;
- verify the repair diff is limited to path/environment validation and observability;
- verify no economic/HJB/KFE/controller/calendar/tolerance/clipping expression changed;
- verify protected MATLAB hashes unchanged;
- verify no-overwrite output roots;
- verify free space as required by the controlling rule;
- focused tests must include at least:
  - logical C input + physical D `which` result => accepted when the junction relation is independently verified;
  - physical D input + physical D `which` result => accepted only when it is the documented physical target;
  - unrelated C/D root => rejected;
  - sibling directory under MatlabProgram => rejected;
  - helper resolving outside the exact protected model root => rejected;
  - missing/incorrect `N_prov` => rejected.

Required marker:

`MP4B_PATH_EQUIVALENCE_GUARD_REPAIR_STATIC_REVIEW_PASS`

## 8. Re-establish presolver same-input identity

After static review and successful non-scientific path smoke, re-run the full presolver manifest comparison and require:

`MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`

Frozen identity remains:

- calendar year `2009`
- analysis index `1`
- workbook numeric row `10`
- physical Excel row `11`
- `data_MAT_index=1`
- output year `2009`
- regression key `10`
- fixed-2020 `IND_Zt` only as the source numerical initialization anchor
- canonical SHA `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`.

Any semantic mismatch aborts before scientific execution.

## 9. Fresh scientific execution budget

All prior MATLAB scientific/top-level attempts remain consumed historical evidence. This task separately authorizes a new bounded pair only after Sections 6-8 PASS.

### MATLAB

- corrected/decoupled calendar-2009 stationary top-level invocation: maximum **1**.
- no scientific rerun.

### Python

- corrected/decoupled calendar-2009 stationary top-level invocation: maximum **1**.
- no scientific rerun.

Ordering:

1. static review;
2. non-scientific path-equivalence smoke;
3. presolver equality;
4. MATLAB first;
5. if MATLAB fails before any provincial household HJB/KFE because of validation/infrastructure/environment failure, stop BLOCKED and do not run Python;
6. if MATLAB reaches provincial household/model execution but scientifically nonconverges or hits a model-domain failure, preserve that outcome and execute the single Python run for comparison;
7. if MATLAB completes, execute Python once;
8. after either scientific invocation, no repair-and-rerun in this task.

Forbidden scientific calls remain zero:

- legacy wrong-year MATLAB route;
- 2010-2023 batch;
- shocks/AR1;
- transition/genuine dynamics/IRF Results;
- historical one-asset R5;
- manuscript Results.

## 10. Python runtime continuity

The existing `src/ch5_two_asset_hank/multi_province/stationary_runtime.py` is already present from prior MP4B preparation.

Before empirical Python execution:

- verify it is unchanged unless this task identifies a purely non-scientific integration-interface issue before the run;
- rerun exact equivalence against all seven MP3 tiny scenarios;
- verify 31 household solves per turn consume the same copied old-turn state;
- verify accepted household adapter/oracle, MP2 one-turn, and MP3 controller arithmetic unchanged.

Do not change accepted scientific arithmetic.

## 11. Required parity comparison if both scientific routes exist

Use the already frozen hierarchy:

1. pre-solver input identity;
2. first-turn household inputs;
3. first-turn household outputs;
4. migration `Lt_mat/Lt_supply`;
5. At-only productive capital / `Kt_supply` / `rah`;
6. firm block;
7. composite wage / Taylor / fiscal diagnostics;
8. controller diagnostics/adaptation/damping;
9. later turns in order;
10. final 31-province stationary state.

Persist enough intermediates to localize the first divergence.

Allowed scientific root-cause classes remain:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

If two scientific routes do not exist, use a source/environment/infrastructure blocker instead of forcing a scientific mismatch class.

No automatic scientific repair or tolerance loosening.

## 12. Numerical and qualitative comparison

Use existing accepted household/source-local parity contracts first.

Freeze any new outer/final floating comparison bounds before inspecting results; no post-hoc relaxation.

Qualitative diagnostics are secondary and may include sign, relative direction, province ranking/order, and clipping/boundary status agreement. They never override unexplained source/formula differences.

## 13. Allowed repository changes

Authorized writes only:

- bounded repair to existing MP4B MATLAB validation wrapper/helper(s) under `validators/multi_province/matlab/`;
- one validation-only path-equivalence smoke helper if needed;
- focused static/path-equivalence tests/validators;
- bounded parity comparator/trace-helper changes only for observability;
- `stationary_runtime.py` only for a non-scientific integration-interface issue found before empirical execution, with seven-scenario exact MP3 regression preserved;
- CURRENT roadmap status update;
- one new report:

`docs/CH5_TWO_ASSET_HANK_MP4B_LOGICAL_PHYSICAL_PATH_EQUIVALENCE_GUARD_REPAIR_AND_FRESH_CALENDAR2009_STATIONARY_PARITY_REPORT.md`

Do not modify:

- protected MATLAB source;
- accepted household/HJB/KFE/oracle arithmetic;
- MP2 arithmetic;
- MP3 controller semantics;
- canonical 2009 input;
- primary workbooks/regression/distance/cache;
- historical R5;
- controlling rule files.

Do not commit raw/private data, scientific `.mat` outputs, figures, large logs, caches, or secrets.

## 14. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. prior blocker and historical call ledger;
4. logical/physical path-equivalence evidence;
5. exact guard repair diff and design;
6. static review marker;
7. non-scientific path smoke marker and manifest/hash;
8. controlling-rule/storage/no-overwrite preflight;
9. re-established presolver equality;
10. fresh scientific call ledger;
11. MATLAB/Python run roots;
12. MATLAB outer-turn and household-call counts;
13. Python outer-turn and household-call counts if run;
14. convergence status and iteration counts;
15. layer-by-layer parity table;
16. exact/within-bound/material classification;
17. first divergent stage/object;
18. bounded root-cause classification;
19. qualitative diagnostics if available;
20. material mismatch/unresolved/environment lists;
21. tests/checks;
22. forbidden-operation check;
23. Git closeout;
24. exactly one recommended next gate.

## 15. Terminals

PASS:

`MP4B_PATH_EQUIVALENCE_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_PASS`

Scientific mismatch:

`MP4B_PATH_EQUIVALENCE_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_MATERIAL_MISMATCH`

Infrastructure/source/environment blocker:

`MP4B_PATH_EQUIVALENCE_REPAIR_AND_CALENDAR2009_STATIONARY_PARITY_BLOCKED`

Owner provenance required:

`MP4B_PATH_EQUIVALENCE_REPAIR_OWNER_PROVENANCE_REQUIRED`

Use PASS only if corrected calendar-2009 MATLAB-Python stationary parity itself is accepted. A successful path smoke alone is never MP4B scientific PASS.

## 16. Next-stage boundary

Even on PASS, shocks and 2010-2023 remain unauthorized.

On PASS recommend exactly one next gate: **MP5A source-law/named-response role freeze and corrected-2009 response preparation**, initially audit/preparation only.

On MATERIAL/BLOCKED recommend one bounded successor addressing only the localized first cause.

## 17. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.
