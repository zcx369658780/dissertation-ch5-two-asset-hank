# CH5_TWO_ASSET_HANK_MP4C_FULL_2009_2023_OWNER_RUN_PARALLEL_BATCH_RUNNER_BUILD_AND_POSTRUN_CLOSEOUT

Date: 2026-09-02

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded implementation builder / post-run evidence auditor

Owner: final scientific authority and manual batch executor

## 1. Route change and supersession

This task is a deliberate Owner-approved route change from Codex-supervised two-year chunks to a build-once / Owner-run-locally workflow.

The immediately preceding live task is:

`tasks/CH5_TWO_ASSET_HANK_MP4C_CHUNK02_2012_2013_PYTHON_ANNUAL_STATIONARY_EXECUTION.md`

authority commit:

`601deb9906888dca4990e042b3f0ea31cfb0a9fd`

The Owner has explicitly confirmed that the 2012–2013 scientific execution under that task has **not started**.

Therefore, on publication of this task:

`MP4C_CHUNK02_2012_2013_EXECUTION_AUTHORITY_SUPERSEDED_UNEXECUTED`

The old Chunk 02 task remains immutable historical authority evidence, but its unconsumed scientific execution budget is withdrawn and replaced by this task. Codex MUST NOT execute 2012 or 2013 under the old task.

Accepted history remains valid:

- corrected-2009 formal MATLAB–Python stationary parity remains accepted;
- Chunk 01 2010/2011 Python coverage remains accepted historical evidence;
- no accepted historical result is invalidated by this route change.

However, the Owner now explicitly authorizes a **new full production/benchmark batch covering every calendar year 2009–2023, including fresh reruns of 2009, 2010 and 2011**. These fresh runs are for one coherent production batch, timing comparison, downstream restart-state preservation, and paper-result generation. They do not retroactively replace the frozen 2009 parity acceptance unless a later L3 gate says so.

## 2. Workflow is two-phase

### Phase A — Codex build only, ZERO scientific execution

Codex shall:

1. inspect the protected MATLAB persistence/shock consumers;
2. freeze an explicit persistence contract;
3. build the reusable terminal-only annual production worker;
4. build the full 2009–2023 resumable parallel batch scheduler;
5. build a user-friendly PowerShell launcher;
6. add zero-science tests;
7. produce an exact local build receipt containing SHA-256 identities of every uncommitted implementation file;
8. give the Owner the exact launch command;
9. STOP without running any annual stationary model and without committing or pushing the implementation.

Required Phase-A terminal to the Owner:

`MP4C_FULL_2009_2023_PARALLEL_BATCH_RUNNER_READY_FOR_OWNER_MANUAL_EXECUTION__NO_SCIENCE__NO_PUSH`

### Owner manual execution

The Owner, not Codex, will manually launch the full 2009–2023 batch from PowerShell using the Phase-A approved script.

### Phase B — Codex post-run audit and GitHub closeout

Only after the Owner explicitly tells Codex that the batch run has finished and provides/identifies the batch output root, Codex shall:

1. verify implementation bytes still match the Phase-A build receipt;
2. audit outputs read-only;
3. perform no stationary rerun;
4. optionally perform the separately authorized 2009 comparator-only cross-check in Section 15 if eligible;
5. generate the final GitHub report;
6. commit and push the approved scripts/tests/contracts/report in one closeout sequence;
7. never commit generated batch data, MAT/NPZ/XLSX/CSV outputs, or local primary data.

This two-phase workflow is mandatory.

## 3. Required live continuity

At Phase A start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `601deb9906888dca4990e042b3f0ea31cfb0a9fd`;
3. require clean tracked worktree before implementation;
4. require `HEAD == origin/main`, ahead/behind `0/0`;
5. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`;
   - formal corrected-2009 parity acceptance report;
   - MP4C scope-freeze report;
   - MP4C local-data staging report;
   - Chunk 01 task/report;
   - superseded Chunk 02 task;
   - current annual input API;
   - current `mp4c_python_annual_empirical.py`;
   - accepted household adapter, `one_turn.py`, `firm.py`, stationary runtime and convergence map.

If live `main` contains an execution commit for the superseded Chunk 02 task, STOP and return to L3 because this task assumes that 2012–2013 have not been launched.

## 4. Protected scientific identities and data

The production runner MUST preserve the accepted source-faithful scientific implementation. No economic equation, calibration, grid, HJB/KFE numerical rule, migration/capital/firm/wage/fiscal/controller rule, update order, Z reset threshold, convergence condition, or source binding may change for speed.

At minimum, require the live accepted identities named by current reports, including:

- repaired `one_turn.py` mapping: same-turn household `Lt -> firm_source["Lt_prev"]`;
- current firm labor remains migration destination `Lt_supply`;
- same-turn `AtTax` mapping preserved;
- accepted household oracle/source-postloop adapter unchanged;
- accepted 2009 canonical SHA `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48` remains the compatibility reference.

Use only the verified ignored local primary-source snapshot:

`data_local\matlab_primary_source_snapshot`

Required files/hashes:

- `2000年后各省数据_填充NA.xlsx`
  `C826B01B6C124EAAADC063DFC2D5510E50E72ED85BB34848F28AB318E4B88929`
- `R语言估计结果_plm估计.xlsx`
  `A6F444FCCCB30CB93AA5DE084F1DD163C54E5F53C4287C2CD3E13A045EB64A68`
- `中国各省省会地理距离矩阵.xlsx`
  `26E44D174A8EFFBDCA526D95DA38F0E5883E0C78FDFD036D2DFF1D1FBA5A3566`

Do NOT use the unfilled workbook as active input. Do NOT use `数据估计结果_1000_100_0.mat` as primary scientific authority.

## 5. Full calendar scope and primary canonical rule

The full new production batch is exactly:

`2009, 2010, ..., 2023`

15 years total.

All 15 years MUST be freshly run by the Owner in this new batch output root, including 2009, 2010 and 2011.

The batch MUST use `PRIMARY_SOURCE_CANONICAL` annual inputs reconstructed from the verified local primary workbooks for every year. Do not use the validation-only `MATLAB_CACHE_RUNTIME_PARITY_OVERLAY` as the production input mode.

For every year keep the decoupled identities separate:

- calendar year;
- physical workbook row;
- workbook numeric row;
- analysis index;
- `data_MAT` index;
- regression vintage;
- output filename year.

The frozen mapping is 2009–2023 as accepted in the MP4C scope-freeze report.

Before the Owner runs science, Phase A zero-science tests MUST rebuild the 2009 canonical bytes from the verified local copies and require exact SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

No 2009 stationary call is permitted in Phase A.

## 6. Mandatory MATLAB persistence-contract audit before defining output schema

Do not guess which steady-state variables or restart objects should be saved.

Read-only inspect the protected MATLAB root and record the exact logical/physical root relationship. At minimum inspect:

- `main.m`;
- `main2.m`;
- `multi_prov_HANK_12sts.m`;
- `mpHANK_equilibrium_2000.m`;
- `HANK_mp_1eq.m`;
- `HANK_mp_1turn.m`;
- `HANK_2ASSETS_HJB.m`;
- `Ltmat_save.m`;
- `coefmat_save.m`;
- `multi_prov_HANK.m`;
- `mpHANK_shock_2000.m`;
- any helper actually responsible for writing/reading the steady-state Excel/MAT artifacts.

Historical source evidence says the MATLAB route persists annual `st` with objects including `results`, `grids`, `data_MAT`, `param`, `num`, `CHI`, and that the shock route later loads annual steady state. Historical workbook names include forms such as `12年稳态值.xlsx` and `12年稳态Ltmat.xlsx`, while annual caches follow `Multi_Province_12sts_<year>.mat`. These are hypotheses/source references only; the protected live source wins.

Before coding the final save schema, create a machine-readable contract:

`matlab_persistence_contract.json`

It must map:

- exact MATLAB writer/caller;
- exact MATLAB file/workbook naming expression;
- exact workbook sheet/range/layout if source-backed;
- exact steady-state fields written;
- exact `Lt_mat` orientation and storage convention;
- exact `st.results{i}` fields consumed by `multi_prov_HANK` / `mpHANK_shock_2000`;
- exact `st.grids`, `st.param`, `st.num`, `st.CHI`, `st.data_MAT`, `st.sigmau_MAT`, `st.N_prov` fields actually consumed downstream;
- which household value-function / distribution / policy objects are required to restart or reproduce the shock path;
- which objects are merely diagnostics and need not be duplicated.

Do not claim drop-in MATLAB `st` compatibility unless a zero-science schema test proves it.

## 7. Required normal steady-state outputs

The new production runner MUST explicitly save final steady-state results, not only a JSON status flag.

At minimum each successful year must persist:

1. calendar/canonical/source manifest;
2. final ordered 31-province state for the frozen 20 fields:
   `Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`;
3. household convergence flags and HJB iteration/statistic summary;
4. final `Lt_mat` with explicit orientation metadata (`destination row × origin column`) and exact province axes;
5. final terminal categories and controller-relevant fields;
6. national `Ct/At/Bt/Yt`;
7. canonical input SHA and three primary source hashes;
8. scientific code identities;
9. per-year timing and process status.

In addition to machine-friendly JSON/CSV, build 2009–2023 summary workbooks analogous to the protected MATLAB source layout after auditing the exact writers:

- preferred new non-misleading filename: `2009_2023_稳态值.xlsx`;
- preferred new non-misleading filename: `2009_2023_稳态Ltmat.xlsx`.

Do not reuse a historical `12年...` filename for a 15-year object.

Where exact legacy workbook layout is source-backed and practical, preserve it. Also produce a machine-friendly long/panel table so later paper processing does not depend on workbook layout.

If the environment lacks the minimal XLSX writer dependency, Phase A must fail closed with an explicit setup instruction; do not silently omit the required workbooks.

## 8. Mandatory restart/IRF checkpoint per year

The Owner explicitly requires final value functions and other steady-state household information to be retained for later shock/IRF work.

The production worker must therefore preserve, for the **final outer iteration only**, the compact per-province household checkpoint required by the source-backed persistence contract.

Do not write all household/HJB/KFE details for every outer turn in normal mode.

Inspect the actual Python HJB/KFE result dataclasses and save the exact available source-equivalent objects required downstream. Candidate categories include value function, final policy/control arrays, KFE density, HJB convergence metadata, grids, parameters and any source-consumed initialization arrays, but the exact list must be derived from the protected MATLAB consumers and current Python objects rather than guessed.

Implementation should retain only a compact extracted checkpoint for the most recent outer turn in memory and overwrite that in-memory checkpoint on each turn; after convergence, persist the final checkpoint.

Per successful year produce both:

- a versioned Python-native compressed checkpoint, preferred `.npz` plus JSON manifest;
- a MATLAB-readable `.mat` representation, preferred name:
  `Python_Multi_Province_12sts_<year>.mat`.

The MATLAB-readable file may be a scientifically equivalent Python checkpoint. Do not label it as a byte/storage-compatible legacy `st` replacement unless explicitly proven.

Each checkpoint must include enough provenance to bind it to:

- year;
- canonical SHA;
- source hashes;
- province order;
- grid/numerics/scientific-code identities;
- final steady-state terminal SHA/manifest.

## 9. Terminal-only normal mode; forensic mode separate

Default production logging mode MUST be:

`terminal-only`

Normal mode MUST NOT write per-turn `turn_###_household_inputs.json` or `turn_###_household_outputs.json` artifacts.

It may keep the existing `result.history` required by the current solver in memory, but disk persistence should be limited to final/results/checkpoint/timing/manifest objects.

Add an optional explicit `forensic` or equivalent debug logging mode only if it can reuse the existing detailed chronology safely. It must remain OFF by default and is not exercised in Phase A or the Owner full batch.

A later anomalous year can receive a separately authorized forensic rerun.

## 10. Production worker and scheduler architecture

Preferred files:

- `validators/multi_province/mp4c_python_annual_production.py`
- `validators/multi_province/mp4c_run_full_annual_batch.py`
- `scripts/run_mp4c_full_annual_batch.ps1`
- `tests/test_mp4c_full_annual_batch_runner.py`

The production worker should reuse the already accepted scientific code path. Do not modify accepted household/HJB/KFE/one-turn/firm scientific modules merely for orchestration.

The full scheduler MUST use independent **year-level subprocesses** on Windows. Do not parallelize the 31 household solves inside one annual equilibrium.

Each year process must be isolated to its own no-overwrite year directory.

Set these environment variables to `1` for each worker subprocess unless already more restrictive:

- `OMP_NUM_THREADS=1`
- `MKL_NUM_THREADS=1`
- `OPENBLAS_NUM_THREADS=1`
- `NUMEXPR_NUM_THREADS=1`

This prevents nested BLAS/OpenMP oversubscription while the outer scheduler parallelizes years.

Worker count must be configurable. For the Owner's Intel Ultra 7 265K + 32 GB machine, provide a conservative default/recommended PowerShell invocation of:

`-Workers 4`

Allow the Owner to lower or raise it explicitly, but do not hard-wire more than 4 as an unconditional default. Record the actual worker count used.

## 11. Scientific ceilings and timing policy

Preserve scientific finite ceilings:

- maximum outer turns: `250`;
- maximum household calls: `7750` per year;
- automatic scientific reruns: `0`.

Do NOT use the previous 14,400-second per-year wall-clock limit as a default kill condition in the parallel owner-run benchmark. Parallel workers may run slower under shared CPU/memory and a fixed wall-clock kill could create a false scientific anomaly.

Default production behavior:

- measure wall clock;
- no wall-clock kill;
- optional user-specified timeout may exist but default must be disabled.

This is orchestration only; it does not change model convergence criteria.

## 12. Resumability, skip and fail-isolation

The full batch MUST be safely resumable within one batch root.

A year may be skipped only if its success marker and manifests prove all of the following match the current run contract:

- calendar year;
- canonical SHA;
- source hashes;
- scientific code identities;
- checkpoint schema version;
- complete final 31-province state;
- `SOURCE_CONVERGED`;
- required output files present and hash-valid.

Otherwise fail closed for that year; never silently reuse incompatible output.

No overwrite.

If one year fails for a year-specific scientific reason, record the failure and allow other queued years to continue. Do not automatically rerun the failed year.

If failure indicates a shared driver/source/data/code-identity defect, scheduler should stop launching new years and mark the batch blocked.

## 13. Progress bar and PowerShell user experience

Provide a simple PowerShell launcher suitable for the Owner.

Preferred invocation shape:

`powershell -ExecutionPolicy Bypass -File .\scripts\run_mp4c_full_annual_batch.ps1 -Workers 4`

The launcher should:

- resolve repository root and verified `data_local` input root;
- create a fresh timestamped external batch output root by default under `D:\ProjectTemp` unless `-OutputRoot` is explicitly supplied;
- print batch start time, Python executable, workers, years, output root and data root;
- start an overall stopwatch;
- invoke the Python scheduler;
- show a simple progress display/one-line progress bar with at least completed/15, running year(s), elapsed time, and pass/fail count;
- print total elapsed time and output root at completion.

The Python scheduler may own the dynamic progress bar; PowerShell must at minimum show the overall timer/header/final duration.

Avoid heavyweight UI dependencies. Standard-library progress output is preferred.

## 14. Timing and benchmark evidence

The Owner intends to compare Python full-batch time with MATLAB full annual steady-state runtime on the same PC.

Record at minimum:

- batch invocation timestamp;
- batch preflight start/end;
- first scientific worker launch timestamp;
- final scientific worker completion timestamp;
- scientific batch wall-clock duration;
- total launcher wall-clock duration;
- per-year queued/start/end timestamps;
- per-year wall-clock duration;
- per-year process exit/status;
- worker count;
- BLAS/OpenMP thread environment;
- Python version;
- NumPy version;
- SciPy version;
- OS/platform;
- logical CPU count;
- available physical-memory snapshot if obtainable without extra dependency;
- PowerShell/CIM CPU model and installed RAM if practical.

Produce:

- `batch_timing.json`;
- timing columns in `batch_summary.csv`;
- human-readable final timing summary.

Do not infer MATLAB speedup until actual MATLAB benchmark evidence is supplied.

## 15. Full-batch aggregate outputs

At batch completion, generate at minimum in the external batch root:

- `batch_manifest.json`;
- `batch_summary.json`;
- `batch_summary.csv`;
- `batch_timing.json`;
- `steady_state_panel_2009_2023.csv`;
- `2009_2023_稳态值.xlsx`;
- `2009_2023_稳态Ltmat.xlsx`;
- `matlab_persistence_contract.json` copy/receipt;
- artifact/hash manifest covering all required batch outputs.

Per-year directories should contain, at minimum:

- canonical annual input JSON;
- canonical/index/source manifest;
- terminal-only run manifest;
- final steady-state JSON and/or CSV;
- final 31×20 panel row set;
- final `Lt_mat`;
- final household restart checkpoint `.npz`;
- `Python_Multi_Province_12sts_<year>.mat`;
- checkpoint manifest;
- year timing JSON;
- success/failure marker with hashes.

Generated outputs remain external/local and are NOT GitHub artifacts.

## 16. 2009 post-run anchor cross-check in Phase B

After the Owner finishes the full batch, Phase B may consume at most one accepted comparator invocation for the new 2009 terminal against the preserved corrected-2009 MATLAB baseline, provided the new terminal is compatible with the accepted comparator contract.

Comparator reruns: `0`.

No MATLAB process and no Python stationary rerun are authorized in Phase B.

This comparator is a read-only regression check of the new production runner. The pre-existing formal 2009 acceptance remains historical authority regardless of this new benchmark run; any material discrepancy must be reported rather than automatically repaired.

## 17. Phase A zero-science tests

Before telling the Owner the runner is ready, Codex must prove with zero scientific/model execution:

1. 2009–2023 annual binding mapping exact;
2. 2009 primary canonical SHA exact;
3. local source hashes exact;
4. terminal-only mode does not create per-turn disk artifacts under synthetic/mocked execution;
5. final 31×20 serialization schema complete;
6. `Lt_mat` orientation and axes explicit;
7. restart checkpoint schema contains the source-backed required fields;
8. checkpoint round-trip works for synthetic arrays;
9. MATLAB-readable MAT file can be loaded back by SciPy with expected field names/shapes;
10. scheduler queues exactly 15 years;
11. worker concurrency cap obeyed;
12. worker subprocess environment forces BLAS/OpenMP threads to 1;
13. resume skips only hash-valid successful years;
14. incompatible existing output fails closed;
15. year-specific failure is isolated;
16. shared-contract failure stops further launches;
17. progress/timing formatting works with mocked workers;
18. PowerShell syntax/static invocation preflight passes without running science;
19. historical 2009/2010/2011 accepted result artifacts are not read as substitutes for the new fresh batch;
20. no protected MATLAB/data file is written.

`py_compile` all changed/new Python files and run focused tests.

Scientific/model calls in Phase A must all be zero.

## 18. Phase A local build receipt and NO PUSH

Use a fresh external preparation root, preferred:

`D:\ProjectTemp\ch5-mp4c-full-batch-runner-build-20260902-001`

Persist at minimum:

- `matlab_persistence_contract.json`;
- `batch_runner_build_receipt.json`;
- `zero_science_test_receipt.json`;
- `owner_launch_instructions.txt`;
- `build_manifest.json`.

`batch_runner_build_receipt.json` must record exact SHA-256/bytes for every uncommitted implementation/test/PowerShell file the Owner is about to use.

At end of Phase A:

- do NOT git commit;
- do NOT push;
- do NOT run annual science;
- report the exact PowerShell command to the Owner;
- report the exact build-receipt path/hash;
- leave implementation changes available locally for the Owner run.

Required Phase-A terminal:

`MP4C_FULL_2009_2023_PARALLEL_BATCH_RUNNER_READY_FOR_OWNER_MANUAL_EXECUTION__NO_SCIENCE__NO_PUSH`

## 19. Owner manual-run authorization

Once Phase A reaches the required ready terminal, this task authorizes the Owner to manually run exactly one fresh/resumable 2009–2023 production batch using those build-receipt-matched scripts.

The Owner may choose the worker count at launch. Recommended first full run on the stated machine is `4` workers.

The Owner may stop/restart the same batch command; resume semantics must avoid rerunning hash-valid completed years. This is not counted as an automatic scientific rerun of completed years.

A failed year must not be manually forced/retried under this task. Report anomalies after the batch.

## 20. Phase B post-run audit

After the Owner tells Codex the batch is finished:

1. fresh-fetch GitHub and confirm this task remains the governing authority or identify any later superseding authority;
2. verify local implementation/test/script hashes match the Phase-A build receipt exactly;
3. verify batch root no-overwrite identity and manifests;
4. parse all 15 year status/checkpoint/timing objects;
5. verify output files and hashes;
6. verify 31-province order and required steady-state fields for successful years;
7. verify final household checkpoint exists for successful years and is bound to that year's canonical/scientific identities;
8. verify aggregate Excel/CSV outputs cover exactly 2009–2023;
9. verify total/per-year timing evidence;
10. perform no model rerun;
11. perform the one 2009 comparator-only check only if eligible under Section 16;
12. report any anomalous year separately; do not hide it and do not repair/retry.

## 21. Repository mutation boundary and final closeout

Allowed implementation paths include only the minimal files needed for this route, preferably:

- `validators/multi_province/mp4c_python_annual_production.py`;
- `validators/multi_province/mp4c_run_full_annual_batch.py`;
- `scripts/run_mp4c_full_annual_batch.ps1`;
- `tests/test_mp4c_full_annual_batch_runner.py`;
- optionally the existing focused annual-driver test if minimally required;
- optionally one small text/schema helper under `validators/multi_province/` if clearly necessary;
- final required report.

Do NOT modify for this task:

- household/HJB/KFE scientific modules;
- `one_turn.py`;
- `firm.py`;
- migration/capital/wage/monetary/fiscal/controller science;
- protected MATLAB source;
- primary data files;
- accepted comparator scientific contract;
- grids/calibration/thresholds/tolerances;
- existing accepted 2009/Chunk01 evidence.

Do not commit:

- `data_local` binaries;
- `.mat`, `.npz`, `.xlsx`, large `.csv`, batch result JSONs;
- run logs;
- generated checkpoints;
- external evidence roots.

After Phase B audit, required GitHub report:

`docs/CH5_TWO_ASSET_HANK_MP4C_FULL_2009_2023_OWNER_RUN_PARALLEL_BATCH_RUNNER_AND_EXECUTION_REPORT.md`

Before final commit:

- explicit allowed-path staging only;
- ensure no generated scientific outputs are staged;
- `git diff --check --cached` PASS;
- record exact changed paths.

Then:

- exactly one final implementation/report commit is preferred;
- exactly one non-force push;
- fresh GitHub read-back of every changed path;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean tracked worktree.

If all 15 years succeed, required final terminal:

`MP4C_FULL_2009_2023_OWNER_RUN_PARALLEL_ANNUAL_STATIONARY_COVERAGE_PASS`

If one or more years fail but the runner/evidence is valid, use a truthful partial/anomaly terminal and list exact years. Do not claim full coverage.

This remains Python production annual coverage, not blanket MATLAB–Python parity for all years.

## 22. Next scientific route after closeout

If full annual coverage is accepted, the next scientific gate is:

`AR(1)/shock source audit -> representative-year Python shock/restart validation -> required paper IRFs -> return project emphasis to paper reconstruction and improvement`.

Do not implement or run shocks/IRFs inside this task.
