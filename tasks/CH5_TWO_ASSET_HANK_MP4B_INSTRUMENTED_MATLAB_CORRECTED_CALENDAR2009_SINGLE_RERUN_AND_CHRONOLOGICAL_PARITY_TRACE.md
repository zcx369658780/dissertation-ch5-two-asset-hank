# CH5_TWO_ASSET_HANK_MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / instrumented MATLAB observability executor / chronological parity evidence producer

Owner: final scientific authority

## 1. Explicit Owner authorization

The Owner has explicitly authorized exactly this bounded scientific execution:

> 同意授权一次 instrumented MATLAB corrected-2009 stationary rerun，仅增加逐轮 observability，禁止改方程、参数和算法，rerun=0。

This task operationalizes that authorization and nothing broader.

Authorized scientific budget:

- exactly one instrumented MATLAB corrected-calendar-2009 full stationary top-level run;
- reruns: `0`;
- instrumentation may add observability only;
- equations, parameters, grids, algorithms, update order, branch conditions, tolerances, controller multipliers and convergence logic are immutable.

This task does **not** authorize another year, annual batch, standalone household replay, HJB/KFE replay, Python stationary rerun, comparator replay, shocks, AR(1), transition, dynamics, IRF, historical R5, calibration redesign, Results, or production refactoring.

## 2. Accepted predecessor authority

Required execution-start predecessor:

`f48ca0faf3fb626dbd65318773892d578c89c2d6`

Accepted predecessor terminal:

`MP4B_MATLAB_SIDE_GOVINV_LOW_RETURN_BRANCH_TRIGGER_PROVENANCE_AUDIT_PASS`

Accepted predecessor classification:

`MP4B_MATLAB_PROFILE_BRANCH_COUNT_PROVENANCE_SHARPENED__TURN_UNAVAILABLE`

Accepted markers/facts which must remain unchanged:

- `MP4B_FINAL_GOVINV_CONTROLLER_BRANCH_COUNT_DIFFERENCE_LOCALIZED`;
- `MP4B_MATLAB_PROFILE_LOW_GOVINV_BRANCH_EXECUTION_COUNT_EXCEEDS_PYTHON_BY_ONE`;
- `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`;
- corrected-calendar-2009 MATLAB and Python both terminate at outer turn `184`;
- both execute/represent `5,704` household calls and end with `31/31` household convergence;
- common Zhejiang initial `GovInv = 486346722590.8805`;
- MATLAB final Zhejiang `GovInv = 161948418.38387933`;
- Python final Zhejiang `GovInv = 179942687.09319925`;
- MATLAB/Python final Zhejiang GovInv ratio is exactly IEEE `0.9`;
- MATLAB bounded controller lattice is `(n_low,n_high)=(76,0)`;
- Python bounded controller lattice is `(75,0)`;
- Python durable history directly records 75 Zhejiang `LOW_RA_DECREASE_0P9` actions and zero high-return actions;
- persisted MATLAB profiler aggregate low-return branch body count is `2,337` versus Python aggregate `2,336`;
- high-return branch actions are `0` on both sides;
- MATLAB profiler records `2,366` Zt reset events versus Python `2,365`;
- preserved MATLAB artifacts do not contain a turn/province controller trace sufficient to identify the chronological divergence;
- no MATLAB/Python fault attribution is currently accepted.

Do not reinterpret these facts before new admissible trace evidence is established.

## 3. Live continuity and governing rules

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `f48ca0faf3fb626dbd65318773892d578c89c2d6`;
3. require a clean worktree and `HEAD == origin/main`, ahead/behind `0/0`;
4. read at minimum:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`;
5. verify repository identity;
6. verify all immutable identities in Section 4;
7. verify the logical/physical MATLAB source-root relationship in Section 5;
8. verify all durable baseline artifacts and corrected-2009 binding evidence;
9. verify sufficient free space on the physical target drive before making a diagnostic copy or running MATLAB.

Any failure before the one authorized top-level MATLAB run => stop with zero science consumed.

## 4. Immutable repository and scientific identities

Require exact SHA-256 unless a Git blob is explicitly specified. Do not modify these objects:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py`:
  `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py`:
  `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py`:
  `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map:
  `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter:
  `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver:
  `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- comparator contract:
  `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map:
  `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- remediated comparator helper:
  `0FD6889E10E502F32C10B3373702445333B452B25A11A63A3899184A67853EF3`
- remediated comparator helper Git blob:
  `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Protected MATLAB source identities:

- `HANK_2ASSETS_HJB.m`:
  `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m`:
  `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m`:
  `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m`:
  `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`
- `HANK_mp_1eq.m`:
  `ED39E661AF951E01D1F5F9D123CE0FAD980F5D3DB33FD338DE60DA87731E0AEF`
- `HANK_mp_1turn.m`:
  `D3D03F37286ED66202673EA63D49BABCE8D5309BAC9C13793C8E60585C21FECF`
- `HANK_firm.m`:
  `EE02C15414ADF9F99AADE04F1F22E64FA7094C8AB77753B6130BC4BFA6CE7BD5`.

The original protected source identities must still match after the diagnostic run.

## 5. Protected source boundary and diagnostic-copy requirement

Protected source root:

- logical compatibility root:
  `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- physical root:
  `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Verify that the logical C path resolves through the documented Junction to the expected D physical storage root. Never use broad path-prefix or Java-canonical-path trust as a substitute for the finite exact root pair, Junction evidence, exact source hashes and parent-membership checks.

The original protected source tree is strictly read-only.

Create exactly one fresh no-overwrite external diagnostic run root under `D:\ProjectTemp`, recommended:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-20260831-001`

Requirements:

- the selected root must not exist before use;
- if the recommended root exists, choose a fresh deterministic suffix before science and record it; do not delete or overwrite an existing root;
- create a copied source tree such as `<run_root>\source_copy` from the protected original;
- preserve relative paths;
- instrument **only the copy**;
- all run outputs and observer artifacts remain under the fresh run root;
- do not write into the original C/D MatlabProgram tree;
- do not submit copied MATLAB source, MAT/HDF5 outputs, large traces, logs or data to GitHub.

Before copy/run, record physical D: free space. Require at least the larger of:

- `10 GB` free;
- five times the copied source-tree size.

If the free-space gate fails, stop before science.

## 6. Corrected-calendar-2009 identity and invocation binding

Canonical corrected-calendar-2009 input:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Frozen identity:

- `calendar_year = 2009`;
- `analysis_index = 1`;
- workbook data row index `10`;
- physical Excel row `11`;
- `data_MAT_index = 1`;
- output filename year `2009`;
- regression vintage key `10`;
- fixed-2020 `IND_Zt` only as the already accepted source numerical initialization anchor.

The legacy wrong-year route is forbidden.

Preserved corrected-2009 MATLAB baseline root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require exact read-only identities:

- `matlab_calendar2009_stationary_output.mat`:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`
- preserved presolver manifest:
  `DF9380BB057ECF24E519BBF03D5A643036CC92B4FFAC18DB411B75783E5AABB9`.

Before science, reconstruct and prove the exact corrected-2009 invocation/data-binding route used by the accepted preserved baseline from the canonical JSON, preserved presolver manifest, protected source entry chain and already accepted audit evidence. Persist a fresh `instrumented_invocation_binding_manifest.json` under the new run root.

Do **not** guess a wrapper, year binding, working directory, MATLAB path, workbook row, `data_MAT` index or saved-cache route.

If exact binding cannot be proved before the top-level MATLAB model call:

`MP4B_INSTRUMENTED_MATLAB_RUN_BLOCKED`

and consume zero science.

## 7. Copied-source identity gate and instrumentation patch

Immediately after copying, before any instrumentation:

1. hash all critical copied MATLAB files;
2. require exact equality to their protected originals;
3. establish a zero scientific diff for the copied tree before instrumentation.

After instrumentation, persist:

- `instrumentation_manifest.json`;
- `instrumentation_patch.diff` containing the complete copied-source instrumentation diff;
- hashes of every modified/added copied diagnostic file.

Allowed copied-tree mutations are limited to observability in:

- copied `HANK_mp_1eq.m`;
- copied `HANK_mp_1turn.m`;
- copied `HANK_firm.m`;
- optional instrumentation-only observer helper(s), preferably namespaced with `MP4B_OBS_`;
- one bounded run wrapper under the fresh run root or copied diagnostic tree.

Do not modify copied:

- `HANK_2ASSETS_HJB.m`;
- `HANK3_FOC.m`;
- `HANK3_cost.m`;
- `lab_solve2.m`;
- any economic/calibration/data source unless the task's observability contract explicitly requires only reading an already-computed value.

Avoid changing function signatures. If a signature change is absolutely necessary solely to route an observer handle/trace sink, document why and prove that it does not alter scientific values or call order; prefer shared diagnostic state/observer helper instead.

## 8. Instrumentation invariance contract

Before mutation, verify a distinctive observer prefix such as `MP4B_OBS_` is absent from the protected source tree.

Instrumentation may:

- copy already-computed model scalars/vectors into a diagnostic trace;
- record turn/province/layer identifiers;
- record exact branch predicates and branch actions;
- record before/after values around existing assignments;
- record output/final/terminal metadata;
- serialize the diagnostic trace at terminal or exception boundary.

Instrumentation must **not**:

- assign diagnostic values back into any scientific variable;
- change an economic equation;
- change a comparison operator or branch condition;
- change `0.9`, `1.1`, `0.6`, `0.4`, `0.01`, return/wage bounds, HJB `maxit`, HJB `crit`, outer threshold, outer max iterations, grids or calibration;
- reorder scientific expressions;
- reorder province loops, household calls, firm calls or controller updates;
- insert extra calls to HJB, KFE, household, firm, one-turn or equilibrium functions;
- alter random state;
- alter solver initialization;
- alter try/catch or early-return behavior in a way that changes the scientific route;
- introduce retries;
- perform per-event disk IO if it could perturb route/timing; prefer lightweight in-memory trace collection and one terminal serialization.

The one instrumented run must be scientifically the same corrected-2009 source execution with observability attached.

## 9. Required chronological trace schema

The trace must be sufficient to align MATLAB with the already durable Python 184-turn history in source order. Do not store full policy arrays or full household distributions unless absolutely necessary; scalar/vector state evidence is preferred.

### 9.1 Outer-turn entry before `HANK_mp_1turn`

For every turn and province record at minimum:

- outer turn number;
- province index and raw/source province name;
- `rb`;
- `rah`;
- `w`;
- `rb_gap`;
- `tau`;
- `Tt`;
- carried `ra`;
- carried `wjt`;
- `GovInv`;
- `Zt`;
- `Yt`;
- `Yt0`;
- `Kt`;
- `Lt`;
- `KNratio`;
- current `tKNratio`;
- `ramin`, `ramax`, `wjtmin`, `wjtmax`.

### 9.2 Household-call boundary inside copied `HANK_mp_1turn.m`

Before each provincial `HANK_2ASSETS_HJB` call record exact active household inputs:

- turn/province;
- `rb`, `rah`, `w`, `rb_gap`, `tau`, `Tt`;
- any source grid/calibration identity already fixed globally, without duplicating large arrays.

After each household call record:

- `Ct`;
- household-returned labor explicitly labeled `household_Lt`;
- `At`;
- `Bt`;
- `AtTax`;
- `convergent`;
- HJB iteration/statistic only if already available at this calling layer without modifying HJB.

After the complete 31-household batch record:

- all 31 household convergence flags;
- convergence count.

### 9.3 Migration-labor layer

Record per province:

- final `Lt_supply`;
- row-sum evidence sufficient to establish the migration result;
- do not persist the full `Lt_mat` unless the observer can do so compactly and without materially increasing IO/memory risk.

### 9.4 At-only productive-capital / pre-firm household-return layer

Record per province:

- household `At` used by the source allocation;
- population and `inter_prv_ratio` if already present and cheap to record;
- old/carry firm `ra` entering the return mapping;
- `Kt_supply`;
- newly computed household `rah`.

Liquid `Bt` must not be substituted into productive capital.

### 9.5 Firm-call input and copied `HANK_firm.m` pre-clipping evidence

For each province record firm inputs sufficient for exact same-input adjudication, including at minimum:

- `Kt_supply`;
- `GovInv`;
- `Lt_supply`;
- `Zt`;
- `alpha`;
- prior/carry values actually read by the source branch;
- return/wage bounds.

Inside copied `HANK_firm.m`, before clipping record already-computed:

- `Kt`;
- `Lt`;
- `Yt`;
- `mt`;
- `KNratio`;
- `wt0`;
- `rk`;
- `ra0`;
- `wjtmin`, `wjtmax`, `ramin`, `ramax`;
- exact raw clip-condition booleans/categories;
- final clipped `wjt`, `ra`;
- `Govinc`;
- `PIt`, `Corptax`, `delta` and other already-computed scalars only where useful for equation reconstruction.

Do not recompute a scientific quantity merely to log it if the source has already computed the same quantity.

### 9.6 Composite wage, Taylor and fiscal layers

After composite household wage:

- record new household `w` per origin province.

After Taylor assignment:

- record `it` and `rb` plus already-available policy inputs that uniquely determine them.

After fiscal diagnostics:

- record `GovSurplus` and per-province `Govinc` if available.

### 9.7 Outer convergence and adaptation controller

Before the terminal/adaptation decision record:

- per-province `KNratio/tKNratio - 1` or exact source gap;
- per-province `Yt/Yt_1 - 1` or exact source gap;
- maximum NK gap;
- maximum Y gap;
- household converged count;
- ra upper/lower counts;
- wage upper/lower counts;
- full convergence/terminal predicate.

For every nonterminal turn record:

- adaptation gate `max_nk_gap < 0.1 && steady_state`;
- for every province `Yt/Yt0 - 1`;
- Zt-reset predicate;
- `Zt` before/after and whether reset occurred;
- low-return predicate `ra < ramin + 0.02`;
- high-return predicate `ra > ramax - 0.02` when evaluated;
- exact `ra`, `ramin+0.02`, `ramax-0.02`;
- `GovInv` before/after;
- action exactly one of `NONE`, `LOW_RA_DECREASE_0P9`, `HIGH_RA_INCREASE_1P1`;
- `tKNratio` before/after.

The observer must make the historically extra MATLAB low action and extra Zt-reset event identifiable by exact turn and province if the instrumented execution reproduces the preserved baseline.

## 10. Scientific execution budget

Only after all pre-science gates pass:

### MATLAB

- instrumented corrected-calendar-2009 full stationary top-level run: maximum `1`;
- reruns: `0`;
- every other MATLAB scientific/model invocation: `0`.

There is no separate smoke model run before the one-shot. Static syntax/source inspection is allowed if it invokes no scientific function.

If the one authorized MATLAB run crashes, fails to converge, produces a trace-write error, or otherwise does not establish an admissible terminal, do not repair and rerun in this task.

### Python

All scientific/model calls remain `0`:

- stationary `0`;
- household adapter `0`;
- HJB `0`;
- KFE `0`;
- household aggregation `0`;
- MP2/MP3 scientific execution `0`.

Read-only parsing of the already durable Python terminal/history is authorized.

### Comparator

Qualified comparator / `compare_terminal`: `0`.

Direct read-only arithmetic comparison of already durable trace/final artifacts is authorized and is not a comparator replay.

### Other forbidden science

- standalone Zhejiang replay `0`;
- Shanxi replay `0`;
- other years / annual batch `0`;
- shocks / AR1 / transition / dynamics / IRF `0`;
- historical R5 `0`;
- Results `0`.

## 11. Required run artifacts

Under the fresh no-overwrite run root persist at minimum:

- `instrumented_run_manifest.json`;
- `instrumented_invocation_binding_manifest.json`;
- `instrumentation_manifest.json`;
- `instrumentation_patch.diff`;
- `instrumented_terminal_status.json`;
- one final-state artifact such as `instrumented_final_state.mat`;
- one complete chronological trace artifact such as `instrumented_outer_trace.mat` / HDF5;
- one compact JSON trace summary suitable for audit;
- `matlab_python_chronological_trace_comparison.json` if the trace is admissible;
- source-copy manifest with pre/post instrumentation hashes.

Record path, bytes and SHA-256 for every durable artifact. Never overwrite an existing artifact.

Do not put these large/local artifacts in Git.

## 12. Instrumented-run baseline-admissibility gate

The trace may be used for fault/chronology adjudication only if the instrumented run reproduces the immutable preserved MATLAB corrected-2009 baseline.

After the one-shot, before any causal interpretation, require direct comparison to the preserved baseline and report at minimum:

- terminal status `COMPLETED` / source-equivalent completion;
- outer turns exactly `184`;
- household calls exactly `5,704`;
- final household convergence `31/31`;
- adaptation-eligible province-turns exactly `3,100`;
- low-return GovInv `0.9` actions exactly `2,337`;
- high-return `1.1` actions exactly `0`;
- Zt reset actions exactly `2,366`;
- source branch/control counts consistent with preserved profiler where directly comparable;
- all final frozen comparison fields for all 31 provinces exactly equal to the preserved MATLAB baseline, after only the already accepted raw-name representation projection where needed;
- final boundary categories exactly equal;
- final national `Ct/At/Bt/Yt` exactly equal under the same operation order when reconstructable.

Frozen final fields:

`Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`.

No new tolerance may be invented to excuse an instrumentation-induced final difference. Because this is the same MATLAB source route, exact final equality is the default admissibility expectation wherever extraction/storage representation is the same.

If any scientific final-state difference or required controller-count mismatch is observed, classify:

`MP4B_INSTRUMENTED_MATLAB_BASELINE_PERTURBED__TRACE_NOT_ADMISSIBLE`

Do not use the trace for MATLAB/Python fault attribution. Do not rerun.

## 13. Chronological MATLAB/Python alignment if and only if trace is admissible

Align the admissible instrumented MATLAB trace against the already durable Python 184-turn history without rerunning Python.

Preserve the accepted source layer order:

1. outer-turn entry;
2. household input;
3. household output;
4. migration labor;
5. `At`-only productive-capital / pre-firm `rah`;
6. firm input;
7. firm pre-clipping state;
8. firm output;
9. composite household wage;
10. Taylor `it/rb`;
11. fiscal diagnostics;
12. convergence/controller adaptation.

For every turn and province where both artifacts expose the same quantity, distinguish:

- first bitwise/nonzero numerical difference — descriptive only;
- first continuous divergence exceeding an already frozen contract threshold;
- first exact categorical/boolean/branch divergence;
- first controller-action divergence.

Frozen comparison semantics only:

- categorical/boolean/integer/order: exact;
- source-identical arithmetic with the same operation order: exact;
- otherwise source-identical outer arithmetic maximum `rtol=1e-12`, `atol=1e-12` only where the frozen contract already permits it;
- same-input household continuous gate: `abs(M-P) <= 1e-7 * max(1,abs(M),abs(P))` for the already frozen household fields;
- no post-output tolerance widening.

A tiny earlier numerical difference must not automatically be called the scientific root cause unless it can be tied to a later discrete branch or same-input source disagreement.

## 14. Mandatory localization of the extra MATLAB low action and Zt reset

If the trace is admissible, identify exactly:

- the turn/province of every MATLAB low-return action;
- the turn/province of every Python low-return action from durable history;
- the set difference, expected from preserved evidence to contain one additional MATLAB low action;
- the turn/province of every MATLAB Zt reset;
- the Python Zt reset set;
- the set difference, expected from preserved evidence to contain one additional MATLAB Zt reset.

At the exact extra-action turn/province, produce a source-layer adjudication table containing MATLAB and Python:

- turn-entry `rb,rah,w,rb_gap,tau,Tt`;
- carried `ra,wjt,GovInv,Zt,Yt,Kt,Lt,KNratio,tKNratio`;
- household inputs;
- household `Ct,household_Lt,At,Bt,AtTax,convergent`;
- migration `Lt_supply`;
- capital `Kt_supply` and new `rah`;
- firm inputs;
- pre-clipping `wt0,ra0`;
- firm `Kt,Lt,Yt,mt,KNratio,wjt,rk,ra,Govinc`;
- composite `w`;
- controller gaps/thresholds;
- `ra-(ramin+0.02)`;
- `(ramax-0.02)-ra`;
- `Yt/Yt0-1`;
- `GovInv` before/after;
- `Zt` before/after;
- exact branch/action categories.

Also trace backward within the same source order, and if needed to earlier turns, until the earliest source-backed interface where the divergence is supported.

## 15. Fault-attribution rule

Use a strict same-input rule.

### MATLAB legacy defect may be confirmed only if

At some source-backed layer, MATLAB and Python have scientifically identical/same-input values under the frozen contract, but MATLAB produces a different output/branch result that contradicts the protected source equation or accepted scientific contract while Python follows it.

Then the bounded classification may be:

`MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`

The report must identify exact turn, province, input values, source lines/equation, MATLAB result and Python result.

### Python implementation defect may be confirmed only if

The same-input evidence instead shows protected MATLAB/source semantics are followed by MATLAB and violated by Python.

Then:

`MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`

### If branch inputs already differ

Do not call the branch itself defective. Trace upstream.

If the first supported evidence is an upstream numerical trajectory difference without a same-input source violation, use:

`MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`

### If chronology is localized but fault remains unresolved

Use:

`MP4B_CHRONOLOGICAL_FIRST_DIVERGENCE_LOCALIZED`

and state explicitly what is and is not proven.

Do not infer that human-written MATLAB is wrong merely because it differs from AI-written Python. Do not infer that Python is correct merely because its code is cleaner or its trajectory is reproducible.

## 16. Primary classifications and terminal verdicts

Choose exactly one primary scientific classification supported by evidence:

- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`
- `MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`
- `MP4B_CHRONOLOGICAL_FIRST_DIVERGENCE_LOCALIZED`
- `MP4B_INSTRUMENTED_MATLAB_BASELINE_PERTURBED__TRACE_NOT_ADMISSIBLE`
- `MP4B_INSTRUMENTED_MATLAB_RUN_BLOCKED`.

Execution terminal must be one of:

- `MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE_PASS`
- `MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE_MATERIAL_MISMATCH`
- `MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE_INSTRUMENTATION_PERTURBED`
- `MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE_BLOCKED`.

`PASS` means the authorized diagnostic execution and admissible chronology objective succeeded. It does **not** establish corrected-2009 stationary parity, any IRF result, or a Results claim.

## 17. Required report

Write exactly one principal report:

`docs/CH5_TWO_ASSET_HANK_MP4B_INSTRUMENTED_MATLAB_CORRECTED_CALENDAR2009_SINGLE_RERUN_AND_CHRONOLOGICAL_PARITY_TRACE_REPORT.md`

The report must include at minimum:

1. exact terminal verdict and primary classification;
2. the Owner authorization quoted in Section 1;
3. live continuity and task authority;
4. all immutable repository/scientific/source identities;
5. Junction/logical/physical root verification;
6. D: free-space gate;
7. fresh diagnostic run-root path and no-overwrite proof;
8. corrected-2009 invocation/binding proof and manifest identity;
9. source-copy pre-instrument hash/diff gate;
10. complete instrumentation patch summary and local patch artifact hash;
11. explicit instrumentation invariance audit;
12. exact science-call ledger before and after run;
13. one-shot MATLAB command, start/end time, wall time and terminal;
14. output artifact paths/bytes/SHA-256;
15. baseline-admissibility comparison against preserved MATLAB final state and profiler counts;
16. whether the trace is admissible;
17. exact MATLAB low-action and Zt-reset turn/province sets if admissible;
18. exact extra-event turn/province;
19. chronological layer-by-layer MATLAB/Python comparison;
20. first bitwise/nonzero difference, first frozen-contract continuous divergence, first categorical divergence and first controller-action divergence as separate concepts;
21. exact same-input adjudication table around the decisive turn/province;
22. wage-upper boundary mismatch chronology if the trace exposes it;
23. supported causal findings;
24. unsupported hypotheses/fault claims;
25. all zero/forbidden operation counts;
26. proof that original protected source hashes remained unchanged after run;
27. `git diff --check`;
28. repository changed paths;
29. forbidden-operation audit;
30. execution commit SHA, non-force push and GitHub read-back;
31. final `HEAD/origin-main`, ahead/behind, clean worktree;
32. exactly one recommended next gate.

## 18. Repository mutation scope

The instrumented MATLAB source copy and all local diagnostic artifacts remain external under `D:\ProjectTemp` and must not be committed.

Allowed repository changes for the executor:

- the report in Section 17;
- optionally one bounded CURRENT-roadmap status update if needed for continuity.

Do not commit:

- copied MATLAB source;
- instrumentation helper/wrapper;
- MAT/HDF5/JSON trace artifacts;
- data/cache/workbooks;
- profiler/log dumps.

Do not modify repository scientific Python code, tests, comparator, contracts, project rules or protected-source mirrors in this task.

## 19. Closeout

Use explicit-path staging, one execution commit, one non-force push, and fresh GitHub read-back.

After push verify:

- exact changed paths;
- report blob identity;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- worktree clean.

No subsequent scientific replay or repair is automatically authorized by this task.