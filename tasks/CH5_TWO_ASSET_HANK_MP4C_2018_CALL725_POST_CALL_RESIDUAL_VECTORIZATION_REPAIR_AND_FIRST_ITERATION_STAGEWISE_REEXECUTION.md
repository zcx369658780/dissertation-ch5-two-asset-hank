# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / external wrapper instrumentation repair certifier and isolated first-iteration stagewise executor

Owner: final scientific authority

## 1. Immediate live authority and purpose

Immediate live parent at publication:

`81037a378ab67f9d54ae8fd5a2eab6020cc3d4b7`

Accepted predecessor report:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_FIRST_ITERATION_HJB_SCALAR_MANIFEST_BINDING_REPAIR_AND_FRESH_STAGEWISE_EXECUTION_REPORT.md`

Predecessor report Git blob at the parent commit:

`46c30b3c0eda3013a4028b93cbe6ca591cb1c010`

Predecessor terminal:

`MP4C_2018_CALL725_SCALAR_BINDING_REPAIR_AND_FIRST_ITERATION_FORENSIC_BLOCKED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Predecessor classification:

`CALL725_FIRST_ITERATION_FORENSIC_BLOCKED__WRAPPER_POST_CALL_INSTRUMENTATION_FAILURE__NO_PRODUCTION_CHANGE`

The predecessor established all of the following:

1. scalar-manifest binding repair passed;
2. exact-MAT MATLAB/Python certify-only ingestion passed;
3. the exact certification marker was obtained;
4. one MATLAB scientific iteration-1 invocation reached the direct solve;
5. the wrapper then failed only in post-call residual instrumentation at `norm(matrix*updated-rhs,inf)` because `matrix` was `800 x 800` and `updated` was `(20,20,2)`;
6. no durable stage artifact was persisted;
7. Python strict-common execution and native-init probe were not started;
8. no scientific earliest-divergence conclusion was established;
9. no production, protected MATLAB, solver, grid, parameter, KFE, GE, annual, R/PLM, shock/IRF, or Results mutation/execution occurred.

This successor authorizes only:

- zero-science repair and certification of that external post-call residual/vectorization and persistence instrumentation;
- one fresh replacement MATLAB first-iteration scientific invocation after certification, because the predecessor's sole invocation produced no durable stage artifact and no comparable scientific object;
- one Python strict-common first iteration only after durable MATLAB stage persistence;
- one conditional Python native-initialization amplification probe under the same scientific condition as the predecessor task.

This is a new, task-owned budget. No predecessor budget is inherited or reused.

No production repair, `maxit` change, iteration 2+, full HJB, KFE, GE, annual rerun, R/PLM, shock/IRF, or Results work is authorized.

## 2. Live continuity and start gate

At start:

1. verify repository identity and remote;
2. `git fetch origin`;
3. require this exact task on live `origin/main` as the direct child of `81037a378ab67f9d54ae8fd5a2eab6020cc3d4b7`;
4. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
5. read `AGENTS.md` and all CURRENT project rules;
6. read this task;
7. read the accepted predecessor report and predecessor task;
8. read the parent exact-MAT execution task/report, initialization-array parity report, MATLAB/Python call-725 replay reports, and accepted propagation-aware HJB parity report required by the predecessor.

If live authority, parentage, task identity, or required evidence differs materially, STOP before any repair or scientific call.

## 3. Frozen source/evidence identities

Carry forward unchanged from the predecessor task/report:

Repository Git blobs:

- `exports/matlab_faithful_two_asset_ha.py`: `9e7dc9556a2b76811e78f89999abecc045886106`;
- `validators/multi_province/mp4b_python_empirical.py`: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`: `0033baee136c0328e80ffb8b794a88d4405c976c`.

Protected MATLAB SHA-256:

- `HANK_2ASSETS_HJB.m`: `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m`: `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m`: `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m`: `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

Authoritative HJB100 initialization MAT SHA-256:

`1718984CB588AE586F74AB8476C57AF849BB2C80CC95500329D29BC14207BB81`

Historical scalar contract:

- `call725_matlab_active_input_contract.json` SHA-256 `02ED3D70AC8217435E7B14122E63F2981FFAB8EF1EDECBA9CDD4EB7317F0DB8E`.

Frozen HJB100 manifest:

- `hjb100_manifest.json` SHA-256 `F81A91DD9DED49C01521CEF2EEC02A5B5BC1DA405AAD8206F68FD698B01B16C0`.

Task-owned scalar binding from predecessor:

- `call725_first_iteration_scalar_binding.json` SHA-256 `A40D088C63FC1F7EDECEA561D649B42959C646DF528ED13298014493DB4808F6`.

Predecessor external wrapper identities:

- MATLAB wrapper SHA-256 `131E1EFA53C260322CC79E30B19B12E18CFD2D8171F97D5345E8EC8510D300FC`;
- Python wrapper SHA-256 `63612FBF57A76EF70FF640A7E3AD9D2CE38535F22F324469B407FA8AC43F7B0E`.

Predecessor evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-first-iteration-scalar-binding-repair-20260904-001`

Require final predecessor supplement SHA-256:

`F0A4E2E43973658D52AA8E207A7B4E28A311902676B289DB848640709A97517D`.

Do not silently substitute any source, manifest, wrapper, or MAT input.

## 4. Frozen exact-MAT common input and scalar contract

The strict pair must continue to direct-load the authoritative HJB100 MAT arrays exactly:

- `b`;
- `ah`;
- `z`;
- `v0`;
- `l0`.

Forbidden in the strict pair:

- `linspace` reconstruction;
- JSON reconstruction of grid arrays;
- decimal-text reconstruction;
- `_source_initial_arrays(...)`;
- any alternate initialization source.

The scalar/economic values remain exactly:

- province `安徽`;
- zero-based province index `11`;
- outer iteration `24`;
- global household call `725`;
- `rah=0.09`;
- `rb=0.02`;
- `rb_gap=0.07`;
- `tau=0.05`;
- `w=16.82014806560587`;
- `Tt=0.1`;
- switch matrix `[[-1/3,1/3],[1/3,-1/3]]`;
- `rho=0.05`;
- `gamma=2`;
- `phi=5` / source `frisch_l=0.2`;
- `chi0=0.1`;
- `chi1=2`;
- `a_bar=1e-6`;
- `Delta=1000`;
- `crit=1e-7`;
- drift tolerance `1e-12`.

No value may be changed.

## 5. Phase 0A — zero-science post-call instrumentation root-cause audit

Before changing any wrapper, inspect the exact predecessor wrapper source and failure log.

Persist:

- wrapper absolute path and SHA-256;
- exact failing line and surrounding persistence order;
- exact shapes/classes of `matrix`, `updated`, and `rhs` intended by the wrapper;
- the flattening/indexing convention required by the accepted MATLAB source and existing parity contracts;
- whether `updated(:)` is the exact MATLAB column-major / F-order vector representation corresponding to the linear system;
- the exact Python equivalent needed only for residual/backward-error instrumentation;
- whether any stage artifact could be persisted before optional post-solve diagnostics without changing scientific arithmetic.

The expected root cause is an omitted vectorization step in diagnostic instrumentation only. If audit shows a scientific equation, matrix assembly, state ordering, or solver-semantic defect instead, STOP with:

`MP4C_2018_CALL725_POST_CALL_INSTRUMENTATION_REPAIR_BLOCKED__NON_INSTRUMENTATION_DEFECT_DISCOVERED__NO_HJB_NO_KFE`.

## 6. Phase 0B — construct fresh repaired external wrappers

Create fresh no-overwrite external wrappers. Do not overwrite predecessor wrappers.

Permitted changes are strictly limited to:

1. residual/backward-error instrumentation shape handling;
2. persistence/serialization ordering;
3. explicit stop after iteration 1;
4. defensive shape assertions that fail before science if representation is inconsistent.

For MATLAB residual instrumentation, the intended operation must use the source-consistent vector representation, e.g. the mathematical equivalent of:

`updated_vec = updated(:);`

before evaluating the linear-system residual.

For Python, use the exact corresponding F-order vector representation only for the same diagnostic object, e.g. the mathematical equivalent of:

`updated.reshape(-1, order="F")`.

Do not change the scientific `updated`/`V1` tensor representation used for persisted stage comparison.

Forbidden changes:

- derivative formulas;
- boundary formulas;
- policy/FOC logic;
- adjustment-cost or return formulas;
- drift/rate formulas;
- sparse `BB`, `AAH`, `Bswitch`, `A`, `M`, or RHS assembly;
- direct solver;
- state flattening/index ordering used by scientific computation;
- arithmetic ordering intentionally;
- any parameter, tolerance, grid, source value, controller, or production code.

Persist an exact predecessor-vs-repaired wrapper diff. Every changed line must be classified `POST_CALL_INSTRUMENTATION_OR_PERSISTENCE_ONLY`; otherwise fail closed before science.

## 7. Phase 0C — zero-science certification

Before any scientific HJB invocation, certify the repaired wrappers with zero scientific evaluations.

Required checks:

1. MATLAB `checkcode` / static parse;
2. Python syntax/static import check without model execution;
3. direct-load and hash exact MAT `b/ah/z/v0/l0`;
4. exact scalar-binding equality against the predecessor-certified scalar manifest;
5. scientific source-expression identity against the predecessor-certified evaluator;
6. synthetic shape-only residual test using non-scientific dummy objects with the same dimensions (`800 x 800`, `(20,20,2)`, length `800`) proving the vectorization/persistence plumbing is dimensionally valid;
7. persistence-order dry-run proving the core stage container can be serialized before optional residual/conditioning diagnostics;
8. zero-science ledger proving no HJB scientific evaluation occurred.

The synthetic shape test must not use call-725 scientific `M`, RHS, `V1`, policies, derivatives, or model outputs.

Required certification marker:

`MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_AND_PERSISTENCE_REPAIRED__ZERO_SCIENCE_CERTIFIED__FRESH_REPLACEMENT_FIRST_ITERATION_BUDGET_OPEN`

If this exact marker is not achieved, STOP with no scientific call.

## 8. Fresh replacement scientific budget

Only after Phase 0 certification passes, authorize this new independent budget:

- MATLAB exact-MAT/common-scalar first iteration: `1`;
- Python exact-MAT/common-scalar first iteration: `1`, only after durable MATLAB core-stage persistence;
- Python native-initialization amplification probe: `1`, conditional under Section 11;
- retries: `0`.

The predecessor's MATLAB invocation is historical and remains consumed. This successor's MATLAB call is a separately authorized replacement run because no durable predecessor stage artifact exists.

Forbidden counts remain zero:

- iteration 2+;
- full HJB100/HJB500 rerun;
- KFE;
- household steady state;
- GE/stationary/annual 2018;
- R/PLM;
- shock/IRF;
- Results.

## 9. Persistence-first requirement for the replacement MATLAB call

During the authorized MATLAB iteration-1 call:

1. execute exactly the same scientific first-iteration arithmetic as the predecessor-certified wrapper;
2. after direct solve, persist the complete core stage artifact including `M`, RHS, and tensor-form `V1/updated` before optional residual/backward-error/conditioning instrumentation;
3. read back and hash that core artifact;
4. only then compute and persist residual/backward-error diagnostics using the certified vector representation.

If optional post-solve instrumentation unexpectedly fails after the core artifact has been durably persisted, do not rerun MATLAB. Preserve the core artifact and report the instrumentation failure. Python strict-common execution may proceed only if the durable MATLAB core artifact contains all stage objects required for causal comparison and passes integrity readback.

If the core artifact itself is not durably persisted, STOP; no Python scientific call is authorized.

## 10. Stagewise first-iteration comparison contract

Persist and compare in causal order exactly:

1. `old=V0` identity;
2. raw `vb_f/vb_b/va_f/va_b`;
3. boundary-adjusted derivatives;
4. categorical liquid/transfer labels and source-identical branch masks if exposed;
5. consumption/labor/transfer/adjustment cost/effective illiquid return;
6. `mu_a`, `mu_b`, utility;
7. `bb/bf/ab/af`;
8. sparse `BB`;
9. sparse `AAH`;
10. `Bswitch`;
11. full `A`;
12. `M=(1/Delta+rho)I-A`;
13. RHS;
14. direct-solve tensor-form `V1`;
15. first-iteration statistic.

Frozen comparison rules:

- categorical/masks: exact equality;
- shape/order: exact equality;
- continuous same-input values: `128*eps64*max(1,abs(x),abs(y))`;
- sparse mathematical support: ignore only exact stored `0.0/-0.0`;
- sparse values: same machine rule;
- no epsilon pruning;
- no post-observation tolerance broadening.

Classification is controlled by the earliest material scientific stage.

## 11. Direct-solve and conditional native-init rules

If `M` and RHS pass parity but tensor-form `V1` materially differs, classify the case as:

`DIRECT_SOLVE_PROPAGATION_OR_CONDITIONING`

not as an equation/formula mismatch.

Record for MATLAB and Python:

- `||M*vec_F(V1)-rhs||_inf`;
- backward-error certificate/scale;
- cross-language tensor-form `V1` difference;
- read-only conditioning diagnostics if available without changing solver semantics.

No regularization, pseudoinverse, alternate solver, intentional reordering, or retry is authorized.

Only if the strict exact-MAT pair reaches `M/RHS/V1` without an earlier material source-formula mismatch may one Python native-init amplification probe run.

Reuse the already persisted digest-bound native Python initialization arrays from the accepted initialization-parity evidence root. Do not regenerate them.

Compare native-init versus exact-MAT-common-init Python stage objects and quantify first-step amplification only. Do not infer multi-iteration convergence causality from one step.

## 12. Required scientific classification

Choose the strongest supported classification:

1. `CALL725_EXACT_MAT_COMMON_INPUT_FIRST_ITERATION_PARITY_PASS__NATIVE_MACHINE_PERTURBATION_AMPLIFICATION_DIAGNOSTIC_RECORDED__MULTI_ITERATION_PROPAGATION_FORENSIC_REQUIRED`

2. `CALL725_FIRST_ITERATION_PRE_SOLVE_SOURCE_PARITY_BREAK_CONFIRMED__EARLIEST_STAGE_IDENTIFIED__NO_PRODUCTION_CHANGE`

3. `CALL725_FIRST_ITERATION_LINEAR_SYSTEM_PARITY_PASS__DIRECT_SOLVE_PROPAGATION_DIVERGENCE_CONFIRMED__NO_SOLVER_CHANGE_AUTHORIZED`

4. `CALL725_FIRST_ITERATION_STAGEWISE_COMPARISON_COMPLETE__POST_SOLVE_INSTRUMENTATION_PARTIAL__CORE_ARTIFACT_DURABLE__NO_PRODUCTION_CHANGE`

5. `CALL725_FIRST_ITERATION_FORENSIC_BLOCKED__POST_CALL_INSTRUMENTATION_OR_PERSISTENCE_CERTIFICATION_FAILED__NO_PRODUCTION_CHANGE`.

No classification authorizes production changes or any downstream KFE/GE/annual/shock/IRF/Results execution.

## 13. Evidence root

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-postcall-residual-vectorization-repair-20260904-001`

Persist at minimum:

- live authority/source identities;
- predecessor report/task identities;
- predecessor wrapper/failure audit;
- vectorization/indexing proof;
- fresh repaired MATLAB/Python wrapper sources;
- exact wrapper diffs with line classification;
- static/checkcode receipts;
- exact MAT/scalar certification receipts;
- synthetic shape-only residual test evidence;
- persistence-order dry-run evidence;
- zero-science ledger;
- MATLAB core stage artifact and readback/hash if run;
- MATLAB residual/backward-error evidence if available;
- Python strict-common stage artifact if run;
- stagewise comparator JSON/CSV;
- sparse support/value comparison;
- conditional native-probe evidence if run;
- scientific call ledger;
- stdout/stderr/warnings;
- classification;
- `audit_manifest.json`.

Hash and read back every current evidence artifact.

## 14. Repository publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_REPORT.md`

If completed consistently, only a report-only commit + non-force push is authorized.

Suggested commit:

`Repair MP4C call-725 post-solve residual instrumentation and rerun first iteration`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Completed terminal:

`MP4C_2018_CALL725_POST_CALL_INSTRUMENTATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_COMPLETE__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_POST_CALL_INSTRUMENTATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_BLOCKED__NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

Stop after publication and return to ChatGPT L3 / Owner. Do not create the next task or proceed to production changes, KFE, 2018 GE, annual runs, shock, IRF, or Results.
