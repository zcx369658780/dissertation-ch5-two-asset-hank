# CH5_TWO_ASSET_HANK_POST_P5_D2_LOWER_A_ACTIVE_ROOT_CERTIFICATION_DIAGNOSIS_AND_RESUMPTION

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / executor

Owner: final scientific authority

## 1. Purpose

Diagnose and, only if the evidence supports a purely numerical correction, repair the frozen external MATLAB D2 root-certification path that failed on `lower_a_active`, then resume the already accepted post-P5 household-decision parity sequence.

Latest accepted report:

`docs/CH5_TWO_ASSET_HANK_POST_P5_D2_COMPARATOR_CATEGORICAL_TERMINAL_SEMANTICS_AND_RESUMPTION_REPORT.md`

Accepted latest terminal classification:

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_BLOCKED_SOURCE_OR_ENVIRONMENT__DYNAMIC_HOLD_CONTINUES`

The latest report establishes:

- D1 remains accepted at `432/432 PASS`, including `216/216` low-`a` PASS, all scalar maximum differences `0`, and all sign/direction mismatches `0`;
- the D2 9-normal/1-near-tie heterogeneous serialization contract is accepted;
- the MATLAB heterogeneous-result preflight is accepted;
- the corrected comparator, including near-tie `gap/bound` and terminal categorical mismatch semantics, passed its one-shot engineering preflight;
- the single replacement D2 MATLAB call then reached `lower_a_active` and failed in `bracket_root -> cert_root` because the post-`fzero` residual did not satisfy the frozen certification `abs(f(x)) <= 1e-12`;
- `d2_matlab.json` was not persisted and no D2 Python/comparison or D3 scientific call followed.

This task must not assume that MATLAB `fzero` is defective. It must first determine whether the observed failure is a root-solver/configuration/representation issue or a genuine inability to certify the frozen root under the accepted equation and bracket.

P5 remains Owner-accepted:

`MATLAB_PYTHON_TWO_ASSET_HA_PARITY_ACCEPTED_FOR_DYNAMIC_EXTENSION`

The voluntary route hold remains:

`DYNAMIC_EXECUTION_HELD_PENDING_POST_P5_HOUSEHOLD_DECISION_PARITY`

## 2. Live authority and continuity

Task-authoring parent observed before publication:

`b51e860083f645ad287ae745207858927dfdad63`

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task and all controlling predecessor reports/tasks exist on live `main`;
3. record live start SHA;
4. verify the P5 marker and active dynamic hold;
5. verify accepted Python production `src/tests` continuity from baseline `7a2388a2ba89073e307f05a909570e8c40a4be13` with:

`git diff --name-only 7a2388a2ba89073e307f05a909570e8c40a4be13 -- src tests`

It must be empty.

## 3. Accepted evidence to reuse without rerun

### D1

Re-verify hashes only; do not execute D1.

- MATLAB `1FCA5F613C52B2E0F5CC45EF8B06BB96ED171ADB0F79F31A709151F2E91775CF`
- Python `1C3A63759027AC8FF0F2FD56AB090CB8584110FCD9ACAAFDF937624245324F42`
- comparison `1B95E6D23A1409874DE2548812FD1C34E11E955A68093C0A2818B54A521712B3`

D1 call budget: exactly `0/0/0`.

### D2 serialization and comparator

Re-verify and reuse without rerunning their accepted engineering preflights:

- heterogeneous D2 MATLAB harness `A7034181F3FC902E39EAB64CB8ED47C77BA52087B4E262325CAD33BAAECE3589`;
- frozen D2 Python `DD9DDCB675BE7A5C3A672CF1936AB7CBA1553AC8334989B2CDAA777FB2914344`;
- corrected comparator `FAF1A6ABB9F21E7DABD6CFB0857A72354CC3A5D0D532F4968F027E5CBBC0ECB5`;
- frozen D3 MATLAB `5D94EDF89259D543758E490EA25C9359C1B65588CF7BB0557EEEDFA6B7FF3F9A`;
- frozen D3 Python `6054A0C59E7C0CD0C83AD1919C3CFAF7B2987D0545EE4CEB26B3345FF2D01ABC`;
- scientific manifest `D46DD0968DA65C592863A9D33FD5FB58059E707432816A67A74CE4DF7AB1BEDA`;
- accepted MATLAB heterogeneous preflight result `815C6703FECE0438B0584C3AD16A1A772D3A42D2AA57C7F2A4FBA0BF6F6808A4`;
- accepted corrected-comparator preflight result `52F55586BAFA456BC811E4CAD885F7C26DD30FF9F15165C405515C6CEAB1D0F9`.

Do not rerun either accepted engineering preflight unless a hash/source contradiction is found; if such a contradiction is found, stop and report it.

## 4. Frozen scientific root authority

For `lower_a_active`, preserve exactly:

- frozen case ID and all case inputs from the accepted manifest;
- accepted economic equations;
- accepted O1 `max(a,a_bar)` adjustment/transfer authority;
- constant-`r_a` illiquid drift;
- the exact scalar root target function currently passed to `bracket_root/cert_root`;
- the exact bracket construction and the final bracket endpoints supplied to `cert_root`;
- root direction/sign convention;
- root certification tolerance `1e-12`;
- all D2 output definitions and comparison tolerances.

Do not widen `1e-12`.
Do not alter the case, equation, target, bracket, state/shadow values, parameters, or expected policy classification merely to obtain a certified root.

## 5. Phase A — static root-path audit

Before any execution, inspect the frozen external D2 MATLAB evaluator and accepted Python root path read-only.

For MATLAB report exactly:

1. the frozen `lower_a_active` inputs;
2. the exact call chain reaching `bracket_root -> cert_root`;
3. the scalar mathematical function `f(x)` being solved, with the meaning of `x`;
4. how the initial/final bracket is constructed;
5. the exact `fzero` call signature and options, including whether MATLAB defaults are used;
6. the post-`fzero` residual certification code;
7. whether any rounding/casting/JSON value conversion occurs before the root call;
8. all other frozen D2 call sites of `cert_root`, without executing them.

For Python report statically:

- the corresponding accepted zero-drift/root target and certification semantics;
- the production/case evaluator's root algorithm and tolerances where relevant;
- whether Python certifies the residual independently after root solve.

Do not infer numerical values that require execution in Phase A.

## 6. Phase B — one bounded scalar root diagnostic

Create a fresh no-overwrite external artifact root.

Before execution freeze/hash/read back:

- one MATLAB scalar diagnostic harness;
- the unchanged scientific manifest;
- the unchanged heterogeneous D2 MATLAB harness;
- the unchanged frozen D2 Python/comparator/D3 harnesses;
- a diagnostic execution ledger.

Authorize exactly **one MATLAB scalar root-diagnostic invocation**. This invocation is bounded scientific/numerical diagnostic evidence; it is not a full D2 call and does not consume the replacement D2 budget.

It must evaluate only the frozen `lower_a_active` root target and must not run the full 10-case D2 loop.

The diagnostic must:

1. reconstruct the exact frozen case and exact original bracket path;
2. record final bracket endpoints `lo, hi`, `f(lo)`, `f(hi)`, sign relationship, and finiteness;
3. invoke the frozen existing `fzero` call exactly once and record candidate `x_fzero`, `f(x_fzero)`, absolute residual, exit flag and available solver diagnostics;
4. independently apply a pre-declared deterministic bracket-preserving reference refinement using the **same `f`, same `[lo,hi]`, same double precision, and same `1e-12` residual certification**;
5. the reference refinement must use ordinary bisection only: at each step take the representable midpoint, preserve the sign-changing bracket, and stop only when `abs(f(mid)) <= 1e-12` or no strictly interior representable midpoint remains; maximum 256 iterations;
6. record the reference candidate, residual, iterations, final bracket width, and whether certification succeeded;
7. make no scientific parameter/case/tolerance change.

The bisection algorithm and 256-iteration cap must be written and frozen before observing the diagnostic result. Do not tune them afterward.

## 7. Diagnostic classification

After the single scalar diagnostic, classify exactly one:

### A. Numerical solver/certification defect

`LOWER_A_ACTIVE_ROOT_CERTIFICATION_SOLVER_REPRESENTATION_DEFECT`

Use only if:

- the frozen bracket is valid and finite;
- the original `fzero` candidate fails `1e-12` residual certification;
- the independent frozen bisection refinement on the identical function/bracket certifies a root with residual `<=1e-12`.

This supports a bounded external `cert_root` numerical-refinement correction.

### B. Genuine uncertified frozen root

`LOWER_A_ACTIVE_ROOT_CERTIFICATION_GENUINE_UNCERTIFIED_ROOT`

Use if the unchanged equation/bracket cannot produce a residual-certified root under the frozen diagnostic, including invalid/no sign-changing bracket or reference refinement exhausting representable precision without meeting `1e-12`.

This is a potential scientific/root-contract contradiction. Do not modify the root algorithm or run D2 further in this task.

### C. Non-reproducible context blocker

`LOWER_A_ACTIVE_ROOT_CERTIFICATION_NONREPRODUCIBLE_CONTEXT_BLOCKER`

Use if the exact frozen `fzero` call unexpectedly certifies the residual in the isolated diagnostic, so the prior failure cannot be explained by the frozen scalar solver path alone.

Do not guess a correction; stop and report exact context differences that remain unresolved.

## 8. Phase C — conditional minimal `cert_root` correction

Enter this phase **only** under classification A.

Create a corrected external D2 MATLAB harness starting from the accepted heterogeneous harness.

The only permitted scientific-numerical change is inside `cert_root` and must preserve the same root equation, original bracket and `1e-12` certification.

Frozen correction contract:

1. call the existing `fzero` path exactly as before;
2. if its residual is finite and `<=1e-12`, return it unchanged;
3. if its residual exceeds `1e-12`, run the same pre-declared bracket-preserving bisection refinement used in Phase B on the original frozen bracket;
4. return only a candidate whose residual is `<=1e-12`;
5. if bisection cannot certify, raise/fail exactly rather than relaxing tolerance.

Every changed line must be classified:

`ROOT_CERTIFICATION_NUMERICAL_REFINEMENT_ONLY`

Forbidden changes include the root function, bracket-generation logic, economic equations, case values, parameters, state constraints, KKT logic, multipliers, output fields, comparator, or tolerances.

## 9. Phase D — one corrected lower-a root preflight

Before a replacement D2 call, freeze/hash/read back:

- corrected D2 MATLAB harness;
- complete harness diff;
- one lower-a root preflight harness;
- unchanged accepted D2 Python/comparator/D3 artifacts;
- successor execution ledger.

Run exactly **one MATLAB corrected-root preflight invocation**, limited to the frozen `lower_a_active` scalar root path.

It must prove:

- same root target and same original bracket as Phase B;
- corrected `cert_root` returns finite candidate;
- absolute residual `<=1e-12`;
- no bracket/tolerance/equation/case change;
- if fallback bisection is used, report that explicitly.

If this preflight fails, do not repair or rerun it in this task.

## 10. Phase E — resume D2 and D3 only after correction evidence passes

Only if classification A and Phase D PASS:

Authorize exactly **one new replacement D2 MATLAB scientific call**.

Historical D2 MATLAB calls must remain separately recorded:

1. input-container blocker: `1`;
2. zero-field output-container blocker: `1`;
3. schema/comparator authority tasks: `0` scientific calls;
4. comparator-terminal task root-certification blocker: `1`;
5. current task replacement D2 MATLAB: at most `1`.

Replacement D2 requirements remain unchanged:

- exit zero;
- `d2_matlab.json` exists;
- exactly 10 rows in frozen order;
- 9 native 16-field normal rows plus one native 10-field near-tie row;
- no fabricated fields;
- independent read-back succeeds.

If the replacement D2 MATLAB call fails at this or another point, do not repair or rerun it in this task.

After valid MATLAB persistence, run exactly once each:

- frozen D2 Python evaluator;
- accepted corrected D2 comparator.

Any numerical or categorical mismatch is terminal.

If D2 passes, execute unchanged D3 exactly once each:

- MATLAB `1`;
- Python `1`;
- comparator `1`.

Use the exact frozen 360 `gamma_c=2`, Python `phi=5`, MATLAB `frisch_l=.2` cases.

## 11. Call budget

Reuse-only:

- D1 MATLAB/Python/comparison: `0/0/0`;
- accepted MATLAB heterogeneous preflight: `0` new calls;
- accepted comparator preflight: `0` new calls.

New bounded diagnostics:

- Phase B scalar MATLAB root diagnostic: at most `1`;
- Phase D corrected-root MATLAB preflight: at most `1`, classification A only.

Scientific execution after PASS evidence:

- replacement D2 MATLAB: at most `1`;
- D2 Python/comparison: at most `1/1`;
- D3 MATLAB/Python/comparison: at most `1/1/1`.

No full `HANK_2ASSETS_HJB`, Python HJB/KFE/steady state, P3/P4/R4, asset-tail, AR(1), transition, IRF, calibration extension, dynamics, or Results execution is authorized.

## 12. Terminal classifications

Return exactly one final terminal classification.

### Full supplementary parity PASS

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_PASS__DYNAMIC_HOLD_MAY_ADVANCE_TO_ASSET_TAIL_GATE`

Use only if classification A is supported, the bounded correction/preflight pass, D2 all ten cases pass, and D3 all 360 cases pass.

### Material contradiction

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_MATERIAL_CONTRADICTION__DYNAMIC_HOLD_CONTINUES`

Use if classification B is supported or a valid D2/D3 scientific comparison shows a material numerical/categorical mismatch under the frozen accepted equations.

P5 is not automatically revoked.

### Source/environment blocked

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_BLOCKED_SOURCE_OR_ENVIRONMENT__DYNAMIC_HOLD_CONTINUES`

Use if classification C occurs, the correction cannot remain within numerical-refinement authority, a correction preflight fails, or a later D2/D3 source/environment blocker occurs before a valid comparison.

## 13. Required report

Write only:

`docs/CH5_TWO_ASSET_HANK_POST_P5_D2_LOWER_A_ACTIVE_ROOT_CERTIFICATION_DIAGNOSIS_AND_RESUMPTION_REPORT.md`

Report at minimum:

1. terminal classification;
2. live start/final `origin/main`;
3. Python `src/tests` continuity;
4. predecessor/successor artifact roots;
5. D1 artifact re-verification and zero-call confirmation;
6. accepted heterogeneous/comparator preflight reuse and hashes;
7. exact static `lower_a_active` root equation/call chain/bracket logic/certification logic;
8. exact Python corresponding root semantics;
9. scalar diagnostic harness/hash/call count;
10. `lo,hi,f(lo),f(hi)`, frozen `fzero` candidate/residual/exit diagnostics;
11. independent bisection candidate/residual/iterations/final bracket width;
12. diagnostic classification A/B/C and justification;
13. if A, complete corrected `cert_root` diff and per-line classification;
14. corrected-root preflight hash/result;
15. full historical/current D2 call ledger;
16. D2 MATLAB/Python/comparison counts and results if reached;
17. D2 normal-case maxima/worst cases, near-tie comparison, KKT/boundary/categorical mismatch counts if reached;
18. D3 360-case statistics if reached;
19. scientific mismatch list;
20. source/environment failure list;
21. forbidden-operation check;
22. git status;
23. acceptance level;
24. exact recommended next gate.

## 14. Explicit prohibitions

Do not:

- rerun D1;
- rerun accepted heterogeneous/comparator preflights;
- modify production MATLAB/Python source/tests/helpers/cache;
- modify frozen D2 Python or accepted comparator;
- change any D2/D3 case, order, parameter, equation, root target, original bracket construction, state/shadow/derivative input, KKT condition, multiplier logic, output field, or tolerance;
- widen `1e-12` root certification;
- tune the bisection cap/logic after observing results;
- add the MATLAB `raah/Rah` taper;
- use production bare-`a` FOC as corrected oracle;
- add `Tt/rb_gap` adapters;
- hard-code expected Python answers;
- repair/rerun a failed scalar diagnostic, corrected-root preflight, or scientific stage in the same task;
- run full HJB/KFE/steady state;
- rerun P3/P4/R4;
- run asset-tail diagnostics;
- enter AR(1), transition, IRF, dynamics, calibration extension or Results;
- revoke or reissue P5 automatically.

## 15. Recommended next gate rule

If the full supplementary parity passes, recommend only the already-identified upper-`a` asset-tail robustness gate before actual dynamics execution.

If classification B or a scientific mismatch occurs, recommend the smallest Owner/reviewer diagnosis of the exact root/policy contradiction.

If blocked, recommend only the smallest unresolved source/environment correction gate.
