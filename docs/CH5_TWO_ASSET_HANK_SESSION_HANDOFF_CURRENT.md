# Chapter 5 当前交接 — interior-a zero-drift switching Owner-adopted / bounded implementation and checkpoint-2 reexecution active

更新：2026-09-19。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
状态：`INTERIOR_A_ZERO_DRIFT_SWITCHING_OWNER_ADOPTED__IMPLEMENTATION_AND_V2_CHECKPOINT2_REEXECUTION_ACTIVE__PRODUCTION_UNCHANGED`。
Results eligibility=`FALSE`。

## Governance

Owner is final scientific authority; ChatGPT is L3 Reviewer/scientific-route authority; Codex is bounded Builder. GitHub live `main` is repository-state authority. `deep-learning-hank` is separate and must never be used for this route.

## Owner-adopted new law

Authority:
`docs/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_OWNER_ADOPTION_20260919.md`.

At an interior `a` node, when ordinary one-sided candidates under the same existing liquid branch/active set and transfer regime exhibit a strict backward-positive / forward-negative `g_a` crossing, the corrected-diagnostic route now authorizes an endogenous zero-`a)-drift candidate:

- `g_a=0`;
- `d_Z=-r_a a`;
- unchanged D3 KKT/subgradient determines `q_a`;
- `q_a` must lie in the closed sorted interval between `p_a^F` and `p_a^B`;
- liquid-face multiplier/domain law remains unchanged;
- active liquid faces require a unique legal `g_b=0` root inside the exact intersection interval;
- candidate controls/KKT/Hamiltonian are reconstructed, never interpolated;
- D2 remains unchanged and consumes canonical zero drift.

The adoption does not authorize simultaneous newly-created `a)-switching plus liquid-`Z` switching.

## Active task

`tasks/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_IMPLEMENTATION_AND_V2_CHECKPOINT2_REEXECUTION_20260919.md`.

Builder must first implement and pass focused tests, including exact cell100 regression, then execute at most one fresh accepted-V2 policy map.

If the map fails, stop at the first new scientific failure.

If it completes, assemble at most one Q2 and compute checkpoint-2 B2/D2/policy/operator/cycle diagnostics. Stop before V2->V3 and before terminal KFE/topology.

## Reviewer action after Builder return

Independently verify:

- exact code diff and no source-faithful/production change;
- literal conformance to the Owner-adopted trigger, interval and root semantics;
- no simultaneous unadopted two-axis switching;
- focused test evidence and non-regression;
- exact cell100 switching receipt;
- scientific ledger and one-map/no-retry budget;
- first failure if any;
- if complete, P2/u2/Q2 and B2/D2/stability/cycle evidence.

Any different scientific failure is the next object; it must not be repaired inside the same Builder task.

Production, GE and Results remain closed.
