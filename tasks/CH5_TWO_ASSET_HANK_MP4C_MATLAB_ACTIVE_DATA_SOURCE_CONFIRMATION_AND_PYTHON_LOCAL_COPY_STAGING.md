# CH5_TWO_ASSET_HANK_MP4C_MATLAB_ACTIVE_DATA_SOURCE_CONFIRMATION_AND_PYTHON_LOCAL_COPY_STAGING

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor: Codex bounded Builder / local-data provenance stager

Owner: final scientific authority

## 1. Authority basis

Accepted predecessor execution:

`MP4C_MULTIYEAR_ANNUAL_SCOPE_FREEZE_AND_BATCH_PARITY_PREPARATION_PASS`

Execution commit:

`59c0611a265854597f47713ad9ee981e7e569ecd`

Accepted annual scope:

- 2009–2023 inclusive, 15 calendar years;
- corrected-2009 is the formally accepted stationary parity anchor and must not be rerun;
- future Python annual execution years are 2010–2023, 14 independent years;
- scientific concurrency is 1;
- future per-year ceilings are 250 outer turns, 7,750 household calls, 14,400 seconds, reruns 0.

The Owner additionally instructs that existing filled-NA source data under the MATLAB project should be copied for Python use, but only after confirming the exact filenames actually invoked by the protected MATLAB source chain.

This task is the required zero-model staging gate before any 2010–2023 batch execution.

## 2. Required live continuity

Required execution-start predecessor:

`59c0611a265854597f47713ad9ee981e7e569ecd`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of the predecessor;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - the accepted MP4C task/report;
   - the formal corrected-2009 L3 acceptance report;
   - the MP4A2 annual binding task/report;
   - protected MATLAB source files that actually load annual, regression, and distance data.

Any authority, identity, or protected-source readability failure => stop before copying.

## 3. Hard zero-model budget

All scientific/model executions are exactly zero:

- MATLAB process/checkcode/stationary/HJB/KFE/household/firm/controller: `0`;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific execution: `0`;
- comparator execution: `0`;
- annual batch execution: `0`;
- shocks/AR(1)/transition/dynamics/IRF: `0`;
- R5/Results: `0`.

Allowed operations are limited to source inspection, local file existence checks, hashing, byte-for-byte file copies, copy-hash verification, narrow local-data ignore setup if needed, and report/manifest authoring.

Do not open and re-save any workbook in Excel, LibreOffice, Python spreadsheet libraries, or MATLAB. This task performs byte copies only.

## 4. Protected MATLAB root and path identity

Use the already accepted protected logical/physical root pair:

- logical: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- physical: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`.

Require the accepted exact Junction/root relationship and do not broaden trust to arbitrary `D:\MatlabProgram` content.

Before any copy, identify the exact protected MATLAB source locations and hashes that determine each active data filename.

At minimum inspect the source chain containing annual-data loading and the relevant regression/distance loaders, including `load_GDPdata.m` and any source-backed caller/loader required to identify the actual active filenames.

## 5. Filename confirmation — source first, copy second

Do **not** treat historical reports or chat text as sufficient copy authority.

For every source asset that Python annual execution will require, record:

- protected MATLAB source file and line/expression where the filename/path is constructed;
- exact filename string as consumed by MATLAB;
- exact resolved source path under the protected MATLAB project;
- whether the file exists exactly once at the resolved expected location;
- file size;
- SHA-256.

Historical candidate names that must be verified rather than assumed include:

- `2000年后各省数据_填充NA.xlsx` — candidate filled annual workbook;
- `R语言估计结果_plm估计.xlsx` — candidate regression workbook;
- `中国各省省会地理距离矩阵.xlsx` — candidate distance workbook.

If the live protected source invokes a different filename, the protected source wins. Report the discrepancy and use the source-backed filename only if all other provenance checks pass.

Also explicitly identify the inactive/unfilled annual workbook if present and prove it is **not** the active scientific source before excluding it from the Python copy set.

Do not copy the derived MAT cache `数据估计结果_1000_100_0.mat` into the scientific primary-source set. It remains a derived/runtime representation unless a later task explicitly authorizes a separate validation copy.

## 6. Python local data target

The scientific Python repository root is the current live repository worktree.

Preferred local runtime-data target inside that worktree:

`data_local/matlab_primary_source_snapshot/`

The copied binary source files must remain local/untracked and must never be committed to Git by this task.

Before copying:

1. inspect whether `data_local/` is already ignored by Git;
2. if not ignored, this task authorizes one narrow `.gitignore` addition exactly for:

   `data_local/`

3. do not add broad wildcard ignores for `.xlsx`, `.mat`, or other data types;
4. verify with `git check-ignore` or equivalent that the exact target files will be ignored before writing them.

If repository/local safety rules prohibit the preferred target, stop and report the exact blocker rather than silently selecting another persistent data location.

## 7. Exact byte-copy contract

Only after Sections 2–6 pass:

1. create the target directory if absent;
2. copy each confirmed primary source file byte-for-byte from the protected MATLAB location to the Python local target;
3. do not rename files — preserve exact MATLAB-consumed filenames;
4. do not transform workbook contents, metadata, compression, sheets, formulas, or timestamps intentionally;
5. hash source before copy and target after copy;
6. require source SHA-256 == target SHA-256 for every copied file;
7. require source byte size == target byte size;
8. do not overwrite an existing target file unless it already has the exact same SHA-256; if an existing target differs, stop before mutation and report the conflict;
9. record copy provenance in an external manifest.

Required scientific primary-source copy set should include exactly the source-backed annual/regression/distance assets needed by the accepted annual-input loader. Do not copy unrelated MATLAB outputs, caches, code, logs, or result files.

## 8. Python annual-input compatibility check — zero science

Without running any household/stationary model, verify that the current annual-input preparation API can be pointed explicitly at the copied local files.

This is a path/interface check only. It may hash/read workbook metadata in the same non-model manner already accepted in MP4A2/MP4C, but must not execute a stationary solve.

For the copied files, verify at minimum:

- the accepted 2009–2023 calendar coverage remains visible;
- province order and distance axes remain consistent with the accepted 31-province order;
- required regression vintage sheets 10–24 are available;
- the copied files produce the same SHA identities as the protected MATLAB originals.

Do not regenerate or replace the accepted corrected-2009 canonical artifact.

## 9. Required external evidence

Create a fresh no-overwrite root, preferred:

`D:\ProjectTemp\ch5-mp4c-matlab-data-source-confirmation-python-copy-20260901-001`

or next deterministic suffix if already present.

Persist:

- `matlab_active_data_source_map.json`;
- `source_file_identity_manifest.json`;
- `python_local_copy_manifest.json`;
- `source_vs_copy_hash_matrix.json`;
- `annual_input_local_path_compatibility.json`;
- `zero_model_execution_ledger.json`;
- `staging_manifest.json`.

Each artifact must record byte size and SHA-256.

The copy manifest must include absolute source path, absolute Python-local target path, exact filename, source SHA, target SHA, byte size, source code provenance, and copy disposition.

## 10. Required repository mutations

Allowed repository mutations are only:

- `.gitignore` — only if the exact `data_local/` entry is not already present/covered;
- required report below.

The copied binary data remain ignored/untracked local files and must not appear in `git diff`, staged paths, or GitHub.

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4C_MATLAB_ACTIVE_DATA_SOURCE_CONFIRMATION_AND_PYTHON_LOCAL_COPY_STAGING_REPORT.md`

## 11. Acceptance markers and terminal

Required markers on PASS:

- `MP4C_MATLAB_ACTIVE_DATA_FILENAMES_SOURCE_CONFIRMED`;
- `MP4C_PYTHON_LOCAL_PRIMARY_SOURCE_COPY_HASH_IDENTICAL`;
- `MP4C_FILLED_NA_ANNUAL_SOURCE_CONFIRMED_ACTIVE`;
- `MP4C_DERIVED_MAT_CACHE_EXCLUDED_FROM_PRIMARY_COPY_SET`;
- `MP4C_LOCAL_DATA_COPY_READY_FOR_2010_2023_BATCH`.

PASS terminal:

`MP4C_MATLAB_ACTIVE_DATA_SOURCE_CONFIRMATION_AND_PYTHON_LOCAL_COPY_STAGING_PASS`

BLOCKED terminal:

`MP4C_MATLAB_ACTIVE_DATA_SOURCE_CONFIRMATION_AND_PYTHON_LOCAL_COPY_STAGING_BLOCKED`

## 12. Closeout

Before commit:

- verify copied data files are ignored/untracked and not staged;
- stage only the report and, if needed, the narrow `.gitignore` change;
- `git diff --check --cached` PASS;
- zero-model ledger remains all zeros.

Close out with:

- exactly one execution commit;
- exactly one non-force push;
- fresh GitHub read-back of every changed repository path;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean tracked worktree;
- report the local ignored data-copy directory separately;
- exactly one recommended next gate: bounded 2010–2023 Python annual stationary batch execution using the verified local primary-source copies and the frozen MP4C compute budget.

Do not start any annual stationary execution inside this task.
