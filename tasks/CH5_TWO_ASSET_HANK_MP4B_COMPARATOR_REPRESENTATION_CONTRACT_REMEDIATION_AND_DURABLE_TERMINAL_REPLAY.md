# CH5_TWO_ASSET_HANK_MP4B_COMPARATOR_REPRESENTATION_CONTRACT_REMEDIATION_AND_DURABLE_TERMINAL_REPLAY

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / comparator representation-contract remediator / durable-terminal replay executor

Owner: final scientific authority

## 1. Purpose

Repair only the already diagnosed province-name representation defect in the read-only preserved-MATLAB/Python final-state comparator, then execute exactly one comparator-only replay against the already durable 2009 MATLAB and Python terminal artifacts.

This task does **not** authorize any stationary rerun or any scientific/model solve. It does not authorize household/HJB/KFE/aggregation replay, MATLAB execution, another year, shocks, transition, IRF, R5, calibration, or Results.

Accepted predecessor terminal:

`MP4B_FINAL_STATE_PROVINCE_ORDER_REPRESENTATION_AND_ORDER_INVARIANT_TERMINAL_DIAGNOSTIC_PASS`

Accepted primary classification:

`VALIDATION_COMPARATOR_MATLAB_STORAGE_ORDER_OR_STRING_REPRESENTATION_ERROR`

Accepted material marker which must be preserved:

`MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`

The representation defect is exactly this: canonical/Python province names are `北京、天津、河北,...`; preserved MATLAB `prvname` uses the same 31 positions and logical order but 25 names add exactly one terminal administrative suffix `省` or `市` (e.g. `北京` vs `北京市`, `河北` vs `河北省`). There is no province-order permutation.

## 2. Required predecessor and live continuity

Required execution-start predecessor:

`00c60ebef99bc7143a7e8d41dc163df1e1f7b44c`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `00c60ebef99bc7143a7e8d41dc163df1e1f7b44c`;
3. require clean worktree;
4. read `AGENTS.md`, `project_rules/PROJECT_RULE_INDEX_CURRENT.md`, `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`, and `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
5. verify every immutable identity below;
6. verify all durable local artifacts below;
7. verify no active historical R5 / `chapter5_model` runtime dependency.

Any identity failure => BLOCKED before mutation/replay.

## 3. Immutable scientific/model identities

Require exact SHA-256 and do not modify:

- `src/ch5_two_asset_hank/economics.py`: `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`: `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`: `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `one_turn.py`: `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `steady_state.py`: `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `stationary_runtime.py`: `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map: `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter: `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver: `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- canonical corrected-2009 input: `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- comparator contract: `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map: `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- corrected entry-test Git blob: `89ff42fb99f24ed89ff162e69f2d6c3e01a052eb`.

The comparator contract and field map remain immutable. Only the comparator helper and focused comparator tests are mutable under this task.

## 4. Predecessor comparator-helper identity

Before editing, require:

- path: `validators/multi_province/mp4b_compare_preserved_matlab_python_final_state.py`
- predecessor SHA-256: `A6F7D2BBF7EE0936A6A0A45880B41D7AC77DB5AA7C3CA4B0F207F2D2A2DC08CF`
- predecessor Git blob: `03db092e2d5885364ab9698c2a07c6d5fa17a0cb`.

The helper's current exact-name gate is the diagnosed defect. Do not modify its scientific field list or numerical difference formulas except as explicitly required below.

## 5. Immutable durable artifacts

### 5.1 Preserved MATLAB

Root: `D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require:

- stationary output: `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- profile: `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- terminal JSON: `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`.

### 5.2 Durable Python one-shot terminal

Root: `D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require:

- run manifest: `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- terminal summary: `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`
- turn-184 household output: `70442A793408DCDE20C84F83CA4795FA3EB95865052714F9FE9A31ABFC350442`.

### 5.3 Accepted diagnostic evidence

Require existing no-overwrite JSON:

`D:\ProjectTemp\ch5-mp4b-final-state-province-order-representation-order-invariant-diagnostic-20260831-001.json`

SHA-256:

`D4AF68622ECF3526784BC4C86AE1D1A08604A4D7F06D145A4D36DE013F0B941B`.

No durable scientific artifact may be overwritten.

## 6. Exact representation-contract remediation

Modify only the comparator helper so province identity comparison supports the already proven reversible MATLAB administrative-suffix representation.

### 6.1 Raw evidence must be retained

The helper must retain and expose, for each row:

- raw MATLAB province name;
- raw Python province name;
- canonical comparison key used for identity matching.

Do not overwrite or rewrite the raw names in memory as if MATLAB originally stored the short form.

### 6.2 Allowed projection only

For the **MATLAB raw name only**, define a bounded comparison-key projection:

- if the raw name ends with exactly one terminal `省` or `市`, remove exactly that one final character;
- otherwise leave the raw name unchanged.

No other normalization is allowed:

- no whitespace stripping;
- no Unicode normalization beyond normal Python string decoding already used;
- no translation;
- no fuzzy matching;
- no pinyin;
- no substring matching;
- no autonomous-region suffix logic;
- no municipality dictionary beyond terminal `市`;
- no reordering.

Python names remain exact canonical strings and are not modified.

### 6.3 Fail-closed identity checks

Before numerical comparison, require all of the following:

1. MATLAB and Python lengths both equal 31;
2. raw MATLAB names unique;
3. projected MATLAB keys unique;
4. Python names unique;
5. projected MATLAB key sequence equals Python raw-name sequence exactly position-by-position;
6. the projection changes only the 25 already diagnosed suffix cases and leaves the other six unchanged;
7. no collision is introduced by projection.

Any failure => comparator replay must not run; terminal BLOCKED.

Required remediation marker after focused tests pass:

`MP4B_COMPARATOR_PROVINCE_NAME_REPRESENTATION_CONTRACT_REMEDIATED`

## 7. Scientific field and numerical comparison contract remains unchanged

The field map remains exactly:

`Ct, At, Bt, Lt, Lt_supply, Kt_supply, rah, Kt, Yt, mt, KNratio, w, wjt, rk, ra, GovInv, rb, it, Zt, Govinc`.

Do not add `AtTax`, household-Lt substitution, or `At+Bt` productive-capital substitution.

For every province and every continuous field, preserve existing output formulas:

- MATLAB raw value;
- Python raw value;
- absolute difference;
- relative difference;
- normalized difference with scale `max(1, abs(M), abs(P))`.

No new final tolerance is authorized.

The comparator contract remains frozen:

- calendar identity exact;
- province order exact after the accepted representation projection;
- categorical/boolean/integer source states exact;
- exposed convergence/controller/return/wage-bound categories exact;
- final continuous values: report differences without post-hoc acceptance tolerance.

## 8. Category comparison must be explicit

Extend the comparator output if necessary so it records both MATLAB and Python terminal categories:

- outer-turn count;
- final household converged count;
- `ra_upper_count`;
- `ra_lower_count`;
- `wage_upper_count`;
- `wage_lower_count`.

Also record an exact-equality boolean for each category.

The already accepted evidence must remain visible:

- MATLAB/Python outer turns: `184/184`;
- final household converged: `31/31`;
- `ra_upper_count`: `0/0`;
- `ra_lower_count`: `0/0`;
- wage lower: `17/17`;
- wage upper: `7` MATLAB vs `5` Python.

If durable replay confirms wage upper `7 vs 5`, preserve marker:

`MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`

Do not hide, normalize, reinterpret, or downgrade that mismatch because province-name representation was repaired.

## 9. Focused zero-science tests before replay

Before the single comparator replay, add/update focused tests sufficient to prove:

1. `北京市 -> 北京`;
2. `河北省 -> 河北`;
3. `内蒙古 -> 内蒙古`;
4. only one final `省`/`市` is removed;
5. names with no permitted suffix remain unchanged;
6. a projected-name collision fails closed;
7. a non-suffix mismatch fails closed;
8. preserved MATLAB projected 31-name sequence equals the durable Python terminal 31-name sequence exactly;
9. helper preserves raw MATLAB and Python names in output rows;
10. field list remains exactly the frozen field map;
11. existing excluded aliases remain excluded;
12. category counts are computed from the same durable terminal fields and grids, not inferred from names.

Run at minimum:

- focused comparator/field-map tests;
- `python -m py_compile` for the comparator helper/tests;
- `git diff --check`.

No scientific/model call may occur during tests.

## 10. Comparator-only replay budget

Only after Sections 2–9 pass:

- comparator replay against the durable MATLAB/Python terminals: maximum `1`;
- comparator reruns: `0`.

This is a new, separately authorized comparator replay after representation-contract repair. It is **not** a stationary/model rerun.

Use a fresh no-overwrite output path under `D:\ProjectTemp`, recommended:

`D:\ProjectTemp\ch5-mp4b-final-state-comparator-replay-after-representation-remediation-20260831-001.json`

Require output path absent before replay.

If the replay raises for any reason, do not repair and retry in the same task. Persist/report the failure and stop.

## 11. Required aligned outputs

If replay succeeds, the durable comparator artifact must expose all 31 aligned province rows.

The report must contain a human-readable 31-province table for at least:

- `Ct`;
- final firm `Lt`;
- `Yt`;
- `At`;
- `Bt`;

For each of those five fields, report MATLAB value, Python value, absolute difference, relative difference, and normalized difference for every province.

Also summarize, for every frozen field:

- maximum absolute difference and province;
- maximum relative difference and province;
- maximum normalized difference and province;
- median normalized difference;
- count of provinces with exact equality.

For `Ct`, `Lt`, `Yt`, `At`, `Bt`, additionally identify the top five provinces by normalized difference.

## 12. National values

Recompute within the aligned comparator artifact national sums for frozen fields:

- `Ct`;
- `At`;
- `Bt`;
- `Yt`.

Expected raw values from the accepted diagnostic, to be verified rather than hard-coded:

- MATLAB `Ct=283.3909431582526`, Python `Ct=276.52720698365306`;
- MATLAB `At=47.95553248807161`, Python `At=47.11415467808319`;
- MATLAB `Bt=65.2831672243048`, Python `Bt=65.21538414270965`;
- MATLAB `Yt=350556701.89460325`, Python `Yt=350585612.6035657`.

Report absolute/relative/normalized differences. Do not apply a new acceptance tolerance.

## 13. No-science ledger

Must remain exactly zero:

- Python stationary;
- Python household adapter;
- HJB;
- KFE;
- household aggregation;
- MP2/MP3 scientific execution;
- MATLAB stationary/household/HJB/KFE/presolver;
- Shanxi replay;
- other years / annual batch;
- shocks / AR1 / transition / dynamics / IRF;
- historical R5;
- Results.

Only the comparator-only replay is allowed once.

## 14. Allowed repository changes

Allowed only:

- `validators/multi_province/mp4b_compare_preserved_matlab_python_final_state.py`;
- focused comparator/field-map test file(s);
- one bounded roadmap status update if useful;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_COMPARATOR_REPRESENTATION_CONTRACT_REMEDIATION_AND_DURABLE_TERMINAL_REPLAY_REPORT.md`.

Do not modify any scientific source, validation driver, source-postloop adapter, field map, comparator contract, canonical input, MATLAB source/artifact, project rule, or historical R5 file.

## 15. Terminal classification

### PASS of this gate

Use:

`MP4B_COMPARATOR_REPRESENTATION_CONTRACT_REMEDIATION_AND_DURABLE_TERMINAL_REPLAY_PASS`

only if:

- representation remediation is proven by focused tests;
- exactly one replay succeeds;
- full 31-province aligned artifact is produced;
- raw MATLAB/Python names are retained in evidence;
- category mismatches are reported, not hidden;
- no scientific/model call occurs.

This PASS is a comparator-gate PASS only. It does **not** establish corrected-2009 stationary parity.

### BLOCKED

Use:

`MP4B_COMPARATOR_REPRESENTATION_CONTRACT_REMEDIATION_AND_DURABLE_TERMINAL_REPLAY_BLOCKED`

for identity failure, representation ambiguity, test failure, replay exception, or any need to rerun science.

## 16. Scientific disposition after successful replay

Because `wage_upper_count` is already confirmed `7` MATLAB vs `5` Python under the frozen exact categorical contract, the successful replay is expected to retain at least one material mismatch unless direct durable evidence disproves the prior diagnostic.

Do not establish:

`MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_ACCEPTED`

in this task.

Instead, after the aligned replay, classify the first scientific divergence using the now-visible province/field evidence. The report must distinguish:

- representation/comparator defects now repaired;
- exact categorical mismatches;
- continuous final-state differences;
- currently unsupported causal explanations.

Do not infer that MATLAB is wrong merely because Python differs. Root-cause attribution requires a later source-backed localization gate.

## 17. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. immutable identities;
4. predecessor helper SHA/blob;
5. exact helper diff and new SHA/blob;
6. exact bounded suffix-projection contract;
7. focused test commands/results;
8. py_compile and git diff check;
9. comparator replay call ledger `1/1`, rerun `0` if executed;
10. output path, size, SHA-256, schema;
11. raw MATLAB and Python province-name evidence;
12. projected exact 31-province identity gate result;
13. full 31-province `Ct/Lt/Yt/At/Bt` table;
14. all-field max/median/exact-equality summaries;
15. top-five province diagnostics for `Ct/Lt/Yt/At/Bt`;
16. MATLAB/Python national `Ct/At/Bt/Yt` values and differences;
17. outer-turn/final household/ra/wage category comparison;
18. status of `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`;
19. representation defect list vs material scientific mismatch list;
20. zero-science ledger;
21. changed paths;
22. forbidden-operation audit;
23. commit SHA and non-force push;
24. GitHub read-back of every changed path;
25. final `HEAD == origin/main`, ahead/behind `0/0`, clean worktree;
26. exactly one recommended next gate.

## 18. Next-stage routing

After successful replay, recommend exactly one **read-only final-state first-divergence localization** gate. It should use the aligned 31-province table to identify the earliest source-backed update layer responsible for the confirmed wage-boundary and continuous differences, without rerunning either stationary model.

Do not proceed directly to IRF/shocks until L3 independently reviews the aligned steady-state comparison and decides whether the stationary mismatch is sufficiently localized or whether a bounded source/trace comparison is required first.
