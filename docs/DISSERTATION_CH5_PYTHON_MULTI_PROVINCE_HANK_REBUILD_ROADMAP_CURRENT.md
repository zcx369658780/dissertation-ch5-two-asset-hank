# Chapter 5 Python 多省份两资产 HANK 路线
更新：2026-09-19。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
Results eligibility=`FALSE`。

## Accepted corrected household route

Accepted corrected evidence includes V0/Q0, V0->V1, V1/Q1, Q1 single closed class and pin-free/source-free unique Q1 invariant mass. The nonlinear design uses same-value `(P_n,u_n,Q_n)`, fixed `Delta=1000`, terminal-only KFE timing and the Owner-adopted Bellman/value convergence law.

Checkpoint 1 is not converged:
- `B1=0.014710294187010184`;
- `D1=0.47118375690461445`.

The accepted V1->V2 direct solve is numerically valid, but the existing V2 corrected policy map does not complete.

## Lower-b selector omission — closed

The lower-b negative/backward-`a` branch omission was attributed, repaired and reexecuted. The complete frozen one-sided census still failed at cell100.

## Interior-a zero-drift switching — adjudicated and Owner-adopted

Zero-science adjudication established a genuine strict interior-`a` crossing at cell100 and a unique statically compatible zero-drift candidate interval/root.

Owner has now adopted:

`docs/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_OWNER_ADOPTION_20260919.md`.

The corrected-diagnostic scientific law now includes an interior-`a` zero-drift switching candidate under the frozen trigger/interval/KKT/liquid-domain contract. Existing D1/D2/D3, liquid-`Z`, lower-`a` zero-kink, solver, grid, calibration and convergence law remain unchanged.

## Current stage — bounded implementation and checkpoint 2

Active task:

`tasks/CH5_MP4C_2018_KFE_D123_INTERIOR_A_ZERO_DRIFT_SWITCHING_IMPLEMENTATION_AND_V2_CHECKPOINT2_REEXECUTION_20260919.md`.

The task first implements/tests the adopted law, then performs exactly one fresh V2 policy-map attempt.

If the map completes:
- assemble one Q2;
- compute B2/D2, policy/operator differences and cycle diagnostics;
- stop before V2->V3 and terminal KFE.

If the map fails:
- stop at the first new scientific failure;
- do not repair another issue inside the task.

## Downstream closure

No new P2/u2/Q2 or B2/D2 object exists until the active task proves it.

Corrected production replacement, market clearing, GE/annual/dynamics/IRF and Results remain downstream and closed.

Capital/labor K1/C1/K1B/K2 routes remain deferred, not abandoned, and do not bypass the household fixed-point gate.
