# CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / local-policy parity executor

Owner: final scientific authority

## 1. Purpose

Resolve only the remaining local-policy parity blocker from:

`MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_IDENTITY_ADJUDICATION_BLOCKED`

The predecessor already established, before any scientific execution:

- `MP4B_ECONOMICS_PREDECESSOR_GIT_BLOB_IDENTITY_ADJUDICATED_PASS`;
- `MP4B_FAITHFUL_RAW_VB_TRANSFER_HELPER_ROUTE_ISOLATION_PASS`;
- `MATLAB_FAITHFUL_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`;
- `MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`.

Its one authorized MATLAB local-policy batch completed successfully and persisted 12 rows. The one authorized Python batch then failed **before any policy call** because the external runner did not bind the repository `src` root:

`ModuleNotFoundError: No module named 'ch5_two_asset_hank'`

Because MATLAB had already produced the first post-repair scientific output, the predecessor correctly prohibited repair/retry and rolled all production/export candidates back to task-authority bytes.

This successor authorizes only:

1. reconstructing the same bounded modular faithful raw-`Vb` repair candidate under a pre-frozen patch;
2. statically proving the replacement Python local-policy runner binds the exact repository `src` root and imports only the intended candidate modules;
3. one zero-policy-call direct-invocation bootstrap smoke;
4. exactly one replacement Python local-policy batch;
5. exactly one local comparator against the **preserved MATLAB 12-row output**;
6. rollback of temporary modular candidate source before closeout, whether PASS or BLOCKED.

MATLAB local-policy rerun is **zero**. No 50-state HJB, KFE, Beijing household, standalone revalidation, multi-province stationary, MP2/MP3, dynamics, or Results work is authorized here.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_AFTER_HELPER_REMEDIATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_AUTHORITY_REVALIDATION_AFTER_ECONOMICS_IDENTITY_ADJUDICATION_REPORT.md`
- accepted MATLAB-faithful local-policy parity and HJB propagation-aware reports

Primary authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Preserve accepted source-semantics evidence:

- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`;
- exact Beijing iteration-5 offending cell `(5,18,1)`;
- raw `VbF=-0.014003744365506235`;
- raw `VbB=0.0036470322698923963`;
- raw `VaF=0.029712870660726632`;
- raw `VaB=0.0183013418028827`;
- MATLAB floors raw `Vb` only for consumption/labor and uses raw boundary-treated `Va/Vb` in `HANK3_FOC`.

## 3. Live continuity and predecessor identities

Expected execution-start parent:

`43c1ae62021caca31fb291a62df5a889e7d9aec5`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the predecessor BLOCKED commit;
3. require clean worktree;
4. verify predecessor report exists and records the runner import blocker exactly;
5. verify production bytes are rolled back to task-authority state;
6. verify frozen MATLAB local-policy artifacts below;
7. verify no historical R5 / `chapter5_model` runtime dependency.

Required current predecessor identities:

- `src/ch5_two_asset_hank/economics.py`
  - Git contents/blob object id: `d6611d7cca702dcaaf1371c44e6179c7f88f5318`
  - exact Git-blob file-byte SHA-256: `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
  - SHA-256: `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py`
  - SHA-256: `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

The stale prose hash `66E3C56F...6DC55` is not authority.

## 4. Immutable preserved MATLAB local-policy evidence

Reuse the predecessor local-policy root read-only:

`D:\ProjectTemp\ch5-mp4b-raw-vb-local-policy-20260831-001`

Required frozen artifacts:

- case manifest SHA-256:
  `5AD56F4C99DF38F43C4F0CB6FB5221F80797EE9B9D6B51A2229B4EE0077007FE`
- source-extracted MATLAB evaluator SHA-256:
  `8AA8E0A5EBFCEF136813FF1A880DE8C96BE63D5B958BC947EEE6D8EEBADAC8EA`
- preserved MATLAB 12-row output SHA-256:
  `3510767089553256B860B53D7BBD042C18E02600948DD711244B7F9E1DBE9F5C`
- predecessor failure ledger SHA-256:
  `23CA316BD1FA9F7E4B7473DD681B17D24B0EEDC176C100D8F43851E9E33A401B`

Do not modify, overwrite, regenerate, or rerun any MATLAB artifact in that root.

Required call budget for preserved MATLAB evidence in this task:

- MATLAB local-policy batch: `0`
- MATLAB local-policy calls: `0`

## 5. Reconstruct and freeze the temporary modular repair candidate

The predecessor candidate was intentionally rolled back and was not assigned transfer authority. Therefore this task must reconstruct the same **scientific design**, then freeze its exact bytes before any replacement Python scientific call.

Temporary candidate changes are allowed only in:

- `src/ch5_two_asset_hank/economics.py`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`

Do **not** modify the standalone export in this local-policy-only gate.

Required scientific design:

### 5.1 Dedicated faithful-only helper

Add a dedicated helper, preferably:

`transfer_candidate_matlab_faithful_raw_vb`

It must:

- be used only by the MATLAB-faithful policy route;
- leave shared `transfer_candidate` unchanged;
- leave `transfer_candidate_corrected_max_scale` unchanged;
- use IEEE-compatible division without Python scalar `ZeroDivisionError`;
- reproduce the frozen protected MATLAB semantics for positive, negative, and zero `pb`;
- reproduce `pa=pb=0 -> ratio NaN -> output finite 0`;
- reproduce the frozen signed `-0` for `a=0,pb<0`;
- use the exact threshold and bare-`a` scaling;
- add no epsilon, clipping, positivity guard, finite substitution, alternate equation, or case-specific branch.

MATLAB-equivalent scalar NaN-ignoring `min/max` behavior may be implemented using `np.fmin/np.fmax` or an exactly equivalent source-faithful construction, but the persisted ten-case comparison must prove it before scientific execution.

### 5.2 Faithful selector ordering

In `matlab_faithful_policy.py` only:

- remove the non-source pre-floor rejection on raw `v_b_forward/v_b_backward`;
- retain `max(raw Vb,1e-6)` strictly for consumption/labor FOCs;
- route all four transfer FOCs through the dedicated faithful raw-`Vb` helper using raw boundary-treated derivatives;
- preserve all `Ic/Idh` ordering, boundaries, taper, cost, bare-`a` scaling, drift tolerance, and sparse-rate formulas unchanged.

Corrected/reference `policies.py`, `hjb.py`, `generator.py`, `boundaries.py` and all KFE/steady-state modules are frozen and must not change.

### 5.3 Candidate patch authority

Before any replacement Python local-policy batch:

1. generate an explicit patch from the task-authority bytes to the temporary two-file candidate;
2. persist it under a validation-only repository path, preferably:
   `validators/multi_province/mp4b_faithful_raw_vb_modular_candidate.patch`;
3. record the patch SHA-256;
4. record the candidate SHA-256 for both modified files;
5. prove reapplying the patch to clean task-authority bytes reproduces the same candidate hashes in a no-science preflight or equivalent deterministic check.

This patch is a successor-transfer artifact only. It does not by itself make the production candidate accepted.

Required marker:

`MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_PATCH_FROZEN`

## 6. Required no-science candidate preflight

Before the replacement Python batch, re-establish on the frozen candidate:

- call-site isolation: corrected/reference routes still call only the unchanged shared helper;
- persisted ten-case Python/MATLAB helper parity using the previously frozen scalar manifest, with no MATLAB rerun;
- focused helper/policy/taper tests;
- Python compile;
- `git diff --check`.

The ten-case direct finite comparison retains the accepted machine-scaled rule:

`128*eps64*max(1,abs(x),abs(y))`

Categorical `finite/+Inf/-Inf/NaN` classes and signed-zero behavior are exact.

Required preflight markers:

- `MP4B_FAITHFUL_RAW_VB_TRANSFER_HELPER_ROUTE_ISOLATION_PASS`
- `MATLAB_FAITHFUL_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`
- `MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_STATIC_REVIEW_PASS`

If any fail, do not execute the replacement Python local-policy batch.

## 7. Replacement Python runner bootstrap contract

Create or update one validation-only runner under `validators/multi_province/`, preferably:

`validators/multi_province/mp4b_faithful_raw_vb_local_policy_python_runner.py`

The runner MUST be direct-file safe from an arbitrary working directory/fresh `D:\ProjectTemp` root.

Before importing `ch5_two_asset_hank`, it must:

1. derive the repository root from `Path(__file__).resolve()` rather than from the current working directory;
2. derive exact `SRC_ROOT = REPO_ROOT / "src"`;
3. verify `SRC_ROOT` exists;
4. insert the exact resolved `SRC_ROOT` into `sys.path` before package import;
5. import the intended candidate modules only after that insertion;
6. verify the imported module origins resolve to the exact current repository candidate paths;
7. fail closed if any imported `ch5_two_asset_hank` module resolves outside the exact repository `src` tree;
8. fail closed on any historical R5 / `chapter5_model` import.

Do not rely solely on ambient `PYTHONPATH`, the shell working directory, user-site packages, or an editable installation.

Required static marker:

`MP4B_RAW_VB_LOCAL_POLICY_RUNNER_REPOSITORY_SRC_BOOTSTRAP_STATIC_REVIEW_PASS`

## 8. Mandatory zero-policy-call direct-invocation smoke

Before consuming the replacement Python scientific batch, run exactly one no-science direct-file bootstrap smoke from a fresh directory outside the repository.

The smoke must:

- invoke the replacement runner as a direct file;
- bind/import the exact temporary candidate modules;
- report exact repository root, `src` root, imported module paths and candidate hashes;
- execute zero local-policy cases and zero policy calls;
- create no scientific output rows;
- reject a synthetic wrong `src`/outside-module origin in focused testing or an equivalent fail-closed preflight.

Required marker:

`MP4B_RAW_VB_LOCAL_POLICY_RUNNER_DIRECT_INVOCATION_SMOKE_PASS`

If this smoke fails, repair is allowed only **before** any replacement Python scientific output because no scientific budget has yet been consumed. After the smoke passes and the scientific batch begins, no repair/rerun is authorized.

## 9. Frozen local-policy comparison contract

The replacement Python batch must consume the exact preserved 12-case manifest from Section 4 without changing case order or values.

The comparator must be frozen before the Python scientific output and must retain the already accepted source-extracted local-policy parity logic.

At minimum compare all common MATLAB/Python local-policy fields statewise:

- liquid label / branch;
- transfer label / branch;
- consumption;
- labor;
- transfer;
- adjustment cost;
- effective illiquid return;
- `mu_a`;
- `mu_b`;
- utility;
- liquid/illiquid directions;
- iteration liquid backward/forward rates;
- derivative-side illiquid backward/forward rates;
- any additional fields present in both frozen schemas.

Categorical outputs: exact.

Same-input continuous outputs: unchanged machine-scaled contract:

`128*eps64*max(1,abs(x),abs(y))`

Do not introduce a broader tolerance because of the new negative-raw-`Vb` witness.

The comparator must explicitly identify the exact Beijing witness row and confirm the repaired Python route does not reject it before the source-defined upwind/transfer selection.

Synthetic comparator preflight must fail at least one deliberately perturbed continuous field and one categorical field before scientific execution.

## 10. One-shot replacement scientific budget

After all static and bootstrap gates pass:

- MATLAB local-policy batch: `0`
- Python replacement local-policy batch: exactly `1`
- Python local-policy batch reruns: `0`
- local comparator: exactly `1`
- comparator reruns: `0`

The Python batch must write a fresh no-overwrite `python_output.json` in a new replacement run root under `D:\ProjectTemp`.

The comparator must compare only that Python output against the preserved MATLAB output SHA `3510767089553256B860B53D7BBD042C18E02600948DD711244B7F9E1DBE9F5C`.

If the Python batch fails after entering the scientific case loop, or the comparator fails, preserve evidence and stop. No candidate repair or scientific rerun is authorized in this task.

## 11. Acceptance

PASS requires:

1. all continuity and current predecessor identities pass;
2. temporary modular candidate patch/hash freeze passes;
3. route isolation and persisted ten-case helper parity pass;
4. replacement runner static bootstrap review passes;
5. zero-policy-call direct invocation smoke passes;
6. one replacement Python batch completes all 12 frozen rows;
7. one comparator passes the unchanged local-policy contract;
8. the exact Beijing negative-raw-`Vb` witness passes;
9. no MATLAB local-policy rerun occurs;
10. all later model/scientific calls remain zero;
11. temporary modular source changes are rolled back to exact task-authority bytes before Git closeout.

On PASS establish:

`MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`

and:

`MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_PASS`

Terminal PASS:

`MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_PASS`

BLOCKED terminal:

`MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_BLOCKED`

A PASS accepts the **frozen modular candidate patch at the local-policy layer only**. It does not yet accept full HJB, KFE, standalone household, GE, or 2009 stationary execution.

## 12. Mandatory rollback before closeout

Whether PASS or BLOCKED, restore the temporary production source paths to exact task-authority bytes before commit:

- `economics.py` exact Git-blob-byte SHA-256:
  `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `matlab_faithful_policy.py` SHA-256:
  `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- standalone export remains unchanged throughout at:
  `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

Final production/export mutation must be exactly `0`.

Do not remove the validation-only frozen candidate patch, runner, focused tests, comparator, report, or bounded roadmap update if they are otherwise authorized and needed for successor transfer.

## 13. Global call prohibitions

This task authorizes zero calls for:

- persisted 10-case MATLAB scalar rerun;
- exact-junction smoke rerun;
- MATLAB local-policy rerun;
- MATLAB or Python 50-state HJB;
- any KFE experiment;
- MATLAB or Python Beijing household;
- second province;
- MATLAB/Python multi-province stationary;
- MP2/MP3 empirical;
- annual batch;
- shocks;
- transition;
- dynamics;
- IRF;
- R5;
- Results.

Do not modify protected MATLAB, canonical data/cache, corrected/reference modules, faithful operator/KFE, MP2/MP3, or historical R5.

## 14. Allowed repository changes

Allowed only:

- `validators/multi_province/mp4b_faithful_raw_vb_modular_candidate.patch`
- `validators/multi_province/mp4b_faithful_raw_vb_local_policy_python_runner.py`
- one narrowly scoped replacement local comparator/manifest helper under `validators/multi_province/` if needed
- focused tests for candidate patch, bootstrap, comparator, and zero-call boundaries
- bounded CURRENT roadmap update if needed
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_REPORT.md`

Temporary edits to `economics.py` and `matlab_faithful_policy.py` are permitted only for candidate execution and MUST be rolled back before closeout.

## 15. Required report and Git closeout

Report at minimum:

- terminal verdict;
- live continuity and current predecessor hashes;
- preserved MATLAB artifact hashes;
- temporary candidate patch path/SHA;
- candidate economics/policy SHA-256 values;
- proof patch reproduction is deterministic;
- route-isolation and ten-case helper parity evidence;
- runner bootstrap design and imported module origins;
- zero-policy-call direct invocation smoke artifact/hash;
- replacement Python batch root/output SHA;
- comparator hash and preflight;
- complete 12-row parity summary including exact Beijing witness;
- scientific call ledger;
- rollback hashes;
- final production/export mutation `0`;
- tests/compile/diff checks;
- forbidden-operation check;
- Git closeout;
- exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

## 16. Successor boundary

On PASS recommend exactly one successor:

**apply the frozen modular candidate patch byte-identically, accept it provisionally for the HJB revalidation gate, run one repaired modular Python 50-state faithful HJB against the preserved MATLAB HJB artifact using the unchanged propagation-aware contract, and only after HJB PASS proceed in a later gate to standalone Beijing-household revalidation.**

Do not authorize 2009 stationary yet.

On BLOCKED, recommend only the minimum evidence/runner/comparator repair needed; do not broaden to HJB or household execution.
