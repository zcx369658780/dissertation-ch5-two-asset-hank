# CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / MATLAB-source parity executor / standalone export revalidator

Owner: final scientific authority

## 1. Purpose

Advance exactly one gate after:

`MP4B_FAITHFUL_RAW_VB_50STATE_HJB_REVALIDATION_PASS`

The accepted frozen modular raw-`Vb` patch has now passed:

- protected `HANK3_FOC` ten-case edge semantics;
- 12/12 local-policy parity including the exact Beijing negative-raw-`Vb` witness;
- full 50-state faithful HJB/operator parity under the unchanged propagation-aware contract.

Production bytes were intentionally rolled back after those gates.

This task must now:

1. restore the accepted modular candidate byte-identically from the frozen patch;
2. apply the same source-faithful raw-`Vb` semantic repair to the standalone export only;
3. prove modular/standalone helper and local-policy semantics are source-identical before household execution;
4. freeze an exact source-bound Beijing calendar-2009 first-turn household input contract;
5. execute exactly one protected MATLAB Beijing `HANK_2ASSETS_HJB` household solve and exactly one repaired standalone Python `solve_household_steady_state` solve on the same source-bound household object;
6. compare the common source-backed household production interface and the source aggregate formulas under a pre-frozen propagation-aware household comparator;
7. revalidate the standalone clean-room dependency/public-API/export contract;
8. on complete PASS, retain and publish the accepted modular repair and new standalone export bytes as the current household authority.

This task does **not** authorize calendar-2009 multi-province stationary execution, any second province, MP2/MP3 empirical execution, annual batch, shocks, transition, dynamics, IRF, R5, or Results.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_AND_FINAL_ACCEPTANCE_RERUN_REPORT.md`
- accepted faithful KFE reports
- accepted/replacement end-to-end stationary household aggregate reports, including the exact MATLAB aggregate-source audit
- standalone export closeout/acceptance reports
- MP4A2 calendar-2009 canonical-input report
- MP4B full-initialization/preflight and first-Beijing HJB diagnostic/localization reports

Primary numerical authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Preserve accepted markers:

- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_PASS`
- `MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_PASS`
- all previously accepted faithful KFE/density/household aggregate/standalone dependency authorities except where superseded by a new standalone SHA on PASS.

## 3. Live continuity

Expected execution-start parent:

`2bb0c4250dbe4fc7725ad9f90bb1caa6df7eaacf`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the 50-state HJB PASS commit;
3. require clean worktree;
4. verify the local-policy PASS report, 50-state HJB PASS report, frozen modular patch, and accepted standalone export exist on live main;
5. verify current production bytes remain the rolled-back predecessor bytes;
6. verify protected MATLAB source hashes;
7. verify canonical 2009 input identity and preserved prior MP4B household-input evidence;
8. verify no historical R5 / `chapter5_model` runtime dependency.

Required current rolled-back identities:

- `src/ch5_two_asset_hank/economics.py`
  - exact Git-blob-byte SHA-256: `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
  - SHA-256: `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py`
  - SHA-256: `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`
- `src/ch5_two_asset_hank/matlab_faithful_operator.py`
  - SHA-256: `0C9F6C1AE3E6428E49086DE900BE55D5038A10144EC061EDDFB9E7249A4A1AAC`
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py`
  - SHA-256: `924831362C904A253D8AD6011FE9BB4C099C6DF4268D0C2AA30AB9139569F1DE`

Protected MATLAB required hashes:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

If any identity fails, stop before mutation.

## 4. Restore the accepted modular candidate byte-identically

Frozen patch:

`validators/multi_province/mp4b_faithful_raw_vb_modular_candidate.patch`

Required patch SHA-256:

`0F044055DA9B4BFF22A2F8342EF189781AD3D536BFD2A67148C1182C1F9AB31D`

Apply exactly once to the clean task-authority bytes.

Required post-patch identities:

- candidate `economics.py`
  - Git object: `810e0875febc873ae85bef7e88edd4de349b00b2`
  - exact blob-byte SHA-256: `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- candidate `matlab_faithful_policy.py`
  - Git object: `2021db630f3057026ffc37d375a43aaddbccec48`
  - exact blob-byte SHA-256: `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`

Do not edit the modular candidate after patch application.

Required marker:

`MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_BYTE_IDENTITY_RESTORED_FOR_STANDALONE_PASS`

## 5. Standalone raw-Vb synchronization — source-identical only

Modify only:

`exports/matlab_faithful_two_asset_ha.py`

The standalone change must be the source-identical equivalent of the already accepted modular candidate semantics:

### 5.1 Dedicated faithful raw-Vb helper

Add the standalone equivalent of the accepted modular helper:

`transfer_candidate_matlab_faithful_raw_vb`

It must reproduce exactly the already frozen MATLAB edge semantics:

- positive `pb` -> ordinary finite source value;
- negative `pb` -> ordinary finite source value;
- `pb==0, pa>0` -> `+Inf`;
- `pb==0, pa<0` -> `-Inf`;
- `pa==pb==0` -> ratio `NaN`, final source output finite `0`;
- `a==0, pb<0` -> signed `-0`.

Required implementation semantics:

- IEEE-compatible division such as `np.divide`, not Python scalar division that raises `ZeroDivisionError`;
- MATLAB-equivalent NaN-ignoring scalar min/max behavior such as `np.fmin/np.fmax` or exactly equivalent semantics;
- exact threshold formula;
- bare-`a` scaling;
- no epsilon, clipping, positivity guard, finite substitution, alternate equation, or case-specific branch.

### 5.2 Standalone local-policy ordering

In the standalone faithful selector only:

- remove the non-source pre-floor raw-`Vb` positivity rejection;
- retain `max(raw Vb,1e-6)` strictly for consumption/labor FOCs;
- route all four transfer FOCs through the dedicated faithful raw-`Vb` helper with raw boundary-treated `Va/Vb`;
- preserve every `Ic/Idh` ordering rule, asset/liquid boundary rule, taper, cost, bare-`a`, drift tolerance, operator rate, HJB, KFE and aggregate formula unchanged.

No other standalone scientific arithmetic may change.

## 6. Freeze standalone candidate patch and source-map equivalence

Before any standalone scientific call:

1. generate a standalone-only patch from the task-authority standalone bytes to the repaired standalone candidate;
2. persist it under a validation-only path, preferably:
   `validators/multi_province/mp4b_faithful_raw_vb_standalone_candidate.patch`;
3. record standalone patch SHA-256;
4. record repaired standalone candidate SHA-256;
5. prove deterministic reapplication to the exact old standalone bytes reproduces the same candidate SHA;
6. prove the standalone dedicated helper and faithful local-policy relevant statements are source-map equivalent to the already accepted modular candidate semantics;
7. prove HJB operator/KFE/aggregate source blocks outside this raw-`Vb` repair are byte/textually unchanged from the old standalone export except for unavoidable line displacement.

Required markers:

- `MP4B_FAITHFUL_RAW_VB_STANDALONE_CANDIDATE_PATCH_FROZEN`
- `MP4B_MODULAR_STANDALONE_RAW_VB_SOURCE_MAP_EQUIVALENCE_PASS`

If source-identical synchronization cannot be proven without altering other standalone arithmetic, stop BLOCKED.

## 7. Pre-household standalone semantic replay — no MATLAB reruns

Before Beijing household execution, reuse prior persisted MATLAB evidence read-only.

### 7.1 Frozen protected HANK3_FOC ten-case artifact

Root:

`D:\ProjectTemp\mp4b_hank3_foc_10case_bcb5db94-c4a2-4d89-9485-6cbb20798126`

Required success manifest SHA-256:

`06A31A509EF696094563CD41C9928A3E83AFB076675B09155CC82B2200E7E74E`

Run a standalone-Python-only ten-case helper replay against the frozen MATLAB manifest.

Budget:

- MATLAB scalar rerun: `0`
- standalone Python helper cases: exactly `10`
- reruns: `0`

Require exact categorical classes, exact signed-zero semantics, and the existing finite bound:

`128*eps64*max(1,abs(x),abs(y))`.

Required marker:

`MATLAB_FAITHFUL_STANDALONE_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`

### 7.2 Preserved 12-row local-policy artifact

Read-only root:

`D:\ProjectTemp\ch5-mp4b-raw-vb-local-policy-20260831-001`

Required hashes:

- case manifest `5AD56F4C99DF38F43C4F0CB6FB5221F80797EE9B9D6B51A2229B4EE0077007FE`
- MATLAB output `3510767089553256B860B53D7BBD042C18E02600948DD711244B7F9E1DBE9F5C`

Run the repaired standalone local-policy selector on the exact 12 preserved cases.

Budget:

- MATLAB local-policy rerun: `0`
- standalone Python local-policy calls: exactly `12`
- comparator: exactly `1`
- reruns: `0`

Use the same accepted local-policy comparator contract:

- categorical exact;
- common continuous fields bounded by `128*eps64*max(1,abs(x),abs(y))`;
- explicit Beijing witness row must pass and must not reject pre-selection.

Required marker:

`MATLAB_FAITHFUL_STANDALONE_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_PASS`

If either Python-only replay fails, preserve evidence, rollback all candidate production/export bytes, and stop. No repair/rerun after the failed replay.

## 8. Canonical Beijing first-turn household input freeze

Only after Sections 4-7 PASS, freeze the exact Beijing first-turn household scientific input without executing a household solve.

Primary annual input authority:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

Required SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Also read and reconcile, as evidence only, the accepted MP4B full-initialization/turn-1 household-input artifacts and preserved first-Beijing diagnostic artifacts. Do not substitute a stale snapshot if it conflicts with the canonical 2009 source binding.

Freeze one new no-overwrite input contract containing every argument needed by both unchanged MATLAB `HANK_2ASSETS_HJB` and repaired standalone Python household entry, including source identities for:

- province = Beijing, exact province index/order;
- calendar year = 2009;
- `param` values actually consumed by protected MATLAB;
- asset/productivity grids and `la_mat` / switching object;
- `num` iteration/tolerance values;
- `CHI`/adjustment-cost fields;
- first-turn `rah`, `rb`, `w`, `tau`, `Tt`, `rb_gap`;
- baseline/initial `results` fields required by protected MATLAB;
- exact Python `EconomicParams`, `HouseholdInputs`, and `MatlabFaithfulHJBGrid` mapping;
- shape/order/orientation mapping between MATLAB `(b,a,z)` and standalone Python;
- any other required source-backed scalar/vector.

The mapping must be one-to-one and source-backed. Do not guess missing values or fill with convenient defaults.

Required marker:

`MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_SAME_INPUT_CONTRACT_FROZEN`

If any required input cannot be uniquely source-bound, stop:

`OWNER_PROVENANCE_REQUIRED`

before either household call.

## 9. MATLAB household source/output audit before execution

Before the household calls, inspect the protected `HANK_2ASSETS_HJB.m` source and freeze the exact output/aggregate mapping for the production interface.

Required aggregate mappings, if unchanged source audit confirms them:

- `results.Ct = sum(C.*g*dah*db,'all')`
- `results.Lt = sum(zzz.*l.*g*dah*db,'all')`
- `results.At = sum(aaah.*g*dah*db,'all')`
- `results.Bt = sum(bbb.*g*dah*db,'all')`

Confirm exact source mapping for `AhTax` / outer-loop `AtTax` if present and used by the multi-province household adapter.

Freeze whether the protected function directly returns or makes available:

- household convergence flag;
- HJB iteration diagnostics;
- stationary density / KFE diagnostics;
- `Ct`, `Lt`, `At`, `Bt`, `AhTax/AtTax`;
- any source-backed normalization diagnostics.

Do not instrument or modify protected MATLAB merely to expose an internal object not already available from its public returned result/output scope.

Required marker:

`MP4B_BEIJING_MATLAB_HOUSEHOLD_OUTPUT_AND_AGGREGATE_SOURCE_MAP_FROZEN`

## 10. Propagation-aware Beijing household comparison contract — freeze before science

Do not choose a broad post-output scalar tolerance.

Before either household output exists, freeze a comparator that separates:

### 10.1 Exact/categorical gates

Require exact equality where common/source-backed:

- province/year/input identity;
- grid values/order/orientation mapping;
- source parameter identities;
- convergence boolean;
- any exposed categorical/source flags;
- result-field mapping.

### 10.2 Same-input source-local arithmetic

Retain:

`128*eps64*max(1,abs(x),abs(y))`

for source-local formulas on identical arrays/scalars and direct mapped fields.

### 10.3 KFE/system certificates

The standalone household's internal faithful KFE must continue to satisfy the already accepted contaminated-row/KFE contracts, including finite raw solution, exact contaminated row/RHS/normalization construction, backward-error certificate, and density normalization identity.

If protected MATLAB returns enough diagnostics to compare the same object directly, compare them; otherwise report Python certificate and source mapping without inventing unavailable MATLAB internals.

### 10.4 Household aggregate reductions

The source aggregate weight remains exactly `dah*db`; no `dz`, productivity probability, trapezoid, or endpoint weighting is permitted unless the protected source audit says otherwise.

Before household execution derive and freeze binary64 reduction/closure bounds for the actual Beijing state count using standard `gamma_n = n*eps/(1-n*eps)` finite-sum reasoning or a stricter justified equivalent.

Use those bounds only for arithmetic reduction/decomposition closure, not to hide implementation differences.

For `Ct`, `Lt`, `At`, `Bt`, and `At+Bt`, report:

- MATLAB value;
- standalone value;
- absolute and relative difference;
- source mapping;
- arithmetic/reduction classification.

If enough common policy/density arrays are exposed by both public outputs to form a decomposition, report policy and density contributions. If protected MATLAB does not expose them, do not reconstruct them by modifying source; instead require the raw aggregate differences to satisfy the pre-frozen source-backed comparator criteria established before execution.

`AtTax`/`AhTax` must be compared only if uniquely source-backed on both sides.

A raw aggregate difference may be accepted as a propagation diagnostic only if:

1. local-policy and 50-state HJB repair authorities remain accepted;
2. standalone raw-`Vb` helper/local-policy replays PASS;
3. both household solves converge;
4. source aggregate formulas/weights/field mappings are identical;
5. Python HJB/KFE certificates pass their accepted contracts;
6. all same-input source-local replay checks pass;
7. all available exact/categorical mappings pass;
8. reduction/identity closures pass the pre-frozen bounds;
9. material mismatch list is empty;
10. unresolved scientific residual list is empty;
11. source/environment failure list is empty.

Do not invent a new permissive raw aggregate tolerance after seeing the result.

Required pre-science marker:

`MP4B_BEIJING_STANDALONE_HOUSEHOLD_PARITY_COMPARATOR_PREFLIGHT_PASS`

Synthetic comparator preflight must fail at least one deliberately perturbed aggregate and one categorical/input identity before scientific execution.

## 11. Runner/wrapper source-binding preflight

Create only validation-only launchers/wrappers as needed.

### MATLAB wrapper

It must:

- bind only the exact protected MATLAB root;
- verify protected source hashes;
- call `HANK_2ASSETS_HJB` directly for Beijing only;
- never call `HANK_mp_1turn`, `HANK_mp_1eq`, `mpHANK_equilibrium_2000`, annual wrappers, or any multi-province controller;
- persist the frozen same-input identity and returned source-backed household result fields;
- use no-overwrite artifact paths.

### Python standalone runner

It must:

- import the exact repaired standalone file by explicit file path or otherwise verify the exact candidate standalone origin/SHA;
- not import modular scientific code as the production solve path;
- execute exactly `solve_household_steady_state` on the frozen same-input contract;
- persist the common source-backed outputs and available certificates;
- use no-overwrite artifacts.

Run one zero-household-call bootstrap smoke for each wrapper/runner before science.

Required markers:

- `MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_SMOKE_PASS`
- `MP4B_BEIJING_STANDALONE_HOUSEHOLD_RUNNER_ZERO_CALL_SMOKE_PASS`

Smoke household/HJB/KFE calls must all be zero.

## 12. Scientific call budget

Only after all previous gates PASS:

### Authorized household calls

- protected MATLAB Beijing `HANK_2ASSETS_HJB` household call: exactly `1`
- repaired standalone Python `solve_household_steady_state` call: exactly `1`
- Beijing household comparator: exactly `1`
- reruns: `0`

### Internal calls

The MATLAB household call may execute its own source HJB and contaminated-row stationary solve internally.

The standalone household call may execute its faithful HJB and faithful KFE internally.

These internal calls are part of the single household call on each side and must be reported, not separately repeated.

### Forbidden scientific calls

- MATLAB 50-state HJB rerun: `0`
- Python modular 50-state HJB rerun: `0`
- separate MATLAB/Python KFE experiment: `0`
- any second province household: `0`
- MATLAB/Python calendar-2009 multi-province stationary: `0/0`
- MP2/MP3 empirical: `0/0`
- annual batch/shocks/transition/dynamics/IRF/R5/Results: `0`
- ten-case MATLAB scalar rerun: `0`
- MATLAB local-policy rerun: `0`

If either household call fails or the comparator fails, preserve exact evidence, rollback all unaccepted modular/standalone candidate bytes, and stop. No repair/rerun after the first household scientific output.

## 13. Beijing household acceptance

PASS requires:

1. modular candidate restored byte-identically from the accepted patch;
2. standalone repair frozen and source-map equivalent to accepted modular semantics;
3. standalone ten-case helper replay PASS;
4. standalone 12-case local-policy replay PASS;
5. exact Beijing same-input contract frozen without provenance ambiguity;
6. MATLAB household wrapper and Python runner zero-call smokes PASS;
7. exactly one MATLAB Beijing household and one standalone Python household solve complete;
8. both converge;
9. common source-backed result-field mappings are valid;
10. `Ct`, `Lt`, `At`, `Bt`, and `At+Bt` pass the pre-frozen propagation-aware household comparator;
11. `AtTax/AhTax` passes if source-backed on both sides, otherwise is explicitly marked unavailable without substitution;
12. Python internal faithful HJB/KFE certificates pass;
13. material mismatch list empty;
14. unresolved scientific residual list empty;
15. source/environment failure list empty;
16. all forbidden call counts zero.

On PASS establish:

`MATLAB_FAITHFUL_STANDALONE_BEIJING_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`

and:

`MP4B_FAITHFUL_RAW_VB_STANDALONE_BEIJING_HOUSEHOLD_PARITY_PASS`

## 14. Standalone export revalidation

Only after Beijing household parity PASS, revalidate the repaired standalone export itself.

Required checks:

- NumPy/SciPy/dataclasses-only dependency boundary remains valid;
- clean-room isolated import PASS;
- public API remains household/HJB/KFE/aggregate only with no GE/dynamics leakage;
- `solve_matlab_faithful_hjb`, `solve_matlab_faithful_stationary_kfe`, `aggregate_stationary_household`, and `solve_household_steady_state` remain exported;
- no `ch5_two_asset_hank` runtime import dependency exists in the standalone file;
- source map to protected MATLAB remains updated and truthful;
- raw-`Vb` repair helper and selector semantics match the accepted modular repair;
- accepted HJB/KFE/aggregate arithmetic outside the repair remains unchanged;
- `py_compile` PASS;
- focused tests PASS;
- clean-room import PASS;
- `git diff --check` PASS.

Freeze the new standalone SHA-256.

Required marker:

`MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

## 15. Production closeout semantics

### On complete PASS

Retain and publish:

- accepted repaired `src/ch5_two_asset_hank/economics.py` candidate bytes;
- accepted repaired `src/ch5_two_asset_hank/matlab_faithful_policy.py` candidate bytes;
- repaired/revalidated `exports/matlab_faithful_two_asset_ha.py` bytes;
- validation-only patches/runners/wrappers/comparators/tests/reports needed for audit.

Do **not** alter faithful operator/HJB/KFE modules if they were not required by the frozen patch.

The old standalone SHA `276D2244...31AB8` becomes historical predecessor evidence only. The report must explicitly state the new current standalone authority SHA.

### On BLOCKED / MATERIAL MISMATCH

Rollback all three candidate production/export files to task-authority bytes:

- economics `5FD4805C...49E7`
- faithful policy `D8A595B9...5F8C2`
- standalone `276D2244...31AB8`

Verify rollback hashes before closeout.

Do not leave an unaccepted repair on `main`.

## 16. Terminal semantics

Complete PASS terminal:

`MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_PASS`

PASS must establish all of:

- `MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_BYTE_IDENTITY_RESTORED_FOR_STANDALONE_PASS`
- `MP4B_FAITHFUL_RAW_VB_STANDALONE_CANDIDATE_PATCH_FROZEN`
- `MP4B_MODULAR_STANDALONE_RAW_VB_SOURCE_MAP_EQUIVALENCE_PASS`
- `MATLAB_FAITHFUL_STANDALONE_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`
- `MATLAB_FAITHFUL_STANDALONE_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_PASS`
- `MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_SAME_INPUT_CONTRACT_FROZEN`
- `MP4B_BEIJING_MATLAB_HOUSEHOLD_OUTPUT_AND_AGGREGATE_SOURCE_MAP_FROZEN`
- `MP4B_BEIJING_STANDALONE_HOUSEHOLD_PARITY_COMPARATOR_PREFLIGHT_PASS`
- both zero-call wrapper/runner smoke markers
- `MATLAB_FAITHFUL_STANDALONE_BEIJING_HOUSEHOLD_AFTER_RAW_VB_REPAIR_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_STANDALONE_BEIJING_HOUSEHOLD_PARITY_PASS`
- `MATLAB_FAITHFUL_TWO_ASSET_HA_STANDALONE_SINGLE_FILE_EXPORT_REVALIDATED_AFTER_RAW_VB_REPAIR`

BLOCKED terminal:

`MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_BLOCKED`

If a true scientific comparison completes but yields an unexplained material mismatch, classify explicitly and rollback; do not force BLOCKED language over a genuine material parity failure.

## 17. Allowed repository changes

Allowed as needed:

- `src/ch5_two_asset_hank/economics.py` — only the already frozen modular patch
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` — only the already frozen modular patch
- `exports/matlab_faithful_two_asset_ha.py` — source-identical standalone raw-`Vb` repair only
- one standalone candidate patch under `validators/multi_province/`
- bounded validation-only MATLAB/Python wrappers/runners/comparator
- focused tests for standalone raw-`Vb` synchronization/clean-room export
- bounded CURRENT roadmap update
- report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`

Do not modify:

- `src/ch5_two_asset_hank/matlab_faithful_operator.py`
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py`
- faithful KFE module
- corrected/reference modules
- protected MATLAB source
- canonical input/data/cache
- MP2/MP3
- historical R5

## 18. Required report and Git closeout

Report at minimum:

- terminal verdict;
- live continuity;
- old/current/pre-candidate hashes;
- modular patch identity and restored candidate hashes;
- standalone patch SHA and repaired standalone SHA;
- modular/standalone source-map equivalence proof;
- standalone ten-case helper parity;
- standalone 12-case local-policy parity including Beijing witness;
- exact Beijing input contract artifact path/SHA;
- protected MATLAB output/aggregate source mapping;
- comparator preflight and frozen arithmetic/reduction rules;
- wrapper/runner zero-call smoke evidence;
- MATLAB Beijing household output artifact SHA;
- standalone Beijing household output artifact SHA;
- complete `Ct/Lt/At/Bt/At+Bt` table and `AtTax/AhTax` status;
- convergence/HJB/KFE certificates available on each side;
- complete material mismatch list;
- complete unresolved scientific residual list;
- complete source/environment failure list;
- new standalone authority SHA if PASS;
- full scientific/model call ledger;
- rollback evidence if not PASS;
- focused tests, clean-room import, `py_compile`, `git diff --check`;
- forbidden-operation audit;
- Git closeout;
- exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

## 19. Successor boundary

On complete PASS recommend exactly one successor:

**authorize one new Python-only corrected-calendar-2009 multi-province stationary invocation against the already preserved completed MATLAB 2009 baseline; MATLAB stationary rerun remains zero.**

Do not authorize that execution inside this task.
