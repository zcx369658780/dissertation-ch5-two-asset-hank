# CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded validation-infrastructure remediator

Owner: final scientific authority

## 1. Purpose

Repair only the validation/evidence contract defects that blocked the predecessor replacement scalar task:

`MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_BLOCKED`

The predecessor scalar batch was **not executed**. Protected `HANK3_FOC` calls remained `0`; all HJB/KFE/household/stationary/model calls remained `0`; production/export mutation remained `0`.

The predecessor preflight found four bounded helper defects:

1. protected-source errors were not durably persisted with the first failing case/error before helper exit;
2. per-case rows lacked resolved protected helper path/SHA;
3. the helper call ledger did not contain the complete required zero-call surface;
4. the fresh `D:\ProjectTemp` execution root itself was not exclusively created before MATLAB execution.

This task repairs those four infrastructure defects only. It does **not** authorize the ten-case `HANK3_FOC` scalar batch and does not authorize any production-oracle repair or scientific/model execution.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_AND_REPLACEMENT_SMOKE_ONLY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_REPLACEMENT_10CASE_RAW_PB_EDGE_SEMANTICS_FREEZE_REPORT.md`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_path_equivalence_smoke.m`

Preserve accepted evidence unchanged:

- `MP4B_HJB_LIQUID_DERIVATIVE_MATLAB_SOURCE_ORDERING_FROZEN`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`
- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`
- `MP4B_HANK3_FOC_EXACT_JUNCTION_GUARD_REPAIR_STATIC_REVIEW_PASS`
- `MP4B_RAW_VB_HANK3_FOC_EXACT_JUNCTION_GUARD_SMOKE_PASS`

The source-edge marker remains unestablished in this task:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN` = NOT AUTHORIZED / NOT REACHED.

Primary numerical authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent:

`cfa33bab76122dccfa7ab92577b754be237cae2c`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the predecessor BLOCKED report commit;
3. require clean worktree;
4. verify the exact-junction PASS report and smoke evidence remain unchanged;
5. verify protected MATLAB identities;
6. verify production/export identities remain unchanged;
7. verify no historical R5 / `chapter5_model` runtime dependency.

Required protected SHA-256:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Required predecessor helper SHA-256 before mutation:

`33AC7212BF6D3F27A11761B2FD29DB713E63DFDE1356B5002FE5B9ED1166AF69`

Required preserved smoke manifest SHA-256:

`A82DC905E7D057EBE0645E3C8F3331F438CF939716762B17AC5C9B270B758D8B`

Any identity failure => stop before mutation.

## 4. Scientific content freeze — cases/formulas may not change

The exact ten frozen cases and their order MUST remain byte/textually unchanged:

1. `localized_BB`
2. `localized_BF`
3. `localized_FB`
4. `localized_FF`
5. `positive_pb`
6. `negative_pb`
7. `zero_pb_positive_pa`
8. `zero_pb_negative_pa`
9. `zero_pa_zero_pb`
10. `zero_a_negative_pb`

The exact arrays for `pa`, `pb`, `a`, `chi0`, `chi1`, classification functions, finite `%.17g` encoding, and the protected call expression must remain unchanged:

`value = HANK3_FOC(dummy,chi,pa(k),pb(k),a(k),0);`

Do not add/remove/reorder/tune cases. Do not add epsilon, clipping, a positivity guard, an alternate formula, exception-to-number substitution, Python emulation, or new scientific interpretation.

The exact-junction source-binding guard must also remain intact. Do not reintroduce `canonical_root/getCanonicalPath`, `startsWith`, substring matching, broad `D:\MatlabProgram` trust, arbitrary sibling trust, or filename-only trust.

## 5. Required infrastructure remediation

Modify only the validation helper/evidence plumbing needed to close the four predecessor defects.

### 5.1 Exclusive fresh run root

Preferred new public signature:

`mp4b_raw_vb_hank3_foc_edge_diagnostic(run_root, logical_root, physical_root)`

The helper must require that `run_root` does not exist and then create that exact directory atomically/exclusively before any protected `HANK3_FOC` call, using a fail-closed mechanism such as Java `mkdir()`.

The run root must be under the caller-selected fresh `D:\ProjectTemp` path. The helper must not delete, reuse, truncate, or repurpose an existing directory.

All artifacts created by this helper must live inside that exclusively created root.

### 5.2 Durable first-source-error evidence

The helper must durably preserve the exact first protected-source error without substituting a numerical result.

Required semantics:

- surround only the protected per-case call/evidence capture with bounded `try/catch` needed for persistence;
- on a protected `HANK3_FOC` error, identify the exact current case id/index;
- persist a fresh no-overwrite `failure.json` (or equivalently explicit failure artifact) inside the exclusively owned run root containing at minimum:
  - case id and one-based case index;
  - `pa`, `pb`, `a`, `chi0`, `chi1`;
  - exact MATLAB error identifier;
  - exact MATLAB error message;
  - resolved protected helper path;
  - resolved protected helper SHA;
  - logical/physical protected roots;
  - number of attempted protected calls;
  - number of completed protected calls;
  - complete zero-call ledger for every prohibited route;
- after durable persistence, terminate the helper fail-closed. Do not continue to later cases and do not synthesize a value.

The implementation may rethrow after persistence or terminate with a dedicated validation error, but the original protected-source identifier/message must remain in the failure artifact.

If no protected-source error occurs in a future authorized run, no failure artifact is required.

### 5.3 Per-case source identity

Extend the typed row schema so every case row carries at minimum:

- `resolved_helper_path`
- `resolved_helper_sha256`

These must be the same verified protected source identity used by the exact-junction guard. Do not compute identity from an unverified filename.

### 5.4 Complete call ledger

The helper's success and failure evidence must both contain an explicit complete ledger including at minimum:

- `matlab_scalar_batches`
- `HANK3_FOC_attempted_calls`
- `HANK3_FOC_completed_calls`
- `matlab_HJB`
- `matlab_KFE`
- `matlab_household`
- `matlab_multi_province`
- `matlab_stationary`
- `matlab_GE`
- `python_local_policy`
- `python_HJB`
- `python_KFE`
- `python_household`
- `python_stationary`
- `old_50_state_HJB_parity`
- `Beijing_household_parity`
- `MP2_empirical`
- `MP3_empirical`
- `annual_batch`
- `shocks`
- `transition`
- `dynamics`
- `IRF`
- `R5`
- `Results`

All non-scalar entries above must remain exactly zero for this helper. Do not hide unavailable counts behind a combined catch-all field.

### 5.5 Success artifact / no-overwrite

A future successful ten-case run must persist exactly one final success manifest inside the exclusive run root after all ten calls complete. The final manifest must include the ten typed rows and complete ledger.

Any manifest/failure artifact must be atomically reserved before first write and must fail closed if the path already exists.

It is acceptable for the helper to write a failure artifact on a failed future scientific call and a final manifest only on full success. It is not required to overwrite a single file progressively.

## 6. Infrastructure-only verification in this task

This task authorizes **zero protected `HANK3_FOC` calls**.

Before closeout, verify the remediated helper without executing the frozen scientific cases.

Required checks:

1. Python/static focused tests prove:
   - exact ten ids/order and numeric arrays remain unchanged;
   - protected call expression remains unchanged;
   - exact-junction guard remains intact;
   - fresh-root exclusive creation logic is present and fail-closed;
   - per-row source identity fields are present;
   - bounded protected-call `try/catch` records first-case error identity and stops rather than substitutes;
   - complete success/failure ledger fields are present;
   - no forbidden scientific/production imports or mutations are introduced.
2. MATLAB R2022b `checkcode(...,'-id')` on the remediated edge helper: zero findings required.
3. `python -m py_compile` for any new Python focused test/helper.
4. `git diff --check` PASS.

A no-science persistence/schema preflight is allowed only if it provably calls protected `HANK3_FOC` zero times and does not evaluate any frozen scientific case. Do not invoke the real scalar helper in a way that reaches its case loop.

Required acceptance marker:

`MP4B_RAW_VB_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_REMEDIATION_PASS`

## 7. Call budget and prohibitions

This task's complete computational budget:

- protected `HANK3_FOC`: `0`
- replacement scalar batch: `0`
- exact-junction smoke rerun: `0`
- MATLAB HJB/KFE/household/multi-province/stationary/GE: all `0`
- Python local-policy/HJB/KFE/household/stationary: all `0`
- old 50-state HJB parity / Beijing household parity: `0/0`
- MP2/MP3 empirical: `0/0`
- annual batch/shocks/transition/dynamics/IRF/R5/Results: all `0`

Do not modify:

- `src/ch5_two_asset_hank/economics.py`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
- faithful HJB/operator/KFE modules
- corrected/reference modules
- `exports/matlab_faithful_two_asset_ha.py`
- protected MATLAB source
- canonical input/data/cache
- MP2/MP3
- historical R5

Production/export mutation must remain exactly `0`.

## 8. Allowed repository changes

Allowed only:

- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`
- one narrowly scoped focused test for the helper evidence contract, preferably `tests/test_mp4b_hank3_foc_edge_helper_evidence_contract.py`
- bounded CURRENT roadmap status update if needed
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION_REPORT.md`

Do not modify the already PASSed exact-junction smoke helper unless static review proves a direct dependency is strictly necessary. Prefer leaving its accepted bytes unchanged.

## 9. Terminal semantics

PASS only if the four predecessor infrastructure/evidence defects are closed, all frozen scientific case/formula bytes remain unchanged, checkcode/tests/diff checks pass, and every protected/model/scientific call remains zero:

`MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION_PASS`

BLOCKED if the helper cannot satisfy this evidence contract without changing frozen scientific content or source-binding semantics:

`MP4B_HANK3_FOC_EDGE_HELPER_EVIDENCE_CONTRACT_INFRASTRUCTURE_REMEDIATION_BLOCKED`

This PASS is infrastructure-only. It does not establish raw-`pb` source semantics.

## 10. Required report and closeout

Report at minimum:

- terminal verdict;
- live continuity;
- predecessor/current helper SHA-256;
- exact helper signature after remediation;
- proof ten cases/formulas/call expression are unchanged;
- exact fresh-root ownership/no-overwrite design;
- exact first-error persistence design;
- success/failure artifact schemas;
- per-row path/SHA fields;
- complete call-ledger schema;
- focused tests and MATLAB checkcode;
- protected `HANK3_FOC` call count `0`;
- complete zero-call model ledger;
- production/export mutation `0`;
- forbidden-operation check;
- `git diff --check`;
- Git closeout;
- exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, clean worktree.

## 11. Successor boundary

On PASS recommend exactly one successor:

**publish a fresh replacement validation-only ten-case `HANK3_FOC` scalar edge-semantics task using the remediated helper bytes, with one scalar batch, exactly ten protected calls on full completion, reruns zero, all model calls zero, and no production/export mutation.**

That successor must not modify the helper after its preflight and must not rerun the exact-junction smoke.