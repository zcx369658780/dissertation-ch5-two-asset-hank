# CH5_TWO_ASSET_HANK_MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_PATH_EQUIVALENCE_REPAIR_AND_REPLACEMENT_FREEZE

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded validator / MATLAB validation-helper repairer

Owner: final scientific authority

## 1. Purpose

Resolve only the validation-infrastructure blocker from the predecessor task:

`MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_BLOCKED`

The predecessor correctly froze the raw-`Vb` defect target and completed the transfer-helper call-site impact audit, but its sole authorized validation-only MATLAB scalar batch stopped before any protected `HANK3_FOC` call because MATLAB resolved the protected helper through the physical `D:\MatlabProgram` target while the helper compared it textually against the logical `C:\MatlabProgram` junction path.

This successor must repair and independently validate only that logical/physical protected-root equivalence guard, then execute exactly one replacement validation-only `HANK3_FOC` scalar edge diagnostic to freeze raw-`pb` source semantics.

This task does **not** authorize production-oracle repair, local-policy parity, HJB, KFE, household solving, multi-province stationary execution, MP2/MP3 empirical execution, shocks, dynamics, or Results.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decision/adjudication documents
- all prior MP4B reports/tasks
- `docs/CH5_TWO_ASSET_HANK_MP4B_ACCEPTED_ORACLE_RAW_VB_SOURCE_ORDER_REPAIR_AND_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`
- `validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`
- `validators/multi_province/matlab/mp4b_path_equivalence_smoke.m`

Preserve predecessor markers:

- `MP4B_RAW_VB_SOURCE_ORDER_REPAIR_TARGET_FROZEN`
- `MP4B_RAW_VB_TRANSFER_HELPER_CALLSITE_IMPACT_AUDIT_PASS`
- `MP4B_FIRST_BEIJING_HJB_OFFENDING_LIQUID_DERIVATIVE_EXACTLY_LOCALIZED`
- `MP4B_HJB_FIRST_DIVERGENCE_SOURCE_SEMANTICS_DIAGNOSIS_COMPLETE`

Primary authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Protected source identity:

- `HANK3_FOC.m` SHA-256 `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`

Accepted standalone remains unchanged in this task:

`276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

## 3. Live continuity

Expected execution-start parent:

`e46e1d5047ef2b6fc4667428e22527ebe8c668d5`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the predecessor blocker commit;
3. require clean worktree;
4. verify protected MATLAB/rule blobs and predecessor helper/report identities;
5. verify no production-oracle, modular faithful policy/HJB/KFE, corrected/reference, canonical data/cache, preserved MATLAB baseline, or historical R5 drift.

Any continuity failure => stop before mutation.

## 4. Exact protected-root equivalence contract

The controlling local-file safety rule and already accepted MP4B path-smoke evidence establish one finite protected storage pair:

- logical root: `C:\MatlabProgram`
- physical junction target: `D:\MatlabProgram`

For this model the only allowed exact model-root pair is:

- logical model root: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`
- physical model root: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

The repaired scalar helper must accept a resolved `HANK3_FOC.m` only if all of the following hold:

1. both exact logical and physical model roots exist;
2. the logical `C:\MatlabProgram` root is independently verified to be a junction whose target is exactly `D:\MatlabProgram`;
3. logical and physical `HANK3_FOC.m` both exist;
4. both files hash exactly to the protected SHA above;
5. MATLAB `which('HANK3_FOC')` resolves to a file whose **parent directory** is exactly one of the two normalized allowed model roots;
6. the resolved file itself hashes exactly to the protected SHA;
7. no sibling model directory, broader `C:\MatlabProgram`, broader `D:\MatlabProgram`, unrelated D-root, or path outside the exact pair is accepted.

Do not weaken this to `startsWith`, substring matching, arbitrary canonicalization, or broad trust of `D:\MatlabProgram`.

The existing `mp4b_path_equivalence_smoke.m` pattern may be reused conceptually: finite `allowed=[normalize_root(logical_root),normalize_root(physical_root)]` and exact parent-root membership.

Required static marker:

`MP4B_RAW_VB_HANK3_FOC_PATH_EQUIVALENCE_GUARD_STATIC_REVIEW_PASS`

## 5. Validation-helper repair boundary

Allowed mutation is limited to:

`validators/multi_province/matlab/mp4b_raw_vb_hank3_foc_edge_diagnostic.m`

and, only if necessary, one narrowly named validation-only guard-smoke helper under the same MATLAB validator directory.

The scalar helper may change its signature to receive both exact logical and physical model roots explicitly. Do not infer the physical model root from string replacement inside scientific logic.

The frozen ten scalar cases, `pa`, `pb`, `a`, `chi0`, `chi1`, protected `HANK3_FOC` call, ratio classification, output classification, and source formulas must remain scientifically unchanged.

### Persistence review

Before consuming the replacement scalar batch, independently review output persistence for strict no-overwrite and MATLAB R2022b compatibility.

Do not use `fopen(...,'x')` because prior project evidence established incompatibility in the local R2022b environment.

Preferred validation-only pattern:

1. require target absent;
2. atomically reserve the exact new file with `java.io.File(...).createNewFile()`;
3. fail if reservation returns false;
4. open only that newly created empty file with `'w'`;
5. never truncate an existing file.

Required combined helper marker:

`MP4B_RAW_VB_HANK3_FOC_DIAGNOSTIC_HELPER_STATIC_REVIEW_PASS`

## 6. Non-scientific guard smoke before scalar budget

Before the replacement scalar diagnostic, run at most one **guard-only** MATLAB smoke that:

- verifies the exact logical/physical model-root pair;
- `addpath`s only the logical protected model root;
- resolves `which('HANK3_FOC')`;
- applies the same finite-pair guard used by the scalar helper;
- verifies protected SHA identity;
- deliberately tests that at least one unrelated/sibling root representation is rejected by the guard logic without changing MATLAB path to an unsafe source;
- calls `HANK3_FOC` exactly zero times;
- calls HJB/KFE/household/multi-province functions exactly zero times;
- persists one compact no-overwrite manifest under a fresh `D:\ProjectTemp` root.

Required marker:

`MP4B_RAW_VB_HANK3_FOC_PATH_EQUIVALENCE_SMOKE_PASS`

If the smoke fails, stop before the replacement scalar batch. Do not repair-and-smoke-rerun inside this task.

## 7. Replacement raw-pb edge diagnostic

Only after Sections 4-6 PASS, execute exactly one replacement validation-only MATLAB scalar diagnostic batch.

Use the exact predecessor ten frozen cases unchanged:

- localized `BB`, `BF`, `FB`, `FF` tuples from Beijing iteration 5;
- positive `pb`;
- negative `pb`;
- zero `pb` with positive `pa`;
- zero `pb` with negative `pa`;
- zero `pa`, zero `pb`;
- `a=0` with negative `pb`.

For every case persist:

- case id;
- `pa`, `pb`, `a`, `chi0`, `chi1`;
- MATLAB `pa./pb` classification and value (`finite`, `+Inf`, `-Inf`, `NaN`);
- protected `HANK3_FOC` output classification and value;
- exact protected source SHA;
- exact resolved helper path;
- call ledger.

Do not add epsilon, clipping, raw-`pb` positivity guards, alternate formulas, or post-hoc case changes.

Required successful marker:

`MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`

The replacement diagnostic invocation is the sole scalar scientific/source-semantics batch authorized by this successor. No second batch is authorized.

## 8. Call budget

Authorized:

- guard-only MATLAB smoke: at most `1`, with `HANK3_FOC` calls `0`;
- replacement MATLAB scalar diagnostic: exactly `1` if the smoke passes;
- protected `HANK3_FOC` calls: exactly the frozen ten cases inside that one batch.

All other scientific/model calls must remain zero:

- MATLAB HJB/KFE/household/multi-province/stationary: `0`;
- Python local-policy/HJB/KFE/household/stationary: `0`;
- 50-state HJB parity: `0`;
- Beijing household parity: `0`;
- MP2 empirical: `0`;
- MP3 empirical/controller: `0`;
- 2010-2023 annual batch: `0`;
- shocks/transition/dynamics/IRF/R5/Results: `0`.

No production/export mutation is authorized.

## 9. Allowed repository changes

Allowed only:

- repaired validation-only MATLAB scalar helper;
- optional one validation-only path-guard smoke helper;
- focused static/helper tests if needed;
- bounded CURRENT roadmap status update;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_PATH_EQUIVALENCE_REPAIR_AND_REPLACEMENT_FREEZE_REPORT.md`

Do not modify:

- `src/ch5_two_asset_hank/economics.py`;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`;
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py`;
- `src/ch5_two_asset_hank/matlab_faithful_operator.py`;
- `src/ch5_two_asset_hank/matlab_faithful_kfe.py`;
- corrected/reference modules;
- `exports/matlab_faithful_two_asset_ha.py`;
- MP1/MP2/MP3 production code;
- canonical inputs/data/cache;
- protected MATLAB source;
- preserved MATLAB 2009 run;
- controlling rules;
- historical R5.

## 10. Acceptance and terminal

PASS requires:

1. live continuity exact;
2. exact logical/physical junction relation independently verified;
3. finite-pair path guard static review PASS;
4. strict no-overwrite/R2022b persistence review PASS;
5. guard-only smoke PASS with zero `HANK3_FOC` calls;
6. one replacement scalar batch completes all ten cases;
7. protected `HANK3_FOC` is actually called exactly ten times;
8. all outputs/classifications are persisted without formula/case/tolerance changes;
9. all forbidden model/scientific calls remain zero;
10. production/export mutation count remains zero.

Terminal on PASS:

`MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_PATH_EQUIVALENCE_REPAIR_AND_REPLACEMENT_FREEZE_PASS`

Terminal on infrastructure/helper failure:

`MP4B_RAW_VB_HANK3_FOC_EDGE_DIAGNOSTIC_REPLACEMENT_BLOCKED`

A PASS is only a source-edge-semantics freeze. It does not accept any oracle repair or household parity.

## 11. Successor boundary

On PASS recommend exactly one successor:

**resume the previously blocked targeted accepted-oracle raw-`Vb` source-order repair and household parity revalidation, consuming no empirical 31-province stationary calls.**

That successor must reuse this frozen scalar edge-semantics artifact and must not rerun this scalar diagnostic unless separately authorized.

On BLOCKED recommend only the minimum validation-helper/evidence correction required; do not advance to production repair.

## 12. Required report and closeout

Report at minimum:

1. terminal verdict;
2. live continuity;
3. protected source hash;
4. junction and exact allowed-root evidence;
5. path-guard repair diff/hash;
6. persistence/no-overwrite review;
7. guard-smoke root/manifest/hash and zero-call ledger;
8. replacement scalar root/manifest/hash;
9. complete ten-case table with ratio/output classifications and values;
10. exact `HANK3_FOC` call count;
11. all model/scientific calls zero;
12. production/export mutation zero;
13. tests/checkcode/compile/diff-check;
14. forbidden-operation check;
15. Git closeout;
16. exactly one recommended next gate.

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back all changed paths. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.