# MP4C 2018 original-parameter observable prefix replay

Date: 2026-09-08. Repository: zcx369658780/dissertation-ch5-two-asset-hank.
Issuer: ChatGPT Reviewer under Owner standing authority for bounded unchanged-science diagnosis.
Accepted predecessor: 8ef4a2a6c6df2ece2cda665890201c27dd083265; PRICE_BOUNDARY_AUDIT_ACCEPTED_WITH_PARTIAL_EVIDENCE__ORIGINAL_ALGORITHM_RETAINED.
Record fresh live main at execution; it must still designate this task active.

## 1. One complete work unit

Instrument, synthetically test, execute ONE original-input Python corrected-2018 prefix, persist missing runtime operands/events, compare its observed prefix with the saved PID67056 path, and report raw provincial prices and adaptation. The saved-evidence audit is complete; do not repeat its annual panel extraction. A fresh bounded run is justified only because actual failed-path ra0/wt0/mt/current household Lt and the complete turn23 controller state were not saved.

Primary questions: why is Anhui's lagged rah still .09 despite GovInv adjustment; what raw ra0/wt0 and mt generated the relevant clipped prices; which province determines the turn23 gate; and at what exact stage does the original run fail? Capture all 31 provinces, not only Anhui. Do not presume high rah is the cause.

Owner retains the original MATLAB-faithful algorithm, a_bar and current calibration. This is an OBSERVATION replay, not a .07/.065 rate experiment, calibration change, stability patch, annual completion attempt or genuine-dynamics task. D1–D3 remains unadopted/deferred. No new scientific choice is made by adding observation.

## 2. Workspace and narrow reading

Existing worktree: D:\ProjectTemp\ch5-astra-local-doc-sync-20260907-001.
New branch: codex/ch5-2018-observable-prefix-replay-20260908, from valid fresh main. Preserve the audit branch and original D:\ResearchCode\dissertation-ch5-two-asset-hank checkout/untracked files; no reset/clean/stash/force-push. Do not work on Zotero or deep-learning-hank.
Read AGENTS, rule index, current status, relevant safety/MATLAB/Python rules, this task and:
- docs/CH5_MP4C_PROVINCE_PRICE_BOUNDARY_AND_ADAPTATION_AUDIT_{REPORT,ACCEPTANCE}.md
- reports/province_price_boundary_audit_20260908/{source_map,source_identity,consumed_inputs,summary,excluded_attempt}.json and the existing failed_path_prices/controller_timeline tables.
- validators/multi_province/mp4c_python_annual_production.py, its source-bound input/household dependencies, and src/ch5_two_asset_hank/multi_province/{stationary_runtime,steady_state,one_turn,firm,capital_allocation}.py.
- the existing PID67056 launch/source receipts needed to identify the actual production route; no full historical re-audit.

Historical capture root, read-only:
D:\ProjectTemp\ch5-mp4c-2018-final-production-path-faithful-durable-execution-20260903-001.
Accepted audit root, read-only:
D:\ProjectTemp\ch5-province-price-boundary-audit-20260908-002.
Original corrected input:
D:\ProjectTemp\ch5-mp4c-owner-a-corrected-2009-2022-8worker-20260902-001\year_2018\calendar_2018_matlab_runtime_cache_input.json
SHA256 F84D25FD49A76229CA49958764D1167CAA56FB68CF99A4ED7B20C508812E6ED0.
Use the byte-identical source-designated file, not the uploaded calibration-cache MAT or rounded report values. Preserve calendar2018 / rolling entry10 / PLM vintage19 / calendar row19 and GDPx1000/CAPx1000/POPx100. Do not rerun data estimation.

Production worker at predecessor: blob7473e04418744d745000afb21d84588273cc5bca; online runtime blob8717cfa759948bd1ad3c8cd788f8f4736f250598; post-loop adapter blob0033baee136c0328e80ffb8b794a88d4405c976c. Frozen export blob9e7dc9556a2b76811e78f89999abecc045886106. Protected MATLAB HJB SHA256049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE. Bind the other executed dependencies through the accepted source_map and receipts once, recording raw versus LF identities accurately. Material source/input differences block the dependent scientific invocation; unrelated main docs do not.

## 3. Preserve the executed science

Use the unchanged original annual-worker/online-controller computation path with an external observer. Preferred implementation is read-only call/return/local-state observation or wrappers that delegate exactly once to the bound original function. In-memory dispatch instrumentation may be used only to add capture/counters and the external prefix stop. It must not change argument values, arrays, ordering, equations, branching, exceptions, native initialization or returned scientific objects. Synthetic tests and a narrow instrumentation/source map must establish these properties before launch. No production file edits and no reimplemented approximate controller.

Retain original source-native initialization for EACH household. Do not warm-start from previous HJB/annual endpoints, use the later MATLAB exact-common-initialization MAT, carry V between calls, or substitute the fixed call725 scalar binding for the evolving original outer state. That MAT belongs to a different isolated experiment. Compare any observed call725 input to its prior binding; do not force equality.

Retain rho=.05, gamma=2, phi=5, chi0=.1, chi1=2, a_bar=1e-6, 20x20x2 grids, Delta1000, crit1e-7, HJBmaxit100, drift tolerance1e-12; original firm bounds, portfolio terms, taxes, rates, depreciation, adaptive thresholds and 500-outer-loop source ceiling remain unchanged. The external prefix guard below stops this run well before that ceiling; it is not a new convergence rule. In particular do not set the source household ceiling to500, normalize investment weights, add local capital terms or replace At*N with At+Bt.

The existing source-faithful post-loop adapter calls KFE even when HJB returns converged=False. Preserve that behavior: do not insert a new HJB-convergence gate or fallback. Capture the HJB object before KFE so that an actual KFE exception cannot destroy its evidence. A thrown numerical exception terminates the run; a normally returned false flag follows the original path. Do not force success or continue after an exception.

## 4. Durable observation before failure

Use unique files with capture phase/outer step/province indices/call ID; copy mutable values at the observation point rather than retaining references which later mutate. Persist before expensive downstream calls. Round-trip binary64 JSON or NPZ, exact sparse support/shape/order; nonfinite values must be explicitly represented and preserved, not coerced. Large arrays remain external, small scalar tables/receipts enter Git.

Required capture:
A. Before the FIRST household of EVERY reached turn, save the complete 31-province common entering state, tKN reference, actual global params, phi/migration operands and source/input identity. This must happen at turn24 before any provincial failure, not opportunistically one province at a time. Preserve the full state field set, distinguishing missing values from zeros.
B. Before every household, record input and native initializer entry. Count root and HJB calls before entry, including failures. Save grid/native initial V0/l0 identity and per-call HJB termination statistics; save final HJB controls/operator before KFE. Per-iteration HJB scalar diagnostics may be observed without extra evaluator calls, but do not recreate the long stagewise HJB experiment or run extra one-step comparisons. Save returned household aggregates including the CURRENT household Lt, At, Bt and convergence flag.
C. At each completed batch, persist the full household-output batch and exact capital/migration/wage inputs. Around EACH firm call, save every consumed province/parameter/kt_supply/lt_supply operand, especially actual Lt_prev/current household Lt, carried rk, mt inputs, pit and previous values. Save the actual returned FirmResult (including ra0/wt0/ra/wjt/rk/mt/profit/Corptax), not a reconstructed stand-in. Capture tax adjustments and clipping branch directly where available; derived attribution must be labelled if internal branches are not observed.
D. Before/after diagnostics/adaptation, save full pre-adaptation states, all nk_gap/yt_gap/tKN values, exact stopping components, global max and all tied deciding provinces, convergence exit, the actual adaptive action records, and full next states. Persist immediately after each completed turn. Distinguish true captured action events, captured before/after state and separately recomputed expectations.
E. On any exception, save exception type/message/traceback and the last durable stage, counters, source identities, current entry/state/HJB evidence. Do not perform an extra KFE/HJB to improve the failure report. Missing returned objects remain missing.

## 5. Finite new run budget and stop rules

ONE Python scientific process, one worker, OMP_NUM_THREADS/MKL_NUM_THREADS/OPENBLAS_NUM_THREADS/NUMEXPR_NUM_THREADS all1. Record Python/NumPy/SciPy/platform/BLAS and historical environment compatibility; no solver installation/switch to chase matching results.
Stop at the FIRST original fatal exception, original early convergence, timeout, or immediately after attempted household call725 finishes, whichever comes first. At most24 entered outer turns and at most23 completed one-turn/firm/controller stages. If call725 returns normally, stop before call726 and before any remaining turn24 province or firm stage. Mark an intentional guard stop as PREFIX_LIMIT, not annual success or numerical failure.

Hard scientific ceilings, counting entries not only successful returns:
- household native-initialization attempts and household HJB attempts: each <=725;
- original HJB value-update/direct solves: <=100 per household, <=72,500 total;
- original initializer labor-root entries: <=800 per household, <=580,000 total; only the unchanged initializer may use them;
- original KFE direct solves: <=1 per household, <=725 total;
- source-ordered full one-turn arithmetic and adaptive-controller decisions: each <=23; firm calls<=713;
- independent diagnostic solves, condition estimation, extra policy/HJB replays, alternate years/batches, R/PLM, shocks/dynamics/IRF/Results and MATLAB startups:0.
Counts are upper bounds, not targets. Root residual/bracketing evaluations are reported separately and keep source limits. Count failed attempts even if the existing worker's success counter has not incremented.

No scientific restart/retry after the first initializer/root/HJB/KFE/one-turn entry. One external launch retry is allowed ONLY when receipts establish zero scientific entry and a diagnosed launch/environment failure; do not treat numerical mismatch/nonconvergence as engineering failure. Repair postprocessing against durable outputs rather than rerunning science. Ordinary synthetic/static checks may repeat in scope.
Scientific wall-time<=3h, full task<=4h. Preserve partial evidence on timeout/resource failure. No hidden resume or continuation past the prefix. Check disk space for expected output at the real target path; do not inspect unrelated drives.

## 6. Comparison and interpretation

Compare every available new household-entry scalar with the corresponding old PID67056 ledger and the accepted derived timeline where comparable, in causal order. Exact province/step/order/categories; continuous abs(x-y)<=128*eps64*max(1,abs(x),abs(y)); no tolerance change. Report first mismatch, maximum errors and prefix length. Do not infer old raw values from a diverged new path. A valid numerical prefix mismatch is a finding, not a reason to retry; the single bounded run may finish within its same ceiling, but all subsequent objects are explicitly NEW-PATH evidence rather than historical gap-filling. Stop on unsafe nonfinite/fatal behavior.

For each province list captured raw excursions, clipped contacts, actual rah/w, initialized contacts separately, exact/near flags and observed denominators. Do not apply wjt bounds to w or call rah's comparison with firm limits a native constraint. No .07 pass/fail threshold is introduced.
Trace Anhui turn22 ra -> turn23 rah -> turn24 input, and distinguish turn23 firm-used from post-adaptation Zt/GovInv. Trace Guizhou and other gate-determining provinces through turns12–23. Explain actual price decomposition rk-delta+divrate, mt and Y/K and current private versus state capital using captured same-stage operands. Check source identities algebraically, not with additional firm/solver calls. Examine the turn4 low-output Zt reset as well as later high prices; do not assume the initial estimate was too high.

Report separately: reproduced-prefix status, whether the original exception recurs and where, newly captured price/controller facts, residual uncertainty about why HJB/KFE fails. A price association or successful replay is not a causal rate experiment, convergence repair, generator validation or annual acceptance. Preserve the separate exact-common-initialization MATLAB143/Python500 finding and known generator/P32 issues.

## 7. Allowed paths and delivery

Allowed repository writes:
- validators/multi_province/observable_prefix_replay/
- tests/test_mp4c_observable_prefix_replay.py
- reports/2018_observable_prefix_replay_20260908/ (small summaries/tables/logs/receipts)
- docs/CH5_MP4C_2018_ORIGINAL_PARAMETER_OBSERVABLE_PREFIX_REPLAY_REPORT.md

External fresh root: D:\ProjectTemp\ch5-2018-observable-prefix-replay-20260908-001; choose unused suffix, never overwrite prior evidence. No private workbooks/calibration MAT/large arrays uploaded to Git.

Synthetic tests must cover simultaneous full-state capture before first provincial call, argument/return identity and no array mutation, captured current household Lt versus old firm Lt, phase/lag labels, original false-HJB-to-KFE path, failure persistence, counters including failed calls, stop before call726, no import-time execution, and no-op scientific delegation exactly once. Do not run actual models under a test label.
Deliver one report, full-state/firm/controller capture index, old/new prefix comparison, 31-province price tables, focused interpretation, complete call ledger, test logs, and a finite source/input/output manifest with independent readback. Keep the scientific run's files immutable while repairing permitted draft scripts/reports. Nonfinite/partial/exception runs are publishable, not PASS.

Completion: OBSERVABLE_PREFIX_CAPTURE_COMPLETE or PARTIAL_EVIDENCE. Reproduction: MATCHED_PREFIX_AND_FAILURE_REPRODUCED / PREFIX_DIVERGED / LIMIT_REACHED_WITHOUT_ORIGINAL_FAILURE / EARLIER_EXCEPTION / PRELAUNCH_BLOCKED, with exact scope. Results eligibility=FALSE.
Stage explicit allowed paths, commit and non-force push branch, verify remote SHA once. Do not merge main or start a successor. Return key price/gate findings, coverage, reproduction result, actual counts, report path and commit. Any .07 sensitivity/calibration or algorithm change needs a separate scientific decision and task.
