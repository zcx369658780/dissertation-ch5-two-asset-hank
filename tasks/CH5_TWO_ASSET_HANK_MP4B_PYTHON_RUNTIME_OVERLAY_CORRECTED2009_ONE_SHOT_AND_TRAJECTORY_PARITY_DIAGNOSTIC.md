# CH5_TWO_ASSET_HANK_MP4B_PYTHON_RUNTIME_OVERLAY_CORRECTED2009_ONE_SHOT_AND_TRAJECTORY_PARITY_DIAGNOSTIC

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / Python one-shot executor / read-only trajectory parity diagnostician

Owner: final scientific authority

## 1. Authority basis and L3 delegated decision

Accepted predecessor execution:

`MP4B_CANONICAL_INPUT_BINDING_REMEDIATION_AND_MATLAB_RUNTIME_PARITY_OVERLAY_PASS`

Execution commit:

`61f400f3a8f32413ad9c68da1c5ad82ed7a0d0a1`

Accepted markers:

- `MP4B_PRIMARY_SOURCE_CANONICAL_BINDING_PRESERVED`
- `MP4B_DUAL_INPUT_AUTHORITY_CONTRACT_FROZEN`
- `MP4B_MATLAB_CACHE_RUNTIME_PARITY_OVERLAY_PREPARED`
- `MP4B_CACHE_OVERLAY_VALIDATION_ONLY_NO_SCIENTIFIC_DEFAULT_CHANGE`

The Owner has standing delegation to L3 for bounded mechanical diagnostic/rerun decisions that introduce no new scientific choice. This task uses that delegation to authorize exactly one Python corrected-calendar-2009 stationary execution under the validation-only `MATLAB_CACHE_RUNTIME_PARITY_OVERLAY` binding.

This task does **not** alter the primary scientific canonical input. It does **not** change any equation, parameter, grid, numerical algorithm, controller threshold, tolerance, update order, household implementation, MP2/MP3 implementation, comparator contract, shock design, or Results claim.

The scientific question is deliberately narrow:

> If Python receives the exact 31-province MATLAB-cache binary64 `initialized_zt` values while every other canonical field is unchanged, how much of the previously observed MATLAB/Python corrected-2009 trajectory divergence disappears, shifts, or remains?

## 2. Required live continuity

Required execution-start predecessor:

`61f400f3a8f32413ad9c68da1c5ad82ed7a0d0a1`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `61f400f3a8f32413ad9c68da1c5ad82ed7a0d0a1`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - predecessor canonical-binding remediation task/report;
   - predecessor source-binding census task/report;
   - accepted L3 instrumented MATLAB chronology task/report;
   - post-trace same-input source-layer adjudication task/report;
   - comparator representation-remediation task/report;
   - accepted Python canonical corrected-2009 stationary report and its durable artifacts;
   - current `validators/multi_province/mp4b_canonical_input_binding.py`;
   - current `validators/multi_province/mp4b_python_empirical.py`;
   - current `validators/multi_province/mp4b_compare_preserved_matlab_python_final_state.py`.

Any authority/identity/readability failure before the Python stationary call => stop with zero science and report BLOCKED.

## 3. Hard execution and retry budget

Authorized maximums:

- MATLAB processes: `0`;
- MATLAB stationary/HJB/KFE/household/firm/controller/checkcode: `0`;
- Python corrected-calendar-2009 stationary top-level execution: `1`;
- Python stationary reruns: `0`;
- any additional Python stationary year: `0`;
- separate standalone Python household/HJB/KFE scientific replay: `0`;
- MP2/MP3 standalone replay outside the one stationary execution: `0`;
- qualified read-only final-state comparator invocation: maximum `1`;
- read-only chronology/alignment parser over already durable outputs: allowed;
- other year / annual batch: `0`;
- shocks / AR(1) / transition / dynamics / IRF: `0`;
- historical R5 / Results: `0`.

Once the single Python stationary execution begins there is no repair-and-rerun lane. Any failure must be preserved and reported exactly; do not patch and retry in this task.

## 4. Immutable scientific and validation identities

Require and do not modify the frozen scientific identities already accepted by MP4B, including at minimum:

- `src/ch5_two_asset_hank/economics.py` SHA-256 `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` SHA-256 `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py` SHA-256 `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py` SHA-256 `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py` SHA-256 `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py` SHA-256 `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map SHA-256 `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter SHA-256 `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver SHA-256 `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- comparator contract SHA-256 `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map SHA-256 `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- final-state comparator helper Git blob `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Also require current validation-only binding helper Git blob:

`validators/multi_province/mp4b_canonical_input_binding.py` = `20123513f232cb2d3cca1264565837e4882ea19f`.

Current Python one-shot driver is read-only for this task:

`validators/multi_province/mp4b_python_empirical.py` Git blob `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`.

Do not modify either scientific code or the existing one-shot driver.

## 5. Binding authorities and immutable source inputs

Primary-source canonical input remains scientific authority:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Accepted derived MATLAB cache SHA-256:

`923CC9E592C14B320C624509A0B498DBCC7D2533F77F0E4B4793521B10849E9A`

Accepted source-binding census:

`D:\ProjectTemp\ch5-mp4b-l3-readonly-source-binding-and-early-household-review-20260901-001\initial_zt_31province_binding_census.json`

The predecessor remediation established exactly:

- 24 equal `initialized_zt` rows;
- 7 cache/canonical representation differences;
- five 1-ULP differences;
- two 2-ULP differences;
- zero non-`initialized_zt` field differences in overlay mode.

`PRIMARY_SOURCE_CANONICAL` remains the default scientific mode before and after this task.

`MATLAB_CACHE_RUNTIME_PARITY_OVERLAY` is validation-only and must be named explicitly in every run/binding manifest.

## 6. Pre-science overlay materialization — no model call

Before the stationary run, create one fresh no-overwrite binding root under `D:\ProjectTemp`, preferred:

`D:\ProjectTemp\ch5-mp4b-python-runtime-overlay-input-20260901-001`

If it exists, choose the next deterministic fresh suffix. Do not overwrite/delete/reuse.

Using only the accepted validation helper `mp4b_canonical_input_binding.py`:

1. construct `MATLAB_CACHE_RUNTIME_PARITY_OVERLAY` explicitly from the immutable canonical plus accepted census evidence;
2. persist a canonical-shaped runtime input JSON containing `binding.object` only, suitable for the existing `mp4b_python_empirical.py` one-shot driver;
3. separately persist a runtime binding manifest recording:
   - binding mode;
   - primary canonical path/SHA;
   - MATLAB cache SHA;
   - census evidence path/SHA;
   - materialized runtime-input SHA;
   - exact 31-province order;
   - 24/7 and 5x1-ULP/2x2-ULP invariants;
   - exact list of seven changed province indices/names;
   - proof that all non-`initialized_zt` fields are identical to canonical;
   - zero scientific/model calls.

The materialized overlay file is validation evidence, not a new scientific canonical artifact.

Do not edit the original canonical JSON or MAT cache.

## 7. Mandatory preflight before the one-shot

Before science:

- run `python -m py_compile` on the binding helper and focused binding test;
- run exactly the focused binding test bundle `python -m pytest -q tests/test_mp4b_canonical_input_binding.py` and require all tests PASS;
- verify all scientific/runtime identities in Section 4;
- verify the materialized overlay file changes only `initialized_zt`;
- verify every overlay `initialized_zt` binary64 hex equals accepted MATLAB cache evidence;
- verify the seven changed rows and ULP pattern exactly;
- verify no scientific model call has occurred yet;
- check free space on the physical output drive;
- persist a pre-science gate JSON.

Do not run a separate household/HJB/KFE smoke.

## 8. Exactly one Python corrected-2009 stationary execution

Create exactly one fresh no-overwrite scientific run root, preferred:

`D:\ProjectTemp\ch5-mp4b-python-runtime-overlay-one-shot-20260901-001`

The root must not exist before the scientific driver starts. If it already exists before science, choose the next deterministic suffix and freeze it in the pre-science manifest.

Invoke the existing immutable one-shot driver exactly once with:

1. the materialized canonical-shaped `MATLAB_CACHE_RUNTIME_PARITY_OVERLAY` JSON;
2. the fresh scientific run root.

Do not edit the driver to make this work. Do not rerun on any outcome.

The existing scientific algorithm remains unchanged, including:

- 31-province synchronous/Jacobi household batch semantics;
- household oracle and source-postloop semantics;
- migration, productive `At*N` capital allocation, firm, wage, Taylor/fiscal/controller logic;
- `|Yt/Yt0-1| > 0.01` Zt-reset threshold;
- `ra < ramin+0.02` low-return controller threshold;
- source `0.9/1.1` controller multipliers;
- `0.6/0.4` tKNratio damping;
- HJB/KFE/grid/tolerance settings;
- outer acceptance and max-iteration rules.

No threshold or tolerance adjustment is authorized here.

If execution fails, preserve the exact partial durable evidence and stop. No repair and no second run.

## 9. Post-run read-only comparison hierarchy

If the Python run produces a terminal artifact, perform read-only diagnostics only. Do not rerun either model.

### 9.1 Compare against the previous canonical Python one-shot

Read-only previous canonical Python root:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require its immutable manifest/terminal identities already accepted:

- manifest `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- terminal `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`.

Report exactly what changes when only the seven `initialized_zt` representations change:

- outer iteration count;
- 31/31 final household convergence;
- wage upper/lower counts;
- ra upper/lower counts;
- low/high controller action counts if durable history supports them;
- Zt reset counts and coordinates if durable history supports them;
- final 20-field state differences;
- national Ct/At/Bt/Yt;
- first turn/layer/category at which overlay-run Python differs from canonical-run Python.

### 9.2 Compare against admissible instrumented MATLAB chronology

Read-only instrumented MATLAB root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-l3-delegated-rerun-20260831-001`

Require at minimum:

- trace MAT SHA-256 `6C4AEC5992A59C1C8D7859DEF082E5711864789CAE009FD1E5CC2B78B3F6A520`
- instrumented final state SHA-256 `D2F4B225AC362A6805AAD1CD065AFA87DA32551833E5FC867BA3A37C9FD0B8F0`
- baseline admissibility SHA-256 `68C9F396C57D385E72E6A46D94A53F4196F7E9BFBC84A6EAF66AB1FF64A0866B`
- accepted chronology V5 SHA-256 `A64172CC32EC7295297CED6DFE27B52CE51B5EAD64AD82453C60EBE0152965C3`.

Because overlay mode is designed specifically to remove the 7 initial-Zt representation differences, first require turn-1 entry `Zt` to be bitwise equal MATLAB/Python for all 31 provinces. If not, classify binding failure and do not interpret downstream parity.

Then locate separately:

1. earliest remaining bitwise/nonzero continuous difference;
2. earliest household-output difference;
3. earliest firm/output difference;
4. earliest categorical Zt-reset divergence;
5. earliest low/high controller-action divergence;
6. whether turn-8 Shanghai/Qinghai reset divergence remains;
7. whether turn-154 Zhejiang MATLAB-only low action remains;
8. whether final wage-boundary category mismatch remains.

Do not infer causality solely from chronology.

## 10. One qualified final-state comparator invocation

If the Python terminal contains a complete 31-province final state, authorize at most one read-only invocation of the already accepted comparator helper:

`validators/multi_province/mp4b_compare_preserved_matlab_python_final_state.py`

Git blob:

`cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Compare only:

- preserved corrected-2009 MATLAB stationary output at the accepted immutable root;
- the new overlay-run Python terminal;
- accepted MATLAB terminal status for outer-turn count.

Persist a fresh no-overwrite comparator artifact. Do not widen or change any comparison formula, field map, province projection, or tolerance contract.

The comparator invocation budget is `1`; rerun is `0`.

## 11. Pre-frozen diagnostic classifications

Use the strongest supported classification, without forcing a desired result.

### A. Overlay removes the known initial representation divergence and materially improves trajectory agreement

`MP4B_RUNTIME_OVERLAY_TRAJECTORY_PARITY_MATERIAL_IMPROVEMENT`

Use only if turn-1 31-province Zt becomes bitwise identical and at least one previously accepted material/categorical mismatch is eliminated or shifted materially later, with evidence.

### B. Overlay removes initial representation divergence but does not materially improve later trajectory

`MP4B_RUNTIME_OVERLAY_INITIAL_BINDING_EQUALIZED__LATER_DIVERGENCE_PERSISTS`

### C. Overlay creates a complete chronology/final-state parity candidate under existing frozen contracts

`MP4B_RUNTIME_OVERLAY_CORRECTED2009_STATIONARY_PARITY_CANDIDATE__FORMAL_L3_ACCEPTANCE_REQUIRED`

Use only if all available frozen trajectory categories align and the single qualified final-state comparison shows no material mismatch under existing contracts. This task itself does not silently broaden acceptance criteria.

### D. Binding/preflight/execution failure

`MP4B_RUNTIME_OVERLAY_CORRECTED2009_ONE_SHOT_BLOCKED_NO_RERUN`

Fault remaining unattributed is not a blocker if the one-shot and diagnostics completed.

## 12. Scientific interpretation limits

This task may establish what effect the 7 ULP-level initial-Zt representation changes have on the corrected-2009 execution trajectory.

It may not establish that the MATLAB cache is scientifically superior to primary-source recomputation.

It may not change the default scientific binding away from `PRIMARY_SOURCE_CANONICAL`.

It may not change controller thresholds or tolerances.

It may not start multi-year runs, AR(1), shocks, transition, IRF, R5, or Results.

## 13. External evidence requirements

Use fresh no-overwrite external evidence roots. Persist at minimum:

- `runtime_overlay_input.json`
- `runtime_overlay_binding_manifest.json`
- `pre_science_gate.json`
- exact scientific launcher command
- Python run manifest/terminal/history generated by the existing driver
- `overlay_vs_canonical_python_trajectory.json`
- `overlay_python_vs_matlab_chronology.json`
- one qualified final-state comparison artifact if eligible
- `diagnostic_manifest.json` with sizes/SHA-256.

Large/raw traces remain external and must not be committed to Git.

## 14. Repository change boundary

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_RUNTIME_OVERLAY_CORRECTED2009_ONE_SHOT_AND_TRAJECTORY_PARITY_DIAGNOSTIC_REPORT.md`

Allowed repository mutation:

- required report only;
- at most one strictly necessary CURRENT status/roadmap line if truly required.

Do not modify:

- `src/` scientific code;
- existing validators/drivers/comparator;
- canonical input;
- MATLAB source;
- tests;
- project rules;
- prior reports/tasks.

## 15. Closeout

At completion report:

- exact terminal verdict;
- strongest diagnostic classification;
- live continuity;
- overlay binding identities and 24/7 ULP invariants;
- pre-science gate;
- exact scientific call ledger;
- no-rerun evidence;
- Python overlay-run convergence and outer-turn count;
- overlay vs canonical Python trajectory differences;
- overlay Python vs MATLAB chronology differences;
- turn-8 Shanghai/Qinghai disposition;
- turn-154 Zhejiang disposition;
- final boundary-category disposition;
- qualified comparator result if eligible;
- immutable artifact paths/sizes/SHA-256;
- protected scientific-code no-mutation audit;
- `git diff --check` on actual staged report;
- exactly one execution commit;
- non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not auto-start a repair, another 2009 stationary rerun, a threshold change, multi-year batch, AR(1), shock, transition, IRF, R5, or Results task from inside this execution.