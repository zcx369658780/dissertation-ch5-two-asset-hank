# CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_EXECUTION_AGAINST_PRESERVED_MATLAB_BASELINE

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / Python-only stationary executor / parity evidence producer

Owner: final scientific authority

## 1. Purpose

Execute exactly one corrected-calendar-2009 Python multi-province stationary route using the newly accepted raw-`Vb` repaired standalone household authority, then compare that result against the already completed and immutable MATLAB calendar-2009 stationary baseline.

Accepted predecessor household markers:

- `MP4B_BEIJING_FIRST_TURN_MATLAB_STANDALONE_HOUSEHOLD_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_RAW_VB_REPAIRED_MODULAR_HOUSEHOLD_AUTHORITY_ACCEPTED`
- `MATLAB_FAITHFUL_RAW_VB_REPAIRED_STANDALONE_HOUSEHOLD_AUTHORITY_ACCEPTED`

This task closes only the corrected-2009 stationary parity question. It does **not** authorize a MATLAB stationary rerun, a different year, annual batch execution, shocks, AR(1), transition, genuine dynamics, IRF, historical R5, calibration redesign, or Results.

## 2. Primary scientific authority

Retain:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Retain the Owner route:

- `OWNER_SINGLE_ACTIVE_CODEBASE_TWO_ASSET_HANK_ONLY`
- active model repository: `zcx369658780/dissertation-ch5-two-asset-hank`
- historical one-asset R5 is evidence only and may not be imported at runtime.

The accepted raw-`Vb` repaired standalone household is now the Python household authority for this stationary rerun. No household equation, HJB/KFE formula, MP2 arithmetic, MP3 controller law, annual-data binding, or calibration may be altered in this task.

## 3. Controlling authority to read

Read in full before mutation or execution:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- accepted MP1, MP2, MP3 and MP4A2 reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_EXECUTION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_FULL_INITIALIZATION_PREFLIGHT_AND_PYTHON_ONLY_CALENDAR2009_STATIONARY_COMPARISON_REPORT.md`
- all intervening MP4B liquid-derivative/raw-`Vb` diagnosis and repair reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_PARITY_REVALIDATION_AFTER_RAW_VB_REPAIR_REPORT.md`
- `validators/multi_province/mp4b_comparison_contract.json`
- `validators/multi_province/mp4b_beijing_household_comparator_contract.json`
- `validators/multi_province/mp4b_beijing_household_source_map.json`

## 4. Live continuity

Required execution-start predecessor:

`486af39d40ca4292e04845a0ba5b30fbe4918f61`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `486af39d40ca4292e04845a0ba5b30fbe4918f61`;
3. require a clean worktree;
4. verify repository identity and controlling-rule files;
5. verify all accepted production/export identities below;
6. verify the preserved MATLAB baseline and canonical input before any scientific call;
7. verify no historical R5 / `chapter5_model` runtime dependency.

Any continuity or identity failure => stop before scientific execution.

## 5. Accepted household and current production identities

Require exact SHA-256:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`

Also preserve:

- accepted Beijing source-map SHA-256:
  `757A39B8EFCAD2F955EE8674DC424D0FA97F0C7751B8361995AFCB7AE0A5BD4E`
- accepted Beijing comparator-contract SHA-256:
  `7BAF5520C0E52D156709FA66E00423D892F926F94462EF2E72DC0FCC382458EC`
- accepted Beijing qualified comparison SHA-256:
  `28966B73605BD82BA858C3B2A3CBC144C867377C71476B1003846C88AE6382BF`

Do not modify the three accepted production/export files in this task.

## 6. Frozen corrected-2009 identity

Use exactly:

- `calendar_year = 2009`
- `analysis_index = 1`
- `workbook_data_row_index = 10`
- physical Excel row `11`
- `data_MAT_index = 1`
- `output_filename_year = 2009`
- `regression_vintage_key = 10`
- fixed-2020 `IND_Zt` only as the accepted source numerical initialization anchor.

Canonical input SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

The legacy coupled/wrong-year route is forbidden.

## 7. Preserved MATLAB baseline — read-only authority

The MATLAB stationary baseline must **not** be rerun.

Immutable root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Required read-only identities:

- completed stationary output SHA-256:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- profile SHA-256:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- terminal JSON SHA-256:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`

Frozen descriptive facts:

- terminal: `COMPLETED`
- outer turns: `184`
- MATLAB household calls: `5,704`
- final household convergence flags: `31/31` true.

The executor must identify the exact matching files inside the preserved root by path and hash and record them. If any required preserved artifact is absent, altered, non-unique, or unreadable, stop BLOCKED. Do not reconstruct or rerun MATLAB.

## 8. Known validation-entry binding defect to repair before science

Current `validators/multi_province/mp4b_python_empirical.py` still binds its standalone identity constant to the superseded pre-raw-`Vb` standalone SHA:

`276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

This is now a validation-entry identity defect, not a scientific-equation defect.

This task authorizes the smallest validation-only correction:

- replace the validation entry's expected standalone/oracle identity with the accepted repaired standalone SHA
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`;
- update related validation-only manifest/version text if required to state the accepted raw-`Vb` household authority;
- add or update focused static tests proving the entry resolves exactly the accepted current-repository standalone and not any historical runtime.

Do **not** alter the household solver arithmetic, source initial-labor arithmetic, MP2, MP3, controller semantics, canonical input, production code, or the standalone file itself.

If any additional scientific-arithmetic change appears necessary, stop BLOCKED before the one-shot stationary call.

## 9. Mandatory zero-science preflight

Before consuming the Python stationary budget, establish all of the following with model/scientific calls exactly zero.

### 9.1 Direct-script bootstrap

Run the existing direct bootstrap mode after the identity correction and require:

- exact repository root;
- exact `src` root;
- exact accepted standalone path and SHA;
- exact current package origin;
- no `chapter5_model` / old R5 runtime;
- scientific calls `0`.

Required marker:

`MP4B_RAW_VB_ACCEPTED_PYTHON_DIRECT_SCRIPT_BOOTSTRAP_PREFLIGHT_PASS`

### 9.2 Fresh presolver equality

Freshly reconstruct the Python corrected-2009 presolver object and compare recursively against the accepted MATLAB presolver manifest.

Require semantic mismatch count exactly `0`.

Required marker:

`MP4B_RAW_VB_ACCEPTED_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`

Do not rerun MATLAB presolver or path-smoke unless an already accepted artifact is missing; if missing, stop BLOCKED.

### 9.3 Full first-turn source initialization

Run the existing zero-model full initialization check over exactly:

`31 * 20 * 20 * 2 = 24,800`

cells.

Require all source-domain, initial-labor, positive-`c0`, finite-real-`v02`, and no-substitution checks to pass with household/HJB/KFE/MP2/MP3/stationary calls all `0`.

Required marker:

`MP4B_RAW_VB_ACCEPTED_FULL_FIRST_TURN_SOURCE_INITIALIZATION_PREFLIGHT_PASS`

### 9.4 First-Beijing stationary-entry contract conformance

Before science, add or use a zero-model validation mode/helper that materializes only the **inputs** that `mp4b_python_empirical.py` would send to the first Beijing household on outer turn 1.

Compare those inputs against the already accepted Beijing same-input/source-map evidence. Require exact semantic identity for every execution-critical mapped field, including grid/numerics/parameters/prices/taxes/transfers/initial value/labor arrays and asset labels.

No household/HJB/KFE solve may occur in this check.

Required marker:

`MP4B_STATIONARY_ENTRY_FIRST_BEIJING_INPUT_CONFORMS_TO_ACCEPTED_HOUSEHOLD_AUTHORITY`

If exact conformance cannot be established without changing scientific arithmetic, stop BLOCKED.

### 9.5 Regression and dependency checks

Require before science:

- `py_compile` for touched Python validation files;
- accepted MP2/MP3 focused tests;
- all seven MP3 scenario contracts;
- online stationary runtime focused regressions;
- accepted raw-`Vb` household/export focused tests;
- clean-process import/API check;
- no active R5 dependency;
- `git diff --check`.

Record exact test counts rather than merely `PASS`.

## 10. Comparison contract — freeze before Python execution

The task-specific comparator contract must be persisted before the Python stationary call.

It must incorporate, without loosening:

### Direct/categorical objects

Exact agreement required for:

- calendar identity;
- province order;
- asset labels;
- categorical/boolean/integer source states;
- convergence flags;
- controller terminal category;
- return-bound veto categories;
- clipping/boundary categories when directly exposed.

### First-turn household object

The first Beijing household input must be exactly the accepted same-input contract.

The first Beijing household output is additionally compared against the accepted repaired standalone Beijing household result when the local preserved artifact is available and hash-matches. Use the already frozen Beijing household continuous gate:

`abs(M-P) <= 1e-7 * max(1, abs(M), abs(P))`

for `Ct`, `Lt`, `At`, `Bt`, `At+Bt` only. Do not add `AtTax` as an accepted same-interface household object; retain `EXCLUDED_NOT_UNIQUELY_SOURCE_BACKED` at that household comparison layer.

### Source-identical outer arithmetic

Where upstream inputs are identical and operation order matches, require exact equality. Where the accepted MP2 contract permits binary64 ordering differences, maximum `rtol=atol=1e-12` applies; do not widen it.

### Propagated stationary state

For final/propagated province and national continuous objects:

- report absolute difference;
- report relative difference;
- report normalized difference;
- do **not** invent a broad post-hoc numerical acceptance tolerance.

Overall PASS is scientific/structural, not "everything is below a newly chosen final tolerance". It requires that every nonzero final difference be traceable to already accepted numerical propagation from same-input household arithmetic, with no unexplained source/formula/data/controller divergence.

A different outer-turn count is not silently acceptable. If turn counts differ, the comparator/report must show the relevant neighboring convergence metrics and branch/adaptation sequence and demonstrate that the difference is solely a threshold-crossing consequence of already accepted numerical propagation. Otherwise classify MATERIAL/UNRESOLVED.

Qualitative sign/ranking/correlation diagnostics are secondary only and may not override an unexplained scientific mismatch.

Required pre-run marker:

`MP4B_PYTHON_ONLY_2009_STATIONARY_COMPARATOR_CONTRACT_FROZEN`

## 11. Scientific execution budget

Only after every Section 9 and Section 10 gate passes:

### Python

- corrected-calendar-2009 stationary top-level invocation: exactly `1` maximum;
- stationary reruns: `0`.

Internal household HJB/KFE calls are part of this single top-level invocation and are bounded only by the accepted source controller semantics (`max_iterations=500`).

### MATLAB

- stationary invocation: `0`;
- household/HJB/KFE rerun: `0`;
- presolver scientific rerun: `0`.

### Qualified comparator

- qualified stationary comparator: maximum `1`;
- comparator reruns: `0`.

Run the comparator only after the Python terminal artifact has been durably persisted. If Python produces a nonconvergent but structurally valid terminal state, the comparator may consume its one call to classify that outcome against preserved MATLAB. If Python fails before a scientifically comparable terminal object exists, do not fabricate comparison data.

After the Python scientific invocation begins, no scientific repair-and-rerun is permitted in this task.

## 12. Python run root and persistence

Use one fresh direct-child no-overwrite root under `D:\ProjectTemp`.

Persist at minimum:

- environment/source identity manifest;
- canonical input identity;
- accepted standalone identity;
- scientific call ledger;
- per-turn household call count;
- first-turn household inputs/outputs;
- outer-turn summaries/history sufficient to compare controller/adaptation behavior;
- final state if produced;
- terminal status;
- errors/warnings;
- output hashes.

Do not overwrite any predecessor run root.

Do not commit large local scientific JSON/NPZ/MAT files; commit text-first hashes/summaries only.

## 13. Layer-by-layer comparison hierarchy

If a Python scientific route exists, compare in this order:

1. canonical/presolver identity;
2. first-turn Beijing household input conformance;
3. first-turn Beijing household output versus accepted repaired household authority;
4. remaining first-turn household outputs/convergence flags;
5. migration `Lt_mat` / `Lt_supply`;
6. productive `At`-only capital / `Kt_supply` / `rah`;
7. firm block;
8. composite wage / Taylor / fiscal diagnostics;
9. controller adaptation/damping/boundary logic;
10. subsequent-turn traces where preserved MATLAB evidence permits;
11. final 31-province state;
12. national aggregates and diagnostics.

Do not count downstream propagated differences as independent root causes once an upstream difference has been localized.

If the preserved MATLAB baseline lacks an intermediate required for deeper localization, do not rerun MATLAB. Mark the deeper comparison as unavailable and decide whether the remaining evidence is sufficient for PASS; if not, terminate MATERIAL/UNRESOLVED and recommend one observability-only successor.

## 14. Root-cause classifications

If two scientifically comparable routes exist, use only:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

If Python fails before a scientifically comparable object exists, use an infrastructure/source/environment blocker only when evidence supports it. A genuine Python HJB/KFE/MP2/MP3/controller scientific failure is not to be relabeled as infrastructure.

## 15. Terminal classifications

PASS:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_PARITY_AGAINST_PRESERVED_MATLAB_PASS`

On PASS establish:

`MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_ACCEPTED`

Do **not** independently claim the broader MP4 stage marker in this executor task; independent L3 review decides whether this evidence is sufficient to promote the roadmap-level `MP4_MULTI_PROVINCE_STATIONARY_ROUTE_ACCEPTED` marker.

Scientific mismatch/unresolved:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_PARITY_AGAINST_PRESERVED_MATLAB_MATERIAL_MISMATCH`

Infrastructure/source blocker before comparable science:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_PARITY_AGAINST_PRESERVED_MATLAB_BLOCKED`

## 16. Forbidden operations

Remain exactly zero:

- MATLAB stationary rerun;
- MATLAB household/HJB/KFE rerun;
- wrong-year MATLAB route;
- Python second stationary attempt;
- 2010--2023 annual batch;
- shock/AR1 execution;
- transition;
- genuine dynamics;
- IRF;
- historical R5 runtime;
- Results/manuscript claims;
- calibration changes;
- tolerance widening after scientific output inspection.

## 17. Allowed repository changes

Allowed only:

- the minimum validation-only accepted-standalone identity correction in `validators/multi_province/mp4b_python_empirical.py`;
- one zero-model first-Beijing input-conformance helper/mode if needed;
- one bounded read-only preserved-MATLAB/Python stationary comparator/helper;
- focused non-scientific tests for those validation changes;
- one task-specific comparator-contract artifact;
- bounded CURRENT roadmap status update after terminal classification;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_EXECUTION_AGAINST_PRESERVED_MATLAB_BASELINE_REPORT.md`.

Do not modify:

- `src/ch5_two_asset_hank/economics.py`;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`;
- `exports/matlab_faithful_two_asset_ha.py`;
- accepted MP2/MP3 scientific modules;
- protected MATLAB;
- MATLAB validation scientific logic;
- canonical 2009 input/data/cache/workbooks;
- accepted Beijing source map/comparator contract;
- controlling project rules;
- historical R5.

If a required repair exceeds validation binding/persistence/comparison plumbing, stop and report instead of widening scope.

## 18. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. accepted raw-`Vb` production/export hashes;
4. exact validation-entry identity correction and diff classification;
5. preserved MATLAB root, exact files and hashes;
6. zero-science bootstrap/presolver/full-init/first-Beijing-input markers;
7. exact preflight test counts;
8. comparator-contract path/SHA;
9. Python run root and output hashes;
10. exact scientific call ledger;
11. Python outer turns and household-call count;
12. MATLAB preserved outer turns and household-call count;
13. convergence and terminal-category comparison;
14. first-turn Beijing accepted-household conformance result;
15. layer-by-layer parity table;
16. controller/boundary/adaptation comparison;
17. final province-by-province continuous differences;
18. national aggregate differences;
19. qualitative diagnostics separately labeled secondary;
20. first divergent stage/object, if any;
21. root-cause classification;
22. material mismatch/unresolved/environment lists;
23. forbidden-operation audit;
24. `git diff --check`;
25. changed paths;
26. Git commit/push/read-back closeout;
27. exactly one recommended next gate.

## 19. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path.

Require terminal:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree.

Do not claim a commit SHA before verifying it.

## 20. Successor boundary

On PASS, recommend exactly one successor:

**MP5 shock/source-law role reconciliation and corrected-2009 response-definition freeze, audit/specification only at first; do not execute shocks, transition, dynamics, or IRFs until that successor separately authorizes them.**

On MATERIAL/BLOCKED, recommend exactly one bounded successor addressing only the first supported blocker/divergence.
