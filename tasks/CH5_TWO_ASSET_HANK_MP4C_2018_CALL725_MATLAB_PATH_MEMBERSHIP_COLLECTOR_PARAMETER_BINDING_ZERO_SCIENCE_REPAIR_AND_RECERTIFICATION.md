# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_PATH_MEMBERSHIP_COLLECTOR_PARAMETER_BINDING_ZERO_SCIENCE_REPAIR_AND_RECERTIFICATION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / zero-science collector-repair executor

Owner: final scientific authority

## 1. Authority basis

Immediate live parent at publication:

`36a3b17ac9bd0368f5f45715dacce2a23b38c537`

That parent authorized zero-science repair/certification of the MATLAB logical/physical finite-root path validator. The first certification attempt failed **before MATLAB `which` smoke and before any scientific model call** because the external PowerShell synthetic-path collector passed its test-case arguments incorrectly. As a result, the four positive exact-root cases were falsely recorded as `False`.

This is a second-layer collector/instrumentation defect. It is not evidence against the accepted finite-root rule, exact Junction, protected source hashes, MATLAB source selection, HJB/KFE logic, convergence, parameters, or scientific route.

No HJB, KFE, household, GE, stationary, annual, Python-model, shock, IRF, or Results call was consumed by the failed certification attempt.

This task authorizes a **fresh zero-science recertification from new external evidence files** after repairing only the synthetic membership collector / argument binding. It does not reopen the MATLAB scientific HJB100/HJB500 call budget.

## 2. Controlling authority

Read and obey:

- `AGENTS.md`;
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
- all CURRENT project rules;
- `tasks/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_PATH_EQUIVALENCE_VALIDATOR_ZERO_SCIENCE_REPAIR_AND_CERTIFICATION.md`;
- `tasks/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_SAME_ACTIVE_INPUT_HJB100_HJB500_AND_LEGACY_KFE_REPLAY.md`;
- `validators/multi_province/matlab/mp4b_beijing_household_wrapper.m`.

The controlling finite-root semantics remain exactly those already frozen:

- logical model root = `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- physical model root = `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- `C:\MatlabProgram` must be a Junction with exactly one target `D:\MatlabProgram`;
- resolved source parent is accepted iff it equals exactly one of the two frozen model roots after the same simple slash/case normalization used by the accepted wrapper;
- resolved HJB source SHA must equal the protected SHA;
- sibling, child, parent, prefix-lookalike, and unrelated roots are rejected.

Reference wrapper Git blob:

`99b25751e8dd30ee12ea6da8dca87a668bb29696`

Do not modify it.

## 3. Task type and hard boundary

Task type:

`ZERO_SCIENCE_PATH_MEMBERSHIP_COLLECTOR_PARAMETER_BINDING_REPAIR_AND_RECERTIFICATION__NO_MODEL_CALL`

Forbidden:

- calling `HANK_2ASSETS_HJB`;
- MATLAB HJB solve;
- MATLAB KFE solve;
- MATLAB protected household scientific call;
- Python HJB/KFE/model call;
- GE/stationary/annual execution;
- R/PLM;
- shock/IRF/Results;
- modifying protected MATLAB source;
- modifying repository production/model/test/validator source;
- changing the finite-root acceptance rule;
- changing source hashes;
- changing call-725 scientific state;
- changing `maxit`, `crit`, `Delta`, grid, economics, or KFE semantics;
- automatic scientific retry.

Allowed activity:

- new external collector/helper files only;
- deterministic synthetic path membership tests;
- PowerShell/Windows Junction inspection;
- exactly one MATLAB **zero-call** `which('HANK_2ASSETS_HJB')` smoke after synthetic certification PASS;
- hashing and evidence serialization.

## 4. Collector root cause and repair rule

Persist the failed collector source/command/argument map if available and identify the exact parameter-passing defect.

The repair must not alter the membership predicate. The membership predicate remains:

1. normalize `/` to `\`;
2. compare case-insensitively;
3. strip trailing `\` only;
4. return true iff candidate equals the normalized exact logical root OR normalized exact physical root.

Do not use prefix containment, substring matching, wildcard matching, filesystem canonicalization, or generic symlink acceptance.

### Preferred collector implementation

To avoid another shell-quoting/argument-binding failure, the synthetic membership cases should preferably be evaluated **inside one process with literal in-memory test-case strings**, not passed as multiple path arguments through nested shell quoting.

Preferred options, in order:

1. a small external Python collector containing the test cases as literal strings and implementing only the same slash/case/trailing-separator normalization; or
2. a PowerShell script whose test-case table is declared literally inside the script itself and whose predicate is called directly, with no outer positional-argument transport for the case values.

PowerShell remains required for the independent Junction proof, but it does not need to be the synthetic-membership collector.

If PowerShell is retained for synthetic collection, persist an explicit parameter-binding self-test showing each input case arrives byte/text-equivalent to its intended value before evaluating membership.

## 5. Fresh no-overwrite evidence root

Use a new root; do not overwrite the failed certification evidence.

Preferred:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-path-collector-zero-science-recertification-20260904-001`

Persist identities of the predecessor failed external evidence root/files if available, but do not mutate them.

## 6. Phase A — collector self-certification, no MATLAB

Before any MATLAB process is started, certify the repaired collector.

Required cases:

| Case | Expected |
|---|---|
| exact logical root | PASS |
| exact physical root | PASS |
| logical root with `/` separators and mixed case in ASCII components | PASS |
| physical root with `/` separators and mixed case in ASCII components | PASS |
| `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK-sibling` | FAIL |
| `D:\MatlabProgram\other-model` | FAIL |
| `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK\child` | FAIL |
| `D:\MatlabProgram` | FAIL |
| prefix/lookalike model-root string | FAIL |

For every case persist:

- intended raw input;
- actually received raw input;
- normalized candidate;
- normalized logical root;
- normalized physical root;
- boolean membership result;
- expected result;
- PASS/FAIL of the test assertion.

The four positive cases must all evaluate true. The five negative cases must all evaluate false.

If any case fails, STOP before MATLAB and publish only the zero-science failure evidence. Do not edit and rerun the same consumed collector inside this task after observing failure.

Required Phase-A marker:

`MP4C_2018_PATH_MEMBERSHIP_COLLECTOR_PARAMETER_BINDING_CERTIFIED__ALL_SYNTHETIC_CASES_PASS__MATLAB_ZERO_CALL_SMOKE_AUTHORIZED`

## 7. Phase B — independent Junction/source identity verification

After Phase A PASS, perform the already accepted finite-root Junction proof:

- `C:\MatlabProgram` exists as Junction;
- target count = 1;
- sole target = `D:\MatlabProgram` under Windows full-path case-insensitive comparison.

Then verify both logical and physical copies/paths of the four protected files hash to:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

Any mismatch: STOP before MATLAB smoke.

## 8. Phase C — exactly one MATLAB `which` zero-call smoke

Only after Phase A and Phase B PASS:

- launch MATLAB once for path-resolution smoke;
- save current MATLAB path;
- `addpath(logical_root,'-begin')`;
- execute `which('HANK_2ASSETS_HJB')` only;
- do not invoke the returned function;
- record MATLAB version/release;
- record exact resolved file path and parent;
- apply the **certified** finite-root predicate to the resolved parent;
- require membership PASS;
- hash the resolved file and require protected HJB SHA;
- restore MATLAB path.

This phase must not call the repository `run` mode of any household wrapper. A smoke-only path resolver is sufficient.

Scientific-call ledger must remain all zeros.

Required Phase-C marker:

`MP4C_2018_MATLAB_WHICH_ZERO_CALL_SMOKE_PASS__RESOLVED_SOURCE_IN_EXACT_LOGICAL_PHYSICAL_FINITE_ROOT_PAIR`

## 9. Integrity regression

Compare the failed collector and repaired collector where both are available.

Allowed semantic change:

- argument transport / parameter binding / collector implementation needed to ensure synthetic inputs are delivered correctly;
- evidence serialization needed to record received-vs-intended paths.

Forbidden changes:

- finite-root membership semantics;
- Junction semantics;
- protected source hashes;
- logical/physical root values;
- parent scientific task;
- HJB100/HJB500 definitions;
- MATLAB scientific call budget;
- KFE semantics;
- no-retry rule.

Explicitly classify:

`COLLECTOR_PARAMETER_BINDING_ONLY__FINITE_ROOT_AUTHORITY_UNCHANGED`.

## 10. Required evidence

Persist at minimum:

- `authority_identity.json`;
- `predecessor_failed_certification_identity.json`;
- failed collector source/command/argument map if available;
- repaired collector source;
- exact collector diff;
- `collector_parameter_binding_selftest.json`;
- `synthetic_path_membership_cases.csv`;
- `finite_root_authority_contract.json`;
- `junction_evidence.json`;
- `protected_source_hashes.json`;
- `matlab_which_zero_call_smoke.json` if Phase C runs;
- `scientific_contract_integrity_regression.json`;
- `zero_science_ledger.json`;
- stdout/stderr;
- `classification.json`;
- `audit_manifest.json`.

Hash and read back all evidence.

## 11. Scientific-call ledger

Final ledger must show exactly:

- MATLAB `HANK_2ASSETS_HJB` calls = 0;
- MATLAB HJB solves = 0;
- MATLAB KFE solves = 0;
- protected household scientific calls = 0;
- Python HJB/KFE/model calls = 0;
- GE/stationary/annual calls = 0;
- R/PLM = 0;
- shock/IRF/Results = 0.

MATLAB `which` smoke is infrastructure only and must be recorded separately from scientific calls.

## 12. PASS/FAIL

PASS terminal:

`MP4C_2018_CALL725_MATLAB_PATH_COLLECTOR_ZERO_SCIENCE_RECERTIFIED__SYNTHETIC_MEMBERSHIP_AND_WHICH_SMOKE_PASS__READY_FOR_FRESH_MATLAB_TERMINATION_REPLAY_TASK`

PASS requires:

- collector parameter-binding self-test PASS;
- all 9 synthetic membership cases PASS;
- exact Junction PASS;
- all protected hashes PASS;
- MATLAB `which` zero-call smoke PASS;
- resolved source belongs to exact finite pair;
- scientific-call ledger all zero;
- no repository/protected-source scientific mutation.

FAIL terminal:

`MP4C_2018_CALL725_MATLAB_PATH_COLLECTOR_ZERO_SCIENCE_RECERTIFICATION_FAILED__MATLAB_TERMINATION_REPLAY_REMAINS_BLOCKED__NO_SCIENCE_CALL`

## 13. Required report and publication

Write only:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_PATH_MEMBERSHIP_COLLECTOR_PARAMETER_BINDING_ZERO_SCIENCE_REPAIR_AND_RECERTIFICATION_REPORT.md`

If PASS, only report-only commit + non-force push is authorized.

Suggested commit:

`Recertify MP4C MATLAB path collector binding`

After push:

- `git fetch origin`;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- tracked worktree clean.

Even after PASS, do not run HJB100/HJB500/KFE. Return to ChatGPT L3 for a fresh scientific execution task and fresh call budget.
