# CH5_TWO_ASSET_HANK_POST_P5_D2_HETEROGENEOUS_CASE_CONTAINER_CORRECTION_AND_RESUMPTION

Date: 2026-08-29

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / executor

Owner: final scientific authority

## 1. Purpose

Resume the already accepted post-P5 household-decision parity experiment after D1 completed with exact parity and the next step, D2 MATLAB, was blocked solely by an external heterogeneous-case-container access defect.

The predecessor scientific specification remains:

`tasks/CH5_TWO_ASSET_HANK_POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_MAP_PARITY.md`

The predecessor correction/resumption task remains accepted execution history:

`tasks/CH5_TWO_ASSET_HANK_POST_P5_HOUSEHOLD_DECISION_MAP_PARITY_MATLAB_STRUCT_HARNESS_CORRECTION_AND_RESUMPTION.md`

The latest accepted report is:

`docs/CH5_TWO_ASSET_HANK_POST_P5_HOUSEHOLD_DECISION_MAP_PARITY_MATLAB_STRUCT_HARNESS_CORRECTION_AND_RESUMPTION_REPORT.md`

Accepted latest terminal classification:

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_BLOCKED_SOURCE_OR_ENVIRONMENT__DYNAMIC_HOLD_CONTINUES`

The only current blocker is D2 external MATLAB case-container traversal at `d2_matlab.m:3`, where the heterogeneous decoded ten-case P2 container did not support the assumed `c.id` dot-indexing path.

This successor authorizes only the minimum external D2 input-container access correction, one no-model heterogeneous-container preflight, one replacement D2 MATLAB call, and—conditional on valid D2 persistence—the remainder of the already frozen D2 -> D3 sequence.

P5 remains Owner-accepted under:

`MATLAB_PYTHON_TWO_ASSET_HA_PARITY_ACCEPTED_FOR_DYNAMIC_EXTENSION`

The voluntary hold remains:

`DYNAMIC_EXECUTION_HELD_PENDING_POST_P5_HOUSEHOLD_DECISION_PARITY`

## 2. Accepted D1 result must be reused

D1 is now accepted supplementary evidence and must not be rerun.

Accepted D1 result:

- `432/432 PASS`;
- all 216 `a<a_bar` cases PASS;
- maximum absolute difference `0` for every compared scalar field;
- transfer-sign mismatch `0`;
- `a`-direction mismatch `0`;
- `b`-direction mismatch `0`.

Accepted D1 artifacts from the predecessor successor root:

`D:\ProjectTemp\ch5-post-p5-household-decision-map-parity-resumption-artifacts-20260829-223000`

- `d1_matlab.json` SHA-256 `1FCA5F613C52B2E0F5CC45EF8B06BB96ED171ADB0F79F31A709151F2E91775CF`
- `d1_python.json` SHA-256 `1C3A63759027AC8FF0F2FD56AB090CB8584110FCD9ACAAFDF937624245324F42`
- `d1_compare.json` SHA-256 `1B95E6D23A1409874DE2548812FD1C34E11E955A68093C0A2818B54A521712B3`

Do not rerun D1 MATLAB, Python, or comparison.

## 3. Live authority and continuity

Task-authoring parent observed before publication:

`a97634b29a07bca08fcf7f144742ae9eb122cda7`

At execution start:

1. fresh-fetch `origin/main`;
2. confirm this exact task exists on live `main`;
3. confirm both predecessor tasks/reports exist;
4. record live start SHA;
5. verify Owner P5 marker and active dynamic hold;
6. verify accepted Python `src/tests` continuity from baseline `7a2388a2ba89073e307f05a909570e8c40a4be13` using:

`git diff --name-only 7a2388a2ba89073e307f05a909570e8c40a4be13 -- src tests`

It must be empty.

## 4. Required reads

Read at minimum:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `tasks/CH5_TWO_ASSET_HANK_POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_MAP_PARITY.md`
- `tasks/CH5_TWO_ASSET_HANK_POST_P5_HOUSEHOLD_DECISION_MAP_PARITY_MATLAB_STRUCT_HARNESS_CORRECTION_AND_RESUMPTION.md`
- `docs/CH5_TWO_ASSET_HANK_POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_MAP_PARITY_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_POST_P5_HOUSEHOLD_DECISION_MAP_PARITY_MATLAB_STRUCT_HARNESS_CORRECTION_AND_RESUMPTION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_P5_OWNER_FINAL_ACCEPTANCE_DECISION.md`
- every source/evidence file required by the predecessor household-decision task that is necessary to execute D2/D3 unchanged.

The original household-decision task controls all D2/D3 scientific equations, cases, ordering, parameters, tolerances, outputs, and fail-closed logic except where this task explicitly authorizes the consumed D2 replacement call and the D2 input-container correction.

## 5. Frozen predecessor identities to preserve

Original household-decision artifact root:

`D:\ProjectTemp\ch5-post-p5-household-decision-map-parity-artifacts-20260829-220000`

Frozen scientific manifest:

- `manifest.json` SHA-256 `D46DD0968DA65C592863A9D33FD5FB58059E707432816A67A74CE4DF7AB1BEDA`

Frozen D2/D3 harnesses:

- original blocked `d2_matlab.m` SHA-256 `8067196C2C680926490B6231EE2FF3125DD43B26AB812A1566719043E270C7C9`
- `d2_python.py` SHA-256 `DD9DDCB675BE7A5C3A672CF1936AB7CBA1553AC8334989B2CDAA777FB2914344`
- `compare.py` SHA-256 `5C6FDBEBDA8BA67D819E69808A43C076E8F7BCB3C85788DABD942CC35EB40BE7`
- predecessor corrected D1/D3 decision-container implementation `decision_matlab_corrected.m` SHA-256 `5D94EDF89259D543758E490EA25C9359C1B65588CF7BB0557EEEDFA6B7FF3F9A`
- `decision_python.py` SHA-256 `6054A0C59E7C0CD0C83AD1919C3CFAF7B2987D0545EE4CEB26B3345FF2D01ABC`

Protected MATLAB/Python scientific identities remain exactly those in the predecessor task/report. No production source/helper/test/cache mutation is authorized.

## 6. Scientific content is immutable

Do not change any frozen D2/D3 scientific content.

The following remain byte/value/semantic identical:

- all 10 D2 cases and ordering;
- all 360 D3 cases and ordering;
- all D2/D3 economic parameters;
- all state/shadow inputs;
- `mu_a = r_a*a + d`;
- `m(a)=max(a,a_bar)`;
- accepted O1 behavior;
- labor-curvature mapping;
- common-budget scope;
- D2 accepted-equation/KKT formulas;
- D3 gamma-2 / Python `phi=5` / MATLAB `frisch_l=.2` calibration;
- all comparison fields;
- all tolerances;
- all fail-closed/stop rules except the explicitly authorized replacement D2 MATLAB call.

Reuse the frozen predecessor manifest after identity verification. Do not regenerate a scientifically different manifest.

## 7. Phase A — diagnose and correct only D2 heterogeneous-case traversal

Work in a fresh no-overwrite external artifact root. Do not modify either predecessor artifact root.

Read the blocked `d2_matlab.m` and the frozen heterogeneous P2 JSON representation. Determine the exact MATLAB decoded container type and why `c=m.p2(k); id=c.id` is invalid.

Create a corrected replacement `d2_matlab_corrected.m` subject to all constraints below.

Allowed edits are only **input-container traversal / schema access plumbing**, such as:

- detecting whether decoded `m.p2` is a cell array, struct array, table-like object, or another JSON-decoded container;
- extracting the k-th already-frozen case without altering its values;
- normalizing field access or container shape solely so the existing scientific evaluator receives the same frozen case content;
- JSON input parsing/access plumbing required by the same heterogeneous-container issue.

Forbidden edits include any change to:

- the ten case definitions or ordering;
- scientific formulas;
- root equations;
- state constraints;
- KKT equations or multiplier recovery;
- O1 transfer scaling;
- constant-`r_a` drift;
- parameters, states, derivatives or shadow inputs;
- tolerances;
- output values or expected answers;
- result-container logic unrelated to the observed input-container blocker unless the existing D2 output path cannot serialize valid rows, in which case stop and report a new blocker rather than broadening this correction silently.

Produce a complete original-versus-corrected diff and classify every changed line. Every changed line must be one of:

- `INPUT_CONTAINER_TRAVERSAL_ONLY`
- `JSON_ACCESS_PLUMBING_ONLY`

If any scientific line would need modification, stop before preflight and return blocked.

## 8. Phase B — mandatory no-model heterogeneous JSON/container preflight

Before the replacement D2 MATLAB scientific call, create a separate synthetic or frozen-input-only MATLAB preflight that does **not** evaluate household equations.

The preflight must not call:

- `HANK_2ASSETS_HJB`
- production `HANK3_FOC`
- accepted O1 FOC
- `HANK3_cost`
- `lab_solve2`
- any D2 scientific root/KKT evaluator
- Python

The preflight must:

1. load either a minimal synthetic heterogeneous JSON object matching the frozen D2 container shape or a structural copy containing only case IDs/types and non-scientific dummy fields;
2. exercise the corrected k-th case extraction/access path for all 10 entries;
3. verify exact order and distinct case IDs;
4. verify required D2 input field names are addressable on each extracted case;
5. verify no case is skipped, duplicated, reordered or merged;
6. exit zero.

Freeze/hash/read back before preflight:

- corrected D2 MATLAB harness;
- heterogeneous-container preflight;
- unchanged scientific manifest;
- unchanged D2 Python/comparison/D3 harnesses;
- a new successor execution ledger.

If the preflight fails, no replacement D2 scientific call is allowed.

## 9. Phase C — exactly one replacement D2 MATLAB scientific call

The predecessor D2 MATLAB call was consumed by the external input-container defect before valid persistence. This task explicitly authorizes exactly **one replacement D2 MATLAB scientific call** using the corrected harness.

It must use the exact frozen ten D2 cases, exact ordering, accepted equations, root/KKT logic, parameters, and tolerances.

Acceptance before advancing:

- process exits zero;
- `d2_matlab.json` exists;
- it contains exactly 10 rows in frozen order;
- every required input identity and every required output field exists;
- all required values are finite where the predecessor contract requires finiteness;
- output parses/read-backs independently.

If the replacement D2 MATLAB call fails for any reason, do not repair or rerun it in this task. Stop fail-closed.

## 10. Phase D — complete D2 comparison and then unchanged D3

Only after valid D2 MATLAB persistence:

### D2 remainder

Run exactly once each:

- D2 Python production evaluator;
- D2 comparison.

Apply the predecessor D2 contract unchanged.

For all ten cases compare:

- `c`
- `l`
- `d`
- adjustment cost
- `mu_a`
- `mu_b`
- utility
- Hamiltonian
- `a` direction
- `b` direction
- `lambda_a`
- `lambda_b`
- maximum KKT residual
- boundary-feasibility result.

If any valid scientific mismatch occurs, stop before D3 and classify material contradiction.

### D3

If D2 passes, execute D3 exactly as originally authorized and frozen:

- MATLAB decision harness: 1 scientific call using the already accepted corrected decision result-container plumbing;
- Python decision harness: 1 scientific call;
- comparison: 1 call.

Use the exact frozen 360 gamma-2/phi-5 cases. Do not change the scientific formula portion of the D3 evaluator.

If a D3 source/environment blocker occurs, stop with no same-task repair. If a valid D3 comparison mismatches, classify material contradiction.

## 11. Exact call budget

Engineering-only heterogeneous-container preflight: at most 1 MATLAB invocation.

Historical scientific calls to report separately:

- original predecessor D1 MATLAB blocked call: 1;
- accepted replacement D1 MATLAB/Python/comparison: `1/1/1`;
- consumed predecessor D2 MATLAB blocked call: 1.

Scientific calls authorized in this successor:

- replacement D2 MATLAB: exactly 1 if preflight passes;
- D2 Python: at most 1, only after valid MATLAB persistence;
- D2 comparison: at most 1;
- D3 MATLAB/Python/comparison: at most `1/1/1`, only after D2 PASS.

D1 calls in this successor: exactly `0/0/0`.

No full HJB, Python HJB/KFE/steady-state, P3/P4/R4, asset-tail, AR(1), transition, IRF, calibration-extension, dynamics or Results call is authorized.

## 12. Terminal classifications

Return exactly one.

### D2 and D3 pass

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_PASS__DYNAMIC_HOLD_MAY_ADVANCE_TO_ASSET_TAIL_GATE`

Use only if:

- D1 accepted PASS evidence is reused without rerun;
- D2 harness diff is input-container/JSON-access only;
- heterogeneous-container preflight passes;
- replacement D2 MATLAB persists all 10 rows;
- D2 all 10 cases pass;
- D3 all 360 cases pass;
- no forbidden mutation or rerun occurs.

### Scientific contradiction

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_MATERIAL_CONTRADICTION__DYNAMIC_HOLD_CONTINUES`

Use if a valid D2 or D3 comparison shows any material mismatch under the frozen accepted equations.

P5 is not automatically revoked. Report the exact contradiction for Owner/reviewer diagnosis.

### Source/environment/harness blocked

`POST_P5_SHARED_INPUT_HOUSEHOLD_DECISION_PARITY_BLOCKED_SOURCE_OR_ENVIRONMENT__DYNAMIC_HOLD_CONTINUES`

Use if:

- D2 correction cannot remain within input-container/JSON-access plumbing;
- preflight fails;
- replacement D2 persistence fails;
- or D3 encounters a new source/environment blocker before a valid scientific comparison.

Do not infer scientific PASS/FAIL from missing comparison evidence.

## 13. Required report

Write only:

`docs/CH5_TWO_ASSET_HANK_POST_P5_D2_HETEROGENEOUS_CASE_CONTAINER_CORRECTION_AND_RESUMPTION_REPORT.md`

The report must include:

1. terminal classification;
2. live start/final `origin/main`;
3. Python `src/tests` continuity;
4. predecessor and successor artifact roots;
5. protected identities;
6. D1 accepted evidence identities and explicit no-rerun confirmation;
7. exact D2 blocker diagnosis including decoded container type;
8. original-to-corrected D2 MATLAB harness diff with per-line classification;
9. corrected harness/preflight/ledger hashes;
10. heterogeneous-container preflight result for all 10 cases;
11. historical consumed D2 count and successor replacement D2 count separately;
12. exact D2/D3 execution counts;
13. D2 ten-case per-field maximum absolute differences and worst cases;
14. D2 direction, multiplier, KKT and boundary-feasibility mismatch counts;
15. D3 360-case gamma-2/phi-5 per-field maximum absolute differences and worst cases if reached;
16. complete scientific mismatch list;
17. complete source/environment failure list;
18. forbidden-operation check;
19. git status;
20. acceptance level;
21. exact recommended next gate.

## 14. Explicit prohibitions

Do not:

- rerun D1;
- modify MATLAB production source/helpers/cache;
- modify Python production source/tests;
- alter any frozen D2/D3 scientific case or ordering;
- alter equations, calibration, grids, state/shadow values or tolerances;
- add the legacy `raah/Rah` taper;
- use production bare-`a` FOC as corrected oracle;
- add `Tt`/`rb_gap` adapters;
- hard-code/import Python result values into MATLAB;
- modify D2 scientific evaluation while fixing container traversal;
- repair and rerun a failed replacement D2 or D3 stage in the same task;
- run full `HANK_2ASSETS_HJB`;
- run Python HJB/KFE/steady state;
- rerun P3/P4/R4;
- run upper-`a` asset-tail diagnostics;
- enter AR(1), transition, IRF, dynamics, calibration extension or Results;
- revoke or reissue P5 automatically.

## 15. Recommended next gate rule

If PASS, recommend only the already identified post-P5 upper-`a` asset-tail robustness gate before actual dynamic execution.

If material contradiction, recommend the smallest Owner/reviewer scientific diagnosis of the exact mismatched D2/D3 decision object.

If blocked, recommend only the smallest new source/environment correction gate for the newly observed blocker.