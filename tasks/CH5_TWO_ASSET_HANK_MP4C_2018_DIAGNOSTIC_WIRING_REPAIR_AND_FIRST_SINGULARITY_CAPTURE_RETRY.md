# CH5_TWO_ASSET_HANK_MP4C_2018_DIAGNOSTIC_WIRING_REPAIR_AND_FIRST_SINGULARITY_CAPTURE_RETRY

Date: 2026-09-03

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / diagnostic-wiring repairer / single-run scientific diagnostician

Owner: final scientific authority

## 1. Authority basis

Immediate predecessor execution:

`8d1f21e911cc11e9bd476df63191d084f5d9eaeb`

with terminal:

`MP4C_2018_FIRST_SINGULARITY_DIAGNOSTIC_INFRASTRUCTURE_BLOCKED__NO_REPAIR_NO_RETRY`

Accepted predecessor facts:

- the frozen 2018 Owner-A input was byte-identical to prior failed attempts;
- pre-call ledger reached outer iteration 1 / Beijing / household call 1;
- zero HJB calls completed and zero KFE calls completed;
- the diagnostic wrapper failed before HJB because it attempted to resolve `solve_matlab_faithful_hjb` through `validators.multi_province.mp4b_python_empirical`, where that symbol does not exist;
- the failure was instrumentation wiring only;
- no scientific equation, parameter, grid, controller, input, HJB, KFE, or model-output evidence was changed;
- no retry occurred.

Because the previous attempt consumed no HJB/KFE science and stopped for a purely diagnostic namespace error, the L3 reviewer authorizes one corrected diagnostic execution under the standing bounded-debugging delegation.

## 2. Purpose

Repair only the diagnostic symbol binding, prove the instrumentation resolves the exact production MATLAB-faithful HJB/KFE callables, then execute exactly one 2018 diagnostic run to capture the first genuine KFE singularity event.

This task is NOT a solver repair task.

## 3. Required continuity

At start:

1. `git fetch origin`;
2. require this exact task live on `origin/main` as direct child of `8d1f21e911cc11e9bd476df63191d084f5d9eaeb`;
3. require `HEAD == origin/main`, ahead/behind `0/0`;
4. require clean tracked worktree;
5. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md` and all CURRENT rules;
   - prior first-singularity task/report;
   - `exports/matlab_faithful_two_asset_ha.py`;
   - `validators/multi_province/mp4b_python_empirical.py`;
   - `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`;
   - `validators/multi_province/mp4c_python_annual_production.py`;
   - current 2018 diagnostic wrapper.

## 4. Exact wiring defect to repair

The predecessor wrapper incorrectly called:

`anchor.solve_matlab_faithful_hjb`

where `anchor` was `mp4b_python_empirical`.

The production MATLAB-faithful HJB/KFE functions are defined in:

`exports.matlab_faithful_two_asset_ha`

and the post-loop household adapter already defaults to those exact functions.

Repair the diagnostic wrapper so instrumentation wraps the exact production callables without changing their scientific arguments or return values.

Preferred approaches:

- import `exports.matlab_faithful_two_asset_ha as faithful` and wrap `faithful.solve_matlab_faithful_hjb` / `faithful.solve_matlab_faithful_stationary_kfe`; or
- obtain the exact adapter-default callable identities and pass instrumented wrappers explicitly.

Do NOT copy/reimplement HJB/KFE science inside the diagnostic wrapper.

## 5. Phase A — zero-science wiring verification

Before any 2018 execution, prove all of the following with imports/dummy callables only:

- the HJB symbol exists and is callable;
- the KFE symbol exists and is callable;
- their module/path identities equal the production MATLAB-faithful exports;
- the adapter accepts explicitly supplied wrapped HJB/KFE callables;
- the wrapper preserves positional/keyword argument flow;
- a dummy HJB result can pass to a dummy KFE wrapper without invoking the real model;
- diagnostic capture hooks can persist a dummy sparse matrix;
- no HANK scientific function is invoked.

Run `py_compile` on changed/new diagnostic files.

Required marker:

`MP4C_2018_DIAGNOSTIC_WIRING_REPAIR_ZERO_SCIENCE_PASS__ONE_CAPTURE_RUN_AUTHORIZED`

If this phase fails, STOP. Do not execute 2018.

## 6. Frozen 2018 scientific input

Use the same corrected Owner-A input as prior failed attempts.

Required SHA-256:

`F84D25FD49A76229CA49958764D1167CAA56FB68CF99A4ED7B20C508812E6ED0`

Required binding:

- year 2018;
- rolling-window entry 10;
- regression vintage 19;
- calendar level row 19;
- rolling window 2009–2018;
- GDP source `GDP`;
- CAP source `R语言计算资本存量`;
- POP source `就业人数`;
- scale factors 1000 / 1000 / 100;
- no 2023 scientific input.

Any input-byte difference is a STOP condition.

## 7. Scientific immutability

Do NOT change:

- HJB equations;
- KFE equations;
- HJB/KFE tolerances or max iterations;
- grid;
- transfer FOC;
- adjustment cost;
- raw-Vb logic;
- illiquid-return taper;
- generator assembly;
- contaminated-row index or RHS;
- normalization;
- firm/wage/monetary/fiscal logic;
- outer controller;
- calibration;
- input values.

No pseudoinverse, regularization, alternate solver, alternate contaminated row, perturbation, fallback, or second run.

## 8. Phase B — exactly one corrected diagnostic execution

Only after Phase-A PASS, execute exactly one 2018 diagnostic run.

Use:

- workers = 1;
- subprocesses = 1;
- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`;
- automatic reruns = 0.

Fresh no-overwrite evidence root, preferred:

`D:\ProjectTemp\ch5-mp4c-2018-first-singularity-generator-diagnostic-wiring-repair-20260903-001`

Do not reuse predecessor root.

## 9. Capture contract at first genuine singularity

Before every household call persist:

- outer iteration;
- province index/name;
- global household-call number;
- `rah`, `rb`, `tau`, `w`, `Tt`, `rb_gap`;
- entering `Yt`, `Lt`, `Kt`, `Zt`, `GovInv`.

For every HJB call, persist at least a compact ledger row after return:

- province/call context;
- `hjb.converged`;
- HJB iterations;
- convergence statistic;
- whether KFE is entered after converged or nonconverged post-loop HJB.

At the FIRST KFE singularity/nonfinite contaminated-row solve, persist and STOP:

- exact localization;
- exact HJB status;
- post-HJB operator `A`;
- `A'`;
- contaminated matrix;
- RHS;
- raw `spsolve` vector if produced;
- MatrixRankWarning records;
- traceback;
- operator row-sum/finite/diagonal/off-diagonal diagnostics;
- SCC count;
- closed SCC count and member indices;
- rank/nullity diagnostics for `A'` and contaminated matrix;
- smallest singular/eigenvalue diagnostics with method/tolerance documented.

Expected state dimension is 800. Dense SVD is acceptable if practical; otherwise use a reproducible sparse method.

## 10. Critical HJB/KFE classification

The report MUST explicitly state whether the first singularity follows:

`HJB_CONVERGED`

or:

`MATLAB_FAITHFUL_POSTLOOP_AFTER_HJB_NONCONVERGENCE`

This is the primary scientific discriminator.

## 11. Root-cause classification

Choose only the strongest supported result:

- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_HJB_NONCONVERGED_POSTLOOP_OPERATOR`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_MULTIPLE_CLOSED_COMMUNICATING_CLASSES`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_OPERATOR_RANK_DEFICIENCY_BEYOND_SINGLE_STATIONARY_NULL_DIRECTION`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_GENERATOR_CONSTRUCTION_DEFECT`
- `2018_KFE_SINGULARITY_ASSOCIATED_WITH_OTHER_CAPTURED_NUMERICAL_PATHOLOGY`
- `2018_KFE_SINGULARITY_ROOT_CAUSE_REMAINS_UNRESOLVED_AFTER_FIRST_EVENT_CAPTURE`

Do not call a MATLAB-faithful convention defective without source/parity evidence.

## 12. If the singularity does not recur before ordinary completion

If the diagnostic run unexpectedly reaches a scientifically complete 2018 stationary solution without any singularity:

- preserve the complete run and all instrumentation receipts;
- do not run again;
- classify:

`2018_FIRST_SINGULARITY_NOT_REPRODUCED_UNDER_CORRECTED_DIAGNOSTIC_WIRING__NO_SECOND_RUN`

Do not automatically accept 2009–2022 composite coverage in this task; L3 must review first.

## 13. No repair in this task

Even if cause is obvious, do NOT alter scientific code or execute a repaired 2018 run.

The next task will authorize the minimal repair only after L3 review.

## 14. Required evidence

Persist at minimum:

- `phase_a_wiring_identity_receipt.json`;
- `phase_a_zero_science_test_receipt.json`;
- `input_2018_identity.json`;
- `scientific_code_identity_manifest.json`;
- `household_call_ledger.csv`;
- `hjb_call_ledger.csv`;
- first-singularity localization/HJB/operator/SCC/rank files when captured;
- stdout/stderr/traceback;
- diagnostic execution receipt;
- bounded-science ledger;
- audit manifest.

## 15. Report

Required:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_DIAGNOSTIC_WIRING_REPAIR_AND_FIRST_SINGULARITY_CAPTURE_RETRY_REPORT.md`

Expected terminal if capture succeeds:

`MP4C_2018_DIAGNOSTIC_WIRING_REPAIRED__FIRST_SINGULARITY_CAPTURE_COMPLETE__NO_SCIENTIFIC_REPAIR_NO_SECOND_RUN`

Alternative terminal if no singularity recurs:

`MP4C_2018_DIAGNOSTIC_WIRING_REPAIRED__FIRST_SINGULARITY_NOT_REPRODUCED__NO_SECOND_RUN`
