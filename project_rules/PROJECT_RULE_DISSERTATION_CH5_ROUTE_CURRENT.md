# Chapter 5 两资产重构路线与科学边界
更新：2026-09-07；Owner 已批准轻量治理迁移。

## 唯一活动路线
唯一活动模型仓库是 `zcx369658780/dissertation-ch5-two-asset-hank`。
旧 one-asset R5 只读历史；不保留其科学 runtime 作为当前依赖。当前工作处于 MATLAB-faithful 两资产、多省份重构与数值比较。
保护源忠实复现的证据不等于所有 legacy 经济公式均正确。忠实实现、经济合理性、数值有效性、正式论文结论分别陈述。不能悄然把忠实版本替换为 redesign 版本。

## 已批准的比较阶段解释
对 call-725 液体导数，主比较对象为应用边界覆盖后、实际被 HJB policy 使用的 `vb_f/vb_b`。
处理前有限差分数组可以保留，明确标为 `interior_vb_f_pre_boundary`、`interior_vb_b_pre_boundary` 等诊断对象；不存在对应阶段时注明不可比较，不补造数据。
两端必须比较同一阶段。不能通过只删除 raw 行掩盖实际差异；后续 policy、算子、线性系统和解仍需各自证据。
本选择由 Reviewer 在 Owner 批准的范围内确定，无需额外 Owner 命名决策。明确取代最新 forensic 报告末尾“Owner 必须先选择 raw 含义”的未来流程建议；该报告的历史发现不改写。
选择不改变方程、边界、生产源码、运算顺序、输入或容差；也不授权执行比较脚本/修改 wrapper，后续执行需要 exact task。

## 不变的比较标准
沿用 call-725 已冻结规则：类别/mask、shape/order 精确一致；连续值按
`abs(x-y) <= 128*eps64*max(1,abs(x),abs(y))`；稀疏数学支持仅忽略精确存储零，不做 epsilon pruning。
仅同输入同阶段对象适用该规则；其他科学契约按各自预先冻结标准。不得观察结果后调阈值。

## 下一科学工作建议
一个任务完成阶段映射、允许的外部 wrapper/comparator 修复、已有持久化数组的完整首轮对比、必要诊断及报告。
优先复用已有 MAT/NPZ，不因重新命名而重跑 HJB。若缺失必要对象，应说明缺口并在 exact task 中预先规定确有必要的新调用预算。
当前迁移和本地同步的科学预算为零。本建议不是后续模型运行授权。

## 后续科学与 Results
首轮通过不代表多轮、KFE、外层 GE、年度或动态通过。新增阶段按预定义契约和预算推进；只在有依赖的地方设置科学验收。
冲击 law、动态时序、终末条件和政策解释等真实选择由 Owner/指定科学 authority 决定。当前 MATLAB 命名 IRF 路线不能被当成已验证的动态 HANK。
文献、研究动机和方法草稿可在已有证据范围内准备；正式结果、福利和政策结论必须绑定已接受输出及对应审核。不能将诊断图误标为论文结果。
