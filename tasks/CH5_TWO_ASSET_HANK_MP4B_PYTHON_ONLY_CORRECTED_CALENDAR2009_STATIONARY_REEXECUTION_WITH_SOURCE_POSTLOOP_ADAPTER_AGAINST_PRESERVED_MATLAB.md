# CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_WITH_SOURCE_POSTLOOP_ADAPTER_AGAINST_PRESERVED_MATLAB

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / one-shot Python stationary executor / parity evidence producer

Owner: final scientific authority

## 1. Purpose

Re-execute exactly one corrected-calendar-2009 Python multi-province stationary route after the source-backed nonconverged-HJB post-loop semantics mismatch has been repaired in validation-only plumbing.

Use the accepted source-postloop household adapter so that an inner HJB with `converged=false` still proceeds through the accepted KFE and household aggregation primitives, returns the original false flag, and lets the accepted MP3 outer controller decide whether to continue.

Compare any scientifically comparable Python terminal result against the same immutable preserved MATLAB calendar-2009 baseline. MATLAB must not be rerun.

This task does not authorize another year, annual batch, shocks, AR(1), transition, dynamics, IRF, historical R5, calibration redesign, production-model refactoring, or Results.

## 2. Accepted predecessor authority

Required predecessor completion:

`45a5e86d197e1032068b2d8b1d468a9a9dfda006`

Accepted predecessor terminal:

`MP4B_NONCONVERGED_HJB_POSTLOOP_SOURCE_SEMANTICS_ADJUDICATION_AND_ZERO_SCIENCE_VALIDATION_ADAPTER_REPAIR_PASS`

Required markers:

- `MP4B_MATLAB_HJB_NONCONVERGENCE_POSTLOOP_KFE_AGGREGATE_SEMANTICS_FROZEN`
- `MP4B_PYTHON_STATIONARY_ABORT_ON_HJB_NONCONVERGENCE_SOURCE_SEMANTICS_MISMATCH_CONFIRMED`
- `MP4B_PYTHON_MULTI_PROVINCE_SOURCE_POSTLOOP_HOUSEHOLD_ADAPTER_STATIC_PASS`

Accepted scientific classification:

`PYTHON_IMPLEMENTATION_ERROR__MULTI_PROVINCE_DRIVER_ABORTS_BEFORE_MATLAB_SOURCE_POSTLOOP_KFE_AND_AGGREGATES`

Do not reinterpret that classification as proof that the raw HJB arithmetic itself differs.

## 3. Primary authority and immutable identities

Retain:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Require exact SHA-256 before any scientific call:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- accepted MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py`:
  `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- accepted MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py`:
  `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- source-semantics map:
  `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter:
  `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- repaired validation driver:
  `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`

The production/export, MP2, MP3, source-semantics map, and accepted adapter bytes are immutable in this task.

## 4. Controlling authority to read

Read in full before mutation or execution:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- accepted MP1/MP2/MP3/MP4A2 reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_EXECUTION_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_EXECUTION_AGAINST_PRESERVED_MATLAB_BASELINE_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_NONCONVERGED_HJB_POSTLOOP_SOURCE_SEMANTICS_ADJUDICATION_AND_ZERO_SCIENCE_VALIDATION_ADAPTER_REPAIR_REPORT.md`
- `validators/multi_province/mp4b_nonconverged_hjb_source_semantics_map.json`
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`
- `validators/multi_province/mp4b_python_empirical.py`
- `validators/multi_province/mp4b_python_only_2009_stationary_comparator_contract.json`
- accepted Beijing source map/comparator evidence.

## 5. Live continuity

Required execution-start parent:

`45a5e86d197e1032068b2d8b1d468a9a9dfda006`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `45a5e86d197e1032068b2d8b1d468a9a9dfda006`;
3. require a clean worktree;
4. verify repository identity and controlling rules;
5. verify every immutable identity in Section 3;
6. verify no historical R5 / `chapter5_model` runtime dependency;
7. verify no scientific call has been made under this task before all preflight gates pass.

Any failure => stop before science.

## 6. Frozen corrected-2009 identity

Use exactly:

- `calendar_year = 2009`
- `analysis_index = 1`
- `workbook_data_row_index = 10`
- physical Excel row `11`
- `data_MAT_index = 1`
- `output_filename_year = 2009`
- `regression_vintage_key = 10`
- fixed-2020 `IND_Zt` only as the accepted source numerical initialization anchor.

Canonical input SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

The legacy coupled/wrong-year route is forbidden.

## 7. Immutable preserved MATLAB baseline

MATLAB scientific/model execution budget is exactly zero.

Read-only baseline root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require exact files and SHA-256:

- `matlab_calendar2009_stationary_output.mat`:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`

Frozen facts:

- terminal `COMPLETED`;
- outer turns `184`;
- MATLAB household calls `5,704`;
- final household convergence flags `31/31` true;
- preserved read-only national extraction: `Ct=283.3909431582526`, `At=47.95553248807161`, `Bt=65.2831672243048`, `Yt=350556701.89460325`, wage-bound counts upper/lower `7/17`.

If any required baseline artifact is missing, altered, unreadable, or non-unique by hash, stop BLOCKED. Do not reconstruct or rerun MATLAB.

## 8. Reused comparator contract — no widening

Reuse unchanged:

`validators/multi_province/mp4b_python_only_2009_stationary_comparator_contract.json`

Required SHA-256:

`E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`

Its rules remain controlling:

- calendar/province order/asset labels/categorical source states exact;
- first Beijing input exact accepted same-input contract;
- first Beijing `Ct/Lt/At/Bt/At+Bt` gate exactly
  `abs(M-P) <= 1e-7 * max(1,abs(M),abs(P))`;
- `AtTax` remains `EXCLUDED_NOT_UNIQUELY_SOURCE_BACKED` at that household layer;
- source-identical outer arithmetic exact when operation order matches, otherwise no wider than `rtol=atol=1e-12` where already accepted;
- final propagated continuous differences must be reported absolute/relative/normalized with no post-hoc acceptance tolerance;
- a turn-count difference requires neighboring convergence metrics and branch/adaptation threshold proof;
- qualitative diagnostics are secondary only;
- tolerance widening after output inspection is forbidden.

Required marker before science:

`MP4B_REEXECUTION_2009_STATIONARY_COMPARATOR_CONTRACT_REUSED_UNCHANGED`

## 9. Pre-run read-only MATLAB final-state field map

Before Python science, inspect only the preserved MATLAB output schema and the accepted Python final-state schema and freeze a small comparison field-map artifact.

Required marker:

`MP4B_PRESERVED_MATLAB_PYTHON_FINAL_STATE_FIELD_MAP_FROZEN`

Rules:

- map only fields with unique source-backed identical economic meaning;
- preserve exact 31-province order;
- do not infer aliases from similar names;
- do not substitute household labor for firm/destination labor or `At+Bt` for productive `At`;
- do not treat `AtTax` as a household same-interface field unless a new unique source-backed mapping is proven independently;
- include at minimum every uniquely mapped final provincial field necessary to diagnose household aggregates, productive capital/returns, firm state, wage/monetary state, government investment, convergence flags, and source controller/boundary categories;
- include mandatory national sums for `Ct`, `At`, `Bt`, and `Yt`;
- include preserved outer-turn count, final household convergence count, return-bound counts, and wage-bound counts when exposed.

The field map must be frozen before Python scientific output exists and may not be broadened or narrowed after inspecting that output except to mark a pre-frozen field `UNAVAILABLE_IN_PRESERVED_ARTIFACT` with evidence.

A bounded read-only comparator helper under `validators/multi_province/` may be added before science if no exact helper already exists. Its source and field-map SHA must be frozen before the Python invocation. It must not execute model science.

## 10. Mandatory zero-science preflight

Before consuming the Python stationary budget, establish all of the following with scientific/model calls exactly zero:

1. direct-script bootstrap resolves the current repository and accepted standalone SHA only;
2. fresh Python corrected-2009 presolver recursively equals the accepted MATLAB presolver manifest, mismatch `0`;
3. full first-turn source initialization passes all `24,800/24,800` cells with model calls `0`;
4. first-Beijing stationary-entry inputs conform to accepted household authority, semantic mismatch `0` under the existing primitive-array gate;
5. current validation driver routes province households only through `solve_matlab_source_postloop_household` and contains no active `solve_household_steady_state` call;
6. source-postloop adapter exact SHA and static composition remain accepted;
7. production/export, MP2, MP3 and canonical identities remain unchanged;
8. `py_compile` for touched validation/test files;
9. focused source-postloop adapter tests plus accepted raw-`Vb`, MP2, all seven MP3 scenario, online-stationary and entry-preflight regressions pass; report exact test count;
10. clean-process import/API check has zero model calls and no R5 runtime;
11. `git diff --check` passes;
12. Python scientific run root is absent and the target drive has adequate space.

Required marker:

`MP4B_SOURCE_POSTLOOP_ADAPTER_2009_STATIONARY_REEXECUTION_PREFLIGHT_PASS`

If any preflight fails, run neither Python science nor comparator science.

## 11. Scientific execution budget

Only after Sections 8-10 are frozen/PASS:

### Python corrected-2009 stationary

- top-level stationary invocation: maximum `1`;
- stationary reruns: `0`.

Internal source-postloop household adapter calls are part of this single top-level invocation. Each completed province household adapter call must contain exactly one accepted HJB primitive, one accepted KFE primitive, and one accepted aggregation primitive. A false HJB convergence flag must not itself abort the household or turn.

The accepted MP3 controller remains bounded by source `max_iterations=500` and `reg_threshold=1e-9`.

### MATLAB

All counts exactly `0`:

- stationary;
- household;
- HJB;
- KFE;
- presolver scientific rerun;
- helper/model rerun.

### Qualified comparator

- maximum `1`;
- comparator reruns `0`.

Run it only after the Python terminal artifact is durably persisted.

If Python reaches source max-iteration exhaustion and returns/persists a structurally valid `ManualSteadyStateResult`, the comparator may run once to classify the mismatch against preserved MATLAB.

If Python fails before a scientifically comparable terminal/final-state object exists, do not fabricate final comparison data and do not run a comparator that requires nonexistent state.

After the Python scientific invocation begins, no repair-and-rerun is permitted in this task.

## 12. Persistence and observability

Use one fresh direct-child no-overwrite Python scientific root under `D:\ProjectTemp`.

Persist at minimum:

- environment/source identity manifest;
- canonical identity;
- source-semantics map, adapter, driver and comparator identities;
- scientific call ledger;
- per-turn completed household count;
- per-turn household convergence count;
- each completed household HJB `converged`, `iterations`, and `convergence_statistic` in the existing batch diagnostics;
- turn-1 household inputs and outputs;
- outer history sufficient to inspect `nk_ratio_gap`, `yt_gap`, household convergence counts, return-bound counts, wage-bound counts, tKNratio before/after and adaptive actions;
- final state if produced;
- terminal category and iteration count;
- errors/warnings;
- output hashes.

Do not overwrite prior MP4B run roots. Do not commit large scientific artifacts.

## 13. Scientific comparison hierarchy

When Python evidence permits, compare in this order:

1. canonical/presolver identity;
2. first-turn Beijing input;
3. first-turn Beijing output against accepted repaired household authority;
4. turn-1 31-household completion/convergence pattern;
5. migration / `Lt_mat` / `Lt_supply` where preserved evidence permits;
6. productive `At`-only capital / `Kt_supply` / `rah`;
7. firm block;
8. wage / Taylor / fiscal diagnostics;
9. controller adaptation/damping/boundary sequence;
10. subsequent-turn household convergence patterns and source controller diagnostics;
11. final 31-province mapped state;
12. mandatory national sums `Ct`, `At`, `Bt`, `Yt`;
13. outer-turn count, final `31/31` household flags, return-bound counts, wage-bound counts.

Do not count downstream propagated differences as independent root causes after a supported upstream difference has been localized.

If MATLAB lacks a matching intermediate trace, mark it unavailable and do not rerun MATLAB.

## 14. Acceptance and failure classification

PASS terminal:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_WITH_SOURCE_POSTLOOP_ADAPTER_AGAINST_PRESERVED_MATLAB_PASS`

On PASS establish:

`MP4B_CORRECTED_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_ACCEPTED`

PASS requires at minimum:

- Python corrected-2009 route produces a complete scientifically comparable terminal state;
- no unexplained source/formula/data/controller/runtime mismatch remains;
- first Beijing accepted-household gate passes;
- source-postloop false-HJB semantics are honored throughout execution;
- Python terminal convergence/outer-controller status is compatible with preserved MATLAB under the frozen comparator contract;
- all mapped final state categories agree as required;
- every nonzero propagated continuous difference is reported and scientifically attributable under existing accepted numerical propagation rules rather than a new implementation/source discrepancy.

Do not establish the broader `MP4_MULTI_PROVINCE_STATIONARY_ROUTE_ACCEPTED` marker inside this executor task. Independent L3 review decides promotion.

Scientific/material mismatch terminal:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_WITH_SOURCE_POSTLOOP_ADAPTER_AGAINST_PRESERVED_MATLAB_MATERIAL_MISMATCH`

Infrastructure/source blocker before comparable science:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_WITH_SOURCE_POSTLOOP_ADAPTER_AGAINST_PRESERVED_MATLAB_BLOCKED`

For mismatch diagnosis use only evidence-supported classes, including:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`
- or `UNRESOLVED` when preserved observability is insufficient.

Do not guess a formula cause from a downstream difference.

## 15. Forbidden operations

Remain exactly zero:

- MATLAB stationary/household/HJB/KFE rerun;
- Python second stationary attempt;
- standalone ad-hoc household/HJB replay outside the authorized stationary invocation;
- turn-2 Shanxi diagnostic replay;
- 2010-2023 annual batch or another year;
- shocks/AR1;
- transition;
- genuine dynamics;
- IRF;
- historical R5 runtime;
- calibration changes;
- tolerance widening after output inspection;
- Results/manuscript claims.

## 16. Allowed repository changes

Execution should be as close to read-only code-wise as possible.

Allowed only:

- one bounded pre-run read-only final-state field-map JSON/MD under `validators/multi_province/`;
- one bounded read-only stationary comparator/helper under `validators/multi_province/` if required, completed and hash-frozen before Python science;
- focused non-scientific tests for that field map/comparator only;
- bounded CURRENT roadmap status update after terminal classification;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_WITH_SOURCE_POSTLOOP_ADAPTER_AGAINST_PRESERVED_MATLAB_REPORT.md`.

Do not modify:

- `src/ch5_two_asset_hank/economics.py`;
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`;
- `exports/matlab_faithful_two_asset_ha.py`;
- `src/ch5_two_asset_hank/multi_province/one_turn.py`;
- `src/ch5_two_asset_hank/multi_province/steady_state.py`;
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py`;
- `validators/multi_province/mp4b_matlab_source_postloop_household_adapter.py`;
- `validators/multi_province/mp4b_python_empirical.py`;
- accepted raw-`Vb` patch/evidence files;
- protected MATLAB;
- canonical input/data/cache/workbooks;
- controlling project rules;
- historical R5.

If any scientific or validation-driver repair appears necessary, stop and issue a successor rather than repairing and rerunning in this task.

## 17. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. immutable production/export/MP2/MP3/adapter/driver identities;
4. preserved MATLAB root/files/hashes;
5. reused comparator-contract identity;
6. frozen final-state field-map path/SHA;
7. comparator-helper path/SHA if created;
8. all zero-science preflight markers and exact test counts;
9. Python scientific root and artifact hashes;
10. exact Python stationary top-level count and rerun count;
11. exact household adapter/HJB/KFE/aggregate completed/attempted counts;
12. per-turn household convergence counts, especially any false flags;
13. Python terminal category and outer-turn count;
14. first-Beijing accepted-household comparison;
15. layer-by-layer comparison table;
16. mapped province-by-province differences when terminal state exists;
17. national `Ct/At/Bt/Yt` differences when terminal state exists;
18. outer convergence/controller/boundary/adaptation comparison;
19. first supported divergence if any;
20. root-cause classification;
21. material/unresolved/environment lists;
22. complete MATLAB-zero and forbidden-operation ledger;
23. `py_compile`, focused tests and `git diff --check`;
24. production/export/scientific-module mutation count;
25. changed paths;
26. commit SHA and GitHub read-back of every changed path;
27. final `HEAD == origin/main`, ahead/behind `0/0`, clean worktree;
28. exactly one recommended next gate.

## 18. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path.

On PASS recommend only independent L3 review for possible `MP4_MULTI_PROVINCE_STATIONARY_ROUTE_ACCEPTED` promotion before any MP5/shock work.

On MATERIAL/BLOCKED recommend exactly one bounded successor addressing the first supported unresolved cause. Do not automatically authorize another stationary rerun from the executor.