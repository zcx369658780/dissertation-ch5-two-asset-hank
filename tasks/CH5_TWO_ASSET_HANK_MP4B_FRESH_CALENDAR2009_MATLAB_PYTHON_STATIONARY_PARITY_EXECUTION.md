# CH5_TWO_ASSET_HANK_MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_EXECUTION

Date: 2026-08-30

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / one-shot stationary executor / parity reviewer

Owner: final scientific authority

## 1. Purpose

Execute the first **scientific** corrected-calendar-2009 stationary comparison after the MP4B validation infrastructure has passed:

- filename-concatenation repair static review;
- logical/physical path-equivalence smoke;
- full MATLAB/Python presolver same-input identity.

This task must perform at most one corrected calendar-2009 MATLAB stationary top-level invocation and, subject to the failure-order rules below, at most one corrected calendar-2009 Python stationary top-level invocation.

The task must not modify the accepted scientific model equations or automatically repair-and-rerun after a scientific invocation.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decision/adjudication documents;
- accepted MP4A2 report;
- all prior MP4B reports/tasks;
- `docs/CH5_TWO_ASSET_HANK_MP4B_MATLAB_FILENAME_CONCATENATION_REPAIR_PATH_SMOKE_AND_FRESH_PRESOLVER_REVALIDATION_REPORT.md`.

Preserve all accepted MP1-MP4A2 and MP4B infrastructure contracts, including:

- `OWNER_SINGLE_ACTIVE_CODEBASE_TWO_ASSET_HANK_ONLY`
- `OWNER_ECONOMIC_CALENDAR_YEAR_IS_WORKBOOK_CALENDAR_YEAR`
- `OWNER_2009_ANCHOR_USES_EXPLICIT_WORKBOOK_2009_ROW`
- `MATLAB_LEGACY_ANNUAL_YEAR_INDEX_COUPLING_DEFECT_CONFIRMED`
- `OWNER_DERIVED_MAT_CALIBRATION_CACHE_NOT_PRIMARY_SCIENTIFIC_AUTHORITY`
- `MATLAB_FIXED_2020_IND_ZT_RETAINED_AS_SOURCE_NUMERICAL_INITIALIZATION_ANCHOR`
- `OWNER_PARITY_MISMATCH_BOUNDED_ROOT_CAUSE_DIAGNOSIS_AUTHORIZED`
- `SOURCE_BINDING_AUDIT_COMPLETE__N_PROV_IS_ONLY_MISSING_REQUIRED_BINDING`
- source-faithful `global N_prov; N_prov=31;`
- finite verified logical/physical protected-root guard
- `MP4B_FILENAME_CONCATENATION_REPAIR_STATIC_REVIEW_PASS`
- `MP4B_LOGICAL_PHYSICAL_PATH_EQUIVALENCE_SMOKE_PASS`
- `MP4B_2009_PRESOLVER_SAME_INPUT_IDENTITY_PASS`.

Primary reconstruction authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

## 3. Live continuity

Expected execution-start parent / accepted infrastructure commit:

`a06216b8f22d21329bbb6b0bcff42fa101cb4166`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the accepted infrastructure commit;
3. require a clean worktree;
4. verify controlling-rule blobs unchanged;
5. verify protected MATLAB hashes unchanged;
6. verify the accepted standalone household oracle, MP2, MP3, MP4A2 `annual.py`, `stationary_runtime.py`, current MATLAB wrapper/helper chain and canonical input identities;
7. verify no historical `chapter5_model` runtime dependency.

If any identity fails, stop before scientific execution.

## 4. Frozen 2009 identity

Use exactly:

- `calendar_year = 2009`
- `analysis_index = 1`
- `workbook_data_row_index = 10`
- physical Excel row `11`
- `data_MAT_index = 1`
- `output_filename_year = 2009`
- `regression_vintage_key = 10`
- fixed-2020 `IND_Zt` only as the accepted source numerical initialization anchor.

Canonical input SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

The literal legacy wrong-year MATLAB route remains forbidden.

## 5. Accepted infrastructure identities to re-verify

Before execution, verify at minimum:

- non-scientific smoke manifest SHA-256: `0D4E337086E6D5CCAE5704CEC45CBB4DDA9E15C8AD8270633EF4C355F6FE7640`;
- current path smoke helper SHA-256: `D505505F3ED24E1E6C0808DAD2DD4BCB753FFCC17AE7E04DA7868E6BCE97148B`;
- current MATLAB stationary wrapper SHA-256: `DB3E9015C51CD9AE468CE693BF272605055276138EE8EAC7D2BF0BC43D37CB83`;
- fresh accepted MATLAB presolver manifest SHA-256: `B810618C7580BB7BFC3A9E78B4309119EF54EA7F2EBD1E492AFCBE9AEA99642D`;
- fresh accepted Python presolver manifest SHA-256: `1F95CFCB2D81A02B38FD1DC2F2BED632E31566809AFD59DBA85A52D7060BECAC`;
- accepted standalone HA oracle SHA-256: `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`.

Do not rerun the path smoke unless an identity above is missing or inconsistent. If the accepted smoke evidence is unavailable, stop BLOCKED rather than widening scope.

## 6. Fresh pre-execution gate

Before either scientific run:

1. check logical `C:\MatlabProgram` / physical `D:\MatlabProgram` relation under the controlling local-file rule;
2. record physical target and output drive free space;
3. verify all required protected MATLAB source hashes;
4. verify the canonical local artifact exists and hash-matches;
5. reconstruct/serialize fresh MATLAB and Python presolver manifests using the already accepted helpers;
6. compare them recursively and require semantic mismatch count zero;
7. verify all seven MP3 tiny scenarios remain exact under `stationary_runtime.py` online integration;
8. verify the current MATLAB helper chain contains zero unsafe char-plus filename suffix constructions;
9. run MATLAB `checkcode` on the current validation helpers;
10. freeze the numerical comparison contract described in Section 10 into a text/JSON manifest **before** inspecting scientific outputs.

Required marker:

`MP4B_FRESH_CALENDAR2009_SCIENTIFIC_EXECUTION_PREFLIGHT_PASS`

If it is not established, run neither scientific model.

## 7. Python one-shot empirical runtime preparation

The accepted `stationary_runtime.py` is an integration layer, not a standalone annual command. Before the Python scientific invocation, prepare a bounded validation-only one-shot entry under `validators/multi_province/` if no exact executable already exists.

It must:

- load the accepted canonical 2009 input;
- construct the source-bound initial province state from the accepted prepared-state manifest;
- for each outer turn, create one copied old-state snapshot;
- solve all 31 province households from that same copied old-state snapshot using the accepted MATLAB-faithful two-asset HA implementation and the accepted static adapter mapping;
- assemble one complete household-output batch only after all 31 household solves finish;
- pass that batch to accepted MP2 one-turn arithmetic and accepted MP3-equivalent online controller semantics;
- preserve `At`-only productive capital and `Lt_supply` firm labor;
- use source `reg_threshold=1e-9`, source `max3iter=500`, and accepted clipping/adaptation rules;
- persist a timestamped no-overwrite run manifest, per-turn summary and final state;
- perform no scientific work merely on import.

Before the scientific Python invocation, static/unit checks must prove the driver calls only accepted scientific components and does not introduce a second economic closure, generic root solver, legacy R5 dependency, or altered household equations.

If a source-required Python runtime field cannot be mapped unambiguously from the accepted adapter/prepared manifests, stop BLOCKED before the MATLAB scientific invocation. Do not consume the MATLAB budget while the Python scientific entry is knowingly incomplete.

## 8. Scientific execution budget

All prior MATLAB validation attempts remain consumed historical evidence and are not retries.

This task separately authorizes exactly:

### MATLAB

- corrected/decoupled calendar-2009 stationary top-level scientific invocation: maximum **1**.
- scientific reruns: **0**.

### Python

- corrected/decoupled calendar-2009 stationary top-level scientific invocation: maximum **1**.
- scientific reruns: **0**.

Internal provincial HJB/KFE calls are part of those top-level stationary invocations and remain bounded by source controller semantics.

Execution order:

1. all preflight/static/runtime-entry checks first;
2. MATLAB first;
3. if MATLAB fails before any provincial household HJB/KFE due infrastructure/environment/helper failure, stop BLOCKED and do not run Python;
4. if MATLAB reaches provincial household/model execution but scientifically nonconverges or hits a model-domain failure, preserve that scientific outcome and run Python once for comparison;
5. if MATLAB completes, run Python once;
6. after the Python invocation or any scientific MATLAB invocation, no repair-and-rerun in this task.

Forbidden calls remain zero:

- legacy wrong-year MATLAB;
- 2010-2023 batch;
- shocks/AR1;
- transition/genuine dynamics/IRF Results;
- historical one-asset R5;
- manuscript Results.

## 9. Run roots and required persistence

Use fresh timestamped no-overwrite roots under `D:\ProjectTemp` for MATLAB and Python.

For MATLAB persist available:

- source/storage/environment manifest;
- presolver manifest;
- source-binding/path identities;
- terminal status;
- profiler household and outer-turn call counts;
- final stationary object if produced;
- warnings/errors;
- output hashes.

For Python persist:

- source/storage/environment manifest;
- presolver manifest;
- terminal status;
- per-turn household call count;
- per-turn household outputs and outer summaries sufficient for diagnosis;
- final stationary object if produced;
- warnings/errors;
- output hashes.

Do not commit raw/private workbooks or large scientific `.mat`/JSON/NPZ outputs to GitHub. Commit only text-first summaries, hashes and small validators/tests.

## 10. Numerical comparison contract — freeze before runs

The executor must read the already accepted household parity reports and extract their existing same-input numerical bounds. Do not invent a looser household tolerance.

Freeze a comparison-contract artifact before scientific execution with these rules:

- categorical/integer/boolean/province-order/branch labels: exact;
- source-identical deterministic outer arithmetic conditional on identical upstream inputs: exact where operation order matches, otherwise maximum `rtol=atol=1e-12` only if the accepted MP2 contract already permits it;
- household HJB/KFE/policy/distribution/aggregate objects: use the already accepted household same-input parity contract exactly;
- final/propagated stationary differences: report absolute, relative and normalized differences; do **not** manufacture a broad post-hoc acceptance tolerance;
- convergence status, `ra` boundary-veto state, wage-bound diagnostics and controller termination: exact categorical agreement is required for PASS;
- outer branch/adaptation sequence must agree unless a difference is explicitly localized to an already accepted household numerical propagation effect and does not change economic/source semantics.

Qualitative diagnostics are secondary only: sign, relative direction, province ranking/order and clipping/boundary status. They never override an unexplained source/formula mismatch.

## 11. Comparison hierarchy

If both scientific routes exist, compare in this order:

1. presolver identity;
2. first-turn household inputs;
3. first-turn household outputs;
4. migration `Lt_mat/Lt_supply`;
5. At-only capital contribution / `Kt_supply` / `rah`;
6. firm block;
7. composite wage / Taylor / fiscal diagnostics;
8. controller diagnostics/adaptation/damping;
9. subsequent turns in order where trace is available;
10. final 31-province stationary state and national aggregates/diagnostics.

Do not treat downstream differences propagated from an already localized upstream difference as independent failures.

If MATLAB's current validation route does not expose a requested intermediate without new scientific instrumentation, do not edit and rerun. Compare the maximum available evidence and state explicitly which deeper localization would require a separate diagnostic gate.

## 12. Bounded root-cause diagnosis

If a mismatch exists after the one-shot runs, perform bounded **read-only** diagnosis using source, code and persisted artifacts. No model rerun is allowed.

Use only these scientific classifications when two scientific routes exist:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_SOURCE_OR_LEGACY_NUMERICAL_BEHAVIOR`
- `DATA_OR_CALIBRATION_PROVENANCE_MISMATCH`
- `SHARED_SOURCE_NUMERICAL_PROPAGATION_DIFFERENCE`
- `SCIENTIFIC_SPECIFICATION_OR_PROVENANCE_AMBIGUITY`

If execution fails before two scientific routes exist, use an infrastructure/source/environment blocker classification instead.

If existing traces are insufficient to identify the first scientific divergence, classify the comparison as MATERIAL/UNRESOLVED and recommend a separately authorized observability-only diagnostic gate. Do not guess and do not rerun.

## 13. Acceptance rules

PASS terminal:

`MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_PASS`

PASS requires at minimum:

- both corrected calendar-2009 scientific routes executed under the same accepted presolver identity;
- no unexplained structural/source-formula mismatch;
- convergence/termination and source-relevant branch/boundary status are compatible under the frozen contract;
- all directly comparable accepted household/source-local objects satisfy their existing contracts;
- propagated/final differences are fully accounted for by accepted numerical propagation rather than an unresolved implementation/source discrepancy.

Scientific mismatch terminal:

`MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_MATERIAL_MISMATCH`

Use it if both scientific routes exist but the frozen comparison contract is materially violated or the first scientific divergence remains unresolved.

Infrastructure/source/environment blocker:

`MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_BLOCKED`

## 14. Allowed repository changes

Allowed writes are limited to:

- one bounded validation-only Python one-shot entry/comparator under `validators/multi_province/` if required;
- focused non-scientific tests for that entry/comparator;
- bounded text-only parity comparison helpers/manifests;
- CURRENT roadmap status update;
- one report:

`docs/CH5_TWO_ASSET_HANK_MP4B_FRESH_CALENDAR2009_MATLAB_PYTHON_STATIONARY_PARITY_EXECUTION_REPORT.md`

Do not modify:

- protected MATLAB;
- current smoke-validated MATLAB wrapper/helper scientific logic;
- accepted household/HJB/KFE/oracle arithmetic;
- MP2 arithmetic;
- MP3 controller semantics;
- canonical 2009 input;
- primary workbooks/regression/distance/cache;
- controlling rules;
- historical R5.

If a pre-run defect would require modifying any accepted scientific arithmetic or the smoke-validated MATLAB wrapper computation, stop BLOCKED and issue a successor task rather than repairing it here.

## 15. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. accepted infrastructure identities;
4. fresh preflight marker;
5. fresh presolver semantic mismatch count;
6. comparison-contract identity;
7. MATLAB/Python run roots;
8. exact scientific call ledger including all historical consumed MP4B attempts versus this task;
9. MATLAB outer-turn and household-call counts;
10. Python outer-turn and household-call counts;
11. convergence/termination and iteration counts;
12. layer-by-layer parity table;
13. object-by-object exact/within-accepted-bound/material classification;
14. final province/national differences;
15. qualitative sign/ranking/boundary diagnostics;
16. first divergent stage/object if any;
17. bounded root-cause classification;
18. material mismatch/unresolved/environment lists;
19. tests/checks;
20. forbidden-operation check;
21. Git closeout;
22. exactly one recommended next gate.

## 16. Next-stage boundary

Even on PASS, this task does not authorize shocks or 2010-2023.

On PASS, recommend exactly one next gate: **MP5A source-law / named-response role freeze and corrected-2009 response preparation**, initially audit/preparation only.

On MATERIAL or BLOCKED, recommend exactly one bounded successor addressing only the localized cause.

## 17. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require `HEAD == origin/main`, ahead/behind `0/0`, and clean worktree.
