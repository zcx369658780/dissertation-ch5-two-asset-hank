# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_SAME_ACTIVE_INPUT_HJB100_HJB500_AND_LEGACY_KFE_FRESH_EXECUTION_AFTER_PATH_RECERTIFICATION

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / isolated MATLAB scientific executor

Owner: final scientific authority

## 1. Explicit scientific-call authorization

Immediate live parent at publication:

`b5c45a8f0dbef7919d8e9dee1b672a889fd4605d`

The parent report closed the zero-science path/collector chain with:

`MP4C_2018_CALL725_MATLAB_PATH_COLLECTOR_ZERO_SCIENCE_RECERTIFIED__SYNTHETIC_MEMBERSHIP_AND_WHICH_SMOKE_PASS__READY_FOR_FRESH_MATLAB_TERMINATION_REPLAY_TASK`.

The Owner/L3 scientific route already authorized the isolated MATLAB same-HJB-active-input termination comparison in the predecessor task

`tasks/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_SAME_ACTIVE_INPUT_HJB100_HJB500_AND_LEGACY_KFE_REPLAY.md`.

The two intervening failures were infrastructure-only and consumed zero scientific HJB/KFE/household calls. This task explicitly reopens a **fresh bounded MATLAB scientific-call budget** for the same unchanged scientific question only.

Authorized scientific question:

> On the exact captured 2018 Anhui call-725 HJB-active household state, does the protected MATLAB-faithful HJB also fail to satisfy the source criterion by iteration 100, converge when only `num.maxit` is increased to 500, and then yield the same admissible source-faithful legacy-KFE household solution as the already persisted Python isolated result?

This is not production termination-policy acceptance, not a 2018 GE/annual rerun, and not IRF/Results evidence.

## 2. Live continuity

At start:

1. `git fetch origin`;
2. require this exact task live on `origin/main` as direct child of `b5c45a8f0dbef7919d8e9dee1b672a889fd4605d`;
3. require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean;
4. read `AGENTS.md`, all CURRENT rules, this task, the original MATLAB replay task, the zero-science recertification report, the Python call-725 replay report, accepted HJB parity report, accepted KFE parity closeout, accepted aggregate parity closeout, and the relevant MATLAB source map/wrapper.

No execution from a dirty tracked worktree.

## 3. Reuse the certified path authority; do not redesign it

Certified evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-path-collector-zero-science-recertification-20260904-001`

Require audit-manifest SHA-256:

`A94A24DD4C32B9DE816B7E3A9B8E507CDC755BEB2759E472FD139BDEDBAD5F75`.

Require the persisted facts:

- 9/9 synthetic finite-root cases PASS;
- `C:\MatlabProgram` is a one-target Junction to `D:\MatlabProgram`;
- all 4 protected sources match on logical and physical paths, 8/8 hash checks;
- MATLAB R2022b `which('HANK_2ASSETS_HJB')` resolves to the exact physical root;
- resolved HJB SHA matches the protected SHA;
- all predecessor scientific call counts are zero.

Do not rebuild or retune the path collector in this task. Read and hash the certified artifacts. A simple fresh protected-source hash check before science is allowed; a new synthetic collector campaign is not required.

Frozen finite-root authority remains exactly:

- logical: `C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`;
- physical: `D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`.

Protected SHA-256 values:

- `HANK_2ASSETS_HJB.m` `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`;
- `HANK3_FOC.m` `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`;
- `HANK3_cost.m` `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`;
- `lab_solve2.m` `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`.

Any identity mismatch: STOP before scientific call.

## 4. Frozen Python comparator evidence — reuse only

Python isolated-call evidence root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-hjb-replay-and-bounded-continuation-20260904-001`

Require audit-manifest SHA-256:

`B15FE27D8531D5A1CE65E5D881327F820D82501FABD10B789F9F8B0544C7A0CF`.

Do not rerun Python.

Frozen call-725 state:

- province `安徽`;
- zero-based province index `11`;
- outer iteration `24`;
- global household call `725`;
- `rah=0.09`;
- `rb=0.02`;
- `tau=0.05`;
- `w=16.82014806560587`;
- `Tt=0.1`;
- `rb_gap=0.07`.

Frozen Python HJB100:

- `converged=false`;
- iterations `100`;
- statistic `0.3038218386543494`.

Frozen Python HJB500:

- `converged=true`;
- iterations `196`;
- statistic `2.2986279546444166e-10`.

Frozen Python HJB500 + source-faithful legacy KFE isolated aggregates:

- `C_ss=10.434969443057815`;
- `L_ss=0.6241861388467347`;
- `A_ss=9.212879584614942`;
- `B_ss=-1.5496915046150406`;
- `A_ss+B_ss=7.663188079999902`.

Permanent caveat remains:

`CAPTURE_TIME_RAW_HASH_GAP_REMAINS__RETROSPECTIVE_CURRENT_FILE_INTEGRITY_CERTIFIED`.

## 5. Hard scientific boundary

Task type:

`FRESH_ISOLATED_MATLAB_CALL725_TERMINATION_PARITY_AFTER_PATH_RECERTIFICATION__NO_GE__NO_PRODUCTION_CHANGE`.

Forbidden:

- 2018 annual/GE outer-loop execution;
- any province other than isolated Anhui call 725;
- Python scientific rerun;
- R/PLM;
- shock/IRF/Results;
- protected MATLAB source edits;
- repository production/model/test edits;
- changing equations, grid, parameters, prices, tax, transfer, borrowing spread, `crit`, `Delta`, KFE gauge/row/RHS/normalization/solver;
- G1/G2, adaptive row, mass-normalization gauge, pseudoinverse, regularization, fallback, clipping, grid expansion, alternate solver;
- warm-starting HJB500 from HJB100 terminal value;
- automatic retry.

The only authorized numerical variation is:

`num.maxit: 100 -> 500`.

No other numerical parameter may change.

## 6. Mandatory zero-call source/dependency audit

Before MATLAB scientific calls, statically re-establish from the protected source and accepted source maps:

- HJB finite ceiling is `num.maxit`;
- `crit=1e-7`;
- `Delta=1000`;
- grid is 20x20x2 with `b=[-2,5]`, `a=[0,10]`, `z=[0.8,1.3]`, source switch matrix;
- `ga=2`, `alphap=1`, `alphal=1`, `rho=0.05`, `frisch_l=0.2`;
- `chi0=0.1`, `chi1=2`, `a_bar=1e-6`.

Identify every `results.*` field used by HJB initialization/iteration/post-loop, KFE, and the requested household aggregates. The captured active state must be sufficient for the chosen source-extracted execution architecture. Do not guess a missing execution-critical field.

If a required input cannot be reconstructed from existing persisted evidence without GE rerun, STOP before science with:

`MP4C_2018_CALL725_MATLAB_FRESH_REPLAY_BLOCKED__REQUIRED_ACTIVE_INPUT_NOT_DURABLY_AVAILABLE__NO_SCIENCE_CALL`.

## 7. MATLAB evaluator architecture freeze before calls

Prefer the already accepted source-extracted MATLAB HJB-only evaluator architecture from:

`D:\ProjectTemp\ch5-hjb-propagation-aware-final-20260830-001`.

Before calls, freeze/hash the exact evaluator path(s), source mapping, input contract, output persistence, and accepted parity contract. If the old evaluator is fixture-specific, an external validation-only adaptation is allowed only for input/grid plumbing and durable output serialization; the HJB iteration formulas, branch logic, source assembly, convergence criterion, initialization formulas, and sparse solve must remain source-identical.

Persist an exact diff/source-map showing that no scientific formula changed. No helper/evaluator is committed to the repository.

If a trustworthy source-extracted 800-state evaluator cannot be established before science, STOP rather than broadening scope.

Required pre-call marker:

`MP4C_2018_CALL725_MATLAB_FRESH_SAME_ACTIVE_INPUT_CONTRACT_AND_EVALUATOR_FROZEN__SCIENTIFIC_BUDGET_OPEN`.

## 8. Phase A — exactly one MATLAB HJB100

Run exactly one isolated MATLAB HJB-only evaluation from the frozen original source initialization with:

- exact call-725 active state;
- `maxit=100`;
- `crit=1e-7`;
- `Delta=1000`;
- all other source fields unchanged.

No KFE in Phase A.

Persist at minimum:

- converged flag;
- iteration count;
- final statistic;
- value/policy finiteness;
- post-loop operator shape/nnz/support/values;
- warnings/errors.

Compare with frozen Python HJB100 under the accepted propagation-aware parity contract:

- convergence boolean hard gate;
- iteration count exact when observable;
- same-input source-formula/coefficient replay uses `128*eps64*max(1,abs(x),abs(y))`;
- mathematical sparse support ignores only exact `0.0/-0.0` storage;
- solver-propagated raw differences remain diagnostic only when all direct replay gates pass.

If Phase A execution/instrumentation fails after the scientific call, persist and STOP; do not rerun HJB100.

## 9. Phase B — exactly one MATLAB HJB500

Only after Phase-A output is durably persisted.

Run exactly one second isolated MATLAB HJB-only evaluation from the **same original source initialization**, not from HJB100 terminal state.

Sole change:

`maxit=500`.

Persist the same diagnostics.

If HJB500 remains nonconverged, STOP before KFE. Do not increase to 1000/2000.

If converged, compare with frozen Python HJB500 under the accepted propagation-aware parity contract, including iteration count/statistic where source-extracted output exposes them.

## 10. Phase C — exactly one unchanged MATLAB legacy KFE

Only if Phase-B HJB500 converges.

Run exactly one source-faithful MATLAB contaminated-row KFE on the HJB500 post-loop operator:

- state count `800`;
- MATLAB one-based row `296`;
- full row replaced by unit row;
- RHS `0.007`;
- direct full solve;
- `db=7/19`;
- `dah=10/19`;
- normalization `sum(raw_g)*db*dah`;
- no additional z/quadrature/trapezoid weight.

No fallback/retry/alternate gauge.

Persist, if exposed:

- warnings;
- raw finiteness;
- normalization factor/mass;
- density min/max;
- negative count/weighted negative mass;
- contaminated-system residual/backward-error certificate;
- household `Ct`, `Lt`, `At`, `Bt`, `At+Bt`.

Compute aggregates with the exact accepted source formulas only. Compare to the frozen Python HJB500+legacy-KFE isolated aggregates using the previously accepted same-input/finite-sum contracts. Do not create a new broad tolerance after observing output.

These remain isolated household diagnostics, not 2018 GE quantities.

## 11. Strict fresh scientific-call budget

Maximum new scientific calls in this task:

- MATLAB HJB100 = `1`;
- MATLAB HJB500 = `1` only after Phase A persistence;
- MATLAB legacy KFE = `1` only after HJB500 convergence.

Retries = `0`.

Python scientific calls = `0`.

GE/annual/stationary outer-loop = `0`.

R/PLM/shock/IRF/Results = `0`.

If an accepted whole-household MATLAB evaluator necessarily couples HJB+KFE, freeze that decomposition before the first call and do not exceed two total protected household executions; never double-count by also running an extra KFE.

## 12. Required scientific classification

Choose the strongest supported:

- `MATLAB_CALL725_HJB100_NONCONVERGED_AND_HJB500_CONVERGED__PYTHON_HJB_CEILING_DIAGNOSIS_CROSS_LANGUAGE_SUPPORTED__LEGACY_KFE_AGGREGATES_PARITY_PASS__PRODUCTION_TERMINATION_POLICY_OWNER_REVIEW_REQUIRED`;
- `MATLAB_CALL725_HJB100_NONCONVERGED_AND_HJB500_CONVERGED__LEGACY_KFE_OR_AGGREGATE_PARITY_FAILS__CROSS_LANGUAGE_TERMINATION_POLICY_NOT_YET_ACCEPTED`;
- `MATLAB_CALL725_HJB100_CONVERGED__PYTHON_MATLAB_TERMINATION_DIVERGENCE_REQUIRES_FORENSIC__NO_PRODUCTION_CHANGE`;
- `MATLAB_CALL725_HJB500_NONCONVERGED__HJB_CEILING_DIAGNOSIS_NOT_CROSS_LANGUAGE_CLOSED__NO_PRODUCTION_CHANGE`;
- `MATLAB_CALL725_FRESH_REPLAY_BLOCKED__SOURCE_CONTRACT_EVALUATOR_OR_ENVIRONMENT_FAILURE__NO_PRODUCTION_CHANGE`.

Even the strongest PASS does not authorize production `maxit=500` automatically.

## 13. Evidence root and publication

Use fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-matlab-termination-replay-after-path-recertification-20260904-001`.

Persist at minimum:

- authority/source/path-recertification identities;
- Python predecessor reuse identity;
- source dependency audit;
- evaluator freeze/source-map/diff;
- exact MATLAB active-input contract;
- HJB100 output;
- HJB500 output if run;
- KFE/aggregate output if run;
- cross-language comparator output;
- scientific call ledger;
- stdout/stderr/warnings;
- classification;
- `audit_manifest.json`.

Write only repository report:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_MATLAB_SAME_ACTIVE_INPUT_HJB100_HJB500_AND_LEGACY_KFE_FRESH_EXECUTION_AFTER_PATH_RECERTIFICATION_REPORT.md`.

If completed consistently, only report-only commit + non-force push is authorized.

Suggested commit:

`Replay MP4C 2018 call-725 MATLAB HJB termination after path recertification`

After push: fresh-fetch; require `HEAD == origin/main`, ahead/behind `0/0`, tracked worktree clean.

PASS terminal:

`MP4C_2018_CALL725_MATLAB_FRESH_TERMINATION_REPLAY_COMPLETE__CROSS_LANGUAGE_HJB_CEILING_AND_LEGACY_KFE_CLASSIFIED__NO_GE_NO_PRODUCTION_CHANGE`

Blocked terminal:

`MP4C_2018_CALL725_MATLAB_FRESH_TERMINATION_REPLAY_BLOCKED__NO_GE_NO_PRODUCTION_CHANGE`.

Do not modify production or launch 2018 GE/IRF after this task. Return to ChatGPT L3 / Owner for the next scientific decision.