# CH5_TWO_ASSET_HANK_MP4B_FINAL_STATE_FIRST_DIVERGENCE_AND_CONTROLLER_BRANCH_LOCALIZATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / read-only final-state divergence localizer

Owner: final scientific authority

## 1. Purpose

Use the now-aligned durable MATLAB/Python corrected-calendar-2009 stationary evidence to localize the earliest **source-backed interface / controller branch** at which the final steady states can be shown to differ.

This is a zero-science forensic task. It must not rerun either stationary model, any household/HJB/KFE solve, MP2/MP3 scientific execution, MATLAB, comparator, another year, shocks, transition, IRF, R5, or Results.

The task must distinguish carefully between:

1. an **aligned final-state mismatch**;
2. a **source dependency / controller branch signature** that can be proven from immutable artifacts;
3. a **chronological first divergence** between MATLAB and Python outer turns.

A chronological first divergence may be claimed only if preserved MATLAB evidence actually contains the required turn-level state. If MATLAB has no matching turn trace, report that limitation explicitly and localize only the earliest source-backed interface / branch signature supported by the evidence.

Do not infer that MATLAB is wrong or Python is wrong merely because final states differ.

## 2. Required predecessor authority

Required execution-start predecessor:

`7276ff60cec418deb7c19b4a5aa5a66b5c90d9e5`

Accepted predecessor terminal:

`MP4B_COMPARATOR_REPRESENTATION_CONTRACT_REMEDIATION_AND_DURABLE_TERMINAL_REPLAY_PASS`

Accepted facts to preserve:

- province representation defect is repaired only in the read-only comparator;
- the 31 provinces are aligned position-by-position;
- comparator replay artifact schema is `MP4B_PRESERVED_MATLAB_PYTHON_FINAL_STATE_COMPARISON_V2`;
- final outer turns are MATLAB/Python `184/184`;
- final household convergence counts are `31/31` and `31/31`;
- `ra` upper/lower counts are `0/0` in both;
- wage lower counts are `17/17`;
- wage upper counts are MATLAB `7`, Python `5`;
- `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT` remains established;
- final aligned continuous differences are material and no post-hoc tolerance has been introduced;
- no corrected-2009 stationary parity marker is established.

## 3. Live continuity

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `7276ff60cec418deb7c19b4a5aa5a66b5c90d9e5`;
3. require clean worktree;
4. read `AGENTS.md`, `project_rules/PROJECT_RULE_INDEX_CURRENT.md`, `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`, and `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
5. verify all immutable repository and local-artifact identities below;
6. verify no active historical R5 / `chapter5_model` runtime dependency.

Any identity failure => BLOCKED before diagnostic work.

## 4. Immutable repository identities

Require exact SHA-256 unless a Git blob is specified:

- `src/ch5_two_asset_hank/economics.py`: `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`: `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`: `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `one_turn.py`: `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `steady_state.py`: `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `stationary_runtime.py`: `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map: `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter: `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver: `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- canonical corrected-2009 input: `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- comparator contract: `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map: `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- remediated comparator helper: `0FD6889E10E502F32C10B3373702445333B452B25A11A63A3899184A67853EF3`
- remediated comparator helper Git blob: `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Do not modify any of these objects in this task.

## 5. Immutable durable artifacts

### 5.1 Preserved MATLAB

Root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require:

- `matlab_calendar2009_stationary_output.mat`: `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`: `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`: `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`.

### 5.2 Durable Python stationary

Root:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require:

- `python_run_manifest.json`: `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- `python_terminal_summary.json`: `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`
- `turn_001_household_inputs.json`: `79B7A2805ECBAACDFCC70FA194E154263FA46EE313415A7060F90C65662DCE28`
- `turn_001_household_outputs.json`: `A464C45C9BBDA8D33314CC8FF6570F34AA51815006E22E3B9997A874E82D2648`
- `turn_184_household_inputs.json`: `5DB3D7024E34FCB4F08388EC33448924C057F89DD52ECB47D8CA09157C66D2ED`
- `turn_184_household_outputs.json`: `70442A793408DCDE20C84F83CA4795FA3EB95865052714F9FE9A31ABFC350442`.

### 5.3 Aligned comparator replay

Artifact:

`D:\ProjectTemp\ch5-mp4b-final-state-comparator-replay-after-representation-remediation-20260831-001.json`

Require:

- bytes: `106389`
- SHA-256: `77916C2376B96D7C94CBF15A2E5DED1BCF366C4430FACCE84746794B468986AB`
- schema: `MP4B_PRESERVED_MATLAB_PYTHON_FINAL_STATE_COMPARISON_V2`.

No durable artifact may be overwritten.

## 6. Frozen source update / dependency order

Use the already accepted source order only:

1. pre-frozen provincial household outputs;
2. migration labor;
3. `At`-only productive-capital allocation and pre-firm household `rah`;
4. province firm block;
5. household composite wage;
6. Taylor `it/rb` assignment;
7. fiscal diagnostics;
8. outer convergence/adaptation controller (`Zt`, `GovInv`, `tKNratio`).

The controller source facts to preserve:

- `GovInv` may change only under the source adaptation branch by multiplication by `0.9` or `1.1`;
- low-return branch: `ra < ramin + 0.02` => `GovInv *= 0.9`;
- high-return branch: `ra > ramax - 0.02` => `GovInv *= 1.1`;
- adaptation is allowed only when `max_nk_gap < 0.1` and steady-state mode is active;
- `Zt` is reset when `abs(Yt/Yt0 - 1) > 0.01` under the same adaptation-allow gate;
- `tKNratio_next = 0.6*KNratio + 0.4*tKNratio_old` on nonterminal turns;
- wage clipping occurs in the firm block before composite household wage;
- wage boundary counts are diagnostic and are not themselves an outer acceptance veto;
- `rb/it` are Taylor-derived and are exact in the aligned final comparison.

Do not introduce another update ordering.

## 7. Required aligned final-state mismatch signature

From the immutable comparator replay, persist a compact mismatch signature table covering all frozen fields.

At minimum record for each field:

- number of provinces with exact equality;
- number with nonzero difference;
- maximum normalized difference and province;
- median normalized difference;
- whether the field is household output, spatial allocation output, firm output, policy output, fiscal output, or controller-carried state.

Explicitly verify the already observed signatures rather than assuming them:

- `rb` exact in all 31 provinces;
- `it` exact in all 31 provinces;
- `GovInv` exact in 30 provinces and different in exactly one province;
- identify that province and raw MATLAB/Python `GovInv` values;
- determine the exact ratio of the two `GovInv` values and whether it equals one source multiplier (`0.9`, `1.1`, `1/0.9`, or `1/1.1`) within floating-representation evidence, without inventing a tolerance after inspection;
- `wjt` exact-equality count and the full exact mismatch set;
- exact MATLAB/Python wage-upper and wage-lower province sets, not just counts;
- the five largest normalized mismatches for `At`, `Bt`, `Lt`, `Yt`, `w`, `wjt`, `ra`, `rah`, `Kt`, `GovInv`, `Zt`, and `KNratio`.

The report must explicitly discuss the large Zhejiang signature if direct replay evidence confirms it; do not hard-code Zhejiang as the answer before reading the artifact.

## 8. Controller-branch provenance localization

### 8.1 Common initial state

Read the canonical corrected-2009 initial state and establish the initial `GovInv`, `Zt`, return/wage bounds, and relevant controller state for the province(s) with final `GovInv` mismatch.

### 8.2 Python recorded controller history

Use only the already durable `python_terminal_summary.json -> history`.

For each province with final `GovInv` mismatch, extract every Python adaptive action:

- outer turn;
- `max_nk_gap` / adaptation allow status;
- `GovInv` before/after;
- action `NONE`, `LOW_RA_DECREASE_0P9`, or `HIGH_RA_INCREASE_1P1`;
- `ra`, `ramin`, `ramax` and distances to both branch thresholds at the state-before-adaptation layer;
- `Zt` before/after and whether `Zt` adjusted;
- `Yt/Yt0 - 1`;
- `KNratio`, `tKNratio_before`, `tKNratio_after`;
- `wjt`, `wjtmin`, `wjtmax` and wage-bound status.

Report the count and ordered sequence of Python `0.9/1.1` actions for that province.

### 8.3 MATLAB final controller-lattice evidence

Using the common canonical initial `GovInv`, the immutable MATLAB final `GovInv`, and the source fact that the only controller mutations are multiplicative `0.9/1.1`, test whether the MATLAB final value can be represented by a small integer pair `(n_low,n_high)` such that:

`GovInv_final = GovInv_initial * 0.9^n_low * 1.1^n_high`

Use exact / floating-forward reconstruction evidence with bounded integer search no larger than the source outer-turn budget; do not use an optimizer or fit a continuous exponent.

Do the same for Python final `GovInv` and compare with its directly recorded action history as an internal cross-check.

If the final MATLAB/Python `GovInv` ratio is exactly explained by one additional/missing source `0.9` or `1.1` action and common initialization is proven, establish the bounded marker:

`MP4B_FINAL_GOVINV_CONTROLLER_BRANCH_COUNT_DIFFERENCE_LOCALIZED`

This marker means only that the durable final controller states differ by a source-defined discrete action count. It does **not** prove the chronological turn at which MATLAB and Python first diverged unless MATLAB turn-level evidence exists.

If multiple integer action-count combinations are observationally equivalent, report them and do not overclaim uniqueness.

## 9. Wage-boundary localization

From aligned final artifacts, identify exact province sets:

- MATLAB `wjt == wjtmax`;
- Python `wjt == wjtmax`;
- MATLAB `wjt == wjtmin`;
- Python `wjt == wjtmin`.

For every province in the symmetric difference of the wage-upper sets, report:

- MATLAB/Python `wjt`;
- `wjtmax`;
- MATLAB/Python `wt0` **if persisted and uniquely source-backed**;
- MATLAB/Python `Kt`, `Lt`, `Yt`, `mt`, `KNratio`, `Zt`, `alpha` if available;
- whether the difference is merely the clipping category or also a pre-clipping firm-state difference.

If `wt0` is not available in one artifact, say so and do not reconstruct it unless all exact source inputs are persisted.

Preserve:

`MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`.

Do not call the wage clipping logic itself a bug without proving a same-input branch disagreement.

## 10. Source-equation residual checks on final artifacts

Without invoking any scientific module, perform direct arithmetic consistency checks only for identities whose complete inputs are uniquely persisted in each final artifact.

Required where inputs exist:

- `Kt = Kt_supply + GovInv`;
- `KNratio = Kt / Lt`;
- `Yt = Zt * Kt^alpha * Lt^(1-alpha)`;
- Taylor `it` and `rb` assignment if its shared inputs are available;
- exact boundary membership for `ra` and `wjt`.

For each identity, report MATLAB residual and Python residual separately.

Do **not** evaluate a formula if one of its source inputs is missing or if a current-vs-old-turn ambiguity exists. Mark it `UNAVAILABLE_WITH_PRESERVED_ARTIFACTS`.

Do not invoke `run_source_faithful_one_turn`, `run_manual_steady_state`, any household solver, or the comparator.

## 11. First-divergence classification discipline

Use the strongest classification supported by evidence.

### A. Chronological first divergence proven

Only if a preserved MATLAB artifact contains source-backed turn-level state matching the Python history, identify the earliest turn + province + field where a source-comparable value/branch differs.

Marker if proven:

`MP4B_CHRONOLOGICAL_FIRST_DIVERGENCE_LOCALIZED`

### B. Controller branch signature localized, chronology unavailable

Use if final/common-initial evidence proves a discrete `GovInv` action-count difference but no matching MATLAB turn trace exists:

`MP4B_CONTROLLER_BRANCH_SIGNATURE_LOCALIZED__CHRONOLOGY_UNAVAILABLE`

### C. Earliest source-interface family localized, root cause unresolved

Use if the dependency graph narrows the mismatch to a household/spatial/firm/controller interface family but same-input evidence is insufficient for a causal attribution:

`MP4B_FINAL_STATE_SOURCE_INTERFACE_DIVERGENCE_LOCALIZED__ROOT_CAUSE_UNRESOLVED`

### D. Unresolved

Use:

`MP4B_FINAL_STATE_FIRST_DIVERGENCE_UNRESOLVED_WITH_PRESERVED_EVIDENCE`

if current artifacts cannot support a stronger localization.

Important: household `Ct/At/Bt` final differences do not by themselves prove a household-solver bug because final household prices (`w`, `rah`, etc.) may also differ. A same-input household arithmetic defect requires same-input evidence.

Likewise, a final wage clipping difference does not prove the firm formula is wrong if pre-clipping inputs differ.

## 12. No-science call ledger

Must remain exactly zero:

- Python stationary;
- Python household adapter;
- HJB;
- KFE;
- household aggregation;
- MP2/MP3 scientific execution;
- qualified comparator / `compare_terminal`;
- MATLAB stationary / household / HJB / KFE / presolver;
- Shanxi replay;
- other years / annual batch;
- shocks / AR1 / transition / dynamics / IRF;
- historical R5;
- Results.

Allowed actions are read/parse/hash of immutable artifacts, source inspection, direct arithmetic residual calculations, and bounded integer enumeration of the source `0.9/1.1` controller lattice.

## 13. Allowed repository changes

Allowed only:

- optional one read-only diagnostic helper under `validators/multi_province/` that imports no scientific runtime and only parses artifacts / calculates bounded arithmetic diagnostics;
- focused zero-science tests for that helper if created;
- bounded update to `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md` only if needed to record the accepted diagnostic terminal;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FINAL_STATE_FIRST_DIVERGENCE_AND_CONTROLLER_BRANCH_LOCALIZATION_REPORT.md`.

Do not modify any scientific module, export, driver, adapter, comparator/helper, comparator contract, field map, canonical data, protected MATLAB, project rules, or historical R5.

## 14. Acceptance

PASS terminal:

`MP4B_FINAL_STATE_FIRST_DIVERGENCE_AND_CONTROLLER_BRANCH_LOCALIZATION_PASS`

PASS requires:

- complete identity verification;
- aligned mismatch signature;
- exact wage-boundary province sets;
- controller-lattice / Python-history analysis for any final `GovInv` mismatch;
- source-equation residual checks where inputs are available;
- one classification from Section 11;
- zero scientific/model calls.

BLOCKED terminal:

`MP4B_FINAL_STATE_FIRST_DIVERGENCE_AND_CONTROLLER_BRANCH_LOCALIZATION_BLOCKED`

Use BLOCKED for identity failure, unreadable artifacts, or if the required diagnostic cannot be completed without prohibited science/reruns.

This task cannot establish corrected-2009 stationary parity and cannot authorize shocks/IRFs.

## 15. Required report

Include at minimum:

1. terminal verdict;
2. live continuity and immutable identities;
3. no-science ledger;
4. full mismatch signature by source layer;
5. exact field equality counts / max / median signatures;
6. exact `GovInv` mismatch province/value/ratio evidence;
7. common initial controller state;
8. full Python adaptive-action history for the mismatch province(s);
9. bounded MATLAB/Python controller-lattice reconstruction;
10. whether `MP4B_FINAL_GOVINV_CONTROLLER_BRANCH_COUNT_DIFFERENCE_LOCALIZED` is established;
11. exact MATLAB/Python wage-upper/lower province sets;
12. detailed symmetric-difference wage-boundary rows;
13. final source-equation residual checks;
14. preserved MATLAB turn-trace availability audit;
15. chronological first-divergence status;
16. one Section-11 classification;
17. unsupported causal hypotheses explicitly separated from supported findings;
18. `py_compile` / focused tests if a helper is added;
19. `git diff --check`;
20. changed paths;
21. forbidden-operation audit;
22. commit/push/GitHub read-back closeout;
23. exactly one recommended next gate.

## 16. Next-stage routing

Recommend exactly one successor based on evidence:

- if a concrete same-input implementation defect is proven, one bounded repair/specification task before any rerun;
- if only a controller branch-count signature is proven, one read-only branch-trigger provenance task if preserved evidence can sharpen the cause;
- if final differences are internally source-consistent but represent a distinct equilibrium/path, one Owner scientific decision gate before any attempt to force parity;
- do not proceed to shocks/IRFs until L3 has reviewed this localization and decided whether the corrected-2009 baseline is sufficiently understood.
