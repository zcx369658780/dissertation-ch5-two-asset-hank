# CH5_TWO_ASSET_HANK_MP4B_FINAL_STATE_PROVINCE_ORDER_REPRESENTATION_AND_ORDER_INVARIANT_TERMINAL_DIAGNOSTIC

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / read-only representation diagnostic executor

Owner: final scientific authority

## 1. Purpose

Diagnose the final-state province-order / representation incompatibility that prevented the frozen preserved-MATLAB/Python comparator from consuming the durable corrected-calendar-2009 Python terminal.

This task is **read-only with respect to all scientific/model code and all existing accepted comparator/field-map objects**. It must use only the already persisted MATLAB and Python terminal artifacts plus canonical 2009 provenance to determine whether the comparator failure is:

1. a pure extraction/storage-order or string-representation problem;
2. a genuine Python final-state province-order problem;
3. a preserved-MATLAB persistence/order problem;
4. or unresolved with current evidence.

It must also independently record only those terminal facts that are permutation-invariant and already frozen by the comparison contract/field map, so that an order mismatch cannot hide an already material final-state discrepancy.

No Python/MATLAB stationary rerun is authorized. No household/HJB/KFE/aggregation replay is authorized. No comparator repair or qualified comparator retry is authorized in this task.

## 2. Accepted predecessor authority

Required execution-start predecessor:

`ffd5b6a0437ea5699bd90f1ef02c0dc87800f9c0`

Accepted predecessor terminal:

`MP4B_PYTHON_ONLY_CORRECTED_CALENDAR2009_STATIONARY_REEXECUTION_AFTER_FULL_PREFLIGHT_MATERIAL_MISMATCH`

Accepted predecessor facts include:

- Python terminal `SOURCE_CONVERGED`;
- Python outer turns `184`;
- Python household/HJB/KFE/aggregate calls `5,704` each;
- Python final household flags `31/31`;
- turn 2 alone had seven `hjb_converged=false` flags and source-postloop semantics handled them correctly;
- preserved MATLAB terminal also completed at `184` outer turns and `5,704` household calls with final `31/31` flags;
- qualified comparator was consumed exactly once and failed at the exact province-order gate before emitting a comparison artifact;
- Python/MATLAB stationary reruns remain zero.

Do not reinterpret the accepted source-postloop adapter repair in this task.

## 3. Live continuity

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `ffd5b6a0437ea5699bd90f1ef02c0dc87800f9c0`;
3. require clean worktree;
4. verify repository identity and controlling rules;
5. verify all immutable identities in Section 4;
6. verify all local preserved artifacts in Section 5 by path and SHA-256;
7. verify no active historical R5 / `chapter5_model` runtime dependency.

Any identity failure => BLOCKED before diagnostic extraction.

## 4. Immutable repository identities

Require exact SHA-256 and do not modify:

- `src/ch5_two_asset_hank/economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`
- `exports/matlab_faithful_two_asset_ha.py`:
  `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`
- MP2 `src/ch5_two_asset_hank/multi_province/one_turn.py`:
  `D18C4385745E33E917B79994CAA069BE8825C9F22919A5A9EDE918845312D98D`
- MP3 `src/ch5_two_asset_hank/multi_province/steady_state.py`:
  `7065D0FCBFDA6A88C8F773DEBDF277F6E300A8E0195B7CC635F7A0033D74D88C`
- `src/ch5_two_asset_hank/multi_province/stationary_runtime.py`:
  `226BE912AB776F57A8D8EFACE912AB2A3331E865638AC36976F6D578BDB086A0`
- source-semantics map:
  `6A4FD1576100D7CE36787EAA7E6B833ACED2D94B89B929EC6ADD45559995C028`
- source-postloop adapter:
  `8A6308870606A02886E9A8E4B32E942A7D433237405E6B9E86EE4175FB2DFF06`
- validation driver:
  `9033218710204CA4EA2AF0351376E47BB5B4F203923E6155DC4776ADD336091E`
- canonical corrected-2009 input:
  `507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`
- stationary comparator contract:
  `E74E5BF8506AF841BEDB07004C9DCD71E64E1F6143DC8B5C01F9FF734C6C3C3A`
- final-state field map:
  `A1D0F04D9FC77975D7E11EDBA44EF91FD860D5344D72688B68494FD9316024CB`
- existing read-only comparator helper:
  `A6F7D2BBF7EE0936A6A0A45880B41D7AC77DB5AA7C3CA4B0F207F2D2A2DC08CF`
- corrected entry-test Git blob:
  `89ff42fb99f24ed89ff162e69f2d6c3e01a052eb`.

The existing comparator helper and field map are evidence under diagnosis, not mutable objects in this task.

## 5. Immutable local artifacts

### 5.1 Preserved MATLAB

Root:

`D:\ProjectTemp\ch5-mp4b-fresh-calendar2009-matlab-20260830-001`

Require:

- `matlab_calendar2009_stationary_output.mat`:
  `6233AB8B7380BE4D4E136851BC59F747EEB78B285B3A7A5903FEFFE64BCF464B`
- `matlab_profile_summary.mat`:
  `040C38E9E5FEE374202840C7741B7A15D6CFAE668FF8302619225FEEEC5DC90C`
- `matlab_terminal_status.json`:
  `04D143B7553D1041B810B875575C06AE3E2F82132B2D55FEFDF5C3ED8AAB7270`.

### 5.2 Durable Python one-shot terminal

Root:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require at minimum:

- `python_run_manifest.json`:
  `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- `python_terminal_summary.json`:
  `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`
- `turn_184_household_outputs.json`:
  `70442A793408DCDE20C84F83CA4795FA3EB95865052714F9FE9A31ABFC350442`.

No file may be overwritten.

## 6. Exact province identity/order diagnostic

Persist one fresh no-overwrite diagnostic JSON under `D:\ProjectTemp` and, if useful, one bounded diagnostic helper under `validators/multi_province/`.

The diagnostic must expose separately:

### 6.1 Canonical order

Read exact `province_order` from the canonical corrected-2009 JSON and from the Python run manifest. Require those two to be exactly equal before proceeding.

Record all 31 canonical names, index, Unicode code points, string length, and a stable UTF-8 hash per name.

### 6.2 Python terminal order

Read `python_terminal_summary.json -> final_state`.

Record:

- exact list of 31 `name` values in sequence;
- length and uniqueness;
- exact equality to canonical order;
- exact name-set equality to canonical names;
- first positional mismatch if any;
- if same exact name set but different sequence, exact permutation from canonical index to terminal index;
- code points / UTF-8 bytes/hash for mismatched strings only.

Do not normalize, trim, translate or reorder before the raw comparison.

### 6.3 MATLAB persistence/storage order

Inspect the raw HDF5/MAT object structure without executing MATLAB:

- shape/dtype of `st/results` and `st/grids`;
- raw object-reference array dimensions;
- `C`-order flatten sequence;
- `F`-order flatten sequence;
- direct logical indexing sequence that best represents MATLAB cell-array indexing, with source-backed justification;
- decoded `prvname` under each relevant sequence;
- exact name-set equality to canonical order;
- exact sequence equality to canonical order;
- first mismatch / permutation if same set.

Do not change `_text` or the existing helper. If there is a string representation issue, record exact code points/encoding/storage evidence before suggesting any reversible interpretation.

### 6.4 Existing helper behavior

Using source inspection and zero-science function-level reads only, show exactly which MATLAB reference sequence the frozen helper currently uses:

`np.asarray(handle["st/results"]).reshape(-1, order="F")`

and identify whether that sequence is or is not the canonical MATLAB logical province sequence.

Do not call `compare_terminal` again in this task.

## 7. Order-invariant terminal facts

The frozen field map already designates national sums and exposed boundary categories. Independently re-read these from the immutable artifacts **without requiring province alignment**.

This is a diagnostic extraction, not a qualified comparator retry.

Record exactly for MATLAB and Python:

- outer-turn count;
- final household converged count;
- `ra_upper_count`;
- `ra_lower_count`;
- `wage_upper_count`;
- `wage_lower_count`;
- national sums `Ct`, `At`, `Bt`, `Yt`.

For the four national sums, report raw values and arithmetic absolute/relative/normalized differences in the diagnostic report, clearly labeled `ORDER_INVARIANT_DIAGNOSTIC_ONLY`. Do not introduce or apply an acceptance tolerance.

Important L3 review observation to verify directly from artifacts:

- predecessor Python report states final wage upper/lower counts `5/17`;
- accepted preserved-MATLAB schema test expects `7/17`.

Because these counts are permutation-invariant, if direct artifact reread confirms `5/17` versus `7/17`, establish:

`MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT`

This mismatch cannot be erased by merely reordering provinces.

Likewise, do not suppress a material order-invariant national-sum difference merely because the aligned comparator has not yet been repaired.

## 8. Classification tree

After the exact diagnostic, use one primary province-order classification:

### A. Comparator extraction/storage representation error

Use:

`VALIDATION_COMPARATOR_MATLAB_STORAGE_ORDER_OR_STRING_REPRESENTATION_ERROR`

only if all are proven:

- canonical order == Python run-manifest order == Python terminal order exactly;
- MATLAB logical/source province order is canonical;
- existing helper extraction alone produces the rejected sequence due to storage flattening or reversible string representation;
- no actual model state order changed.

### B. Python final-state order mismatch

Use:

`PYTHON_FINAL_STATE_PROVINCE_ORDER_SCIENTIFIC_MISMATCH`

if Python terminal `final_state` sequence differs from its own canonical/run-manifest province order beyond a pure reversible representation issue.

### C. MATLAB persistence order difference

Use:

`MATLAB_PERSISTED_RESULT_ORDER_DIFFERS_FROM_CANONICAL_SOURCE_ORDER`

if the persisted MATLAB `st/results` logical order itself is demonstrably different from canonical source order, rather than only the helper extraction being wrong.

### D. Representation ambiguity

Use:

`FINAL_STATE_PROVINCE_IDENTITY_OR_REPRESENTATION_UNRESOLVED`

if the exact source-backed correspondence cannot be established without modifying source or rerunning science.

A primary order classification does **not** override any independently confirmed order-invariant boundary/national mismatch.

## 9. No-science call ledger

Must remain exactly zero:

- Python stationary;
- Python household adapter;
- HJB;
- KFE;
- household aggregation;
- MP2/MP3 scientific execution;
- qualified comparator / `compare_terminal`;
- MATLAB stationary/household/HJB/KFE/presolver;
- Shanxi replay;
- other years / annual batch;
- shocks / AR1 / transition / dynamics / IRF;
- historical R5;
- Results.

Reading/parsing/hashing immutable JSON/HDF5/MAT artifacts is authorized.

## 10. Allowed repository changes

Allowed only:

- optional one bounded read-only diagnostic helper under `validators/multi_province/` that performs identity/order/storage introspection only;
- focused zero-science tests for that helper if created;
- bounded CURRENT roadmap status update;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FINAL_STATE_PROVINCE_ORDER_REPRESENTATION_AND_ORDER_INVARIANT_TERMINAL_DIAGNOSTIC_REPORT.md`.

Do not modify:

- any `src/` scientific module;
- `exports/matlab_faithful_two_asset_ha.py`;
- validation driver;
- source-postloop adapter;
- existing frozen comparator helper;
- existing field map;
- existing comparator contract;
- protected MATLAB;
- canonical input/data/cache/workbooks;
- project rules;
- historical R5.

## 11. Acceptance

Diagnostic PASS terminal:

`MP4B_FINAL_STATE_PROVINCE_ORDER_REPRESENTATION_AND_ORDER_INVARIANT_TERMINAL_DIAGNOSTIC_PASS`

PASS means the province-order failure has been exactly localized to one of the classifications in Section 8 and all available order-invariant terminal facts have been durably extracted without science.

BLOCKED terminal:

`MP4B_FINAL_STATE_PROVINCE_ORDER_REPRESENTATION_AND_ORDER_INVARIANT_TERMINAL_DIAGNOSTIC_BLOCKED`

Use BLOCKED for identity failure, unreadable artifacts, ambiguous source correspondence, or any need to rerun/modify science to continue.

This task cannot establish corrected-2009 stationary parity acceptance, even if the order problem is a comparator-only error, because the prior scientific terminal is already MATERIAL and order-invariant mismatches must be separately adjudicated.

## 12. Required report

Include at minimum:

1. terminal verdict;
2. live continuity;
3. all immutable identities;
4. zero-science ledger;
5. canonical 31-province sequence;
6. Python run-manifest and terminal sequences;
7. raw MATLAB `st/results` / `st/grids` shapes and storage orientation;
8. MATLAB province sequences under relevant logical/storage traversal orders;
9. exact existing-helper extraction sequence;
10. first mismatch and permutation maps;
11. exact string/codepoint/encoding evidence where relevant;
12. primary order/representation classification;
13. MATLAB/Python outer-turn counts and final household convergence counts;
14. MATLAB/Python return/wage boundary counts;
15. whether `MP4B_FINAL_WAGE_BOUNDARY_CATEGORY_MISMATCH_CONFIRMED_ORDER_INVARIANT` is established;
16. order-invariant national `Ct/At/Bt/Yt` raw values and abs/rel/normalized differences, without acceptance tolerance;
17. material mismatch list separate from representation mismatch list;
18. `py_compile` / focused tests if a helper is added;
19. `git diff --check`;
20. changed paths;
21. forbidden-operation audit;
22. commit/push/GitHub read-back closeout;
23. exactly one recommended next gate.

## 13. Next-stage routing

Recommend exactly one next gate based on evidence:

- if A (comparator extraction error): one zero-science comparator-helper remediation plus one separately authorized comparator-only replay against the already durable MATLAB/Python terminals; **no stationary rerun**;
- if B (Python order mismatch): one read-only source/order propagation localization gate before any scientific repair;
- if C (MATLAB persistence order difference): one source-backed persisted-order mapping specification/comparator remediation gate;
- if D: one smallest representation/provenance ambiguity-resolution gate.

If any order-invariant material mismatch is confirmed, the successor must preserve it explicitly and may not claim parity merely by repairing province alignment.

No other year, annual batch, shocks, AR1, transition, dynamics, IRF, R5 or Results is authorized.

## 14. Closeout

Explicit-path staging only. One execution commit. One non-force push. GitHub read-back every changed path. Require final:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- forbidden-operation audit PASS.
