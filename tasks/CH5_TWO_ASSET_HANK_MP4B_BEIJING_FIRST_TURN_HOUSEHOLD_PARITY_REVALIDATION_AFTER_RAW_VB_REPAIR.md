# CH5_TWO_ASSET_HANK_MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_PARITY_REVALIDATION_AFTER_RAW_VB_REPAIR

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / scientific parity executor

Owner: final scientific authority

## 1. Purpose

Resume exactly the Beijing first-turn household-parity route that was blocked before scientific execution by the MATLAB wrapper persistence gate.

The predecessor infrastructure gate is now accepted:

- `MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_REPAIR_AND_REPLACEMENT_ZERO_CALL_SMOKE_PASS`
- `MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_ZERO_CALL_SMOKE_PASS`

This task may therefore reuse the accepted wrapper smoke evidence, re-establish the remaining source-map/comparator/runner preflight, restore the already accepted raw-`Vb` candidate bytes, and execute exactly one same-input Beijing household solve in MATLAB and exactly one in the repaired standalone Python implementation.

This is **not** a multi-province stationary task. It does not authorize `HANK_mp_1turn`, `HANK_mp_1eq`, a second province, calendar-2009 outer iteration, annual batch execution, shocks, transition, dynamics, IRF, R5, or Results.

## 2. Primary scientific authority

Retain:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

The objective is numerical faithfulness to the working protected MATLAB household algorithm, not replacement by a cleaner numerical design.

Frozen source semantics include:

1. adjustment cost:
   `chi0*abs(d) + chi1*d^2/(2*max(a,a_bar))`;
2. transfer FOC uses bare/raw `a`, not `max(a,a_bar)`;
3. illiquid-return taper:
   `r_a * (1 - 0.1*(a/a_max)^9)`;
4. liquid asset `b` may be negative; illiquid asset `a>=0`;
5. stationary KFE uses the MATLAB contaminated-row solve;
6. raw-`Vb` ordering:
   - construct raw forward/backward `Vb` plus source boundary treatment;
   - consumption/labor FOCs use `max(raw Vb,1e-6)` only at their source use site;
   - transfer FOCs use raw `Va/Vb`;
   - MATLAB does not reject a candidate merely because raw `Vb<=0` before that source ordering.

Do not alter these contracts in this task.

## 3. Controlling authority to read

Read in full before mutation:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_BEIJING_MATLAB_HOUSEHOLD_WRAPPER_R2022B_PERSISTENCE_REPAIR_AND_REPLACEMENT_ZERO_CALL_SMOKE_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_STANDALONE_SYNC_AND_BEIJING_HOUSEHOLD_PARITY_REVALIDATION_REPORT.md`
- accepted raw-`Vb` source-semantics, local-policy, 50-state HJB and standalone reports/tasks referenced by those reports
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_END_TO_END_AGGREGATE_COMPARATOR_NUMPY_BOOL_SERIALIZATION_CORRECTION_AND_CLOSEOUT_REPORT.md`

The last report is comparison-method evidence only. Its frozen-price fixture values are not the Beijing target values.

## 4. Live continuity

Required execution-start predecessor:

`1ff7722a969b736ef0a801d5502e4d332e43a869`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as the direct child of `1ff7722a969b736ef0a801d5502e4d332e43a869`;
3. require clean worktree;
4. verify repository identity and current rule files;
5. verify predecessor wrapper/report identities and accepted smoke evidence;
6. verify the three production/export objects are still at the rolled-back authority bytes before applying candidate patches;
7. verify all protected MATLAB hashes before any scientific execution.

Any identity failure => stop before candidate mutation or scientific execution.

## 5. Required starting identities

### 5.1 Rolled-back current Python bytes

- `src/ch5_two_asset_hank/economics.py` SHA-256:
  `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py` SHA-256:
  `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py` SHA-256:
  `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

### 5.2 Protected MATLAB source

Protected logical root:

`C:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Protected physical root:

`D:\MatlabProgram\2023年12月2日 多省份神经网络HANK`

Required hashes:

- `HANK_2ASSETS_HJB.m`
  `049136B769560040BC678F828F5D3EC5338DDCAA2090D6BED4E40732F56C3EAE`
- `HANK3_FOC.m`
  `772B7B7BBF528DFDD246BD152B3E3026035012FE50F30DA808C1EE18C0F8463D`
- `HANK3_cost.m`
  `3504A74BB2DB9FBEE3B292AB68D758954248643D6A5178E42554E0D4794F9A3C`
- `lab_solve2.m`
  `74FD6AE8D76AB50A571831FAB95464AE4FCE919F3E70B660E5ABF6B9C5662C20`

Source binding must use the exact C/D root pair + Junction evidence + source SHA + finite exact resolved-parent membership.

Do **not** use Java `getCanonicalPath()` as Junction authority. Do not use `startsWith`, substring matching, filename-only matching, broad `D:\MatlabProgram` trust, or arbitrary sibling trust.

### 5.3 Accepted wrapper smoke

Required wrapper:

`validators/multi_province/matlab/mp4b_beijing_household_wrapper.m`

Required SHA-256:

`518B0F9137ADA16155EE76EA2A08B21C0B3D91D67C321A2EF89C063B1EAC5AFD`

Required focused-test SHA-256:

`24C009E1D179D6F4FC8CB8DA96C2AF3AB7D057BD902EC93F6A34B373620CFF16`

Required preserved replacement-smoke evidence:

- root:
  `D:\ProjectTemp\ch5-mp4b-beijing-wrapper-replacement-smoke-20260831-001`
- manifest:
  `matlab_wrapper_smoke_manifest.json`
- manifest SHA-256:
  `99C19A4C2676E052F7D5C3F2A8C3AF0CB704EADAF29E7D87EF7E4F0A4D40023D`

Read back this smoke manifest if locally available and verify the exact marker and zero scientific-call ledger. Do not rerun smoke. If the local artifact is unavailable, the repository report may establish historical acceptance, but current wrapper SHA and protected source binding must still be independently reverified before the scientific call.

## 6. Frozen Beijing same-input contract

Canonical annual input:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Frozen Beijing candidate contract:

`D:\ProjectTemp\ch5-mp4b-beijing-household-20260831-001\beijing_same_input_contract.json`

SHA-256:

`FE833FAEB48521CD0C7594627AF6FB5012F9497A455E9B2C5E7490E0C40E6F22`

This task may promote that candidate contract only to the extent demonstrated by the one-call MATLAB/standalone parity result. Do not change its scientific values.

## 7. Frozen raw-Vb patches

### 7.1 Modular patch

Path:

`validators/multi_province/mp4b_faithful_raw_vb_modular_candidate.patch`

Required SHA-256:

`0F044055DA9B4BFF22A2F8342EF189781AD3D536BFD2A67148C1182C1F9AB31D`

Apply exactly once to the starting authority bytes.

Required candidate hashes after application:

- `economics.py`:
  `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- `matlab_faithful_policy.py`:
  `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`

The shared corrected/reference `transfer_candidate` path must remain unchanged.

### 7.2 Standalone patch

Path:

`validators/multi_province/mp4b_faithful_raw_vb_standalone_candidate.patch`

Required SHA-256:

`FC4DAC660130DEB73E1A88C6638F1C4B282D511AA06875123437693FBE4C5A71`

Apply exactly once to the starting standalone authority bytes.

Required repaired standalone candidate SHA-256:

`B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`

The patch must remain limited to the already accepted faithful raw-`Vb` source semantics. No new economic/numerical repair is authorized.

If either patch is not byte-identically applicable or produces a different hash, stop before scientific execution.

## 8. Reused accepted raw-Vb evidence — no reruns

Preserve and reuse without rerunning MATLAB scalar/local-policy/HJB batches:

- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_PASS`
- `MP4B_FAITHFUL_RAW_VB_STANDALONE_CANDIDATE_PATCH_FROZEN`
- `MP4B_MODULAR_STANDALONE_RAW_VB_SOURCE_MAP_EQUIVALENCE_PASS`
- `MATLAB_FAITHFUL_STANDALONE_RAW_VB_TRANSFER_HELPER_10CASE_SOURCE_PARITY_PASS`
- `MATLAB_FAITHFUL_STANDALONE_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_PASS`

Do not rerun the ten-case MATLAB `HANK3_FOC` batch, the 12-case MATLAB local-policy batch, or the accepted 50-state MATLAB/Python HJB batch.

## 9. Mandatory source-map preflight before scientific calls

Re-establish and persist a validation-only source map for the exact Beijing household input and output objects.

Every required MATLAB `param`, `grid`, `num`, `CHI`, and incoming `results` field used by the wrapper's `run` mode must be bound to exactly one of:

1. the frozen Beijing same-input contract;
2. the canonical 2009 input;
3. an exact protected-source constant/assignment with source file SHA and location;
4. an already accepted frozen artifact whose identity is explicitly verified.

No guessed value, hidden fallback, convenient default, inferred calibration, or legacy-R5 value is allowed.

The same source map must bind the corresponding explicit arguments/configuration consumed by standalone `solve_household_steady_state`.

Required properties:

- exact Beijing province identity;
- exact first-turn state;
- exact `(b,a,z)` grid/order and dimensions;
- exact household prices/returns/wage/tax/transfers/migration objects required by source;
- exact labor-initialization inputs;
- exact `CHI`/adjustment parameters;
- exact HJB/KFE tolerances and source algorithm settings;
- exact incoming first-turn `results` state required by MATLAB;
- no historical `chapter5_model`/one-asset R5 runtime dependency;
- no second-province state.

If any execution-critical field has ambiguous or multiple scientific sources, classify the blocker and stop before both scientific calls. Do not resolve scientific ambiguity by choosing a convenient value.

Required preflight marker:

`MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_SAME_INPUT_SOURCE_MAP_PREFLIGHT_PASS`

## 10. MATLAB runner/persistence preflight

Do not modify the accepted wrapper bytes in this task.

A separate validation-only parity runner/harness may be created under `validators/multi_province/` to:

- construct the exact source-mapped input structs;
- call the accepted wrapper in `run` mode exactly once;
- persist the returned MATLAB household result and a compact JSON summary into a fresh no-overwrite `D:\ProjectTemp` root;
- record command/stdout/stderr/exit code and hashes.

Before consuming the MATLAB household budget, statically prove:

- the accepted wrapper SHA remains exact;
- the parity runner reaches the wrapper exactly once;
- the wrapper `run` mode reaches exactly one `HANK_2ASSETS_HJB` call;
- no `HANK_mp_1turn`, `HANK_mp_1eq`, `mpHANK_equilibrium_2000`, `multi_prov_HANK_12sts`, second province, annual loop, shock, transition, dynamics or IRF route is reachable;
- all persistence paths are fresh and no-overwrite;
- full local scientific artifacts are never staged to GitHub.

Use R2022b-compatible strict no-overwrite persistence. No `fopen(...,'x')`.

Required preflight marker:

`MP4B_BEIJING_FIRST_TURN_MATLAB_HOUSEHOLD_RUNNER_AND_PERSISTENCE_PREFLIGHT_PASS`

## 11. Standalone runner preflight

Create or reuse a validation-only Python runner that imports the repaired standalone candidate and invokes exactly one top-level:

`solve_household_steady_state`

for the frozen Beijing same-input contract.

Before the call, verify:

- standalone candidate SHA is exactly `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`;
- function API/signature is source-mapped and explicit;
- no repo-local production import is required by the standalone file;
- no historical R5 runtime dependency exists;
- no hidden economic default fills a source-mapped field;
- runner persistence is fresh/no-overwrite;
- one top-level standalone call is syntactically and structurally enforced.

Required preflight marker:

`MP4B_BEIJING_FIRST_TURN_STANDALONE_HOUSEHOLD_RUNNER_PREFLIGHT_PASS`

## 12. Comparator contract — freeze before scientific execution

Freeze the comparator implementation and gate formula before either household call.

### 12.1 Mandatory source-backed comparison objects

Compare:

- household convergence flag/status;
- `Ct` / total consumption;
- `Lt` / source-defined productivity-weighted effective household labor;
- `At` / illiquid household assets;
- `Bt` / liquid household assets;
- `At + Bt`, derived separately on each side from the preceding source-backed fields.

Compare `AhTax` / `AtTax` only if preflight proves one unique source-backed MATLAB field/formula and one unambiguous standalone counterpart with identical economic meaning. If this uniqueness is not established, mark the tax aggregate `EXCLUDED_NOT_UNIQUELY_SOURCE_BACKED` rather than guessing or failing the core parity gate.

Do not substitute other MATLAB diagnostics such as `Bt_pos`, `Bt_neg`, `Bdotres`, `Income`, `St`, `Ut`, `Borrow`, `Borrowint`, `CHI_H`, or `UC` for the required aggregates.

### 12.2 Exact/categorical checks

Require:

- MATLAB household converged: true;
- standalone household converged: true;
- no NaN/Inf in compared outputs;
- asset labels are exact (`At` illiquid, `Bt` liquid);
- `At+Bt` on each side is the arithmetic identity from the persisted `At` and `Bt` fields;
- any included tax field has exact source-role identity.

### 12.3 Continuous numerical gate

Freeze before execution:

`abs(M-P) <= 1e-7 * max(1, abs(M), abs(P))`

for each mandatory continuous aggregate and for the optional tax aggregate if included.

This `1e-7` scaled gate is a ceiling, not a tuning target. It must not be widened after seeing results.

Also report:

- absolute difference;
- relative difference;
- ULP/binary64-scale diagnostics where practical;
- the previously accepted finite-sum arithmetic rule and `gamma_n` diagnostic for the actual aggregate term count when source weights/order are available.

The tighter arithmetic diagnostics are explanatory evidence; the frozen `1e-7` scaled gate controls material aggregate parity for this Beijing integration run.

Required pre-execution marker:

`MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_COMPARATOR_CONTRACT_FROZEN`

## 13. Scientific execution budget and mandatory ordering

Only after Sections 9-12 all PASS:

### 13.1 MATLAB

Exactly one top-level Beijing household invocation through the accepted wrapper `run` mode.

Budget:

- MATLAB Beijing `HANK_2ASSETS_HJB` top-level calls: `1`
- MATLAB household reruns: `0`

The internal stationary HJB/KFE stages executed inside that one protected household function are part of this single top-level household invocation; they do not authorize any separate MATLAB HJB/KFE replay.

Persist the complete returned household result locally if feasible plus a compact source-backed comparison summary and hashes.

If MATLAB fails, stop. Do not execute the standalone household.

### 13.2 Standalone Python

Only after a complete persisted MATLAB household result exists:

- repaired standalone `solve_household_steady_state` top-level calls: `1`
- standalone household reruns: `0`

Its internal HJB/KFE work is contained within that one call and does not authorize any separate replay.

If standalone execution fails, preserve evidence and stop. Do not run comparator.

### 13.3 Comparator

Only after both complete outputs exist:

- qualified comparator calls: `1`
- comparator reruns: `0`

If comparator infrastructure fails after its single call, preserve evidence and stop. Do not repair/rerun it in this task.

## 14. Explicit zero budgets outside the two household calls

All remain exactly zero:

- modular Python household solve;
- separate modular HJB/KFE scientific replay;
- MATLAB ten-case `HANK3_FOC` rerun;
- MATLAB 12-case local-policy rerun;
- accepted 50-state MATLAB/Python HJB rerun;
- accepted wrapper smoke rerun;
- exact-junction smoke rerun;
- `HANK_mp_1turn`;
- `HANK_mp_1eq`;
- second-province household;
- MATLAB calendar-2009 stationary rerun;
- Python calendar-2009 multi-province stationary;
- MP2/MP3 empirical/model reruns;
- 2010-2023 annual batch;
- shock execution;
- transition;
- dynamics;
- IRF;
- historical R5 runtime;
- Results.

## 15. PASS behavior — household authority promotion

Only if the qualified comparator PASSes all mandatory objects:

1. establish:
   `MP4B_BEIJING_FIRST_TURN_MATLAB_STANDALONE_HOUSEHOLD_PARITY_ACCEPTED`;
2. revalidate the repaired standalone file's clean-room/API/dependency boundary **without another household solve**;
3. require a clean-process import/API smoke that does not invoke the solver;
4. require static proof that the standalone file does not import current repo production modules or historical R5 runtime modules;
5. freeze the repaired standalone SHA:
   `B92F6EFC59D9398F89F8FB6EE67BF6C5F947282D76895051BEC194967EC9C3E3`;
6. retain the two modular candidate hashes and the repaired standalone bytes as the new accepted raw-`Vb` household implementation authority;
7. establish:
   - `MATLAB_FAITHFUL_RAW_VB_REPAIRED_MODULAR_HOUSEHOLD_AUTHORITY_ACCEPTED`
   - `MATLAB_FAITHFUL_RAW_VB_REPAIRED_STANDALONE_HOUSEHOLD_AUTHORITY_ACCEPTED`.

This promotion means the corrected raw-`Vb` household implementation is accepted for the bounded household block. It does **not** accept calendar-2009 multi-province stationary equilibrium, GE closure, shocks, transitions, IRFs, or Results.

## 16. Failure/mismatch behavior

If any preflight, MATLAB call, standalone call, or qualified comparison fails:

- stop at the first failed gate;
- preserve no-overwrite local evidence;
- do not rerun any consumed scientific call;
- do not tune tolerances;
- do not invent adapters or additional economic semantics after execution;
- do not run calendar-2009 stationary;
- before repository closeout, restore the three production/export candidate files to the starting rolled-back authority bytes unless PASS under Section 15 has been established;
- commit only bounded validation/report evidence allowed below.

Classify a completed comparison failure as one of:

- `PYTHON_IMPLEMENTATION_ERROR`
- `MATLAB_LEGACY_LIMITATION`
- `SCIENTIFIC_SPECIFICATION_AMBIGUITY`
- `MATERIAL_NUMERICAL_MISMATCH_UNRESOLVED`

Do not classify by guess; retain `UNRESOLVED` when evidence is insufficient.

## 17. Allowed repository changes

Allowed:

- on PASS only:
  - `src/ch5_two_asset_hank/economics.py` at exact accepted candidate SHA;
  - `src/ch5_two_asset_hank/matlab_faithful_policy.py` at exact accepted candidate SHA;
  - `exports/matlab_faithful_two_asset_ha.py` at exact accepted repaired standalone SHA;
- validation-only source-map/runner/comparator files under `validators/multi_province/`;
- narrowly scoped focused tests for those validation-only objects;
- one report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_PARITY_REVALIDATION_AFTER_RAW_VB_REPAIR_REPORT.md`;
- a bounded CURRENT-roadmap status update only if needed to prevent stale active status.

Do not modify:

- accepted wrapper `validators/multi_province/matlab/mp4b_beijing_household_wrapper.m`;
- either frozen raw-`Vb` patch file;
- protected MATLAB source;
- canonical input/data/cache;
- corrected/reference transfer helper path;
- accepted HJB/operator/KFE source outside the exact candidate patch;
- MP1/MP2/MP3 scientific logic;
- historical one-asset R5 repository/code.

Do not commit local scientific `.mat`, `.npz`, JSON run outputs, MATLAB logs, raw data, cache, credentials, or large runtime artifacts. Commit text-first report/hash/provenance only.

## 18. Required local evidence package

Use one new fresh no-overwrite root under `D:\ProjectTemp`.

Persist at minimum:

- source-map manifest and SHA;
- exact candidate code identities;
- MATLAB command/environment/exit/stdout/stderr identities;
- MATLAB full household result artifact if feasible and compact summary JSON;
- standalone Python command/environment/exit/stdout/stderr identities;
- standalone full household result artifact if feasible and compact summary JSON;
- comparator contract/hash;
- qualified comparison JSON;
- call ledger;
- output manifest with SHA-256 and sizes;
- warnings/failures.

No pre-existing evidence root may be overwritten or repurposed.

## 19. Required report

Report:

- terminal verdict;
- live continuity;
- all starting/protected/wrapper/patch identities;
- exact source-map disposition;
- exact candidate hashes after patch application;
- comparator contract and pre-execution freeze evidence;
- MATLAB local artifact root/output identities and call count;
- standalone local artifact root/output identities and call count;
- comparison table for convergence, `Ct`, `Lt`, `At`, `Bt`, `At+Bt`, and optional tax field disposition;
- no-NaN/Inf and identity checks;
- clean-room standalone/API/dependency review if PASS;
- full scientific-call ledger;
- production/export mutation disposition;
- forbidden-operation audit;
- `git diff --check`;
- exact staged file list;
- Git closeout;
- exactly one recommended next gate.

## 20. Git closeout

Use explicit-path staging only. Never `git add .` or `git add -A`.

One execution commit and one non-force push are authorized.

After push:

- fresh GitHub read-back every changed path;
- require `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree.

On PASS, the execution commit may contain the three promoted candidate code files plus bounded validation/test/report/status files.

On BLOCKED/MISMATCH, the three production/export files must be rolled back before commit, so the closeout commit contains only bounded validation/test/report/status evidence.

## 21. Expected terminal classifications

Full success:

`MP4B_BEIJING_FIRST_TURN_HOUSEHOLD_PARITY_REVALIDATION_AFTER_RAW_VB_REPAIR_PASS`

Required acceptance marker:

`MP4B_BEIJING_FIRST_TURN_MATLAB_STANDALONE_HOUSEHOLD_PARITY_ACCEPTED`

Otherwise use a precise `..._BLOCKED` or `..._MATERIAL_MISMATCH` classification and stop at the first failed gate.

## 22. Exactly one successor boundary

Only after PASS and independent L3 review, recommend one successor:

**one Python-only corrected-calendar-2009 multi-province stationary execution against the already preserved MATLAB 2009 baseline, with a separately frozen one-shot budget and comparator. The preserved MATLAB stationary baseline must not be rerun.**

Until this household gate PASSes and is independently accepted, calendar-2009 multi-province stationary remains closed.