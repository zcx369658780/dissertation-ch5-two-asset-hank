# Chapter 5 Python 多省份两资产 HANK 路线
更新：2026-09-16。唯一活动仓库：`zcx369658780/dissertation-ch5-two-asset-hank`。
Results eligibility=`FALSE`。

## Accepted route

The frozen source-faithful reference and separately governed corrected successor remain distinct. Owner adopted D1 upper numerical state constraints, D2 consumed-total-drift conservative assembly, and D3 regularized-cost-consistent KKT for the corrected diagnostic target. Its isolated static implementation is accepted.

## KFE-D2B selector gate status

The active-equality representation mismatch was repaired without relaxing D2. A fresh exact ten-cell historical panel then completed once under frozen code and budget.

Accepted fail-closed outcome:
- Cells 1,2,3,5,6,7,9: `SELECTED_ADMISSIBLE` and strict D2 PASS.
- Cells 4,8,10: `NO_ADMISSIBLE_POLICY`.
- 10 selector evaluations, 31 scalar roots, 0 retries.
- HJB/KFE/MATLAB/downstream calls remained zero.

This is not a ten-cell PASS, but it is valid bounded evidence. Cells 4/8 are dual-upper corners with an apparent upper-b `q_b>0` domain incompatibility under frozen historical derivatives; Cell 10 is a different upper-b/interior-a derivative-direction/KKT conflict. No repair is yet authorized.

### KFE-D2B-R2 — active: zero-science failure attribution

`tasks/CH5_MP4C_2018_KFE_D123_FAILED_CELL_ALGEBRAIC_ATTRIBUTION_ZERO_SCIENCE_20260916.md`

Use only frozen equations, selector source and persisted receipts to prove the exact algebraic failure classes for Cells 4/8/10 and audit whether the selector omitted any mathematically legal branch. Selector/root/model calls are zero.

The attribution must separate historical-derivative incompatibility from selector/enumeration defects. It must also state the interpretation boundary: the panel consumes derivative arrays generated by the historical MATLAB-faithful path, not a corrected-target HJB fixed point.

### KFE-D2C — conditional next scientific gate

After D2B-R2 acceptance:
- if a concrete selector omission is proven, publish the smallest repair plus minimal fresh validation panel;
- if no omission is found and the failures are structural only for frozen historical derivative inputs, consider a separately budgeted corrected-target **single policy-map/direct HJB step** rather than repeatedly testing the same historical derivative states.

Neither route is authorized by this roadmap alone.

### KFE-D3 — later production closure

KFE mass/nonnegativity/uniqueness, corrected multi-province production replacement, steady state, GE/annual/dynamics/IRF and Results remain downstream gates with separate evidence and authority.

Capital/labor K1/C1/K1B/K2 routes remain deferred, not abandoned, behind the current household/KFE integration blocker.
