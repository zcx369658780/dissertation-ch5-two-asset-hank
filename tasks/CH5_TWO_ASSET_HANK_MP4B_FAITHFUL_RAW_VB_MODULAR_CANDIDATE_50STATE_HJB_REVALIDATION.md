# CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION

Date: 2026-08-31

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / faithful HJB parity executor

Owner: final scientific authority

## 1. Purpose

Advance exactly one gate after:

`MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_PASS`

The predecessor established exact 12/12 local-policy parity, including the Beijing iteration-5 negative-raw-`Vb` witness, and froze a byte-replayable two-file modular repair patch. Production bytes were then rolled back.

This task must:

1. restore that accepted modular candidate **byte-identically** from the frozen patch;
2. revalidate exactly one repaired modular Python faithful HJB on the original accepted 50-state fixture;
3. compare it against the already preserved MATLAB HJB output under the **unchanged accepted propagation-aware contract**;
4. roll the temporary candidate back to task-authority bytes before closeout.

This task does **not** authorize:

- a MATLAB 50-state HJB rerun;
- KFE execution;
- standalone export modification or household revalidation;
- Beijing empirical household execution;
- any second province;
- MP2/MP3 empirical execution;
- calendar-2009 multi-province stationary execution;
- annual batch, shocks, transition, dynamics, IRF, R5, or Results.

## 2. Controlling authority

Read in full and obey:

- `AGENTS.md`
- `project_rules/PROJECT_RULE_INDEX_CURRENT.md`
- `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`
- `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`
- `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`
- `docs/DISSERTATION_CH5_PYTHON_MULTI_PROVINCE_HANK_REBUILD_ROADMAP_CURRENT.md`
- all Owner MP4 decisions/adjudications
- all prior MP4B raw-`Vb` / `HANK3_FOC` tasks and reports
- `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_REPORT.md`
- `docs/CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_AND_FINAL_ACCEPTANCE_RERUN_REPORT.md`
- accepted faithful KFE / end-to-end household reports only as historical downstream evidence; do not execute KFE here

Primary numerical authority remains:

`MATLAB_FAITHFUL_NUMERICAL_IMPLEMENTATION_IS_PRIMARY_RECONSTRUCTION_AUTHORITY`

Preserve accepted markers:

- `MP4B_RAW_VB_TRANSFER_FOC_SOURCE_EDGE_SEMANTICS_FROZEN`
- `MATLAB_FAITHFUL_NEGATIVE_RAW_VB_LOCAL_POLICY_PARITY_ACCEPTED`
- `MP4B_FAITHFUL_RAW_VB_REPLACEMENT_LOCAL_POLICY_PARITY_AFTER_RUNNER_BOOTSTRAP_REPAIR_PASS`
- `MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_ACCEPTED`
- `MATLAB_PYTHON_TWO_ASSET_HA_FAITHFUL_HJB_OPERATOR_PARITY_ACCEPTED`
- `MATLAB_FAITHFUL_HJB_MATHEMATICAL_SPARSE_SUPPORT_IGNORES_EXACT_STORED_ZEROS`
- `MATLAB_FAITHFUL_HJB_PARITY_SEPARATES_FIXED_POINT_SOLVER_PROPAGATION_FROM_SAME_INPUT_FORMULA_PARITY`

## 3. Live continuity

Expected execution-start parent:

`35ccd8a5ddf2ee99992dd4056cd689318788f5d8`

At start:

1. fresh-fetch `origin/main`;
2. confirm this exact task is live on `main` as a direct child of the predecessor PASS commit;
3. require clean worktree;
4. verify the predecessor PASS report and frozen patch exist on live main;
5. verify current production bytes are the rolled-back task-authority bytes;
6. verify accepted faithful operator/HJB source identities;
7. verify the preserved HJB artifact root and its frozen fixture/comparator identities;
8. verify no historical R5 / `chapter5_model` runtime dependency.

Required rolled-back predecessor identities:

- `src/ch5_two_asset_hank/economics.py`
  - exact Git-blob-byte SHA-256: `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
  - SHA-256: `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`
- `exports/matlab_faithful_two_asset_ha.py`
  - SHA-256: `276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

Accepted unchanged faithful HJB route identities:

- `src/ch5_two_asset_hank/matlab_faithful_operator.py`
  - SHA-256: `0C9F6C1AE3E6428E49086DE900BE55D5038A10144EC061EDDFB9E7249A4A1AAC`
- `src/ch5_two_asset_hank/matlab_faithful_hjb.py`
  - SHA-256: `924831362C904A253D8AD6011FE9BB4C099C6DF4268D0C2AA30AB9139569F1DE`

If any required identity fails, stop before mutation.

## 4. Frozen accepted modular repair patch

Patch path:

`validators/multi_province/mp4b_faithful_raw_vb_modular_candidate.patch`

Required patch SHA-256:

`0F044055DA9B4BFF22A2F8342EF189781AD3D536BFD2A67148C1182C1F9AB31D`

Required candidate identities after applying the patch to exact task-authority bytes:

- candidate `economics.py`
  - Git object: `810e0875febc873ae85bef7e88edd4de349b00b2`
  - exact blob-byte SHA-256: `F2B67D393D7495A83281C259F6D9CC5F8AFF18B3C4ABDEB7A07F354582DEF2D1`
- candidate `matlab_faithful_policy.py`
  - Git object: `2021db630f3057026ffc37d375a43aaddbccec48`
  - exact blob-byte SHA-256: `ECF56FAA7E87A0F5156E5866F1873556D6603E98AA33C8E41C400FD016C75CDC`

Apply the patch exactly once to the clean authority bytes.

Do not edit, amend, normalize, regenerate, or reinterpret the candidate after patch application.

Required marker before any scientific HJB call:

`MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_BYTE_IDENTITY_RESTORED_FOR_HJB_PASS`

If the patch does not reproduce the exact candidate hashes, stop BLOCKED.

## 5. Candidate no-science preflight

Before the repaired Python HJB call:

1. confirm the dedicated faithful raw-`Vb` helper exists only on the faithful route;
2. confirm shared `transfer_candidate` and corrected/reference routes are unchanged;
3. confirm the faithful selector has no pre-floor raw-`Vb` positivity rejection;
4. confirm consumption/labor still use `max(raw Vb,1e-6)` only;
5. confirm all four transfer FOCs consume raw boundary-treated `Va/Vb` through the dedicated helper;
6. confirm `matlab_faithful_operator.py` and `matlab_faithful_hjb.py` bytes remain exactly unchanged;
7. rerun focused non-scientific helper/policy tests and `py_compile` as needed;
8. run `git diff --check`.

Reuse the predecessor local-policy PASS as scientific authority. Do not rerun MATLAB or Python local-policy batches here.

Required marker:

`MP4B_FAITHFUL_RAW_VB_HJB_CANDIDATE_STATIC_PREFLIGHT_PASS`

## 6. Preserved 50-state HJB acceptance fixture

Use the already accepted artifact root read-only:

`D:\ProjectTemp\ch5-hjb-propagation-aware-final-20260830-001`

Do not overwrite or regenerate any accepted artifact in that root.

Required preserved identities:

- MATLAB HJB output SHA-256:
  `7351351B5D0F7012F03CB6A8CB79A6E31D8FC65FF5D7C26B4A241047F1B5DE94`
- prior accepted Python HJB output SHA-256, diagnostic baseline only:
  `A33EEA2F7A1698624EC4F773FFE42C2BF88B255F9C231960E8C182ECA2C870AB`
- final propagation-aware comparator SHA-256:
  `E049D0B48901799A07D551978BF3C767CD326AB33034CC2C21CD2E2F815EE231`
- comparator preflight SHA-256:
  `6F1F12F28570B1DD846B047CD028CE150F1BDB4E32F6364421339357C33ADAA2`
- freeze manifest SHA-256:
  `EBF67BE53238B864BA5AEA459C164C13E9BB3F48AA79C45E4D103BED6C7E21BE`

The preserved freeze manifest must identify and bind the exact:

- 50-state parameter/grid manifest;
- ordering adapter;
- initialization artifact;
- tolerance/acceptance contract;
- accepted evaluator/runner/comparator artifacts.

Historical accepted fixture remains:

- `5 b x 5 a x 2 z = 50` states;
- `Delta=1000`;
- `crit=1e-7`;
- `maxit=100`;
- unchanged economic parameters, boundaries, ordering, initialization and switching matrix.

If any required preserved artifact is missing or hash-mismatched, stop BLOCKED. Do not recreate it from memory.

## 7. Runner/source-binding preflight

Before consuming the HJB scientific budget, prove the Python execution environment will import the temporary patched candidate from this exact repository.

Preferred route:

- reuse the accepted preserved Python HJB runner byte-identically if the freeze manifest identifies it and it can be invoked with explicit repository `src` binding;
- otherwise create one validation-only launcher/runner that reproduces the accepted runner's input/output schema without changing any scientific arithmetic.

In either route:

1. derive or explicitly bind the exact current repository `src` root before `ch5_two_asset_hank` import;
2. verify imported `economics.py`, `matlab_faithful_policy.py`, `matlab_faithful_operator.py`, and `matlab_faithful_hjb.py` origins are inside this exact repository `src` tree;
3. verify their current hashes match the frozen candidate / unchanged HJB identities above;
4. reject any module origin outside this repository;
5. reject historical R5 / `chapter5_model` imports;
6. do not rely solely on cwd, ambient `PYTHONPATH`, editable install, or user-site packages.

Execute exactly one zero-HJB-call import/bootstrap smoke before the scientific call. The smoke may import modules and inspect fixture/runner schemas but must execute zero HJB iterations and produce no HJB scientific result.

Required markers:

`MP4B_FAITHFUL_RAW_VB_HJB_RUNNER_SOURCE_BINDING_STATIC_REVIEW_PASS`

`MP4B_FAITHFUL_RAW_VB_HJB_RUNNER_ZERO_CALL_BOOTSTRAP_SMOKE_PASS`

If the zero-call smoke fails, repair is allowed only before the scientific HJB call begins. Once the repaired Python HJB scientific call begins, no repair/rerun is authorized.

## 8. Unchanged propagation-aware comparator contract

Do not redesign the comparator or acceptance thresholds.

Use the accepted propagation-aware contract exactly as frozen in:

`CH5_TWO_ASSET_HANK_MATLAB_FAITHFUL_HJB_PROPAGATION_AWARE_PARITY_CONTRACT_AND_FINAL_ACCEPTANCE_RERUN_REPORT.md`

Required rules include:

### 8.1 Direct/source-local arithmetic

Same exact input scalar/formula checks remain bounded by:

`128*eps64*max(1,abs(x),abs(y))`

No broad tolerance increase is authorized.

### 8.2 Converged value

Repaired Python `V` versus preserved MATLAB `V` retains absolute gate:

`max_abs_diff <= 1e-7`

### 8.3 Categorical/branch gates

Require exact equality for:

- grid/state/order identities;
- initialization identity;
- convergence boolean;
- iteration count;
- liquid labels;
- transfer labels;
- derivative-floor activation pattern;
- `Bswitch` pattern/values.

### 8.4 Sparse support

Mathematical sparse support is compared after removing **exact stored numerical zero only** (`0.0` / `-0.0`) from comparison copies.

Forbidden:

- epsilon pruning;
- `isclose` zeroing;
- threshold-based sparse removal;
- removal of any nonzero entry.

Raw stored NNZ remains diagnostic.

### 8.5 Solver-propagated differences

Raw downstream continuous/operator differences caused solely by independently solved HJB fixed points may be classified only as:

`SOLVER_PROPAGATED_DIAGNOSTIC_DIFFERENCE`

and only if:

- both converge;
- iteration count exact;
- `V` passes the frozen absolute gate;
- branch/floor patterns exact;
- same-input continuous replay passes direct machine bounds everywhere;
- same-input sparse coefficient replay passes direct machine bounds everywhere;
- mathematical sparse support exact;
- no unexplained residual or source formula/index/assembly discrepancy remains.

Any unexplained same-input residual => material mismatch.

## 9. Pre-science comparator/fixture checks

Before the repaired Python HJB call:

- verify the preserved comparator hash exactly;
- verify its existing synthetic material/nonzero-support/categorical failure preflight evidence remains intact;
- verify the frozen MATLAB HJB output is readable and hash-exact;
- freeze one fresh no-overwrite Python output path and one fresh no-overwrite comparison output path under a new `D:\ProjectTemp` run root;
- freeze the complete call ledger before execution.

Do not modify the preserved MATLAB artifact or comparator after any HJB output exists.

Required marker:

`MP4B_FAITHFUL_RAW_VB_50STATE_HJB_REVALIDATION_PREFLIGHT_PASS`

## 10. Scientific call budget

Only after all prior gates pass:

- MATLAB HJB: `0`
- repaired modular Python faithful HJB: exactly `1`
- Python HJB reruns: `0`
- propagation-aware comparator: exactly `1`
- comparator reruns: `0`

All other scientific/model calls remain zero:

- MATLAB/Python local-policy batches: `0/0`
- MATLAB/Python KFE: `0/0`
- standalone household: `0`
- Beijing household parity: `0`
- second province: `0`
- MATLAB/Python multi-province stationary: `0/0`
- MP2/MP3 empirical: `0/0`
- annual batch/shocks/transition/dynamics/IRF/R5/Results: `0`.

If the repaired Python HJB fails, preserve evidence and stop. No repair/rerun.

If the comparator fails, preserve evidence and stop. No repair/rerun.

## 11. Acceptance

PASS requires all of the following:

1. candidate restored byte-identically from the frozen accepted patch;
2. runner/source binding verified before science;
3. exactly one repaired Python HJB call completes;
4. Python HJB converges;
5. iteration count equals preserved MATLAB (`12` in predecessor acceptance);
6. `V` passes the unchanged absolute `1e-7` gate;
7. categorical/branch/floor/Bswitch gates exact;
8. same-input continuous replay passes the direct machine bound everywhere;
9. same-input sparse coefficient replay passes direct machine bound everywhere;
10. mathematical sparse support exact after exact-zero-only canonicalization;
11. every remaining raw downstream difference is fully classified as solver-propagated diagnostic only;
12. complete material mismatch list empty;
13. complete unresolved scientific residual list empty;
14. complete source/environment failure list empty;
15. no forbidden scientific call occurs;
16. candidate source is rolled back before repository closeout.

On PASS establish:

`MATLAB_FAITHFUL_HJB_AFTER_RAW_VB_SOURCE_ORDER_REPAIR_PARITY_ACCEPTED`

and:

`MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_PASS`

Terminal PASS:

`MP4B_FAITHFUL_RAW_VB_50STATE_HJB_REVALIDATION_PASS`

BLOCKED terminal:

`MP4B_FAITHFUL_RAW_VB_50STATE_HJB_REVALIDATION_BLOCKED`

MATERIAL terminal if comparator identifies a genuine scientific mismatch:

`MP4B_FAITHFUL_RAW_VB_50STATE_HJB_REVALIDATION_MATERIAL_MISMATCH`

A PASS accepts the frozen modular candidate patch through the faithful HJB/operator layer only. It does not yet accept a new standalone export, KFE rerun, Beijing household, GE, or 2009 stationary execution.

## 12. Mandatory rollback before closeout

Whether PASS, BLOCKED, or MATERIAL_MISMATCH, restore temporary production candidate files before commit:

- `src/ch5_two_asset_hank/economics.py`
  - `5FD4805CBBF7E5222ABB403B976AE74617904E776336D5B42F58AB05D3FF49E7`
- `src/ch5_two_asset_hank/matlab_faithful_policy.py`
  - `D8A595B93689ED7A9457738620479E3158C734F6878A063FA6B12B0CCFD5F8C2`

Standalone export must remain unchanged:

`276D2244B389D6EDE140DAF8B1F9B0BE1F4AA859368941CED1A12BA8A5831AB8`

`matlab_faithful_operator.py` and `matlab_faithful_hjb.py` must remain at their accepted hashes.

Final production/export mutation must equal `0`.

If rollback identity fails, terminal cannot be PASS.

## 13. Allowed repository changes

Allowed only:

- one validation-only HJB launcher/runner if needed;
- one bounded validation-only HJB execution ledger/manifest helper if needed;
- bounded tests for runner/source binding only;
- bounded CURRENT roadmap status update if needed;
- report:
  `docs/CH5_TWO_ASSET_HANK_MP4B_FAITHFUL_RAW_VB_MODULAR_CANDIDATE_50STATE_HJB_REVALIDATION_REPORT.md`

Do not modify the frozen modular candidate patch.

Do not publish temporary patched production files.

Do not modify:

- standalone export;
- accepted faithful operator/HJB source;
- KFE modules;
- corrected/reference modules;
- protected MATLAB;
- canonical data/cache;
- MP2/MP3;
- historical R5.

## 14. Required report

Report at minimum:

- terminal verdict;
- live continuity;
- patch SHA and restored candidate hashes;
- unchanged operator/HJB hashes;
- preserved HJB root/artifact/comparator hashes;
- runner/bootstrap route and imported module origins;
- zero-HJB-call smoke evidence;
- repaired Python HJB output SHA;
- comparator output SHA;
- convergence/iteration/value parity;
- categorical/branch/floor/Bswitch parity;
- same-input continuous replay maxima/bounds;
- same-input sparse replay maxima/bounds;
- raw/mathematical sparse support diagnostics;
- solver-propagated difference summary;
- complete mismatch/residual/source failure lists;
- complete call ledger;
- rollback hashes;
- production/export mutation `0`;
- tests/checks;
- forbidden-operation audit;
- Git closeout;
- exactly one recommended next gate.

## 15. Repository closeout

Explicit-path staging only.

One execution commit.

One non-force push.

GitHub read-back every changed path.

Require:

- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree.

## 16. Successor boundary

On PASS recommend exactly one next gate:

**reconstruct the same accepted modular patch plus a source-identical standalone raw-`Vb` repair, then perform one Beijing first-turn MATLAB/standalone household parity and standalone export revalidation; do not yet authorize calendar-2009 multi-province stationary execution.**
