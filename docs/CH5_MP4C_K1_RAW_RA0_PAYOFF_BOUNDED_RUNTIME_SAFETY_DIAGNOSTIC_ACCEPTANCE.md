# Chapter 5 MP4C K1 raw `ra0` payoff bounded runtime-safety diagnostic acceptance

Date: 2026-09-12.

Reviewer verdict:

`RAW_RA0_PAYOFF_SAFETY_GATE_BLOCKED_PRE_RUN_ACCEPTED__NO_PRIOR_COMPLETED_RA0_IN_ACCEPTED_INITIALIZATION__ZERO_SCIENCE_CALLS__OWNER_BOOTSTRAP_DECISION_REQUIRED`

Accepted Builder candidate:

`2c29a38b153ee56e58c833297b7dad2da639d6e7`

Baseline:

`629cdb030f8b557f25d5c6c3ba5e9c42aab4038e`

## Scope accepted

The Builder correctly stopped at the pre-run provenance gate and made no scientific/runtime change. The candidate is one commit ahead of baseline and adds only the diagnostic report. No payoff selector, runner, HJB/KFE/firm/capital-network implementation, parameter, bound, grid, tolerance, solver semantic, K1B/K2 route or Results artifact was changed.

## Accepted blocker

The accepted 31-province initialization contains `ra`, `rah`, and `rk` in all 31 province states but contains no `ra0` field in any province state (`0/31`). A completed firm turn does persist raw `ra0` into the subsequent state, so a provenance-safe raw payoff becomes available only after the first completed turn.

The active raw-payoff safety task required Raw R to use a prior-completed raw `ra0` vector from its first allocation. Because no such object exists in the accepted entering state, the Builder was not authorized to invent a value or silently introduce a bootstrap convention.

The rejected workaround—using entering clipped/source-used `ra` for turn 1 and switching Raw R to raw `ra0` from turn 2 onward—is scientifically plausible but changes initialization/timing semantics and therefore requires explicit Owner/Reviewer freeze.

## Call ledger

All new scientific/model/runtime calls are accepted as zero:

- trajectory: 0;
- HJB: 0;
- KFE: 0;
- MATLAB: 0;
- standalone KFE: 0;
- K1B/K2: 0;
- GE/annual/shock/IRF/Results: 0;
- retry: 0.

No scientific state advanced.

## Scientific boundary

This acceptance does not support `RAW_RA0_PAYOFF_SHORT_HORIZON_RUNTIME_SAFETY_SUPPORTED`; the Raw path did not run.

The Owner-approved final payoff contract remains raw `ra0` as the intended K1 household illiquid payoff object, but runtime switching remains blocked until initialization semantics are frozen.

K1B `beta_return=.5` remains preregistered and runtime-unauthorized. K2 remains unauthorized. Corrected-2018 finite-box upper-`b` leakage and MATLAB-style pinning remain an independent KFE blocker. Results eligibility remains `FALSE`.

## Required Owner decision

Before a successor Builder task can be published, Owner/Reviewer must freeze one of the following:

1. construct/provide an accepted initialization with provenance-bound prior-completed `ra0` for all 31 destinations; or
2. authorize a common turn-1 clipped/source-used-`ra` bootstrap for both Control and Raw paths, with Raw payoff first entering the household block from turn 2 and with exact turn-count/comparison semantics.

Reviewer recommendation: option 2 is the lower-assumption and more provenance-preserving route because it uses the already accepted entering state unchanged, keeps Control and Raw identical through turn 1, and lets raw `ra0` enter only once it has been generated by a completed firm iteration. No successor task is published until Owner confirms that convention.
