# CH5_TWO_ASSET_HANK_MP4B_L3_READ_ONLY_SOURCE_BINDING_CENSUS_AND_EARLY_HOUSEHOLD_OUTPUT_ATTRIBUTION

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / read-only provenance and same-input diagnostic analyst

Owner: final scientific authority

## 1. Authority basis and purpose

The accepted predecessor execution is:

`MP4B_POST_TRACE_SAME_INPUT_SOURCE_LAYER_AND_INITIAL_ZT_PROVENANCE_ADJUDICATION_PASS`

Execution commit:

`1dd008a62c17e77d33d15d6d86f8bd58118c2e4c`

Accepted strongest classification:

`MP4B_TURN1_ZT_SOURCE_BINDING_REPRESENTATION_DIVERGENCE_LOCALIZED`

Accepted preserved markers:

- `MP4B_L3_DELEGATED_INSTRUMENTED_MATLAB_BASELINE_ADMISSIBLE`
- `MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`
- `MP4B_TURN8_RESET_DIVERGENCE_UPSTREAM_INPUT_DRIVEN`

The predecessor localized one exact-ULP Tianjin initial-`Zt` divergence at the MATLAB-cache versus canonical-JSON binding boundary, but did not establish whether Tianjin is the only province affected or what the intended provenance contract for canonical `initialized_zt` should be.

The predecessor also found the earliest recorded measurable continuous output difference at turn 1 Beijing household `Ct`:

- MATLAB: `11.400731651946101`
- durable multi-province Python: `11.400731651949162`

An earlier accepted bounded same-input Beijing household parity task already established that the repaired household block passes its frozen comparator contract, with MATLAB `Ct = 11.400731651946101` and standalone Python `Ct = 11.40073165194351` under the accepted Beijing same-input contract.

This task must determine, without rerunning any model:

1. the complete 31-province initial-`Zt` binding difference census between immutable MATLAB cache and canonical corrected-2009 input;
2. the exact authority/provenance meaning of canonical `initialized_zt` — cache snapshot versus recomputed source-expression value — from existing task/report/source evidence only;
3. whether the turn-1 Beijing household call in the durable multi-province Python run is bitwise/exactly the same scientific input object as the previously accepted Beijing standalone same-input contract;
4. if the inputs are the same, whether the multi-province Python `Ct/Lt/At/Bt` differences are merely within the already accepted household numerical equivalence envelope or reveal a distinct wrapper/state-initialization/reduction path;
5. the earliest actionable parity defect or provenance correction candidate, if one is supported.

This is read-only diagnosis. It does not authorize canonical-input mutation, Python scientific-code mutation, MATLAB mutation, or stationary rerun.

## 2. Required live continuity

Required execution-start predecessor:

`1dd008a62c17e77d33d15d6d86f8bd58118c2e4c`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `1dd008a62c17e77d33d15d6d86f8bd58118c2e4c`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - predecessor post-trace adjudication task/report;
   - predecessor L3 chronology task/report;
   - accepted Beijing first-turn household parity report at commit `486af39d40ca4292e04845a0ba5b30fbe4918f61`;
   - the Beijing same-input source map and comparator contract named by that report;
   - any earlier corrected-calendar-2009 input-binding task/report that defined the canonical JSON semantics;
   - source-semantics map and relevant protected MATLAB source lines for `load_GDPdata`, `mpHANK_equilibrium_2000`, and the household call path.

Any authority/identity/readability failure => stop with the BLOCKED classification in Section 12.

## 3. Hard zero-model-execution budget

All model/scientific execution counts are exactly zero:

- MATLAB processes: `0`;
- MATLAB stationary/HJB/KFE/household/firm/controller: `0`;
- MATLAB `checkcode`: `0`;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific execution: `0`;
- comparator replay / `compare_terminal`: `0`;
- standalone Beijing household rerun: `0`;
- Zhejiang/Shanxi replay: `0`;
- other year / annual batch: `0`;
- shocks / AR(1) / transition / dynamics / IRF: `0`;
- historical R5 / Results: `0`.

Allowed work is limited to read-only hashing, source/text inspection, direct parsing of durable JSON/MAT/HDF5 evidence, and standard-library-only scalar/array identity comparison. Do not import repository scientific modules.

## 4. Immutable evidence to verify

At minimum reverify:

### 4.1 Current chronology package

Read-only MATLAB trace root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-l3-delegated-rerun-20260831-001`

- trace MAT: `6C4AEC5992A59C1C8D7859DEF082E5711864789CAE009FD1E5CC2B78B3F6A520`
- instrumented final state: `D2F4B225AC362A6805AAD1CD065AFA87DA32551833E5FC867BA3A37C9FD0B8F0`
- baseline admissibility: `68C9F396C57D385E72E6A46D94A53F4196F7E9BFBC84A6EAF66AB1FF64A0866B`
- chronology alignment V5: `A64172CC32EC7295297CED6DFE27B52CE51B5EAD64AD82453C60EBE0152965C3`

Read-only post-trace package:

`D:\ProjectTemp\ch5-mp4b-post-trace-same-input-source-layer-adjudication-20260901-002`

Require all artifact identities from the accepted predecessor report.

### 4.2 Durable Python run

Read-only root:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

- manifest: `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`
- terminal summary: `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`

### 4.3 Canonical corrected-2009 input

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

### 4.4 Accepted Beijing household parity authority

Accepted execution commit:

`486af39d40ca4292e04845a0ba5b30fbe4918f61`

Require from that report:

- Beijing same-input contract SHA-256: `FE833FAEB48521CD0C7594627AF6FB5012F9497A455E9B2C5E7490E0C40E6F22`
- Beijing source-map SHA-256: `757A39B8EFCAD2F955EE8674DC424D0FA97F0C7751B8361995AFCB7AE0A5BD4E`
- household comparator contract SHA-256: `7BAF5520C0E52D156709FA66E00423D892F926F94462EF2E72DC0FCC382458EC`
- MATLAB household result SHA-256: `024B097CEAC5872B5E83421B186C8424ECA92CD06A6D5D0FAF7B220BBFAB9DFE`
- standalone Python result SHA-256: `9E9967F023C89F22550AEAC3C8CD53215B70E13C3C70E16C0CCC5C3D518A0F83`
- qualified comparison SHA-256: `28966B73605BD82BA858C3B2A3CBC144C867377C71476B1003846C88AE6382BF`

Do not rerun any of these artifacts.

## 5. Objective A — 31-province initial-Zt binding census

Using only durable cache/canonical evidence, produce a row for all 31 provinces containing at minimum:

- canonical province index/name;
- raw MATLAB cache `IND_Zt` binary64 value;
- canonical JSON `initialized_zt` binary64 value;
- decimal difference;
- integer ULP distance;
- exact equality boolean;
- whether canonical equals a standard-library reproduction of the protected source expression from persisted GDP/CAP/POP/alpha inputs;
- whether cache equals that reproduction;
- whether any difference is only one ULP or larger;
- source/cache provenance identifiers.

Do not assume Tianjin is unique.

Classify the cross-sectional result as one of:

- `MP4B_INITIAL_ZT_BINDING_CENSUS_EXACT_31_OF_31`
- `MP4B_INITIAL_ZT_BINDING_CENSUS_REPRESENTATION_DIVERGENCES_LOCALIZED`
- `MP4B_INITIAL_ZT_BINDING_CENSUS_MATERIAL_SOURCE_VALUE_DIVERGENCE_FOUND`
- `MP4B_INITIAL_ZT_BINDING_CENSUS_INCOMPLETE`

No canonical file may be edited.

## 6. Objective B — canonical initialized-Zt provenance contract

Read the task/report/source evidence that originally created or froze the canonical corrected-2009 input.

Determine which of the following was actually authorized:

1. exact snapshot of MATLAB cache `IND_Zt` binary64 values;
2. source-formula recomputation from persisted GDP/CAP/POP/alpha values;
3. a representation-normalized equivalent allowed within a documented tolerance;
4. unspecified/ambiguous provenance.

Distinguish:

- scientific equation authority;
- cache-as-execution-input authority;
- canonical-validation representation authority.

Do not infer that recomputation is automatically preferable merely because it matches the protected expression. The MATLAB stationary execution consumed the persisted cache value; exact same-input parity may require cache-exact binding even when the recomputed value differs by only one ULP.

Use the strongest supported classification:

- `MP4B_CANONICAL_INITIAL_ZT_CACHE_EXACT_BINDING_REQUIRED`
- `MP4B_CANONICAL_INITIAL_ZT_SOURCE_RECOMPUTATION_AUTHORIZED`
- `MP4B_CANONICAL_INITIAL_ZT_REPRESENTATION_TOLERANCE_AUTHORIZED`
- `MP4B_CANONICAL_INITIAL_ZT_PROVENANCE_CONTRACT_AMBIGUOUS`

This classification is review-only and does not itself authorize mutation.

## 7. Objective C — turn-1 Beijing household input identity

Compare the durable multi-province Python turn-1 Beijing household call against the accepted Beijing same-input contract and against the instrumented MATLAB turn-1 Beijing household input.

For every execution-critical household input in the accepted source map, compare:

- exact decimal/binary64 values;
- grids/parameters;
- `rb`, `rah`, `w`, `rb_gap`, `tau`, `Tt`;
- productivity/initialization objects;
- incoming value-function/results state or initial guess, if part of the accepted contract;
- any scalar/array object that can alter HJB/KFE/aggregate output.

Report separately:

- bitwise-identical fields;
- numerically equal but representation-different fields;
- materially different fields;
- fields unavailable in the durable run.

Do not call the household outputs same-input unless every execution-critical input is established identical or the accepted comparator contract explicitly permits a representation equivalence.

Use one classification:

- `MP4B_TURN1_BEIJING_HOUSEHOLD_INPUTS_EXACTLY_SAME_AS_ACCEPTED_CONTRACT`
- `MP4B_TURN1_BEIJING_HOUSEHOLD_INPUT_REPRESENTATION_DIFFERENCE_LOCALIZED`
- `MP4B_TURN1_BEIJING_HOUSEHOLD_INPUT_MATERIAL_DIFFERENCE_LOCALIZED`
- `MP4B_TURN1_BEIJING_HOUSEHOLD_INPUT_IDENTITY_INCOMPLETE`

## 8. Objective D — early Beijing household-output attribution

Only after Objective C, compare three already durable outputs:

1. protected MATLAB turn-1 Beijing household result;
2. accepted standalone Python Beijing result;
3. durable multi-province Python turn-1 Beijing result.

At minimum compare `Ct`, `Lt`, `At`, `Bt`, `At+Bt`, convergence, and any available household diagnostic metadata.

The accepted household comparator bound is:

`abs(M-P) <= 1e-7 * max(1, abs(M), abs(P))`

Do not invent a tighter pass/fail threshold after seeing the data.

Determine whether the durable multi-province Python result:

- matches the accepted standalone result bitwise;
- differs only at ordinary floating reduction scale while both remain far inside the frozen household parity envelope;
- follows a distinct initialization/wrapper/reduction path despite same economic inputs;
- or receives materially different household inputs.

If inputs are exact and all outputs remain far inside the frozen accepted envelope, classify the difference as numerical non-identity, not a household scientific-parity failure.

Potential classifications:

- `MP4B_TURN1_BEIJING_HOUSEHOLD_OUTPUT_NONBITWISE_BUT_WITHIN_ACCEPTED_PARITY_ENVELOPE`
- `MP4B_TURN1_BEIJING_MULTIPROVINCE_WRAPPER_OR_INITIALIZATION_PATH_DIVERGENCE_LOCALIZED`
- `MP4B_TURN1_BEIJING_HOUSEHOLD_OUTPUT_MATERIAL_SAME_INPUT_DIVERGENCE`
- `MP4B_TURN1_BEIJING_HOUSEHOLD_OUTPUT_ATTRIBUTION_INCOMPLETE`

A same-input material divergence must be adjudicated against the frozen household contract before any Python/MATLAB fault label.

## 9. Objective E — propagation relevance and next actionable defect

Using the already durable trace only, answer:

- Is the turn-1 Tianjin `Zt` binding difference demonstrably connected to the Beijing turn-1 household `Ct` difference? If not, say no causal link is established.
- Does the Beijing household-output numerical non-identity propagate into the turn-2 Beijing wage difference or later turn-8 reset divergence in a source-backed manner?
- What is the earliest discrepancy that is both (a) source-backed/actionable and (b) large or categorical enough to warrant a repair/replay gate?

Do not rank discrepancies solely by chronology. Rank by source-backed actionability and propagation evidence.

End with exactly one recommendation class:

- `CANONICAL_INPUT_BINDING_REMEDIATION_REVIEW_WARRANTED`
- `MULTIPROVINCE_HOUSEHOLD_WRAPPER_DIAGNOSTIC_WARRANTED`
- `EARLY_OUTER_LAYER_SAME_INPUT_DIAGNOSTIC_WARRANTED`
- `NO_REPAIR_YET__ADDITIONAL_READ_ONLY_PROVENANCE_REQUIRED`

This recommendation is not execution authority.

## 10. External audit package

Create one fresh no-overwrite root under `D:\ProjectTemp`, recommended:

`D:\ProjectTemp\ch5-mp4b-l3-readonly-source-binding-and-early-household-review-20260901-001`

If it exists, choose the next deterministic suffix.

Persist at minimum:

- `initial_zt_31province_binding_census.json`
- `canonical_initialized_zt_provenance_contract.md`
- `turn1_beijing_household_input_identity.json`
- `turn1_beijing_three_way_household_output_comparison.json`
- `early_propagation_relevance.json`
- `source_authority_map.md`
- `audit_manifest.json`

Any helper scripts must be standard-library-only, invoke no model, and remain external.

## 11. Repository change boundary

Required report:

`docs/CH5_TWO_ASSET_HANK_MP4B_L3_READ_ONLY_SOURCE_BINDING_CENSUS_AND_EARLY_HOUSEHOLD_OUTPUT_ATTRIBUTION_REPORT.md`

Allowed repository change:

- required report only;
- at most one strictly necessary CURRENT status/roadmap line if truly required.

Do not change:

- canonical input/data/cache;
- scientific Python code;
- MATLAB source;
- comparator/validator code;
- tests;
- task/rule files other than this authority publication;
- prior reports.

## 12. Terminal classifications

PASS terminal:

`MP4B_L3_READ_ONLY_SOURCE_BINDING_CENSUS_AND_EARLY_HOUSEHOLD_OUTPUT_ATTRIBUTION_PASS`

Use PASS even if fault remains unattributed, provided all required evidence is read and adjudicated.

BLOCKED terminal:

`MP4B_L3_READ_ONLY_SOURCE_BINDING_CENSUS_AND_EARLY_HOUSEHOLD_OUTPUT_ATTRIBUTION_BLOCKED`

Use BLOCKED only for authority, identity, readability, or provenance incompleteness that prevents the required review.

## 13. Closeout

Report:

- terminal verdict;
- all objective classifications;
- live continuity;
- immutable identity sweep;
- complete zero-model ledger;
- 31-province initial-Zt census summary;
- canonical-input provenance contract classification;
- turn-1 Beijing household input identity classification;
- three-way household output comparison;
- propagation/actionability conclusion;
- unsupported claims explicitly rejected;
- external artifact sizes/SHA-256;
- `git diff --check` on staged report;
- changed paths;
- forbidden-operation audit;
- exactly one execution commit;
- non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not auto-start repair, canonical-input mutation, stationary rerun, shocks, IRF, annual batch, R5, or Results work.