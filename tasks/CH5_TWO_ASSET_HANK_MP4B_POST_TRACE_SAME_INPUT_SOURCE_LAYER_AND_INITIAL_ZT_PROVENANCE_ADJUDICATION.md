# CH5_TWO_ASSET_HANK_MP4B_POST_TRACE_SAME_INPUT_SOURCE_LAYER_AND_INITIAL_ZT_PROVENANCE_ADJUDICATION

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer role: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor role: Codex bounded Builder / immutable-trace source-layer adjudicator

Owner: final scientific authority

## 1. Authority basis

The Owner has granted the L3 reviewer standing delegation for analogous simple rerun/debugging decisions that are mechanical diagnostic/infrastructure work and introduce no new scientific choice. The delegation does not authorize changes to equations, calibration, grids, solver/model algorithms, controller semantics, parity criteria, shock design, transition/IRF design, Results claims, or scientific route.

The immediately preceding accepted execution is:

`MP4B_L3_DELEGATED_FINITE_INSTRUMENTED_MATLAB_CORRECTED2009_RERUN_AND_CHRONOLOGICAL_ADJUDICATION_PASS`

Execution commit:

`b7be2233576a2342385db8ef0ba8a9c3359f1245`

Strongest accepted scientific classification:

`MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`

Accepted baseline-admissibility marker:

`MP4B_L3_DELEGATED_INSTRUMENTED_MATLAB_BASELINE_ADMISSIBLE`

This task is a bounded **read-only / zero-model-execution source-layer adjudication** over already durable MATLAB/Python traces and source evidence. It does not authorize any stationary rerun or scientific-model execution.

## 2. Required live continuity

Required execution-start predecessor:

`b7be2233576a2342385db8ef0ba8a9c3359f1245`

At execution start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as the direct child of `b7be2233576a2342385db8ef0ba8a9c3359f1245`;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely:
   - `AGENTS.md`;
   - `project_rules/PROJECT_RULE_INDEX_CURRENT.md`;
   - `project_rules/PROJECT_RULE_GITHUB_CAPABILITY_AND_AUTHORITY_ROUTING_CURRENT.md`;
   - `project_rules/PROJECT_RULE_LOCAL_FILE_SAFETY_CURRENT.md`;
   - `project_rules/PROJECT_RULE_MATLAB_MODEL_DIAGNOSTIC_GATES_CURRENT.md`;
   - predecessor L3-delegated chronology task/report;
   - bounded observability remediation task/report;
   - final-state localization report;
   - MATLAB-side branch provenance report;
   - comparator representation-remediation report;
   - all source-semantics maps and field maps named by those reports.

Any authority/identity failure => stop before analysis with the BLOCKED terminal in Section 14.

## 3. Hard execution budget

For this task all model/scientific execution counts are exactly zero:

- MATLAB processes: `0`;
- MATLAB stationary/HJB/KFE/household/firm/controller execution: `0`;
- MATLAB `checkcode`: `0`;
- Python stationary/HJB/KFE/household/MP2/MP3 scientific execution: `0`;
- comparator replay / `compare_terminal`: `0`;
- Zhejiang/Shanxi standalone scientific replay: `0`;
- other year / annual batch: `0`;
- shocks/AR1/transition/dynamics/IRF: `0`;
- historical R5 / Results: `0`.

Allowed work is limited to:

- read-only hashing/text/source inspection;
- direct parsing of already durable JSON/MAT/HDF5 evidence without importing repository scientific modules;
- standard-library-only scalar IEEE-754 arithmetic reproductions of explicitly identified source formulas using persisted scalar inputs;
- exact binary64/hex/ULP comparisons;
- creation of fresh no-overwrite diagnostic artifacts under `D:\ProjectTemp`;
- one text-first repository report.

No model module may be imported or invoked.

## 4. Frozen accepted evidence

Preserve without reinterpretation unless stronger same-input evidence directly adjudicates a source layer:

- instrumented MATLAB exactly matches the preserved MATLAB baseline for all 31 provinces and all 20 frozen final fields bitwise;
- MATLAB/Python both terminate at outer turn `184`;
- MATLAB/Python both represent `5,704` household calls and final `31/31` household convergence;
- MATLAB low-return actions `2,337`, Python `2,336`;
- MATLAB Zt resets `2,366`, Python `2,365`;
- wage upper/lower MATLAB `7/17`, Python `5/17`;
- final Zhejiang GovInv differs by exactly one source `0.9` multiplier;
- unique MATLAB-only low-return event is turn `154`, Zhejiang index `11`;
- first recorded reset-category divergence is turn `8`: MATLAB resets Qinghai index `29`, Python resets Shanghai index `9`;
- earliest bitwise/nonzero recorded numerical difference is turn `1`, Tianjin index `2`, entry `Zt`:
  - MATLAB `0.02363369287443703`;
  - Python `0.023633692874437028`;
  - difference `3.469446951953614e-18`;
- turn-154 Zhejiang is not the chronological first divergence;
- no same-input source-contract violation is yet accepted;
- neither MATLAB nor Python is currently classified as scientifically wrong.

## 5. Immutable artifacts and identities

Read-only instrumented MATLAB run root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-l3-delegated-rerun-20260831-001`

Require at minimum:

- trace MAT:
  `6C4AEC5992A59C1C8D7859DEF082E5711864789CAE009FD1E5CC2B78B3F6A520`
- instrumented final state:
  `D2F4B225AC362A6805AAD1CD065AFA87DA32551833E5FC867BA3A37C9FD0B8F0`
- baseline admissibility:
  `68C9F396C57D385E72E6A46D94A53F4196F7E9BFBC84A6EAF66AB1FF64A0866B`
- chronology alignment V5:
  `A64172CC32EC7295297CED6DFE27B52CE51B5EAD64AD82453C60EBE0152965C3`
- trace summary:
  `34AB06E7695987100D594DED94A0982DD3E42C78379CE5F67D358CC126096D5C`
- terminal status:
  `D7676990F4B75A48BCF9C520015864C20BE3FFE3A1515F2722ECC1043ECA8628`
- launcher completion:
  `4135BAF08BDF72C5128596A4E99E4F6691E5BF47622A9E8A417154404090703F`.

Read-only durable Python root:

`D:\ProjectTemp\ch5-mp4b-python-only-source-postloop-reexecution-authorized-20260831-001`

Require:

- run manifest `030A4241D4FB7A8CFA5370811FC4502028A61E46521F9329D7768B45278F6774`;
- terminal summary `CE943372D0F313A33E1D326747683F47CC3065B502A8B2646B492FF3B64A8F01`.

Canonical corrected-2009 input:

`D:\ProjectTemp\ch5-mp4a2-2009-input-binding-20260830-001\calendar_2009_primary_premodel_input.json`

SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`

Preserved MATLAB baseline root and protected source identities remain immutable exactly as frozen by the predecessor report.

## 6. Diagnostic objective A — turn-1 Tianjin initial `Zt` provenance

The earliest recorded difference already exists at outer-turn entry, so first determine **where turn-1 `Zt` is produced and bound on each side**.

Read protected MATLAB source and current Python implementation/read-only validators to reconstruct, source-line by source-line, the initialization path for provincial `Zt` before the first `HANK_mp_1turn` call.

At minimum determine:

- exact MATLAB assignment/source of initial `Zt`;
- exact upstream object(s): workbook/cache/`IND_Zt`/selected annual-data field or formula inputs;
- exact Python assignment/source of initial `Zt`;
- whether Python consumes the canonical JSON value directly or recomputes it;
- exact Tianjin raw decimal/string/value representations persisted at every available boundary;
- exact binary64 hex values at each boundary;
- whether the MATLAB/Python difference is already present in source-data ingestion/binding, or is created by arithmetic after identical bound inputs.

Do not assume the fixed-2020 `IND_Zt` anchor itself is wrong; establish its exact source role.

If the two implementations begin from the same exact binary64 source inputs and evaluate a source-defined formula, reproduce only that scalar formula in a standard-library audit script using the exact source operation order. Do not import scientific modules.

Classify the turn-1 Tianjin difference under the strongest supported one of:

- `MP4B_TURN1_ZT_SOURCE_BINDING_REPRESENTATION_DIVERGENCE_LOCALIZED`
- `MP4B_TURN1_ZT_ARITHMETIC_OPERATION_ORDER_DIVERGENCE_LOCALIZED`
- `MP4B_TURN1_ZT_SAME_INPUT_SOURCE_SEMANTICS_VIOLATION_LOCALIZED`
- `MP4B_TURN1_ZT_PROVENANCE_UNRESOLVED_WITH_PRESERVED_EVIDENCE`.

A one-ULP difference alone is not a fault classification.

## 7. Diagnostic objective B — turn-8 reset-category divergence

Using only the already durable traces, compare MATLAB and Python for **both** coordinates involved in the first categorical reset divergence:

- turn `8`, Shanghai index `9`;
- turn `8`, Qinghai index `29`.

For each side and province extract the complete source-defined controller-reset input tuple at the exact predicate evaluation point, including all persisted fields needed for:

- adaptation eligibility;
- `Yt/Yt0 - 1` or exact equivalent source predicate;
- absolute deviation threshold `0.01`;
- `Zt` before;
- any `Kt`, `Lt`, `Yt`, `Yt0`, `alpha` inputs used by the reset assignment;
- `KNratio`, `tKNratio`, `ra`, bounds, wage status where available;
- reset predicate boolean;
- reset action and `Zt` after.

Record every scalar in decimal, binary64 hex, and where relevant distance to the exact reset threshold.

First answer:

**Were the MATLAB and Python reset-predicate inputs bitwise identical at either turn-8 coordinate?**

If yes and the branch result differs, adjudicate against the protected source predicate and assign the violating implementation only if the evidence is exact.

If no, do not blame the reset logic. Trace upstream within the persisted source layers of turn 8 to the earliest available differing input that feeds the predicate.

Use one of:

- `MP4B_TURN8_RESET_SAME_INPUT_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_TURN8_RESET_SAME_INPUT_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`
- `MP4B_TURN8_RESET_DIVERGENCE_UPSTREAM_INPUT_DRIVEN`
- `MP4B_TURN8_RESET_INPUT_PROVENANCE_UNRESOLVED`.

## 8. Diagnostic objective C — propagation from turn 1 to turn 8

Build a bounded provenance chain from the earliest turn-1 Tianjin `Zt` difference to the first categorical reset divergence at turn 8.

Do **not** claim causation merely because one event is earlier.

For turns `1..8`, identify the earliest recorded source-layer differences that are inputs to later layers. At minimum audit:

- turn entry `Zt`;
- household input tuple;
- household outputs;
- migration `Lt_supply`;
- `Kt_supply` / `rah`;
- firm input;
- firm pre-clip;
- firm output `Yt`, `ra`, `wjt`, `KNratio`;
- composite wage;
- policy/fiscal fields;
- controller-reset inputs/actions;
- next-turn carried `Zt`, `GovInv`, `tKNratio`.

For each earliest change, state whether:

1. it is merely bitwise different;
2. it changes a continuous output measurably;
3. it changes a categorical boundary/branch;
4. it demonstrably propagates to a later recorded state.

If the turn-1 Tianjin ULP difference is not on a supported propagation path to the turn-8 Shanghai/Qinghai categorical divergence, explicitly say so.

The task must distinguish **chronological first difference** from **first demonstrated branch-relevant divergence**.

## 9. Diagnostic objective D — turn-154 Zhejiang same-input check

The unique MATLAB-only low-return event is already localized. Do not rerun it.

Using the durable traces, compare the exact turn-154 Zhejiang low-return predicate inputs on both sides:

- `ra`;
- `ramin + 0.02`;
- adaptation eligibility;
- `GovInv` before;
- `Zt` before;
- immediately upstream firm/pre-clip/output inputs sufficient to understand why `ra` differs.

Answer whether the low-return branch itself receives bitwise-identical inputs.

If inputs differ, preserve the existing rule that controller action is downstream, not root cause.

Only if bitwise-identical inputs produce different source predicate outcomes may this task assign a same-input controller implementation defect.

## 10. Same-input fault rule

Fault attribution requires all of:

1. exact same source-layer scientific inputs;
2. exact protected source/scientific contract for that layer;
3. one implementation produces an output/branch inconsistent with that contract;
4. mismatch is not explained by representation-only observation/serialization.

Only then may establish one of:

- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`.

Otherwise remain at trajectory/provenance localization.

Do not decide correctness from historical precedence, coding language, or AI authorship.

## 11. External diagnostic artifacts

Create one fresh no-overwrite root, recommended:

`D:\ProjectTemp\ch5-mp4b-post-trace-same-input-source-layer-adjudication-20260901-001`

If it exists, choose the next deterministic fresh suffix. Never delete or overwrite prior evidence.

Persist at minimum:

- `turn1_tianjin_zt_provenance.json`;
- `turn8_reset_same_input_matrix.json`;
- `turn1_to_turn8_propagation_chain.json`;
- `turn154_zhejiang_same_input_matrix.json`;
- `source_expression_map.md`;
- `binary64_audit.json`;
- `diagnostic_manifest.json` with size/SHA-256/source identities.

Any audit script must be standard-library-only, must not import repository scientific modules, and must be preserved with SHA-256 under the external root.

## 12. Scientific boundaries

This task must not:

- modify MATLAB or Python scientific code;
- patch the legacy MATLAB model;
- patch Python;
- rerun stationary;
- rerun a province/turn model solve;
- alter thresholds, tolerances, controller logic, data, calibration, or comparison criteria;
- proceed to shocks/IRF;
- write Results prose;
- call a one-ULP difference a scientific defect without same-input evidence.

The accepted instrumented MATLAB baseline remains the historical MATLAB execution oracle for chronology evidence only; it does not by itself establish that every legacy scientific choice is correct.

## 13. Required report

Required repository report:

`docs/CH5_TWO_ASSET_HANK_MP4B_POST_TRACE_SAME_INPUT_SOURCE_LAYER_AND_INITIAL_ZT_PROVENANCE_ADJUDICATION_REPORT.md`

Allowed repository mutation:

- exactly the required report;
- at most one strictly necessary CURRENT roadmap/status line if truly needed.

Do not commit raw trace/model output, external audit scripts, copied MATLAB source, MAT/HDF5 files, logs, or scientific code.

Before commit, run `git diff --check` on the actual staged content and fix whitespace issues before the one execution commit.

## 14. Terminal classifications

PASS terminal:

`MP4B_POST_TRACE_SAME_INPUT_SOURCE_LAYER_AND_INITIAL_ZT_PROVENANCE_ADJUDICATION_PASS`

The report must then state one strongest scientific classification from the evidence, for example:

- `MP4B_SAME_INPUT_SOURCE_LAYER_MATLAB_LEGACY_DEFECT_CONFIRMED`
- `MP4B_SAME_INPUT_SOURCE_LAYER_PYTHON_IMPLEMENTATION_DEFECT_CONFIRMED`
- `MP4B_TURN1_ZT_SOURCE_BINDING_REPRESENTATION_DIVERGENCE_LOCALIZED`
- `MP4B_TURN1_ZT_ARITHMETIC_OPERATION_ORDER_DIVERGENCE_LOCALIZED`
- `MP4B_TURN8_RESET_DIVERGENCE_UPSTREAM_INPUT_DRIVEN`
- `MP4B_UPSTREAM_NUMERICAL_TRAJECTORY_DIVERGENCE_LOCALIZED__FAULT_NOT_YET_ATTRIBUTED`

BLOCKED terminal:

`MP4B_POST_TRACE_SAME_INPUT_SOURCE_LAYER_AND_INITIAL_ZT_PROVENANCE_ADJUDICATION_BLOCKED`

Use BLOCKED only for authority/identity/readability/provenance failure, not merely because fault attribution remains unavailable.

## 15. Closeout

At completion report:

- terminal verdict and strongest classification;
- live continuity;
- immutable identities;
- zero-model-execution ledger;
- exact turn-1 Tianjin `Zt` provenance chain;
- source expression and binding map;
- binary64/ULP evidence;
- turn-8 Shanghai/Qinghai complete reset-input matrices;
- same-input verdict for each turn-8 coordinate;
- turn-1 to turn-8 propagation assessment;
- turn-154 Zhejiang same-input verdict;
- any same-input fault classification and exact source basis;
- explicitly unsupported hypotheses;
- external artifact paths/sizes/SHA-256;
- `git diff --check` PASS;
- changed paths;
- forbidden-operation audit;
- exactly one execution commit;
- non-force push;
- fresh GitHub read-back;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not auto-start a repair, stationary rerun, shock, IRF, annual batch, R5, or Results task from within this execution.