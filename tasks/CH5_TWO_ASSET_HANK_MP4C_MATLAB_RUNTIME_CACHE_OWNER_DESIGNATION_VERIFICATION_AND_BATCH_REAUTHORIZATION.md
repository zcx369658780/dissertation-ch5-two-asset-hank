# CH5_TWO_ASSET_HANK_MP4C_MATLAB_RUNTIME_CACHE_OWNER_DESIGNATION_VERIFICATION_AND_BATCH_REAUTHORIZATION

Date: 2026-09-02

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / MATLAB-runtime provenance verifier

Owner: final scientific authority and manual batch executor

## 1. Owner scientific designation and task supersession

This task supersedes the still-unexecuted remediation route in:

`tasks/CH5_TWO_ASSET_HANK_MP4C_2022_2023_NEGATIVE_CAPITAL_MATLAB_RUNTIME_PROVENANCE_AUDIT_AND_OWNER_BATCH_REAUTHORIZATION.md`

Previous authority commit:

`d37965357d49f1f7d6ae29e4ca08ecdf0b7822fa`

The Owner has now supplied the missing provenance decision directly:

> The MATLAB multi-province annual steady-state route actually uses the already processed cache `数据估计结果_1000_100_0.mat`, generated by `load_GDPdata.m`, rather than rebuilding the annual scientific runtime inputs from the current workbook on every run.

This Owner statement is a scientific provenance designation. Codex must **verify it against protected MATLAB source and local artifact identity**, not rediscover or replace it with a different data treatment.

Required designation marker:

`OWNER_DESIGNATES_数据估计结果_1000_100_0_MAT_AS_ACTUAL_PROTECTED_MATLAB_RUNTIME_INPUT`

The previous blocker remains factual:

`MP4C_FULL_2009_2023_OWNER_BATCH_BLOCKED_PRE_SCIENCE__PRIMARY_SOURCE_2022_2023_NEGATIVE_CAPITAL__SCIENTIFIC_CALLS_0`

No annual scientific worker was launched in that failed attempt.

The old PowerShell command remains revoked until this task issues a new READY marker.

## 2. Scientific interpretation

The upstream workbooks remain provenance-bearing source data, but for MATLAB-faithful reconstruction the actual annual numerical runtime representation is now Owner-designated as:

`数据估计结果_1000_100_0.mat`

Expected protected artifact SHA-256:

`923CC9E592C14B320C624509A0B498DBCC7D2533F77F0E4B4793521B10849E9A`

This cache must be described as:

`MATLAB_PROTECTED_RUNTIME_DATA_CACHE`

or equivalently explicit wording.

Do **not** relabel it as raw/primary external data. Do **not** hide the current filled-workbook negative CAP values. The provenance chain should remain explicit:

`upstream workbook(s) -> load_GDPdata.m processing -> 数据估计结果_1000_100_0.mat -> MATLAB annual runtime -> Python MATLAB-faithful runtime binding`.

## 3. Verification target

Codex must verify all of the following before patching the Owner-run batch:

1. protected `load_GDPdata.m` contains the cache-save/load route for `数据估计结果_1000_100_0.mat`;
2. when the cache exists, the protected MATLAB annual route actually selects/consumes it;
3. `multi_prov_HANK_12sts.m` / `mpHANK_equilibrium_2000.m` consume the corresponding `mydata2/data_MAT` objects from that route;
4. the local protected MAT file is exactly the expected SHA above;
5. its schema, province order and annual/index structure can be mapped exactly to the already accepted decoupled 2009–2023 calendar contract;
6. the fields needed by the annual stationary initialization are finite/admissible for all 15 years;
7. in particular, the 2022–2023 capital objects actually consumed by MATLAB are finite and positive/admissible even though the current filled workbook contains negative CAP values;
8. no `abs`, clipping, interpolation, manual imputation, formula change or calendar relabeling is required.

If any of 1–7 fails or the protected source contradicts the Owner designation, STOP with:

`MP4C_MATLAB_RUNTIME_CACHE_OWNER_DESIGNATION_VERIFICATION_BLOCKED__SOURCE_OR_ARTIFACT_CONTRADICTION`

Do not patch around a contradiction.

## 4. Required live continuity and local-work preservation

At start:

1. fresh-fetch `origin/main`;
2. require this exact task live on `main` as direct child of `d37965357d49f1f7d6ae29e4ca08ecdf0b7822fa`;
3. require `HEAD == origin/main`, ahead/behind `0/0`;
4. inspect tracked/untracked worktree before modification;
5. the prior full-batch Phase-A scripts were intentionally left uncommitted, so do not require an empty worktree;
6. locate the prior `batch_runner_build_receipt.json` and verify all covered local implementation bytes before modifying them;
7. preserve the failed pre-science output root read-only;
8. verify its annual worker/scientific-call counts remain zero.

Unexplained local edits => STOP.

## 5. Mandatory protected-source reads

Read completely, read-only, under the accepted `C:\MatlabProgram -> D:\MatlabProgram` Junction identity:

- `main.m`
- `main2.m`
- `multi_prov_HANK_12sts.m`
- `load_GDPdata.m`
- `mpHANK_equilibrium_2000.m`
- `HANK_mp_1eq.m`
- `HANK_mp_1turn.m`
- relevant helpers used by `load_GDPdata.m`
- persistence/load statements for `数据估计结果_1000_100_0.mat`

Also read the repository annual input API and current local uncommitted production/batch scripts.

No MATLAB process is authorized.

## 6. Runtime-cache structure and calendar audit

Inspect the protected MAT artifact read-only.

Produce a machine-readable map that proves:

- top-level variable names;
- `mydata2` / `data_MAT` nesting and shapes;
- annual entry count;
- province axis/order;
- field names used for GDP, CAP, POP, `IND_alpha`, `IND_Zt`, and any other annual initialization object;
- exact mapping from accepted calendar year 2009–2023 to cache entry/index;
- exact mapping to accepted regression vintage/index identities where relevant;
- all values consumed by the annual stationary initialization are finite;
- all capital values used as `CAP/Kt0` are positive/admissible.

Do not infer calendar identity from filename arithmetic alone. Cross-check against protected source, workbook year structure, accepted MP4C annual-index authority, and cache contents.

Persist:

`matlab_runtime_cache_structure.json`

`matlab_runtime_cache_calendar_map.json`

## 7. 2022–2023 negative-capital verification

Create an exact 31-province census for 2022 and 2023 comparing:

- current filled-workbook CAP;
- Owner-designated MATLAB runtime-cache CAP;
- fallback workbook CAP if useful for provenance only.

Required columns include at least:

- calendar_year
- province
- filled_workbook_cap
- runtime_cache_cap
- runtime_cache_entry/index
- cache artifact SHA
- sign/finiteness/admissibility flags
- source field path

Persist:

`negative_capital_workbook_vs_runtime_cache_2022_2023.csv`

The raw negative workbook values must remain visible in this evidence.

## 8. Full 2009–2023 workbook-vs-runtime census

Because the Python production batch is being rebound from direct workbook reconstruction to the MATLAB runtime cache, quantify the difference over all 15 years.

For every annual scientific input field that exists in both representations, report exact/max differences by year and field.

At minimum cover:

- GDP
- CAP
- POP
- `IND_alpha`
- `IND_Zt` / initialized Zt where comparable
- GovInv initialization if represented
- inter-province asset ratio if represented

Classify each difference as exact, floating representation scale, or material.

Persist:

`matlab_runtime_cache_vs_workbook_2009_2023_diff.json`

Do not use this comparison to reject the Owner runtime designation merely because the processed cache differs from current workbook values; the purpose is to document the transformation/runtime lineage and protect against accidental mixed representations.

## 9. Local runtime-cache copy authorization

If Sections 3–8 verify the Owner designation, Codex is authorized to byte-copy the protected MAT cache into a new ignored local directory:

`data_local\matlab_runtime_snapshot\数据估计结果_1000_100_0.mat`

Rules:

- copy, never move;
- preserve exact filename;
- source SHA before copy;
- target SHA after copy;
- require exact SHA equality;
- no MAT rewrite/resave/transformation;
- if target exists and differs, STOP; do not overwrite;
- `data_local/` is already ignored; verify exact target is ignored and untracked.

Persist copy provenance in:

`matlab_runtime_cache_copy_manifest.json`

## 10. Python runtime-binding remediation

After verification, patch the local uncommitted production runner to use the Owner-designated MATLAB runtime cache as the actual annual scientific runtime input representation.

Important:

- do not mutate historical accepted `annual.py` semantics solely to force the new production route;
- prefer a separate runtime-cache loader/adapter used by the production runner;
- keep the existing canonical/workbook representation available as provenance and comparison evidence;
- do not mix workbook CAP with cache GDP/POP or other ad hoc field combinations unless protected MATLAB source explicitly does so;
- if protected MATLAB consumes the whole processed `data_MAT/mydata2` annual object, reproduce the whole semantically relevant runtime object rather than a CAP-only overlay;
- bind the cache by exact SHA and exact calendar/province/index contract;
- wrong SHA, missing cache, wrong year/index, wrong province order, wrong field schema => fail closed.

Required representation label in manifests:

`MATLAB_PROTECTED_RUNTIME_DATA_CACHE`

## 11. All-15-year ZERO-SCIENCE preflight

Before issuing a new Owner READY marker, materialize/validate the runtime scientific entry representation for **every calendar year 2009–2023**.

For each year require:

- exact decoupled calendar binding;
- exact cache entry/index mapping;
- exact 31-province order;
- cache SHA exact;
- all runtime scientific entry arrays finite;
- positive/admissible `CAP/Kt0` wherever required;
- finite log-derived values if the Python production path still computes them for diagnostics/provenance;
- regression vintage identity correct;
- fixed source numerical Zt anchor semantics preserved where applicable;
- annual worker launches = 0;
- household/HJB/KFE/stationary calls = 0.

Persist one consolidated:

`all_15_year_matlab_runtime_cache_input_preflight.json`

The task MUST NOT repeat the previous 2009-only test gap.

## 12. 2009 / 2010 / 2011 compatibility accounting

Without running science:

- compare the new cache-bound 2009 runtime input with the input representation used in the formally accepted corrected-2009 chain;
- identify whether the accepted historical runtime-overlay representation already matches the protected cache for any fields;
- compare 2010/2011 new cache-bound inputs with the prior Chunk-01 workbook-canonical inputs;
- list every changed scientific entry field by year/province.

This is input-provenance accounting only. Do not invalidate historical acceptance and do not claim new parity.

## 13. Production runner and scheduler constraints remain frozen

The previous full-batch design remains otherwise unchanged:

- full Owner-run years: 2009–2023 inclusive;
- year-level subprocess parallelism only;
- default `Workers = 4`;
- `OMP_NUM_THREADS=1`;
- `MKL_NUM_THREADS=1`;
- `OPENBLAS_NUM_THREADS=1`;
- `NUMEXPR_NUM_THREADS=1`;
- max outer turns `250` per year;
- max household calls `7750` per year;
- automatic reruns `0`;
- no default wall-clock kill;
- terminal-only normal logging;
- resumable no-overwrite success-marker semantics;
- final steady-state variables, Lt_mat, value-function/distribution/restart checkpoints and timing outputs remain required exactly as in the full-batch task.

Do not change equations, calibration, grid, HJB/KFE numerics, controller rules, convergence rules or persistence requirements.

## 14. ZERO-SCIENCE tests

At minimum test:

- protected cache selection/source contract;
- expected cache SHA;
- cache copy hash equality;
- cache schema parser;
- exact 2009–2023 calendar/cache mapping;
- exact province order;
- full 15-year runtime input materialization;
- positive/admissible 2022/2023 runtime capital values;
- all 15 years finite;
- wrong SHA fails closed;
- wrong cache schema fails closed;
- wrong year mapping fails closed;
- wrong province order fails closed;
- workbook negative CAP remains visible in provenance;
- no mixed-representation fallback;
- terminal-only logging remains no-per-turn;
- restart checkpoint schema remains valid;
- scheduler remains exactly 15 years;
- PowerShell static syntax passes;
- scientific calls remain zero.

`py_compile` all changed/new Python files.

## 15. Fresh remediation evidence root

Preferred fresh no-overwrite root:

`D:\ProjectTemp\ch5-mp4c-runtime-cache-owner-designation-verification-20260902-001`

Persist at minimum:

- `matlab_runtime_cache_structure.json`
- `matlab_runtime_cache_calendar_map.json`
- `negative_capital_workbook_vs_runtime_cache_2022_2023.csv`
- `matlab_runtime_cache_vs_workbook_2009_2023_diff.json`
- `matlab_runtime_cache_copy_manifest.json`
- `all_15_year_matlab_runtime_cache_input_preflight.json`
- `historical_2009_2011_input_compatibility.json`
- `zero_science_test_receipt.json`
- `batch_runner_build_receipt_v2.json`
- `owner_retry_instructions.txt`
- `remediation_manifest.json`

`batch_runner_build_receipt_v2.json` must contain bytes + SHA-256 for every local implementation/test/launcher file the Owner will use.

## 16. Phase-A execution and Git rules

During this verification/remediation phase:

- MATLAB process/model calls: `0`;
- Python annual stationary calls: `0`;
- household/HJB/KFE scientific calls: `0`;
- MP2/MP3 scientific calls: `0`;
- comparator calls: `0`;
- shocks/AR(1)/IRF: `0`.

Do not commit or push the local production implementation yet.

The only GitHub mutation already authorized is this task authority publication.

## 17. READY gate

If and only if protected MATLAB source + exact MAT artifact confirm the Owner designation and all 15-year zero-science preflights pass, issue:

`MP4C_MATLAB_RUNTIME_CACHE_OWNER_DESIGNATION_VERIFIED__FULL_2009_2023_OWNER_BATCH_READY_FOR_RETRY__SCIENTIFIC_CALLS_0`

Then give the Owner:

1. exact fresh PowerShell launch command;
2. exact `batch_runner_build_receipt_v2.json` path;
3. receipt SHA-256;
4. exact local runtime-cache copy path;
5. exact fresh output-root policy;
6. confirmation that the previous failed output root must not be reused.

Codex must STOP after the READY marker. It must not launch the batch.

If verification fails, use the BLOCKED marker from Section 3 and stop without scientific execution.

## 18. Later Phase B

After the Owner manually completes the fresh full batch and explicitly tells Codex the new output root, Codex may re-enter the prior full-batch Phase-B closeout route:

- verify scripts against `batch_runner_build_receipt_v2.json`;
- audit all 15 years read-only;
- no stationary rerun;
- optional separately bounded 2009 comparator-only regression check if eligible;
- generate final report;
- commit/push code/tests/contracts/report only;
- never commit runtime MAT cache, primary workbooks, generated MAT/NPZ/XLSX/CSV batch outputs or large logs.

Do not start shocks, AR(1), IRFs or Results in this task.
