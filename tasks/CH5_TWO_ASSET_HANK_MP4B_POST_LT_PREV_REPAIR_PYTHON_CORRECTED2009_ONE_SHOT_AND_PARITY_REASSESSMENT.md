# CH5_TWO_ASSET_HANK_MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_AND_PARITY_REASSESSMENT

Date: 2026-09-01

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Executor: Codex bounded Builder / one-shot executor / parity diagnostician

Owner: final scientific authority

## 1. Authority basis

Accepted predecessor execution:

`MP4B_FIRM_LT_PREV_SAME_TURN_HOUSEHOLD_MAPPING_REPAIR_AND_ZERO_MODEL_VALIDATION_PASS`

Execution commit:

`216283cf4f1303afa60f1915e143a0b7303a9ffb`

Accepted repaired semantics:

- same-turn `household.household_lt[index]` is mapped to firm reference `Lt_prev` / protected MATLAB `Lt_1` role;
- current firm labor remains `migration.lt_supply[index]`;
- same-turn `AtTax` mapping remains unchanged;
- `firm.py` formulas remain frozen.

The preceding read-only attribution established the repaired mapping as a same-input Python implementation defect, not a model redesign.

Under the Owner's standing delegation, L3 authorizes exactly one Python corrected-calendar-2009 stationary execution to measure the scientific effect of this accepted source-faithful repair. No MATLAB rerun is authorized.

## 2. Required live continuity

Required execution-start predecessor:

`216283cf4f1303afa60f1915e143a0b7303a9ffb`

At execution start:

1. fresh-fetch `origin/main`;
2. require this task live on `main` as the direct child of the required predecessor;
3. require clean worktree, `HEAD == origin/main`, ahead/behind `0/0`;
4. read completely all governing project rules plus:
   - predecessor repair task/report;
   - predecessor turn-1 source-layer attribution task/report;
   - runtime-overlay one-shot task/report;
   - accepted instrumented MATLAB chronology task/report;
   - canonical-binding remediation task/report;
   - current scientific driver, repaired `one_turn.py`, frozen `firm.py`, binding helper, and accepted comparator.

Any authority/identity/readability failure before science => stop with the BLOCKED terminal in Section 11.

## 3. Frozen identities and semantics

Require before science:

- repaired `src/ch5_two_asset_hank/multi_province/one_turn.py` blob: `e5d6835cdc9e6d182e1c84e11f4d51938be592e1`;
- frozen `src/ch5_two_asset_hank/multi_province/firm.py` blob: `1f7d37247e2d712fc0477a9f562dce81d1b367ce`;
- existing `validators/multi_province/mp4b_python_empirical.py` blob: `b1710ae3c5d8d7baf96e85c932d777fa5f3b908c`;
- binding helper blob: `20123513f232cb2d3cca1264565837e4882ea19f`;
- final-state comparator blob: `cbe7ce4e4855c139cc7bb3b20b56d124c4add266`.

Require the repaired line to be present exactly in the firm-source preparation path:

`firm_source["Lt_prev"] = float(household.household_lt[index])`

Require current firm labor still enters `evaluate_firm` through:

`float(migration.lt_supply[index])`.

No equation, parameter, grid, household implementation, migration/capital formula, firm formula, wage rule, controller rule, threshold, tolerance, update order, comparator logic, or binding default may change.

## 4. Hard execution budget

Maximum scientific/model calls:

- MATLAB process/checkcode/stationary/HJB/KFE/household/firm/controller: `0`;
- Python corrected-calendar-2009 stationary top-level: `1`;
- Python stationary rerun: `0`;
- separate household/HJB/KFE replay: `0`;
- standalone MP2/MP3 replay outside the one stationary execution: `0`;
- qualified final-state comparator: maximum `1`;
- comparator rerun: `0`;
- other years / batch: `0`;
- shocks / AR(1) / transition / dynamics / IRF: `0`;
- R5 / Results: `0`.

Once the single stationary process starts, there is no repair-and-rerun lane.

## 5. Validation input

Use the already accepted validation-only exact-runtime input:

`D:\ProjectTemp\ch5-mp4b-python-runtime-overlay-input-20260901-001\runtime_overlay_input.json`

Required SHA-256:

`072E5E943FB6BFF6768CD40001B031C3AF1A6DD92FCC3A86E1B6D476E03E0137`

Its role remains:

`MATLAB_CACHE_RUNTIME_PARITY_OVERLAY`

It is not scientific canonical authority. The immutable primary-source canonical remains SHA-256:

`507D1259A8A515E4224F40D434BD89645C02472650B51F5ADA3181BA4BCDAD48`.

Require overlay provenance/invariants from the accepted binding package before science: 31-province order exact; 24 equal rows; 7 replacement rows; 5x1-ULP and 2x2-ULP; only `initialized_zt` differs from canonical.

The purpose of using the overlay is to isolate the accepted `Lt_prev` repair while keeping turn-1 entry `Zt` bitwise identical to the admissible MATLAB chronology.

## 6. Pre-science gate

Before the one stationary call:

- verify every frozen identity above;
- verify the exact runtime-overlay input SHA and provenance;
- `py_compile` the repaired `one_turn.py`, the immutable scientific driver, and the focused repaired test;
- run only non-scientific focused/regression tests sufficient to re-establish the accepted repair, including:
  - `tests/test_mp2_source_faithful_one_turn.py`
  - `tests/test_mp1_asymmetric_one_turn_fixture.py`
  - `tests/test_mp4b_canonical_input_binding.py`
- require all tests PASS;
- require zero scientific/model calls;
- verify sufficient D-drive free space;
- persist a fresh no-overwrite `pre_science_gate.json` outside Git.

Do not run a household/HJB/KFE smoke.

## 7. Exactly one Python stationary execution

Create one fresh no-overwrite run root, preferred:

`D:\ProjectTemp\ch5-mp4b-post-lt-prev-repair-python-overlay-one-shot-20260901-001`

or next deterministic suffix if it already exists.

Invoke the immutable current scientific driver exactly once with:

1. the accepted `runtime_overlay_input.json`;
2. the fresh run root.

No retry.

If the process fails, preserve all partial evidence and stop scientific execution.

## 8. Required post-run comparisons

### A. Repair-run Python versus pre-repair overlay Python

Pre-repair root:

`D:\ProjectTemp\ch5-mp4b-python-runtime-overlay-one-shot-20260901-001`

Require old terminal SHA-256:

`5C9675702D27FF03A3ADA8959166C757F152B8C9E91126533E557373CB7C74E0`

Report at minimum:

- outer iteration count;
- household call count and final convergence count;
- earliest turn/layer/province changed by the repair;
- turn-1 `mt`, `wjt`, `ra`, `Govinc`, composite wage and next-turn inputs;
- low/high GovInv action counts and coordinates;
- Zt-reset counts and coordinates;
- wage upper/lower and ra upper/lower terminal counts;
- final 31x20 frozen field differences;
- national `Ct/At/Bt/Yt` differences.

This comparison isolates the implementation repair effect.

### B. Repair-run Python versus admissible instrumented MATLAB chronology

MATLAB root:

`D:\ProjectTemp\ch5-mp4b-instrumented-matlab-calendar2009-l3-delegated-rerun-20260831-001`

Require immutable evidence:

- trace MAT SHA-256 `6C4AEC5992A59C1C8D7859DEF082E5711864789CAE009FD1E5CC2B78B3F6A520`;
- final state SHA-256 `D2F4B225AC362A6805AAD1CD065AFA87DA32551833E5FC867BA3A37C9FD0B8F0`;
- baseline admissibility SHA-256 `68C9F396C57D385E72E6A46D94A53F4196F7E9BFBC84A6EAF66AB1FF64A0866B`;
- chronology V5 SHA-256 `A64172CC32EC7295297CED6DFE27B52CE51B5EAD64AD82453C60EBE0152965C3`.

First require turn-1 entry `Zt` 31/31 bitwise exact.

Then determine:

1. whether Beijing turn-1 repaired firm reference `Lt_prev/Lt_1` now matches the protected MATLAB role;
2. whether Beijing turn-1 `mt` now matches MATLAB exactly or within existing representation evidence;
3. earliest remaining continuous difference by source layer;
4. earliest remaining categorical Zt-reset divergence;
5. whether the former turn-8 Shanghai/Qinghai reset exchange remains, disappears, or moves;
6. whether the former turn-154 Zhejiang MATLAB-only low action remains, disappears, or moves;
7. final wage-upper/lower and ra-upper/lower counts;
8. national/final-state mismatch magnitude relative to the pre-repair Python run.

Do not infer causality from chronology alone.

### C. Qualified final-state comparator

If the new Python terminal contains a complete 31-province state, invoke the accepted comparator at most once against the preserved MATLAB final state and terminal-status evidence.

Do not change comparator logic, fields, province projection, tolerances, or output schema.

## 9. Interpretation and classification

Use the strongest supported classification without inventing a new tolerance.

Preferred classifications:

- `MP4B_LT_PREV_REPAIR_TRAJECTORY_PARITY_MATERIAL_IMPROVEMENT`
  - accepted defect is fixed and substantial chronology/final-state mismatch shrinks, but formal stationary parity is not yet established;

- `MP4B_LT_PREV_REPAIR_IMPLEMENTED__LATER_DIVERGENCE_PERSISTS`
  - repaired source layer behaves correctly but material downstream divergence remains;

- `MP4B_LT_PREV_REPAIR_CORRECTED2009_STATIONARY_PARITY_CANDIDATE__FORMAL_L3_ACCEPTANCE_REQUIRED`
  - only if all existing frozen categorical/source/input/final comparison gates support a parity candidate without tolerance alteration;

- `MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_BLOCKED_NO_RERUN`
  - on preflight or scientific-execution failure.

Do not claim Results acceptance, multi-year parity, AR(1), dynamics, or paper-result validity from this one year.

## 10. Required external artifacts

Use a fresh no-overwrite diagnostic root, preferably:

`D:\ProjectTemp\ch5-mp4b-post-lt-prev-repair-parity-reassessment-20260901-001`

Persist at minimum:

- `pre_science_gate.json`;
- `repair_run_vs_prerepair_overlay.json`;
- `repair_run_vs_matlab_chronology.json`;
- `turn1_beijing_repaired_source_chain.json`;
- `controller_branch_disposition.json`;
- `qualified_final_state_comparison.json` if eligible;
- `scientific_call_ledger.json`;
- `diagnostic_manifest.json`.

Record SHA-256 and byte size for every artifact.

## 11. Terminal and repository report

PASS terminal:

`MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_AND_PARITY_REASSESSMENT_PASS`

BLOCKED terminal:

`MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_BLOCKED_NO_RERUN`

Required repository report:

`docs/CH5_TWO_ASSET_HANK_MP4B_POST_LT_PREV_REPAIR_PYTHON_CORRECTED2009_ONE_SHOT_AND_PARITY_REASSESSMENT_REPORT.md`

Repository mutation must be the report only. Do not modify source, tests, validators, rules, task indexes, thresholds, tolerances, MATLAB, or canonical/binding artifacts.

Before the execution commit:

- explicit-path staging of the report only;
- `git diff --check --cached` PASS.

Closeout:

- exactly one execution commit;
- exactly one non-force push;
- fresh GitHub read-back of the report;
- `HEAD == origin/main`;
- ahead/behind `0/0`;
- clean worktree;
- exactly one recommended next gate.

Do not auto-start another stationary run, other year, multi-year batch, threshold sensitivity, AR(1), shock, transition/IRF, R5, or Results task from inside this execution.
