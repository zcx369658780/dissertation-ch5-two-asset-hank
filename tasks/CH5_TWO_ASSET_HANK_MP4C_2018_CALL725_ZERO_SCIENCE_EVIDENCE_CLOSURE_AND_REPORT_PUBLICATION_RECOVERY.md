# CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_ZERO_SCIENCE_EVIDENCE_CLOSURE_AND_REPORT_PUBLICATION_RECOVERY

Date: 2026-09-04

Repository: `zcx369658780/dissertation-ch5-two-asset-hank`

Issuer: ChatGPT L3 independent reviewer / scientific route authority / GitHub task issuer

Builder: Codex bounded Builder / evidence-closure auditor

Owner: final scientific authority

## 1. Purpose and authority boundary

Immediate live parent at task publication:

`9463531cbd0091326b7b736a6f0045ad6ef9cc79`

Predecessor live task:

`tasks/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION.md`

Predecessor task blob:

`fdb9f2cd0d46ca005fbf3d4abe1d4f6160d1e49f`

The predecessor execution has already consumed its authorized scientific budget and has reached a provisional forensic result, but publication is blocked because the Section 13 evidence-closure requirements were not fully durably satisfied.

This successor task authorizes only **zero-science evidence closure, audit completion, and final report publication recovery**.

It does **not** authorize any new scientific model evaluation.

The purpose is to determine whether the already-existing durable evidence can satisfy the predecessor task's evidence contract without rerunning MATLAB, Python, HJB, KFE, GE, or any downstream model object.

## 2. Start gate

At start:

1. verify repository identity and remote;
2. `git fetch origin`;
3. record fresh `origin/main`;
4. require this task to be present on live `origin/main` as the direct child of `9463531cbd0091326b7b736a6f0045ad6ef9cc79`;
5. require local `HEAD == origin/main`, ahead/behind `0/0` before repository mutation;
6. inspect tracked and untracked worktree state before touching anything;
7. read `AGENTS.md` and all CURRENT project rules;
8. read this task;
9. read the predecessor task in full;
10. inspect the existing predecessor evidence root and the existing uncommitted candidate report before creating any new artifact.

If live authority, task identity, parentage, or required local evidence differs materially, STOP.

## 3. Frozen scientific call ledger

No scientific call budget is opened by this task.

Recover and preserve the existing durable ledger. The expected already-consumed state reported at handoff is:

- MATLAB exact-MAT/common-scalar iteration-1 scientific call: `1`;
- Python exact-MAT/common-scalar iteration-1 scientific call: `1`;
- Python native-init amplification probe: `0`;
- retries: `0`;
- iteration 2+: `0`;
- full HJB100/HJB500: `0`;
- KFE: `0`;
- household steady state: `0`;
- GE/stationary/annual: `0`;
- R/PLM: `0`;
- shock/IRF: `0`;
- Results: `0`.

These counts are not to be recreated from chat. Verify them from durable local evidence and preserve the verified ledger.

Any attempt to execute a new scientific MATLAB/Python model call under this task is forbidden.

## 4. Existing scientific result is provisional until evidence closure

The handoff reports that the existing durable stagewise comparator identifies the earliest material scientific divergence at raw liquid-asset derivatives:

- `vb_f`: 40 material entries, reported maximum absolute difference `0.01228045195367922`;
- `vb_b`: 40 material entries, reported maximum absolute difference `0.012844718557132808`.

This information is **provisional** until re-read directly from the existing durable comparator and bound to the closure evidence.

Do not recompute the model.

Permitted:

- read existing MATLAB/Python stage artifacts;
- re-run pure comparison scripts only if they consume already-persisted stage artifacts and perform no model evaluation;
- independently recompute comparison statistics from persisted arrays/files;
- confirm the earliest material stage from existing artifacts.

Forbidden:

- regenerate derivatives from the HJB evaluator;
- rerun wrappers in scientific mode;
- rerun direct solves;
- change formulas, tolerances, ordering, parameters, grids, or solver semantics;
- repair `vb_f`/`vb_b` under this task.

If existing durable evidence does not independently support the provisional earliest divergence, classify the task as blocked and do not publish a completed scientific classification.

## 5. Existing evidence root and fresh closure root

Existing predecessor evidence root, read-only under this successor:

`D:\ProjectTemp\ch5-mp4c-2018-call725-postcall-residual-vectorization-repair-20260904-001`

Do not overwrite, delete, rename, or mutate any file in that root.

Create a fresh no-overwrite closure root:

`D:\ProjectTemp\ch5-mp4c-2018-call725-zero-science-evidence-closure-20260904-001`

If that path already exists, fail closed and use a new monotonically suffixed no-overwrite path, recording the exact path.

The fresh closure root must contain only closure/audit artifacts. Do not place tool caches, bytecode caches, temporary editor files, or unrelated logs there.

## 6. Required zero-science evidence recovery

Complete all of the following using only file reads, hashes, static parsing, diffing, serialization checks, and comparisons of already-persisted objects.

### 6.1 Wrapper diff receipt

Recover the exact predecessor and repaired external wrapper sources referenced by the predecessor task/evidence.

Produce an exact line-level diff for MATLAB and Python wrappers.

Every changed line must be classified as one of:

- `POST_CALL_INSTRUMENTATION_OR_PERSISTENCE_ONLY`;
- `NON_SCIENTIFIC_DEFENSIVE_ASSERTION_ONLY`.

No changed line may alter scientific derivatives, boundary logic, policy/FOC logic, drifts, sparse operators, `M`, RHS, direct solver semantics, state ordering, economic parameters, grid values, or scientific arithmetic.

If any scientific expression changed, STOP with:

`MP4C_2018_CALL725_ZERO_SCIENCE_EVIDENCE_CLOSURE_BLOCKED__SCIENTIFIC_WRAPPER_CHANGE_DETECTED__NO_RERUN`.

### 6.2 Exact-MAT identity receipt

Re-hash the authoritative HJB100 initialization MAT file using the already-frozen file identity:

`1718984CB588AE586F74AB8476C57AF849BB2C80CC95500329D29BC14207BB81`

Verify that the existing strict-common wrappers/evidence direct-reference the authoritative `b`, `ah`, `z`, `v0`, and `l0` fields.

Do not reconstruct those arrays from JSON, decimal text, linspace, or helper generators.

Loading the MAT only for field/hash inspection is authorized and counts as zero-science. Do not invoke HJB/model evaluation while doing so.

Use the same canonical array-hash convention already established by predecessor evidence if available. Do not invent a new hash convention merely to make a comparison pass.

### 6.3 Scalar-binding equality receipt

Re-read the predecessor-certified scalar binding:

`call725_first_iteration_scalar_binding.json`

Expected SHA-256 from predecessor authority:

`A40D088C63FC1F7EDECEA561D649B42959C646DF528ED13298014493DB4808F6`

Verify exact equality of every frozen scalar/matrix consumed by the strict pair against the predecessor task contract, including province/call provenance, `rah`, `rb`, `rb_gap`, `tau`, `w`, `Tt`, switch matrix, `rho`, `gamma`, `phi/frisch_l`, `chi0`, `chi1`, `a_bar`, `Delta`, `crit`, and drift tolerance.

This is a static JSON/value audit only. No model call is authorized.

### 6.4 Scientific-expression identity receipt

Using exact source text/diffs only, prove that the repaired wrappers' scientific evaluator expressions are identical to the predecessor-certified evaluator expressions for all stages through the direct solve.

The receipt must identify the compared source files/hashes and the exact source regions or normalized-expression method used.

Do not execute the evaluator to establish identity.

### 6.5 Existing stage-artifact integrity receipt

Locate and re-hash/read back the already-persisted MATLAB and Python strict-common stage artifacts and the stagewise comparator outputs.

Verify that they are the artifacts produced by the already-consumed scientific calls, not regenerated objects.

Bind the provisional `vb_f`/`vb_b` earliest-divergence finding to those hashes.

No model rerun is permitted.

## 7. Complete inventory and finite manifest-closure protocol

The predecessor task required every current evidence artifact to be hashed and read back. This successor defines the finite, non-self-referential closure method explicitly.

### 7.1 Existing-root full file inventory

Create `source_root_full_inventory.json` in the fresh closure root.

It must recursively enumerate **every regular file** currently present in the existing predecessor evidence root at snapshot time, including:

- prior `audit_manifest.json` files;
- prior readback receipts;
- comparator outputs;
- wrapper sources;
- MAT/JSON/CSV evidence;
- logs;
- cache files if any are physically present;
- any other regular file.

For every file record:

- relative path;
- byte size;
- SHA-256;
- readback success;
- classification: `SCIENTIFIC_EVIDENCE`, `ZERO_SCIENCE_CERTIFICATION`, `AUDIT_METADATA`, `TRANSIENT_CACHE`, or `OTHER`;
- whether it is required for the final scientific classification.

Do not delete or ignore cache files merely because they are transient. Inventory them and classify them.

### 7.2 Closure-root manifest

Create `closure_audit_manifest.json` only after all substantive closure artifacts are finalized.

It must hash and read back every substantive artifact in the fresh closure root **except itself and the final detached readback receipt described below**.

This exclusion is required to avoid an impossible self-referential hash requirement.

### 7.3 Detached manifest readback receipt

After `closure_audit_manifest.json` is finalized:

1. hash it;
2. read it back and verify parse/content integrity;
3. create `closure_manifest_readback_receipt.json` containing its SHA-256, size, readback result, entry count, and closure-root path.

Do not modify `closure_audit_manifest.json` after this receipt is written.

The detached readback receipt is intentionally not required to be listed inside the manifest it certifies. This is the finite terminal closure boundary.

The final repository report must record SHA-256 values for both:

- `closure_audit_manifest.json`;
- `closure_manifest_readback_receipt.json`.

No further recursive self-hashing layer is required.

## 8. Acceptance gate for the scientific classification

A completed predecessor scientific classification may be published only if all of the following hold simultaneously:

1. live GitHub authority continuity passes;
2. frozen scientific call ledger is durably verified;
3. wrapper diff receipt proves no scientific wrapper change;
4. exact-MAT identity receipt passes;
5. scalar-binding equality receipt passes;
6. scientific-expression identity receipt passes;
7. existing stage artifacts are hash/readback bound to the already-consumed calls;
8. stagewise comparison independently confirms the earliest material scientific stage from persisted artifacts;
9. full existing-root inventory is complete;
10. closure manifest and detached readback receipt pass.

If all ten conditions pass and the persisted comparison still establishes raw `vb_f`/`vb_b` as the earliest material scientific divergence, use the predecessor-authorized strongest classification:

`CALL725_FIRST_ITERATION_PRE_SOLVE_SOURCE_PARITY_BREAK_CONFIRMED__EARLIEST_STAGE_IDENTIFIED__NO_PRODUCTION_CHANGE`

Do not broaden that classification into a cause claim beyond what the persisted evidence proves.

If any condition fails or remains ambiguous, use a blocked terminal and explicitly identify the missing evidence. Do not rerun science to fill the gap.

## 9. Repository report publication boundary

The only repository scientific/report file authorized for mutation is:

`docs/CH5_TWO_ASSET_HANK_MP4C_2018_CALL725_POST_CALL_RESIDUAL_VECTORIZATION_REPAIR_AND_FIRST_ITERATION_STAGEWISE_REEXECUTION_REPORT.md`

An existing local untracked/unstaged candidate at that exact path may be edited/replaced to reflect the final evidence-closure result.

No other `docs/`, `src/`, `validators/`, `exports/`, MATLAB, Python scientific source, tests, parameters, or task files may be changed by the Builder under this task.

If the acceptance gate passes, a report-only commit and non-force push are authorized.

Suggested commit:

`Close MP4C call-725 first-iteration evidence and publish forensic report`

If the gate remains blocked, do not commit a false completed report. Preserve the candidate locally and return the blocker.

## 10. Post-publication verification

After any authorized report-only push:

1. `git fetch origin`;
2. record fresh `origin/main`;
3. require `HEAD == origin/main`;
4. require ahead/behind `0/0`;
5. require tracked worktree clean;
6. report the exact report Git blob and commit SHA;
7. STOP and return to ChatGPT L3 / Owner.

## 11. Forbidden actions

This task does not authorize:

- any new MATLAB scientific invocation;
- any new Python scientific invocation;
- any native-init probe;
- any retry of predecessor scientific calls;
- derivative recomputation from the evaluator;
- production repair;
- protected MATLAB mutation;
- Python scientific-source mutation;
- tolerance broadening;
- parameter/grid/solver/controller change;
- iteration 2+;
- full HJB rerun;
- KFE;
- household steady state;
- GE/stationary/annual run;
- R/PLM;
- shock/IRF;
- Results work;
- successor task creation by Codex.

## 12. Terminal markers

If evidence closure succeeds and the report is published consistently:

`MP4C_2018_CALL725_ZERO_SCIENCE_EVIDENCE_CLOSURE_COMPLETE__PRE_SOLVE_DERIVATIVE_BREAK_DURABLY_CERTIFIED__NO_RERUN_NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

If closure cannot be completed from existing durable evidence:

`MP4C_2018_CALL725_ZERO_SCIENCE_EVIDENCE_CLOSURE_BLOCKED__EXISTING_EVIDENCE_INSUFFICIENT__NO_RERUN_NO_KFE_NO_GE_NO_PRODUCTION_CHANGE`

STOP after either terminal and return to ChatGPT L3 / Owner.